---
id: 0.17.4-W1-001
title: 實作 Ticket Create 清單式欄位驗證（PROP-009 面向 B）
type: IMP
status: completed
version: 0.17.4
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:實作
  final_decision: PROP-009 P0 優先項
  rationale: 每次 create 都受益，減少交接返工
who:
  current: pending
  history: {}
what: 實作 Ticket Create 清單式欄位驗證（PROP-009 面向 B）
when: create 命令執行時，寫入檔案前
where: .claude/skills/ticket/ticket_system/commands/create.py
why: Create 後 5W1H 有待定義、交接才發現遺漏，需在 create 時強制驗證必填欄位（PROP-009）
how: 在 _persist_and_report 的持久化步驟前新增 _validate_checklist 函式，檢查 5 項清單（why/where/acceptance/decision_tree/when），輸出
  WARNING 但不阻擋（why 已有既有阻擋邏輯）
acceptance:
- '[x] create 時自動檢查 5 項清單（why/where/acceptance/decision_tree/when）'
- '[x] 缺失項輸出 WARNING 列出具體缺失欄位名'
- '[x] IMP/ANA/ADJ 的 why 欄位阻擋（已有），其餘為警告不阻擋'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-11T18:14:18'
completed_at: '2026-04-11T18:22:01'
created: '2026-04-11'
updated: '2026-04-11'
---

# Execution Log

## Task Summary

實作 Ticket Create 清單式欄位驗證（PROP-009 面向 B）

---

## Problem Analysis

### Context Bundle

**目標**：在 create 命令持久化前新增清單式欄位驗證。

**現有架構分析**：
- `execute()` (L810) 是主進入點，協調 3 步驟
- Step 2: `_parse_cli_args_to_config()` (L522) 解析 CLI → TicketConfig dict
  - why 阻擋驗證已在此處 (L581-586)，DOC 豁免
- Step 3: `_persist_and_report()` (L760) → `_validate_before_persist()` → `_build_and_save_ticket()`
  - `_validate_before_persist()` (L613) 目前只做 blockedBy 驗證和重複偵測

**實作位置**：在 `_persist_and_report()` 中，步驟 1（驗證）之後、步驟 2（持久化）之前新增清單驗證。

**PROP-009 清單 5 項**：
1. why 已填寫（IMP/ANA/ADJ）→ 已有阻擋，不需重複
2. where.files >= 1 → 警告：`config['where_files'][:]`
3. acceptance >= 1 → 警告：`config['acceptance']`
4. decision_tree_path 已填 → 警告：`config['decision_tree_path']` 各欄位非空
5. when 非「待定義」→ 警告：`config['when'] != DEFAULT_UNDEFINED_VALUE`

**WARNING 輸出格式**：使用現有 `format_warning()` + 列出缺失欄位清單。

**不動的東西**：
- why 阻擋邏輯不動（已在 _parse_cli_args_to_config）
- _print_create_checklist 不動（事後檢查清單，不同職責）

**測試策略**：
- 新增 test_create_checklist_validation.py
- 測試 5 項清單各自的 WARNING 輸出
- 測試全部通過時無 WARNING
- 測試 why 阻擋仍正常（回歸）
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending