---
id: 1.5.0-W6-030
title: 修正 E2E 測試遷移（uc04/uc07 靜態按鈕 id 不再對應可互動元素）
type: IMP
status: in_progress
version: 1.5.0
wave: 6
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.5.0-W6-022
dispatch_reason: ''
decision_tree_path:
  entry_point: ANA 落地
  final_decision: SR-1 spawn request 落地
  rationale: quality-baseline-rule-5
who:
  current: dill-javascript-developer
  history: {}
what: 修正 E2E 測試遷移（uc04/uc07 靜態按鈕 id 不再對應可互動元素）
when: 後續 Wave 容量排入
where:
  layer: 待定義
  files:
  - tests/e2e/browser/uc04-import.e2e.test.js
  - tests/e2e/browser/uc07-error-handling.e2e.test.js
why: W6-022 遷移後靜態 Modal A/B 按鈕 id 不再對應可互動元素，E2E 測試需改用動態查詢
how:
  task_type: Implementation
  strategy: E2E 測試改用與 unit 測試相同的 getOpenDialog/getDialogButtons helper 或等價 Puppeteer
    查詢
acceptance:
- '[ ] uc04-import.e2e.test.js 動態查詢 createDialog 產出的按鈕'
- '[ ] uc07-error-handling.e2e.test.js 同步適配'
- '[ ] npm run test:comprehensive 全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-13T12:46:49'
completed_at: null
created: '2026-07-13'
updated: '2026-07-13'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

修正 E2E 測試遷移（uc04/uc07 靜態按鈕 id 不再對應可互動元素）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

W6-022 遷移 promptImportMode 到 createDialog 後，靜態 HTML 元素行為改變：

| 元素 | 遷移前 | 遷移後 |
|------|--------|--------|
| `#importModeOverlay` | 被設為 `display:block` 顯示 | 永久 `display:none`（存在性防禦錨點） |
| `#importModeOverwriteBtn` / `#importModeMergeBtn` | 接受 click 事件 | 無事件監聽（不互動） |
| createDialog 動態 overlay | 不存在 | `div.modal-overlay`（無 ID），`display:flex` 時可見 |
| createDialog 動態按鈕 | 不存在 | `button.button.primary`（無 ID），文字 = action.text |

E2E 測試仍用靜態 ID 選擇器（`#importModeOverlay` / `#importModeOverwriteBtn`），會永遠等待不到可見元素而 timeout。

### 影響範圍

| 檔案 | 受影響函式 | 受影響行 |
|------|-----------|---------|
| `tests/e2e/browser/uc04-import.e2e.test.js` | `importFixture()` | L208-214（步驟 4-5） |
| `tests/e2e/browser/uc07-error-handling.e2e.test.js` | `injectImport()` | L358-366（modal 等待+點擊） |

### 修復策略

改用動態查詢 createDialog 產出的 DOM：

1. **等待 overlay 出現**：`page.waitForFunction` 檢測 `.modal-overlay` 元素 `style.display === 'flex'`（createDialog open() 設定的 inline style）
2. **按文字找按鈕並點擊**：`page.evaluate` 在可見 overlay 內找 `button` 元素，以 `textContent` 匹配
   - `mode === 'overwrite'` → 文字 `'覆蓋（清空現有書庫）'`
   - `mode === 'merge'` → 文字 `'合併（保留現有書庫）'`
3. **等待 overlay 消失**：`page.waitForFunction` 確認無可見 `.modal-overlay`

### 相關 Error Pattern

無直接對應。本問題為 W6-022 遷移的預期後續工作（source_ticket spawn）。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: dill-javascript-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.5.0-W6-022 | chars: 388 -->

### Task Reference
- 1.5.0-W6-022 what: 實作 createDialog 契約元件（confirm/alert 二 variant）並收斂 UC-04 兩個手刻 modal 與 import-panel window.confirm

### Rationale Chain
- 1.5.0-W6-022 why: 契約 §14.6 標 AppDialog.confirm/.alert ↔ createDialog v1 待建；v1 現有 3 個對話消費點（UC-04 Modal A/B 手刻 DOM、import-panel 原生 window.confirm）各自實作，收斂後對齊契約並消除重複

### Related Files
- src/core/ui/components/ui-factory.js  # from 1.5.0-W6-022
- src/overview/import-flow-controller.js  # from 1.5.0-W6-022
- src/overview/overview-page-controller.js  # from 1.5.0-W6-022
- src/popup/components/import-panel.js  # from 1.5.0-W6-022
