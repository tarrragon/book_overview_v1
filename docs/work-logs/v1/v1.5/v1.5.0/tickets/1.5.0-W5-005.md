---
id: 1.5.0-W5-005
title: 檢討 ticket 系統設計缺失與改進方向（process 模型回顧）
type: ANA
status: in_progress
version: 1.5.0
wave: 5
priority: P2
parent_id: null
children:
- 1.5.0-W5-005.1
- 1.5.0-W5-005.2
- 1.5.0-W5-005.3
- 1.5.0-W5-005.4
- 1.5.0-W5-005.5
- 1.5.0-W5-005.6
- 1.5.0-W5-005.7
- 1.5.0-W5-005.8
- 1.5.0-W5-005.9
- 1.5.0-W5-005.10
- 1.5.0-W5-005.11
- 1.5.0-W5-005.12
- 1.5.0-W5-005.13
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:分析（PM 前台）
  final_decision: 用戶要求 ticket 系統設計回顧：PM 前台量化普查 + 質性盤點 + WRAP 分析，結論交 parallel-evaluation
    審查
  rationale: wrap-mandatory-for-analysis + quality-baseline-rule-5
who:
  current: rosemary-project-manager
  history: {}
what: 檢討 ticket 系統設計缺失與改進方向（process 模型回顧）
when: 用戶指示啟動（sync-pull 收尾後）
where:
  layer: 待定義
  files:
  - docs/work-logs
  - .claude/error-patterns
  - .claude/skills/ticket
why: ticket 系統上線數月，PC error-pattern 與 hook 防禦層持續增生，需回顧資料模型層設計缺失，判斷是繼續事後補丁還是修正底層設計
how:
  task_type: Implementation
  strategy: 量化普查（find/grep ticket 語料庫：總量/狀態/類型/深度/欄位使用率/placeholder 殘留）→ 質性盤點（error-patterns
    + memory feedback 分類）→ Linux process 模型逐項對照（類比成立處/斷裂處）→ WRAP 檢核 → 改進方向逐項 spawn
    規劃
acceptance:
- '[x] 完成 ticket 語料庫量化普查（總量/狀態/類型/深度分佈/欄位使用率/placeholder 殘留），資料記錄於重現實驗結果'
- '[x] 完成質性證據盤點：ticket 系統相關 error-patterns 與使用摩擦分類統計'
- '[x] Solution 以 process 模型對照組織：類比成立處 / 類比斷裂處（設計缺失，每項附實證來源）/ 改進方向'
- '[x] 改進方向逐項標註 spawn 規劃（建 ticket 或顯性豁免理由），符合 quality-baseline 規則 5 ANA spawn 落地'
- '[x] WRAP 四步檢核記錄於 Solution'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-07-04T13:58:11'
completed_at: null
created: '2026-07-04'
updated: '2026-07-04'
---

# Execution Log

## Task Summary

檢討 ticket 系統設計缺失與改進方向（process 模型回顧）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 分析範圍與方法

**問題**：ticket 系統以 Linux process 模型設計（ticket=process 承載資訊、ticket ID=PID、派發 agent=多線程、以 ID 為記錄核心），使用數月後需以實證回顧設計缺失。

**證據基礎（三源）**：
1. 量化：2285 張歷史 ticket 語料庫全量 grep 普查（見重現實驗結果）
2. 質性：289 個 error-pattern（PC 200 + IMP 89）、數月累積的 memory feedback（約 90 條，其中 ticket 系統相關約 40 條）
3. 結構：ticket CLI（39 子命令）、25 個 ticket skill hooks、settings.json 80 註冊 hooks

**方法**：Linux process 模型逐項對照（成立處/斷裂處）→ 每項缺失錨定實證 → WRAP 檢核關鍵判讀 → 改進方向 spawn 規劃。

## 重現實驗結果
## ticket 語料庫量化普查（2026-07-04，find/grep 全量掃描）

### 語料庫規模
- ticket 總數：**2285**（docs/work-logs/**/tickets/*.md）
- 版本分佈 top5：v0.18.0=985（43%）、v0.19.0=389、v1.0.0=291、v0.17.2=105、v0.19.1=63
- 深度分佈（檔名尾段層級近似）：1層=1533（67%）、2層=627（27%）、3層=102、**4層=18、5層=5**（超出 MAX_TICKET_DEPTH=3 的長尾）

### 生命週期狀態（注意量測混雜，見 caveat a）
- frontmatter `status: completed`（grep -l 檔案級）= **2190**；closed=68、pending=8、blocked=5、skipped=6
- 舊版本目錄（v0*）pending 殘留 = **0**（stale 清理 SOP 有效）
- body Exit Status 章節之 `status:` 行：success 約 1242+141、partial_success=24、needs_context=7

### type 分佈與枚舉漂移
- IMP=1217、ANA=527、DOC=487、ADJ=31
- **漂移尾巴**：test-design=3、INV=3、analysis=2、strategy=1、fix=1、design-spec=1（計 11 筆非正典 type）

### 欄位使用率
- `parent_id: null`=1526（67%）；巨型父票 top：0.18.0-W10-017=18 子、W17-008=16 子、W10-074=14 子、1.0.0-W8-020=13 子
- `blockedBy: []`=**1960（86%）**；非空（多行）=301（13%）
- `priority`：P2=1628（**71%，預設值主導**）、P1=361、P3=197、P0=55；**漂移**：P9=6、P4=4、中=5、高=2（計 17 筆非法值）
- `spawned_tickets: []`=1850（81%）；非空=408+3（含漂移值「無」1 筆、inline list 2 筆）
- `relatedTo: []`=2064（90%）

### 身份回填缺口
- completed 且 `who.current: pending`（身份從未回填）= **376 / 2190 = 17%**

### body 章節使用頻率
- 模板五章節近全量：Solution=2260、Test Results=2258、Problem Analysis=2257、Task Summary=2254、Completion Info=2254
- 後期協議章節：Exit Status=1378（60%）、NeedsContext=1377、Context Bundle=751（33%）、重現實驗結果=454（ANA 527 之 86%）
- **章節名漂移尾巴**：實作摘要=20、測試結果=19（Test Results 中文變體）、multi_view_status=17、變更摘要=11、WRAP 分析=9、修正內容=8

### error-pattern 與防禦層規模
- PC error-pattern=200、IMP error-pattern=89（合計 289）
- 內文提及 ticket 的 PC = **173/200（87%）**（提及密度，非因果歸因）
- hook 總量：settings.json 註冊 80、可執行檔 249；ticket skill 自有 hooks=25；ticket CLI 子命令約 39

### 量測 caveats
- (a) `^status:` grep 同時命中 frontmatter 生命週期與 body Exit Status 兩層——**同名鍵跨層衝突本身即為發現 F9**
- (b) 深度以檔名小數點近似 parent_id 鏈，兩者可能少量偏差
- (c) 「提及 ticket 的 PC=87%」為提及密度；ticket 為框架核心名詞，提及不等於肇因
- (d) 模板五章節近全量含 create 時自動蓋章的空白章節，非全為實質填寫

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### WRAP 檢核

**錨點**：流程決策——ticket 系統是框架骨幹，改善具持續回報；框架修改優先於專案進度（用戶既定原則）。
**Step 0**：資料充足（2285 票普查 + 289 error-pattern + 數月 memory）；已標記 4 項量測 caveat。
**W（假設層級多元）**：對「事故密度高」列 6 個候選根因假設——H1 單檔職責過載 / H2 寫入介面未收斂 / H3 類比對象錯位 / H4 枚舉無強制 / H5 三平面無單一事實來源 / H6 反向假設：非設計缺失而是 LLM agent 特性必然成本。方案性質三類涵蓋確認：改進方向含新增工具（D7）、改造既有（D1/D2/D4/D5）、零工具純文件（D8）與顯性不動（D6 拆檔豁免）。
**R（現實檢驗）**：基本率——成熟 tracker（Jira/Linear/GitHub）= DB + API 唯一寫入；OS kernel = 單一寫入者 + syscall；本系統 = 可自由 Edit 的平面檔案 + 事後 hook 驗證，屬離群設計。反向基本率——git-based tracker（git-bug/fossil）與 md-based PM 確實存在，以弱完整性換版本化與人類可讀性，故儲存選型可辯護，缺口在寫入紀律而非儲存介質。
**A（拉開距離 + Consider the Opposite 修正）**：初判「hook 增生 85 個是病」，反向思考後修正——hook 增生是規則 6（失敗案例學習）的制度化記憶，屬資產；真正的病是**同一類錯誤跨欄位重複發生**（枚舉漂移在 type/priority/status/章節名四處各自發生一次），表示缺的是一個寫入時驗證機制，而非 200 個事後 pattern。10/10/10：不修寫入邊界，hook 數與 PC 數將隨欄位數線性增生。
**P（準備犯錯）**：本分析最大風險 = 建議大改重寫一個運作中的系統（2190 completed、舊版 pending 歸零證明系統整體有效）。防護：所有改進方向設計為增量式、各自獨立可驗證、不含 big-bang 重構。次風險 = 普查為 grep 近似（caveat a-d 已標）。

---

### 一、類比成立處（保留資產，不動）

| # | 資產 | 實證 |
|---|------|------|
| A1 | ticket ID 作為 PID：全域穩定錨點 | 2285 票跨 commit/dispatch/hook/error-pattern 全以 ID 關聯，路由成功 |
| A2 | claim/complete 生命週期狀態機 | completed 2190 + 舊版 pending 歸零，回收機制最終有效 |
| A3 | runqueue/dashboard 排程器 | 接手流程 7 tool call 降至 2-3（W10-113 量測） |
| A4 | Wave 作為 batch cohort | 版本-波次結構承載了 86% 未填 blockedBy 的隱式排序 |
| A5 | handoff 純指針 context switch | 成熟後（--next 絕對指向）運作穩定 |

### 二、設計缺失（F1-F9，每項附實證）

### F1【最高層級】類比對象錯位：ticket 是 batch job 不是 process；agent 是 thread 不是 process
- Linux process：fork 即繼承身份（UID）、位址空間預設隔離。
- 本系統實況：身份在 claim 時晚綁定（**17% completed 票身份從未回填**；who.current 預設字面 "pending" 引發 identity-guard deny 鏈 → W2-018 claim --as 補丁）；agent 預設共享 working tree（執行緒模型），隔離（worktree/feat branch）是 opt-in 且昂貴。
- 事故聚集正好在兩個預設反轉點：共享樹競態（PC-092 commit race、PC-137 並行 Edit deny、PM Edit 期間 agent 越界 commit）、worktree stale base（IMP-066）。
- 正確類比是 SLURM/k8s Job：submit/schedule/assign/execute 分離，身份在 assign 綁定，工作區預設隔離。系統演化實際上一直朝此方向補丁（claim --as、feat branch 三句 SOP、dispatch-active.json），但自我描述仍是 process 模型，導致每次補丁都被當個案而非模型修正。

### F2 單檔五職責融合（descriptor + context + journal + contract + audit）
- Linux PCB 是固定 schema 的 kernel struct；ticket md 是無界文件。
- 單一 write surface 由 PM/agent/hook 三方共寫 → auto-stage 同檔 merge conflict（W1-048.2）、body 未 commit 被 git 還原（W7-001 auto-commit 補丁的起因）、file-size guardian 37 檔超標。
- 反向 steelman（保留判斷）：對 LLM agent，單檔一次 Read 取得全上下文是**優勢**，PCB+address space 融合是 feature。故 F2 的解不是拆檔，是收斂寫入介面（見 D6 豁免）。

### F3【根因核心】無 kernel：寫入路徑未收斂，一致性靠事後 hook 免疫系統
- 枚舉漂移四處實證（同一類錯誤重複發生）：type 漂移 11 筆（test-design/INV/analysis...）、priority 漂移 17 筆（P9/P4/中/高）、spawned_tickets 漂移（「無」/inline）、章節名漂移 77 筆（測試結果/實作摘要/變更摘要...）。
- priority 無 CLI 命令 → 71% 停在預設 P2，排程器主排序鍵無人維護（F5 併入此因）。
- 上游剛確診同病：PC-MON-002「必填不等於有效——CLI 必填欄位無格式/存在性驗證」。
- Linux 對照：kernel 是唯一寫入者，invariant 由 syscall 邊界強制；本系統 CLI 只覆蓋部分欄位，直接 Edit frontmatter 可繞過一切。

### F4 依賴三重編碼：Wave 序 + When 散文 + blockedBy 彼此漂移
- blockedBy 空值率 86%（1960/2285），依賴實際藏在 Wave 編號與 When 散文。
- 已有實證：blockedBy CLI 滿足 ≠ When 語意前提滿足（memory feedback）；runqueue 的答案品質受限於 DAG 編碼覆蓋率。

### F6 記錄平面 vs 世界平面雙面帳（ticket status 是第三平面）
- ghost branch、並行 session race（W1-024）、agent 回報完成但 worktree 未合併（PC-039）、API 錯誤中斷但實質完成（W2-009.3）。
- 已固化為 tool-output-trust 規則 5，但 complete 時的 git ground truth 自動比對仍缺（現靠 PM 手動）。

### F7 深度與扇出的長尾失控
- 深度 4-5 層票計 23 筆（MAX_TICKET_DEPTH=3 僅 warning 且 W1-056.5 才補）；巨型父票 18/16/14/13 子。
- 對照 PC-042（subagent ~20 tool call）：資源限制（ulimit 對應物）是被動發現而非建票時主動檢查，dispatch-readiness 到 W17-053 才補。

### F8 協議演進無 schema 版本化，corpus 異質
- Exit Status/NeedsContext 僅覆蓋後期 60%、Context Bundle 33%、重現實驗結果為 ANA 的 86%。
- frontmatter 無 schema_version 欄位，每次工具鏈假設變更都需相容全歷史變體或靜默排除。

### F9 同名鍵跨層衝突（自證式發現）
- frontmatter `status:`（生命週期）與 body Exit Status `status:`（執行結果）同名，本次普查 grep 即被混雜污染（caveat a）。任何以 grep 為查詢介面的消費者（含 hook）都承受同樣歧義。

### 三、改進方向與 spawn 規劃

| # | 方向 | 對應缺失 | 性質 | spawn 規劃 |
|---|------|---------|------|-----------|
| D1 | 寫入 syscall 化：CLI 補 set-priority/set-status；寫入時枚舉白名單驗證（type/priority/status/章節名）；Edit frontmatter 偵測擴至全欄位 | F3/F5/F9 | 改造既有 | 待用戶核可後建 ANA（設計）+ IMP（實作） |
| D2 | 身份前移：派發模板強制 claim --as / dispatch hook 自動 set-who | F1 | 改造既有（小） | 待核可建 IMP |
| D3 | 隔離預設反轉評估：檔案變更型派發預設 feat branch | F1 | 可能零機制（純 SOP 文件化） | 待核可建 ANA（結論可能是文件化而非機制） |
| D4 | frontmatter 加 schema_version；歷史 corpus 凍結豁免不回填 | F8 | 改造既有 | 待核可建 IMP |
| D5 | create/claim 時偵測「When 含 ticket ID 但 blockedBy 空」→ warn | F4 | 改造既有（小） | 待核可建 IMP |
| D6 | 單檔拆分（journal/descriptor 分檔）——**顯性豁免不做**：LLM 單 Read 全上下文是 feature，拆檔破壞之；改為 append-log 章節白名單與 ticket-body-schema 對齊（Completion Info 缺席問題） | F2 | 顯性豁免 + 小修 | 豁免拆檔；白名單對齊待核可建 IMP |
| D7 | in_progress watchdog：dispatch-active.json 比對逾時 → release 建議（補 stale-list 的 in_progress 端） | F6/F7 | 新增工具 | 待核可建 IMP |
| D8 | 自我描述修正：process 模型 → 「job queue + 共享記憶體執行緒」混合模型，明示兩個預設反轉點 | F1 認知固化 | 零工具純文件 | 待核可建 DOC |

**spawn 落地狀態**：本 Solution 含 spawn 規劃表格，依 quality-baseline 規則 5，complete 前須逐項轉實際 ticket 或顯性豁免。目前狀態：D6 拆檔部分已顯性豁免；D1-D8 其餘項**待用戶決策 spawn 範圍與優先序後建票**（用戶為系統設計者，方向取捨屬用戶決策；ANA 維持 in_progress 直到 spawn 落地）。

### 多視角審查結果與修正（parallel-evaluation 情境 F，5 視角）

報告檔：session scratchpad w5-005-review-{evidence,alternatives,scope,linux,basil}.md

### 各視角判定

| 視角 | 總判定 |
|------|--------|
| Evidence | 結論獲實證支持——關鍵數字獨立重算全部吻合；漂移被低估（type 實 12、priority 實約 24），方向為「缺陷比宣稱更嚴重」 |
| linux | Good taste（邊緣）——F3 根因定位準、D6 豁免有品味；扣分 D4 YAGNI、F7/F8 理論性稀釋信號 |
| Alternatives | 無需推翻任何結論；系統性缺口為「刪除性/降級性方案」全表缺席；11 項發現 |
| Scope | 排序骨架可辯護，但兩處事實錯誤 + 兩處重複造輪需修 |
| basil | 1 Critical（acceptance「數據」已修正為「資料」）+ 3 Warning（F5 編號斷層、L84 無分母、2 處句型） |

### 事實修正（Scope 實測推翻票內宣稱）

1. **F3/F5 修正**：「priority 無 CLI 命令」錯——`set-priority` 已存在且完整接線（fields.py:307 → track.py:315/686）。正確歸因：「命令存在但零枚舉驗證（execute_set_field 直接落盤）且採用率低（71% 停預設）」。錯誤來源：引用 ticket SKILL.md 的 stale 描述（「priority 等欄位無 CLI 命令」）未以 code 驗證——此為 ticket-skill-sync-check 規則域的文件漂移實例，亦違反 feedback_audit_report_suggestions_need_ssot_verification。
2. **F8/D4 修正**：「無 schema 版本化」高估——`protocol_version` 框架已完整存在（constants.py:403-409、lib/migrations.py、protocol_version_checker.py），真缺口是**欄位從未 emit 至票檔（0/2285）**。D4 若照原案建 schema_version 新欄位會與既有機制重名重造。
3. **F1 降級**：拆為 F1a（機制債——已被 claim --as / dispatch-active 叢集補丁覆蓋，列「已緩解」）與 F1b（自我描述漂移——文件債，對應 D8）。「最高層級」標籤移交 F3。
4. **F9 影響面限縮**：14/24 hook 走 yaml.safe_load 只讀 frontmatter，免疫混雜；受影響者限裸 grep 行消費者。
5. **F2 判定採 linux**：已被 F3 吸收（症狀真、獨立病因不成立）。
6. **F7 判定**：深度端已修完不建票（linux）；扇出端（巨型父票 18/16/14/13 子）creation 時無把關為真缺口（Alternatives 發現 10），補 D11。

### 衝突處理記錄

- D4：linux「凍結線取代新欄位」×Scope「emit 既有 protocol_version」→ 收斂為 emit 既有欄位 + 文件化 lazy-migration/凍結契約（零新欄位，兩視角同時滿足）。
- D7：三視角一致降級為「改造既有（小）」；Alternatives 指出 dispatch-active.json 比對會引入新雙面帳（自違 F6），採單平面 started_at 為主、dispatch-active 交叉比對須在票內顯性論證。
- F7 建票與否：linux 反對（deadline 端）×Alternatives 支持（creation 端）→ 減法預設下仍建 D11（一行 warning 比照既有 depth warning，長在既有輪子上，符合 linux 形態要求），實作時 linux 品味否決權保留。

### 修正後改進清單（D1-D11 + 1 doc-fix）

| # | 修正後內容 | 形態 | 審查依據 |
|---|-----------|------|---------|
| D1 | 寫入邊界收斂 ANA：step0 枚舉 SSOT 宣告（CLI help vs body schema vs 普查口徑三方對齊）→ argparse choices=（兩行封 CLI 端）→ save_ticket 落盤前驗證閘（真正主體，收斂約 15 寫入點）→ lifecycle 狀態轉移驗證強化（非裸 set-status）；升級路徑 warn 一個 Wave 量測後再 deny；只驗被改動欄位、舊票化石豁免（acceptance 必含此測試） | ANA→IMP | Alt 發現 1/2、Scope Q2、linux 鐵律 |
| D2 | 派發身份前移（模板強制 claim --as）；TOFU 對照方案（pending 無主態 complete --as 回填）納入設計比較 | IMP | Alt 發現 11 |
| D3 | 隔離預設反轉評估（可能純 SOP 文件化） | ANA | 原案維持 |
| D4 | emit 既有 protocol_version 至新票 + lazy-migration/凍結契約文件化；不建新欄位、歷史 2285 票不回填 | IMP（小） | Scope Q3 + linux |
| D5 | 「When 提及 ticket ID ≠ 依賴」誤判率評估（source_ticket vs blockedBy 語意分離），再決定 warn/auto-populate/零機制慣例 | ANA（小） | Alt 發現 5 |
| D6 | 拆檔豁免維持；理由改寫為條件式（寫入競合由 D1 + append-log 通道吸收、尺寸受控由 file-size guardian 把關）；append-log 章節白名單與 body-schema 對齊 | 豁免 + IMP（小） | Alt 發現 8 |
| D7 | stale-list/runqueue in_progress release hint（單平面 started_at；與 subagent-stop-dispatch-cleanup 去重） | IMP（小） | 三視角收斂 |
| D8 | 自我描述修正：以「issue tracker + CI runner」為主類比（tickets=issues、agents=ephemeral runners）、batch job 為輔；明示身份綁定與隔離兩預設 | DOC | Alt 發現 9 |
| D9 | triage cadence：priority 校準儀式掛 wave 收尾/版本檢討（零機制純 SOP）——priority 71% 停預設的流程面解法 | DOC | Alt 發現 9 新增 |
| D10 | F9 定點修：body Exit Status 鍵 status: 改名 exit_status:（模板 + 消費端同步） | IMP（小） | Alt 發現 3 |
| D11 | create --parent 時 children 數 warning（比照既有 depth warning 一行擴充） | IMP（小） | Alt 發現 10 |
| doc-fix | ticket SKILL.md stale 描述修正（「priority 等欄位無 CLI 命令」與現行 code 不符） | DOC（併入 D8 票或獨立） | Scope Q1-F3 |

**priority 對照方案記錄**（Alt 發現 4）：刪除 priority 欄位/降 binary flag/由 type+wave 推導——列為 D1 ANA 的對照方案，不獨立建票（納入 D1 設計空間）。

### 結構性發現對應（parallel-evaluation Phase 3 步驟 5 判斷）

- 「必填無驗證」→ 既有 PC-MON-002 已涵蓋，不新建。
- 「框架文件宣稱 CLI 能力與 code 漂移」→ 既有 ticket-skill-sync-check 規則域 + feedback_audit_report_suggestions_need_ssot_verification 已涵蓋，不新建 error-pattern；本票 Solution 記錄實例。
### F10（補充發現，用戶指示納入）：agent 結果回傳通道不對稱——本次審查派發的活體實證

**主張**：審查型派發的交付通道非結構化且各 agent type 能力不對稱，final message 是已知劫持風險通道卻是唯讀型 agent 的唯一退路。

**本次五視角派發實證（2026-07-04）**：
1. Plan agent（Alternatives 視角）被系統層禁止以 heredoc/重導向建檔——4/5 agent 可寫報告檔、1/5 不可；派發 prompt 的「寫入報告檔」指示對其無效，交付退化為 final message（恰為 W2-011 已知 hook 劫持風險通道），PM 人工落檔保全。
2. create --parent 版本繼承 bug 現場觸發（D13 起因）：三張建票靜默失敗，錯誤訊息被 PM 的 grep 過濾吞掉——輸出過濾遮蔽錯誤同時違反 bash-tool-usage 規則二精神，PM 流程自省記錄。
3. 既有已知模式在本次重現：hook 提示淹沒 CLI 輸出（claim 輸出被 wrap-decision 提醒覆蓋，需依 tool-output-trust 重發乾淨命令驗證）。

**對應改進**：D12（.12）agent 結果回傳通道結構化評估——能力矩陣 + 交付契約 + 唯讀型 fallback SOP。

### Spawn 落地確認（quality-baseline 規則 5）

| 方向 | 子票 | 型別/優先級 |
|------|------|------------|
| D1 寫入邊界收斂 | 1.5.0-W5-005.1 | ANA/P1 |
| D2 身份前移 + TOFU 對照 | 1.5.0-W5-005.2 | IMP/P2 |
| D3 隔離預設反轉評估 | 1.5.0-W5-005.3 | ANA/P3 |
| D4 protocol_version emit | 1.5.0-W5-005.4 | IMP/P2 |
| D5 When 提及 vs 依賴誤判率 | 1.5.0-W5-005.5 | ANA/P2 |
| D6 append-log 白名單對齊 | 1.5.0-W5-005.6 | IMP/P2 |
| D7 in_progress release hint | 1.5.0-W5-005.7 | IMP/P2 |
| D8 自我描述模型修正（含 doc-fix：SKILL.md stale 描述併入） | 1.5.0-W5-005.8 | DOC/P1 |
| D9 triage cadence | 1.5.0-W5-005.9 | DOC/P3 |
| D10 exit_status 鍵改名 | 1.5.0-W5-005.10 | IMP/P2 |
| D11 children 數 warning | 1.5.0-W5-005.11 | IMP/P2 |
| D12 結果回傳通道結構化（F10） | 1.5.0-W5-005.12 | ANA/P2 |
| D13 create --parent 版本繼承修復（本次 spawn 活體 bug） | 1.5.0-W5-005.13 | IMP/P2 |

**顯性豁免記錄**：D6 拆檔（單 Read 全上下文 feature + 寫入競合由 D1/append-log 吸收，理由已改寫為條件式）；F7 深度端 watchdog（已修完的歷史，linux 判定）；priority 刪除性對照方案（納入 .1 設計空間，不獨立建票）；F10 之 error-pattern 新建（既有 W2-011 memory + PC-166 家族已涵蓋通道劫持模式，不重複建檔）。

**執行優先序**（linux 建議 + 審查修正）：.1（D1）> .8（D8）> .2（D2）> .13（D13，小而急——阻礙後續所有 --parent spawn）> .5 > .7 > .10 > .6 > .11 > .4 > .12 > .3 > .9。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
