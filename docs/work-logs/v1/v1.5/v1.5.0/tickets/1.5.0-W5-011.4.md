---
id: 1.5.0-W5-011.4
title: 接線 version-release memory 升級稽核
type: IMP
status: in_progress
version: 1.5.0
wave: 5
priority: P2
parent_id: 1.5.0-W5-011
children: []
blockedBy:
- 1.5.0-W5-011.3
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-python-developer
  history: {}
what: 接線 version-release memory 升級稽核
when: 1.5.0-W5-011.3 promote/scan 工具核心完成後（稽核復用 scan 分類邏輯）
where:
  layer: 框架層（.claude/ skills + scripts + error-patterns，無產品碼）
  files:
  - .claude/skills/version-release/scripts/version_release.py
  - .claude/skills/version-release/templates/release-checklist.md
why: ANA W5-011 方案 D 節奏稽核兜底：發版前掃描 memory 升級狀態使積壓與分流遵循率可見化；稽核輸出作為是否建強制層 hook 的決策 trigger（decision-trigger-binding
  規則 2）。本票由 W5-011.3 範圍拆分而來（原稽核接線段）
how:
  task_type: Implementation
  strategy: version_release.py 新增 check 函式（復用 W5-011.3 scan 分類邏輯，輸出已標註/未評估分流遵循率），對齊既有
    check_* 風格（如 check_stale_active_versions）；release-checklist.md 增稽核項；測試放 .claude/hooks/tests/
acceptance:
- '[ ] version_release.py 新增 memory 升級稽核 check，輸出分流遵循率（已標註/未評估比例）'
- '[ ] release-checklist.md 增列 memory 升級稽核項'
- '[ ] 稽核輸出作為是否建強制層 hook 的決策 trigger（decision-trigger-binding 規則 2）'
- '[ ] pytest 測試綠燈（uv run --project .claude/hooks pytest 對應 tests）'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: true
started_at: '2026-07-05T18:10:33'
completed_at: null
created: '2026-07-05'
updated: '2026-07-05'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

接線 version-release memory 升級稽核

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
新增 `check_memory_upgrade_status()` 於 version_release.py（插入於 `preflight_check` 前），復用
`memory_upgrade.scan_memory_dir` 分類邏輯：

- `_resolve_memory_dir(root)`：由專案根目錄推導 `~/.claude/projects/<slug>/memory/`（slug = 絕對路徑
  `/` 換 `-`）
- `_count_memory_feedback(memory_dir)`：計算已標註數與總 feedback 數，供分流遵循率計算
- `check_memory_upgrade_status(version)`：呼叫 scan + count，輸出
  `[Memory 升級稽核] 已標註: X, 未評估: Y, deferred: Z, dangling: N (遵循率: XX%)`；
  unevaluated > 0 時回傳 `(False, messages)` 並附決策 trigger 提示文字（規則 2：連續多次
  unevaluated > 0 應建強制層 hook）
- `preflight_check()` 新增步驟 1.6 呼叫此 check，`all_ok` AND 鏈納入 `mu_ok`

`release-checklist.md` 於「技術債務驗證」後新增「Memory 升級稽核」章節（三項 checklist）。

測試：`.claude/hooks/tests/test_memory_upgrade_tools.py` 新增 3 個 test case（全 upgraded /
有 unevaluated / 有 dangling），透過 `patch.object` mock `scan_memory_dir` 與
`_count_memory_feedback` 驗證三態行為。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
`uv run --project .claude/hooks pytest .claude/hooks/tests/test_memory_upgrade_tools.py -v`：
20 passed（含新增 3 個 check_memory_upgrade_status 測試）。

`uv run --project .claude/hooks pytest .claude/hooks/tests/`：3088 passed, 48 skipped。

Commit: f72dcba14

## Context Bundle

### 實作範圍

**AC1 — version_release.py 新增 check 函式**

新增 `check_memory_upgrade_status()` 於 `.claude/skills/version-release/scripts/version_release.py`：
- import `scan_memory_dir` from `.claude/skills/continuous-learning/scripts/memory_upgrade`（需 `sys.path` 或相對 import）
- 函式簽名對齊 `Tuple[bool, List[str]]` 慣例（L675 `check_worklog_completed` 為範本）
- 呼叫 `scan_memory_dir(memory_dir, error_patterns_dir)`，memory_dir 為 `~/.claude/projects/<project-slug>/memory/`，error_patterns_dir 為 `<project_root>/.claude/error-patterns/`
- 計算分流遵循率：`upgraded / total_feedback` 與 `unevaluated / total_feedback`
- 輸出格式：`[Memory 升級稽核] 已標註: X, 未評估: Y, deferred: Z, dangling: N (遵循率: XX%)`
- pass 條件：unevaluated == 0（所有 feedback 都已評估）；unevaluated > 0 時回傳 `(False, errors)`
- 在 `preflight_check()` L1697 前插入呼叫（步驟 1.6），對齊既有 print_info/print_success/print_error 風格
- `all_ok` 結果加入 memory check 結果（與既有 AND 鏈合併）

**memory_dir 定位**：project slug 路徑可用 `Path(os.path.expanduser("~")) / ".claude" / "projects"` 列舉，但本專案已知路徑為常數：`~/.claude/projects/-Users-tarragon-Projects-book-overview-v1/memory/`。建議優先嘗試 `get_project_root()` 衍生 slug（將 `/` 替換為 `-` 並加前綴 `-`），fallback 掃描 `~/.claude/projects/` 目錄。

**AC2 — release-checklist.md 增稽核項**

在 `### 技術債務驗證` 章節後新增 `### Memory 升級稽核`：
```markdown
### Memory 升級稽核

- [ ] Memory feedback 分流遵循率 100%（無未評估檔案）
- [ ] 無 dangling pointer（error-pattern 引用存在）
- [ ] Deferred 項目附理由且量級可控
```

**AC3 — 決策 trigger 記錄**

AC1 的 check 輸出即為稽核結果；此稽核結果作為「是否需要強制層 hook」的決策 trigger（decision-trigger-binding 規則 2：trigger 須為可觀測事件，此處 trigger = 連續 N 次發版 unevaluated > 0）。在 check 函式的 warning 輸出中加入此語意提示。

**AC4 — 測試**

在 `.claude/hooks/tests/test_memory_upgrade_tools.py` 新增 test case（或獨立 test 檔），測試 check 函式：
- mock `scan_memory_dir` 回傳三態（全 upgraded / 有 unevaluated / 有 dangling）
- 驗證 `(bool, list)` 回傳值正確

### Commit policy

- commit 於 main（所有檔案在 `.claude/` exempt 範圍）
- `git add` 精確路徑，禁 `git add .`
- commit message: `feat(1.5.0-W5-011.4): version-release memory 升級稽核 check`

### 現有程式碼參考

- `check_worklog_completed()` L675-750：回傳 `Tuple[bool, List[str]]` 模式
- `preflight_check()` L1608-1698：check 編排（步驟 1.1-1.5，插入 1.6）
- `scan_memory_dir()` memory_upgrade.py L119-137：回傳 `{"unevaluated":[], "deferred":[], "dangling":[]}`
- `classify_memory()` L76-92：三態分類（unevaluated/deferred/upgraded）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
