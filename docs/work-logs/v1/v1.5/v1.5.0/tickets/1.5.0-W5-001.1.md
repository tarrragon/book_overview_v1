---
id: 1.5.0-W5-001.1
title: 掃描 hook matchers exact-match breaking change (2.1.194)
type: ANA
status: in_progress
version: 1.5.0
wave: 5
priority: P0
parent_id: 1.5.0-W5-001
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: 主線程
  history: {}
what: 掃描 80+ hooks 確認無子串匹配依賴
when: CC 升級至 2.1.194+ 前
where:
  layer: 待定義
  files:
  - .claude/hooks/
why: 2.1.194 將 hook matchers 從子串匹配改為 exact-match，可能是 silent breaking change
how:
  task_type: Analysis
  strategy: grep 所有 hook matchers，對照 exact-match 行為確認相容性
acceptance:
- '[ ] 掃描完成，列出受影響 hooks 或確認無影響'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-07-02T13:51:39'
completed_at: null
created: '2026-07-02'
updated: '2026-07-02'
---

# Execution Log

## Task Summary

掃描 80+ hooks 確認無子串匹配依賴

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

CC 2.1.195 修復「hook matchers with hyphenated identifiers accidentally substring-matching — they now exact-match.」（官方 CHANGELOG 原文，例：`code-reviewer`、`mcp__brave-search`）。**注意**：ticket 描述寫 2.1.194，實際落在 2.1.195；breaking change 範圍**僅限「含連字號（hyphen `-`）的 matcher」**——這類 matcher 過去以子串（substring）比對，修復後改為 exact-match。非連字號 matcher（如 `Edit`、`Bash`）的比對行為**未受此修復影響**。

> 修正父票 T1 與本票原始假設：原假設（來自 team-lead 訊息）認為 `Bash` 等一般 matcher 也可能曾意外子串匹配。官方 changelog 將 bug 範圍明確限縮於「hyphenated identifiers」，故 `Bash`/`Edit`/`Task` 等無連字號 matcher 不在此 breaking change 範圍內。

### 影響範圍

掃描範圍：`.claude/settings.json`（22 個 hook 群組）、`.claude/settings.local.json`（無 hooks 區塊）、`.claude/templates/settings-local-template.json`（hooks 僅註解無 matcher）、`.claude/skills/*/hooks/`（skill hooks 透過 settings.json 註冊，非自帶 matcher frontmatter）。

結論：**全部 22 個 matcher 皆使用底線（`__`）或 CamelCase，零個含連字號**，故此 breaking change **不影響本專案任何 hook**。

### 相關 Error Pattern

無直接對應 PC。屬平台版本相容性掃描，父票 1.5.0-W5-001 T1 張力項的落地驗證。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending
