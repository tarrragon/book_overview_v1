---
id: 1.5.0-W5-023
title: 實作 Spawn Requests body section + add-spawn-request CLI 命令
type: IMP
status: in_progress
version: 1.5.0
wave: 5
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.5.0-W5-022
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 新增 Spawn Requests section 和 CLI
  rationale: 1.5.0-W5-022 ANA 結論
who:
  current: thyme-python-developer
  history: {}
what: 實作 Spawn Requests body section + add-spawn-request CLI 命令
when: W5-022 完成後
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/constants.py
  - .claude/skills/ticket/ticket_system/commands/track.py
  - .claude/pm-rules/ticket-body-schema.md
why: agent 執行中發現應開新 ticket 的議題缺專屬結構化回報通道，需新增 Spawn Requests section 和 add-spawn-request
  CLI 命令
how:
  task_type: Implementation
  strategy: 1. CANONICAL_BODY_SECTIONS 新增 Spawn Requests；2. create 模板生成 placeholder；3.
    實作 add-spawn-request 子命令（auto-numbering + enum 驗證 + auto-commit）；4. 更新 ticket-body-schema.md
acceptance:
- '[x] CANONICAL_BODY_SECTIONS 含 Spawn Requests（第 10 章節）'
- '[x] ticket create 生成的 body 含 Spawn Requests placeholder'
- '[x] ticket track add-spawn-request 命令可寫入結構化 spawn request（what/why/type/priority/files）'
- '[x] add-spawn-request 驗證 type 和 priority 為合法枚舉值'
- '[x] 既有測試 100% 通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-05T21:35:38'
completed_at: null
created: '2026-07-05'
updated: '2026-07-05'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實作 Spawn Requests body section + add-spawn-request CLI 命令

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### Context Bundle

**來源 ANA**：1.5.0-W5-022（已 completed），設計規格位於該 ticket 的 Solution 章節。

**實作範圍**（四個切入點）：

1. **constants.py** (`CANONICAL_BODY_SECTIONS`)
   - 新增 `"Spawn Requests"` 為第 10 章節
   - 位置：`Exit Status` 之後、`Completion Info` 之前
   - 路徑：`.claude/skills/ticket/ticket_system/constants.py:349-359`

2. **ticket_builder.py** (`create_ticket_body`)
   - 在 `## Exit Status` 與 `## Completion Info` 之間插入 `## Spawn Requests` placeholder
   - 路徑：`.claude/skills/ticket/ticket_system/lib/ticket_builder.py:929-946`

3. **track_acceptance.py**（新增 `execute_add_spawn_request` 函式）
   - 仿照 `execute_append_log` 模式（file_lock + load → modify → save + auto-commit）
   - 參數：`--what`（必填）、`--why`（必填）、`--type`（必填，枚舉 `VALID_TICKET_TYPES`）、`--priority`（必填，枚舉 `VALID_PRIORITIES`）、`--files`（選填）、`--context`（選填）
   - 自動編號 SR-1, SR-2, ...（掃描現有 body 中 `SR-\d+` 最大值 +1）
   - 寫入格式：每個 request 一個結構化條目（見 ANA Solution schema）
   - Status precondition：與 append-log 同（`in_progress` or `completed`）
   - auto-commit（與 append-log 同機制 `git_utils._auto_commit_ticket_md`）

4. **track.py**（subparser 註冊）
   - 在 `_register_acceptance_subcommands` 或適當位置新增 `add-spawn-request` subparser
   - dispatcher dict 新增 `"add-spawn-request": execute_add_spawn_request`

**枚舉常數**（已存在，直接 import）：
- `VALID_TICKET_TYPES`：`{"IMP", "ADJ", "ANA", "DOC"}`
- `VALID_PRIORITIES`：`{"P0", "P1", "P2", "P3"}`

**commit policy**：所有變更一次 commit，branch `feat/1.5.0-W5-023-spawn-requests`。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
四切入點依 ANA 規格實作：
1. constants.py: CANONICAL_BODY_SECTIONS 新增 "Spawn Requests"（Exit Status 後、Completion Info 前）
2. ticket_builder.py: create 模板 Exit Status/Completion Info 間插入 Spawn Requests placeholder
3. track_acceptance.py: 新增 execute_add_spawn_request（仿 append-log 模式：file_lock + load/modify/save + auto-commit + dedupe_schema_sections），自動編號 SR-N（掃 body 內 SR-N 最大值 +1），--type/--priority 驗證 VALID_TICKET_TYPES/VALID_PRIORITIES
4. track.py: 新增 add-spawn-request subparser + dispatcher 註冊

VALID_SECTIONS/SCHEMA_H2_SECTIONS 皆由 CANONICAL_BODY_SECTIONS 衍生，新增章節自動納入白名單，無需額外改動。

副作用修正：兩個既有測試（test_w10_107 / test_w17_010）斷言 VALID_SECTIONS 總數為 10，因新增章節後為 11，已同步更新斷言數字（非測試契約變更，純計數對齊功能新增）。

### 自檢結果

類別 A（文字品質）：已依三明示原則撰寫 Solution 說明（為何新增章節、不同步會有計數斷言漂移、已同步修正兩處測試斷言）。
類別 B（禁用字/字元集）：無簡體字、無 emoji、無禁用詞彙。
類別 C（Schema 結構）：Problem Analysis / Solution / Test Results 皆已填寫實質內容，無殘留 placeholder。
結論：無需 Layer 2 委員介入。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
指令：cd .claude/skills/ticket && uv run pytest tests/ -x -q
結果：1932 passed, 1 deselected（含更新後的 test_w10_107_valid_sections_alignment.py 與 test_w17_010_exit_status_schema.py）
另手動驗證：uv run ticket track add-spawn-request --help 正確列出 --what/--why/--type/--priority/--files/--context/--version/--force

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.5.0-W5-022 | chars: 398 -->

### Task Reference
- 1.5.0-W5-022 what: 設計 ticket body 新增結構化 spawn-request 欄位及對應 CLI 命令

### Rationale Chain
- 1.5.0-W5-022 why: agent 執行中發現應開新 ticket 的議題時，現有回報通道不明確：(1) prompt 回傳資訊量不足且違反 PC-040；(2) 寫在 Solution/Completion Info 會被其他內容稀釋，PM 讀時易漏；(3) spawn 建議有確定性 schema（what/why/priority/type），依制式化方法論應有專屬欄位和 CLI 命令

### Related Files
- .claude/skills/ticket/ticket_system/  # from 1.5.0-W5-022

### Source Acceptance (reference)
- [ref] [ ] 產出 spawn-request 欄位設計（schema + 命名 + 位置）  # from 1.5.0-W5-022
- [ref] [ ] 產出對應 CLI 命令設計（add-spawn-request 或類似）  # from 1.5.0-W5-022
- [ref] [ ] 產出與 spawned_tickets/Solution Spawn 落地確認的關係釐清  # from 1.5.0-W5-022
