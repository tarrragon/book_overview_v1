---
id: 1.5.0-W4-002
title: 評估 parallel-evaluation Workflow 增強 POC
type: ANA
status: in_progress
version: 1.5.0
wave: 4
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.5.0-W3-002
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager
  history: {}
what: 評估 parallel-evaluation Workflow 增強 POC
when: W4 排程時
where:
  layer: 待定義
  files:
  - .claude/skills/parallel-evaluation/SKILL.md
why: W3-002 結論建議 parallel-evaluation 改用 Workflow pipeline 減少 wall-clock
how:
  task_type: Implementation
  strategy: 設計 POC Workflow script 取代 Phase 2 barrier
acceptance:
- '[ ] 分析 Phase 2 掃描改用 Workflow pipeline 的可行性'
- '[ ] 產出 POC script 骨架（含 agentType + pipeline 範例）'
- '[ ] 評估常駐委員機制在 Workflow 內的實現方式'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-07-01T16:31:21'
completed_at: null
created: '2026-07-01'
updated: '2026-07-01'
---

# Execution Log

## Task Summary

評估 parallel-evaluation Workflow 增強 POC

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 假設

parallel-evaluation 的 Phase 2（並行視角掃描）是 PM 模型驅動的 barrier——所有視角 agent 必須全部完成才進入 Phase 3 彙整。假設改用 Workflow pipeline 可使每個視角掃描完成後立即進入驗證階段，減少 wall-clock。

### WRAP

W：(A) Workflow pipeline 取代 Phase 2 barrier，(B) 保持現狀但改善 Phase 3 自動化，(C) 混合方案（pipeline 內嵌 parallel verify）
A：不擠壓產品開發
P：parallel-evaluation 的 Phase 3 需跨視角彙整（Worth-It Filter + 衝突處理），這是合法 barrier 需求，pipeline 無法完全取代

### 現有流程分析

parallel-evaluation 三階段：
- Phase 1：PM 前台收集標的（確定掃描範圍）
- Phase 2：PM 派發 3+2 agents 並行掃描（barrier：全部完成才進 Phase 3）
- Phase 3：PM 彙整 + Worth-It Filter + 建 Ticket + 錯誤模式記錄

Phase 2 的 barrier 是必要的嗎？
- 跨視角衝突處理需要所有視角結果 → 是合法 barrier
- Worth-It Filter 需全覽才能分類 → 是合法 barrier
- 但每個視角內部的「掃描 → 驗證發現」可以 pipeline 化 → 視角間不需等待

### 核心問題

不是「要不要用 Workflow 取代 parallel-evaluation」，而是「Workflow 可以改善 parallel-evaluation 的哪個環節」。

## 重現實驗結果
### 實驗方法

結構分析：對 parallel-evaluation 現有 7 個情境（A-G）的流程拆解，識別哪些步驟可 pipeline 化、哪些必須保留 barrier。

### 實驗執行

**Phase 2 barrier 分析**：

| 情境 | 視角數（含常駐） | 每視角產出 | 跨視角需求 |
|------|----------------|----------|----------|
| A 程式碼審查 | 3+2=5 | findings 清單 | 去重（同位置不同視角可能重複） |
| B 重構評估 | 3+2=5 | 重構建議 | 衝突（加法 vs 減法方向） |
| C 架構評估 | 3+2=5 | 架構建議 | 衝突（linux 品味否決權） |
| F 結論審查 | 3+2=5 | 審查意見 | 衝突（彼此矛盾需仲裁） |

**結論**：Phase 3 的跨視角彙整（去重 + 衝突處理）是 parallel-evaluation 的核心設計——它需要 ALL results at once，這是 Workflow 文件明確定義的「barrier 合法場景」之一（「Dedup/merge across the full result set before expensive downstream work」）。

**可改善環節**：

1. **Phase 2 內部**（每個視角的掃描）：如果標的有多個檔案，可在每個視角內用 pipeline 化——掃描完一個檔案立即出 findings，不需等全部檔案掃完
2. **Phase 2 → Phase 3 之間**：Phase 2 用 parallel()（barrier）收集所有視角結果，Phase 3 的 Worth-It Filter 和衝突處理可程式化（schema + JavaScript 邏輯）
3. **Phase 3 後**：findings 驗證（adversarial verify）可用 pipeline 化——每個 finding 獨立驗證，不需等其他 finding

### 實驗發現

**已驗證**：
1. Phase 3 跨視角彙整是合法 barrier，不可改為 pipeline
2. Phase 2 內部（多檔案掃描）和 Phase 3 後（findings 驗證）可 pipeline 化
3. 最大改善在「掃描 → barrier → filter → pipeline verify」四階段 Workflow

**仍未驗證**：
1. 常駐委員（linux/basil）在 Workflow 中的角色——是每個 phase 都加入，還是只在 barrier 後的 filter 階段加入？
2. Worth-It Filter 的程式化程度——目前靠 PM 人工判斷「改善幅度 x 風險」，能否用 schema 量化？

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 主線程
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.5.0-W3-002 | chars: 325 -->

### Task Reference
- 1.5.0-W3-002 what: 評估 Workflow tool 融入框架可行性分析

### Rationale Chain
- 1.5.0-W3-002 why: Workflow tool 提供確定性腳本編排與框架 Agent 派發互補，需評估融入邊界

### Related Files
- .claude/skills/  # from 1.5.0-W3-002
- .claude/pm-rules/  # from 1.5.0-W3-002
- .claude/methodologies/  # from 1.5.0-W3-002

### Source Acceptance (reference)
- [ref] [ ] 完成 Workflow vs Agent/bulk-evaluate/parallel-evaluation 四機制能力比對表  # from 1.5.0-W3-002
- [ref] [ ] 識別 Workflow 最適用場景與不適用場景邊界  # from 1.5.0-W3-002
- [ref] [ ] 產出 opts.agentType 橋接方案設計  # from 1.5.0-W3-002
- [ref] [ ] 產出融入建議（哪些 Skill 可改用 Workflow 增強）  # from 1.5.0-W3-002
- [ref] [ ] 產出 PM 決策樹路由建議（何時選 Workflow vs Agent）  # from 1.5.0-W3-002
