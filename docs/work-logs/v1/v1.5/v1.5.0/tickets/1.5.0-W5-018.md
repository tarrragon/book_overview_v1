---
id: 1.5.0-W5-018
title: 評估 multi_view_status hook 警告訊息改善（顯示正確格式範例）
type: ANA
status: in_progress
version: 1.5.0
wave: 5
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:分析任務
  final_decision: opinionated-default 改善評估
  rationale: W5-015 執行期間發現 LLM 反覆寫錯格式
who:
  current: 待派發
  history: {}
what: 評估 multi_view_status hook 警告訊息改善（顯示正確格式範例）
when: LLM 反覆寫錯 multi_view_status 格式（heading vs flat key-value）
where:
  layer: 待定義
  files:
  - .claude/hooks/acceptance_checkers/multi_view_checker.py
why: 'LLM 傾向寫 heading 結構（### multi_view_status + status: skipped）而非 flat key-value（multi_view_status:
  skipped），hook WARNING 訊息未顯示正確格式範例，違反 opinionated-default 原則（預設行為應引導正確做法）'
how:
  task_type: Implementation
  strategy: 評估兩方向：(1) hook WARNING 訊息加正確格式範例（低成本）；(2) hook _parse_field 是否應兼容 heading
    格式（需評估 schema 語意）
acceptance:
- '[ ] 產出改善建議（顯示格式範例 vs 兼容 heading 格式 vs 維持現狀），含取捨與建議'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-07-05T21:02:56'
completed_at: null
created: '2026-07-05'
updated: '2026-07-05'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

評估 multi_view_status hook 警告訊息改善（顯示正確格式範例）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

hook WARNING 訊息列出合法值和子欄位要求，但未展示正確寫入格式範例。LLM 看到「skipped 需附 reason」後直覺寫成 heading 結構：

```markdown
### multi_view_status    ← LLM 自然傾向
status: skipped
reason: ...
```

而 hook \_parse\_field 用 regex \`^\\s\*multi\_view\_status\\s\*:\\s\*(.+?)\\s\*\$\` 搜尋，期望 flat key-value：

```markdown
multi_view_status: skipped
reason: ...
```

### 影響範圍

- \`.claude/hooks/acceptance\_checkers/multi\_view\_checker.py\` 的 \_format\_missing\_warning 函式

### 相關 Error Pattern

- opinionated-default-design 原則：「預設行為 > 文件規範」——WARNING 訊息是 LLM 的即時引導，應展示格式範例

## 重現實驗結果
### 實驗方法

1. 檢視 \_format\_missing\_warning 輸出的完整文字
2. 與 W5-015 實際寫入的格式比對

### 實驗執行

hook 輸出的 WARNING 訊息：
- 列出合法值：reviewed, skipped, n\_a
- 列出子欄位要求：skipped 需附 reason
- **未包含正確寫入格式的具體範例**

W5-015 實際寫入（被 hook 判定為缺失）：
- 使用 heading 格式 \`### multi\_view\_status\` + 下方 \`status: skipped\`
- hook regex 搜尋 \`multi\_view\_status: <value>\` 行未匹配

### 實驗發現

**已驗證事實**：
1. hook 的 WARNING 訊息確實未顯示寫入格式範例（confirmed by source code inspection）
2. LLM 寫錯格式（heading vs flat）是 W5-015 的實際發生案例
3. hook \_format\_invalid\_value\_warning 有 nested YAML 偵測（PC-117 / W17-111），但 \_format\_missing\_warning 無格式引導
4. 問題本質是 opinionated-default 信號：「提醒遵守格式」→ 應改為「工具展示正確格式」

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending
