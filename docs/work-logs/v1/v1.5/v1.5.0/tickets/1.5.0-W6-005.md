---
id: 1.5.0-W6-005
title: 實作 測試同步：ui-factory 搬遷測試修正 + 新元件測試補充
type: IMP
status: in_progress
version: 1.5.0
wave: 6
priority: P2
parent_id: null
children: []
blockedBy:
- 1.5.0-W6-002
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: PROP-013 工作項 5 拆分
  rationale: PROP-013 confirmed，依 In Scope 5 工作項拆 tickets
who:
  current: dill-javascript-developer
  history: {}
what: 實作 測試同步：ui-factory 搬遷測試修正 + 新元件測試補充
when: W6-002 元件庫升格完成後
where:
  layer: 待定義
  files:
  - tests/unit/
  - tests/integration/
why: ui-factory 搬遷後既有測試 import 路徑失效需修正，新增 divider 等元件需補測試，確保元件庫整體測試覆蓋（PROP-013 工作項
  5）
how:
  task_type: Implementation
  strategy: 修正 ui-factory 既有測試的 import 路徑對應新核心目錄；為新元件（divider 三變體）補充 jsdom 單元測試；驗證全套測試通過
acceptance:
- '[x] ui-factory 既有測試 import 路徑已修正且全數通過'
- '[x] 新元件（divider 三變體）各有對應單元測試'
- '[x] npm test 100% 通過且無 skip/pending 測試'
- '[x] npm run test:coverage 元件庫目錄覆蓋率不低於搬遷前'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-12T20:35:40'
completed_at: null
created: '2026-07-11'
updated: '2026-07-11'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實作 測試同步：ui-factory 搬遷測試修正 + 新元件測試補充

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### AC 漂移偵測（PM claim 前查證，PC-055）

本票建立於 W6-003/W6-004 執行前，部分 AC 已被兄弟票覆蓋：

| AC | 漂移狀態 | 世界平面證據 |
|----|---------|-------------|
| 1 import 路徑修正 | **已完成**（W6-002 搬遷時同步） | ui-factory.test.js L17 require `src/core/ui/components/ui-factory`；tests/ 全域 rg 無 `popup/components/ui-factory` 殘留 |
| 2 divider 三變體測試 | **已完成**（W6-004 補 5 case） | ui-factory.test.js 含 createDivider 7 處 |
| 3 npm test 100% 且無 skip/pending | **需稽核**：現況全套 41 skipped（多屬既存、非元件庫範疇） | W6-004 Test Results：5940 passed / 41 skipped |
| 4 coverage 不低於搬遷前 | **待驗證** | 尚無比對數據 |

### AC3 範圍判定（PM 決策）

「無 skip/pending 測試」以**元件庫範疇**（tests/unit/core/ui/components/ + 元件庫相關 integration）為界。既存 41 skipped 若屬元件庫外（如 E2E/live 類既有豁免）不在本票範圍；若稽核發現元件庫內有 skip，須修復或記錄豁免理由。全套 skip 清零屬獨立議題，發現具體項目時用 add-spawn-request 上報。

### 執行重點（漂移修正後）

1. AC1/AC2 做驗證性確認（已完成，勾選前須自行以 rg + npm test 確證）。
2. AC3：列出 41 skipped 的分佈（檔案/理由），確認元件庫範疇內 0 skip。
3. AC4：npm run test:coverage 取 src/core/ui/components/ 覆蓋率；搬遷前 baseline 以 git 歷史（W6-002 前 commit 的 tests/unit/popup/components 對應數據）或以「新增函式全數有測試」替代論證，方法與數據記錄於 Test Results。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
本票為 AC 漂移稽核票（AC1/AC2 已由 W6-002/W6-004 完成，本次執行為驗證性確認 + AC3/AC4 實質稽核）。

- AC1/AC2：rg 全域掃描確認無 `popup/components/ui-factory` 殘留引用；`tests/unit/core/ui/components/ui-factory.test.js` import 已指向 `src/core/ui/components/ui-factory`；createDivider 三變體測試存在（7 處命中）。無需修改。
- AC3：稽核全套 41 skipped 測試分佈，逐一定位到 2 個檔案、皆為 `describe.skip` 整檔跳過：
  - `tests/unit/content/utils/page-detection-utils.test.js`（29 個 test，legacy 已被 simple test 取代）
  - `tests/integration/workflows/error-recovery-workflow.test.js`（12 個 test）
  29+12=41，與 jest 報告的 `41 skipped` 完全吻合。兩檔皆不在元件庫範疇（`tests/unit/core/ui/components/`），對該目錄執行 `grep -rn ".skip(\|xdescribe\|xit(\|xtest("` 結果為 0 命中。依 Problem Analysis「AC3 範圍判定」，元件庫範疇內 0 skip 即達成，全套 skip 清零屬獨立議題不在本票範圍。
- AC4：以 git worktree 取 W6-002 feat commit（83c222dc7）之 parent（65fcfc616，搬遷前）跑 `tests/unit/popup/ui-factory.test.js` 對 `src/popup/components/ui-factory.js` 的覆蓋率作為 baseline，與現況 `tests/unit/core/ui/components/ui-factory.test.js` 對 `src/core/ui/components/ui-factory.js` 的覆蓋率比對：
  - Baseline（搬遷前）：Stmts 76.31% / Branch 66.66% / Funcs 60% / Lines 75.28%（31 tests）
  - 現況（搬遷後）：Stmts 77.27% / Branch 67.5% / Funcs 62.5% / Lines 76.34%（39 tests）
  四項指標全數持平或提升，符合「覆蓋率不低於搬遷前」。

未修改任何 src/tests 檔案（純稽核驗證），無需 feat commit。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
指令與結果：

1. `rg -n "popup/components/ui-factory" tests src` → 0 命中（AC1 確證）
2. `rg -n "require(.*ui-factory" tests/unit/core/ui/components/ui-factory.test.js` → L17 指向 `src/core/ui/components/ui-factory`
3. `rg -c "createDivider" tests/unit/core/ui/components/ui-factory.test.js` → 7（AC2 確證）
4. `npm test` → Test Suites: 2 skipped, 268 passed, 268 of 270 total；Tests: 41 skipped, 5940 passed, 5981 total；0 failed
5. `grep -rn ".skip(\|xdescribe\|xit(\|xtest(" tests/unit/core/ui/components/` → 0 命中（元件庫範疇內無 skip，AC3 依範圍判定達成）
6. 41 skipped 分佈定位：page-detection-utils.test.js（29）+ error-recovery-workflow.test.js（12）= 41，兩檔皆 describe.skip 整檔跳過，皆不在元件庫範疇
7. `npx jest --coverage --collectCoverageFrom='src/core/ui/components/**/*.js' tests/unit/core/ui/components/` → All files: 77.27% Stmts / 67.5% Branch / 62.5% Funcs / 76.34% Lines（39 tests passed）
8. Baseline（git worktree @ 65fcfc616，W6-002 搬遷前）：`npx jest --coverage --collectCoverageFrom='src/popup/components/ui-factory.js' tests/unit/popup/ui-factory.test.js` → 76.31% Stmts / 66.66% Branch / 60% Funcs / 75.28% Lines（31 tests passed）
9. 比對結論：四項覆蓋率指標搬遷後均持平或提升（AC4 達成）

無檔案變更（純驗證性稽核），無 feat commit。

## Context Bundle

### 派發 Context（PC-040）

**前置狀態**：W6-002（搬遷）/W6-003（命名契約）/W6-004（divider）均已完成合併 main。現況 npm test 5940 passed / 41 skipped / 0 failed。

**關鍵路徑**：
- 元件庫測試：`tests/unit/core/ui/components/ui-factory.test.js`（import 已指向 `src/core/ui/components/ui-factory`）
- 測試守護：`tests/unit/helpers/popup-migrated-selectors.js`（W6-003/004 已同步 9 個新選擇器）
- 覆蓋率指令：`npm run test:coverage`；搬遷前 baseline 參考 W6-002 前 commit（git log 找 `1.5.0-W6-002` feat commit 的 parent）

**執行依據**：Problem Analysis「AC 漂移偵測」表 +「AC3 範圍判定」+「執行重點」三節。AC1/AC2 為驗證性確認；實質新工作是 AC3 skip 稽核與 AC4 覆蓋率比對。

**Commit policy**：分支 `feat/1.5.0-W6-005-test-sync` commit（若僅驗證無檔案變更則無需 feat commit，稽核數據寫入 Test Results 即可）。ticket md 由 append-log 自動 commit。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
exit_status: success
reason: "AC 漂移稽核完成：AC1/AC2 驗證性確認通過（已由 W6-002/W6-004 完成）；AC3 元件庫範疇內 0 skip（41 skipped 全數定位於範疇外 2 檔）；AC4 覆蓋率四項指標搬遷後均持平或提升"
confidence: 1.0
acceptance_met: [1, 2, 3, 4]
acceptance_unmet: []
artifacts: []
context_dependencies: ["1.5.0-W6-002", "1.5.0-W6-003", "1.5.0-W6-004"]
estimated_recovery_effort: ""
```

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending
