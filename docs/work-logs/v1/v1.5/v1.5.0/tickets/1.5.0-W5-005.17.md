---
id: 1.5.0-W5-005.17
title: 實作 runqueue/list type 權重排序與 type 標籤顯示
type: IMP
status: in_progress
version: 1.5.0
wave: 5
priority: P2
parent_id: 1.5.0-W5-005
children: []
blockedBy:
- 1.5.0-W5-005.15
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-python-developer
  history: {}
what: 實作 runqueue/list type 權重排序與 type 標籤顯示
when: W5-005.15 完成後
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/commands/track_runqueue.py
why: W5-005.15 ANA 結論：現行排序缺 type 維度，ANA 應排 IMP 前（同 priority 內）；list 輸出缺 type 標籤，PM
  無法快速識別可並行 ANA 組
how:
  task_type: Implementation
  strategy: 新增 _type_rank() 函式（ANA=0 DOC=1 IMP=2），修改 L322-323 和 L420-422 兩處 sort key，list
    輸出加 type 標籤
acceptance:
- '[ ] 新增 _type_rank 排序函式（ANA < DOC < IMP）'
- '[ ] 兩處 sort key 加入 type_rank 維度（priority 之後）'
- '[ ] list/runqueue 輸出含 type 標籤（如 [P1|ANA]）'
- '[ ] 測試覆蓋：同 priority 下 ANA 排在 IMP 前'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-05T15:51:13'
completed_at: null
created: '2026-07-05'
updated: '2026-07-05'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

實作 runqueue/list type 權重排序與 type 標籤顯示

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
執行指令：`(cd .claude/skills/ticket && uv run pytest tests/ -x -q)`
結果：1927 passed, 1 deselected（全套件綠燈，含新增 8 個 `test_type_rank_sorting.py` 測試）
Commit: 1d5cbf5e2（feat/W5-005.17-type-rank-sorting，未 merge main）

## Context Bundle

### Context Bundle

#### 來源 ANA

W5-005.15 Solution 結論：新增 _type_rank() + 修改兩處 sort key + list 輸出加 type 標籤。

#### 實作定位

檔案：`.claude/skills/ticket/ticket_system/commands/track_runqueue.py`

**變更 1**：新增 `_type_rank(ticket)` 函式
- ANA=0, DOC=1, IMP=2（其他 type fallback=1）
- 從 ticket frontmatter `type` 欄位讀取

**變更 2**：修改 `_sort_key`（約 L322-323）
- 現行：`(priority_rank, ticket_id)`
- 改為：`(priority_rank, type_rank, ticket_id)`

**變更 3**：修改 `_render_list` 排序（約 L420-422）
- 同上 pattern 加入 type_rank

**變更 4**：list/runqueue 輸出加 type 標籤
- 現行：`[P2] [ready]`
- 改為：`[P2|ANA] [ready]`

#### 測試

新增測試檔 `ticket_system/tests/test_type_rank_sorting.py`：
- 同 priority 下 ANA 排在 IMP 前
- 不同 priority 下 priority 仍優先（P0 IMP > P2 ANA）
- 未知 type fallback 為 DOC 等級

#### Commit Policy

單一 commit 於 feat 分支，不 merge main。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
