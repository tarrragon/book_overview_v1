---
id: 1.5.0-W5-005.10
title: 重命名 body Exit Status 鍵 status 為 exit_status
type: IMP
status: in_progress
version: 1.5.0
wave: 5
priority: P2
parent_id: 1.5.0-W5-005
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-python-developer
  history: {}
what: 重命名 body Exit Status 鍵 status 為 exit_status
when: W5-005 spawn 落地後排入後續 wave
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system
  - .claude/pm-rules/ticket-body-schema.md
why: frontmatter status:（生命週期）與 body Exit Status status:（執行結果）同名，裸 grep 消費者必混雜（本次普查自證）；yaml
  解析路徑免疫故影響面限縮但定點修便宜；W5-005 F9/D10
how:
  task_type: Implementation
  strategy: '模板生成端改 exit_status: → 消費端（grep/解析 Exit Status 章節者）同步 → 歷史票不回填，消費端相容兩形態或依凍結線分流'
acceptance:
- '[x] 新票 Exit Status 章節使用 exit_status: 鍵'
- '[x] 消費端盤點與同步（grep 或解析該章節的 hook/CLI 清單附於票內）'
- '[x] 歷史票零回填、消費端相容驗證'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-05T15:19:13'
completed_at: null
created: '2026-07-04'
updated: '2026-07-04'
---

# Execution Log

## Task Summary

重命名 body Exit Status 鍵 status 為 exit_status

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 影響面盤點

#### 生產端（模板）

| 檔案 | 行號 | 變更 |
|------|------|------|
| `ticket_system/lib/ticket_builder.py` | 931 | `status: success` → `exit_status: success` |

#### 消費端（解析 `status` key）

| 檔案 | 行號 | 變更 |
|------|------|------|
| `ticket_system/commands/handoff.py` | 1386 | `parsed.get("status")` → `parsed.get("exit_status")` |
| `ticket_system/commands/handoff.py` | 1387 | `_VALID_EXIT_STATUSES` 不變（值集非 key） |
| `ticket_system/commands/track_runqueue.py` | 184 | `exit_status.get("status")` → `exit_status.get("exit_status")` |
| `ticket_system/commands/track_runqueue.py` | 259 | `exit_status_obj.get("status")` → `exit_status_obj.get("exit_status")` |

#### 相容處理（歷史票）

- 歷史票 `status:` key 零回填（acceptance 明訂）
- `_extract_exit_status_for_handoff` 需相容兩 key：先查 `exit_status`，fallback `status`（舊格式）

#### 框架文件

| 檔案 | 變更 |
|------|------|
| `.claude/references/pm-agent-observability.md` | Exit Status 範例 `status:` → `exit_status:` |
| `.claude/skills/ticket/SKILL.md` | Exit Status YAML schema 範例中 `status:` → `exit_status:` |

#### 測試

- `test_track_runqueue.py` 無 exit_status 相關測試覆蓋
- `test_session_start_scheduler_hint_hook.py` 使用 `exit_status` JSON key（外層），不碰內部 `status` key，無需改
- 新增測試：`_extract_exit_status_for_handoff` 的雙 key 相容性

#### 不在範圍

- `AGENT_PRELOAD.md` 提及 Exit Status `needs_context` 為值描述非 key，無需改
- Handoff JSON 外層 `"exit_status"` key 不變
- `constants.py` 的 `CANONICAL_BODY_SECTIONS` "Exit Status" 章節名不變

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
機械式 key rename 完成：`ticket_builder.py` 模板 `status:` → `exit_status:`；`handoff.py` `_extract_exit_status_for_handoff` 與 `track_runqueue.py` `_get_exit_status_tag`/`_compute_readiness` 均改為優先讀 `exit_status`、fallback `status`（歷史票相容，零回填）。同步更新 `.claude/references/pm-agent-observability.md` 範例；`.claude/skills/ticket/SKILL.md` 核實後無 Exit Status YAML key 字面範例需改。

### 自檢結果

- 命名：無新增變數/函式，僅 key 字面重命名，命名一致
- 函式設計：修改行數極小（各 1-2 行 fallback 邏輯），未新增分支複雜度
- DRY：兩處 fallback pattern 相同（`.get("exit_status") or .get("status")`），因分屬不同模組職責未抽共用函式，範圍極小可接受
- 認知負擔：無新增巢狀或依賴，指數低
- 測試：新增 8 個 dual-key 相容測試，全數通過；既有 1927 測試全綠

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
執行指令：`(cd .claude/skills/ticket && uv run pytest tests/ -x -q)`
結果：1927 passed, 1 deselected（含新增 ticket_system/tests/test_exit_status_key_rename.py 8 個測試）
新增測試單獨執行：`uv run pytest ticket_system/tests/test_exit_status_key_rename.py -v` → 8 passed
Commit: ad0801029（feat(1.5.0-W5-005.10): rename Exit Status body key status → exit_status）
變更檔案：
- ticket_system/lib/ticket_builder.py（模板 status: → exit_status:）
- ticket_system/commands/handoff.py（_extract_exit_status_for_handoff 雙 key 相容：優先 exit_status，fallback status）
- ticket_system/commands/track_runqueue.py（_get_exit_status_tag + _compute_readiness 兩處雙 key 相容）
- .claude/references/pm-agent-observability.md（Exit Status 範例 status: → exit_status:）
SKILL.md 未發現需同步的 Exit Status YAML key 字面範例，經核實後未變更（範圍內僅提及章節名非 key schema）。
歷史票 status: key 零回填，消費端雙路徑相容驗證通過。

## Context Bundle

### Context Bundle

#### 實作策略

機械式 key rename：Exit Status YAML 內的 `status:` → `exit_status:`。

**五處程式碼變更**：

1. `ticket_system/lib/ticket_builder.py:931` — 模板 `status: success` → `exit_status: success`（及同行 comment）
2. `ticket_system/commands/handoff.py:1386` — `parsed.get("status")` 改為雙 key 相容：
   ```python
   status = parsed.get("exit_status") or parsed.get("status")
   ```
3. `ticket_system/commands/track_runqueue.py:184` — `exit_status.get("status")` → `exit_status.get("exit_status")` 並 fallback `status`
4. `ticket_system/commands/track_runqueue.py:259` — 同上 pattern

**兩處文件變更**：

5. `.claude/references/pm-agent-observability.md` — Exit Status 範例 `status:` → `exit_status:`
6. `.claude/skills/ticket/SKILL.md` — Exit Status YAML schema 範例同步

**測試**：新增 `_extract_exit_status_for_handoff` 的雙 key 相容測試（新 key + 舊 key fallback + 兩者皆無）。

#### Commit Policy

單一 commit，訊息含 ticket ID。在 feat 分支上 commit，不要 merge 回 main。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending
