---
id: 1.0.0-W1-079
title: '分析 agent 定義 model 欄位行內註解的間歇性解析失敗（inherit + # 註解被當 model 值）'
type: ANA
status: in_progress
version: 1.0.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:執行中額外發現
  final_decision: 建 pending ANA 排入後續
  rationale: quality-baseline-rule-5
who:
  current: rosemary-project-manager
  history: {}
what: '分析 agent 定義 model 欄位行內註解的間歇性解析失敗（inherit + # 註解被當 model 值）'
when: W1-049.1 Layer 2 審查派發失敗時發現（2026-06-11）
where:
  layer: 待定義
  files:
  - .claude/agents/
why: basil-writing-critic 派發失敗（runtime 回報 model 值含整串行內註解），但 thyme 同樣寫法同 session 多次成功——間歇性；10+
  agent 檔採此寫法，潛在影響所有派發
how:
  task_type: Implementation
  strategy: 重現條件調查（model override 是否規避、與 background/agentType 組合關係）；裁決：移除行內註解改寫至 frontmatter
    外 vs 報 CC issue；批量修正 10+ 檔需獨立 IMP
acceptance:
- '[ ] 解析失敗條件查明或標記不可重現（附嘗試記錄）'
- '[ ] 裁決落地 spawn IMP 或顯性標註無需變更理由'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-11T23:41:27'
completed_at: null
created: '2026-06-11'
updated: '2026-06-11'
---

# Execution Log

## Task Summary

分析 agent 定義 model 欄位行內註解的間歇性解析失敗（inherit + # 註解被當 model 值）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果
### 取樣記錄（2026-06-11 同 session，PC-168 分佈紀錄）

| 對象 | 條件 | 結果 |
|------|------|------|
| basil | 原始派發（無 model 覆寫） | FAIL ×1（22:5x，W1-049.1 審查） |
| basil | 唯讀探針 ×3（無覆寫） | FAIL ×3（0 tokens / 0 tool / 2-4s，啟動前即拒） |
| basil | 檔案移除 mcp__zhtw-mcp__zhtw 後探針 | FAIL（錯誤仍回顯舊字串） |
| basil | 檔案 model 改 sonnet 後探針 | FAIL（**錯誤仍回顯舊污染字串** → 快取證明） |
| basil | 派發帶 model: sonnet 覆寫參數 | SUCCESS（完整完成 Layer 2 審查任務） |
| thyme | 真實任務 ×6（無覆寫，同 model 行字面） | SUCCESS ×6 |
| linux | 唯讀探針（無覆寫，同 model 行字面） | SUCCESS |

### 已驗證事實

1. **非間歇**：basil 同 session 內 6/6 確定性失敗；thyme/linux 同字面 7/7 成功
2. **agent 定義 session-start 快取**：session 內編輯 agent .md（tools / model 欄位）對 dispatch resolver 完全不可見，錯誤訊息持續回顯快取的舊字串——本 session 無法以檔案修改驗證任何修復
3. **model 覆寫參數為有效 workaround**（繞過快取的壞值路徑）
4. 三 agent 的 model 行同字面、同 commit 引入（b7f67fb5，16 agent opus→inherit 統一），排除字面差異與引入時間差
5. 失敗發生在 agent 啟動前（usage 0 tokens / 0 tool uses），錯誤為模型驗證拒絕

### 仍未驗證假設

- basil 與 thyme/linux 同字面不同命運的 discriminator（位於 runtime 快取/解析層，session 內不可觀測；候選：tools 含未連線 MCP server 工具、permissionMode 有無、欄位組合——H-tools 實驗因快取失效未獲證據）
- 乾淨 `model: inherit`（已寫入檔案）能否在新 session 修復——需新 session 驗證（spawn W1-079.1）

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending