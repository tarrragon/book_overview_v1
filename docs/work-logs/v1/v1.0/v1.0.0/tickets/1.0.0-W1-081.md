---
id: 1.0.0-W1-081
title: 清理 23 個 agent 檔 model 行行內註解（消除解析失敗暴露面）
type: IMP
status: in_progress
version: 1.0.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.0.0-W1-079.1
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層:品質基線
  final_decision: 1.0.0-W1-079.1 探針驗證成功後擴大清理全部帶註解 agent 檔
  rationale: quality-baseline-rule-3-設計問題立即修正；失敗暴露面結構性消除
who:
  current: thyme-python-developer
  history: {}
what: 清理 23 個 agent 檔 model 行行內註解（消除解析失敗暴露面）
when: claim 後執行；驗收探針需新 session（agent 定義快取重建）
where:
  layer: 待定義
  files:
  - .claude/agents/
why: 1.0.0-W1-079 確認 model 行行內註解是間歇性解析失敗的暴露面；W1-079.1 探針驗證清理寫法有效，裁決擴大清理全部 23 檔結構性消除失敗模式
how:
  task_type: Implementation
  strategy: '對 grep ''^model:.*#'' 命中的 23 檔逐一 Edit：刪除 model 行 # 起的行內註解，保留 model 值不變；背景資訊已集中於
    .claude/references/agent-model-selection.md 無需搬遷；完成後 grep 驗證歸零 + diff 確認無值變更'
acceptance:
- '[x] grep -c ''^model:.*#'' .claude/agents/*.md 全部歸零（23 檔行內註解移除）'
- '[x] 各檔 model 值本身不變（僅刪註解，diff 驗證無值變更）'
- '[ ] 新 session 抽樣派發 1 個清理檔 agent 探針成功'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-12T01:06:31'
completed_at: null
created: '2026-06-12'
updated: '2026-06-12'
---

# Execution Log

## Task Summary

清理 23 個 agent 檔 model 行行內註解（消除解析失敗暴露面）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 執行方式

PM 前台直接執行（非派發）。理由：23 檔同構單行機械式編輯，單一 sed 命令完成；且 `.claude/` 編輯有 subagent 三方 deadlock 歷史（hook 強制 worktree + PC-115 transient deny + worktree commit gate），PM 主線程為可靠路徑。

### 變更內容

- 命令：`sed -i '' -E 's/^model:[[:space:]]*([^#[:space:]]+)[[:space:]]*#.*$/model: \1/' .claude/agents/*.md`
- 範圍：23 檔（與 ticket 描述一致，claim 後以 grep 獨立驗證確認）
- 移除三類行內註解：W8-031 改 inherit 說明（7 檔）、跟隨 session model 說明（15 檔）、john-carmack DEPRECATED 豁免說明（1 檔，值為 sonnet）
- 背景資訊保留於 `.claude/references/agent-model-selection.md`，無資訊遺失

### 工作區隔離記錄（PC-076）

claim 時工作區存在 2 個非本任務遺留變更（`.claude/hooks/tests/test_agent_commit_verification_hook_dedup.py` staged、`test_agent_commit_verification_hook_porcelain.py` modified），來源為前 session；本任務 commit 採精確路徑 `.claude/agents/*.md` 隔離，不混入。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 驗證指令與結果

1. 行內註解歸零：`grep -c '^model:.*#' .claude/agents/*.md | grep -v ':0' | wc -l` → 輸出 `0`（acceptance 1 通過）
2. 值不變驗證：`git diff -- .claude/agents/ | grep -E '^[+-]model' | sort | uniq -c` →
   - 移除行：`-model: inherit  # ...`（22 行）、`-model: sonnet  # ...`（1 行）
   - 新增行：`+model: inherit`（22 行）、`+model: sonnet`（1 行）
   - 23 files changed, 23 insertions(+), 23 deletions(-)，無 model 值變更（acceptance 2 通過）
3. acceptance 3（新 session 抽樣派發探針）：agent 定義有 session 快取（W1-079.1 實證），本 session 無法驗證，留待新 session 執行後勾選並 complete。

### acceptance 3 探針結果（新 session，2026-06-12）

- /clear 後新 session 派發 incident-responder（清理檔之一，model: inherit）探針
- 回覆「探針成功：incident-responder 定義解析正常」，定義解析載入正常（agentId: a18fa844539ab3a19，1 tool use，12s）
- 三項 acceptance 全數通過
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.0.0-W1-079.1 | chars: 324 -->

### Task Reference
- 1.0.0-W1-079.1 what: 驗證 basil model 行清理修復（需新 session 重建 agent 定義快取）

### Rationale Chain
- 1.0.0-W1-079.1 why: basil-writing-critic 派發失敗（runtime 回報 model 值含整串行內註解），但 thyme 同樣寫法同 session 多次成功——間歇性；10+ agent 檔採此寫法，潛在影響所有派發

### Related Files
- .claude/agents/basil-writing-critic.md  # from 1.0.0-W1-079.1

### Source Acceptance (reference)
- [ref] [ ] 新 session basil 無覆寫探針結果記錄（成功或失敗）  # from 1.0.0-W1-079.1
- [ref] [ ] 成功 → 15 檔擴大清理裁決明確；失敗 → discriminator 隔離計畫含下一步單變因  # from 1.0.0-W1-079.1
- [ref] [ ] basil 派發 workaround（model 覆寫）的解除或保留決定明確  # from 1.0.0-W1-079.1