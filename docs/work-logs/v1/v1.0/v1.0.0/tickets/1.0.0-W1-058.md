---
id: 1.0.0-W1-058
title: 改善 ticket append-log 的 PM bookkeeping 寫入路徑
type: IMP
status: in_progress
version: 1.0.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo:
- 1.0.0-W1-056.4
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: checkpoint-1.5:錯誤學習
  final_decision: 建 IMP 消除 PC-100 流程與 CLI precondition 的系統性摩擦
  rationale: quality-baseline-rule-5
who:
  current: thyme-python-developer
  history: {}
what: 改善 ticket append-log 的 PM bookkeeping 寫入路徑
when: PM 依 PC-100 於 ticket create 後立即寫入 Context Bundle / 評估表時
where:
  layer: .claude/skills/ticket/ticket_system/
  files:
  - .claude/skills/ticket/ticket_system/
why: append-log 的 status precondition 要求 in_progress，但 PC-100 要求 create 後（pending
  狀態）立即寫入派發前 context，PM 只能反覆 --force（2026-06-11 session 單日 4 次）；逃生閥常態化會稀釋 --force
  的警示價值，且每次使用都需在後續 review 說明，摩擦與審計噪音同步累積
how:
  task_type: Implementation
  strategy: Phase 3a 評估三候選：(1) 新增 --bookkeeping 旗標（限 Problem Analysis / Context Bundle
    章節，記 audit 不發警示）；(2) create 時允許直接帶入 PCB 內容；(3) 放寬 pending 狀態對指定章節的 append。行為變更須依
    ticket-skill-sync-check 同步 SKILL.md 與 pm-rules，並維持既有 --force 路徑不變（Never break
    userspace）
acceptance:
- '[ ] PM bookkeeping 寫入（Problem Analysis / Context Bundle）於 pending 狀態不再需要 --force'
- '[ ] 既有 --force 行為與 audit 紀錄完全不變'
- '[ ] SKILL.md 與引用 append-log 的 pm-rules 同步更新'
- '[ ] ticket CLI pytest 測試 100% 通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-11T21:55:21'
completed_at: null
created: '2026-06-11'
updated: '2026-06-11'
---

# Execution Log

## Task Summary

改善 ticket append-log 的 PM bookkeeping 寫入路徑

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

append-log 的 status precondition（W3-044，`lib/precondition.py` 的 `require_in_progress`）要求 status=in_progress 才可寫入 body，但 PC-040 / PC-100 要求 PM 於 ticket create 後（status=pending）立即將派發 context 寫入 Problem Analysis / Context Bundle。兩條規則在「派發前 bookkeeping」場景結構性衝突，PM 唯一出路是 `--force` 逃生閥——2026-06-11 單日 4 次，逃生閥常態化稀釋警示價值並累積 audit 噪音（每次需於 review 說明理由）。

### 影響範圍

- `.claude/skills/ticket/ticket_system/lib/precondition.py`（status 判定主邏輯）
- `.claude/skills/ticket/ticket_system/commands/track_acceptance.py`（append-log 呼叫端）
- 測試：`tests/test_require_in_progress.py`、`tests/test_cli_append_log_precondition.py`、`tests/test_append_log_section_autocreate.py`
- 文件：`SKILL.md`、`references/track-command.md`、`.claude/pm-rules/context-bundle-spec.md`

### 相關 Error Pattern

PC-040（context 存 ticket 不存 prompt）、PC-100（create 後立即寫派發 context）為本摩擦的上游規則來源；W1-025（commit fa647243）已解「缺失章節被迫 Edit」的一半摩擦，本 ticket 解 status precondition 的另一半。

### 簡化 WRAP 三問

- W（其他做法）：(1) `--bookkeeping` 旗標——仍需記旗標，摩擦未除盡且擴 CLI 表面；(2) create 時帶入 PCB——不涵蓋 create 後的多次補寫；(3) 放寬 pending 對派發前章節（採用）。
- A（機會成本）：佔用 W1 容量一個 P2 槽位；但每日多次 --force 的審計成本持續累積，淨值為正。
- P（最可能失敗）：放寬範圍過寬破壞 W3-044「未認領不得寫執行紀錄」防護——以 PRE_DISPATCH_SECTIONS 白名單（僅 2 章節）+ 回歸測試防護。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending