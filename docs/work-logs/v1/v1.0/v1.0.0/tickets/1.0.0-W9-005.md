---
id: 1.0.0-W9-005
title: 跨平台收尾：保留檔尾換行 + race 測試 Windows skip ticket_system save + race test
type: IMP
status: completed
version: 1.0.0
wave: 9
priority: P3
parent_id: null
children: []
blockedBy:
- 1.0.0-W9-001
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: save 吃換行 + race 測試 fork Windows 不可攜
  final_decision: save 保留尾換行；race 測試 skipif win32
  rationale: race 測試本質依賴 fork 共享 monkeypatch，改 spawn 會破壞測試正確性，skipif 是 issue 建議的正解
who:
  current: rosemary-project-manager（PM 直接執行，PC-115）
  history: {}
what: save_ticket 寫回保留檔尾換行；test_lifecycle_race 對 Windows skipif（非改 spawn）
when: W9-001 完成並重裝 CLI 後（同 ticket skill，避免交錯）
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/lib/ticket_builder.py
  - .claude/skills/ticket/ticket_system/tests/test_lifecycle_race.py
why: 'issue #1 問題5：claim/release roundtrip 後 ticket.md 檔尾換行被吃產生 git diff 雜訊；問題6：race
  測試寫死 multiprocessing fork context，Windows 無 fork 於 collect 階段 ValueError 崩潰'
how:
  task_type: Implementation
  strategy: save 路徑確保寫回內容以單一換行結尾；race 測試加 pytest.mark.skipif(sys.platform=='win32')（保留
    fork 正確性，spawn 下 monkeypatch 不傳遞會 false GREEN，故不可改 spawn）
acceptance:
- '[x] save_ticket 寫回保留檔尾單一換行，claim/release roundtrip 無 No newline at end of file
  雜訊'
- '[x] test_lifecycle_race 對 win32 skipif，macOS 仍以 fork 正常執行通過'
- '[x] macOS ticket_system 測試套件全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-15T23:53:16'
completed_at: '2026-06-15T23:57:40'
created: '2026-06-15'
updated: '2026-06-15'
---

# Execution Log

## Task Summary

save_ticket 寫回保留檔尾換行；test_lifecycle_race 對 Windows skipif（非改 spawn）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 修復實作（framework issue #1 問題5+6）

### 問題5：save_ticket 保留檔尾換行
- ticket_system/lib/parser.py save_ticket：Markdown content 寫入前加「若不以換行結尾則補單一換行」。僅在缺換行時補，不動既有換行（避免改動帶尾換行 body）。
- roundtrip 副作用消除：claim/release 後 ticket.md 不再吃檔尾換行，無 No newline at end of file git diff 雜訊。

### 問題6：race 測試 Windows skip
- test_lifecycle_race.py + test_create_id_allocation_race.py：加 module-level pytestmark = skipif(sys.platform=="win32")。
- 不改 spawn：兩套件以 fork mode 共享 monkeypatch state，spawn 下 patch 不傳遞至 child 會 false GREEN，故 skipif 是正解（issue 建議），非換 context。macOS 仍以 fork 正常執行。
- 鎖跨平台正確性已由 test_reap_stale_locks / test_precondition_lock_safety / create lock 非並行測試覆蓋，skip 並行測試不損鎖驗證。

### 自檢結果（Layer 1）
- [x] 測試通過率：完整 ticket 套件 2473 passed（+3 新換行測試），1 deselected（perf）
- [x] 換行修復：3 測試（缺換行補一 / 既有保留 / 空 body）+ live CLI roundtrip 驗證 W9-006.md 檔尾 pending\n
- [x] race skip：macOS 仍 fork 執行通過，win32 整檔 skip 不損鎖覆蓋
- [x] 最小修復：save_ticket 僅補缺失換行不動既有，避免誤改
- [x] 重裝 CLI：filelock + 換行修復已生效於 live tooling

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 測試環境
macOS / Python 3.11.13 / pytest 9.0.2

### W9-005 受影響測試
test_save_ticket_trailing_newline.py 3 passed + test_lifecycle_race 1 passed + test_create_id_allocation_race 4 passed（macOS fork 正常，未 skip）。

### 完整迴歸
uv run --extra test pytest（全 ticket_system）：2473 passed, 1 deselected。

### Runtime 驗證
重裝 CLI 後 claim+release 1.0.0-W9-006 roundtrip → W9-006.md 檔尾 od -c 顯示 pending\n（換行保留），副作用已 git checkout 還原。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-15T23:57:40
**Executing Agent**: rosemary-project-manager（PM 直接執行，PC-115）
**Review Status**: pending
