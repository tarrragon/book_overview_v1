---
id: 1.0.0-W1-066
title: 查證 uv-tool-staleness hook 誤報 source dir missing
type: ANA
status: in_progress
version: 1.0.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: merge-impact-verification:附帶發現
  final_decision: hook 回報與 ls 實況矛盾，建 ANA 查證
  rationale: quality-baseline-rule-5
who:
  current: thyme-python-developer
  history: {}
what: 查證 uv-tool-staleness hook 誤報 source dir missing
when: session-start 回報 branch-worktree-guardian / version-release 'source dir missing'，但
  ls 證實兩目錄存在
where:
  layer: 待定義
  files:
  - .claude/hooks/uv-tool-staleness-check-hook.py
why: staleness hook 的 source dir 解析與實際目錄佈局不符（誤報 2 例），誤報會讓真 stale 警示失去信任度（與 PC-074
  false positive 信任侵蝕同構）
how:
  task_type: Implementation
  strategy: 重現 hook 判定邏輯，比對其 source dir 推導路徑 vs 實際 skill 目錄；區分 hook bug vs registry
    過時；產出修復方向
acceptance:
- '[ ] 誤報根因定位（hook 邏輯 / registry / 路徑佈局三者之一，含證據）'
- '[ ] 修復方向明確（spawn IMP 或本票內修）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-11T11:15:52'
completed_at: null
created: '2026-06-11'
updated: '2026-06-11'
---

# Execution Log

## Task Summary

查證 uv-tool-staleness hook 誤報 source dir missing

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

uv-tool-staleness-check-hook.py 的 `SKILLS` registry（行 52-89）對 7 個 skill 寫死 `module_subpath`（source 端 hash 計算來源目錄）與 `package_dir_name`（installed 端子目錄名）。`check_single_skill`（行 110-113）以 `project_root / skill.module_subpath` 推導 source dir，若該目錄不存在即回 `ERROR / "source dir missing"`。

兩例誤報的真正原因是 **registry 路徑佈局假設與實際 skill 佈局不符**（hook 邏輯本身正確，registry 過時/錯誤）：

| skill | hook 假設 module_subpath | 實際佈局 | 是否真為 uv tool |
|-------|------------------------|---------|-----------------|
| version-release | `.claude/skills/version-release/version_release/`（目錄） | source 在 `scripts/version_release.py`（單檔）；installed 在 site-packages `version_release.py`（單檔，非目錄） | 是（CLI `version-release` 已安裝） |
| branch-worktree-guardian | `.claude/skills/branch-worktree-guardian/branch_worktree_guardian/`（目錄） | source 在 `scripts/`；無 `branch_worktree_guardian/` 目錄 | **否**（pyproject 無 `[project.scripts]`，`uv tool list` 無此項，`which` 找不到 CLI） |

驗證（皆以 raw 指令固定值確認）：
- `get_project_root()` = `/Users/mac-eric/project/book_overview_v1`（正確）
- 7 skill module_subpath 存在性掃描：5 EXISTS（ticket / doc / mermaid-ascii / worktree / project-init）+ 2 MISSING（version-release / branch-worktree-guardian），恰與 ticket 描述「誤報 2 例」吻合。
- `uv tool list`：doc-system / mermaid-ascii / project-init / ticket-system / version-release / worktree-skill（6 項，**無 branch-worktree-guardian**）。
- `which version-release` = `/Users/mac-eric/.local/bin/version-release`（存在）；`which branch-worktree-guardian` = not found。
- version-release pyproject：`[tool.hatch.build.targets.wheel] sources=["scripts"] only-include=["scripts/version_release.py"]` → 將 `scripts/version_release.py` 映射為 installed module `version_release.py`（單檔）。
- installed module 實體：`.../uv/tools/version-release/lib/python3.12/site-packages/version_release.py`（單檔，非目錄）。

### 影響範圍

- 檔案：`.claude/hooks/uv-tool-staleness-check-hook.py`（registry 行 58-67 version-release entry、行 78-83 branch-worktree-guardian entry）。
- 功能：SessionStart staleness 提示對此 2 skill 永遠回報 `[ERROR] source dir missing`，污染提示訊息、侵蝕真實 stale 警示信任度（與 why 欄位指出的 PC-074 false positive 信任侵蝕同構）。
- 連帶：即便修正 source path，version-release 為**單檔模組**，hook 現有 source/installed 皆用「目錄 + rglob」模型（`module_subpath` 目錄、`find_installed_module_dir` 找目錄、`_has_any_py` rglob），無法套用於單檔 skill。

### 相關 Error Pattern

PC-074（false positive 信任侵蝕）為 why 欄位明示的同構模式（誤報使真警示失去信任）。staleness 誤報本質屬「registry 靜態假設與實況漂移」，與 PC-145（stale CLI install 診斷）/ PC-175（框架 sync 攜帶不符當前佈局的資產）家族相近——registry 對 skill 佈局的硬編碼假設未隨佈局演進更新。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending