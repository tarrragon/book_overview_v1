---
id: 1.0.0-W7-004
title: 分析 自動載入文件集 token 收斂（82.5k 降至 40-45k）
type: ANA
status: completed
version: 1.0.0
wave: 7
priority: P2
parent_id: null
children:
- 1.0.0-W7-004.1
- 1.0.0-W7-004.2
- 1.0.0-W7-004.3
- 1.0.0-W7-004.4
- 1.0.0-W7-004.5
- 1.0.0-W7-004.6
- 1.0.0-W7-004.7
blockedBy: []
relatedTo: []
spawned_tickets:
- 1.0.0-W7-005
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:框架改善
  final_decision: PM 前台完成評估，ANA 收錄分析並規劃 spawn 收斂子任務
  rationale: quality-baseline-rule-5
who:
  current: rosemary-project-manager
  history: {}
what: 分析 自動載入文件集 token 收斂（82.5k 降至 40-45k）
when: 用戶提出 session 啟動即佔 82.5k tokens（200k 模型 40%）疑慮後
where:
  layer: .claude/rules/core/,CLAUDE.md,docs/project-conventions.md,MEMORY.md（user-level）
  files:
  - .claude/rules/core/
  - CLAUDE.md
  - docs/project-conventions.md
why: 自動載入層缺 token 預算與回收機制，三明示格式 + 案例考古 + 半外移不徹底 + MEMORY.md 超量四因疊加，頻繁刷新 session 時成本過高
how:
  task_type: Implementation
  strategy: PM 前台逐檔分類（保留/降 stub/外移/刪除）→ Solution 寫 spawn 規劃表 → 建立 DOC/IMP 子任務逐檔收斂
    → 新 session /context 驗收
acceptance:
- '[x] 完成自動載入集合逐檔分類表（每檔標注：保留原樣 / 降為路由 stub / 外移 references / 刪除），含預估節省量'
- '[x] Solution 含 spawn 規劃表且 complete 前實際建立對應 DOC/IMP tickets（或顯性標註豁免理由）'
- '[x] 定義收斂驗收基準：新 session /context 量測 Memory files <= 45k tokens'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-12T03:23:28'
completed_at: '2026-06-12T15:29:25'
created: '2026-06-12'
updated: '2026-06-12'
---

# Execution Log

## Task Summary

分析 自動載入文件集 token 收斂（82.5k 降至 40-45k）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 背景量測（2026-06-12 /context 實測）

Memory files 合計 82.5k tokens（22 檔），200k 模型下佔 40%。前六大：MEMORY.md 12.2k、decision-trigger-binding 6.3k、test-assertion-design-rules 6.1k、document-writing-style 5.8k、bash-tool-usage-rules 5.5k、quality-baseline 5.4k。

### 根因

框架有「開放內容放 rules/、封閉內容放 references/」原則，但缺乏自動載入集合的 token 預算上限與回收機制。每次事故教訓傾向寫進自動載入層（怕忘記），file-size-guardian 只量單檔行數（300 行閾值，29 檔超標且不阻擋），無機制量測 auto-load 集合總量，成長單調遞增。

### 五個結構性問題

### 1. 三明示原則套用在錯誤的載入層（最大單一因素）

document-writing-style 強制每段論述含 Why/Consequence/Action 三層，適合按需讀取的教學文件，不適合每 session 自動載入的行為提示層。實例：bash-tool-usage-rules 規則六一條約束寫約 50 行（Why 段 + Consequence 段 + Action 表 + 三慣例表 + 調和表），約束本身兩行可說完。三明示讓每條規則約 3 倍化。

### 2. 案例考古混進規則層（規則 vs error-pattern 職責不分）

grep 實測：test-assertion-design-rules 與 decision-trigger-binding 各含 18 處 ticket/PC 編號引用。前者整檔複述 W1-017/W1-018 實證細節（2.48ms、23/23、195 suites），且已有同名 skill test-assertion-design 可按需觸發。quality-baseline 規則 1 兩個「邊界」段落複述 PC-165/PC-168 事件鏈，PC 檔案本身已存在，規則只需一行路由。

### 3. 半外移不徹底——stub 仍是半篇主文

成功範本已存在：quality-common（609 tokens 完全外移）、ticket-skill-sync-check（806 tokens 純路由 stub）。失敗案例：ai-communication-rules 規則 5 聲稱外移卻留完整觸發情境表 + 授權邊界，規則 6 整段本文全留；cognitive-load 自稱路由 stub 仍 192 行；decision-trigger-binding 的豁免 marker 位置 / frontmatter 場景 / history vs ticket-tracked 語意全是「寫 ticket 命中 hook 時」才需要的內容，卻整檔自動載入。

### 4. 自動載入判準錯置——「重要」被當成「高頻」

document-writing-style（5.8k，受眾=正在寫文件的人）、agent-definition-standard（4.6k，受眾=正在編輯 agent 定義的人）、test-assertion-design-rules（受眾=正在寫測試的人）皆屬「動作觸發時才需要」。正確判準：「PM 在不做任何特定動作時，這行會改變行為嗎？」答否即外移。另：各檔「與其他規則的邊界」表雙向重複（A 寫與 B 差異、B 又寫與 A 差異）；footer 版本歷史留多代完整描述（tool-output-trust-rules 留 1.0/1.1/1.2 三代）。

### 5. MEMORY.md 違反單行索引設計（12.2k，單檔最大）

120 條目，多數是完整教訓濃縮（含案例編號、commit hash、多子句）而非單行 pointer。大量條目已升級為 PC error-pattern 或正式規則（PC-040/PC-064/PC-087 系列已固化），依四問升級設計應於升級後刪除，實際只加不減。

### 附帶：CLAUDE.md 本身（4.9k）

pre-commit hook + CI lint 兩章節（含豁免對照表、完整事故敘事）約佔全檔 40%，屬 docs/development-setup.md 內容；CLAUDE.md 只需指令表 + 一行原則 + 路由。

### 收斂潛力估算

| 動作 | 預估節省 |
|------|---------|
| 六重災檔降為 0.5-1.5k 路由 stub（decision-trigger-binding / test-assertion-design / document-writing-style / bash-tool-usage / quality-baseline / agent-definition-standard） | ~25k |
| MEMORY.md 修剪（已升級者刪、其餘壓真單行） | ~7k |
| CLAUDE.md hook/CI 章節外移 | ~2k |
| 全檔邊界章節去雙向重複 + footer 只留最新版 | ~3-5k |

目標：82.5k → 40-45k（200k 模型佔比 40% → 約 20%）。

### 收斂時的不變量（防止瘦身傷及功能）

1. 外移內容必須有明確路由（stub 內保留觸發條件表 + 完整版路徑），不可直接刪除 substance。
2. hook 強制層引用的規則檔路徑不可失效（外移前 grep .claude/hooks/ 確認引用點）。
3. CLAUDE.md @ 引用鏈與 rules/README.md 檔數描述需同步更新（避免 W1-032 stale 模式重演）。
4. 收斂後需建立預算守門機制（如 file-size-guardian 擴充 auto-load 集合總量量測），否則回彈。

## 重現實驗結果
### 量測方法與結果（可重現）

1. `/context` 指令（新 session 啟動後）：Memory files 22 檔合計 82.5k tokens，佔 200k 模型 40%。
2. `wc -l` 實測（2026-06-12）：自動載入集合合計 2992 行。前六大：CLAUDE.md 262 行、document-writing-style 231、quality-baseline 228、decision-trigger-binding 207、test-assertion-design-rules 201、agent-definition-standard 198。
3. `grep -cE "W[0-9]+-[0-9]+|PC-[0-9]+|IMP-[0-9]+"` 案例編號密度：test-assertion-design-rules 18 處、decision-trigger-binding 18 處、quality-baseline 12 處——證實案例考古混入規則層。
4. MEMORY.md：`grep -c "^- \["` = 120 條目，12.2k tokens，為單檔最大。
5. 對照組：quality-common.md 30 行（609 tokens）、ticket-skill-sync-check.md 42 行（806 tokens）——完全外移 / 路由 stub 模式已在 repo 內驗證可行。

### WRAP 決策紀錄（快速+模式）

- **Step 0**：資料充足——/context 實測 + 全檔內容已檢視 + wc/grep 量化。風險低（git 可逆）。
- **W 擴增選項**（三類涵蓋）：(a) 零工具——換 1M 模型或減少刷新頻率；(b) 改造既有——文件降 stub + substance 外移 references/（主體方案）；(c) 新增工具——file-size-guardian 擴充 auto-load 總量預算量測（防回彈）。反向思考：「自動載入多 = 防錯」不成立——規則價值在禁令被讀到，82.5k 中大量是論證非禁令，且 attention 稀釋本身降低遵循率（PC-072/PC-080 token 污染先例）；外移後 substance 仍可按需讀，防錯價值不損失。
- **R 現實檢驗**：基本率——repo 內已有兩個成功外移範本（W10-137 / W10-076.1 同類工作），可行性已驗證。R.2 事實宣稱：「外移後 hook 引用不失效」未驗證 → 列為每張 spawn ticket 的強制 AC（grep .claude/hooks/ 引用點）。Ooch：建議先做 test-assertion-design-rules 單檔試水溫（已有同名 skill 承接，風險最低）。
- **A 機會成本**：不做 → 每 session 固定多耗 ~40k；做 → 6-7 張 DOC/IMP 容量。悖論自檢：收斂結論寫入 ticket（按需），不新增自動載入規則文字；守門交 hook 機器層。
- **P Premortem 三風險**：(1) 外移砍掉 hook 錨點文字 → 防護：AC 含 grep hooks 驗證；(2) stub 過瘦致行為退化 → 防護：stub 保留禁令速查表 + 路由，分批執行可觀察；(3) .claude/ sync 把舊版覆蓋回來 → 防護：收斂完成後 sync-push 固化。回退：單 commit git revert，成本低。
- **偏好暴露**：傾向「文件收斂 + 預算守門」組合。盲點：未量測外移內容的按需讀取頻率，若高頻回讀會吃回部分節省（仍優於每 session 固定載入）。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 逐檔分類表（acceptance 1）

處置定義：**降 stub** = 主文外移 references/，留禁令速查表 + 觸發路由（仿 ticket-skill-sync-check 模式）；**瘦身** = 留核心結構，砍案例考古/邊界雙向重複/多代 footer；**保留** = 不動。

| 檔案 | 現量(tokens) | 處置 | 目標 | 要點 |
|------|------|------|------|------|
| MEMORY.md | 12.2k | 修剪 | ~5k | 已升級為 PC/規則者刪除；其餘壓真單行 |
| decision-trigger-binding | 6.3k | 降 stub | ~0.8k | 規則 1/2 速查留；豁免 marker/frontmatter 場景外移 |
| test-assertion-design-rules | 6.1k | 降 stub | ~0.6k | 4 禁令速查 + 路由 skill test-assertion-design |
| document-writing-style | 5.8k | 降 stub | ~0.5k | 三明示一表 + 路由（受眾情境性，basil 按需讀） |
| bash-tool-usage-rules | 5.5k | 降 stub | ~1.2k | 統一檢查清單 + 各規則一行速查留；Why/案例外移 |
| quality-baseline | 5.4k | 瘦身 | ~2.5k | 6 規則核心留；規則 1 邊界段落、規則 5 spawn 細節改一行路由 |
| CLAUDE.md | 4.9k | 瘦身 | ~2.5k | pre-commit/CI 章節外移 docs/development-setup.md |
| ai-communication-rules | 4.7k | 降 stub | ~1.5k | 五原則+檢查清單留；規則 5/6 本文全外移 |
| agent-definition-standard | 4.6k | 降 stub | ~0.5k | 編輯 agent 定義時才需要，純路由 |
| pm-role | 4.5k | 微調 | ~3.5k | 高頻核心保留；Caveat/Session-start 清點壓縮為速查 |
| tool-output-trust-rules | 4k | 瘦身 | ~2k | 5 規則句保留（每回合行為禁令）；Why 長文/三代 footer 砍 |
| project-conventions | 3.3k | 改載入方式 | ~0 | CLAUDE.md §6 的 @ 引用改純路徑（檔頭本就自稱 lazy-load，與 @ 強制載入矛盾） |
| language-constraints | 3k | 瘦身 | ~1.5k | 禁用詞表留；規則 3 豁免條款/規則 5 外移 |
| cognitive-load | 2.6k | 瘦身 | ~1k | 閾值表留；五原則細節外移 |
| document-format-rules | 2.1k | 瘦身 | ~1.2k | 表格保留，敘述壓縮 |
| tool-selection | 2k | 瘦身 | ~0.8k | 規則 1 表保留 |
| observability/tool-discovery/chrome-quickref/ticket-skill-sync/README/quality-common | 各 <1.3k | 保留 | — | 已精簡或為範本 |

合計目標約 30-38k，保守驗收基準 <= 45k（acceptance 3：新 session `/context` 量測 Memory files）。

### Spawn 落地確認（acceptance 2）

| 子任務 | type | 範圍 | 順序 |
|------|------|------|------|
| .1 | DOC | 試水溫單檔：test-assertion-design-rules 降 stub（Ooch，風險最低，已有同名 skill 承接） | 先行 |
| .2 | DOC | 降 stub 批次 A：decision-trigger-binding / document-writing-style / agent-definition-standard | blockedBy .1 |
| .3 | DOC | 降 stub 批次 B：bash-tool-usage-rules / ai-communication-rules / quality-baseline 瘦身 | blockedBy .1 |
| .4 | DOC | CLAUDE.md 瘦身 + project-conventions @ 改路徑 + rules/README 同步 | blockedBy .1 |
| .5 | DOC | 中型檔修剪：pm-role / tool-output-trust-rules / language-constraints / cognitive-load / document-format-rules / tool-selection（footer+邊界去重為主，單檔變更小） | blockedBy .1 |
| .6 | DOC | MEMORY.md 修剪（user-level 檔案，PM 親自執行） | 可並行 |
| .7 | IMP | file-size-guardian 擴充 auto-load 集合 token 總量量測 + 預算閾值（防回彈）+ 最終 /context 驗收 | blockedBy .1-.6 |

每張 DOC ticket 強制 AC：外移前 `grep -rl "<檔名>" .claude/hooks/` 確認 hook 引用點不失效；stub 保留禁令速查 + 完整版路徑路由。
全部完成後 sync-push 固化（防 sync 覆蓋回舊版，P 風險 3）。

### 不採用的替代方案（W 階段紀錄）

- 換 1M 模型：不解決增長根因，成本高。
- 減少 session 刷新：限制用戶工作流，治標。
- 接受現狀：違反 attention 稀釋已有實證（PC-072/PC-080）。

### 評估階段完成補記（2026-06-12）

依用戶指示完成全部評估與規劃，實際檔案修改延後至各子 ticket 執行：

1. **R.2 引用驗證完成**：13 個規則檔在 hooks/agents/pm-rules/skills 的引用全為「訊息/註解中路徑提及」，無任何 hook runtime 讀取規則檔內容——stub 化保持原路徑零功能破壞。兩個需同步的字串點已記入 .2（phase4-hook L757、language-guard-hook L95 行號註解）。
2. **重要發現**：docs/development-setup.md L149 起已有完整 Pre-commit Hook 章節，CLAUDE.md 該章節屬重複載錄（.4 風險下修）；tool-output-trust-rules 零引用（.5 修剪自由度最高）。
3. **MEMORY.md 分類完成**：49 個引用 PC/IMP 編號全數驗證存在於 error-patterns（265 檔）；120 條目分類為 Group A 刪除約 52 條（已固化 error-pattern）/ Group B 刪除約 5 條（已固化規則或過時）/ Group C 保留壓縮約 63 條，明細在 .6 Problem Analysis。
4. **各子 ticket 已含完整執行規劃**：.1-.5 stub 設計與保留/外移明細、.6 分類對照表、.7 hook 實作規格（整合 file-size-guardian、AUTO_LOAD_BUDGET_TOKENS=45000、pytest 測試清單）。

執行順序維持：.1 試水溫 →（.2/.3/.4/.5 可並行，注意 PC-137 並行 .claude/ Edit <= 2）→ .6 隨時可做 → .7 收尾驗收。
### 最終收斂驗收（收口記錄，2026-06-12）

新 session /context 實測 Memory files = **41.9k tokens（22 檔）<= 45k 基準，PASS**（量測明細見 1.0.0-W7-004.7 Test Results）。收斂目標 82.5k 降至 40-45k 達成。防回彈機制（file-size-guardian Auto-load 預算量測）已由 W7-004.7 落地；hook 估算係數偏差校準由 spawned 1.0.0-W7-006 追蹤。
### multi_view_status

multi_view_status: reviewed
reviewers: acceptance-auditor
conclusion: 父票 complete 前審計通過（無 FAIL）——三 AC 實質一致、收斂不變量四條全 PASS、測試親自抽驗 23 passed；3 WARN 已落地（WARN-A sync-push 固化、WARN-B/C 由 W7-005 收口）。最終收斂驗收 /context 實測 41.9k <= 45k PASS。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->
### acceptance-auditor complete 前審計（2026-06-12）

總判定：通過（附前置條件 + 3 WARN，無 FAIL）。結構完整性 / 三項 AC 實質一致性 / 執行日誌 / 收斂不變量四條（hook 引用、stub 路由、@ 鏈與 README 同步、預算守門）全 PASS；測試親自抽驗 23 passed。完整報告見審計 agent 回報（PM 驗收存檔）。

WARN 處置：
- WARN-A（sync-push 固化無載體）：已落地至下方「complete 收口清單」。
- WARN-B（.1/.2/.3/.6 Completion Info 缺變更摘要段）+ WARN-C（.5 where.files 未列 references/ 配對慣例）：建 1.0.0-W7-005 (DOC) 追蹤。

### complete 收口清單（前置條件）

1. 新 session /context 量測 Memory files <= 45k → 記入 .7 Test Results → 勾 .7 acceptance #3
2. complete 1.0.0-W7-004.7 → complete 1.0.0-W7-004
3. 執行 /sync-push 固化收斂後的 .claude/（防 sync 覆蓋回舊版，P 風險 3 / WARN-A）

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-12T15:29:25
**Executing Agent**: rosemary-project-manager
**Review Status**: pending