---
id: 1.0.0-W1-030
title: 分析 agent 最終訊息劫持來源驗證（CC runtime vs 專案 7 個 Stop hooks）
type: ANA
status: in_progress
version: 1.0.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.0.0-W1-024
dispatch_reason: ''
decision_tree_path:
  entry_point: 複審挑戰 3-B2 部分成立：證據不足的限制性結論
  final_decision: spawn 小 ANA 驗證來源
  rationale: tool-discovery 規則 2：採限制性解法前先窮盡探索性框架
who:
  current: saffron-system-analyst
  history: {}
what: 分析 agent 最終訊息劫持來源驗證（CC runtime vs 專案 7 個 Stop hooks）
when: 1.0.0-W1-024 complete 前對抗性複審發現 B2 裁決證據不足後
where:
  layer: 待定義
  files:
  - .claude/settings.json
  - .claude/hooks/
why: W1-024 B2 裁決把劫持歸為 runtime limitation 但未驗證來源：settings.json 註冊 7 個專案自有 Stop hooks（evaluate-session/handoff-auto-resume/worktree-auto-commit
  等），若劫持提示部分源自專案 hooks 則框架可修（subagent context 偵測即跳過輸出）；未調查不等於不可修（tool-discovery 規則
  2）
how:
  task_type: Analysis
  strategy: 重現一次劫持（派發小型討論型 agent）並比對 7 個 Stop hooks 的輸出指紋；判定來源後裁決：專案 hook 可修則 spawn
    IMP，確屬 CC runtime 則更新 W1-024 B2 定性並維持 /tmp 檔案交付 workaround
acceptance:
- '[ ] 劫持訊息來源判定有第一手證據|可修/不可修裁決明確|結論回寫或 spawn 落地'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-10T16:48:11'
completed_at: null
created: '2026-06-10'
updated: '2026-06-10'
---

# Execution Log

## Task Summary

分析 agent 最終訊息劫持來源驗證（CC runtime vs 專案 7 個 Stop hooks）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

W1-024 B2 將「agent 最終訊息被劫持」歸為 CC runtime limitation，但**未驗證劫持內容的實際來源**。settings.json 註冊 7 個專案自有 Stop hooks，其中部分會向 stdout 輸出 JSON（CC runtime 讀取 Stop hook stdout 並注入對話），可能是劫持的部分或全部來源。

### 影響範圍

- 所有被 PM 派發的 subagent（Agent() 呼叫）在 session 結束時都會觸發 7 個 Stop hooks
- 劫持導致 agent 回傳值不可靠（W1-024 實證：basil 回傳「無動作」實則完整完成）
- 現行 workaround：deliverable 寫 ticket/檔案 + PM 以 git ground truth 驗證

### 相關 Error Pattern

- PC-166（confabulation）：agent 回傳值不可信的上位問題
- tool-output-trust-rules 規則 5（記錄平面非 ground truth）

## 重現實驗結果
### 實驗方法

靜態分析法：逐一檢視 7 個 Stop hooks 的原始碼，識別哪些在正常執行（非 --self-test）時向 stdout 輸出內容（CC runtime 透過讀取 Stop hook stdout JSON 來注入對話內容）。同時檢查是否有任何 hook 偵測 subagent context。

### 實驗執行

**步驟 1：列出 7 個 Stop hooks**（來源：`.claude/settings.json`）

| # | Hook 檔案 | 簡稱 |
|---|----------|------|
| 1 | `evaluate-session.py` | evaluate-session |
| 2 | `handoff-auto-resume-stop-hook.py` | handoff-auto-resume |
| 3 | `session-experience-persistence-reminder-hook.py` | session-persistence |
| 4 | `worktree-auto-commit-hook.py` | worktree-auto-commit |
| 5 | `stop-worklog-handoff-sync-check-hook.py` | worklog-sync-check |
| 6 | `malformed-tool-call-detector-hook.py` | malformed-tool-call |
| 7 | `context-depth-warning-hook.py` | context-depth-warning |

**步驟 2：逐 hook 原始碼分析——正常執行時的 stdout 輸出**

| # | Hook | 正常執行 stdout 輸出 | 輸出內容 | 劫持潛力 |
|---|------|---------------------|---------|---------|
| 1 | evaluate-session | **無**（僅 stderr） | — | 無 |
| 2 | handoff-auto-resume | **有**：`print(json.dumps(hook_output))` | `{"decision":"block","reason":"[Handoff] 偵測到未完成的 handoff 任務...請執行恢復步驟..."}` 或 `{"suppressOutput":true}` | **高**：block + reason 會注入恢復指令到對話 |
| 3 | session-persistence | **有**：`print(json.dumps(hook_output))` | `{"suppressOutput":true}`（即使找到敏感 ticket 也 suppress） | 無（始終 suppress） |
| 4 | worktree-auto-commit | **無**（僅 subprocess capture） | — | 無 |
| 5 | worklog-sync-check | **有**：`print(json.dumps(output))` | `{"systemMessage":"...drift warning..."}` | **中**：systemMessage 會注入漂移警告 |
| 6 | malformed-tool-call | **無**（stdout 僅 --self-test） | — | 無 |
| 7 | context-depth-warning | **無**（stdout 僅 --self-test） | — | 無 |

**步驟 3：subagent context 偵測**

`grep -in 'subagent\|sub_agent\|is_agent\|agent_name' *.py` 對全部 7 個 hook 回傳空——**無任何 hook 偵測是否在 subagent 環境中執行**。

### 實驗發現

**已驗證事實**：

1. **劫持來源混合型**——CC runtime 行為 + 專案 hooks 共同構成：
   - CC runtime 負責「讀取 Stop hook stdout 並注入對話」的機制（不可修）
   - 專案 hooks 負責「產出什麼內容給 runtime 注入」（可修）

2. **兩個專案 hook 有劫持潛力**：
   - `handoff-auto-resume`（高）：`decision: "block"` + `reason` 欄位注入恢復指令（"[Handoff] 偵測到未完成的 handoff 任務..."），這是最可能覆蓋 agent 最終訊息的元兇
   - `worklog-sync-check`（中）：`systemMessage` 欄位注入漂移警告

3. **零個 hook 偵測 subagent context**：所有 7 個 hook 對 PM session 和 subagent session 的行為完全相同。handoff pending JSON 若存在，即使是 subagent stop 也會觸發 block。

4. **可修性明確**：在 handoff-auto-resume 和 worklog-sync-check 中加入 subagent context 偵測（例如檢查 stdin input_data 的 session 特徵或環境變數），subagent stop 時 `suppressOutput: true` 即可消除劫持。

**仍未驗證的假設**：

- CC runtime 對 Stop hook 的 `decision: "block"` 在 subagent context 中的具體行為（是否真的阻止 session 結束 + 覆寫回傳值，或只是注入 systemMessage）——需實際派發 agent 觀察。但靜態分析已足以確認「專案 hook 有產出劫持內容的能力且未做 subagent 豁免」。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.0.0-W1-024 | chars: 546 -->

### Task Reference
- 1.0.0-W1-024 what: 檢查 ticket 系統 CLI 與 agent dispatch 系統的指令設計問題（基於已知錯誤）

### Rationale Chain
- 1.0.0-W1-024 why: 本 session 暴露 ticket CLI（create vs track create 命名不一致、ANA 必填欄位分批報錯需多次試錯、--how 歧義、append-log section 白名單）與 dispatch 系統（subagent .claude/ Edit deadlock、agent 最終訊息被回合結束 hook 劫持、唯讀 agent 越界）多項指令設計問題，需系統檢查設計合理性並修復

### Related Files
- .claude/skills/ticket/  # from 1.0.0-W1-024
- .claude/agents/  # from 1.0.0-W1-024
- .claude/references/agent-dispatch-template.md  # from 1.0.0-W1-024

### Source Acceptance (reference)
- [ref] [ ] 收集本 session + 既有 error-pattern 的 ticket CLI 指令設計問題（命名不一致／必填欄位錯誤提示完整性／縮寫歧義／section 白名單）  # from 1.0.0-W1-024
- [ref] [ ] 收集 dispatch 系統指令設計問題（.claude/ deadlock／最終訊息劫持／agent 越界防護）  # from 1.0.0-W1-024
- [ref] [ ] 逐項評估設計缺陷並提出修復方向  # from 1.0.0-W1-024
- [ref] [ ] spawn 修復 IMP/DOC（可落地者），或標明無需落地理由  # from 1.0.0-W1-024