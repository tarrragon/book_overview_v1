---
id: 1.0.0-W4-005
title: 處理 CrossDeviceSyncService 死碼（標 deferred 或移除）
type: IMP
status: completed
version: 1.0.0
wave: 4
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 版本規劃:v1.0.0發佈就緒度盤點
  final_decision: 建 IMP 處理同步死碼
  rationale: quality-baseline-rule-5
who:
  current: thyme-extension-engineer
  history: {}
what: 處理 CrossDeviceSyncService 死碼（標 deferred 或移除）
when: v1.0.0 W4
where:
  layer: 待定義
  files:
  - src/background/domains/data-management/services/cross-device-sync-service.js
  - src/background/domains/data-management/data-domain-coordinator.js
why: 盤點發現 CrossDeviceSyncService 為 6-phase stub（核心方法全 todo + 從未實例化），coordinator 用
  MockService 接衝突服務；存在於 codebase 會誤導為已有自動同步
how:
  task_type: Implementation
  strategy: 移除未接線 stub 或加 deferred 標記 + trigger ticket 引用
acceptance:
- '[x] stub 明確標 deferred（綁 trigger ticket）或移除'
- '[x] 不再誤導為已有自動同步'
- '[x] npm test 全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-07T12:51:06'
completed_at: '2026-06-07T12:58:31'
created: '2026-06-07'
updated: '2026-06-07'
---

# Execution Log

## Task Summary

處理 CrossDeviceSyncService 死碼（標 deferred 或移除）

---

## Context Bundle

### 修改檔案
- src/background/domains/data-management/services/cross-device-sync-service.js（474 行，6-phase stub）
- src/background/domains/data-management/data-domain-coordinator.js（787 行）

### PM 前台 caller 分析（2026-06-07，已查證，接手者應獨立複驗 PC-162）
死碼確認三證據：
1. service 在 src 完全無 caller —— 僅自身 line 474 `module.exports`，無任何 `new CrossDeviceSyncService` / require / import
2. coordinator 不引用此 service —— line 144-148 用 5 個 `MockService('SynchronizationOrchestrator' 等)` 佔位（註解「未來實作的服務，使用 Mock 保持架構完整」），sync handler `handleCrossPlatformSyncRequest`(line 398) 走 MockService，與 CrossDeviceSyncService 無連線
3. 核心方法全 todo —— line 204/217/234/244/267 五處 `todo:`（export/transfer/import/validation/options 全未實作）

### 兩個相關 test 檔（非直接依賴 service 方法）
- tests/integration/workflows/cross-device-sync-workflow.test.js —— require CrossDeviceSyncSimulator（helper）並標 no-unused-vars（未實際使用）；測 E2ETestSuite 匯出入流程，不呼叫 service 方法
- tests/e2e/workflows/cross-device-sync.test.js —— 自定義 Mock 類別，標「TDD 紅燈，等待 Phase 3」；位於 tests/e2e/ 不在 `npm test`（tests/unit + tests/integration）掃描範圍

### 決策方向（PM 傾向：移除 service.js）
- 真正跨裝置同步走 v3 canonical 匯出入（book-interchange-v1，W4-031.3 已定稿），不需此 6-phase service
- service.js 孤立死碼，移除安全（無 src caller）
- coordinator MockService 佔位（architecture placeholder）可保留——名稱已明示非真實實作，不誤導為「已有自動同步」；但須確認 coordinator 無殘留指向 CrossDeviceSyncService 的死引用

### Phase 1 須確認項（交執行 agent）
1. 移除 service.js 後 `npm test` 是否仍全綠（integration test 是否實際依賴 service？預期否）
2. coordinator MockService 佔位去留——建議保留並加註指向 v3 同步方向，避免誤導
3. 若評估選 deferred 而非移除：須綁 trigger ticket；但 PM 評估真正同步走 v3、service 無復用價值，傾向移除

### 邊界
- ticket where.files 限 service.js + coordinator.js
<!-- PC-093-exempt: history:1.0.0-W4-005 Context Bundle 邊界條件，Phase 1 已查證兩 test 檔與生產 service 獨立，無需動 test，停止條件未觸發 -->
- 若 Phase 1 發現需動兩個 test 檔，先回報 PM 評估範圍擴展（勿擅自擴大）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 安全閥複驗結論（PC-162 獨立複驗）

三證據全數獨立驗證通過，移除安全：

1. **service 在 src/tests 完全無 caller** —— `grep -rn "CrossDeviceSyncService" src/ tests/` 僅命中自身 3 處（class def line 25、name string line 461、export line 474）；`grep -rn "cross-device-sync-service"` 在 src/tests 零命中（無任何 require/import 此檔路徑）。

2. **coordinator 無指向 CrossDeviceSyncService 的死引用** —— `grep -n "CrossDeviceSync\|cross-device-sync" data-domain-coordinator.js` 零命中。coordinator line 144-148 的 5 個 MockService 與 CrossDeviceSyncService 無任何連線（MockService class 定義於 line 766，自包含）。無死引用需移除。

3. **兩個 sync test 檔與生產 service 獨立** —— integration test require `CrossDeviceSyncSimulator`（test helper，該 helper 不 require 生產 service）；e2e test require `mocks/cross-device-sync.mock`（自包含 mock）。停止條件（移除破壞 sync test）未觸發。

### 變更內容

1. **移除** `src/background/domains/data-management/services/cross-device-sync-service.js`（474 行 6-phase stub，核心方法全 todo，孤立死碼）。

2. **coordinator.js 調整**：5 個 MockService 佔位**保留**（架構 placeholder，名稱已明示未實作）。在 line 142-148 MockService 區塊上方加註說明同步方向——v1.0 不提供自動跨裝置同步，真正跨裝置資料交換走 v3 canonical 匯出入（book-interchange-v1，everything-as-tags），由匯出/匯入流程承擔，非這些 Mock 服務。避免誤導為已有自動同步。

### 不在本 ticket 範圍

- 兩個 sync test 檔（integration/e2e）未動——驗證確認與生產 service 獨立，無需修改。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 測試指令與結果

**Build 驗證**：`npm run build:dev` → 成功（無 broken import，輸出 [NEXT] 載入步驟）

**回歸測試**：`npm test`（tests/unit + tests/integration）
- Baseline（移除前）：Test Suites 241 passed / 2 skipped；Tests 5577 passed / 41 skipped / 5618 total
- After（移除後）：Test Suites 241 passed / 2 skipped；Tests 5577 passed / 41 skipped / 5618 total
- 結論：移除前後完全一致，零回歸。

**Lint**：`npx eslint data-domain-coordinator.js` → exit 0（零違規，pre-commit hook 可通過）

不需新 RED 測試（死碼清理，以回歸防護為主）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-07
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending

**變更摘要**:
- 移除 src/background/domains/data-management/services/cross-device-sync-service.js（474 行 6-phase stub 死碼）
- coordinator.js 加註 MockService 同步方向說明（指向 v3 book-interchange-v1），保留 5 個架構佔位
- Phase 1 安全閥三證據複驗通過；移除前後 npm test 完全一致（241 suites / 5577 tests passed），零回歸