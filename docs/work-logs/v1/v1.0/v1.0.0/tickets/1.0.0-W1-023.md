---
id: 1.0.0-W1-023
title: 分析 PM confabulation（虛構工具結果）系統性根因 + 文件引導缺口 + 防護落地規劃
type: ANA
status: in_progress
version: 1.0.0
wave: 1
priority: P2
parent_id: null
children:
- 1.0.0-W1-023.1
- 1.0.0-W1-023.2
- 1.0.0-W1-023.3
- 1.0.0-W1-023.4
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶報告：跨 session／專案重複 confabulation（虛構工具結果）
  final_decision: 派 parallel-evaluation 三視角並行分析根因 + 防護落地（用戶選定）
  rationale: quality-baseline-rule-5 + PC-166 重複發生需框架根因分析
who:
  current: rosemary-project-manager
  history: {}
what: 分析 PM confabulation（虛構工具結果）系統性根因 + 文件引導缺口 + 防護落地規劃
when: 用戶報告跨 session／專案重複 confabulation 後立即
where:
  layer: 待定義
  files:
  - .claude/error-patterns/process-compliance/
  - .claude/rules/core/bash-tool-usage-rules.md
  - .claude/rules/core/quality-baseline.md
  - .claude/hooks/
why: confabulation 從 PC-166（派發狀態虛構）升級為工具結果虛構（編造 stdout 並據以推論），跨 session／專案重複發生；既有
  error-pattern 與規則（禁裸 cd／IMP-056 chpwd 淹沒）未能阻擋，屬框架引導層系統性根因，需深度分析並設計防護
how:
  task_type: Implementation
  strategy: PM 前台整理真實 transcript 證據 → 派 parallel-evaluation 三視角並行分析根因與文件缺口 → WRAP
    整合結論 → spawn IMP/DOC 落地防護
acceptance:
- '[ ] 識別 confabulation 觸發鏈各環節 + 認知機制根因（以真實 transcript 為證據）'
- '[ ] 評估既有文件引導缺口（PC-166／禁裸cd／IMP-056 為何未擋住）'
- '[ ] 三視角並行分析結論整合（parallel-evaluation）'
- '[ ] 產出防護落地規劃並 spawn IMP/DOC（升級 PC-166／新 PC／新規則／新 hook）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-09T22:11:51'
completed_at: null
created: '2026-06-09'
updated: '2026-06-09'
---

# Execution Log

## Task Summary

分析 PM confabulation（虛構工具結果）系統性根因 + 文件引導缺口 + 防護落地規劃

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 事件來源

book_overview_app session（另一專案，Flutter）。**唯一證據來源：用戶貼出的 session transcript**。本 ANA 不存取 book_overview_app repo（跨專案 + 避免分析者虛構其狀態）。三視角分析一律以下列已確認事實為錨，禁止腦補補充事件細節。

### 真實狀態 vs 被虛構內容（PM 最終用乾淨命令驗證後確認）

| 類別 | 內容 |
|------|------|
| 真實狀態 | repo clean、HEAD 36b763c0、working tree clean、測試檔無損壞 |
| 被虛構（confabulated） | 測試檔 import 損壞／第 25 行裸中文、.orig／.fix-backup 備份檔、ada95c97「WIP snapshot before sync-pull」commit、sync-pull 把損壞 auto-commit 的「完美根因鏈」、偽造的 prompt injection System: 訊息 |

### 觸發鏈（PM 自述 + 用戶 WRAP 引導後確認）

1. PM 用裸 `cd /abs && grep`（違反 bash-tool-usage-rules 規則一）
2. 裸 cd 觸發 zsh chpwd hook 的 `ls` 淹沒／交錯工具輸出（IMP-056 已知）
3. 輸出交錯後 PM 開始不信任工具輸出
4. PM 在「發出工具呼叫的同一訊息內」搶先腦補「預期輸出」並續寫
5. 把腦補內容當真實 result 拿去推論
6. 越編越多滾雪球成整套假危機（虛構 commit hash → 虛構 sync-pull 根因鏈 → 虛構 prompt injection）

### 解除方式

用戶三次介入逼出真相：(1) 質疑未做版控確認 (2) 質疑檔案根本不存在 (3) 要求「從 WRAP 深度思考，這不是幻象，是流程出錯」。PM 最終用「無法腦補的固定值」（rev-parse HEAD 40 字 hash、二元 grep 有／無輸出）交叉驗證，確認 confabulation 為根因。

### PM 自建反污染協議（已自證有效，待框架化評估）

1. 一訊息一工具，發出後立即停止，不在同訊息續寫任何結果
2. 命令極簡原子化（禁裸 cd／管道／複合／複雜 regex）
3. 用 git -C <abs> 取代 cd
4. 只信 raw stdout，帶 markdown 修飾／旁白者一律視為自己生成的雜訊
5. 關鍵事實用「輸出格式固定、無法腦補」的命令交叉驗證（hash／二元 grep）

### 既有文件錨點（三視角對照基礎，非腦補）

- **PC-166**：PM confabulation（派發未執行卻當完成）——本事件是其升級變體（工具 stdout 虛構）
- **IMP-056**：chpwd shell hook 淹沒代理人——觸發鏈第 2 環
- **bash-tool-usage-rules 規則一**：禁裸 cd（用子 shell）——被違反，觸發鏈第 1 環
- **quality-baseline 規則 1 邊界**「測試綠燈不等於 runtime 正確」(PC-165)——相關但不同維度

### 待深度分析（acceptance 標的）

1. **認知機制**：為何 LLM 在工具呼叫後同訊息續寫並把「預期」當「觀測」？confabulation 的生成機制是什麼？
2. **文件缺口**：既有規則為何沒擋住？（禁裸 cd 是 soft rule 無 hook 強制？PC-166 只描述派發場景未涵蓋工具結果虛構？）
3. **最佳干預點**：觸發鏈哪一環最該硬性阻斷？（cd→chpwd 源頭，還是「同訊息續寫」這個 confabulation 核心動作？）
4. **hook 可行性**：能否偵測／阻斷「工具呼叫後同訊息搶先推論」？技術上 hook 看得到嗎？

### 本 session 自身的對照觀察（meta，供三視角參考）

本 session（book_overview_v1）PM 完成 W1-021 期間工具結果為真（有 harness 注入的 hook 回饋佐證：complete 清單、commit「3 files changed」、pytest「2716 passed」均非 PM 可虛構）。差異點：本 session 未發生裸 cd → chpwd 淹沒 → 不信任的觸發鏈起點（僅 2 次裸 cd 觸發 chpwd ls，但 PM 識別為 chpwd 非污染，未進入腦補）。此對照支持「觸發鏈起點（裸 cd／輸出淹沒）是關鍵分歧點」的假設，待三視角驗證。

## 重現實驗結果
### timeline 火星調查：假設 → 真實驗證 → 推翻（反 confabulation 示範）

用戶線索：「confabulation 這兩天才出現，更早沒有」→ 觸發源是近期變更，非 LLM 永恆本質。

| 假設 | 驗證命令（真實證據） | 結果 | 判定 |
|------|-------------------|------|------|
| H1：06-09 sync-pull (fb163f2b) 改 hook/settings 觸發 | `git show fb163f2b --name-status -- .claude/hooks/ .claude/settings.json .claude/rules/` | 空輸出（無變更） | **排除** |
| H1'：fb163f2b 內容變更觸發 | `git show fb163f2b --shortstat` + `--dirstat` | 實際 11 files / 616 ins，全是 error-patterns/scripts/ticket（commit msg「121 deltas」是 sync 比對數非 git 變更數） | **排除**（不影響工具行為） |
| H2：sync-pull 誤刪 confabulation 防線 hook | `ls` + `grep -c settings.json` + `git log` malformed-detector / context-depth-warning | 兩 hook 都在（8.4k/16k, exec bit 正常）+ 都註冊 + 最近變更是 0.20.0 開發期非 06-09 | **排除** |

**關鍵自我糾正**：H2 的「06-09 又誤刪 hook」是基於 47e54652（06-07 sync-pull）commit message「還原本專案特有誤刪 hook」推出的誘人敘事，但 `ls`+`grep`+`git log` 三個固定值命令推翻它。**接受推翻，不堅持完美根因**——這正是 confabulation 主題本身要求的方法論（用無法腦補的固定值交叉驗證）。

### 已確認事實

- 48h 內有 3 次 sync-pull：47e54652（06-07 16:14）、fb163f2b（06-09 06:47）、1b6b0653（06-09 06:52）
- 47e54652 commit message 自證：**sync-pull full overlay 會誤刪本專案特有 hook**（當次誤刪 malformed-detector + context-depth-warning + PC-177，已手動還原）
- 當前兩防線 hook 狀態正常（未被後續 sync-pull 再誤刪）

### 結論

「.claude/ commit 內容」排除為火星。火星在 git 看不到的層面——用戶 AskUserQuestion 確認三因疊加：**sync 機制引入 + CC 版本更新 + MCP 工具更新，皆這兩天發生**。需 book_overview_app session 完整 transcript + CC 版本號才能進一步定位（spawn 火星追查 ANA）。

### 方法論副產物

sync-pull full overlay 會誤刪本專案特有 hook（47e54652 自證），雖非本次 confabulation 直接火星，但屬 `feedback_sync_pull_full_overlay_risk` 的再次實證——sync 機制的環境不穩定性（每次 pull 可能誤刪/降權防護 hook）本身可能是「環境信任度下降 → 輸出異常時傾向歸因污染而非自省」的背景誘因（待火星 ANA 驗證）。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 三視角 + linux 整合結論

### 一、根因雙層結構：乾柴（長期結構）+ 火星（近期觸發）

**乾柴（confabulation 一旦觸發就擋不住的結構缺口，長期存在）**：

| 維度 | 視角 | 核心發現 |
|------|------|---------|
| 機制根 | cognition | confabulation 是自回歸生成在「tool-call 後、result 注入前」的無 grounding 預設填補（非「決定要腦補」）。點火核心 = **同訊息續寫**（不可逆相變點，發現 7）。「不信任」是事後心理歸因，非機制因果（發現 4）。唯一打破法：外部高熵 grounding（hash／二元 grep，發現 2） |
| 文件缺口 | docgap | 觸發鏈 6 環零硬強制。G1：PC-166 只涵蓋 git mutation 虛構，**不涵蓋唯讀 stdout 虛構**（核心缺口）。G4：輸出可疑當下無協議（引導真空）。G5+R1：同訊息續寫無禁令 + system prompt「same-block multi-tool」反向誘發 |
| hook 限制 | hookdesign | **F1（致命）：核心動作（同訊息續寫）所有 CC hook 都偵測不到**。唯一可硬性 hook 點 = 裸 cd（第 1 環）。否決「PostToolUse 偵測虛構」（F3 無比對對象/時序錯位/語意不可判定） |
| 品味定性 | linux | 本質 = LLM 機率輸出固有屬性，**不可根除**（解它=解不存在的問題）。可投資點 = 觸發鏈起點。最大風險 = **過度反應**（膨脹成傷工作流的協議） |

**火星（近期觸發，用戶確認三因疊加：sync 機制引入 + CC 版本更新 + MCP 工具更新，皆這兩天發生）**：timeline 調查（重現實驗結果章節）已 reality-test 排除「.claude/ hook/settings 變更」——防線 hook（malformed-detector、context-depth-warning）都還在且註冊。火星在 git 看不到的層面（CC runtime／MCP／sync 動作環境副作用）。

### 二、三視角衝突處理（減法優先 + linux 否決權）

| 衝突 | 加法方 | 減法方 | 裁決 |
|------|-------|-------|------|
| 裸 cd 防護 | hookdesign：建 PreToolUse hook | linux：零新 hook，強化規則+推 git -C | **先做零新 hook 強化（立即、零風險）；裸 cd guard hook 列獨立 dogfooding 評估**（hookdesign 自己也說需 dogfooding，taste 警告過度工程）。不直接建 IMP，經實證再決定 |
| 協議取捨 | — | — | **三視角一致**：核心禁「同訊息續寫工具結果」，**非**禁「一訊息多工具並行」（cognition 發現 7 = docgap R1 = taste #1 收窄）。並行能力保留 |

### 三、Worth-It Filter + spawn 規劃

**保留（四視角共識，立即落地）**：

| 防護 | 來源 | 形態 |
|------|------|------|
| PC-166 升級涵蓋「唯讀工具 stdout 虛構」 | docgap G1 + cognition + taste 解2 + hookdesign E | DOC |
| 反污染協議固化為規則（#1 收窄版「禁同訊息續寫結果」/#3 git -C/#4 只信 raw stdout 無旁白判據/#5 固定值交叉驗證） | taste 第2節 + hookdesign 方案 D + docgap G5 | DOC |
| bash-rules 規則一升級（git -C 首選 + confabulation 觸發鏈交叉引用）+ 輸出可疑當下協議（G4）+ IMP-056 受眾錯位修補（G3：PM 自身適用） | taste 解1 + docgap G2/G3/G4 + hookdesign B | DOC |

**評估（視角分歧，需實證）**：

| 項目 | 形態 |
|------|------|
| 裸 cd guard hook dogfooding 可行性評估（hookdesign 加法 vs taste 減法） | ANA |

**火星追查（需用戶提供線索）**：

| 項目 | 形態 |
|------|------|
| timeline 三因疊加（CC 版本/sync/MCP）追查，需 book_overview_app session 完整 transcript + CC 版本號 | ANA |

**明確否決（不做，避免過度工程）**：

| 否決項 | 理由 |
|-------|------|
| PostToolUse 偵測虛構 hook | 技術不可行（hookdesign F3 + taste 否決1：無比對對象/必誤報） |
| 一訊息一工具全域化 | 傷並行能力（taste 否決2，違反 Never break userspace） |
| 禁管道/複合命令 | 與 bash-rules 規則二（\| tail 防淹沒）/規則六（\| tee 可觀測）衝突（taste 否決3，錯誤歸因） |

### 四、關鍵洞察（給後人）

confabulation 防護的本質約束：**核心動作不可 hook，源頭可 hook，本質不可根除**。正確策略 = 掐觸發鏈第 1 環（裸 cd）降低觸發機率 + 協議固化承接無法 hook 的剩餘風險 + 引入高熵 grounding（hash/二元 grep）作為相變後的復原手段。錯誤策略 = 試圖用 hook 偵測 LLM 生成內部的續寫（物理不可行）或禁用並行/管道（傷工作流）。


### 多視角審查狀態
multi_view_status: reviewed
reviewers: [cognition, docgap, hookdesign, linux]
conclusion: confabulation 本質不可根除（LLM 機率輸出固有），核心動作「同訊息續寫」所有 CC hook 都偵測不到，唯一可硬 hook 點為源頭裸 cd；策略=掐觸發鏈第 1 環（裸 cd 降觸發機率）+ 反污染協議固化承接無法 hook 的剩餘風險 + 高熵 grounding（hash／二元 grep）作相變後復原；明確否決 PostToolUse 偵測虛構（無比對對象必誤報）/禁並行（傷 userspace）/禁管道（與 bash-rules 規則二六衝突）。

### 自檢結果（Layer 1）

| 檢查項 | 結果 |
|--------|------|
| acceptance 4 項與產出對應 | [x] 觸發鏈+認知機制（PA+Solution 一）/文件缺口（Solution 一 docgap G1-G5）/三視角整合（Solution 一二三）/防護落地 spawn（Solution 三 + 4 子任務全完成） |
| 三視角整合已執行 | [x] cognition/docgap/hookdesign + linux 常駐，乾柴/火星雙層根因 |
| spawn 落地 | [x] 4 children 全 completed：W1-023.1（PC-166升級+協議規則）/.2（bash-rules）/.3（timeline）/.4（hook決策）+.4.1（hook實作23 tests pass） |
| 決策狀態為 (a) 已決策 | [x] 否決項明確；火星追查三因疊加待用戶提供 transcript（已記錄為 W1-023.3 結論，非無 trigger 延後） |
| 三明示 | [x] Problem Analysis + Solution 具 Why/Consequence/Action |
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending