---
id: 1.0.0-W1-088
title: 設計 下游 semver + pin 版本拆解爆炸半徑（C3）
type: ANA
status: in_progress
version: 1.0.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.0.0-W1-085
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:所有發現必須追蹤
  final_decision: 設計版本 pin 機制拆爆炸半徑，與 live-edit 取捨並陳
  rationale: quality-baseline-rule-5
who:
  current: rosemary-project-manager
  history: {}
what: 設計 下游 semver + pin 版本拆解爆炸半徑（C3）
when: P0/P1 落地後（P1，需設計）
where:
  layer: 待定義
  files:
  - .claude/scripts/sync-claude-push.py
  - .claude/scripts/sync-claude-pull.py
why: 現行 sync-pull 直拉 main HEAD，框架壞變更瞬時傳染全部 13 個 consumer；下游 pin 版本可使崩潰不再全量瞬時
how:
  task_type: Analysis
  strategy: 評估 semver tag 方案 + pull 端 pin（拉已發版而非 main HEAD）對既有三方合併 base SHA 錨點的衝擊與遷移路徑
acceptance:
- '[ ] 產出 semver+pin 方案設計含對 sync-pull base 錨點衝擊評估'
- '[ ] 至少 2 個遷移路徑（漸進 pin / 全面 pin）取捨對比'
- '[ ] 結論明確採用或否決並 spawn 後續 IMP'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-16T13:15:18'
completed_at: null
created: '2026-06-16'
updated: '2026-06-16'
---

# Execution Log

## Task Summary

設計 下游 semver + pin 版本拆解爆炸半徑（C3）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 展開設計（版本 pin 拆爆炸半徑 C3）

### 現行機制（爆炸半徑根源，已讀碼確認）
sync-pull target 永遠是 remote/main HEAD；per-consumer 只記 last_synced_base_sha 作三方合併 base。push 不打 tag、VERSION（v1.48.5）已算但被 pull 忽略。→ 壞 HEAD 下次 pull 即傳染全 consumer。

### Pin 設計（option A：tag-based，最小正確）
1. push 對每版打 git tag（VERSION 已算，僅加 tagging，低成本）。
2. consumer .sync-state.json 加 pinned_version 欄（如 v1.48.5 或 latest）。
3. sync-pull target 改為 pinned tag commit（非 HEAD）；delta = base→pinned-tag。
4. bump：顯式 sync-pull --bump [version] 更新 pin 至 latest/指定版，重跑合併。
5. 預設 latest（=現行 HEAD 行為，向後相容）；pin 特定版為 opt-in。

### 爆炸半徑效果
壞 change push 到 HEAD（tag v1.48.6）不會碰到 pin 在 v1.48.5 的 consumer，直到其 --bump。consumer 刻意 bump、可查 CHANGELOG，壞版可拒升。「全 consumer 瞬時」→「選擇 bump 到壞版者」。

### 與 W1-087(C1 smoke test) 防禦縱深
C1=防壞 change 被 push（生產閘）；C3=壞 change 漏過時限爆炸半徑（分發閘）。縱深：罕見壞 push × 刻意更新 = 幾乎不會瞬時炸 consumer。

### 成本/取捨
- 傳播變慢：改進只在 bump 時到 pin 住的 consumer（刻意非自動）。單人框架若要快傳播是摩擦——latest pin=現行行為 緩解（pin 為 opt-in）。
- sync-pull 複雜度：target ref 解析（tag vs HEAD）+ bump 命令 + base 錨點互動，中等。
- live-edit 不受影響：pin 只管「消費端 pull 哪版」，push/熱改照常。

### 遷移（漸進、向後相容）
P1 push 開始 tagging（consumer 零改）→ P2 sync-pull 支援 pinned_version + --bump（預設 latest=現行）→ P3 consumer 按需 opt-in pin 特定版。

### 根決策關鍵refinement
W1-088(tagged-release+pin) 與 W1-090(GitHub Issues) 是**可分離的兩個升級**，成本剖面差很大：
- W1-088 只需 git tags + pin-aware pull——純 git、離線可、不離開現有 overlay 模型。**較輕、較易採納**。
- W1-090 需 GitHub Issues——平台特性、網路依賴。
故「根決策」非單一「升不升級正式上游」，而是兩個獨立定價的選擇。

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.0.0-W1-085 | chars: 237 -->

### Task Reference
- 1.0.0-W1-085 what: 分析 框架跨專案治理策略：識別符衛生+框架issue溯源+sync-push審查閘門

### Rationale Chain
- 1.0.0-W1-085 why: 框架 code 嵌 project-local ticket ID 跨專案斷源/偽源；不完整重構零審查直推所有 consumer 致崩潰

### Related Files
- .claude/references/reference-stability-rules.md  # from 1.0.0-W1-085

### Source Acceptance (reference)
- [ref] [ ] 五委員審查報告彙整完成  # from 1.0.0-W1-085
- [ref] [ ] 三層方案盲點與替代方案釐清  # from 1.0.0-W1-085
- [ref] [ ] Worth-It Filter 分類並對採用項 spawn IMP/DOC ticket  # from 1.0.0-W1-085
