---
id: 1.0.0-W4-007
title: 移除 v2 JSON 匯出 UI（v3 為唯一 JSON 匯出入口）
type: IMP
status: in_progress
version: 1.0.0
wave: 4
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo:
- 1.0.0-W4-001
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 用戶 reality-test:v2 匯出必要性質疑
  final_decision: 移除 v2 匯出 UI，v3 canonical 為唯一 JSON 匯出；import 保留向後相容讀 v2
  rationale: quality-baseline-rule-5
who:
  current: thyme-extension-engineer
  history: {}
what: 移除 v2 JSON 匯出 UI（v3 為唯一 JSON 匯出入口）
when: v1.0.0 W4，W4-001 後續設計修正（reality-test）
where:
  layer: 待定義
  files:
  - src/overview/overview.html
  - src/overview/overview-page-controller.js
  - tests/unit/overview/overview-export-v2.test.js
  - tests/unit/overview/overview-page-controller.test.js
  - tests/e2e/browser/uc03-export.e2e.test.js
why: W4-001 採方案 B 保留 v2 鈕；用戶 reality-test（2026-06-07）確認 v2 匯出無前向消費者：spec book-interchange-v1
  已 deprecate v2（對 APP everything-as-tags 多值資料有損）、跨專案同步走 v3、spreadsheet 受眾走 CSV、舊檔回讀走
  import（四來源向後相容，不需 v2 匯出）。保留 v2 鈕屬冗餘且誘導用戶誤用對 APP 有損的格式。
how:
  task_type: Implementation
  strategy: 移除 overview.html exportJSONv2Btn + controller click 綁定 + handleExportJSONv2（確認無
    caller）；還原 W4-001 為 v2 鈕改的 UI 測試；不動 import 與 exporter v2 lib
acceptance:
- '[ ] 移除 exportJSONv2Btn 按鈕（overview.html）+ click 綁定 + handleExportJSONv2 方法（確認無其他
  caller 後移除，否則保留並註記）'
- '[ ] 主「匯出 JSON」維持 v3 canonical（不回歸）'
- '[ ] import 向後相容讀 v2 不變（不改 import 端）'
- '[ ] 還原/移除 W4-001 為 v2 鈕新增的 UI 測試（overview-export-v2.test.js / uc03-export.e2e.test.js）；v2
  export 改由 exporter-lib 層測試覆蓋'
- '[ ] npm test 全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-07T13:21:58'
completed_at: null
created: '2026-06-07'
updated: '2026-06-07'
---

# Execution Log

## Task Summary

移除 v2 JSON 匯出 UI（v3 為唯一 JSON 匯出入口）

---

## Context Bundle

### 移除 v2 匯出 UI（src，PM 前台已驗證錨點）
- src/overview/overview.html line 35：移除 `<button id="exportJSONv2Btn">匯出 JSON (v2 相容)</button>`（line 34 主 `exportJSONBtn`「匯出 JSON」= v3，保留不動）
- src/overview/overview-page-controller.js：
  - line 214：buttons 陣列移除 `'exportJSONv2Btn'`
  - line 304-306：移除 exportJSONv2Btn 的 click 綁定（保留 line 299 exportJSONBtn → handleExportJSONv3）
  - line 975：移除 `handleExportJSONv2` 方法（caller 已驗證：src 僅 line 306 click 綁定，移除綁定後無 src caller）
  - 保留：handleExportJSONv3 (line 941)、exportJSONBtn → v3 (line 299)

### 測試還原（移除 v2 鈕後必然失敗，須同步處理）
- tests/unit/overview/overview-export-v2.test.js（328 行）：整檔測 handleExportJSONv2（W4-001 新增），還原/移除
- **tests/unit/overview/overview-page-controller.test.js（PM 發現 ticket 原漏列，已補 where.files）**：line 1462-1669 有 v2 鈕 test group（含「acceptance 3：點擊 exportJSONv2Btn 仍走 v2」、line 1509 兩鈕 DOM）。移除此 v2 group，保留 exportJSONBtn(v3) 相關測試
- tests/e2e/browser/uc03-export.e2e.test.js（346 行，tests/e2e/ 不在 npm test 範圍）：還原 W4-001 改動

### 不動（向後相容）
- import 端（讀 v2 向後相容不變）
- exporter v2 lib（v2 export 改由 lib 層測試覆蓋，不刪 lib）

### acceptance 對應
- AC1 移除 v2 鈕+綁定+方法 / AC2 主匯出 JSON 維持 v3 不回歸 / AC3 import 不動 / AC4 還原 3 測試檔 / AC5 npm test 全綠

### 範圍發現（PM 前台 grep 2026-06-07）
ticket where.files 原列 export-v2.test.js + uc03-export.e2e.test.js，**漏列 overview-page-controller.test.js**（其 line 1462+ 有 v2 group）。已補 where.files。

### 邊界
- 不擴及 import / exporter v2 lib
- 若發現其他 src 檔依賴 handleExportJSONv2，停止並 NeedsContext 回報 PM

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 移除範圍（src）

| 檔案 | 變更 |
|------|------|
| src/overview/overview.html | 移除 `<button id="exportJSONv2Btn">匯出 JSON (v2 相容)</button>`（line 35） |
| src/overview/overview-page-controller.js | buttons 陣列移除 `'exportJSONv2Btn'`；移除 exportJSONv2Btn click 綁定；移除 `handleExportJSONv2` 方法（35 行） |

保留不動：`handleExportJSONv3`（v3 唯一 JSON 入口）、exportJSONBtn → v3 綁定。

### 邊界保留決策（CSV v2 不刪）

grep 驗證 `handleExportCSVv2`（src line 969）仍由 exportCSVBtn 觸發，且依賴 `_loadTagData`（line 1020）與 `EXPORT_V2` 常數。本 ticket 僅移除 **JSON v2**，CSV v2 為 spreadsheet 受眾主要格式（reality-test 確認 CSV 受眾走 CSV），保留 `handleExportCSVv2` / `_loadTagData` / `EXPORT_V2` / `handleExportCSVv2` 測試。section comment header 改為「v2 CSV 匯出方法」並註記 JSON v2 已於 W4-007 移除。

### 測試處理（3 檔）

| 檔案 | 處理 |
|------|------|
| tests/unit/overview/overview-export-v2.test.js | 移除 `handleExportJSONv2` describe（4 案）+ exportJSONv2Btn DOM + JSON v2 click 測試；selection-aware 由 JSON v2 改點 `handleExportCSVv2`（驗 id 欄而非全文，避免 cover URL 含 book-001 字面誤判）；保留 CSV v2 全部測試 |
| tests/unit/overview/overview-page-controller.test.js | Group H 移除 exportJSONv2Btn DOM（line 1509）+「acceptance 3：點擊 exportJSONv2Btn 仍走 v2」測試；保留全部 v3 canonical 測試（不回歸） |
| tests/e2e/browser/uc03-export.e2e.test.js | G2 JSON group 由點 exportJSONv2Btn（v2 斷言）改點 exportJSONBtn（v3 canonical 斷言：format=book-interchange-v1 / formatVersion=3.0.0 / book.tags everything-as-tags）；移除 unused `V2_ROOT_SECTIONS`；G3 CSV group 不變 |

### 設計判斷說明

ticket Context Bundle 寫「整檔 v2 測試還原/移除」與「還原 W4-001 e2e 改動」，但機械還原會與現行 src 行為衝突：
- export-v2.test.js 含 CSV v2 測試（測 live 的 handleExportCSVv2），整檔刪會誤刪 live 方法覆蓋 → 改為僅刪 JSON v2 部分。
- e2e 機械還原 = exportJSONBtn 斷言 v2，但 exportJSONBtn 現產 v3（W4-001 後）→ 改為 exportJSONBtn 斷言 v3。
判斷依據：v3 不回歸（AC2）+ CSV v2 為 live src（不在移除範圍）。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 建置
- `npm run build:dev` 成功（esbuild 三入口 bundle，無錯誤）

### Lint
- `npx eslint src/overview/overview-page-controller.js tests/unit/overview/overview-export-v2.test.js tests/unit/overview/overview-page-controller.test.js tests/e2e/browser/uc03-export.e2e.test.js` → exit 0（0 error / 0 warning）

### 受影響測試檔
- `npx jest tests/unit/overview/overview-export-v2.test.js tests/unit/overview/overview-page-controller.test.js` → 2 suites passed, 70/70 tests passed

### 全套件（AC5）
- `npm test` → Test Suites: 241 passed / 2 skipped；Tests: 5578 passed / 0 failed / 41 skipped（skipped 為既有，非本 ticket 引入）

### e2e（不在 npm test 範圍）
- tests/e2e/browser/uc03-export.e2e.test.js 經 build + lint 驗證；需真實 Chrome 環境（npm run test:e2e:browser）執行，不在本 ticket commit 驗證範圍（與 ticket 定位一致）

### acceptance 對應
- AC1 移除 exportJSONv2Btn + click 綁定 + handleExportJSONv2（grep 確認 src 無殘留 caller）
- AC2 主匯出 JSON 維持 v3（Group H v3 canonical 測試全綠，未回歸）
- AC3 import 端未觸碰
- AC4 3 測試檔已還原/調整（JSON v2 測試移除，CSV v2 保留，e2e 改 v3 斷言）
- AC5 npm test 全綠

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-07
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending（待 PM 驗證後合併 feat/1.0.0-W4-007-remove-v2-export-ui）

**摘要**: 移除 v2 JSON 匯出 UI（exportJSONv2Btn 鈕 + click 綁定 + handleExportJSONv2 方法），v3 canonical 成為唯一 JSON 匯出入口。3 測試檔同步調整（JSON v2 測試移除、CSV v2 測試保留、e2e G2 改 v3 斷言）。CSV v2 路徑（handleExportCSVv2）與 import 端不動。npm test 5578 passed / 0 failed；lint 0；build:dev 成功。