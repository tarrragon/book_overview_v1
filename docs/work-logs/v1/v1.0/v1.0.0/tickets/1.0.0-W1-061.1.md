---
id: 1.0.0-W1-061.1
title: 實作 ticket CLI 效能測試 pytest -m perf 標記隔離（W1-061 方案 A）
type: IMP
status: in_progress
version: 1.0.0
wave: 1
priority: P2
parent_id: 1.0.0-W1-061
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: saffron-system-analyst
  history: {}
what: 實作 ticket CLI 效能測試 pytest -m perf 標記隔離（W1-061 方案 A）
when: W1-061 ANA 選定方案 A 後，排入當前 wave 批次 3 派發
where:
  layer: .claude/skills/ticket/tests/test_performance.py,.claude/skills/ticket/pyproject.toml
  files:
  - .claude/skills/ticket/tests/test_performance.py
  - .claude/skills/ticket/pyproject.toml
why: test_E1_checkpoint_state_performance_median_under_100ms 以 100ms median 為 pass-fail
  門檻，全套件高負載下觀察到 1 次 fail、單獨取樣 5/5 pass（PC-168 取樣記錄：GREEN 5 / RED 0 standalone，full-suite
  GREEN 1 / RED 1）——負載敏感的計時硬門檻在主套件中違反 test-assertion-design 原則（該規則檔以 Jest 為標的，Python
  套件無對應條款，屬規則覆蓋缺口）
how:
  task_type: Implementation
  strategy: test_E1_* 加 @pytest.mark.perf；pyproject.toml 註冊 marker + addopts 預設排除；package.json
    加 test:hooks:perf script；測試檔頭加效能聲明（詳見 parent W1-061 Solution 實作規劃 4 步）
acceptance:
- '[x] test_performance.py 的 test_E1_* 加 @pytest.mark.perf 裝飾器'
- '[x] pyproject.toml 註冊 perf marker 並 addopts 預設排除（-m ''not perf''）'
- '[ ] package.json 新增 test:hooks:perf script 且可獨立執行 perf 測試'
- '[x] 預設套件執行不再包含計時門檻測試（驗證：預設 pytest 不收 test_E1_*）'
- '[x] 測試檔頭加效能測試聲明（仿 Jest tests/perf/ 標準）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-11T22:12:49'
completed_at: null
created: '2026-06-11'
updated: '2026-06-11'
---

# Execution Log

## Task Summary

實作 ticket CLI 效能測試 pytest -m perf 標記隔離（W1-061 方案 A）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 實作內容（W1-061 方案 A）

| 檔案 | 變更 |
|------|------|
| `.claude/skills/ticket/tests/test_checkpoint_state/test_performance.py` | (a) `test_E1_checkpoint_state_performance_median_under_100ms` 加 `@pytest.mark.perf` 裝飾器；(b) 檔頭加效能測試聲明（仿 test-assertion-design-rules 規則 2 的 Jest tests/perf/ 檔頭標準：標明計時硬門檻定位為退化防護、PC-168 取樣記錄、執行方式 `pytest -m perf`） |
| `.claude/skills/ticket/pyproject.toml` | `[tool.pytest.ini_options]` 註冊 `markers = ["perf: ..."]` + `addopts = "--verbose -m 'not perf'"`（預設排除 perf 標記，附 Why 註解） |

### 設計決策

- ticket where.files 寫 `tests/test_performance.py`，實際檔案位於 `tests/test_checkpoint_state/test_performance.py`（grep 確認 test_E1_* 唯一所在），以實際路徑為準。
- addopts 保留既有 `--verbose`，附加 `-m 'not perf'`（pytest 以 shlex 解析，單引號正確處理空白）。
- marker 採邏輯標記隔離（非物理拆目錄），與 parent W1-061 Solution 結論一致：Python 1 個效能測試不構成拆目錄理由，`@pytest.mark.perf` 與 Jest tests/perf/ 等效。

### 不在本 ticket 範圍（PM 接手）

- repo 根目錄 `package.json` 新增 `test:hooks:perf` script（AC3）——本代理人被約束不碰 repo 根目錄檔案。

Commit: 254338ac

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 雙向驗證（皆於 .claude/skills/ticket 子 shell 執行，uv run --project . --extra test）

**方向 1：預設不收（test_E1 deselect、其餘全綠）**

```
$ pytest tests/test_checkpoint_state/
======================= 68 passed, 1 deselected in 0.62s =======================

$ pytest   # 全套件（testpaths = tests + ticket_system/tests）
===================== 2449 passed, 1 deselected in 12.06s ======================
```

**方向 2：perf 可收（test_E1 執行並通過）**

```
$ pytest tests/test_checkpoint_state/ -m perf -v
collected 69 items / 68 deselected / 1 selected
tests/test_checkpoint_state/test_performance.py::test_E1_checkpoint_state_performance_median_under_100ms PASSED [100%]
======================= 1 passed, 68 deselected in 0.61s =======================
```

deselect 數量交叉驗證：方向 1 deselect 恰 1（= test_E1）、方向 2 select 恰 1 且為 test_E1，兩向互補無遺漏。

Commit: 254338ac

## NeedsContext
- **缺失項**: AC3「package.json 新增 test:hooks:perf script」未執行
- **觸發位置**: repo 根目錄 package.json（派發約束：不碰 repo 根目錄任何檔案）
- **影響**: AC3 留空未勾選
- **建議補料**: 由 PM 接手新增 script（parent W1-061 Solution 規劃步驟 3）：`"test:hooks:perf": "uv run --project .claude/skills/ticket --extra test pytest tests/test_checkpoint_state/ -m perf -v"`。注意 parent 規劃原文寫 `--project .claude/hooks pytest .claude/skills/ticket/tests/`，但 perf marker 註冊於 .claude/skills/ticket/pyproject.toml，須以該 project 為準（.claude/hooks 環境讀不到此 marker 設定且 addopts 不生效），且需 `--extra test` 拉入 pytest dev deps
- **重派成本**: 無需重派；PM 1 個 Edit + 1 次驗證指令即可

## Exit Status
```yaml
status: partial_success
reason: "範圍內兩檔實作完成並雙向驗證通過（commit 254338ac）；AC3 npm script 屬 repo 根目錄，依派發約束留 PM 接手（詳 NeedsContext）"
confidence: 0.97
acceptance_met: [1, 2, 4, 5]
acceptance_unmet: [3]
artifacts:
  - .claude/skills/ticket/tests/test_checkpoint_state/test_performance.py
  - .claude/skills/ticket/pyproject.toml
context_dependencies:
  - 1.0.0-W1-061 Solution（方案 A 實作規劃）
  - .claude/rules/core/test-assertion-design-rules.md（規則 1-2 檔頭標準）
estimated_recovery_effort: "PM 1 Edit（package.json script）+ 1 驗證指令"
```

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending