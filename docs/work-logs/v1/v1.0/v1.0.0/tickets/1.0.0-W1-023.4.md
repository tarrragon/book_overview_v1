---
id: 1.0.0-W1-023.4
title: 評估 裸 cd guard PreToolUse hook dogfooding 可行性（hookdesign 加法 vs taste 減法分歧）
type: ANA
status: in_progress
version: 1.0.0
wave: 1
priority: P2
parent_id: 1.0.0-W1-023
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager
  history: {}
what: 評估 裸 cd guard PreToolUse hook dogfooding 可行性（hookdesign 加法 vs taste 減法分歧）
when: DOC W1-023.2 規則層強化落地後
where:
  layer: 待定義
  files:
  - .claude/hooks/
  - .claude/rules/core/bash-tool-usage-rules.md
why: hookdesign 視角主張裸 cd guard hook（soft rule 已證失效需 hook 強制），taste 視角主張零新 hook（過度工程風險）；需
  dogfooding 掃本專案既有裸 cd 殘留實證決定是否值得建 hook
how:
  task_type: Implementation
  strategy: grep 本專案近期裸 cd 違規數 → 評估規則強化（W1-023.2）後殘留率 → 殘留高則建 warn 級 hook，低則維持零新 hook（linux
    減法）
acceptance:
- '[ ] dogfooding 統計本專案近期裸 cd 違規數與規則強化後殘留率'
- '[ ] 決策：建 warn 級裸 cd hook（含成本估算）或維持零新 hook，結論寫入'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-09T23:54:49'
completed_at: null
created: '2026-06-09'
updated: '2026-06-09'
---

# Execution Log

## Task Summary

評估 裸 cd guard PreToolUse hook dogfooding 可行性（hookdesign 加法 vs taste 減法分歧）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 決策：建 warn 級裸 cd guard hook（用戶定奪）

### Dogfooding 證據（活體自證為最強）

本 session PM 讀過 bash-rules 規則一（禁裸 cd）+ 正在固化 tool-output-trust 規則 4（git -C）+ 整個任務寫 confabulation 防護，**仍裸 cd 至少 2 次觸發 chpwd ls 淹沒**。連寫規則的 PM 都違反 → soft rule 對「當下行為」無效（鐵證）。

**測量限制**：「跨 session 裸 cd 違規數」無 git 痕跡無法統計（裸 cd 是 Bash 命令不進 git），acceptance [0] 此部分合理漂移，用本 session 活體自證替代。W1-023.2 規則強化剛落地，殘留率需後續觀察；本 session（強化前基線）已證 soft rule 無效。

### 決策權衡與結論

| 視角 | 主張 | dogfooding 對照 |
|------|------|----------------|
| hookdesign（加法）| 裸 cd 唯一可硬 hook 點，soft rule 失效需 hook | 活體自證支持 |
| taste（減法）| 零新 hook | 未否決裸 cd hook（否決的是 hook 偵測續寫/一訊息一工具/禁管道），偏好被 dogfooding 推翻 |

**結論：建 warn 級裸 cd guard hook**。理由：dogfooding 鐵證 soft rule 失效 + warn 不 deny 不傷工作流（裸 cd 本就該避免，提示改 git -C）+ taste 未否決此 hook。

### Hook 設計要點（供 IMP 落地）

- 類型：PreToolUse Bash hook（UV single-file，含 shebang + chmod +x，PC-086）
- 偵測：`tool_input.command` 含裸 cd（`^cd ` 或 `&& cd ` / `; cd `），**排除**子 shell `(cd ...)`、`git -C`、`uv -d`、絕對路徑還原 `cd /project/root`
- 行為：**warn**（permission_decision="allow" + reason 提示改 git -C / 子 shell），**不 deny**（不傷工作流）
- 可觀測性：reason 寫 stderr（規則 4）
- 註冊：settings.json PreToolUse matcher Bash（只註冊一處，PC-148）
- 範例參照：error-pattern flat gate hook（PreToolUse + 模式 filter + emit_hook_output）

multi_view_status: n_a
multi_view_reason: 本 ANA 為決策評估（裸 cd hook 建/不建），以 dogfooding 活體自證 + hookdesign/taste 分歧裁決，由用戶定奪；母 ANA（1.0.0-W1-023）已含 parallel-evaluation 四視角，本 ANA 不重複多視角審查。

### Spawn 落地確認

決策「建 hook」→ spawn IMP child（建 warn 級裸 cd guard hook + pytest）。

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending