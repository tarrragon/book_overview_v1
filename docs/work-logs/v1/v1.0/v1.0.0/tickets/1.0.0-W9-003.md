---
id: 1.0.0-W9-003
title: install 修復指令改免 cd 絕對路徑 uv-tool-staleness-check-hook
type: IMP
status: in_progress
version: 1.0.0
wave: 9
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: PowerShell 5.1 無 && 致安裝指令貼上失敗
  final_decision: 改免 cd 絕對路徑 uv tool install <path>
  rationale: uv tool install <path> 不需 cd 跨平台一致（issue 實測 6 skill 安裝成功）
who:
  current: rosemary-project-manager（PM 直接執行，PC-115）
  history: {}
what: format_results 的修復/安裝指令由 cd X && uv tool install . 改為 uv tool install "<abs
  path>"
when: W9 wave，可與其他 IMP 並行（獨立 hook）
where:
  layer: 待定義
  files:
  - .claude/hooks/uv-tool-staleness-check-hook.py
why: 'issue #1 問題3：PowerShell 5.1 不支援 && chain operator，使用者複製 cd X && uv tool install
  . 得 parser error'
how:
  task_type: Implementation
  strategy: 第205/210行改用 uv tool install "{project_root / source_subpath}" 絕對路徑形式，免
    cd、PowerShell/bash/zsh 皆可直接執行
acceptance:
- '[ ] format_results 修復/安裝指令改絕對路徑免 cd 形式'
- '[ ] 輸出指令在 macOS 實跑可成功安裝 skill'
- '[ ] hook 既有測試（若有）全綠、session 啟動輸出正常'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-15T23:41:34'
completed_at: null
created: '2026-06-15'
updated: '2026-06-15'
---

# Execution Log

## Task Summary

format_results 的修復/安裝指令由 cd X && uv tool install . 改為 uv tool install "<abs path>"

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 修復實作（framework issue #1 問題3）

### 變更檔案
- .claude/hooks/uv-tool-staleness-check-hook.py：
  - format_results(results) → format_results(results, project_root)，main 傳入既有 get_project_root()。
  - OUTDATED 修復指令：cd {source_subpath} && uv tool install . --force --reinstall → uv tool install "{project_root / source_subpath}" --force --reinstall。
  - MISSING 安裝指令同改絕對路徑免 cd 形式。
  - docstring + source_subpath 註解同步更新（移除 cd 目標描述）。

### caller/test 同步（PC-135）
test_uv_tool_staleness_check_hook.py 3 處 format_results 輸出斷言改新形式（258 ticket OUTDATED / 305 MISSING / 577 version-release OUTDATED），各加 assert "&&" not in msg 驗 PowerShell 相容。test_skill_md_files_contain_reinstall_warning（473）測 SKILL.md 內容非 hook 輸出，不受影響、未改。

### 自檢結果（Layer 1）
- [x] 測試通過率：23/23 全綠
- [x] Runtime 驗證：live hook 輸出 uv tool install "/abs" --force --reinstall，無 cd/&&
- [x] caller-sync：format_results 唯一 caller（main）已同步傳 project_root；3 測試斷言同步
- [x] PowerShell 相容：免 cd 絕對路徑，bash/zsh/PowerShell 皆可直接執行
- [x] 引用一致性：docstring + 欄位註解同步移除 cd 描述

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 測試環境
macOS / Python 3.12.11 / pytest 9.0.3

### Hook 測試
uv run --directory .claude/hooks pytest tests/test_uv_tool_staleness_check_hook.py：23 passed

### Runtime 驗證
echo SessionStart JSON | hook → additionalContext 含
「修復: uv tool install "/Users/.../.claude/skills/ticket" --force --reinstall」
無 cd、無 && chain operator。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager（PM 直接執行，PC-115）
**Review Status**: pending