---
id: 1.0.0-W8-036
title: 調查修復 sync-pull full overlay 復活已刪 .claude/ 檔機制 + 上游 claude.git repo 清理方案
type: ANA
status: in_progress
version: 1.0.0
wave: 8
priority: P2
parent_id: null
children:
- 1.0.0-W8-036.1
- 1.0.0-W8-036.2
- 1.0.0-W8-036.3
- 1.0.0-W8-036.4
blockedBy: []
relatedTo:
- 1.0.0-W8-023
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層:分析任務落地
  final_decision: WRAP 調查 sync-pull 復活根因 + 跨專案修復方案
  rationale: framework-priority + quality-baseline-rule-6
who:
  current: rosemary-project-manager
  history: {}
what: 調查修復 sync-pull full overlay 復活已刪 .claude/ 檔機制 + 上游 claude.git repo 清理方案
when: 立即（框架級最高優先，W8-023 診斷揭露）
where:
  layer: sync 機制 + 上游 repo + .sync-state.json
  files:
  - .claude/commands/sync-pull.md
  - .claude/skills/sync-pull
  - .claude/.sync-state.json
why: commit e791a64d 的 sync-pull 從上游 claude.git full overlay 把 W10-049（6ab3d762）已刪/已
  archive 的 16 個 .claude/ root 檔全部復活；治標再刪會被下次 sync 復活，正確修法在 sync 機制 + 上游 repo；命中既有
  sync-pull full overlay 風險 memory；框架級跨專案問題（companion Flutter app 共用同一上游）
how:
  task_type: Analysis
  strategy: WRAP 驅動：讀 sync-pull 邏輯 + .sync-state.json（驗 last_synced_base_sha 缺失假說）+
    git 史（6ab3d762 刪→e791a64d 復活）→ 查上游 claude.git 是否仍含 orphans → 評估跨專案污染 → 產出修復方案（sync
    尊重 local 刪除 + 上游清理 + local 孤兒移除 + W8-023.1 revert + carrier-map 清理）含 spawn 規劃
acceptance:
- '[x] 查明 sync-pull 為何 full overlay 復活 W10-049 已刪檔（機制 + .sync-state.json last_synced_base_sha
  缺失假說驗證）'
- '[x] 查明上游 tarrragon/claude.git 是否仍含這些 orphans（決定清理範圍）'
- '[x] 評估跨專案污染（companion Flutter app 是否同遭 sync 復活）'
- '[x] 產出修復方案（sync 機制尊重 local 刪除 + 上游 repo 清理 + local 16 孤兒移除 + W8-023.1 兩搬移 revert
  評估 + carrier-map 殘留清理）含 spawn 落地規劃'
- '[x] 重現實驗結果記錄（PC-063）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-14T22:31:14'
completed_at: null
created: '2026-06-14'
updated: '2026-06-14'
---

# Execution Log

## Task Summary

調查修復 sync-pull full overlay 復活已刪 .claude/ 檔機制 + 上游 claude.git repo 清理方案

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因（雙層）

**近因（4/20 事件本身，已被 W1-025 修復）**：commit e791a64d（2026-04-20 sync-pull）執行當時 `.claude/.sync-state.json` 在 repo 中不存在（`git show e791a64d:.claude/.sync-state.json` → fatal: not in tree；parent 亦無）。腳本 `should_use_full_overlay()` 在 `read_base_sha()` 回 None 時回傳 True → 走全量 overlay 路徑 → 把上游全部檔案覆蓋到本地，復活了 6ab3d762（W10-049，2026-04-15）刪除的 16 個 root .md 檔。當前腳本（1817 行，W1-025 schema）已具 base-SHA-aware 三方合併（`compute_upstream_delta(base, HEAD)` 只套用上游 delta），現況 `.sync-state.json` 含 `last_synced_base_sha=60132eb9`（= 當前 GitHub 上游 HEAD），故穩態下 delta 模式不會再復活。

**持久根因（尚未修復，本 ticket 真正標的）**：上游 `tarrragon/claude.git` 仍保留全部 16 孤兒檔（逐一 `git ls-files` 驗證皆 [1]）。W10-049 的刪除只發生在 book_overview_v1 本地，從未經 sync-push 傳播到上游。後果：任何 full-overlay fallback（新 clone / 遺失 .sync-state.json / base 不可達 / force-push / GC）都會從上游再次復活全部 16 檔。delta 模式雖防穩態復活，但 fallback 路徑仍是活的復活源。

### 影響範圍

- **book_overview_v1**：root 12/16 孤兒仍在（4 個已被 W8-023 處理：TECH-DEBT-REMINDER-DELIVERY-REPORT.md / code-smell-checklist.md〔已遷移 references/〕/ fix-progress-tracking.md / hook-system-reference.md）。
- **book_overview_app（companion Flutter，獨立 repo book_overview_app.git）**：root 16/16 孤兒全在 → 跨專案污染確認。
- **上游 tarrragon/claude.git**：16/16 孤兒仍在 root；另 7 個 session-start 報告的「未註冊 hook」（changelog-update / cli-failure-help-reminder / commit-handoff / post-commit-fetch / session-context-guard / skill-cli-error-feedback / test-timeout-post）亦同源於上游 hooks/（次要觀察，登記缺口可能另有原因，不混入本 ticket 主標的）。
- **本地上游工作 clone**：/Users/tarragon/Projects/claude（remote=tarrragon/claude.git，HEAD=faa3383d，領先 GitHub remote 60132eb9）為上游清理的操作點。

### 相關 Error Pattern / Memory

命中既有 memory「sync-pull full overlay 風險」（W1-003 ANA：.sync-state.json 缺 last_synced_base_sha → 退化 full overlay → 三類副作用：孤兒 hook 複製 / stale 誤刪本專案特有防護 / 可執行 .py 降權）。本 ticket 為該風險的具體爆發案例 + 跨專案 + 上游清理延伸。

## 重現實驗結果
### 實驗方法

以 git ground-truth 指令（固定值，不靠推斷）逐項驗證根因假設鏈，並對上游 / 兩個下游專案做存在性普查。所有遠端查證由 PM 親自用腳本內精確 URL（tarrragon/claude.git，三 r）clone 後先驗 `remote get-url origin` 防 clone 錯 repo。

### 實驗執行

1. **commit 存在性**：`git cat-file -t e791a64d` / `6ab3d762` → 皆 commit。e791a64d=「chore(sync): pull .claude framework updates」(4/20)；6ab3d762=「W10-049 根目錄清理 REMOVE 16+ARCHIVE 2+MIGRATE 2」(4/15)。
2. **刪除清單**：`git show 6ab3d762 --name-status | grep ^D` → 16 個 root .md。
3. **復活清單**：`git show e791a64d --name-status | grep ^A` → 含全部 16 檔 + 額外檔 + 7 個 hooks/*.py。
4. **base SHA 缺失驗證**：`git show e791a64d:.claude/.sync-state.json` 與 `e791a64d^:...` → 皆 fatal: 不在 tree。確認事件當時無 .sync-state.json。
5. **機制確認**：讀 sync-claude-pull.py：`read_base_sha`(L410 無檔回 None) → `should_use_full_overlay`(L626 base_sha None 即 True) → `compute_upstream_delta`(L476 有 base 時只套 delta)；`cleanup_stale_files`(L809) 只刪「local 有上游無」檔，孤兒同存上游故不被刪。
6. **上游普查**：clone tarrragon/claude.git（HEAD=60132eb9=local last_synced_base_sha），`git ls-files` 逐一 16 檔 → 全 [1]；7 hooks → 全 [1]。上游追蹤檔總數 1937。
7. **下游普查**（zsh word-split 修正後 + ls 交叉驗證）：v1 root 12/16；book_overview_app root 16/16。
8. **上游 clone 身份**：/Projects/claude remote=tarrragon/claude.git，HEAD=faa3383d（領先 GitHub 60132eb9）。

### 實驗發現（已驗證事實 vs 假設）

**已驗證事實**：
- [事實] 16 孤兒 = W10-049（6ab3d762）本地刪 → e791a64d sync 全量 overlay 復活。
- [事實] 復活直接成因 = 事件當時 .sync-state.json 不存在 → full overlay fallback。
- [事實] 當前腳本已具 W1-025 delta 三方合併，穩態（base SHA 存在）不再復活。
- [事實] 上游 tarrragon/claude.git 仍含 16/16 孤兒 → 為持續復活源（full-overlay fallback 路徑下）。
- [事實] 跨專案污染：companion app 16/16、v1 12/16。
- [事實] 上游清理操作點 = 本地 clone /Projects/claude。

**仍未驗證假設（交執行 IMP 處理）**：
- [假設] 16 孤兒在上游無 inbound 框架引用（刪除安全）——需執行 IMP 時 grep 上游驗證。
- [假設] 上游刪 16 + push 後，下游 sync-pull 的 delta（含 D status）會自動清除下游 12/16——需實測。
- [假設] 7 個未註冊 hook 是否屬同類孤兒應移除，或屬登記缺口應補註冊——本 ticket 不結論，另開評估。
- [假設] W8-023.1 兩搬移（待查 commit）是否需 revert——需查 W8-023.1 變更內容。

**測試環境學習（zsh）**：本環境 zsh 對 `for f in $VAR` 不做 word-split（與 bash 不同），首次普查得假性 0/16；以 `${=VAR}` 強制分割 + ls 交叉驗證後修正為 v1 12 / app 16。記為跨環境 shell 差異教訓。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 修復策略（WRAP 驅動）

### 問題重框（反向思考）
此非 sync-bug（W1-025 delta 已正確），而是**上游 SSOT 衛生問題**：W10-049 本地刪除未傳播上游，上游仍將 16 檔視為框架成員。持久修復 = 清理上游 SSOT → 下游收斂。

### 方案性質三類涵蓋（toolification-bias 防護）
- **零工具（採用）**：清理上游 repo（刪 16 孤兒 + push）+ 倚賴既有 W1-025 delta 同步。最小、最高槓桿、移除復活源頭。
- **改造既有（評估後可能不採）**：為 full-overlay fallback 加 deletion-manifest/tombstone，使 fallback 亦尊重刪除意圖。邊際收益（fallback 罕見），列入評估 child，預期結論「上游清理 + W1-025 已足夠」。
- **新增工具（不採）**：新建 deletion-ledger 同步特性——過度設計。

### Attain Distance（機會成本 / 優先序）
上游清理是達成持久修復的最小必要動作；tombstone 機制為邊際防禦，不阻擋核心修復、降優先。

### Premortem（P — 準備好犯錯）
- **R1 inbound 引用**：上游刪 16 可能斷框架內部引用。緩解：IMP 刪前 grep 上游 inbound refs，有則先修引用或保留該檔。
- **R2 不可逆 + 影響全體消費者**：push 上游影響**所有**共用 tarrragon/claude.git 的專案（不只此二）。緩解：派發前用戶確認；/Projects/claude HEAD(faa3383d) 領先 GitHub(60132eb9)，push 前先 fetch/對齊分歧勿 clobber。
- **R3 下游自動收斂未驗**：假設「上游刪→下游 pull 的 delta 含 D status 自動清」需實測一專案再推廣。
- **R4 references/ 版本一致性**：code-smell-checklist 已遷 references/；上游刪 root 版正確，但須確保各專案/上游一致保留 references/ 版。

### Spawn 落地規劃

| child | type | 動作 | blockedBy | 關鍵約束 |
|-------|------|------|-----------|---------|
| 上游清理+push | IMP | /Projects/claude：grep 驗 inbound refs → 對齊 HEAD 分歧 → 刪 16 root 孤兒 + commit → push GitHub | — | 不可逆+outward-facing，派發前用戶確認；inbound ref 有則先處理 |
| 下游收斂+清理 | IMP | 上游清理後 v1 + companion app 各 sync-pull，驗 delta 自動清；殘留手動移除；含 W8-023.1 兩搬移 revert 評估 | 上游清理 | 先測一專案再推廣（R3）|
| 機制防禦+hook 孤兒評估 | ANA | (a) 評估 full-overlay fallback 是否需 tombstone（預期：否）(b) 7 未註冊 hook 屬孤兒移除或登記缺口補註冊 | — | toolification-bias 防護，可獨立並行 |

### Spawn 落地確認
上述 3 項為「發現」，complete 前須建為 children（--parent 1.0.0-W8-036）。本 ANA 屬規劃型（計畫≠問題已解決），children 建立後維持 in_progress 至清理完成（防護性，feedback_ana_followup_as_children）。**派發前置**：上游清理 IMP 含不可逆 GitHub push + 跨全體專案影響，須先經用戶確認執行範圍與時機。

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending