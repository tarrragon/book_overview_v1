---
id: 1.0.0-W8-003
title: 評估 ticket body 持久化的資料層表現層分離架構
type: ANA
status: in_progress
version: 1.0.0
wave: 8
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.0.0-W7-001
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: W7 盲點評估發現根因框架未反向質疑，建探索性架構 ANA 綁 v2.0.0 重評
  rationale: quality-baseline 規則 5 + decision-trigger-binding 規則 2（版本錨點 trigger）
who:
  current: sassafras-data-administrator
  history: {}
what: 評估 ticket body 持久化的資料層表現層分離架構
when: 現在可執行（v1.0.0 發佈受 APP 端進度阻塞，提前處理；原 trigger=v2.0.0 架構窗口。注意：缺 v2.0.0 多書城 context，遷移成本評估須在
  ANA 結論標明假設與限制）
where:
  layer: ticket-system / 資料持久化架構
  files:
  - .claude/skills/ticket/ticket_system/
  - .claude/skills/ticket/ticket_system/lib/checkpoint_state.py
why: W7 鏈全部方案（auto-stage / 方案 C hook / auto-commit / 人為補充）都接受『ticket body 必須存 git-tracked
  .md』未質疑前提；auto-commit 是在脆弱架構上加即時 commit 非移除脆弱性；反向根因解是資料層表現層分離（body 權威狀態存獨立 state
  如 checkpoint_state / SQLite，不受 git 還原影響，git-tracked md 降為渲染產物）
how:
  task_type: Implementation
  strategy: 反向質疑 git-tracked md 前提 → 列資料層表現層分離架構選項（checkpoint_state 模式 / SQLite /
    其他）→ 評估對 CLI / hook / sync / worktree merge 工作流影響與遷移成本 → WRAP 完整含反向思考 → 結論明確或綁
    v2.0.0 重評
acceptance:
- '[ ] 反向質疑 ticket body 必須存 git-tracked md 前提，列出資料層表現層分離架構選項（checkpoint_state 模式 /
  SQLite / 其他）'
- '[ ] 評估各選項對既有 ticket 工作流（CLI / hook / sync / worktree merge）的影響與遷移成本'
- '[ ] WRAP 完整含反向思考 + 結論明確（採方向 / 否決 / 綁 v2.0.0 重評）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-08T03:00:20'
completed_at: null
created: '2026-06-08'
updated: '2026-06-08'
---

# Execution Log

## Task Summary

評估 ticket body 持久化的資料層表現層分離架構

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

W7 鏈（W7-001 auto-commit / 方案 C hook / auto-stage / 人為補充）全部接受同一未質疑前提：**「ticket body 必須以 git-tracked .md 檔案作為權威狀態」**。

直接根因（W1-017 ANA 已驗證）：ticket body 經 append-log 寫入 working tree 後若未即時 commit，會被三種 git 還原操作覆蓋——`git checkout -- <file>` / `git reset --hard` / `git stash`，body 退回 create commit 的 placeholder。

更深層根因（本 ANA 質疑點）：將「權威資料」與「git-tracked 檔案」綁定，使 body 暴露於 git 還原語意。W7-001 的 auto-commit 是在脆弱架構上加即時 commit 縮短暴露窗口，**未移除暴露本身**。反向解：資料層（authoritative state）與表現層（git-tracked md，渲染產物）分離，body 權威狀態存於不受 git 還原影響的獨立 store。

### 影響範圍

- `.claude/skills/ticket/ticket_system/lib/parser.py`：`parse_frontmatter` / `load_ticket` / `save_ticket`（資料層核心讀寫，每次以整檔 read_text/write_text round-trip md）
- `.claude/skills/ticket/ticket_system/lib/ticket_loader.py`：統一載入入口
- `.claude/skills/ticket/ticket_system/lib/worklog_appender.py` + `git_utils.py`：append-log + auto-commit
- 全系統 46 處 read/write ticket md 站點（grep `read_text|.open(|load_ticket|frontmatter`）
- 1926 個既有 ticket md 檔（遷移規模 baseline）
- hook 層：acceptance-gate / branch-verify 等多個 hook 直接讀 ticket md frontmatter/body
- sync 機制：實測 sync 範圍僅 `.claude/`（README-subtree-sync.md），ticket md 位於 `docs/work-logs/` **不在跨專案 sync 範圍**——ticket 資料為專案本地（重要：降低遷移複雜度，無跨專案 schema 對齊問題）

### 相關 Error Pattern

- W1-017 ANA（git 還原覆蓋 body 根因驗證，W7-001 source）
- W7-001（auto-commit 根因解，本 ANA 反向質疑對象）
- PC-178（ticket body git 還原覆蓋 + auto-commit 防護演進，最近 commit 59d88640）
- checkpoint_state.py：既有「獨立 state 不存 git-tracked md」模式範例（dispatch-active.json / handoffs/pending/*.json 為非 git-tracked 運行時 state），本 ANA 評估是否將此模式推廣至 ticket body

## 重現實驗結果
### 實驗方法

N/A：本 ANA 為**評估型（架構選型）非 bug 重現型**。根因「git 還原覆蓋未 commit body」已於 W1-017 ANA 完成重現實驗並驗證為事實，本 ANA 不重複重現，改以**代碼分析**為事實依據（取代重現實驗，符合 PC-063 精神：列方案前先用事實錨定，事實來源為靜態代碼分析而非運行重現）。

### 實驗執行

代碼分析依據（已驗證事實）：

1. **資料層現狀**：`parser.py:284 save_ticket` 以整檔 write_text round-trip，frontmatter（YAML）+ body（markdown）合存於單一 .md。權威狀態 = 該檔內容。
2. **暴露面**：實測 46 處 read/write ticket md 站點直接依賴 .md 檔存在與內容正確（CLI commands + hooks + validators）。
3. **既有獨立 state 範例**：checkpoint_state.py 讀 `.claude/dispatch-active.json`、`.claude/handoffs/pending/*.json`——這些是**非 git-tracked 運行時 state**，已證實「state 不存 git-tracked md」模式在本系統可行且已上線。
4. **遷移規模**：`find docs/work-logs -name "*.md" -path "*/tickets/*"` = 1926 檔。
5. **sync 邊界**：README-subtree-sync.md 明示 sync 範圍僅 `.claude/`；ticket md 在 `docs/work-logs/` 不跨專案同步——ticket 資料專案本地，遷移無跨專案 schema 對齊負擔。
6. **auto-commit 現狀**：git_utils.py `_auto_commit_ticket_md` 精確路徑 add+commit，graceful degrade（not_git_repo / no_change / git_failed 四狀態），縮短但未消除「寫入到 commit」之間的暴露窗口。

### 實驗發現

**已驗證的事實**：
- git 還原覆蓋未 commit body 為真（W1-017）。
- auto-commit 縮短窗口但 body 仍以 git-tracked 檔為權威，暴露於 git 還原語意（架構性，非實作 bug）。
- 「獨立非 git-tracked state」模式已在 checkpoint_state 上線可行。
- ticket 資料專案本地（非跨專案 sync），1926 檔規模。

**仍未驗證的假設（明示限制）**：
- 缺 v2.0.0 多書城 context：無法確認 v2.0.0 是否引入 ticket schema 變更、是否有跨專案 ticket 共享需求；遷移成本評估在此前提下標明假設。
- 各替代方案的實際遷移工時未做 PoC，成本為架構推估非實測。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: sassafras-data-administrator
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.0.0-W7-001 | chars: 317 -->

### Task Reference
- 1.0.0-W7-001 what: 實作 ticket body working-tree 還原防護（append-log auto-commit 根因解）

### Rationale Chain
- 1.0.0-W7-001 why: W1-017 ANA 證實 ticket body 寫入後停留未 commit working tree 會被 git 還原操作覆蓋；採 auto-commit 根因解使 body 即時進 commit 歷史，三種還原（checkout/reset --hard/stash）全失效

### Related Files
- .claude/skills/ticket/ticket_system/commands/track_acceptance.py  # from 1.0.0-W7-001
- .claude/skills/ticket/ticket_system/lib/git_utils.py  # from 1.0.0-W7-001