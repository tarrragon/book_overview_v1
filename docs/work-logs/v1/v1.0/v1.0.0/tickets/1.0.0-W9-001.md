---
id: 1.0.0-W9-001
title: 改用 filelock 取代 fcntl ticket_system file_lock 跨平台鎖
type: IMP
status: pending
version: 1.0.0
wave: 9
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: Windows fcntl 缺失致 ticket 工具崩潰
  final_decision: 採跨平台 filelock 庫（issue 已驗證方案），非 portalocker 或 WSL 規避
  rationale: filelock 純 Python 雙後端、語義等同原 LOCK_EX、issue 已在 Windows 11 驗證 roundtrip
    通過
who:
  current: rosemary-project-manager（PM 直接執行，PC-115 .claude/ 限制 subagent 無法 Edit）
  history: {}
what: 將 file_lock.py 的 POSIX fcntl 鎖改為跨平台 filelock 庫，移除 _HAS_FCNTL 平台分支與 Windows NotImplementedError
when: W9 wave 啟動後立即執行（P0，Windows 阻斷所有 ticket 狀態變更命令）
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/lib/file_lock.py
  - .claude/skills/ticket/pyproject.toml
  - .claude/skills/ticket/uv.lock
why: 'Windows 11/PowerShell 5.1 實測 claim/complete/release 等全數崩潰於 NotImplementedError（fcntl
  缺失），ticket 工具在 Windows 實質唯讀；framework github issue #1 問題0（P0）'
how:
  task_type: Implementation
  strategy: 改用 filelock.FileLock（POSIX 走 fcntl、Windows 走 msvcrt、語義一致），完全移除 fcntl 直接依賴；reap_stale_locks
    用 timeout=0 non-blocking 取鎖偵測 active；保留 W14-005 race 防護與全部公開 API；pyproject 加 filelock>=3.12
    並更新 uv.lock；完成後 uv tool install --force --reinstall
acceptance:
- '[ ] file_lock.py 移除 fcntl 直接依賴與 _HAS_FCNTL 分支，改用 filelock.FileLock'
- '[ ] pyproject.toml 加入 filelock>=3.12，uv.lock 同步更新'
- '[ ] 公開 API（file_lock/reap_stale_locks/create_id_allocation_lock/CREATE_LOCK_FILENAME）簽章不變'
- '[ ] macOS 跑鎖測試 test_reap_stale_locks 7 項 + test_precondition_lock_safety 2 項全綠'
- '[ ] uv tool 重裝後 ticket track claim/release roundtrip 正常、無殘留 .lock'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-06-15'
updated: '2026-06-15'
---

# Execution Log

## Task Summary

將 file_lock.py 的 POSIX fcntl 鎖改為跨平台 filelock 庫，移除 _HAS_FCNTL 平台分支與 Windows NotImplementedError

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager（PM 直接執行，PC-115 .claude/ 限制 subagent 無法 Edit）
**Review Status**: pending
