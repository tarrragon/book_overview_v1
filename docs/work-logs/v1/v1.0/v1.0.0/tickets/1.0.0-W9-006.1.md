---
id: 1.0.0-W9-006.1
title: get_tickets_dir 加既有 flat 結構讀取相容 paths.py 扁平/階層解析
type: IMP
status: completed
version: 1.0.0
wave: 9
priority: P2
parent_id: 1.0.0-W9-006
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: W9-006 ANA 決策落地（Option A-refined）
  final_decision: get_tickets_dir 加 existence-aware flat 讀取相容，default 仍三層
  rationale: 保 W14-052 不變式（新版本三層、無殘留）同時修問題4（讀既有 flat），與既有測試零衝突
who:
  current: rosemary-project-manager（PM 直接執行，PC-115）
  history: {}
what: get_tickets_dir 改為：hierarchical 存在用之；否則 flat 存在用之（讀既有 flat）；否則 default hierarchical（保
  W14-052 新版本三層不變式）
when: W9-006 ANA 決策 Option A-refined 後立即
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/lib/paths.py
  - .claude/skills/ticket/tests/test_paths_get_tickets_dir.py
why: 'issue #1 問題4：scanner 找得到扁平 docs/work-logs/v0.1.0/tickets/ 但 get_tickets_dir（W14-052）一律回三層致
  track list 讀 0 張。W9-006 ANA reality-test 確認 W14-052 真正關切是『新 ticket 建在三層』非『禁讀既有 flat』，故可在不違反不變式下補
  flat 讀取相容'
how:
  task_type: Implementation
  strategy: get_tickets_dir：parts>=2 時 hierarchical.exists()→hierarchical；elif flat.exists()→flat；else
    hierarchical。既有 4 測試（flat 不存在時 default 三層）不破；新增 flat-exists→flat + 既有 flat 不影響新版本三層
    兩測試
acceptance:
- '[x] get_tickets_dir：flat 存在且 hierarchical 不存在時回 flat；兩者皆不存在時回 hierarchical（保不變式）'
- '[x] 既有 4 個 W14-052 不變式測試全綠'
- '[x] 新增 flat-fallback 測試（flat 存在→flat、新版本 neither→hierarchical）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-16T00:02:56'
completed_at: '2026-06-16T00:03:20'
created: '2026-06-16'
updated: '2026-06-16'
---

# Execution Log

## Task Summary

get_tickets_dir 改為：hierarchical 存在用之；否則 flat 存在用之（讀既有 flat）；否則 default hierarchical（保 W14-052 新版本三層不變式）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 修復實作（W9-006 ANA 決策 Option A-refined 落地）

### 變更：ticket_system/lib/paths.py get_tickets_dir
parts>=2 階層分支改為 existence-aware：
- hierarchical.exists() → 回 hierarchical（三層正規）
- elif flat.exists() → 回 flat（讀既有 flat 結構，issue #1 問題4）
- else → 回 hierarchical（新版本一律三層，保 W14-052 不變式、無殘留）
W14-052 註解同步更新（區分「新建一律三層」vs「讀既有 flat 相容」）。

### W14-052 不變式保全證明
既有 4 測試（v2/v10.3.1/v0.18/v-prefix，皆 flat 不存在）→ default hierarchical → 全綠。
新增 3 測試：既有 flat→flat、both exist→hierarchical 優先、既有 flat 不影響新版本 v0.2.0 仍三層。

### 自檢結果（Layer 1）
- [x] 測試通過率：paths 7/7 + 完整套件 2476 passed
- [x] W14-052 不變式：4 既有測試全綠（flat 不存在→三層）
- [x] 問題4 修復：flat 存在→flat（test_existing_flat_returned_when_hierarchical_absent）
- [x] existence 檢查僅影響「讀取解析」，新建（flat 不存在）仍三層，無 residue
- [x] 重裝 CLI 生效

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 測試環境
macOS / Python 3.11.13 / pytest 9.0.2

### paths 測試
uv run --extra test pytest tests/test_paths_get_tickets_dir.py：7 passed（4 W14-052 不變式 + 3 W9-006.1 flat 相容）

### 完整迴歸
全 ticket_system 套件：2476 passed, 1 deselected（perf）

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-16T00:03:20
**Executing Agent**: rosemary-project-manager（PM 直接執行，PC-115）
**Review Status**: pending
