---
id: 1.0.0-W1-055.3
title: 修復 handoff-auto-resume-stop-hook Stop 事件雙重註冊移除（PC-148）
type: IMP
status: completed
version: 1.0.0
wave: 1
priority: P2
parent_id: 1.0.0-W1-055
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager
  history: {}
what: 修復 handoff-auto-resume-stop-hook Stop 事件雙重註冊移除（PC-148）
when: W1-055 ANA 附帶發現後，排入當前批次序列
where:
  layer: .claude/hooks/,.claude/settings.json,.claude/hook-logs/
  files:
  - .claude/settings.local.json
why: W1-052 Phase 4 派發 2 個背景 agent（linux + general-purpose），linux 完成後等待期間 SubagentStop
  hook 連續 11+ 次輸出「[WAIT] 仍有 1 個代理人在執行」並喚醒主線程，但期間無任何 agent 完成事件。每次喚醒消耗主線程回合與 context
  重載成本，與 W1-040.1 孤兒 waiter 重複通知同症狀但觸發源不同（該案是 run_in_background Bash waiter 清理，本案是
  SubagentStop 事件來源不明的重複觸發）。需查明：觸發源（主線程自身回合結束被計入？hook 雙重註冊 PC-148 模式？）與抑制手段
how:
  task_type: Implementation
  strategy: 移除 settings.local.json:196-207 的重複註冊（settings.json 已有同 hook 註冊）；移除後驗證
    Stop 事件僅觸發一次
acceptance:
- '[x] settings.local.json 不再含 handoff-auto-resume-stop-hook 註冊'
- '[x] settings.json 原註冊保留且 hook 仍正常觸發（grep 確認單一註冊）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-11T21:57:40'
completed_at: '2026-06-11T21:59:20'
created: '2026-06-11'
updated: '2026-06-11'
---

# Execution Log

## Task Summary

修復 handoff-auto-resume-stop-hook Stop 事件雙重註冊移除（PC-148）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 修復內容

移除 `.claude/settings.local.json` 內 `hooks.Stop` 整段（handoff-auto-resume-stop-hook.py 重複註冊，原 196-208 行），保留 `settings.json:696` 的唯一註冊。PC-148 模式（雙檔重複註冊致 Stop 事件雙重觸發）。

### 簡化 WRAP 三問

- W：候選二 = 保留 local 註冊改刪 settings.json 版；否決——settings.json 為 sync 共用層，local 檔屬本機覆寫，重複源頭是 local 殘留
- A：機會成本極低（單段 JSON 移除），不擠壓批次佇列
- P：最可能失敗 = JSON 結構破損；防護 = json.load 驗證已通過

### 自檢結果

- [x] JSON 合法（python3 json.load 驗證）
- [x] 全 repo 僅剩 1 處註冊（grep 計數 = 1）
- [x] 由 PM 前台執行（settings.local.json 為 local-only 語意檔，worktree 派發不可行；.claude/ 屬 main 分支豁免路徑）

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
- `python3 -c "json.load(...)"` → JSON valid
- `grep -rn "handoff-auto-resume-stop-hook" .claude/settings.json .claude/settings.local.json | wc -l` → **1**（僅 settings.json:696）
- Stop 事件單次觸發的行為驗證：依 hook 載入機制於下個 session 生效，由 W1-055.1 受控重現實驗一併覆蓋（其實驗本就需觀測 Stop/SubagentStop 事件數）

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-11T21:59:20
**Executing Agent**: rosemary-project-manager
**Review Status**: pending