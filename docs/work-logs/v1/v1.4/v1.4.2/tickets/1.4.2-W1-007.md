---
id: 1.4.2-W1-007
title: 設計 CI 發布工作流（tag v* → 跑測試 + 打包 release → GitHub Releases）
type: IMP
status: completed
version: 1.4.2
wave: 1
priority: P2
parent_id: null
children: []
blockedBy:
- 1.4.2-W1-006
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:任務評估
  final_decision: 建 IMP CI 發布工作流，tag 觸發 + GitHub Releases 發佈
  rationale: 用戶指示：版本更新時自動測試並重新打包公開 release
who:
  current: thyme-extension-engineer
  history: {}
what: 設計 CI 發布工作流（tag v* → 跑測試 + 打包 release → GitHub Releases）
when: v1.4.2 W1（W1-006 package:release 腳本完成後）
where:
  layer: 待定義
  files:
  - .github/workflows/release.yml
  - package.json
why: 正式上架前需自動化版本發布把關：推送版本 tag 時自動跑完整測試確保品質，並重新打包生產版 release zip 發佈到 GitHub Releases
  供測試者下載，避免手動打包遺漏與未測試版本流出
how:
  task_type: Implementation
  strategy: 新增 GitHub Actions workflow（.github/workflows/release.yml）：on push tag
    v* → job1 跑 npm test 全套件（fail 則中止）→ job2 npm run package:release 產生版本 zip → 以
    softprops/action-gh-release 或 gh release 將 zip 上傳為 GitHub Release asset。與既有 lint.yml
    CI 並存
acceptance:
- '[x] 新增 .github/workflows/release.yml，觸發條件為 push tag v*'
- '[x] workflow 於版本 tag 推送時執行完整測試套件（npm test），失敗則中止發布'
- '[x] 測試通過後執行 npm run package:release 重新打包生產版 zip'
- '[x] 打包的 release zip 以 asset 形式發佈到對應 tag 的 GitHub Release'
- '[x] 與既有 .github/workflows/lint.yml 並存不衝突'
- '[x] workflow YAML 通過語法驗證（actionlint 或實機觸發驗證）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-25T01:57:55'
completed_at: '2026-06-25T02:00:10'
created: '2026-06-25'
updated: '2026-06-25'
---

# Execution Log

## Task Summary

設計 CI 發布工作流（tag v* → 跑測試 + 打包 release → GitHub Releases）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 變更檔案
- 新增 `.github/workflows/release.yml`（53 行）

### YAML 語法驗證
actionlint 本機未安裝，改用 python yaml.safe_load 驗證：

```
YAML parse OK
top keys: ['name', True, 'concurrency', 'permissions', 'jobs']
on: {'push': {'tags': ['v*']}}
jobs: ['release']
permissions: {'contents': 'write'}
concurrency: {'group': 'release-${{ github.ref }}', 'cancel-in-progress': False}
```

註：`on` 被 YAML 1.1 解析為布林 True key（YAML 規範既有行為），GitHub Actions 解析器正常處理，不影響觸發。

### 步驟邏輯審查
job `release` steps（python 解析確認）：
- Checkout | actions/checkout@v4
- Setup Node.js | actions/setup-node@v4（node-version lts/*, cache npm）
- Install dependencies | npm ci --legacy-peer-deps
- Run tests | npm test（失敗非零退出 → 後續步驟不執行 → 中止發布）
- Package production release | npm run package:release（內部先跑 build:prod，再壓出 release/readmoo-book-extractor-v<version>.zip）
- Publish to GitHub Release | softprops/action-gh-release@v2，files: release/*.zip（glob 避免硬編版本）

### 資產確認
- npm test → test:core → jest tests/unit tests/integration（CI 適用，不含 E2E）
- npm run package:release = node scripts/package-release.js，產物 release/readmoo-book-extractor-v<version>.zip，符合 release/*.zip glob
- permissions contents:write 支援 action-gh-release 發佈，GITHUB_TOKEN 自動帶入

### 並存驗證
concurrency group 隔離：release.yml=release-${{ github.ref }}、lint.yml=lint-${{ github.ref }}，前綴不衝突。
lint.yml 未被修改（git status 僅 release.yml 新增 + ticket md）。

### Commit
- branch: feat/1.4.2-W1-007-ci-release（未 merge main）
- SHA: a56758a92
- git add 精確路徑 .github/workflows/release.yml

## Context Bundle

### 既有資產

- `.github/workflows/lint.yml`（既有 CI，push main/feat + PR 觸發 ESLint）— 參考其 job 結構
- CI 標準步驟：`actions/checkout@v4` → `actions/setup-node@v4`（node-version `lts/*`, cache `npm`）→ `npm ci --legacy-peer-deps`
- `npm run package:release`（W1-006 已落地）= `node scripts/package-release.js`，產出 `release/readmoo-book-extractor-v<version>.zip` + `release/README.md`
- `npm test` 全套件（unit + integration）

### 目標（本 ticket）

新增 `.github/workflows/release.yml`：
1. 觸發：`on: push: tags: ['v*']`
2. job 跑 `npm ci --legacy-peer-deps` → `npm test`（失敗則中止，不發布）
3. 測試通過 → `npm run package:release` 重新打包生產版 zip
4. 將 `release/*.zip` 以 asset 發佈到對應 tag 的 GitHub Release（建議 `softprops/action-gh-release@v2`，附 `GITHUB_TOKEN` 權限 `contents: write`）
5. 與既有 lint.yml 並存，job/concurrency group 命名不衝突

### 注意

- `package.json` 的 `package:release` 已存在，**本 ticket 通常不需改 package.json**（如真需調整再動）。
- workflow 內若需版本號可從 tag（`github.ref_name`）取得。
- zip 路徑為 `release/*.zip`（檔名含 package.json version；以 glob 上傳避免硬編版本）。

### Commit Policy

feature branch `feat/1.4.2-W1-007-ci-release`，commit 不自行 merge，PM 驗證後合併。git add 精確路徑。

### 驗證

YAML 語法以 actionlint 驗證（或 `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))"`）。實機 tag 觸發需 push 權限，非本 ticket 範圍——以語法驗證 + 步驟邏輯審查為 acceptance。既有 `npm test` 不可破壞。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
- 結果：完成
- 產出：.github/workflows/release.yml（push v* tag → npm ci → npm test → npm run package:release → softprops/action-gh-release@v2 上傳 release/*.zip）
- 驗證：python yaml.safe_load 通過；步驟邏輯 + concurrency 隔離 + lint.yml 未破壞已審查
- commit：a56758a92（feat/1.4.2-W1-007-ci-release，未 merge main，待 PM 驗證合併）
- 未涵蓋：實機 tag 觸發需 push 權限，非本 ticket 範圍（acceptance 以語法 + 步驟邏輯審查為準）

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-25T02:00:10
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending
