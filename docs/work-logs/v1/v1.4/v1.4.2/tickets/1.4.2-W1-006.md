---
id: 1.4.2-W1-006
title: 新增 prod 打包發布腳本（build:prod + zip + 測試者 README → release/）
type: IMP
status: completed
version: 1.4.2
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:任務評估
  final_decision: 建 IMP 打包發布腳本，可重複執行
  rationale: 用戶指示：正式上架前準備測試者分發包
who:
  current: thyme-extension-engineer
  history: {}
what: 新增 prod 打包發布腳本（build:prod + zip + 測試者 README → release/）
when: v1.4.2 W1（正式上架前的測試分發準備）
where:
  layer: 待定義
  files:
  - package.json
  - scripts/
  - release/
  - .gitignore
why: 正式上架 Chrome Web Store 前，需提供測試者一個可安裝的生產版分發包。打包流程自動化（build:prod + zip + 安裝說明）讓每次釋出可重複、降低人為遺漏
how:
  task_type: Implementation
  strategy: 新增 npm run package:release 腳本：呼叫 build:prod → 將建置產物壓縮為含版本號的 zip → 輸出至根目錄
    release/ → 產出安裝說明 README → release/ zip 入 .gitignore。腳本可重複執行（清理舊產物）
acceptance:
- '[x] 新增 npm run package:release 腳本，執行 build:prod 產生生產版建置'
- '[x] 建置產物壓縮為 zip（檔名含版本號，如 readmoo-book-extractor-v1.4.2.zip）'
- '[x] zip 輸出至根目錄 release/ 資料夾'
- '[x] release/ 內含安裝說明 README（Chrome 載入 unpacked / 解壓安裝步驟、版本、注意事項）'
- '[x] release/ 內 zip 產物加入 .gitignore（不入版控），README 可入版控'
- '[x] 腳本可重複執行（重跑前清理舊產物，不殘留）'
- '[x] 實機驗證：產出 zip 可解壓並在 Chrome 以 unpacked 載入成功'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-25T01:49:13'
completed_at: '2026-06-25T01:55:21'
created: '2026-06-25'
updated: '2026-06-25'
---

# Execution Log

## Task Summary

新增 prod 打包發布腳本（build:prod + zip + 測試者 README → release/）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

**Commit**: `0b7408a8d`（feat branch `feat/1.4.2-W1-006-package-release`）

### 改動檔案

- `scripts/package-release.js`（新增）：測試者發布打包腳本（build:prod → 版本化 zip → release/ → README）。
- `package.json`：新增 `package:release` 腳本（`node scripts/package-release.js`）。
- `.gitignore`：新增 `release/*.zip`（zip 不入版控；README.md 可入版控）。
- `release/README.md`（新增，入版控）：測試者安裝說明。

### 設計

沿用既有 `scripts/package.js` 驗證過的壓縮做法（在 build 目錄內執行 zip 使 manifest.json 落在 ZIP 根層），目標改為 `release/`、主動觸發 build:prod、新增 README 生成。兩腳本刻意不共用模組：package.js 在 module load 即執行 `packageExtension()`，無法乾淨匯入其函式；複製穩定壓縮邏輯比為打包工具引入 export 重構更低風險（已在腳本 doc-comment 記錄分工）。

### 實機驗證輸出

`npm run package:release`：

```
[VERSION] 打包版本：1.4.2（來源：build/production/manifest.json）
[PACKAGE] 開始壓縮 build/production/ ...
[VERIFY] ZIP 大小：1.42 MB（上限 5 MB）
[VERIFY] manifest.json 位於 ZIP 根層
[README] 已產出安裝說明：release/README.md（v1.4.2）
[DONE] 發布包完成：release/readmoo-book-extractor-v1.4.2.zip
```

release/ 產物：`README.md`（1.8k）、`readmoo-book-extractor-v1.4.2.zip`（1.5M）。

ZIP 解壓 manifest 完整性（unpacked 載入要求）：

```
unzip -l: manifest.json 位於根層（無路徑前綴）
manifest_version: 3 | name: Readmoo 書庫數據提取器 (模組化版本) | version: 1.4.2
has_background: true | has_content_scripts: true | has_action: true
```

.gitignore 行為：`git check-ignore release/...v1.4.2.zip` → ZIP 忽略；`release/README.md` → 可入版控（correct）。

冪等性：模擬殘留 `readmoo-book-extractor-v1.4.1.zip` → 重跑 `[CLEAN] 移除舊版本 ZIP`，重跑後僅存當前版本 zip。

### 測試

- `npm test`（test:core = jest tests/unit tests/integration）：263 passed / 2 skipped suites；5804 tests passed / 41 skipped / 0 failed。
- `npx eslint scripts/package-release.js`：exit 0。
- `node scripts/check-no-emoji.js`：OK（267 檔無 emoji）。

---

## Context Bundle

### 既有資產（必先讀，勿重造）

- `scripts/package.js`（既有壓縮腳本，npm `package:zip` = `node scripts/package.js`）
- npm `package` = `npm run build:prod && node scripts/package.js`
- npm `build:prod` = `node scripts/build.js --prod`（輸出至 `build/`）
- npm `clean` = `rimraf build/ coverage/ test-results/`
- 版本號來源：`package.json` version / `manifest.json` version

**先 Read `scripts/package.js`** 了解現有壓縮邏輯與輸出位置，再決定是擴充它或新增薄包裝，避免與既有 packaging 重複。

### 目標（本 ticket）

新增 `npm run package:release`：
1. `build:prod` 產生生產版（移除 log）到 `build/`
2. 壓縮為含版本號 zip（如 `readmoo-book-extractor-v<version>.zip`，version 讀 package.json）
3. 輸出至根目錄 `release/`（重跑前清理舊 zip，不殘留）
4. 在 `release/` 產出安裝說明 `README.md`（Chrome `chrome://extensions` 開發者模式 → 載入未封裝/解壓後載入步驟、版本、已知注意事項）
5. `.gitignore` 忽略 `release/*.zip`（zip 不入版控）；`release/README.md` 可入版控

### Commit Policy

feature branch `feat/1.4.2-W1-006-package-release`，commit 不自行 merge，PM 驗證後合併。git add 精確路徑。

### 必讀

`docs/project-conventions.md`（寫 src 前）；build:prod 移除 log 為設計（CLAUDE.md 5 建置指令）。

### 驗證

實機跑 `npm run package:release`，確認 `release/` 產出版本化 zip + README，且 zip 解壓後 manifest 完整可在 Chrome unpacked 載入。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-25
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending PM review（commit `0b7408a8d` 於 feat 分支，未自行 merge）
