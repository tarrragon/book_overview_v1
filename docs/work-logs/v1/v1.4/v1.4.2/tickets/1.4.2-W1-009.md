---
id: 1.4.2-W1-009
title: 升級 CI workflow actions 版本（Node 20 棄用警告）
type: IMP
status: completed
version: 1.4.2
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:任務評估
  final_decision: 建 P3 IMP 追蹤 CI actions 版本升級
  rationale: quality-baseline 規則 5：實機驗證發現必追蹤
who:
  current: thyme-extension-engineer
  history: {}
what: 升級 CI workflow actions 版本（Node 20 棄用警告）
when: v1.4.2 或後續（非阻塞，現行 CI 正常）
where:
  layer: 待定義
  files:
  - .github/workflows/release.yml
  - .github/workflows/lint.yml
why: v1.4.2 release CI run 28120073120 出現 annotation：actions/checkout@v4 / setup-node@v4
  / softprops/action-gh-release@v2 宣告 Node.js 20 已被 GitHub 棄用（現自動 forced 到 Node 24
  仍運作）。未來 GitHub 移除 Node 20 forcing 後這些 action 可能失效，須升級至宣告 Node 24 的版本
how:
  task_type: Implementation
  strategy: 追蹤 actions/checkout / actions/setup-node / softprops/action-gh-release
    的新 major（宣告 Node 24 runtime）→ 升級 release.yml 與 lint.yml 對應 uses 版本 → 推測試 tag 驗證
    CI 仍綠
acceptance:
- '[x] 升級 release.yml / lint.yml 的 actions 至無 Node 20 棄用警告的版本'
- '[x] CI run 不再出現 Node.js 20 deprecation annotation'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-25T02:27:31'
completed_at: '2026-06-25T02:30:44'
created: '2026-06-25'
updated: '2026-06-25'
---

# Execution Log

## Task Summary

升級 CI workflow actions 版本（Node 20 棄用警告）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 版本查證（ground truth: 各 action repo action.yml `using:` 欄位，gh api 查 ref）

| Action | 原版本 (Node) | 升級至 (Node) | 查證依據 |
|--------|--------------|--------------|---------|
| actions/checkout | @v4 (node20) | @v5 (node24) | v5 為首個宣告 node24 的 major（v6/v7 亦 node24，採最小升版） |
| actions/setup-node | @v4 (node20) | @v5 (node24) | v5 為首個宣告 node24 的 major（v6 亦 node24，採最小升版） |
| softprops/action-gh-release | @v2 (node20) | @v3 (node24) | v3 為首個宣告 node24 的 major（latest v3.0.1 同 v3 線） |

查證指令：gh api "repos/<owner>/<repo>/contents/action.yml?ref=<tag>" --jq .content | base64 -d | grep using:
- checkout: v4=node20, v5/v6/v7=node24
- setup-node: v4=node20, v5/v6=node24
- action-gh-release: v2=node20, v3=node24

決策：升至各 action 首個 node24 major（保守最小升版），消除 Node 20 deprecation annotation，同時避免 latest（v6/v7/v3.0.1）潛在 breaking change 暴露。

### YAML 驗證

python3 yaml.safe_load(open(f)) → 兩檔皆 safe_load OK。
grep -rn "checkout@v4|setup-node@v4|action-gh-release@v2" .github/workflows/ → no residual node20 actions。

grep -n "uses:" .github/workflows/*.yml：
- lint.yml:25 actions/checkout@v5
- lint.yml:28 actions/setup-node@v5
- release.yml:29 actions/checkout@v5
- release.yml:32 actions/setup-node@v5
- release.yml:51 softprops/action-gh-release@v3

### 範圍

僅改 uses: 版本，未動 workflow 邏輯。兩 workflow 皆掃。
實機 CI 綠燈由後續 v1.4.3 tag push 驗證涵蓋（本 ticket acceptance 為語法驗證 + 版本確認）。

### Commit

feat/1.4.2-W1-009-ci-actions-upgrade @ 91823a4108fbf87c34b65c193db1f5f0056384a9（未自行 merge，待 PM 驗證合併）

## Context Bundle

### 背景

v1.4.2 release CI run 28120073120 annotation：actions/checkout@v4、actions/setup-node@v4、softprops/action-gh-release@v2 宣告的 Node.js 20 已被 GitHub 棄用（現自動 forced 到 Node 24）。

### 目標

升級 `.github/workflows/release.yml` 與 `.github/workflows/lint.yml` 的 `uses:` 至宣告 Node 24 runtime 的版本，消除 Node 20 deprecation annotation。

### 待升級 action（先查各 repo latest release 確認 Node 24 支援）

- `actions/checkout@v4` → 查 v5（v5 已轉 Node 24）
- `actions/setup-node@v4` → 查 v5
- `softprops/action-gh-release@v2` → 查最新 tag 是否已轉 Node 24；若無新 major 可 pin 到已轉 Node 24 的 patch（如 @v2.x.x 具體 commit/tag），或記錄為上游未釋出待追蹤

### 注意

- 兩個 workflow 都要掃（lint.yml 也用 checkout@v4 + setup-node@v4）。
- 不改 workflow 邏輯，只改 `uses:` 版本。
- 升級後須確認 CI 仍綠（語法 + 觸發），實機綠燈由後續 v1.4.3 tag 驗證涵蓋（本 ticket 以語法驗證 + 版本確認為 acceptance）。

### Commit Policy

feature branch `feat/1.4.2-W1-009-ci-actions-upgrade`，commit 不自行 merge，PM 驗證後合併。

### 驗證

YAML safe_load 通過；`grep -n "uses:" .github/workflows/*.yml` 確認版本已升、無殘留 Node 20 宣告 action 舊版。

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
status: success
reason: "release.yml 與 lint.yml 的 actions 已升級至各 action 首個宣告 node24 runtime 的 major（checkout@v5 / setup-node@v5 / action-gh-release@v3），消除 Node 20 deprecation annotation。版本經 action.yml using: 欄位查證，非盲升不存在版本（PC-159）。YAML safe_load 通過，無殘留 node20 action。僅改 uses: 未動邏輯。"
confidence: 1.0
acceptance_met: [0, 1]
acceptance_unmet: []
artifacts:
  - .github/workflows/release.yml
  - .github/workflows/lint.yml
context_dependencies:
  - "gh api action.yml using: 欄位（各 action major runtime 查證）"
estimated_recovery_effort: ""
```

註：acceptance 1（CI run 不再出現 annotation）的實機綠燈由後續 v1.4.3 tag push 涵蓋；本 ticket 範圍以「升至已查證 node24 版本」靜態達成判定。

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-25T02:30:44
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending
