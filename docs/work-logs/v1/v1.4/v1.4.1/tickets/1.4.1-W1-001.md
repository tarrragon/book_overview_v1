---
id: 1.4.1-W1-001
title: 評估 根目錄額外檔案來源並修正產出路徑
type: ANA
status: in_progress
version: 1.4.1
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:任務評估
  final_decision: 用戶直接要求清理根目錄
  rationale: 用戶指示
who:
  current: rosemary-project-manager
  history: {}
what: 評估 根目錄額外檔案來源並修正產出路徑
when: v1.4.1 W1
where:
  layer: 待定義
  files:
  - '*.log'
  - babel.config.js
  - jest.e2e.config.js
why: 根目錄有 13 個歷史 .log 檔（2025-09 產生，共 ~4MB）+ 1 個異常中文目錄，不符專案整潔預期。需識別來源並修正產出路徑或刪除
how:
  task_type: Implementation
  strategy: 盤點根目錄非核心檔案 → 識別產生來源 → 分類處理（刪除/移動/修正路徑）→ 更新 .gitignore
acceptance:
- '[ ] 盤點根目錄所有非專案核心檔案（13 個 .log + 異常目錄）'
- '[ ] 識別每個檔案的產生來源（腳本/指令/歷史遺留）'
- '[ ] 修正可修正的產出路徑（移至適當位置）或刪除無用檔案'
- '[ ] 更新 .gitignore 確保未來不再產出於根目錄'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-24T14:15:24'
completed_at: null
created: '2026-06-24'
updated: '2026-06-24'
---

# Execution Log

## Task Summary

評估 根目錄額外檔案來源並修正產出路徑

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 根目錄盤點結果

#### 類別 1：歷史 .log 檔（13 個，共 ~4MB，全部 2025-09 產生）

全部未被 git 追蹤（.gitignore 已有 *.log 規則）。是專案初期（v0.0.x ~ v0.8.x）一次性 ESLint 分析/測試/build 的重定向輸出（如 npm test > test-results.log），**無任何腳本會再次產生**。

| 檔案 | 大小 | 來源推測 |
|------|------|---------|
| baseline-test-results.log | 2.5M | 一次性測試 baseline |
| test-results.log | 767K | 測試輸出 |
| eslint-before-fix.log | 129K | ESLint 修復前快照 |
| eslint-current-status.log | 102K | ESLint 狀態 |
| eslint-after-fix.log | 98K | ESLint 修復後快照 |
| quick-eslint-check.log | 98K | 快速 ESLint 檢查 |
| console-statements.log | 79K | console.log 盤點 |
| console-remaining.log | 74K | console 殘留盤點 |
| error-handling-issues.log | 37K | 錯誤處理問題 |
| unused-vars-remaining.log | 21K | 未使用變數 |
| build-results.log | 10K | build 輸出 |
| remaining-paths.log | 4K | 路徑問題 |
| path-fixer.log | 740B | 路徑修復 |

**處理建議**：直接刪除（.gitignore 已排除，無腳本引用，歷史分析產物無保留價值）。

#### 類別 2：異常目錄（1 個）

`（待恢復 handoff）\n  1. 0.18.0-W17-001` — 目錄名包含中文和換行符，疑似前 session 手動操作產物。未被 git 追蹤。

**處理建議**：直接刪除。

#### 類別 3：正式設定檔（不處理）

`babel.config.js`、`jest.e2e.config.js` — git 追蹤的正式設定檔，屬專案核心，不是額外產物。

#### 類別 4：build 產物目錄（已有 .gitignore）

`build/`、`dist/`、`coverage/` — 已被 .gitignore 排除，是正常 build 產物。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
