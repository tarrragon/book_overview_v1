---
id: 1.4.0-W2-010
title: 正規化 lib/ 內 from hook_utils import（common_functions / markdown_formatter）改為
  lib 自身模組
type: IMP
status: pending
version: 1.4.0
wave: 2
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 規則3:設計問題立即修正
  final_decision: 從 W2-008 拆出功能性 import 重構，避免與機械清理混做
  rationale: quality-baseline-rule-3
who:
  current: thyme-python-developer
  history: {}
what: 正規化 lib/ 內 from hook_utils import（common_functions / markdown_formatter）改為 lib
  自身模組
when: v1.4.0 W2
where:
  layer: 待定義
  files:
  - .claude/lib/common_functions.py
  - .claude/lib/markdown_formatter.py
  - .claude/lib/hook_io.py
  - .claude/lib/hook_logging.py
  - .claude/lib/git_utils.py
why: W2-008 拆出：lib/ 內 from hook_utils import 是 lib→hooks/hook_utils 反向依賴，且 lib/ 與
  hooks/hook_utils/ 模組已分歧（hook_logging 無 get_project_root、hook_io 無 read_json_from_stdin），改錯會
  runtime 崩潰，需完整 TDD
how:
  task_type: Implementation
  strategy: 走完整 TDD：先 RED 測 import 可解析 + 函式行為等價，再補 lib/ 缺失符號（read_json_from_stdin
    等價）後改 import，最後跑 hook 測試套件 + sync-pull import 煙霧驗證
acceptance:
- '[ ] lib/common_functions.py 不再 from hook_utils import（get_project_root 改自 lib/git_utils）'
- '[ ] lib/markdown_formatter.py 不再 from hook_utils import（setup_hook_logging 自 lib/hook_logging；read_json_from_stdin
  補等價或改用 lib/hook_io.read_hook_input）'
- '[ ] hook 測試套件全綠'
- '[ ] sync-pull import 煙霧驗證（test-hook-imports.sh）通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-06-23'
updated: '2026-06-23'
---

# Execution Log

## Task Summary

正規化 lib/ 內 from hook_utils import（common_functions / markdown_formatter）改為 lib 自身模組

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 調查發現（W2-008 拆出，PM 前台調查）

現況 import：
- `.claude/lib/common_functions.py:33` `from hook_utils import get_project_root`；唯一使用點 line 42 `project_dir = get_project_root()`。
- `.claude/lib/markdown_formatter.py:13` `from hook_utils import setup_hook_logging, read_json_from_stdin`；使用點 line 68-69（`setup_hook_logging("markdown-formatter")` + `read_json_from_stdin(logger)`）。

符號在 lib/ 的對應（關鍵：非一對一）：
- `get_project_root` → lib/ 定義在 `lib/git_utils.py:219`（簽章 `get_project_root(cwd=None)`），**不在** lib/hook_logging.py。注意 hook_utils 版是從 `hook_utils/hook_logging.py` 匯出，與 lib 版來源模組不同。
- `setup_hook_logging` → `lib/hook_logging.py:25` 有，可直接改。
- `read_json_from_stdin` → **lib/ 無此函式**。lib/hook_io.py 只有 `read_hook_input()`（無 logger 參數），簽章與 `read_json_from_stdin(logger)` 不同，不能直接替換。需評估：補一個 lib 等價 `read_json_from_stdin`，或改用 `read_hook_input()` 並調整呼叫端。

風險：
- `.claude/hooks/hook_utils/` 是 package（hook_base/hook_io/hook_logging/hook_ticket/__init__）；lib/ 有自己分歧版的 hook_logging.py（無 get_project_root）、hook_io.py（read_hook_input 而非 read_json_from_stdin）。盲目替換會 runtime 崩潰。
- lib/ → hooks/hook_utils/ 屬反向依賴（lib 應為較低層）。

建議 TDD：Phase 2 先寫 RED 驗證 (a) 改後 import 可解析 (b) get_project_root / read_json 行為等價；Phase 3b 補 lib 缺失符號後改 import；驗收含 hook 測試套件 + `test-hook-imports.sh`（W2-004 已接入 sync-pull）。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
