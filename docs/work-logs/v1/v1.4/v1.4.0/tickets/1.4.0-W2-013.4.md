---
id: 1.4.0-W2-013.4
title: 實作 sync 端 framework hook enforcement（sync-pull 偵測並拒絕框架 hook 留在 local）+ prune
  併入 hook-completeness --fix flag
type: IMP
status: in_progress
version: 1.4.0
wave: 2
priority: P2
parent_id: 1.4.0-W2-013
children: []
blockedBy:
- 1.4.0-W2-013.2
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager
  history: {}
what: 實作 sync 端 framework hook enforcement（sync-pull 偵測並拒絕框架 hook 留在 local）+ prune
  併入 hook-completeness --fix flag
when: v1.4.0 W2，S2(013.2) reality-test 確認區分依據後
where:
  layer: 待定義
  files:
  - .claude/scripts/sync-claude-pull.py
  - .claude/hooks/hook-completeness-check.py
why: 'framework issue #11：框架版本遷移（2.0 hooks 搬移）時 sync-pull 更新 settings.json 但無法觸及 settings.local.json（per-project
  不同步），stale hook 註冊成幽靈阻擋派發。需框架層機制應對版本遷移攜帶 settings 變更'
how:
  task_type: Implementation
  strategy: 依 S2 結論在 sync/安裝端加 enforcement：sync 流程偵測 settings.local.json 含框架 hook
    即拒絕/告警；prune 幽靈 entry 併入既有 hook-completeness 的 --fix flag（opt-in，只刪 command 指向不存在檔的
    entry），不開新命令
acceptance:
- '[ ] sync 端 enforcement 已拆分至 1.4.0-W2-013.6（既有 SessionStart WARNING 已偵測；本票聚焦 prune
  --fix，PM 範圍決策）'
- '[ ] prune 幽靈 entry 併入 hook-completeness --fix（opt-in 只刪不存在檔 entry）'
- '[ ] 不新增獨立 prune 命令'
- '[ ] 完整 log + 不誤刪 working hook'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-24T01:19:31'
completed_at: null
created: '2026-06-24'
updated: '2026-06-24'
---

# Execution Log

## Task Summary

實作 sync 端 framework hook enforcement（sync-pull 偵測並拒絕框架 hook 留在 local）+ prune 併入 hook-completeness --fix flag

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
TDD（feat/1.4.0-W2-013.4-sync-hook-enforcement 分支）：
- RED：新增 tests/test_hook_completeness_prune.py（8 案：回報幽靈/dry-run 不寫/apply 刪除/保留 working/保留 inline/混合只刪幽靈/缺檔回空/無 hooks key 回空），初跑 8 failed（AttributeError 函式未實作）。
- GREEN：實作 prune_phantom_local_registrations + main() --fix opt-in 區塊後 8 passed。
- 回歸：prune + reverse-check + autocommit 三檔合跑 23 passed。
- smoke：SessionStart 模式（無 --fix）exit 0 行為不變；--fix 模式正常（當前 local 無 hook → 印「無需修正」）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
變更摘要：hook-completeness-check.py 新增 prune_phantom_local_registrations（opt-in，只刪 settings.local.json 中 command 指向不存在 .py 檔的幽靈 hook entry，保留 working hook 與 inline 指令，全刪後清理空 hooks 結構）+ main() --fix 旗標（偵測層一律執行、移除動作僅 --fix 觸發）。新增 tests/test_hook_completeness_prune.py（8 案）。
範圍決策（PM）：原 acceptance 含「sync 端偵測並拒絕框架 hook 留在 local」，依 linux 審查「最小方案=去重+既有 WARNING+prune --fix」拆出為 1.4.0-W2-013.6（belt-and-suspenders，既有 SessionStart find_local_hook_registrations 已偵測，且動 2280 行 sync 腳本風險高，獨立追蹤）。本票聚焦 prune --fix remediation 缺口。
acceptance：條目 2（prune 併入 --fix）✅、3（不新增獨立命令，folded 為 flag）✅、4（完整 log + 不誤刪 working/inline）✅；條目 1 拆至 013.6。
僅作用 settings.local.json（不可自癒層）；settings.json 幽靈由 sync 自癒屬 SSOT 不自動改寫。
