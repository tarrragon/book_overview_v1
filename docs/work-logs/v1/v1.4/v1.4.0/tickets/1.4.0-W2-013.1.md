---
id: 1.4.0-W2-013.1
title: 移除 settings.local.json handoff-auto-resume 重複註冊（去重，真正的 fix）
type: IMP
status: in_progress
version: 1.4.0
wave: 2
priority: P2
parent_id: 1.4.0-W2-013
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager
  history: {}
what: 移除 settings.local.json handoff-auto-resume 重複註冊（去重，真正的 fix）
when: v1.4.0 W2，W2-013 收尾後優先（解當前唯一實證地雷 F1）
where:
  layer: 待定義
  files:
  - .claude/settings.local.json
why: 'framework issue #11：框架版本遷移（2.0 hooks 搬移）時 sync-pull 更新 settings.json 但無法觸及 settings.local.json（per-project
  不同步），stale hook 註冊成幽靈阻擋派發。需框架層機制應對版本遷移攜帶 settings 變更'
how:
  task_type: Implementation
  strategy: 從 settings.local.json 移除 Stop handoff-auto-resume-stop-hook 註冊（settings.json
    已有副本，去重安全）；驗證 SessionStart HookCheck 不再報重複註冊 WARNING
acceptance:
- '[ ] settings.local.json 不含 handoff-auto-resume 註冊'
- '[ ] settings.json 仍保留該 hook 註冊'
- '[ ] SessionStart HookCheck 無重複註冊 WARNING'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-24T00:57:42'
completed_at: null
created: '2026-06-24'
updated: '2026-06-24'
---

# Execution Log

## Task Summary

移除 settings.local.json handoff-auto-resume 重複註冊（去重，真正的 fix）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
驗證（手動執行 hook-completeness-check + grep 計數）：
1. settings.local.json 移除 hooks key 後 JSON 合法（python json.load 通過，has hooks key: False）。
2. grep -c handoff-auto-resume：settings.local.json=0、settings.json=1（去重正確，settings.json 保留）。
3. 手動跑 .claude/hooks/hook-completeness-check.py（exit 0），grep「重複註冊 / settings.local.json 內含 / handoff-auto-resume」皆無命中 → 無重複註冊 WARNING、無 latent ghost WARNING。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
變更摘要：從 .claude/settings.local.json 移除整個 hooks key（僅含 Stop: handoff-auto-resume-stop-hook 一筆框架資產註冊）。settings.json 保留該註冊作單一來源。消滅 W2-013 F1 唯一實證地雷（雙重註冊休眠地雷）。
備註：settings.local.json 為 gitignored 未追蹤檔，無 commit；ticket md 由 complete auto-stage 處理。PM 前台執行（subagent 無法寫 .claude/，PC-115）。
