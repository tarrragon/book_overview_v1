---
id: 1.6.1-W3-007
title: 評估 PlatformSwitcherService.attemptFallback 無窮遞迴風險
type: ANA
status: in_progress
version: 1.6.1
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: saffron-system-analyst
  history: {}
what: attemptFallback 在目標平台持續失敗時形成無窮遞迴，無重試上限保護
when: W3
where:
  layer: src
  files:
  - src/background/domains/platform/services/platform-switcher-service.js
why: W3-004 SR-2 發現，實測已觸發 heap OOM
how:
  task_type: Implementation
  strategy: 分析遞迴路徑並評估保護措施
acceptance:
- '[ ] 分析完成;修復方案已提出'
tdd_phase: phase0
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-07-24T09:40:32'
completed_at: null
created: '2026-07-23'
updated: '2026-07-23'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

attemptFallback 在目標平台持續失敗時形成無窮遞迴，無重試上限保護

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

`attemptFallback` (L589) 與 `switchToPlatform` (L236) 之間存在無保護的互相遞迴呼叫：

**遞迴鏈**：`switchToPlatform` 失敗 → catch block L307-308 呼叫 `attemptFallback` → `attemptFallback` L597/L617 呼叫 `switchToPlatform` → 再次失敗 → 再次 `attemptFallback` → 無窮迴圈

**三個失效的防護點**：

| 防護點 | 現狀 | 應有行為 |
|--------|------|---------|
| `maxRetries: 3` (L62) | 已定義但從未在邏輯中使用（僅出現於日誌 L177） | 應作為遞迴深度上限 |
| `isFallback: true` (L599/L619) | `attemptFallback` 傳入此選項，但 `switchToPlatform` catch block (L307) 不檢查 | 應跳過二次 fallback |
| `concurrentLimit: 1` (L66) | 僅保護 L252 之後的路徑；驗證失敗 (L248) 在 L252 之前拋出，完全繞過並發保護 | N/A（設計目的非防遞迴） |

**直接原因**：L307-308 無條件觸發 fallback，不檢查 `options.isFallback`

### 影響範圍

**受影響檔案**：`src/background/domains/platform/services/platform-switcher-service.js`（單一檔案）

**受影響功能**：
- `switchToPlatform` (L236) — 所有平台切換操作
- `attemptFallback` (L589) — 故障轉移邏輯
- `handlePlatformError` (L766) — 嚴重錯誤自動 fallback 入口 (L777)

**已驗證的事實**：
1. `maxRetries` 在此檔案中僅 L62（定義）和 L177（日誌）出現，邏輯中零使用
2. 測試檔案 L54-57 已用 `autoFallback = false` 迴避此問題（開發時已知但未修復）
3. W3-004 SR-2 實測已觸發 heap OOM

**仍未驗證的假設**：
- heap OOM 觸發路徑是驗證失敗的純遞迴還是後設路徑的死鎖 promise 堆積，需 runtime 實測確認

### 相關 Error Pattern

無直接匹配的既有 error pattern。本問題屬實作缺陷（遞迴無終止條件），非流程或框架問題。

## 重現實驗結果
### 實驗方法

靜態呼叫鏈追蹤：逐行追蹤 `attemptFallback` ↔ `switchToPlatform` 的相互呼叫路徑，識別所有遞迴入口與終止條件（或缺失的終止條件）。

### 實驗執行

**路徑 1：驗證失敗導致的純無窮遞迴**（最危險，無任何保護）

```
switchToPlatform(B) 呼叫
  → L243: validateSwitchRequest(B) 失敗（如平台未註冊/非 active）
  → L248: throw error（注意：此時尚未到達 L252 enforceConcurrencyLimit，也未到 L259 activeSwitches.set）
  → L297: catch block
  → L308: attemptFallback(switchId)
    → L594: previousPlatform 存在時 → L597: switchToPlatform(previousPlatform)
      → 若 previousPlatform 也驗證失敗 → L248 throw → L308 attemptFallback → 無窮迴圈
    → L610: previousPlatform 為 null 時 → L614-631: 遍歷 activePlatforms
      → L617: switchToPlatform(platformX) → 若失敗 → 其 catch block L308 再次呼叫 attemptFallback
      → 每個失敗的 switchToPlatform 都觸發新的 attemptFallback → 指數級遞迴展開
```

**關鍵**：驗證失敗在 L248 拋出，早於 `enforceConcurrencyLimit` (L252) 和 `activeSwitches.set` (L259)，因此 `concurrentLimit` 保護完全被繞過。這是純無窮遞迴，無任何機制可阻止。

**路徑 2：後設失敗導致的死鎖**（concurrentLimit=1 時）

```
switchToPlatform(B) 呼叫
  → L252: enforceConcurrencyLimit 通過（activeSwitches.size=0 < 1）
  → L259: activeSwitches.set(switchId1)（size=1）
  → L276: executeSwitchProcess 失敗
  → L297: catch block（注意：activeSwitches 尚未刪除，在 finally L319 才刪）
  → L308: attemptFallback(switchId1)
    → L597: switchToPlatform(previousPlatform)
      → L252: enforceConcurrencyLimit（activeSwitches.size=1 >= concurrentLimit=1）→ 進入 queue
      → queue 等待 processQueuedSwitches 解鎖
      → processQueuedSwitches 在第一次 switchToPlatform 的 finally block (L323) 才呼叫
      → 但 finally 等待 catch block 中的 attemptFallback 完成
      → 死鎖：A 等 B 完成，B 等 A 的 finally 解鎖
```

**路徑 3：handlePlatformError 觸發的遞迴入口**

```
PLATFORM.ERROR.OCCURRED 事件（severity='critical'）
  → L770-773: 條件滿足（當前平台 + critical + autoFallback + 非 switching）
  → L777: attemptFallback('error-fallback-...')
  → 進入路徑 1 或路徑 2 的遞迴鏈
```

### 實驗發現

**已驗證的事實**：
1. 存在兩種故障模式：純無窮遞迴（路徑 1，驗證失敗繞過並發保護）和死鎖（路徑 2，並發保護導致 promise 懸掛）
2. `maxRetries=3` 是死程式碼——定義於 L62，日誌記錄於 L177，邏輯中零引用
3. `isFallback: true` 由 `attemptFallback` 傳入（L599/L619），但 `switchToPlatform` catch block (L307) 不檢查此選項
4. 測試已用 `autoFallback = false` 迴避（L57），等同屏蔽了整個 fallback 功能的測試覆蓋
5. `attemptFallback` 的 else 分支 (L614-631) 遍歷所有活躍平台，每個失敗的 `switchToPlatform` 都獨立觸發 `attemptFallback`，在所有平台皆失敗時造成指數級遞迴展開
6. 存在三個觸發入口：L308（switchToPlatform catch）、L777（handlePlatformError）、L723（handleSwitchRequest 間接觸發）

**仍未驗證的假設**：
- W3-004 SR-2 實測 heap OOM 的具體觸發路徑（路徑 1 vs 路徑 2）需 runtime profiling 確認

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 分析結論

`attemptFallback` ↔ `switchToPlatform` 的互相呼叫缺少終止條件，在所有目標平台皆失敗時必然觸發無窮遞迴或死鎖。`maxRetries` 配置為死程式碼，`isFallback` 選項已傳入但未被消費。需修復遞迴保護並啟用 `maxRetries`。

### 候選方案

#### 方案 A：isFallback 守衛（最小修改，推薦）

**修改內容**：在 `switchToPlatform` catch block (L307) 加入 `isFallback` 檢查

```javascript
// L307-308 修改為：
if (this.switchConfig.autoFallback && !options.isFallback) {
    await this.attemptFallback(switchId)
}
```

| 維度 | 評估 |
|------|------|
| 修改量 | 1 行（L307 加條件） |
| 風險 | 極低——利用已存在的 `isFallback` 選項，無新概念引入 |
| 效果 | 徹底阻斷遞迴鏈：fallback 呼叫的 switchToPlatform 失敗時不再觸發二次 fallback |
| 限制 | 僅允許一層 fallback（attemptFallback 內的 switchToPlatform 失敗即停止） |
| 測試影響 | 測試可移除 `autoFallback = false` 覆蓋，改為正常測試 fallback 行為 |

#### 方案 B：深度計數器 + maxRetries 啟用（縱深防禦）

**修改內容**：
1. `attemptFallback` 新增 `depth` 參數，遞迴時遞增
2. `switchToPlatform` 透過 options 傳遞 `_fallbackDepth`
3. 達到 `maxRetries` 時停止遞迴

```javascript
// attemptFallback 簽名變更：
async attemptFallback(failedSwitchId, depth = 0) {
    if (depth >= this.switchConfig.maxRetries) {
        await this.log(`故障轉移已達最大重試次數 (${this.switchConfig.maxRetries})，停止`)
        return
    }
    // ... 內部 switchToPlatform 呼叫加入 _fallbackDepth: depth + 1
}

// switchToPlatform catch block 變更：
if (this.switchConfig.autoFallback) {
    await this.attemptFallback(switchId, (options._fallbackDepth || 0))
}
```

| 維度 | 評估 |
|------|------|
| 修改量 | 約 10 行（attemptFallback 簽名 + 3 處 switchToPlatform 呼叫 + catch block） |
| 風險 | 低——`maxRetries` 已存在，新增參數與既有 API 相容 |
| 效果 | 允許最多 N 次 fallback 嘗試後優雅停止；啟用先前的死程式碼配置 |
| 限制 | 多層 fallback 在所有平台皆失敗時仍會嘗試 N 次才停止（但有上限） |
| 測試影響 | 同方案 A，並可新增 maxRetries 邊界測試 |

### 方案比較

| 比較項 | 方案 A | 方案 B |
|--------|--------|--------|
| 修改量 | 1 行 | ~10 行 |
| 防護強度 | 單層（一次 fallback 機會） | 多層（maxRetries 次機會） |
| 死程式碼消除 | 否（maxRetries 仍不使用） | 是（maxRetries 啟用） |
| 複雜度增加 | 無 | 低（新增 depth 參數） |
| 向後相容 | 完全相容 | 完全相容（新參數有預設值） |

**建議**：方案 A + 方案 B 組合實施。方案 A 作為主防護（阻斷遞迴鏈），方案 B 作為縱深防禦（限制最大重試次數並啟用死程式碼）。

### Spawn 規劃表

| 類型 | 標題 | 優先級 | 說明 |
|------|------|--------|------|
| IMP | 修復 attemptFallback 無窮遞迴（方案 A+B 組合） | 高 | 實作 isFallback 守衛 + 深度計數器 + maxRetries 啟用 |

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## Context Bundle

### 關鍵檔案

### src/background/domains/platform/services/platform-switcher-service.js (965 行)
- attemptFallback(failedSwitchId)：L589，被 L308 和 L777 呼叫
- L597-605：嘗試切回 previousPlatform（呼叫 switchToPlatform）
- L617-622：遍歷其他平台逐一嘗試 switchToPlatform
- switchToPlatform 失敗路徑（L308）再次呼叫 attemptFallback → 潛在無窮遞迴
- 無 MAX_RETRIES / retry 計數器保護
- W3-004 SR-2 發現，實測已觸發 heap OOM

### 分析方向
1. 追蹤 attemptFallback → switchToPlatform → attemptFallback 遞迴路徑
2. 評估在哪一層加重試上限最合適
3. 提出至少 2 個修復方案（含利弊）
4. 若需建 IMP ticket 修復，在 Solution 的 Spawn 規劃表列出

### 注意
ANA 類型：只分析不修改程式碼。必填章節：Problem Analysis + 重現實驗結果 + Solution

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending
