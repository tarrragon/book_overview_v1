---
id: 1.6.1-W3-007
title: 評估 PlatformSwitcherService.attemptFallback 無窮遞迴風險
type: ANA
status: in_progress
version: 1.6.1
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: saffron-system-analyst
  history: {}
what: attemptFallback 在目標平台持續失敗時形成無窮遞迴，無重試上限保護
when: W3
where:
  layer: src
  files:
  - src/background/domains/platform/services/platform-switcher-service.js
why: W3-004 SR-2 發現，實測已觸發 heap OOM
how:
  task_type: Implementation
  strategy: 分析遞迴路徑並評估保護措施
acceptance:
- '[ ] 分析完成;修復方案已提出'
tdd_phase: phase0
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-07-24T09:40:32'
completed_at: null
created: '2026-07-23'
updated: '2026-07-23'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

attemptFallback 在目標平台持續失敗時形成無窮遞迴，無重試上限保護

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

`attemptFallback` (L589) 與 `switchToPlatform` (L236) 之間存在無保護的互相遞迴呼叫：

**遞迴鏈**：`switchToPlatform` 失敗 → catch block L307-308 呼叫 `attemptFallback` → `attemptFallback` L597/L617 呼叫 `switchToPlatform` → 再次失敗 → 再次 `attemptFallback` → 無窮迴圈

**三個失效的防護點**：

| 防護點 | 現狀 | 應有行為 |
|--------|------|---------|
| `maxRetries: 3` (L62) | 已定義但從未在邏輯中使用（僅出現於日誌 L177） | 應作為遞迴深度上限 |
| `isFallback: true` (L599/L619) | `attemptFallback` 傳入此選項，但 `switchToPlatform` catch block (L307) 不檢查 | 應跳過二次 fallback |
| `concurrentLimit: 1` (L66) | 僅保護 L252 之後的路徑；驗證失敗 (L248) 在 L252 之前拋出，完全繞過並發保護 | N/A（設計目的非防遞迴） |

**直接原因**：L307-308 無條件觸發 fallback，不檢查 `options.isFallback`

### 影響範圍

**受影響檔案**：`src/background/domains/platform/services/platform-switcher-service.js`（單一檔案）

**受影響功能**：
- `switchToPlatform` (L236) — 所有平台切換操作
- `attemptFallback` (L589) — 故障轉移邏輯
- `handlePlatformError` (L766) — 嚴重錯誤自動 fallback 入口 (L777)

**已驗證的事實**：
1. `maxRetries` 在此檔案中僅 L62（定義）和 L177（日誌）出現，邏輯中零使用
2. 測試檔案 L54-57 已用 `autoFallback = false` 迴避此問題（開發時已知但未修復）
3. W3-004 SR-2 實測已觸發 heap OOM

**仍未驗證的假設**：
- heap OOM 觸發路徑是驗證失敗的純遞迴還是後設路徑的死鎖 promise 堆積，需 runtime 實測確認

### 相關 Error Pattern

無直接匹配的既有 error pattern。本問題屬實作缺陷（遞迴無終止條件），非流程或框架問題。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

（必填：分析結論、至少 2 個候選方案含利弊、Spawn 規劃表）

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

（選填：若有實驗輸出才填，無實驗可留 placeholder）

---

## Context Bundle

### 關鍵檔案

### src/background/domains/platform/services/platform-switcher-service.js (965 行)
- attemptFallback(failedSwitchId)：L589，被 L308 和 L777 呼叫
- L597-605：嘗試切回 previousPlatform（呼叫 switchToPlatform）
- L617-622：遍歷其他平台逐一嘗試 switchToPlatform
- switchToPlatform 失敗路徑（L308）再次呼叫 attemptFallback → 潛在無窮遞迴
- 無 MAX_RETRIES / retry 計數器保護
- W3-004 SR-2 發現，實測已觸發 heap OOM

### 分析方向
1. 追蹤 attemptFallback → switchToPlatform → attemptFallback 遞迴路徑
2. 評估在哪一層加重試上限最合適
3. 提出至少 2 個修復方案（含利弊）
4. 若需建 IMP ticket 修復，在 Solution 的 Spawn 規劃表列出

### 注意
ANA 類型：只分析不修改程式碼。必填章節：Problem Analysis + 重現實驗結果 + Solution

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending
