---
id: 1.6.0-W4-002
title: 實作 event-coordinator 書目 ID 正規化（bookId→platformBookId 映射 + UUID 自動生成）
type: IMP
status: in_progress
version: 1.6.0
wave: 4
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 1.6.0-W3-007
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: Phase 3b 實作 ID 正規化
  rationale: W3-007 ANA 根因修復
who:
  current: 待派發
  history: {}
what: 實作 event-coordinator 書目 ID 正規化（bookId→platformBookId 映射 + UUID 自動生成）
when: W3-007 ANA 完成後
where:
  layer: 待定義
  files:
  - src/background/events/event-coordinator.js
why: 各 adapter ID 欄位名不統一且正規化層未映射，導致書目缺 id、checkbox 不渲染
how:
  task_type: Implementation
  strategy: 在 event-coordinator L556-574 正規化 map 內新增 ID 正規化邏輯
acceptance:
- '[ ] event-coordinator 正規化邏輯新增 bookId→platformBookId 映射'
- '[ ] id 為 falsy 時自動生成 UUID（crypto.randomUUID）'
- '[ ] 所有 adapter 提取的書目存入 storage 後皆有 truthy id'
- '[ ] 新增測試驗證正規化覆蓋 Readmoo/Kobo/博客來三種 adapter 欄位結構'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-16T12:29:05'
completed_at: null
created: '2026-07-14'
updated: '2026-07-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實作 event-coordinator 書目 ID 正規化（bookId→platformBookId 映射 + UUID 自動生成）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因（從 W3-007 ANA 確認）

event-coordinator.js L556-574 正規化 map callback 只做 tags/readingStatus/applyDefaults，**不處理 ID**：
- 不映射 `bookId` → `platformBookId`
- 不為 `id` 為 falsy 的書目生成 UUID

各 adapter ID 欄位名不統一：
| Adapter | 欄位名 | 值來源 |
|---------|--------|--------|
| readmoo | `id` | stable-id-generator（4 策略 cascade，fallback 保證 truthy） |
| kobo | `bookId` | DOM 提取 ebook slug |
| books-com-tw | `bookId` | `info.item`（可為空字串 `''`） |

### 實作策略

在 L566 `const withStatus = { ...b, tags, readingStatus }` 之後、`applyDefaults()` 之前，新增 ID 正規化邏輯：

1. **`bookId` → `platformBookId` 映射**：若 `b.bookId` 存在且 truthy，寫入 `platformBookId`
2. **`id` 自動生成**：若合併後 `id` 仍為 falsy，用 `crypto.randomUUID()` 生成（Chrome SW 環境原生支援）
3. **保留既有 `id`**：readmoo 的 `id` 由 stable-id-generator 產生，不覆蓋

順序理由：ID 正規化必須在 `applyDefaults()` 之前——`BookSchemaV2` 的 `id` 定義為 `required: true` 但無 `default`，applyDefaults 不會補 `id`。

### 影響範圍

修改檔案：`src/background/events/event-coordinator.js`（L556-574 正規化 map 內新增約 5-8 行）
新增測試：`tests/unit/background/events/event-coordinator-id-normalization.test.js`

### 相關 Error Pattern

無直接相關 error pattern。

### Context Bundle

- **source_ticket**: 1.6.0-W3-007（四層缺口分析，本 ticket 修復 L2 正規化層）
- **where.files**: `src/background/events/event-coordinator.js`
- **正規化插入點**: L566-567 之間（`withStatus` 構建後、`applyDefaults` 前）
- **ID 生成方案**: `crypto.randomUUID()`（Chrome 92+ Service Worker 原生可用）
- **三種 adapter 欄位結構**: readmoo(`id`) / kobo(`bookId`) / books-com-tw(`bookId`)
- **commit policy**: 新建 feature branch，測試通過後 commit

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 1.6.0-W3-007 | chars: 302 -->

### Task Reference
- 1.6.0-W3-007 what: 分析 書目缺少 ID 導致 Overview checkbox 不顯示

### Rationale Chain
- 1.6.0-W3-007 why: 用戶回報 Overview 書目列表部分書籍無勾選框。根因：_getBookId() 依賴 book.id || book.bookId，部分書目兩欄位皆為 falsy，導致 checkbox 不渲染（L813 if(bookId) 不通過）

### Related Files
- src/overview/overview-page-controller.js  # from 1.6.0-W3-007
- src/content/  # from 1.6.0-W3-007

### Source Acceptance (reference)
- [ref] [ ] 確認哪些書缺少 id/bookId 欄位及其比例  # from 1.6.0-W3-007
- [ref] [ ] 找出提取流程中 ID 未賦值的根因  # from 1.6.0-W3-007
- [ref] [ ] 確認是提取端問題還是資料遷移問題  # from 1.6.0-W3-007
- [ref] [ ] 提出修復方案（含既有無 ID 資料的補救）  # from 1.6.0-W3-007
