---
id: 1.6.0-W2-002
title: 修復 book-data-extractor 頁面類型判斷委派給 adapter
type: IMP
status: in_progress
version: 1.6.0
wave: 2
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 修復多書城頁面類型判斷
  rationale: 阻擋博客來和 Kobo 核心提取功能
who:
  current: dill-javascript-developer
  history: {}
what: 修復 book-data-extractor 頁面類型判斷委派給 adapter
when: v1.6.0 W2
where:
  layer: 待定義
  files:
  - src/content/extractors/book-data-extractor.js
why: extractor 的 getPageType 為 Readmoo 硬編碼（只認 /library /shelf /book/），博客來 URL (/viewer/index.html)
  不匹配導致提取失敗
how:
  task_type: Implementation
  strategy: extractor 接受 setPlatformAdapter 後優先呼叫 adapter 方法，fallback 到既有邏輯
acceptance:
- '[ ] book-data-extractor.getPageType() 優先使用 platformAdapter.getPageType()'
- '[ ] book-data-extractor.isExtractablePage() 優先使用 platformAdapter.isExtractablePage()'
- '[ ] 博客來書庫頁面提取不再顯示「不支援的頁面類型: unknown」'
- '[ ] Readmoo 既有功能不受影響（向後相容）'
tdd_phase: phase3b
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-14T10:07:03'
completed_at: null
created: '2026-07-14'
updated: '2026-07-14'
protocol_version: '2.0'
tdd_phase_source: auto
---

# Execution Log

## Task Summary

修復 book-data-extractor 頁面類型判斷委派給 adapter

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 修改內容

`src/content/extractors/book-data-extractor.js`：

1. `getPageType()` 改為 `async`，優先呼叫 `platformAdapter.getPageType()`（若存在此方法），
   回傳非 `unknown` 時直接採用；否則（或適配器無此方法）退回既有 Readmoo 硬編碼路徑判斷
   （`/library` / `/shelf` / `/book/`、`/api/reader/`）。
2. `isExtractablePage()` 改為 `async`，優先呼叫 `platformAdapter.isExtractablePage()`
   （若存在此方法）；否則退回以 `getPageType()` 結果判斷（`library`/`shelf`）。
3. 內部呼叫端同步改為 `await`：`checkPageReady()`、`startExtractionFlow()`。

### 向後相容性

ReadmooAdapter（`src/content/adapters/readmoo-adapter.js`）未實作 `getPageType`/
`isExtractablePage` 方法，`typeof platformAdapter.getPageType === 'function'` 為
`false`，故 Readmoo 情境維持走原硬編碼判斷邏輯，行為不變。

### 測試調整

兩處既有測試同步呼叫 `getPageType()`/`isExtractablePage()`（未 `await`），因方法改為
`async` 而需補上 `await`（純呼叫方式調整，斷言邏輯未變）：
- `tests/unit/content/modular/content-modular.test.js`
- `tests/integration/chrome-extension/content-script-extractor.test.js`

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Context Bundle

### 目標

修改 book-data-extractor.js 的 getPageType() 和 isExtractablePage() 委派給 platformAdapter，解決博客來和 Kobo 提取失敗。

### 根因

book-data-extractor.js:87-104 的 getPageType() 硬編碼只認 /library、/shelf、/book/ 路徑。博客來 URL (/viewer/index.html?readlist=all) 不含這些路徑，回傳 unknown，startExtractionFlow 第 176 行拒絕提取。

### 關鍵檔案

- src/content/extractors/book-data-extractor.js — 修改 getPageType() 和 isExtractablePage()
- src/content/content-modular.js:92 — 已有 bookDataExtractor.setPlatformAdapter(platformAdapter)

### 實作策略

1. 在 getPageType() 中：若 this.platformAdapter 存在，先 await this.platformAdapter.getPageType()，結果非 unknown 則直接回傳；否則 fallback 到既有硬編碼邏輯
2. 在 isExtractablePage() 中：若 this.platformAdapter 存在，先用 await this.platformAdapter.isExtractablePage()；否則 fallback 到既有邏輯
3. 注意 adapter 的 getPageType 是 async，extractor 的是 sync — 需確認呼叫端是否已在 async context
4. 既有 Readmoo 路徑不受影響（ReadmooAdapter.getPageType 也回傳 library）

### commit policy

- 分支: feat/1.6.0-W2-002-extractor-adapter-delegation
- commit 前綴: fix(1.6.0-W2-002)

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending
