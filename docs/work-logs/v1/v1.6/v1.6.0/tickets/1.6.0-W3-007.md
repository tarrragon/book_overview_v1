---
id: 1.6.0-W3-007
title: 分析 書目缺少 ID 導致 Overview checkbox 不顯示
type: ANA
status: in_progress
version: 1.6.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:分析
  final_decision: 分析書目 ID 缺失根因與影響範圍
  rationale: 用戶回報 UI 異常，需釐清是提取端 bug 還是歷史資料問題
who:
  current: rosemary-project-manager
  history: {}
what: 分析 書目缺少 ID 導致 Overview checkbox 不顯示
when: v1.6.0 W3
where:
  layer: 待定義
  files:
  - src/overview/overview-page-controller.js
  - src/content/
why: 用戶回報 Overview 書目列表部分書籍無勾選框。根因：_getBookId() 依賴 book.id || book.bookId，部分書目兩欄位皆為
  falsy，導致 checkbox 不渲染（L813 if(bookId) 不通過）
how:
  task_type: Implementation
  strategy: 檢查 storage 中書目資料結構，比對有 ID 與無 ID 書目的差異，追溯提取流程 ID 賦值邏輯
acceptance:
- '[ ] 確認哪些書缺少 id/bookId 欄位及其比例'
- '[ ] 找出提取流程中 ID 未賦值的根因'
- '[ ] 確認是提取端問題還是資料遷移問題'
- '[ ] 提出修復方案（含既有無 ID 資料的補救）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-07-14T15:32:21'
completed_at: null
created: '2026-07-14'
updated: '2026-07-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

分析 書目缺少 ID 導致 Overview checkbox 不顯示

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 用戶補充設計方向

多書城場景下，書目 ID 不應依賴各書城原始 ID（不同書城 ID 體系不同，可能為 null）。正確做法：

- **書目 ID**：由系統自行生成（如 UUID），作為唯一識別符
- **書城原始 ID**：作為備用欄位記錄（如 `platformBookId`），供回查書城用

### 初步根因分析

Overview `_getBookId(book)` 依賴 `book.id || book.bookId`（overview-page-controller.js:428-431）。
部分書目兩欄位皆為 falsy，導致 checkbox 不渲染（L813 `if (bookId)` 不通過）。
截圖顯示約 3/8 本書缺少 checkbox，比例不低。

### 跨專案影響（用戶補充）

書目來源不只書城提取，還有 APP 端掃描建立。兩個專案（Chrome Extension + Flutter APP）都必須自建 ID：

- **Chrome Extension**：從書城提取的書目
- **Flutter APP**：用戶手動掃描（ISBN barcode）建立的書目

兩端產生的書目最終會透過同步機制交換，ID 體系必須統一。此問題影響範圍跨 V1（Extension）和 V2（APP）兩個專案。
### ISBN 不適合作唯一 ID（用戶補充）

ISBN 會因再版或重新出版而變動，但在用戶認知中仍是「同一本書」。系統已支援將多本書（不同 ISBN/不同版本）歸類到同一本書底下。

因此 ID 策略需考慮三層結構：

| 層級 | 含義 | 範例 |
|------|------|------|
| 書目 ID（自建） | 系統內唯一識別符 | UUID v4 |
| 書城原始 ID | 各書城平台的商品 ID | platformBookId |
| ISBN | 出版識別碼，同一本書可能有多個 | 再版/重出版/不同格式 |

關鍵約束：同一本「邏輯書」可對應多個 ISBN、多個書城商品、多個書目記錄。系統已有「歸類到同一本書」的功能支援這個合併語意。ID 生成策略不能用 ISBN 作為 natural key 去重——去重應在更高的「邏輯書」層級處理。
## 重現實驗結果
### 實驗方法

以靜態程式碼分析追蹤資料流：adapter 提取 → event-coordinator 正規化 → Chrome Storage 儲存 → overview 載入 → checkbox 渲染。

### 實驗執行

**步驟 1：追蹤 `_getBookId()` 與 checkbox 渲染邏輯**

`overview-page-controller.js:428-431`：
```js
_getBookId (book) {
    if (!book) return null
    return book.id || book.bookId || null
}
```

`overview-page-controller.js:810-828`：`if (bookId)` 為 falsy 時不建立 checkbox DOM 元素。

**步驟 2：各 adapter 的 ID 欄位名稱盤點**

| Adapter | 提取路徑 | ID 欄位名 | ID 來源 |
|---------|---------|----------|--------|
| readmoo-adapter | `extractBookData()` L496 | `id` | `generateStableBookIdWithInfo()` 4 策略 cascade（reader/cover/title/fallback），fallback 保證 truthy |
| kobo-adapter | `parseBookElement()` L222 | `bookId` | `_extractBookId(element)` — 從 DOM 提取 |
| books-com-tw API | `_mapApiRecordToBookData()` L375 | `bookId` | `info.item`（可能為空字串 `''`） |
| books-com-tw DOM | `parseBookElement()` L230-236 | **無 ID 欄位** | 未設計 — 完全缺失 |

**步驟 3：儲存層驗證缺口**

- `event-coordinator.js:556-574`：正規化只做 tags/readingStatus/applyDefaults，**不映射 `bookId` → `id`**，**不自動生成 `id`**
- `BookSchemaV2.applyDefaults()` L108-118：只補有 `default` 的欄位；`id` 定義為 `required: true` 但**無 `default`**，不會被補上
- `event-coordinator.js:589`：`chrome.storage.local.set()` **不呼叫 `validateBook()`**，無 ID 的書目直接存入
- `storage-save-handler.js:205`：有 `validateBooksStructure` 檢查 `book.id`，但 event-coordinator 未呼叫此 handler

**步驟 4：Overview 載入範圍**

- `overview-page-controller.js:510`：`chrome.storage.local.get(['readmoo_books'])` — **只讀 Readmoo**
- `loadAllPlatformBooks()` 存在於 `multi-platform-storage.js` 但 overview **未使用**
- L286：storage change listener 也只監聽 `changes.readmoo_books`

**步驟 5：遷移對無 ID 書目的處理**

- `cover-to-reader.js:180-182`：`if (!book.id)` → 跳過（logged but not fixed）
- `v1-to-v2.js:201-203`：`if (!book.id)` → 跳過（同上）
- 遷移不補 ID，只丟棄無 ID 書目

### 實驗發現

**已驗證事實**：

1. 各 adapter ID 欄位名稱不統一（`id` / `bookId` / 無），是結構性問題
2. 正規化層（event-coordinator）不做 `bookId → id` 映射也不自動生成 `id`
3. 儲存時不做 BookSchemaV2 驗證，無 ID 書目可直接存入
4. Overview 只載入 `readmoo_books`，多書城書目不可見
5. 遷移邏輯對無 ID 書目採「丟棄」策略
6. Readmoo 的 stable-id-generator 有 fallback（`'reader-undefined'`），新提取書目理論上必有 `id`
7. `_getBookId()` 已考慮 `book.id || book.bookId` 兩種命名，但無法處理「兩者皆空」

**仍未驗證假設**：

- 用戶截圖中 3/8 無 checkbox 書目的實際 storage 資料結構未確認（需實機檢查）
- 歷史 Readmoo 資料（stable-id-generator 實裝前）是否殘留無 ID 書目未確認
- 匯入功能（json-importer）匯入的書目是否可能缺 ID 未確認

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 根因診斷

書目缺少 ID 是**四層缺口疊加**的結果：

| 層級 | 缺口 | 影響 |
|------|------|------|
| L1: Adapter 層 | ID 欄位名不統一（`id` / `bookId` / 無） | 不同書城書目欄位語意不對齊 |
| L2: 正規化層 | event-coordinator 不映射 `bookId → id`、不自動生成 | 欄位差異未被攔截 |
| L3: 儲存驗證層 | `validateBook()` 未被呼叫 | 無 ID 書目靜默存入 |
| L4: 展示層 | overview 只讀 `readmoo_books` | 多書城書目不可見 |

用戶截圖中的 3/8 無 checkbox 書目，推測來源：歷史 Readmoo 資料（stable-id-generator 實裝前），或匯入資料缺少 `id`。

### 修復方案

依用戶設計方向（書目 ID 由系統自建 UUID，書城原始 ID 作備用欄位）：

### 方案概述

在正規化層（event-coordinator）統一處理：每本書存入 storage 前，若 `book.id` 為 falsy，自動生成 UUID 並賦值。同時將各 adapter 的原始 ID 統一映射到 `platformBookId` 備用欄位。

### 實作項目（Spawn 規劃）

| # | Type | 標題 | 優先級 | 說明 |
|---|------|------|--------|------|
| 1 | IMP | event-coordinator 書目 ID 正規化 | P1 | L556-574 正規化邏輯新增：(a) `bookId` → `platformBookId` 映射；(b) `id` 為 falsy 時用 `crypto.randomUUID()` 生成（SW 環境可用）。確保所有書目存入 storage 前必有 `id` |
| 2 | IMP | books-com-tw DOM 路徑補 ID | P2 | `parseBookElement()` L230 補 `bookId` 欄位（從 DOM 提取書籍唯一識別——如書名連結的 URL path segment），確保 DOM 路徑不產生完全無 ID 的書目 |
| 3 | IMP | overview 多書城載入 | P2 | overview-page-controller L510 改用 `loadAllPlatformBooks()` 載入所有書城書目；L286 storage change listener 擴展監聽所有 `*_books` key |
| 4 | IMP | 既有無 ID 資料修復遷移 | P2 | 新增遷移腳本：掃描 storage 中 `id` 為 falsy 的書目，補生成 UUID。遷移時保留 `platformBookId` 指向原始書城 ID（如有） |

### 不做的事

- **不改 `_getBookId()` 做更多 fallback**：根因在上游正規化層，展示層做 fallback 只是遮掩問題
- **不在各 adapter 統一欄位名**：adapter 職責是「忠實提取書城資料」，欄位名統一是正規化層職責（Single Responsibility）
- **不用 ISBN 作 ID**：用戶已明確 ISBN 會因再版變動，不適合作唯一識別（見 Problem Analysis）

### 風險

- UUID 生成會讓同一本書在不同裝置產生不同 ID → 跨裝置同步需以 `platformBookId` + `source` 為匹配鍵做去重（已有同步機制可擴展）
- 既有資料遷移需處理「同一本書已有舊 ID」的情況 → 遷移只補 `id` 為 falsy 者，不動已有 `id` 的書目

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: 待派發
**Review Status**: pending
