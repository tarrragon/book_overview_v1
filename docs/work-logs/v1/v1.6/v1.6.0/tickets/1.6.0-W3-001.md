---
id: 1.6.0-W3-001
title: 實作 Kobo 台灣站書庫書目擷取適配器
type: IMP
status: completed
version: 1.6.0
wave: 3
priority: P2
parent_id: null
children:
- 1.6.0-W3-001.1
- 1.6.0-W3-001.2
blockedBy:
- 1.6.0-W2-001
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 實作 Kobo 適配器
  rationale: 擷取是 Extension 核心操作
who:
  current: 待派發
  history: {}
what: 實作 Kobo 台灣站書庫書目擷取適配器
when: v1.6.0 W3
where:
  layer: 待定義
  files:
  - src/content/adapters/kobo-adapter.js
  - src/content/
why: Extension 核心功能：從 Kobo 擷取書目資料
how:
  task_type: Implementation
  strategy: 建立 KoboAdapter 實作純 DOM 解析路徑，SSR 分頁透過 background fetch 取各頁 HTML
acceptance:
- '[x] KoboAdapter 實作 PlatformAdapterInterface 的 extractAllBooks'
- '[x] SSR 分頁處理：pageSize=60 + pageNumber 翻頁直到最後一頁'
- '[x] DOM 解析正確提取書名/作者/類別/系列/狀態/日期/封面/slug'
- '[x] 擷取結果寫入 chrome.storage.local'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-07-16T15:31:14'
completed_at: '2026-07-16T16:07:23'
created: '2026-07-14'
updated: '2026-07-14'
protocol_version: '2.0'
---

# Execution Log

## Task Summary

實作 Kobo 台灣站書庫書目擷取適配器

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

KoboAdapter 的 `extractAllBooks()` 目前僅解析當前頁面 DOM。Kobo 書庫是 SSR 多頁應用，超過 60 本書需透過 URL 參數 `pageNumber` 翻頁。Content script 無法自行 navigate（會失去執行 context），需透過 background service worker 代理 fetch 後續頁面 HTML。

### 影響範圍

| 檔案 | 變更類型 |
|------|---------|
| `src/content/adapters/kobo-adapter.js` | 修改 `extractAllBooks()` 加入分頁迴圈 |
| `src/background/messaging/content-message-handler.js` | 新增 `FETCH_PAGE_HTML` message type |
| `src/core/constants/` 或既有 message constants | 新增 message type 常數 |
| `tests/` | 新增/修改對應測試 |

### 架構策略

**SSR 分頁 fetch 路徑**：

```
Content Script (KoboAdapter)
  ├─ 解析當前頁面 (page 1)
  ├─ chrome.runtime.sendMessage({ type: 'FETCH_PAGE_HTML', url: page2Url })
  │   └─ Background SW: fetch(url) → response.text() → sendResponse({ html })
  ├─ DOMParser 解析回傳 HTML
  ├─ 重複直到最後一頁
  └─ 彙整所有書籍
```

**停止條件**：DOM 中 `li.item-wrapper.book` 數量 < 60（最後一頁），或 `.page-navigation` 不存在

**分頁參數**：`pageSize=60`（最大值）+ `pageNumber=N`（從 1 開始）

### 相關 Error Pattern

無直接相關。需注意 Chrome Extension 的 CORS 限制：background SW fetch 同域（kobo.com）頁面需 manifest `host_permissions` 配合。

### Context Bundle

**規格文件**：`docs/bookstores/kobo.md`（DOM 結構、分頁機制、選擇器速查）
**既有 adapter**：`src/content/adapters/kobo-adapter.js`（W2 成果，單頁解析已完成）
**介面契約**：`src/content/platform/platform-adapter-interface.js`（17 個抽象方法）
**參考模式**：`src/content/adapters/books-com-tw-adapter.js`（API 分頁 + DOM fallback 雙路徑）
**訊息系統**：`src/background/messaging/content-message-handler.js`（現有 message types）

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 功能規格

### 1. Background Fetch Message Protocol（新增跨 context 通訊能力）

**新 message type**: `CONTENT.FETCH.PAGE_HTML`

| 項目 | 值 |
|------|---|
| 方向 | Content Script → Background SW |
| 用途 | Content script 請求 background 代理 fetch SSR 頁面 HTML |
| 安全性 | Background handler 驗證 URL hostname 屬已註冊平台 |

**Request**:
```js
chrome.runtime.sendMessage({
  type: 'CONTENT.FETCH.PAGE_HTML',
  url: 'https://www.kobo.com/tw/zh/library/books?pageSize=60&pageNumber=2',
  platform: 'kobo'
})
```

**Response**:
```js
{ success: true, html: '<html>...</html>', status: 200 }
// 或
{ success: false, error: 'Fetch failed: 403 Forbidden' }
```

**安全約束**:
- handler 驗證 `url` hostname 屬已註冊平台域名（防任意 URL fetch）
- 超時 30 秒
- 回應大小無硬上限（一頁 HTML 約 200-500KB）

### 2. KoboAdapter `extractAllBooks()` 分頁迴圈

**修改範圍**: `extractAllBooks()` 方法

**流程**:
```
1. 解析當前頁面 DOM（page 1，沿用既有邏輯）
2. 從篩選器 "(N)" 取得總筆數 → 計算總頁數 ceil(total / 60)
3. for pageNumber = 2 to totalPages:
   a. 建構 URL: baseUrl + ?pageSize=60&pageNumber=N
   b. chrome.runtime.sendMessage({ type: 'CONTENT.FETCH.PAGE_HTML', ... })
   c. DOMParser 解析回傳 HTML
   d. querySelectorAll('li.item-wrapper.book') 取書目元素
   e. 逐一 parseBookElement → sanitizeData
   f. 追加至結果陣列
4. 回傳全部書籍
```

**停止條件**（防禦性，任一成立即停止）:
- pageNumber > 計算的 totalPages
- 該頁書目元素數量 = 0
- background fetch 失敗
- 累計頁數 > 100（硬上限防無限迴圈）

**新增私有方法**:
| 方法 | 職責 |
|------|------|
| `_extractTotalCount()` | 從 `.filter-chip` 的 `(N)` 文字解析總筆數 |
| `_buildPageUrl(pageNumber)` | 建構分頁 URL |
| `_fetchPageHtml(url)` | 透過 chrome.runtime.sendMessage 取得 HTML |
| `_parseHtmlToBooks(html)` | DOMParser 解析 HTML 並提取書目 |

**新增常數**:
```js
const PAGE_SIZE = 60
const MAX_PAGES = 100
const FETCH_TIMEOUT_MS = 30000
const TOTAL_COUNT_PATTERN = /\((\d+)\)/
```

### 3. manifest.json 確認

`host_permissions` 需包含 `*://*.kobo.com/*`（W2 已處理，確認即可）。

### 4. 驗收條件對齊

| AC | 實作位置 | 說明 |
|----|---------|------|
| 1. extractAllBooks | kobo-adapter.js | 修改為分頁迴圈 |
| 2. SSR 分頁 pageSize=60 | kobo-adapter.js | _buildPageUrl + 迴圈 |
| 3. DOM 解析正確 | kobo-adapter.js | 既有 parseBookElement 沿用 |
| 4. chrome.storage.local | book-data-extractor.js | 既有 pipeline 已處理（EXTRACTION.COMPLETED 事件） |

### 5. 拆分建議

Phase 1 分析後建議拆分為 2 個子 ticket：
- **W3-001.1**: Background fetch handler（新增 CONTENT.FETCH.PAGE_HTML 處理）
- **W3-001.2**: KoboAdapter 分頁迴圈（依賴 .1 的 handler，blockedBy .1）

**理由**：兩個功能職責可獨立測試、獨立驗收，且 .1 是跨書城通用能力（未來 BookWalker/Kindle 等 SSR 書城可重用）。

### 實作完成摘要

**子 ticket 狀態**：
- W3-001.1（Background fetch handler）：已完成，14/14 測試通過
- W3-001.2（KoboAdapter 分頁迴圈）：已完成，16/16 測試通過

**實作架構**：
```
Content Script (KoboAdapter.extractAllBooks)
  ├─ 解析當前頁面（page 1）
  ├─ _extractTotalCount() → 計算總頁數
  ├─ _extractRemainingPages() 分頁迴圈
  │   ├─ _buildPageUrl(pageNumber)
  │   ├─ _fetchPageHtml(url) → chrome.runtime.sendMessage
  │   │   └─ Background SW: handleFetchPageHtml
  │   │       ├─ HTTPS only + 域名白名單驗證
  │   │       └─ fetch(credentials: 'include') + 30s timeout
  │   └─ _parseHtmlToBooks(html) → DOMParser
  └─ 回傳全部書籍
```

**安全修復**：commit e1862b35e 新增 HTTPS-only 協定限制（背景安全審查 finding）

**Commits**：08666b8dc, e1862b35e, 0d04b6ac6
### Phase 4 重構評估

**評估範圍**：
- `src/background/messaging/content-message-handler.js`（+80 行）
- `src/content/adapters/kobo-adapter.js`（+100 行）

**程式碼品質**：A

| 維度 | 評估 | 說明 |
|------|------|------|
| 單一責任 | 通過 | handler 只負責 fetch 代理；adapter 分頁邏輯封裝在 `_extractRemainingPages` |
| 命名 | 通過 | 方法名清楚表達意圖（`_buildPageUrl`、`_parseHtmlToBooks`、`isAllowedFetchDomain`） |
| 認知負擔 | 通過 | `_extractRemainingPages` 認知負擔指數約 6（迴圈 + try/catch + 2 break），可接受 |
| DRY | 通過 | 常數集中管理，域名白名單統一 |
| 安全性 | 通過 | HTTPS-only + 域名白名單 + AbortController timeout + sanitizeData |
| 可觀測性 | 通過 | 分頁失敗有 `KOBO_PAGE_FETCH_FAILED` 日誌 |

**重構需要**：否

**技術債務**：無新增

**潛在改善（低優先，不建票）**：
- `_parseHtmlToBooks` 中 `parseBookElement` 的 catch 會被 `_extractRemainingPages` 的 successCount 計入——但 `parseBookElement` 是同步方法不拋異常（已有 fallback 空字串），實際無影響
- `ALLOWED_FETCH_DOMAINS` 硬編碼於 `content-message-handler.js`，未來書城增多時可考慮從 PlatformRegistry 動態讀取——但目前 3 個域名硬編碼足夠

### 自檢結果

- [x] 測試通過率 100%（6091/6091）
- [x] 新增程式碼有對應測試（30 test cases）
- [x] 無新增技術債務
- [x] 可觀測性完整（啟動/錯誤/完成日誌）
- [x] 安全性審查已處理（HTTPS-only 修復）
- [x] 常數集中管理
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 測試結果

| 測試範圍 | 結果 |
|---------|------|
| W3-001.1 測試（content-message-handler-fetch-page） | 14/14 通過 |
| W3-001.2 測試（kobo-adapter-pagination） | 16/16 通過 |
| 核心全套件（npm test: unit + integration） | 278 suites / 6091 tests 全部通過 |
| messaging 目錄回歸 | 53/53 通過 |
| adapters 目錄回歸 | 291/291 通過 |

E2E（tests/e2e/）9 suite 失敗為 Puppeteer 環境缺失，非本次修改引入。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
exit_status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Spawn Requests

<!-- agent 執行中發現應開新 ticket 的議題時，用
`ticket track add-spawn-request <id> --what ... --why ... --type ... --priority ...`
追加結構化請求。PM 處理後標記 status: processed（已建 ticket）或
dismissed（評估後不建）+ reason。 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-07-16T16:07:23
**Executing Agent**: 待派發
**Review Status**: pending
