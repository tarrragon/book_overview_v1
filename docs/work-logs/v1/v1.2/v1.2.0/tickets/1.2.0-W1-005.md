---
id: 1.2.0-W1-005
title: 分析 popup 匯入 vs overview 匯入定位差異與整合方案
type: ANA
status: in_progress
version: 1.2.0
wave: 1
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo:
- 1.2.0-W1-002
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: Phase 3b 發現規格與既有架構衝突
  rationale: quality-baseline-rule-3
who:
  current: saffron-system-analyst
  history: {}
what: 分析 popup 匯入 vs overview 匯入定位差異與整合方案
when: W1-002 Phase 3b 發現規格衝突後
where:
  layer: 待定義
  files:
  - src/overview/import/
  - src/popup/
why: 既有 src/overview/import/ 已有完整匯入系統，W1-002 規格要新建平行系統違反 DRY
how:
  task_type: Implementation
  strategy: 讀取既有匯入模組 → 比較 popup vs overview 需求差異 → 產出整合方案
acceptance:
- '[ ] 釐清 popup 匯入與 overview 匯入是否應共用同一流程'
- '[ ] 確認防舊蓋新邏輯的正確放置位置'
- '[ ] 產出修訂後的 W1-002 規格方向'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-20T19:32:26'
completed_at: null
created: '2026-06-20'
updated: '2026-06-20'
---

# Execution Log

## Task Summary

分析 popup 匯入 vs overview 匯入定位差異與整合方案

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

W1-002 Phase 1 規格設計 src/import/json-importer.js 時，未充分盤點既有 src/overview/import/ 匯入系統。既有系統已涵蓋 JSON 解析、四來源格式偵測、版本轉換、書籍驗證、合併持久化等核心功能。W1-002 的 parseAndValidate() 與 ContentParser.parse() + detectInterchangeSource() 高度重疊。

SA Phase 0 審查報告只列出零散函式可複用，未識別到 ContentParser 類別本身已是完整的 parse+validate+extract 管線。

### 影響範圍

- src/overview/import/content-parser.js — 核心解析 SSOT
- src/overview/import/file-validator.js — 檔案驗證 SSOT
- src/overview/import/file-reader.js — File API 讀取 SSOT
- src/overview/book-file-importer.js — Orchestrator
- src/overview/import-flow-controller.js — overview 專屬 UI 流程
- W1-002 規格 src/import/json-importer.js — 應取消新建

### 相關 Error Pattern

SA Phase 0 重複實作檢查粒度不足。尚無對應 PC。

## 重現實驗結果
### 實驗方法

比對 W1-002 Phase 1 規格的三個公開函式 vs 既有 src/overview/import/ 系統的等價能力。

### 實驗執行

#### 對照 1：parseAndValidate() vs ContentParser.parse()

W1-002 的 JSON.parse → ContentParser._parseJSONContent() 已有。
W1-002 的 detectInterchangeSource → ContentParser._extractBooksFromData() 內部已呼叫。
W1-002 的 BOM 移除 → ContentParser._removeBOM() 已有。
W1-002 的書籍驗證 → ContentParser._filterValidBooks() 已有。
metadata 提取 → 新需求，既有系統無。
bookCount 直接回傳 → 新需求。

結論：8 個子步驟中 6 個已有等價，僅 metadata 提取和 bookCount 是新增。

#### 對照 2：checkStaleness() — 全新功能，既有系統無對應

#### 對照 3：executeImport() vs ImportFlowController.execute()

步驟 1(parse) → ContentParser.parse() 已有。
步驟 2(staleness) → 新需求。
步驟 3(來源分流) → ContentParser._extractBooksFromData() 已有。
步驟 4(合併持久化) → tagStorageAdapter.mergeAllData/replaceAllData 已有。
步驟 5(合併摘要) → 新需求。
步驟 6(last_imported_at) → 新需求。

結論：6 個步驟中 3 個已有等價，3 個是新增。

### 實驗發現

已驗證事實：
1. ContentParser 已是完整的 parse+validate+extract 管線，支援 JSON 四來源和 CSV
2. BookFileImporter 是 ContentParser+FileValidator+FileContentReader 的 orchestrator
3. ImportFlowController 綁定 overview DOM modal，popup 不可複用此層
4. popup 目前完全無匯入功能引用
5. src/import/ 目錄不存在

核心判斷：
- 解析/驗證/轉換/持久化層（ContentParser/FileValidator/BookFileImporter/mergeAllData）是 context-free 的，popup 可直接複用
- UI 流程層（ImportFlowController 的 modal DOM 操作）是 context-bound 的，popup 不可複用

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending
