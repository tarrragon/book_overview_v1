---
id: 0.18.0-W10-054.1
title: 診斷 Windows 平台 Hook 啟動失敗無 stderr 輸出問題
type: ANA
status: pending
version: 0.18.0
wave: 10
priority: P1
parent_id: 0.18.0-W10-054
children:
- 0.18.0-W10-054.1.1
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: create-analysis-ticket
  rationale: 用戶回報 Windows 復發 macOS 已修復問題，需先 ANA 確認根因再分派 IMP；與 W10-054 編碼問題同源（Windows
    平台適配缺口）但症狀層級不同（啟動失敗 vs 執行中亂碼），設為 W10-054 子 Ticket
who:
  current: Explore
  history: {}
what: '診斷 Windows 平台 Hook 顯示 Failed with non-blocking status code: No stderr output
  的根因'
when: 用戶回報 Windows 環境下 PostToolUse:Bash hook error 並出現 No stderr output 訊息（macOS 已修復
  IMP-054 但 Windows 仍復發）
where:
  layer: Infrastructure
  files:
  - .claude/hooks/
  - .claude/settings.json
  - .claude/hook-utils/
  - .gitattributes
why: macOS 已透過 IMP-054（chmod +x）解決 Permission denied 問題，但 Windows 沒有 Unix 執行權限機制，必然另有根因。可能涉及
  shebang 不被原生 shell 解析、CRLF 換行污染 shebang、uv PATH 未設定、編碼導致 stderr 寫入前 crash、settings.json
  路徑分隔符等多種 Windows 適配缺口。No stderr 表示 Hook 啟動就失敗，連異常處理都沒機會執行，需系統性分析
how:
  task_type: Analysis
  strategy: 1) 列出所有可能根因（shebang/CRLF/PATH/編碼/路徑分隔符）並設計重現實驗 2) 請 Windows 使用者協助收集診斷資訊（chcp、python
    -c sys.stdout.encoding、git config core.autocrlf、uv --version、單一 hook 直接執行 stderr）3)
    對照 IMP-054 的修復假設驗證為何 Windows 失效 4) 提案跨平台 Hook 啟動規範（如改用 python launcher、加 .gitattributes
    強制 LF、settings.json 用 forward slash）5) 與 W10-054 編碼修復整合產出 Windows 適配指南
acceptance:
- '[ ] 已列出至少 6 種可能根因並排序可能性'
- '[ ] 已收集 Windows 環境診斷資訊（chcp/encoding/autocrlf/uv path）'
- '[ ] 已驗證 IMP-054 在 Windows 失效的具體機制'
- '[ ] 已提案跨平台 Hook 啟動規範修正'
- '[ ] 已記錄至 error-patterns/implementation/（Windows hook 啟動失敗模式）'
- '[ ] 為高可能性根因建立修復子 Ticket'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-04-15'
updated: '2026-04-15'
---

# Execution Log

## Task Summary

診斷 Windows 平台 Hook 顯示 Failed with non-blocking status code: No stderr output 的根因

---

## Problem Analysis

### 問題根因（候選清單，依可能性排序）

#### 候選 1：Microsoft Store Python Stub（致命疑點）

Windows 11 預裝 `C:\Users\<user>\AppData\Local\Microsoft\WindowsApps\python.exe`，這是一個 **Microsoft Store 啟動器存根**——執行時會打開 Store 安裝頁面後 exit，**不寫任何 stderr**。

若 Claude Code 在 Windows 上透過 `.py` 副檔名關聯啟動 hook，會呼叫到 stub 而非真實 Python，產生「No stderr output」症狀。

#### 候選 2：CRLF 污染 Shebang

`git config core.autocrlf=true`（Windows 預設）會將 hook 檔案的 LF 換成 CRLF，導致 shebang `#!/usr/bin/env -S uv run` 變成 `#!/usr/bin/env -S uv run\r`。env 找不到 `uv run\r` → exit 127，無 stderr。

#### 候選 3：cp950 Console 編碼斷層（編碼問題，與 W10-054 同源）

`chcp = 950`（Big5），與框架預期 UTF-8 不符。Python stdin/stdout 預設用 locale codepage，hook 解析 Claude Code 傳入的 UTF-8 JSON 時 `json.load` 拋出異常，但異常寫 stderr 時又因 cp950 二次失敗 → 空 stderr。

#### 候選 4：Shebang 機制本身在 cmd 不被解析

Windows cmd/PowerShell 原生不解析 shebang。Claude Code 必須有自己的 launcher 機制（呼叫 uv 或 python）；若 launcher 邏輯在 Windows 不完整，shebang 就被忽略。

### 影響範圍

- 所有 `.claude/hooks/` 下的 75 個 Hook 在 Windows 環境
- 框架使用者體驗：「No stderr output」充斥 UI，干擾正常工具呼叫
- macOS 不受影響（chmod +x、shebang 解析、UTF-8 預設）

### 相關 Error Pattern

- IMP-054：Hook 缺執行權限（macOS 場景，Windows 機制不同但症狀類似）
- IMP-048：Hook stderr 觸發 hook error（Windows 為「無 stderr」反向問題）
- IMP-055：Hook stdout 純文字破壞 JSON validation（與本問題並行存在）

---

## 重現實驗結果

### 實驗方法

請 Windows 使用者執行以下診斷命令並回報結果：

```cmd
:: 1. 直接用 uv 跑 hook，繞過 shebang 機制
uv run .claude\hooks\worktree-merge-reminder-hook.py < nul

:: 2. 驗證 CRLF 污染
certutil -encodehex .claude\hooks\worktree-merge-reminder-hook.py | findstr "0000"

:: 3. 確認 Claude Code hook 啟動方式
dir .claude\hook-logs\
type .claude\hook-logs\<最新檔案>

:: 4. 驗證 Python stub 假說
python --version
where python
```

### 實驗執行

**第一輪診斷收集（2026-04-15）**：

| 命令 | 輸出 | 解讀 |
|------|------|------|
| `chcp` | `使用中的字碼頁: 950` | 確認 cp950 console |
| `python -c "import sys; print(sys.stdout.encoding, sys.stdin.encoding)"` | **空輸出** | Python stub 假說證據 |
| `git config --get core.autocrlf` | `true` | 確認 CRLF 污染風險 |
| `where uv` | `C:\Users\user\.local\bin\uv.exe` | uv 安裝完整 |
| `where python` | `C:\Users\user\AppData\Local\Microsoft\WindowsApps\python.exe` | **MS Store stub** |

**第二輪驗證（待執行）**：等待用戶執行上述 4 個診斷命令以確認候選根因。

### 實驗發現

**已驗證事實**：
1. 環境為 Windows cmd（非 Git Bash），cp950 codepage
2. `python` 路徑指向 MS Store stub，執行 `python -c` 完全無輸出
3. `core.autocrlf=true`，`.py` 檔案理論上會被 CRLF 污染
4. uv 已正確安裝在 PATH

**仍待驗證**：
- Claude Code 在 Windows 實際用什麼機制啟動 hook（看 hook-logs 確認）
- hook 檔案在使用者電腦上的實際換行符（certutil 確認）
- uv 直接執行 hook 是否成功（驗證 uv 鏈路本身無問題）

**結論**：根因高度可能是「**MS Store Python stub + CRLF 污染**」雙重作用，或 Claude Code Windows hook launcher 機制與 macOS 不同但框架未提供對應規範。需第二輪診斷確認。

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: Explore
**Review Status**: pending