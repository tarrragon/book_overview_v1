---
id: 0.18.0-W15-004
title: 新增 ticket track deps 命令顯示衍生關係
type: IMP
status: completed
version: 0.18.0
wave: 15
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W15-001
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: W15-001 WRAP 決策派發實作
  rationale: 業界慣例血緣與衍生分離，獨立命令零 breaking
who:
  current: thyme-python-developer
  history: {}
what: 新增 ticket track deps 命令顯示衍生關係
when: 待定義
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/SKILL.md
why: W15-001 WRAP 決策方案 D：保留 tree/chain 純血緣語意，新增獨立 deps 命令顯示 spawned_tickets 與 source_ticket
  衍生關係，對齊 Jira/Linear/GitHub 業界慣例（血緣與衍生分離展示）
how:
  task_type: Implementation
  strategy: 新增 execute_deps 函式；subparser 註冊 deps 子命令；輸出格式：目標 ticket 基本資訊 + Spawned
    IMPs 列表（遞迴展開）+ Source ticket（若存在）；循環引用防護；單元測試涵蓋純衍生/混合/無關係三情境
acceptance:
- '[x] ticket track deps 命令已註冊'
- '[x] execute_deps 顯示 spawned_tickets 狀態清單'
- '[x] 顯示 source_ticket（若存在）'
- '[x] 循環引用防護'
- '[x] 單元測試 3+ 情境'
- '[x] SKILL.md 更新命令說明'
- '[x] 以 W14-024 驗證輸出符合預期'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-18T03:27:42'
completed_at: '2026-04-18T03:31:05'
created: '2026-04-18'
updated: '2026-04-18'
---

# Execution Log

## Task Summary

新增 ticket track deps 命令顯示衍生關係

---

## Problem Analysis

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

實作摘要：
1. 新增 execute_deps() 於 track_query.py（含輔助函式 _collect_spawned_tree 遞迴展開 + visited set 循環引用防護）
2. track.py 註冊 deps 命令：import execute_deps、command handlers 字典新增 "deps" key、_register_query_commands 新增 p_deps subparser
3. SKILL.md 更新：命令總覽加 deps、新增 deps 用法說明區塊（與 tree/chain 血緣語意對照）
4. 新測試檔 tests/test_deps_command.py（5 情境：遞迴/混合/無關係/循環/不存在），全通過
5. uv tool install --reinstall 完成
6. W14-024 實證驗證：4 spawned + 遞迴展開 + CYCLE DETECTED 防護正常顯示
備註：placeholder .claude/skills/ticket/ticket_system/tests/test_spawned_tickets_audit.py 未刪除（Bash rm 被權限拒絕；該檔僅 2 行註解且在 pytest testpaths 外，無功能影響）
## Context Bundle (for executor)

### 核心工作
新增 `ticket track deps` 子命令顯示衍生關係（spawned_tickets + source_ticket），與 tree/chain 血緣語意分離。

### 實作要點
1. **新增 execute_deps 函式**（`.claude/skills/ticket/ticket_system/commands/track_query.py` 或新檔）:
   - 遞迴展開 spawned_tickets（循環引用防護 via visited set）
   - 顯示目標 ticket 基本資訊
   - Spawned IMPs 列表（含狀態、標題）
   - Source ticket (若存在)
   - 無衍生關係時明示「無」

2. **subparser 註冊**（`.claude/skills/ticket/ticket_system/cli.py` 或 track 子命令 dispatcher）:
   - deps <ticket_id> 子命令
   - 沿用既有 --version 參數

3. **循環引用防護**: visited set 追蹤，偵測到已存在則標記並跳過

4. **單元測試 (3+ 情境)**:
   - 純衍生關係: A spawns B, B spawns C → 遞迴展開
   - 混合: 有 spawned + source
   - 無關係: 無 spawned 無 source → 輸出「無衍生」
   - （可選 4）: 循環引用 → 防護觸發

5. **SKILL.md 更新**: 在子命令總覽表加 deps 列 + 詳細用法區塊引用

6. **W14-024 驗證**: 以 W14-024 為測試案例確認輸出符合預期（預期 4 spawned + source=W14 core 或類似）

### 順帶清理
刪除 placeholder: `.claude/skills/ticket/ticket_system/tests/test_spawned_tickets_audit.py`（W15-003 留下的空檔，內容僅 2 行註解，Write 工具覆寫為空或 git rm）

### AC (7)
- [ ] deps 命令註冊
- [ ] execute_deps 顯示 spawned 清單
- [ ] 顯示 source_ticket
- [ ] 循環引用防護
- [ ] 單元測試 3+ 情境
- [ ] SKILL.md 更新
- [ ] W14-024 驗證

### 修改檔案（僅 .claude/，主 repo cwd）
- .claude/skills/ticket/ticket_system/commands/track_query.py (或新檔 track_deps.py)
- .claude/skills/ticket/ticket_system/cli.py (subparser)
- .claude/skills/ticket/SKILL.md
- .claude/skills/ticket/tests/test_deps_command.py (新增)

### 交付
1. append-log Solution 記實作摘要
2. check-acceptance --all
3. complete --yes
4. `uv tool install .claude/skills/ticket --reinstall`（修改 CLI 必需）
5. 驗證: `ticket track deps 0.18.0-W14-024` 輸出正確
6. git commit 獨立呼叫, 含 ticket ID
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending