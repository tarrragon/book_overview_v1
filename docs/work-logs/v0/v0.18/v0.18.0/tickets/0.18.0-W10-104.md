---
id: 0.18.0-W10-104
title: 分析 TestK_DocSync test_k2/k3 pre-existing 失敗根因
type: ANA
status: completed
version: 0.18.0
wave: 10
priority: P2
parent_id: null
children:
- 0.18.0-W10-104.1
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 建獨立 ANA 追蹤
  rationale: quality-baseline-rule-5 所有發現必須追蹤
who:
  current: rosemary-project-manager
  history: {}
what: 分析 TestK_DocSync test_k2/k3 pre-existing 失敗根因
when: W10-058.1.1.1/.2 完成後即發現，獨立追蹤
where:
  layer: 待定義
  files:
  - .claude/skills/wrap-decision/SKILL.md
  - .claude/skills/wrap-decision/references/tripwire-catalog.md
  - .claude/hooks/tests/test_wrap_decision_tripwire_hook.py
why: W10-058.1.1.1/.2 兩 IMP 完成時 thyme 代理人皆獨立 git stash 對照驗證確認此失敗為 pre-existing，與本任務無關。CATALOG
  缺 W10-009 ref + SKILL.md 缺機器可讀版本 / wrap-triggers.yaml 引用。屬框架文件同步檢查獨立議題。
how:
  task_type: Implementation
  strategy: 讀 test_k2/k3 斷言內容 / 對照 catalog 與 SKILL.md 現況 / 判定是文件失同步還是測試過時 / Solution
    給修正建議（更新文件 vs 更新測試 vs 移除測試）
acceptance:
- '[x] 重現 test_k2 失敗：CATALOG_PATH 是否仍應引用 0.18.0-W10-009'
- '[x] 重現 test_k3 失敗：SKILL.md 是否仍應含「機器可讀版本」+「wrap-triggers.yaml」'
- '[x] Solution 判定：更新文件 / 更新測試斷言 / 移除過時測試 之一'
- '[x] 提 IMP 修復建議（含 commit policy）'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-05-11T23:39:42'
completed_at: '2026-05-11T23:44:48'
created: '2026-05-11'
updated: '2026-05-11'
---

# Execution Log

## Task Summary

分析 TestK_DocSync test_k2/k3 pre-existing 失敗根因

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

W11-004（commit 6f3f1aba）與 W13-004 排程的 `4a945c87` 解耦改寫，**有意**移除 wrap-decision SKILL.md 對 `.claude/config/wrap-triggers.yaml` 與 W10-009 ticket 的反推註記，理由為「wrap-decision 為通用 WRAP 規則，獨立於任何專案框架」。但 `test_wrap_decision_tripwire_hook.py::TestK_DocSync::test_k2/k3` 為 W10-009 時期建立的 doc-sync 測試，未隨解耦同步更新，因而失敗。根因為「測試漂移」（test drift），非「SKILL 缺內容」。

### 影響範圍

- `.claude/hooks/tests/test_wrap_decision_tripwire_hook.py`（test_k2 line 1009、test_k3 line 1013-1015）
- 不影響任何產品程式碼或框架資產

### 相關 Error Pattern

W10-104 為 TestK_DocSync test_k2/k3 pre-existing 失敗根因分析（W10-058.1.1.1/.2 完成時即發現獨立追蹤）。未對應既有 PC pattern；本案啟示為「框架資產解耦重構需同步檢視對應 doc-sync 測試」，但因為跨專案通用化是個案決策（W13-004 排程外移獨立 claude repo 前奏），不升級為一般 error-pattern。

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## 重現實驗結果

**已驗證事實**：

1. 當前 `.claude/skills/wrap-decision/SKILL.md` 不含字串「機器可讀版本」與「wrap-triggers.yaml」（grep -c → 0）
2. 當前 `.claude/skills/wrap-decision/references/tripwire-catalog.md` 不含「0.18.0-W10-009」字串（grep -c → 0）
3. `.claude/config/wrap-triggers.yaml` 不存在；YAML 已移至 `.claude/skills/wrap-decision/references/project-integration/triggers-alignment.yaml`
4. test_k2/k3 失敗訊息：
   - `assert "0.18.0-W10-009" in <catalog text>` 失敗
   - `assert "機器可讀版本" in <skill text>` 失敗

**Git 歷史證據**：

| Commit | 日期 | 影響 |
|--------|------|------|
| `82fc0210` (W10-009) | 引入「機器可讀版本」+ `.claude/config/wrap-triggers.yaml` 引用 + W10-009 ticket 註記 |
| `4cb2727d` (W10-029) | SKILL 簡化三問擴充（保留 YAML 引用）|
| `6f3f1aba` (W11-004) | **大改寫 SKILL.md (-189 +120 行)**：解耦 wrap-decision 外部引用，移除「機器可讀版本」段與 W10-009 反推註記 |
| `4a945c87` (W13-004 排程) | refactor(skills): 解耦 wrap-decision 外部引用 + 移除違規 README；YAML 外移至 `references/project-integration/` |

**根因結論**：

W11-004 / W13-004 的 SKILL 解耦改寫**有意**移除 `wrap-triggers.yaml` 與 W10-009 引用，理由為「wrap-decision SKILL 為通用 WRAP 規則，獨立於任何專案框架」（SKILL.md 開頭明示）。專案層 YAML 對齊規格現存於 `references/project-integration/triggers-alignment.yaml`，不再從 SKILL.md 主文引用。

test_k2/k3 是 W10-009 時期建立的 doc-sync 測試，未隨 W11-004 / W13-004 解耦同步更新，因而失敗。屬「測試漂移」而非「SKILL 缺內容」。


### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---
## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

multi_view_status: skipped
reason: 根因明確（git log -S 已直接定位到 W11-004 commit 6f3f1aba 與 W13-004 解耦改寫），PM 與用戶共同判定多視角邊際收益低於 token 成本（W17-104 反模式防護案例參考）。處理範圍限「修過時測試」單一動作，無設計選項空間。

### 結論

W11-004 / W13-004 的 SKILL 解耦改寫**有意**移除 SKILL.md 對 `.claude/config/wrap-triggers.yaml` 與 W10-009 ticket 的引用。這是設計層的有意決策（wrap-decision SKILL 跨專案通用化），不應為了讓測試通過而還原。test_k2/k3 屬「測試未隨重構同步」（test drift），應修測試而非修 SKILL。

### 落地方案

| 動作 | 對象 | 處理 |
|------|------|------|
| 修測試 test_k2 | `test_wrap_decision_tripwire_hook.py:1009` | 移除（或改驗證 `references/project-integration/triggers-alignment.yaml` 存在） |
| 修測試 test_k3 | `test_wrap_decision_tripwire_hook.py:1013-1015` | 移除（或改檢查 SKILL.md 跨專案通用化的描述存在） |
| TestK_DocSync 整類意圖 | 整個 class | 評估是否仍需保留——doc sync 檢查已由 framework-rule-edit-skill-trigger-hook（W17-199）與 broken-link-check skill 接手 |
| W10-009 引用補檢查 | 不需 | W10-009 已 completed；catalog 引用為歷史 audit 標記，可保留缺失或補在註解區 |

### Spawn IMP

1 個 IMP ticket（W10-104.1，已建立並完成於 commit 886bedbd）：修 test_k2 / test_k3，含 npm run test:hooks 回歸驗證綠燈。

### 不在本 ANA 範圍

- 不重新引入 `wrap-triggers.yaml`（違反 W11-004 / W13-004 設計意圖）
- 不擴大檢視 TestK_DocSync class 其他測試（test_k1 已通過，留現狀）

### 自檢結果

- [x] Layer 1：根因有 git 證據（commit 6f3f1aba 顯示移除）
- [x] 落地方案有對應 spawn IMP（W10-104.1）並已完成
- [x] 結論未違反 W11-004 / W13-004 設計意圖
- multi_view_status=skipped，理由已記錄於 Solution 頂部

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-11T23:44:48
**Executing Agent**: rosemary-project-manager
**Review Status**: pending