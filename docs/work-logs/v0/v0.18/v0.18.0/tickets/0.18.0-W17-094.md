---
id: 0.18.0-W17-094
title: 修復 _is_placeholder regex 字邊界問題（TODO 誤判 TodoList 為佔位符）
type: IMP
status: in_progress
version: 0.18.0
wave: 17
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: 建立 IMP 修復同家族 false positive
  rationale: W17-007 完成期間踩到 bug
who:
  current: thyme
  history: {}
what: 修復 _is_placeholder regex 字邊界問題（TODO 誤判 TodoList 為佔位符）
when: complete 驗證階段
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/lib/ticket_validator.py
why: W17-007 complete 期間踩到此 bug；validate_execution_log_by_type 把含 TodoList 字串的 Problem
  Analysis 判為 unfilled，同 W17-072/W17-074 false positive 家族
how:
  task_type: Implementation
  strategy: regex 加 \b 字邊界（TODO/TBD/N/A 同步）+ 補測試 + 還原 W17-007 TodoList 字眼驗證
acceptance:
- '[x] regex 加字邊界（TODO/TBD/N/A 同步）'
- '[x] 新增測試覆蓋 TodoList 不誤判'
- '[x] W17-007 body TodoList 字眼可還原'
- '[x] pytest 全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-30T12:17:34'
completed_at: null
created: '2026-04-30'
updated: '2026-04-30'
---

# Execution Log

## Task Summary

修復 _is_placeholder regex 字邊界問題（TODO 誤判 TodoList 為佔位符）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### Context Bundle

**檔案**：`.claude/skills/ticket/ticket_system/lib/ticket_validator.py:369`

```python
if re.search(r"\(pending\)|TBD|TODO|N/A", content_no_separator, re.IGNORECASE):
    return True
```

**觸發案例**（W17-007 complete 失敗）：

Problem Analysis section 內容「列所有 CC tasks（非 TodoList）」。`re.search` IGNORECASE 把 `TodoList` 內的 `Todo` 命中 → 整個 2953 字元 section 判 placeholder → `validate_execution_log_by_type('ANA', body)` 回傳 `(False, ['Problem Analysis', 'Solution', 'Test Results'])`。

實證複現：

```bash
python3 -c "import re; print(re.findall(r'TODO', '列所有 CC tasks（非 TodoList）', re.IGNORECASE))"
# 輸出：['Todo']  ← 不應命中
```

### 修復方向

regex 加字邊界 `\b`：

```python
r"\(pending\)|\bTBD\b|\bTODO\b|\bN/A\b"
```

`\(pending\)` 已含括號邊界；`N/A` 的 `/` 為非字字元天然邊界，但保留 `\b` 一致性。

### 同家族案例

- W17-072：agent 自定義 H2 切斷 schema section（已修）
- W17-074：backtick 內 `## Test Results` 引用誤判章節（已修）
- W17-094（本 ticket）：substring TODO 匹配 placeholder false positive

### 測試補充

至少新增 3 條 pytest：

1. content 含 `TodoList` 不誤判為 placeholder
2. 真正 `# TODO: implement` 仍判 placeholder
3. `TBD`/`N/A` 加字邊界行為一致

### 驗證標準

1. `pytest .claude/skills/ticket/ticket_system/tests/test_ticket_validator.py` 全綠
2. 還原 `docs/work-logs/v0/v0.18/v0.18.0/tickets/0.18.0-W17-007.md:77` 為原始 `（非 TodoList）`，`ticket track validate 0.18.0-W17-007` 通過
3. 不破壞既有 placeholder 偵測

### 不在範圍

- 不改中文 `（待填寫：...）` placeholder 偵測
- 不改 `_find_next_schema_section_boundary`
- 不擴充 VALID_SECTIONS

### 來源

- 上游：W17-007 ANA complete 期間實證（commit 75ef9fd8）
- 同家族：W17-072 / W17-074 / PC-110
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 修復內容

`.claude/skills/ticket/ticket_system/lib/ticket_validator.py:369` regex 加 `\b` 字邊界：

```diff
- if re.search(r"\(pending\)|TBD|TODO|N/A", content_no_separator, re.IGNORECASE):
+ if re.search(r"\(pending\)|\bTBD\b|\bTODO\b|\bN/A\b", content_no_separator, re.IGNORECASE):
```

`\(pending\)` 已含括號邊界保留原狀；`TBD` / `TODO` / `N/A` 加 `\b` 避免被 `TodoList` / `ATBDay` 等 substring 命中。

### 還原 W17-007

`docs/work-logs/v0/v0.18/v0.18.0/tickets/0.18.0-W17-007.md:77` 還原為原始 `（非 TodoList）` 字眼，重跑 `ticket track validate 0.18.0-W17-007` 通過（4/4 合規）。

### 補測試

`tests/test_ticket_validator.py` 新增 3 條 W17-094 回歸測試：

- `test_todolist_substring_is_not_placeholder`：實證 W17-007 真實字串「列所有 CC tasks（非 TodoList）」不誤判
- `test_real_todo_marker_still_is_placeholder`：保護真正 `# TODO: implement` 仍判 placeholder
- `test_tbd_and_na_substring_is_not_placeholder`：TBD / N/A 加字邊界後 substring 不誤判但本字仍判 placeholder

### multi_view_status

multi_view_status: skipped
reason: 範圍極小（1 行 regex + 3 條測試 + 1 行還原），fix 模式為 W17-072 / W17-074 既有家族 false positive 修補；ROI 不足以派 linux/Explore 多視角審查
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### Pytest 結果

```bash
$ cd .claude/skills/ticket && uv run pytest ticket_system/tests/test_ticket_validator.py -x
============================== 50 passed in 0.11s ==============================
```

新增 3 條 W17-094 測試 + 既有 47 條全綠，無 regression。

### W17-007 真實案例驗證

```bash
$ ticket track validate 0.18.0-W17-007
[VALIDATE] 0.18.0-W17-007
  合規欄位 (4/4)：status, completed_at, acceptance, who
  狀態：全部合規
```

W17-007 line 77 還原為原始 `（非 TodoList）` 後仍通過驗證，bug 修復確認。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme
**Review Status**: pending