---
id: 0.18.0-W14-042
title: 實作 ticket CLI save_ticket 並發保護（fcntl.flock + race regression test）
type: IMP
status: in_progress
version: 0.18.0
wave: 14
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W14-005
dispatch_reason: ''
decision_tree_path:
  entry_point: ANA spawn IMP
  final_decision: 從 W14-005 ANA Solution 落地實作 file lock
  rationale: 'quality-baseline-rule-5 + W17-167: ANA Solution 必須轉 IMP ticket 才能落地'
who:
  current: thyme-python-developer
  history: {}
what: 實作 ticket CLI save_ticket 並發保護（fcntl.flock + race regression test）
when: W14-005 ANA complete 後，下次 wave 14 推進時 claim
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/lib/parser.py
  - .claude/skills/ticket/ticket_system/tests/test_save_ticket_race.py
  - .gitignore
why: W14-005 重現實驗確認 race condition 100% 觸發（66.5% lost rate / 三模式皆現），Solution 設計 fcntl.flock
  方案完整。本 ticket 落地 ~10 行 lock 注入 + race regression test。
how:
  task_type: Implementation
  strategy: 依 W14-005 Solution 3.4 骨架：parser.save_ticket() 加 _file_lock context manager；複用
    W14-005 重現實驗腳本轉為 pytest race regression test（N=20 fork mode，驗證 0 lost update）；.gitignore
    加 *.lock
acceptance:
- '[ ] fcntl.flock 加入 parser.save_ticket()（per-ticket-file lock）'
- '[ ] Race regression test 通過（N=20 並行 fork，0 lost update）'
- '[ ] 既有 hook test suite 全綠（npm run test:hooks）'
- '[ ] .gitignore 加 *.lock'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-18T20:48:17'
completed_at: null
created: '2026-05-18'
updated: '2026-05-18'
---

# Execution Log

## Task Summary

實作 ticket CLI save_ticket 並發保護（fcntl.flock + race regression test）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

### Phase 1 設計範圍指引（PM 派發 context）

**設計目標**：對 `parser.save_ticket()` 加入 fcntl.flock per-ticket-file advisory lock，三模式 race 全消除（lost update / empty read / file corruption）。實作骨架已在 W14-005 Solution §3.4。

### 1. API 設計

| 元素 | 規範 |
|------|------|
| `_file_lock(path: Path)` | 內部 helper context manager，私有不對外 |
| `save_ticket(ticket, ticket_path)` | signature 不變，caller 透明 |
| Lock file 命名 | `{ticket_path}.lock` 後綴 |

### 2. 錯誤處理

- `fcntl.flock` 失敗（資源不足 / 被另一 process 長期持有）：blocking 等待還是 timeout？建議 blocking（advisory lock 一般情境短暫，timeout 增加複雜度）
- `permission denied` / `disk full`：rethrow，由 caller 處理
- Crash safety：OS fd 回收自動釋放，無需主動清理

### 3. 邊界條件

- Lock file cleanup：保留不刪（advisory lock，crash 後自動釋放，下次 reuse 不衝突），加入 .gitignore
- 與 `_ticket_cache` 互動順序：取 lock → 修改 → cache invalidate → 釋 lock（lock 內完成 cache invalidate 確保下次 load 取最新）
- **不在範圍**：Windows 支援（fcntl POSIX-only，後續 ticket 用 filelock library fallback）

### 4. Acceptance 細化方向

將 IMP 4 項 acceptance 拆為可驗證的 Phase 2 RED 測試項目：
- per-ticket-file lock 隔離（不同 ticket 並發無等待）
- 同一 ticket 序列化（同 ticket 並發 → 0 lost update）
- Crash recovery（process kill 後 lock 自動釋放）
- Cache invalidation timing 正確

### 5. 參考實作骨架（W14-005 Solution §3.4）

```python
import fcntl
from contextlib import contextmanager

@contextmanager
def _file_lock(ticket_path: Path):
    lock_path = ticket_path.with_suffix(ticket_path.suffix + ".lock")
    lock_path.parent.mkdir(parents=True, exist_ok=True)
    fd = open(lock_path, "w")
    try:
        fcntl.flock(fd, fcntl.LOCK_EX)
        yield
    finally:
        fcntl.flock(fd, fcntl.LOCK_UN)
        fd.close()

def save_ticket(ticket, ticket_path):
    with _file_lock(ticket_path):
        # ... 既有邏輯不變 ...
```

Phase 1 任務：將上述 context 整合為正式 spec，並用 /spec skill 流程產出。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### Phase 1 Feature Spec（Full 模式，6 區段）

#### 1. Purpose

為 `parser.save_ticket()` 提供 per-ticket-file 並發寫入安全保護，消除 W14-005 重現實驗確認的 race condition（lost update / empty read / file corruption 三模式，66.5% lost rate）。對 caller 完全透明（signature 不變），影響範圍為 ticket CLI 所有寫入路徑（update_source_spawned_tickets / update_parent_children / claim / complete / append-log）。

#### 2. API Signatures

| 元素 | Signature | 可見性 |
|------|-----------|--------|
| `_file_lock` | `@contextmanager def _file_lock(ticket_path: Path) -> Iterator[None]` | 私有（模組內） |
| `save_ticket` | `def save_ticket(ticket, ticket_path: Path) -> None` | 公開，signature 不變 |

**Lock file 命名**：`{ticket_path}{suffix}.lock`（例：`0.18.0-W14-042.md.lock`）

**鎖型別**：`fcntl.LOCK_EX` blocking exclusive advisory lock，per-ticket-file。

#### 3. GWT Scenarios

**場景 1：同一 ticket 並發寫入序列化**
- Given: 兩個 process A、B 同時呼叫 `save_ticket(t, P)`，P 為同一 path
- When: A 先取得 lock，B 等待
- Then: A 完成寫入並釋放後 B 取得 lock；最終檔案內容為 B 寫入版本（last-writer-wins），無 lost update / empty read / corruption

**場景 2：不同 ticket 並發無阻塞**
- Given: process A 寫 ticket P1，process B 寫 ticket P2（P1 ≠ P2）
- When: 兩者同時呼叫 save_ticket
- Then: 兩 lock 互不干擾，並發完成（per-file lock 隔離）

**場景 3：Crash recovery**
- Given: process A 持有 P.lock，未釋放即被 kill -9
- When: process B 後續呼叫 save_ticket(t, P)
- Then: OS 自動回收 fd 釋放 flock，B 立即取得 lock 並完成寫入；lock file 殘留但不影響後續使用

**場景 4：Cache invalidation 時序**
- Given: `_ticket_cache` 已 cache ticket P 內容
- When: save_ticket(t, P) 執行
- Then: cache invalidate 動作在 lock 範圍內完成（取 lock → 寫入 → invalidate → 釋 lock），下次 load 必取到最新內容

**場景 5：權限或磁碟錯誤**
- Given: lock file 目錄無寫入權限，或磁碟已滿
- When: `open(lock_path, "w")` 拋 `PermissionError` / `OSError`
- Then: 例外直接 propagate 給 caller，不吞錯

#### 4. Error Handling

| 情境 | 處理策略 |
|------|---------|
| `fcntl.flock` blocking 等待 | 預設 blocking，advisory lock 一般情境短暫；不設 timeout（避免複雜度） |
| `PermissionError` (lock file 建立失敗) | rethrow 給 caller |
| `OSError` (disk full 等) | rethrow 給 caller |
| Process crash 持鎖中 | OS fd 回收自動釋放 flock，無需主動清理 |
| Lock file 殘留 | 保留（不刪），下次 reuse 不衝突；加入 .gitignore 避免誤 commit |

#### 5. Dependencies

| 項目 | 說明 |
|------|------|
| `fcntl` (stdlib) | POSIX-only，macOS / Linux 支援；Windows 不在本 ticket 範圍 |
| `contextlib.contextmanager` | stdlib |
| 既有 `parser.save_ticket` 邏輯 | 包覆於 `with _file_lock(...)` block 內，邏輯不變 |
| `_ticket_cache` | invalidate 動作須在 lock 範圍內 |
| 測試環境 | pytest + multiprocessing fork mode（macOS Python 3.13 預設 spawn，需顯式 set_start_method('fork')） |

#### 6. Acceptance（細化）

對應 IMP 4 項 acceptance，細化為 Phase 2 RED 可驗證測試項目：

- [ ] **A1**：`parser.save_ticket()` 加入 `_file_lock` 包覆，per-ticket-file lock；signature 不變
- [ ] **A2-1**：Race regression test — N=20 並行 fork 同一 ticket 寫入，0 lost update（最終內容為其中一個 writer 的完整 payload，無 corruption / empty）
- [ ] **A2-2**：不同 ticket 並發測試 — 兩個 process 並發寫不同 ticket，總時間 ≈ 單次寫入時間（驗證 per-file 隔離）
- [ ] **A2-3**：Crash recovery 測試 — 子 process 持鎖中被 SIGKILL，主 process 後續寫入立即成功（< 1s）
- [ ] **A2-4**：Cache invalidation 測試 — save 後 immediate load 必取得新內容
- [ ] **A3**：`npm run test:hooks` 既有 hook test suite 全綠（含 ticket_system 全部既有測試）
- [ ] **A4**：`.gitignore` 加 `*.lock`（位於 `.claude/skills/ticket/ticket_system/` 或 root level，視既有 .gitignore 結構決定）

#### 情境相關提示（Full 模式提醒，非未回答問題）

- 並發安全：本 spec 主要解決對象，已於場景 1-3 涵蓋
- 效能約束：advisory lock 開銷 << 1ms，N=20 並發測試應 < 2s 完成
- 安全性：lock file 為 process 內部協調機制，無敏感資料
- 依賴明確性：fcntl POSIX-only 已於 Dependencies 明示；Windows fallback（filelock library）後續 ticket 處理

#### Layer 2 語義推演自驗

| 維度 | 結果 |
|------|------|
| 邊界完整性 | crash mid-write、不同 ticket 並發、cache 時序皆涵蓋 |
| 錯誤路徑 | PermissionError / OSError rethrow 明示；flock blocking 行為說明 |
| 狀態完整性 | lock acquired / released / crash-released 三狀態完整；lock file 殘留為已知設計選擇 |

無未回答問題；達 Phase 2 RED 可進入狀態。
### Phase 1 修正版（saffron 重評）

#### WRAP 決策紀錄

**W（Widen）**：三方案 A/B/C + 維持原 spec 共 4 選項。

**R（Reality-Test）**：

| 方案 | 機制 | 優點 | 缺點 |
|------|------|------|------|
| 原 spec（lock in save_ticket） | save_ticket 內 flock | caller 透明、零修改 | **不保護 logical race**（load→modify→save 三步驟），RED 6/7 通過即為證據 |
| A: lock in update_* caller | update_parent_children / update_source_spawned_tickets 內 flock 包圍 load→modify→save | 精準保護真正 race 點；per-ticket-file lock 無 deadlock | 需改 2 個 caller（但僅 2 處，非 30+） |
| B: save_ticket lock + optimistic version | save_ticket 加 version tag，update_* 偵測衝突重試 | save_ticket 層仍透明 | 複雜度高（version schema 變更 + retry loop），overengineering |
| C: append-only log | spawned_tickets/children 改 append-only 日誌檔 | 徹底消除 read-modify-write | 破壞現有 YAML frontmatter schema，影響 30+ 讀取點，遷移成本極高 |

**A（Attain distance）**：
- 原 spec 的 RED 失效是決定性證據：save_ticket 的 single f.write 本身已 effectively atomic（parser.py:346-347），lock 加在此處保護的是不存在的問題。
- W14-005 §3.1「lock 在 save_ticket 不在 caller」的理由（30+ 呼叫點自動受保護）基於錯誤前提——race 發生在 caller 的 load→modify→save 序列，不在 save_ticket 的 write 動作。
- 方案 B/C 的複雜度與收益不成比例。

**P（Prepare to be wrong）**：
- 若 A 方案後發現其他 caller（lifecycle.py claim/complete 等）也有 load→modify→save race：擴展 lock 到該 caller，模式相同。
- 若 per-ticket-file lock 粒度不足（如跨 ticket 原子性需求）：建 follow-up ticket 評估 transaction-level lock。

**結論：採方案 A。**

---

#### W14-005 §3.1 決策重評

**原決策**：「lock 在 save_ticket 不在 caller」（parser.py:280）。理由：save_ticket 是所有寫入唯一入口，30+ 呼叫點自動受保護。

**重評結論**：決策錯誤。

- **錯誤前提**：假設 race 發生在 physical write 層級。實際 race 發生在 logical read-modify-write 層級（ticket_builder.py:744-763 的 load→append→save 序列）。
- **證據**：Phase 2 RED 7 項測試僅 1 項失敗（.gitignore），其餘 6 項在無 lock 下全部通過。save_ticket 的 single f.write call 對 ticket 大小 content 已 effectively atomic。
- **修正**：lock 應注入 update_parent_children / update_source_spawned_tickets，包圍完整 load→modify→save 三步驟。save_ticket 本身不需要 lock。

---

#### 修正版 Phase 1 Spec

##### 1. Purpose

為 `update_parent_children` 與 `update_source_spawned_tickets`（ticket_builder.py）提供 per-ticket-file 並發寫入安全保護，消除 logical race condition（lost update on spawned_tickets / children array）。

##### 2. API Signatures

| 元素 | Signature | 可見性 | 位置 |
|------|-----------|--------|------|
| `_file_lock` | `@contextmanager def _file_lock(ticket_path: Path) -> Iterator[None]` | 私有 | parser.py（共用 utility） |
| `update_parent_children` | 不變 | 公開 | ticket_builder.py |
| `update_source_spawned_tickets` | 不變 | 公開 | ticket_builder.py |
| `save_ticket` | 不變，**不加 lock** | 公開 | parser.py |

**Lock file 命名**：`{ticket_path}.lock`（同原 spec）。
**鎖型別**：`fcntl.LOCK_EX` blocking exclusive advisory lock，per-ticket-file。

##### 3. GWT Scenarios（修正版）

**場景 1：同一 parent/source ticket 並發 update 序列化**
- Given: Process A、B 同時呼叫 `update_parent_children(v, parent_id, child_A)` 和 `update_parent_children(v, parent_id, child_B)`
- When: A 先取得 parent ticket 的 file lock
- Then: A 完成 load→append→save→release 後 B 取得 lock；最終 parent.children 含 child_A 和 child_B，0 lost update

**場景 2：不同 parent ticket 並發無阻塞**
- Given: Process A 更新 parent P1，Process B 更新 parent P2
- When: 同時呼叫 update_parent_children
- Then: per-file lock 互不干擾，並發完成

**場景 3：Crash recovery**（同原 spec 場景 3，但 lock 在 update_* 層級）

**場景 4：update_source_spawned_tickets 同場景 1 鏡像**
- Given: 兩個 create 命令同時 spawn 新 ticket 到同一 source
- When: 並發呼叫 update_source_spawned_tickets
- Then: 最終 source.spawned_tickets 含兩個新 ID，0 lost update

**場景 5：權限/磁碟錯誤**（同原 spec 場景 5）

##### 4. 實作骨架

```python
# parser.py — 新增 _file_lock（共用 utility）
import fcntl
from contextlib import contextmanager

@contextmanager
def _file_lock(ticket_path: Path):
    lock_path = ticket_path.with_suffix(ticket_path.suffix + ".lock")
    lock_path.parent.mkdir(parents=True, exist_ok=True)
    fd = open(lock_path, "w")
    try:
        fcntl.flock(fd, fcntl.LOCK_EX)
        yield
    finally:
        fcntl.flock(fd, fcntl.LOCK_UN)
        fd.close()

# ticket_builder.py — update_parent_children 修改
def update_parent_children(version, parent_id, child_id):
    resolved_version = extract_version_from_ticket_id(parent_id)
    if resolved_version is None:
        return False
    parent_path = Path(get_ticket_path(resolved_version, parent_id))
    with _file_lock(parent_path):          # <-- 包圍 load→modify→save
        parent = load_ticket(resolved_version, parent_id)
        if not parent:
            return False
        children = _normalize_children(parent.get("children", []))
        if child_id not in children:
            children.append(child_id)
            parent["children"] = children
            save_ticket(parent, parent_path)
    return True

# update_source_spawned_tickets 同理
```

##### 5. Deadlock 防護分析

- 每個 update_* call 只持有**一把 lock**（target ticket 的 file lock）
- load→modify→save 完成後釋放，才可能取下一把
- `ticket create --parent P --source S` 會依序呼叫 update_parent_children 和 update_source_spawned_tickets，兩者 lock 不同 file，不會 deadlock
- 唯一 deadlock 場景：同一 call stack 內對同一 file 取兩次 lock（reentrant）→ fcntl.flock 對同一 fd 是 reentrant 的，但不同 fd 指向同一 file 會 block。分析 caller 確認不存在此場景。

##### 6. 修正版 Acceptance

- [ ] **A1**: `_file_lock` context manager 加入 parser.py
- [ ] **A2-1**: Race regression test — N=20 並行 fork 同一 parent ticket 執行 update_parent_children，0 lost update（最終 children 含全部 20 個 child_id）
- [ ] **A2-2**: 不同 parent ticket 並發 update 無阻塞
- [ ] **A2-3**: Crash recovery（child process 持鎖被 SIGKILL，main 後續 update 成功）
- [ ] **A2-4**: update_source_spawned_tickets 同 A2-1 鏡像測試
- [ ] **A3**: `npm run test:hooks` 全綠
- [ ] **A4**: `.gitignore` 加 `*.lock`

##### 7. Phase 2 RED 測試策略（修正方向）

原 Phase 2 測試直接呼叫 save_ticket 無法觸發 race。修正版測試應：
- Test 1/4: 並發呼叫 `update_parent_children` / `update_source_spawned_tickets`（非 save_ticket）
- Worker function: `_worker_update(parent_id, child_id)` 呼叫 update_parent_children
- Assertion: `load_ticket(parent).children` 長度 == N（0 lost update）
- 預期 RED: 無 lock 時 children 長度 < N（logical race lost update）

##### 8. 不在範圍

- save_ticket 本身不加 lock（已證明不需要）
- 其他 caller（lifecycle.py claim/complete 等）的 load→modify→save 序列：若實證有 race 再擴展，本 ticket 只處理 ticket_builder.py 的兩個 update 函式
- Windows 支援（同原 spec）

### Phase 3 GREEN 實作摘要

依「Phase 1 修正版 spec（saffron 重評）方案 A」落地：

**檔案變更**：

1. `.claude/skills/ticket/ticket_system/lib/parser.py`
   - 新增 `_file_lock(ticket_path)` context manager（fcntl.LOCK_EX blocking advisory lock）
   - `save_ticket` 本身**不**加 lock（單次 f.write 對小 content 已 effectively atomic）
2. `.claude/skills/ticket/ticket_system/lib/ticket_builder.py`
   - import `_file_lock` from parser
   - `update_parent_children`：用 `_file_lock(parent_path)` 包圍 load → modify → save 三步驟
   - `update_source_spawned_tickets`：用 `_file_lock(source_path)` 包圍 load → modify → save 三步驟
   - 兩函式 signature 不變（caller 透明）
3. `.gitignore`：在既有 `*.pid.lock` 後新增 generic `*.lock` pattern（含註解標示 W14-042 來源）

**設計要點**：

- Path resolution 移到 lock 取得前（lock 自身需要明確 path），但 lock 內仍以 `parent.get("_path", ...)` 作 actual_path 維持向後相容語義
- fcntl.flock 使用 `fd.fileno()` 確保 POSIX 行為一致
- finally 區塊以 try/finally 巢狀確保 close 必然執行（即便 LOCK_UN 異常）

**不在本 ticket 範圍**：

- `save_ticket` 加 lock（已證明不需要，RED v1 失效根因）
- `lifecycle.py` claim/complete 等其他 caller 的 load→modify→save 序列（若實證 race 由 follow-up ticket 處理；spec §8 已聲明）
- Windows 支援（fcntl POSIX-only，後續 ticket）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---


### [Phase 2 RED 設計]

**測試檔位置**：`.claude/skills/ticket/ticket_system/tests/test_save_ticket_race.py`

**設計原則**：
- 對應 Phase 1 spec 5 個 GWT 場景與 7 項 acceptance（A1/A2-1~A2-4/A3/A4）
- multiprocessing 使用 `set_start_method('fork', force=True)`（macOS Python 3.13 預設 spawn 需顯式切換）
- 每測試自帶 tmp dir fixture 隔離；測試完成後 lock file 殘留為設計選擇，由 fixture cleanup 處理
- A3（既有 hook test suite 全綠）由 `npm run test:hooks` 整體執行驗證，非單一測試
- A4（.gitignore）由文字比對驗證

---

#### Fixtures

```python
@pytest.fixture
def tmp_ticket_dir(tmp_path):
    """提供隔離的 ticket 目錄，避免汙染 docs/work-logs/"""
    d = tmp_path / "tickets"
    d.mkdir()
    return d

@pytest.fixture
def sample_ticket_factory(tmp_ticket_dir):
    """產生最小合法 ticket dict 與 path"""
    def _make(tid="0.0.0-TEST-001", payload_size=1):
        path = tmp_ticket_dir / f"{tid}.md"
        ticket = {
            "id": tid,
            "title": "race test fixture",
            "status": "pending",
            "body": "X" * payload_size,  # 可放大 payload 增加寫入耗時放大 race window
        }
        return ticket, path
    return _make

@pytest.fixture(scope="module", autouse=True)
def _force_fork_mode():
    import multiprocessing as mp
    try:
        mp.set_start_method('fork', force=True)
    except RuntimeError:
        pass
```

---

#### Test 1: `test_save_ticket_concurrent_same_path_no_lost_update`

**對應**：場景 1 / A2-1（N=20 並行同一 ticket，0 lost update）

- **Setup**：建立 sample ticket，path P 固定；payload 含 unique writer marker（process index）
- **Action**：`mp.Pool(20)` fork 並發呼叫 `save_ticket(ticket_variant_i, P)`，每 variant payload 含 `writer_id=i` 與大 body 放大 race window（建議 body 4KB+）
- **Assertion**：
  1. P 檔案存在且 YAML+body 完整可解析（無 corruption / empty）
  2. parse 後的 ticket payload 為 20 個 variant 之一的**完整**內容（last-writer-wins，但須是某個完整 payload，非交錯片段）
  3. `parser.load_ticket(P)` 不拋例外
- **Mock**：無，使用真實 fcntl
- **預期 RED 失敗訊號**：未實作 lock 時 lost rate ~60-70%（W14-005 重現實驗 66.5%）→ assertion 2 失敗（payload 不完整或 YAML parse 錯誤）

---

#### Test 2: `test_save_ticket_concurrent_different_paths_no_blocking`

**對應**：場景 2 / A2-2（per-file lock 隔離，不同 ticket 並發無阻塞）

- **Setup**：建立兩個不同 path P1、P2，準備兩 ticket
- **Action**：
  1. 測量 baseline：序列執行 `save_ticket(t1, P1); save_ticket(t2, P2)` 耗時 T_seq
  2. 測量並發：`mp.Pool(2)` 同時寫 P1 與 P2，耗時 T_par
  3. 加入人工延遲（save_ticket 內部 monkey-patch sleep 0.5s 或 payload 放大至 100KB）放大時序差異
- **Assertion**：`T_par < T_seq * 0.7`（並發應接近單次耗時，per-file lock 不應 cross-block）
- **Mock**：可用 `monkeypatch` 在 save_ticket flush 前 sleep 放大時序窗
- **預期 RED**：實作正確時測試應通過；若誤用 global lock 則 T_par ≈ T_seq 失敗

---

#### Test 3: `test_save_ticket_crash_releases_lock`

**對應**：場景 3 / A2-3（process SIGKILL 後 lock 自動釋放）

- **Setup**：建立 sample ticket P
- **Action**：
  1. fork child process，child 內部呼叫 `save_ticket` 但在 lock 取得後 `time.sleep(10)`（monkeypatch 注入 sleep）
  2. 主 process 等 0.3s 確保 child 已持鎖
  3. 對 child 發 `os.kill(pid, signal.SIGKILL)`
  4. 主 process 立即呼叫 `save_ticket(t, P)`，記錄耗時 T_recover
- **Assertion**：
  1. T_recover < 1.0s（OS 自動回收 fd 釋放 flock）
  2. 寫入成功，檔案內容為主 process 寫入版本
  3. `P.with_suffix('.md.lock')` 殘留但不影響後續（主 process 可再次寫入）
- **Mock**：monkeypatch `_file_lock` 或 save_ticket 注入 sleep hook（child 端）
- **預期 RED**：未實作 lock 或誤用非 fcntl（如 file 自旋鎖）會 hang > 5s

---

#### Test 4: `test_save_ticket_cache_invalidation_within_lock`

**對應**：場景 4 / A2-4（cache invalidate 時序）

- **Setup**：
  1. 建立 ticket P 並 `save_ticket` 初版
  2. `load_ticket(P)` 觸發 `_ticket_cache` 填入
- **Action**：
  1. 修改 ticket 內容（title 變更）後 `save_ticket(t_new, P)`
  2. 立即 `load_ticket(P)` 取回
- **Assertion**：
  1. 第二次 load 取得新 title（非舊 cache）
  2. （可選）以 monkeypatch 觀察 cache invalidate call 發生在 lock release 之前
- **Mock**：可 spy `_ticket_cache.invalidate` 或檢查內部 dict 狀態
- **預期 RED**：若 invalidate 在 lock 外執行或漏 invalidate，第二次 load 取到舊內容

---

#### Test 5: `test_save_ticket_propagates_permission_error`

**對應**：場景 5（PermissionError / OSError rethrow）

- **Setup**：建立 read-only 目錄作 ticket_path 父目錄（`tmp_path.chmod(0o555)`）
- **Action**：呼叫 `save_ticket(t, readonly_dir / "x.md")`
- **Assertion**：`pytest.raises((PermissionError, OSError))`，例外訊息含 lock_path 或 ticket_path
- **Mock**：無
- **預期 RED**：未實作 lock 但實作其他寫入時拋 PermissionError 也可能通過；本測試主要防止實作吞錯

---

#### Test 6: `test_gitignore_contains_lock_pattern`

**對應**：A4（.gitignore 加 `*.lock`）

- **Setup**：定位 .gitignore（root level 或 `.claude/skills/ticket/ticket_system/.gitignore`）
- **Action**：讀取檔案
- **Assertion**：含 `*.lock` 或 `**/*.lock` pattern（regex `r'^\*+\.lock\s*$'` multiline）
- **Mock**：無

---

#### Test 7: `test_save_ticket_signature_unchanged`

**對應**：A1（signature 不變透明）

- **Setup**：無
- **Action**：`inspect.signature(parser.save_ticket)`
- **Assertion**：參數仍為 `(ticket, ticket_path)`（或既有 signature 完全相同），無新增必填參數
- **Mock**：無
- **預期 RED**：若實作意外改變 signature 會 catch

---

#### 既有測試影響（對應 A3）

不在本檔內驗證，由 `npm run test:hooks` 整體跑通。設計提醒 thyme：
- `tests/test_parser_*.py` 既有測試若 mock `open` 需檢查是否同時 mock fcntl 路徑
- `_ticket_cache` 相關測試可能因 invalidate 時機調整需同步更新

---

#### 實作指引（pseudo code 給 thyme）

```python
# tests/test_save_ticket_race.py 骨架

import os
import signal
import time
import inspect
import multiprocessing as mp
from pathlib import Path

import pytest

from ticket_system.lib import parser


# === Fixtures (見上述) ===


def _worker_save(tid_path_payload):
    """multiprocessing target，需在 module top-level（fork 可用但 spawn 需要）"""
    tid, path_str, writer_id = tid_path_payload
    ticket = {"id": tid, "title": f"writer-{writer_id}", "body": "X" * 4096}
    parser.save_ticket(ticket, Path(path_str))


class TestSaveTicketRace:
    def test_concurrent_same_path_no_lost_update(self, sample_ticket_factory):
        ticket, path = sample_ticket_factory()
        args = [(ticket["id"], str(path), i) for i in range(20)]
        with mp.Pool(20) as pool:
            pool.map(_worker_save, args)

        # 不應 corruption
        loaded = parser.load_ticket(path)
        assert loaded is not None
        assert loaded["title"].startswith("writer-")
        # writer_id 為 0..19 其一
        wid = int(loaded["title"].split("-")[1])
        assert 0 <= wid <= 19

    def test_concurrent_different_paths_no_blocking(self, sample_ticket_factory, monkeypatch):
        # 注入人工延遲放大時序差
        ...

    def test_crash_releases_lock(self, sample_ticket_factory, monkeypatch):
        # fork child, child sleep in lock, kill -9, main retry
        ...

    def test_cache_invalidation_within_lock(self, sample_ticket_factory):
        ...

    def test_propagates_permission_error(self, tmp_path):
        ...

    def test_gitignore_contains_lock_pattern(self):
        ...

    def test_save_ticket_signature_unchanged(self):
        sig = inspect.signature(parser.save_ticket)
        params = list(sig.parameters.keys())
        assert params[:2] == ["ticket", "ticket_path"]
```

---

#### Test Matrix 總表

| Test | GWT 場景 | Acceptance | 並發數 | Mock 需求 |
|------|---------|-----------|--------|----------|
| 1 | 場景 1 | A2-1 | N=20 fork | 無 |
| 2 | 場景 2 | A2-2 | N=2 fork | monkeypatch sleep |
| 3 | 場景 3 | A2-3 | 1+1 fork+SIGKILL | monkeypatch sleep hook |
| 4 | 場景 4 | A2-4 | 序列 | spy cache.invalidate |
| 5 | 場景 5 | (error path) | 序列 | chmod readonly |
| 6 | — | A4 | 序列 | 無 |
| 7 | — | A1 | 序列 | 無 |
| (npm run test:hooks) | — | A3 | — | — |

---

**RED 階段預期失敗**：未實作 fcntl.flock 時，Test 1 失敗率 ~60-70%（lost update / corruption），Test 3 hang，Test 4 可能取到舊 cache。Test 6/7 為配置型測試，實作前後皆需通過或失敗依 .gitignore 與 signature 而定。

### [Phase 2 RED 結果]

**執行指令**：`uv run --project .claude/skills/ticket pytest .claude/skills/ticket/ticket_system/tests/test_save_ticket_race.py -v`

**結果摘要**：1 FAILED / 6 PASSED（commit 48a3a310）

| Test | 對應 AC | 狀態 | 說明 |
|------|---------|------|------|
| test_concurrent_same_path_no_lost_update | A2-1 | PASSED | OS-level write atomicity 對 ~50KB body 給出 last-writer-wins；body/title 一致性檢查通過 |
| test_concurrent_different_paths_no_blocking | A2-2 | PASSED | 含 0.5s sleep 放大時序 |
| test_crash_releases_lock | A2-3 | PASSED | 目前無 lock 即無 contention，main 直接寫入 < 1s |
| test_cache_invalidation_within_lock | A2-4 | PASSED | parser 既有 `_ticket_cache.pop` 已運作 |
| test_propagates_permission_error | scenario 5 | PASSED | 既有 OSError propagate 行為 |
| **test_gitignore_contains_lock_pattern** | **A4** | **FAILED** | **.gitignore 缺 generic `*.lock` pattern（僅有 `*.pid.lock` / `.claude/dispatch-active.lock` 等具體項）** |
| test_save_ticket_signature_unchanged | A1 | PASSED | signature 未變 |

**邊界發現（≤200 字）**：
1. Test 1 RED 訊號未如 sage 預測（66.5% lost rate）出現：macOS Python 3.12 + tmp_path 下 fwrite 對 ~50KB content 表現為原子。GREEN 後建議將 payload 提升至 1MB+ 或注入 IO 中斷點重驗 race coverage。
2. Test 3/4 預期失敗訊號需 lock 實作後才會體現對比（目前無 lock 即無 contention 路徑可測）；GREEN 階段須回頭強化 monkeypatch 注入點。
3. 真正 RED 僅 Test 6（.gitignore 缺 pattern）；其餘 6 項屬「不會被破壞」防護性測試。
4. mp.set_start_method('fork') 已強制；macOS Python 3.13 預設 spawn 環境必要。

**Phase 3a 建議**：lock 實作後須重跑全套並依 (1) 提升 Test 1 payload 至 1MB 重驗 RED→GREEN 轉換是否真實。

### [Phase 2 RED 結果 v2]

**執行指令**：`uv run --project .claude/skills/ticket pytest .claude/skills/ticket/ticket_system/tests/test_save_ticket_race.py -v`

**結果摘要**：3 FAILED / 3 PASSED（v2 改寫後真紅）

| Test Class / Test | 對應 AC / 場景 | 狀態 | 證據 |
|---|---|---|---|
| TestUpdateParentChildrenRace::test_concurrent_append_no_lost_update | A2-1 / 場景 1 | **FAILED** (RED) | N=20×3 rounds，lost_rate=**55.6%**（成功 writers 54，最終 children unique 24） |
| TestUpdateSourceSpawnedRace::test_concurrent_append_no_lost_update | A2-4 / 場景 4 | **FAILED** (RED) | N=20×3 rounds，lost_rate=**71.9%**（成功 writers 57，最終 spawned unique 16） |
| TestConfiguration::test_gitignore_contains_lock_pattern | A4 | **FAILED** (RED) | .gitignore 缺 generic `*.lock` pattern（同 v1 結果） |
| TestConfiguration::test_save_ticket_signature_unchanged | A1 | PASSED | save_ticket signature 不變（修正版 spec 不改 save_ticket） |
| TestConfiguration::test_update_parent_children_signature_unchanged | A1 | PASSED | update_parent_children signature 不變（lock 注入後應仍透明） |
| TestConfiguration::test_update_source_spawned_signature_unchanged | A1 | PASSED | update_source_spawned_tickets signature 不變 |

**Lost Update Detail（首輪樣本）**：
- update_parent_children round 0：18 成功 writers → final unique 9（lost 9）
- update_source_spawned_tickets round 0：19 成功 writers → final unique 4（lost 15）

**設計變更（v1 → v2）**：
- v1 測試直接呼叫 `save_ticket(t, P)`：未觸發 race（save_ticket 單次 f.write 對小 content 已 effectively atomic），僅 1/7 真紅
- v2 測試改呼叫 `update_parent_children` / `update_source_spawned_tickets`：包圍完整 load→modify→save 序列，logical race 顯化
- monkeypatch `ticket_builder.get_ticket_path` 與 `ticket_builder.load_ticket` 重導至 tmp_path（fork 模式繼承 patch state）
- 失敗的 worker（return False，因 mid-write 讀到部分檔案使 load_ticket 回 None）排除於 expected 計數，純看「成功宣稱寫入但最終 list 缺失」=真 lost update

**對齊 W14-005 重現實驗**：W14-005 yaml.safe_load + modify + yaml.safe_dump pattern 重現 66.5% lost rate；本 v2 在 update_* 真實函式上量測得 55.6%~71.9%，trend 一致，證實 race condition 確實存在於 ticket_builder.py:744-763 / 803-836 兩處 load→modify→save 序列。

**RED 結論**：Phase 1 修正版 spec（saffron 重評）方向正確。Phase 3a 應在 ticket_builder.py 兩個 update_* 函式注入 `_file_lock` 包圍 load→modify→save，並重跑此測試驗證 lost_rate → 0%。

### [Phase 3 GREEN 結果]

**執行指令**：`uv run --project .claude/skills/ticket pytest .claude/skills/ticket/ticket_system/tests/test_save_ticket_race.py -v`

**結果摘要**：6 PASSED / 0 FAILED（Phase 2 RED v2 的 3 個 RED 全數轉綠）

| Test | 對應 AC | RED v2 狀態 | GREEN 狀態 |
|------|---------|-------------|-----------|
| TestUpdateParentChildrenRace::test_concurrent_append_no_lost_update | A2-1 | FAILED (lost_rate 55.6%) | PASSED |
| TestUpdateSourceSpawnedRace::test_concurrent_append_no_lost_update | A2-4 | FAILED (lost_rate 71.9%) | PASSED |
| TestConfiguration::test_gitignore_contains_lock_pattern | A4 | FAILED | PASSED |
| TestConfiguration::test_save_ticket_signature_unchanged | A1 | PASSED | PASSED |
| TestConfiguration::test_update_parent_children_signature_unchanged | A1 | PASSED | PASSED |
| TestConfiguration::test_update_source_spawned_signature_unchanged | A1 | PASSED | PASSED |

**回歸驗證**：
- `npm run test:hooks`：2343 passed / 48 skipped / 0 failed
- ticket_system 全套 pytest：413 passed / 0 failed

**結論**：方案 A（lock 注入 update_* 而非 save_ticket）落地成功，logical race 完全消除，update_*/save_ticket 三者 signature 保持不變，caller 透明。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

### RED 失效訊號（thyme Phase 2 RED 落地後發現）

**現象**：7 項 RED 測試僅 1 項真正失敗（Test 6 .gitignore），其餘 6 項在無 lock 實作下亦 PASSED。

**根因分析**（PM 補充）：

Phase 1 spec 將 lock 注入 `save_ticket()` 是錯誤設計。觀察 `parser.save_ticket()` (lib/parser.py:280) 實際實作：

```python
content = f"---\n{frontmatter_yaml}---\n\n{body}"  # 整個 content 預先在記憶體拼接
with open(ticket_path, "w", encoding="utf-8") as f:
    f.write(content)  # 單次 f.write call
```

Python file IO 對小 content 的單次 `f.write` 已 effectively atomic（buffered + flush 一次）。所以 `save_ticket` 層級加 lock：
- 保護的是 physical write atomicity（但 Python 本來就提供）
- **無法**保護 logical race（read-modify-write 三步驟）

**真正的 race**（W14-005 重現實驗證實）在 `update_source_spawned_tickets` / `update_parent_children`：

```
Process A: load → spawned=[X]
Process B: load → spawned=[X]
Process A: append → spawned=[X, A]
Process A: save_ticket()  ← 即使這裡有 lock 也救不了
Process B: append → spawned=[X, B]
Process B: save_ticket()  ← 覆蓋 A，A 遺失
```

W14-005 我的重現實驗用 `yaml.safe_load + modify + yaml.safe_dump` 同樣模擬此 logical race（66.5% lost rate），但 thyme 透過 `parser.save_ticket()` 寫入時，因 single-process workflow 內無 load → modify → save 序列，race 沒被觸發。

### 修正方向

- **方案 A**：Lock 注入 `update_source_spawned_tickets` / `update_parent_children` 包圍 load → modify → save 三步驟（per-ticket-file lock，避免 deadlock）
- **方案 B**：在 save_ticket 內加 lock + 加版本標籤（optimistic lock），update_* 偵測版本衝突時重試
- **方案 C**：把 spawned_tickets / children 操作改用 append-only 日誌檔（避免 read-modify-write）

### 下一步建議

1. 不要進 Phase 3a：Phase 1 spec 設計有缺陷，先修
2. 建議派 incident-responder 確認本根因分析，或直接派 saffron 重新評估方案
3. 修 Phase 1 spec 後重做 Phase 2 RED（測試應真正模擬 update_* 並發場景）
4. W14-005 Solution §3.1 的「lock 在 save_ticket 不在 caller」決策需要重評
## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.18.0-W14-005 | chars: 233 -->

### Task Reference
- 0.18.0-W14-005 what: 設計 ticket CLI save_ticket file lock 並發安全機制（spec review 延後追蹤）

### Rationale Chain
- 0.18.0-W14-005 why: W12-006 spec review（Plan 視角）指出 update_source_spawned_tickets 與既有 update_parent_children 均缺並發保護；若多個 ticket CLI 程序同時寫入同一 source ticket，可能產生 spawned_tickets 陣列丟失更新。屬系統性風險需統合設計。