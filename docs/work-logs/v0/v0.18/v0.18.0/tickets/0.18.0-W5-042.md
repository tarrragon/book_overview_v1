---
id: 0.18.0-W5-042
title: 新增 Hook 偵測 PM 回覆含選項 pattern 提醒使用 AskUserQuestion
type: IMP
status: in_progress
version: 0.18.0
wave: 5
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: Hook 補強規則層 提高 AUQ 規則落地率
  rationale: W5-038 方案 4 Hook 是 Infrastructure 層改善的核心 但需在規則層完成後執行 避免 Hook 誤報無規則可基
who:
  current: basil-hook-architect
  history: {}
what: 新增 Hook 偵測 PM 回覆含選項 pattern 提醒使用 AskUserQuestion
when: W5-039 W5-040 W5-041 完成後
where:
  layer: Infrastructure
  files:
  - .claude/hooks/
why: W5-038 真根因 Hook 覆蓋缺失現有 askuserquestion-reminder-hook 只處理 Task 派發多 Ticket 場景無
  Hook 偵測 PM 對話回覆中的 A B C 選項 pattern
how:
  task_type: Implementation
  strategy: 新增 Stop Hook 或 UserPromptSubmit Hook 掃描 PM 上一次輸出文字 偵測高信心度選項 pattern 例如連續
    3 個以上 A B C 標記加冒號 含 請選擇 等關鍵字 觸發時下次 UserPromptSubmit 注入提醒 PM 應使用 AUQ
acceptance:
- '[ ] 新增 Hook 偵測 PM 輸出含高信心度選項 pattern'
- '[ ] Hook 設為 warning 非 block 不阻擋流程'
- '[ ] 偵測規則 連續 3 個以上 A B C 標記 + 冒號結尾 + 不在 code block 內'
- '[ ] false positive 豁免清單 引用既有文件選項 歷史回顧列表'
- '[ ] 偵測到時 UserPromptSubmit Hook 注入提醒'
- '[ ] 單元測試覆蓋 真陽性 3 場景 假陽性豁免 3 場景'
- '[ ] settings.json 註冊 Hook'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-14T00:21:37'
completed_at: null
created: '2026-04-13'
updated: '2026-04-13'
---

# Execution Log

## Task Summary

新增 Hook 偵測 PM 回覆含選項 pattern 提醒使用 AskUserQuestion

---

## Problem Analysis

### Context Bundle

#### 目標（PC-064 多層防護層 1 — 最後一層）

補齊 PC-064 的 Hook 覆蓋缺失。既有 askuserquestion-reminder-hook.py 只在 Task 工具派發且含 2+ Ticket ID 時觸發，不覆蓋 PM 對話中途決策點（列選項 / 問句結尾 / 等待用戶回應）。

#### PC-064 已部署層（W5-039/W5-040/W5-041 完成）

- 層 2 pm-rules：askuserquestion-rules.md 規則 1/3（已存在）
- 層 3 rules/core：pm-role.md 行為循環章節新增列選項必用 AUQ 強制條款（v3.6.0）
- 層 4 CLAUDE.md：1.1 節首要提醒 blockquote
- 層 5 Memory：feedback_pm_options_must_use_auq.md
- 待完成層 1 Hook：本 Ticket

#### 參考實作

.claude/hooks/askuserquestion-reminder-hook.py（127 行）— 同系列既有 Hook，可借鏡以下：
- UV single-file script header
- hook_utils 匯入（setup_hook_logging, is_subagent_environment, read_json_from_stdin）
- subagent 環境偵測跳過（is_subagent_environment）
- hookSpecificOutput JSON 輸出（permissionDecision + additionalContext）
- logger.info/debug 分級（PC-053 避免觸發 hook error）

#### 關鍵技術抉擇點（待 basil 規格設計）

1. Hook event 選擇：
   - UserPromptSubmit：讀 transcript 上一輪 assistant 輸出，注入 additionalContext 提醒
   - Stop：assistant 回合結束時自檢 output
   - PreToolUse：工具呼叫前（不適合，PM 輸出尚未結束）
   - 建議 UserPromptSubmit（既有 Hook 生態充分，下一輪用戶輸入前注入提醒效果好）

2. 偵測 pattern 設計：
   - 連續 3+ 個 A./B./C. 或 1./2./3. 標記行首
   - 末尾含「要選哪個」「先做 X 還是 Y」「需要做 Z 嗎」等問句
   - 非 code block 內（排除 ```fence）

3. False positive 豁免：
   - 文件引用列舉（非 PM 決策呈現）
   - 歷史決策回顧（過去時態）
   - 程式碼註解

4. Action：additionalContext 注入提醒 PM 應使用 AUQ

#### 驗收條件（7 項）

[ ] 新增 Hook 偵測 PM 輸出含高信心度選項 pattern
[ ] Hook 設為 warning 非 block 不阻擋流程
[ ] 偵測規則：連續 3+ A/B/C 標記 + 冒號結尾 + 不在 code block 內
[ ] false positive 豁免清單：引用既有文件選項 / 歷史回顧列表
[ ] 偵測到時 UserPromptSubmit Hook 注入提醒
[ ] 單元測試覆蓋：真陽性 3 場景 + 假陽性豁免 3 場景
[ ] settings.json 註冊 Hook

#### TDD 流程

tdd_phase: phase1, tdd_stage: [phase1-phase4]
本次派發限定 Phase 1 功能規格設計，完成後回報 PM 推進 Phase 2。

#### 派發約束

- 含 .claude/hooks/ 和 settings.json 寫入 → 主 repo cwd 派發（ARCH-015）
- basil-hook-architect 具 Hook 領域知識（event、UV single-file、stdout JSON）
- PC-053 教訓：Hook logger 僅用 info/debug，避免 stderr 觸發 hook error
- PC-064 附註：Hook 只須 warning 不阻擋，避免增加新的 permission 摩擦
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

Phase 1 完成：規格寫入 .claude/plans/hooks/auq-option-pattern-detector-spec.md（~280 行）。核心決策：(1) Hook event 選 UserPromptSubmit（唯一有 additionalContext 注入通道的候選），(2) 偵測雙路徑——連續 3+ A./B./C./1./2./3. 標記+問句結尾 或 二元問句+問號結尾，(3) 五層豁免 E1–E5 排除引用/歷史回顧/code block/規則寫作/subagent 環境，(4) Warning-only 不阻擋（PC-064 設計原則），(5) 寧可漏報不可誤報（避免訓練 PM 忽略）。7 項驗收條件 Phase 1 可核對：規格已定義偵測規則、warning 行為、3+冒號+code block 排除、豁免清單、UserPromptSubmit 注入機制、測試場景 3+3、settings.json 註冊方式。
Phase 1（功能規格設計）完成 by basil-hook-architect

**產出**：.claude/plans/hooks/auq-option-pattern-detector-spec.md（267 行）

**核心設計決策**：
- Hook event：UserPromptSubmit（唯一具 additionalContext 注入通道；Stop/PreToolUse 不適用）
- 偵測雙路徑：
  (1) 3+ 連續 A./B./C. 或 1./2./3. 標記 + 結尾 400 字內問句關鍵字
  (2) 二元問句 + 明顯問號結尾
- 豁免 E1–E5：引用文件 / 歷史回顧（過去時態）/ code block 與表格 / 規則寫作場景 / subagent 環境
- 設計原則：Warning-only 不阻擋（對齊 PC-064 避免 permission 摩擦）；寧可漏報不可誤報
- PC-053 合規：只用 logger.info/debug，禁止 stderr；IMP-055 hookSpecificOutput 必含 hookEventName

**7 項驗收條件對應**（規格內部）：
- AC1: §3 偵測規則
- AC2: 設計原則 Warning-only
- AC3: §3 規則（3+ 連續標記 + 結尾問句）
- AC4: §3.4 code block 排除
- AC5: §4 豁免 E1–E5
- AC6: §6.2 注入格式
- AC7: §7 settings.json 註冊
- AC8（測試）: §8 測試場景 3+3 + 4 邊界

**下一步建議（代理人）**：Phase 2（sage 測試設計）獨立派發以符合 TDD 紀律，或選擇性合併 Phase 2+3b（規格測試案例已具體化）。

**附帶 PM 教訓（PC-050 模式 D）**：
PM 因 hook 提示「dispatch-active 清空」過早判斷代理人失敗，實際 task-notification 後才確認成功。commit 613597b5 message 標為「失敗」為誤判，保留 commit 作為歷史紀錄，本次 commit 澄清。
Phase 2 完成 by sage-test-architect：測試設計 .claude/plans/hooks/auq-option-pattern-detector-test-design.md (~330 行)。共設計 16 個 RED 測試（3 TP 真陽性 + 3 FP 豁免 + 4 邊界 B1-B4 + 6 契約 C1-C6），涵蓋 AC1-AC7（AC7 settings.json 註冊建議 Phase 3b 加 smoke test 驗證）。附 TDD 紅燈推進順序 10 步供 thyme/basil 遵循；標註未決事項（transcript JSONL 格式、豁免關鍵字外部化）預設值供實作者對齊。下一步：派發 Phase 3b 實作（推薦 basil-hook-architect 執行，熟悉 hook_utils / UV single-file / IMP-055 契約）。
Phase 3b 實作完成 by basil-hook-architect。產出檔案：(1) .claude/hooks/auq-option-pattern-detector-hook.py（Hook 主體，~240 行，UV single-file）(2) .claude/hooks/lib/ask_user_question_reminders.py 新增 AUQOptionPatternMessages.REMINDER 常數，並在 lib/hook_messages.py re-export (3) .claude/hooks/tests/test_auq_option_pattern_detector.py（16 測試，使用 importlib 動態載入 hook 模組）(4) .claude/settings.json UserPromptSubmit 陣列新增 auq-option-pattern-detector-hook.py entry（排序在 dispatch-count-guard-hook 之後）。AC1-AC7 全數達成。PC-053 合規：logger 僅用 info/debug；IMP-055 合規：所有輸出 JSON 含 hookEventName。
## Test Results

<!-- To be filled by executing agent -->

---

pytest 結果：16 passed in 0.04s（100% 綠燈）。測試組成：3 TP（A./B./C./數字標記/二元問句）+ 3 FP 豁免（引用 E1/歷史 E2/code block）+ 4 邊界（B1 僅 2 標記不觸發 / B2 問句距離超 400 字 / B3 規則寫作豁免 / B4 subagent 環境）+ 6 契約（C1 hookEventName / C2 無 permissionDecision / C3 transcript 不存在放行 / C4 損壞 JSONL 放行 / C5 預期路徑無 stderr / C6 logger 只用 info/debug）。Smoke test：echo {} | 獨立執行 Hook → 輸出 {"hookSpecificOutput":{"hookEventName":"UserPromptSubmit"}}，無錯誤。
## Completion Info

**Completion Time**: (pending)
**Executing Agent**: basil-hook-architect
**Review Status**: pending