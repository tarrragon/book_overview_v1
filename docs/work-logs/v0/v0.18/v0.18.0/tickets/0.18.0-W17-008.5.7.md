---
id: 0.18.0-W17-008.5.7
title: 補強 W17-008.5 group 引導文件（PC-105 防護）
type: IMP
status: completed
version: 0.18.0
wave: 17
priority: P2
parent_id: 0.18.0-W17-008.5
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-python-developer
  history: {}
what: 補強 W17-008.5 group 引導文件（PC-105 防護）
when: W17-008.5.6 complete 後（5 commits 已落地，需文件背書）
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/SKILL.md
  - .claude/skills/ticket/references/track-command.md
  - .claude/hooks/README.md
why: W17-004 落差 5 + P2 錯誤訊息雙軌；argparse 英文 vs Hook 中文風格衝突造成用戶困惑；此為所有 CLI 錯誤的系統性修復
how:
  task_type: Implementation
  strategy: 盤點 W17-008.5 group 引入功能（ErrorEnvelope / format_error 雙路徑 / ArgparseFormatErrorParser
    / hook envelope 偵測跳過），在 SKILL.md 或 references/ 或 hooks/README.md 至少一處補使用範例與行為說明，使新功能可被
    grep 到
acceptance:
- '[x] 至少一處引導文件含 ErrorEnvelope 使用範例'
- '[x] 至少一處引導文件含 hook envelope 偵測跳過行為說明'
- '[x] grep ErrorEnvelope / format_error / ENVELOPE_VERSION_MARKER 在 .claude/ 文件可命中（非僅
  src/test code）'
- '[x] W17-008.5 父 acceptance 第 6 項 [PC-105 防護] 可勾選'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-03T12:42:48'
completed_at: '2026-05-03T12:42:48'
created: '2026-05-03'
updated: '2026-05-03'
---

# Execution Log

## Task Summary

補強 W17-008.5 group 引導文件（PC-105 防護）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

W17-008.5 group 5 commits 引入 4 個新功能（ErrorEnvelope dataclass / format_error 雙路徑 / ArgparseFormatErrorParser / hook envelope 偵測跳過），但引導文件未同步更新——讀者無法從 SKILL.md / references / hooks/README.md grep 到使用範例。違反 PC-105 防護「文件同步引導 + 強制可被 grep 命中」。

W17-008.5.2/.5.4 commit 雖更新 references/track-command.md，但內容是否含 ErrorEnvelope 使用範例需驗證；W17-008.5.5 hook 改動完全未更新文件。

### 影響範圍

- **可改檔案**（至少一處補充即可滿足 acceptance）：
  - `.claude/skills/ticket/SKILL.md`：CLI 錯誤格式說明段落
  - `.claude/skills/ticket/references/track-command.md`：append-log / create 命令的錯誤輸出範例
  - `.claude/hooks/README.md`：skill-cli-error-feedback-hook envelope 偵測跳過行為說明
- **驗證**：grep `ErrorEnvelope` / `format_error` / `__error_envelope_v1__` 於 `.claude/` 文件層應命中，非僅 src/test code 可見

### 相關 Error Pattern

PC-105：「subagent 完成功能但忘記同步引導文件」防護機制要求新功能可被 grep 到使用範例。本 ticket 是 PC-105 防護於 W17-008.5 group 的具體落實。

### 4 個新功能盤點（補文件需覆蓋）

| 功能 | 來源 commit | 文件補充重點 |
|------|------------|------------|
| ErrorEnvelope dataclass | 611c5b86 (W17-008.5.2) | 結構（component/action/errno/hint）+ 何時看到 + 範例輸出 |
| format_error 雙路徑 | 611c5b86 (W17-008.5.2) | legacy str vs envelope 的選用時機 |
| ArgparseFormatErrorParser | ead003cb (W17-008.5.4) | 業務錯誤 vs 語法錯誤分流 + 範例 |
| hook envelope 偵測跳過 | 10c01ad7 (W17-008.5.5) | hook 在 envelope 命中時跳過引導補充 |

### 不在本 ticket 範圍

- legacy str 淘汰時程（W17-117.2）
- ErrnoCodes StrEnum 集中（屬集中化優化，三視角共識 P1.5，待後續 ticket）
- messages.py 拆檔（屬集中化優化 P2，待後續 ticket）

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 補強範圍

兩處 `.claude/` 文件補強，覆蓋 W17-008.5 group 4 個新功能（PC-105 防護）：

| 檔案 | 補強內容 |
|------|---------|
| `.claude/skills/ticket/SKILL.md` | 「參考資料」表新增 `references/track-command.md` 列；摘要列出 `format_error()` 雙路徑、`ErrorEnvelope`、`ArgparseFormatErrorParser`、`__error_envelope_v1__` 標記，使 SKILL.md 層即可 grep 命中 |
| `.claude/skills/ticket/references/track-command.md` | 「版本標記 `__error_envelope_v1__`」段落新增子節「Hook 端跳過機制」，補上 `.claude/hooks/skill-cli-error-feedback-hook.py` 的 `ENVELOPE_VERSION_MARKER` / `is_envelope_output()` 程式碼節錄與「含標記跳過 vs 不含標記補充」行為對照表 |

既有 `references/track-command.md`（W17-008.5.2 / .5.4 已 touch）已涵蓋 ErrorEnvelope dataclass 結構、format_error 雙路徑（legacy str / envelope）、ArgparseFormatErrorParser 業務 vs 語法錯誤分流，本次補強聚焦 W17-008.5.5 hook 端缺口（原文件未提及 hook 跳過機制的具體位置與程式碼）。

### grep 驗證命中

| 關鍵字 | 命中檔案 |
|-------|---------|
| `ErrorEnvelope` | SKILL.md, track-command.md |
| `format_error` | SKILL.md, architecture.md, track-command.md |
| `__error_envelope_v1__` | SKILL.md, track-command.md |
| `is_envelope_output` / `跳過重複補充` / `跳過補充` | track-command.md |

acceptance #1（ErrorEnvelope 使用範例）+ #2（hook envelope 偵測跳過行為說明）+ #3（grep 命中 .claude/ 文件層）皆滿足。

### 不在本 ticket 範圍

- `.claude/hooks/README.md` 補強：該檔案結構過時（Hook 清單未涵蓋 skill-cli-error-feedback-hook），需獨立 DOC ticket 重整。本 ticket acceptance 採「至少一處」門檻，已透過 SKILL.md + track-command.md 滿足。
## Context Bundle

### 派發位置

**主 repo cwd**（非 worktree）— prompt 含 `.claude/skills/ticket/` + `.claude/hooks/` Edit/Write，依 ARCH-015 必須在主 repo 派發。

### 環境前置

- 純文件變更，不需 reinstall
- 無需執行測試（純文件）；但若新增範例含執行命令，可選跑 `(cd .claude/skills/ticket && uv run pytest tests/)` 確認 baseline 1588 不變

### 既有 references/track-command.md 狀態

W17-008.5.2 + W17-008.5.4 commits 已 touch 此檔案，需先 grep 確認既有內容是否已涵蓋 ErrorEnvelope；若已有則此 ticket 重點轉向 hook README 補充（W17-008.5.5 完全未更新文件）。

### 範例素材建議

- **ErrorEnvelope 範例輸出**（從 messages.py 渲染格式）：

  ```
  [Error] __error_envelope_v1__
    component: create
    action: enforce_checklist
    errno: CHECKLIST_VALIDATION_FAILED
    hint: 以下欄位為必填（缺失將阻擋建立）: when, how_strategy
  ```

- **hook envelope 偵測跳過行為**（skill-cli-error-feedback-hook.py:288-292）：

  ```python
  # 跳過 ErrorEnvelope 已輸出完整結構化訊息的情況（W17-008.5.5）
  if is_envelope_output(stderr, stdout):
      return EXIT_SUCCESS
  ```

### Commit Policy

- 一個 commit 含文件補充（至少 1 處 .claude/ 文件）
- commit message format: `docs(0.18.0-W17-008.5.7): 補強 W17-008.5 group 引導文件 PC-105 防護（ErrorEnvelope / hook envelope 偵測說明）`
- `git add` 精確路徑（避 PC-092），按實際改動列出

### 完成後行為（agent 自律收尾）

1. body 填寫：append-log 至 Solution（補了哪些檔案 + grep 驗證命中數量）
2. commit 完成
3. `ticket track check-acceptance --all 0.18.0-W17-008.5.7`
4. `ticket track complete 0.18.0-W17-008.5.7`

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

純文件變更，無測試執行。

驗證方式：grep 於 `.claude/skills/ticket/` 與 `.claude/hooks/` 範圍下 `--include="*.md"` 命中所有 acceptance 關鍵字（ErrorEnvelope / format_error / __error_envelope_v1__ / hook 跳過行為），結果記錄於 Solution 段。

baseline 1588 ticket_system 測試未動，不需重跑。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-03T12:42:48
**Executing Agent**: thyme-python-developer
**Review Status**: pending