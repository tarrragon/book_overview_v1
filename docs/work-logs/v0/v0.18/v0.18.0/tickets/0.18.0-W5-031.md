---
id: 0.18.0-W5-031
title: 調查與規劃 測試範本 0.31.0 版本字面值污染真實 repo 根治方案
type: ANA
status: pending
version: 0.18.0
wave: 5
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: W5-021 執行收尾時發現 docs/work-logs/v0/v0.31/ 殘留 tickets；用戶確認為測試範本副作用重複復現，需根治
  rationale: quality-baseline-rule-5
who:
  current: saffron-system-analyst
  history: {}
what: 調查與規劃 測試範本 0.31.0 版本字面值污染真實 repo 根治方案
when: 本 session 清 context 後立即執行 WRAP 決策（2026-04-13 之後）
where: .claude/skills/ticket/tests/ (35 files using 0.31.0 string literals) + .claude/skills/ticket/ticket_system/lib/parser.py
  + paths.py (doctest examples) + docs/work-logs/v0/v0.31/ (pollution artifact to
  clean)
why: W5-021 commit + pull 後發現 docs/work-logs/v0/v0.31/v0.31.0/tickets/0.31.0-W4-001.md
  與 0.31.0-W4-001.1.md 兩個 untracked 檔案。用戶確認：『這個我們之前處理過，這是因為有一個程式範本是用 v0.31，結果每次測試都會建立，所以我們應該要找出來並且研究如何調整範例，避免每次測試都建立。』此為重複復現的
  flaky 污染問題，需 WRAP 決策根治方案。本 session context 已高度飽和，用戶指示清除 context 後再做 WRAP 決策
how: 兩階段：(1) WRAP 決策根治方案——候選：方案 A 哨兵版本號 (99.99.99-sentinel，改 35 檔)、方案 B .gitignore
  遮罩 (docs/work-logs/v0/v0.31/)、方案 C Session Hook 警告偵測非 in-scope 版本目錄、方案 D pytest
  session autouse fixture 結束時 assert 專案根目錄無污染。(2) WRAP 決定後派生 IMP Ticket 執行實作。注意：本
  session context 飽和不宜直接決策，用戶指示 /clear 後再 resume 本 Ticket 進行 WRAP
acceptance:
- '[ ] 完整盤點所有使用 0.31.0 / 0.31.0-W4-001 硬編碼版本字面值的位置（含 test、doctest、範例字串）'
- '[ ] 定位實際寫入真實 repo 的操作鏈（哪個測試/doctest/hook 會觸發 save_ticket 到非 tmp_path 路徑）'
- '[ ] WRAP 決策選出最佳根治方案（本 ANA 已列出 4 候選：哨兵版本號/gitignore 遮罩/Session Hook 警告/pytest session
  fixture 檢查）'
- '[ ] 產出實作 Ticket（IMP）執行根治方案'
- '[ ] 清理殘留檔案（docs/work-logs/v0/v0.31/）'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-04-13'
updated: '2026-04-13'
---

# Execution Log

## Task Summary

調查與規劃 測試範本 0.31.0 版本字面值污染真實 repo 根治方案

---

## Problem Analysis

### 問題現象

git status 顯示兩個 untracked 檔案長期存在於 repo：

```
docs/work-logs/v0/v0.31/v0.31.0/tickets/0.31.0-W4-001.md
docs/work-logs/v0/v0.31/v0.31.0/tickets/0.31.0-W4-001.1.md
```

檔案 mtime: 2026-04-12 22:17:51。內容稀疏（非正式 ticket create 產出）：

```yaml
# 0.31.0-W4-001.md
---
id: 0.31.0-W4-001
phase: Phase 1
current_phase: Phase 4
assignee: parsley-flutter-developer
---

# 0.31.0-W4-001.1.md
---
id: 0.31.0-W4-001.1
chain:
  parent: 0.31.0-W4-001
  root: 0.31.0-W4-001
parent_id: 0.31.0-W4-001
---
```

### 關鍵反常點

- 本專案當前版本 **0.18.0**（Chrome Extension, JavaScript）
- remote 版本目錄最高 **v0.20**，**不存在 v0.31**
- assignee `parsley-flutter-developer` 是 Flutter 代理人，本專案並非 Flutter
- v0.31 純屬測試範例版本，不是實際開發版本

### 用戶確認

> 這個我們之前處理過，這是因為有一個程式範本是用 v0.31，結果每次測試都會建立，所以我們應該要找出來並且研究如何調整範例，避免每次測試都建立

此為重複復現的 flaky 污染問題，**未完全根治**。

### 已調查的位置（本 session 部分搜尋結果）

```
# 含 0.31.0-W4-001 字面值的檔案（35 個）
.claude/skills/ticket/tests/ 下 25 個測試檔案
.claude/skills/ticket/ticket_system/ 下 10 個 source 檔案
```

#### 安全位置（已確認不會污染）

| 位置 | 範本類型 | 保護機制 |
|------|---------|---------|
| tests/conftest.py L27 | `work-logs/v0.31.0/tickets` fixture 路徑 | 在 tempfile.TemporaryDirectory 內 |
| tests/test_ticket_builder.py L107-220+ | `get_tickets_dir` | monkeypatch.setattr mock |
| tests/test_ticket_loader.py L490-530 | 測試 save/load 循環 | 設 CLAUDE_PROJECT_DIR env 指向 temp_project_dir，finally 清理 |
| tests/test_bulk_create.py | get_tickets_dir | patch context manager |

#### 疑似污染源（未定案）

| 位置 | 行號 | 疑慮 |
|------|------|------|
| lib/parser.py | 195 | doctest 範例呼叫 `load_ticket("0.31.0", "0.31.0-W4-001")`——load_ticket 本身不寫檔（return None when missing），理論安全 |
| lib/paths.py | 82, 126 | doctest 範例呼叫 get_tickets_dir/get_ticket_path——只計算路徑不寫檔 |
| 未發現的測試 | - | 可能有測試漏設 CLAUDE_PROJECT_DIR 或漏 mock get_tickets_dir 時呼叫 save_ticket |

### 已排除的嫌疑犯

- Hook 系統：.claude/hooks 內僅有範例字串（comment/docstring），無寫檔行為
- test_ticket_loader.py L490 的 `test_load_and_save_cycle`：正確設置 env + finally 清理

### 根因假設（待 WRAP 驗證）

**假設 1**：某個 integration test 未在 stdlib 或 pytest temp 內執行且未 mock get_ticket_path，真實 save_ticket 時寫入真實 repo。本 session grep 未找到該測試具體 ID。

**假設 2**：doctest 執行時（pytest --doctest-modules 或 doctest.testmod），某 doctest 呼叫 save_ticket 並傳入非 tmp 路徑。需列出所有 doctest 實際程式碼確認。

**假設 3**：並非測試，而是開發過程中某次手誤或 hook 觸發，CLI 在 cwd 為本 repo 時執行了 `ticket create --version 0.31.0 --parent 0.31.0-W4-001`。

### 相關歷史

- `.claude/error-patterns/test/TEST-004`：重構 wrapper 造成 mock path 失效的教訓（用 0.31.0-W4-001 當範例 ID）——證明 0.31.0 是**測試標準範例版本**
- 本 session 執行 W5-029 和 W5-030 時用 `ticket create` 成功觸發過 ID 覆寫 bug（已恢復），但版本都是 0.18.0，未產生 v0.31 殘留
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

### 候選根治方案（4 個，等 WRAP 決策）

| # | 方案 | 修復成本 | 根治程度 | 副作用風險 |
|---|------|---------|---------|-----------|
| A | **哨兵版本號重構**：測試範本 `0.31.0` 改為 `99.99.99-sentinel`，並在 .gitignore 排除 `docs/work-logs/v0/v99.99.99-sentinel/` | 中（35 檔連動改） | 高（污染可立即識別且不入 git） | 改動面大，測試套件需重跑全量驗證 |
| B | **.gitignore 遮罩**：加入 `docs/work-logs/v0/v0.31/` 到 .gitignore | 極低（1 行） | 中（遮罩而非防範，實際污染仍會 mkdir） | 遮罩真實問題，長期累積 |
| C | **Session Stop Hook 警告**：偵測 repo 根目錄有非 in-scope 版本目錄時 stderr 警告 | 低（寫新 Hook） | 中（提示但不自動清理） | 可能誤報 |
| D | **pytest session autouse fixture**：pytest 測試 session 結束時 assert 專案根目錄無污染（snapshot before/after） | 低（加 conftest fixture） | 高（CI level 硬性檢查） | 需選擇 snapshot 範圍避免誤報 |
| E | **組合方案 A+D**：哨兵版本 + pytest guard fixture | 中+低 | 極高 | 短期改動大 |

### WRAP 決策需考量

- **W**iden options: 是否有未列出的方案？例如重構測試架構、改用 monorepo-style version sandbox、改用 conftest.py level autouse mock
- **R**eality test: 需驗證假設——實際跑 pytest 看是否能重現污染？跑 doctest 是否觸發？
- **A**ttain distance: 不要因為方案 B 修復成本最低就選它；須評估長期技術債
- **P**repare to be wrong: 即使選定方案，設定 trip wire——若下次又發現污染則升級方案

### 殘留檔案清理（建議於 WRAP 決策前先做）

```bash
rm -rf /Users/tarragon/Projects/book_overview_v1/docs/work-logs/v0/v0.31/
```

內容稀疏、非正式 Ticket，無保留價值。若確定其歸屬為其他專案，另外備份到外部目錄。

### Resume 指示（新 session）

1. `ticket resume 0.18.0-W5-031`
2. 讀 Problem Analysis + Solution 全文
3. 呼叫 WRAP 決策框架（wrap-decision skill）
4. 產出選定方案
5. 派生 IMP Ticket 執行根治
6. 清理殘留檔案（若尚未清理）

### 進入新 session 前的必備上下文

- 此 Ticket：0.18.0-W5-031（ANA，pending）
- 相關 commit 歷史（本 session 產出）：148b37f1、25a8e6ba、9de9f2d6、2738293d、21e7adb3、05026248、834646c2
- relatedTo：W5-029（thyme permissionMode 調查，已 completed）、W5-030（9 個範圍外代理人 permissionMode 評估，pending）
- 應保持的約束：WRAP 決策前先清理殘留，避免 git status 持續提醒
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending