---
id: 0.18.0-W15-028.2
title: 修復 ticket CLI exit code 違規點
type: IMP
status: completed
version: 0.18.0
wave: 15
priority: P2
parent_id: 0.18.0-W15-028
children: []
blockedBy:
- 0.18.0-W15-028.1
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:PC 防護
  final_decision: 依 W15-028.1 規範修復 4 檔 7 處違規並補測試
  rationale: 規則層先固化（W15-028.1）後 hook/實作層對齊；參考 feedback_rule_before_hook_refactor_order
who:
  current: thyme-python-developer
  history: {}
what: 修復 track_audit/track_acceptance/track_batch/track_dashboard 業務拒絕與內部錯誤 exit code
  混用，並補 exit code 三值單元測試
when: W15-028.1 規範完成後執行
where:
  layer: ticket-skill
  files:
  - .claude/skills/ticket/ticket_system/commands/track_audit.py
  - .claude/skills/ticket/ticket_system/commands/track_acceptance.py
  - .claude/skills/ticket/ticket_system/commands/track_batch.py
  - .claude/skills/ticket/ticket_system/commands/track_dashboard.py
why: PC-096 防護需在規範落地後對齊既有 CLI；4 檔案 7 處違規已稽核確認（見父 ticket Problem Analysis）
how:
  task_type: Implementation
  strategy: 依 W15-028.1 規範重構：(1) track_audit.py:138 業務拒絕改 return 2 (2) track_acceptance.py:291/296/301/306/335
    用戶輸入錯誤改 return 2，IO 異常維持 return 1 (3) track_batch.py 批量部分失敗改 return 2 (4) track_dashboard.py
    查詢無資料改 return 2 (5) 每檔補單元測試覆蓋 return 2 與 return 1 路徑
acceptance:
- '[x] track_audit.py 業務拒絕 return 2，至少 1 個測試斷言 returncode == 2'
- '[x] track_acceptance.py 用戶輸入錯誤 return 2，至少 2 個測試斷言'
- '[x] track_batch.py 部分失敗 return 2，至少 1 個測試斷言'
- '[x] track_dashboard.py 查詢無資料 return 2，至少 1 個測試斷言'
- '[x] ticket skill 既有測試 100% 通過'
- '[x] 新增測試數量 ≥ 5 個'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-17T23:30:09'
completed_at: '2026-05-17T23:50:10'
created: '2026-05-17'
updated: '2026-05-17'
---

# Execution Log

## Task Summary

修復 track_audit/track_acceptance/track_batch/track_dashboard 業務拒絕與內部錯誤 exit code 混用，並補 exit code 三值單元測試

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

依 `.claude/references/cli-exit-code-rules.md`（W15-028.1 落地 SSOT）修復 4 檔 exit code 三值合規：

### 修復摘要

| 檔案 | 行號 | 修復前 | 修復後 | 路徑類型 |
|------|------|--------|--------|---------|
| `commands/track_audit.py` | 138 | `return 1` | `return 2` | 業務拒絕（audit not passed） |
| `commands/track_audit.py` | 142, 145 | `return 1` | `return 1`（維持） | 內部錯誤（ValueError / Exception） |
| `commands/track_acceptance.py` | 293 | `return 1` | `return 2` | 用戶輸入錯誤（互斥旗標） |
| `commands/track_acceptance.py` | 297 | `return 1` | `return 2` | 用戶輸入錯誤（缺旗標） |
| `commands/track_acceptance.py` | 302 | `return 1` | `return 2` | 用戶輸入錯誤（找不到 ticket） |
| `commands/track_acceptance.py` | 308 | `return 1` | `return 2` | 業務拒絕（acceptance 為空） |
| `commands/track_acceptance.py` | 337 | `return 1` | `return 2` | 用戶輸入錯誤（index 解析失敗） |
| `commands/track_batch.py` | 307 | `return 1` | `return 2` | 業務拒絕（無有效 ticket ID） |
| `commands/track_batch.py` | 355 | `return 0/1` | `return 0/2` | 批量全失敗 → 業務拒絕 |
| `commands/track_batch.py` | 383 | `return 1` | `return 2` | 用戶輸入錯誤（resolve 失敗） |
| `commands/track_dashboard.py` | 255 | `return 1` | `return 2` | 業務拒絕（查無 active version） |
| `commands/track_dashboard.py` | 261 | `return 1` | `return 1`（維持） | 內部錯誤（list_tickets 例外） |

### 設計決策

1. **track_audit.py:122 行 (322)** 為 `return 0`（dry-run summary），非違規，未修改（Problem Analysis 列入掃描範圍但本身合規）。
2. **track_dashboard.py:255** 雖屬「資料源狀態」邊界情境，依 cli-exit-code-rules 規則 2 解為「查詢無資料 → 業務拒絕」，與 track_handoff_ready 的 IO_ERRORS 保守 NO-GO 同模式。
3. 各檔 docstring 之 Returns 區塊均更新為 0/1/2 三值說明，並引用 `.claude/references/cli-exit-code-rules.md` 作為 SSOT。
4. 既有測試 `test_track_dashboard.test_D6_no_active_version` 之 `rc == 1` 斷言同步更新為 `rc == 2`（屬同 commit 行為對齊，非破壞既有契約）。

### ticket-skill-sync 影響檢查

`SKILL.md` 與 `pm-rules/` 內既有 exit code 引用僅涉及 `dispatch-readiness` 與 `acceptance-gate-hook`，與本 ticket 修復的四個 CLI 無交集，無需同步更新。

### 不在本 ticket 範圍

- `commands/create.py:736, 905` 之 `sys.exit(1)`（父 ticket Problem Analysis 標註為待個別審視）
- 各 `__main__` guard 之 `sys.exit(1)`（純語法錯誤類，cli-exit-code-rules 例外條款）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 測試指令

```
uv run --project .claude/skills/ticket pytest .claude/skills/ticket/ticket_system/tests/
```

### 結果

- **既有測試 + 新增測試**：407 passed in 762.93s
- **新增測試檔**：`tests/test_exit_code_compliance_w15_028.py` — 11 個測試案例
  - track_audit: 3 案（business_reject=2、success=0、internal_error=1）
  - track_acceptance: 4 案（mutually_exclusive、missing_index、ticket_not_found、empty_acceptance 全為 rc=2）
  - track_batch: 2 案（no_valid_tickets、complete_resolve_failure 全為 rc=2）
  - track_dashboard: 2 案（no_active_version=2、internal_load_failure=1）
- **修改既有測試**：`tests/test_track_dashboard.py::test_D6_no_active_version` rc 斷言由 1 改為 2

### Acceptance 對照

| Acceptance | 達成證據 |
|-----------|---------|
| track_audit.py 業務拒絕 return 2，≥ 1 個 returncode==2 斷言 | `test_audit_business_reject_returns_2` |
| track_acceptance.py 用戶輸入錯誤 return 2，≥ 2 個斷言 | 4 個 rc==2 案（mutex、missing、not_found、empty） |
| track_batch.py 部分失敗 return 2，≥ 1 個斷言 | `test_batch_no_valid_tickets_returns_2` + `test_batch_complete_resolve_failure_returns_2` |
| track_dashboard.py 查無資料 return 2，≥ 1 個斷言 | `test_dashboard_no_active_version_returns_2` |
| ticket skill 既有測試 100% 通過 | 407 passed（含修改後的 D6） |
| 新增測試 ≥ 5 個 | 11 個新案 + 1 個既有更新 |
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-17T23:50:10
**Executing Agent**: thyme-python-developer
**Review Status**: pending