---
id: 0.18.0-W17-084
title: 改善 PC-093 exempt marker UX 與 multi_view_status H2/H3 衝突（W10-017 session 學習彙整）
type: ANA
status: in_progress
version: 0.18.0
wave: 17
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.18.0-W17-085
- 0.18.0-W17-086
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第八層:框架優化
  final_decision: 彙整本 session 觸碰 hook 機制的 UX 缺口
  rationale: 規則 5（所有發現必須追蹤）
who:
  current: rosemary-project-manager
  history: {}
what: 改善 PC-093 exempt marker UX 與 multi_view_status H2/H3 衝突（W10-017 session 學習彙整）
when: 待定義
where:
  layer: 待定義
  files: []
why: 'W10-017 父 ANA complete 過程觸碰兩個非顯而易見的 hook 規範：(1) PC-093 ticket-tracked 豁免類別要求
  reason 含 W{wave}-{seq} 格式 ID（用 017.9 短格式不通過），錯誤訊息 ticket-tracked-need-id 未顯示在用戶端；(2)
  multi_view_status 在 ANA Schema 中規定，但用 ## H2 標題會被 W17-072 hook 標為非 Schema H2 警告，需用
  ### H3 才相容兩端'
how:
  task_type: Implementation
  strategy: 待定義
acceptance:
- '[x] 分析報告已撰寫（含問題敘述、根因分析、改善方案 3 部分）'
- '[x] 根因已通過 5W1H 或因果鏈分析確認'
- '[x] 改善方案至少包含症狀修復和根因防護兩個方向'
- '[x] 分析結論已建立修復 Ticket（症狀修復），Ticket ID 已記錄在 spawned_tickets'
- '[x] 根因已建立防護 Ticket（機制防護），Ticket ID 已記錄在 spawned_tickets'
- '[x] 若無後續 Ticket 需建立，需說明理由'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-04-28T15:03:06'
completed_at: null
created: '2026-04-28'
updated: '2026-04-28'
---

# Execution Log

## Task Summary

改善 PC-093 exempt marker UX 與 multi_view_status H2/H3 衝突（W10-017 session 學習彙整）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

W10-017 父 ANA complete 過程觸碰兩個非顯而易見的 hook 規範，造成 PM 額外摩擦：

## 問題 1：PC-093 ticket-tracked 豁免要求 W{wave}-{seq} 完整 ID 格式

**症狀**：
- 用戶寫 `<!-- PC-093-exempt: ticket-tracked:Phase 1b 已由 017.9 完成評估 ... -->`
- complete 被擋，但 hook 用戶端輸出未顯示原因；只在程式內部標 `valid=False, err='ticket-tracked-need-id'`
- 必須直接讀 hook 程式碼或 dry-run 才知道需求

**根因**：
- `phase4-decision-enforcement-hook.py:66` `TICKET_ID_PATTERN = re.compile(r"\bW\d+-\d+")` 強制 `W` 前綴
- 專案內部慣用簡寫 `017.9` / `017.1` 不符合
- 驗證失敗的 err 字串 `ticket-tracked-need-id` 未在 hook stderr 輸出 humanized 訊息

**用戶端影響**：
- 5 處豁免有 3 處被拒（235/270 用簡寫；743/758 含 W17-011/W10-014 全格式通過）
- PM 重試多次才推論出格式要求

## 問題 2：multi_view_status H2 vs H3 標題級別衝突

**症狀**：
- ana-solution-schema.yaml 提供 `multi_view_status: reviewed` 純 key-value 範例（line 34-46）
- 用戶寫 `## multi_view_status` 滿足 schema 偵測（field_key 前綴匹配）
- 但 W17-072 hook（agent-definition-standard.md「禁止自定義 H2」）標 `## multi_view_status` 為非 Schema H2 警告
- 必須改 `### multi_view_status` 才同時滿足兩端

**根因**：
- ana-solution-schema 未指定標題層級，留給用戶自由發揮
- W17-072 hook Schema H2 白名單（Task Summary / Problem Analysis / Solution / Test Results / Completion Info / Context Bundle / NeedsContext / Exit Status）未包含 multi_view_status
- 兩端規則設計時未對齊：一端把 multi_view_status 視為「Solution 子標註」，另一端視為獨立 H2 章節

**用戶端影響**：
- W10-017.17 標註後遭 W17-072 警告
- 必須再 commit 改 H3
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## multi_view_status

**status**: skipped

**reason**: 本 ANA 為單 session 觸發 UX 缺口的盤點與修復方向定義，非新設計決策。findings 為事實性觀察（程式碼行號 + 用戶端摩擦次數），結論為兩個獨立、低風險的修復點，不涉及方案池選擇或結構性偏見風險。

---

## 改善方向（兩個 spawned IMP）

### IMP A：PC-093 hook 用戶端訊息可見性

**症狀修復**：當 marker 因 `ticket-tracked-need-id` 失敗時，在 hook stderr 輸出 humanized 訊息，例如：
> `[PC-093] line 235 marker 失效：ticket-tracked 類別要求 reason 含 W{wave}-{seq} 格式 ID（如 W10-017.9），偵測到的 reason 為「短 ID 引用未含 W 前綴」` <!-- PC-093-exempt: ticket-tracked:本 ANA 引用 W10-017.9 為 W10-017 任務鏈衍生 ticket（已 completed），用以說明 hook 拒絕的具體案例 -->

**根因防護**（可選）：放寬 `TICKET_ID_PATTERN` 接受專案內慣用的短格式（如 `\b\d+\.\d+`），或在 PC-093 error-pattern 文件首段顯式列出格式要求 + 反例。

**目標檔案**：`.claude/hooks/phase4-decision-enforcement-hook.py:66, 273-274`

### IMP B：multi_view_status 與 W17-072 規則對齊

**選項 B1（推薦）**：W17-072 hook 加 multi_view_status 到 Schema H2 白名單。理由：ana-solution-schema 已將其定為 ANA Solution 區段必填標註，等同 Schema 規範一部分。

**選項 B2**：ana-solution-schema 範例改為 H3 形式（`### multi_view_status`），明示是 Solution 子節，配合 W17-072 邏輯。

**選項 B3**：兩規則文件互相連結，但仍需用戶記住 H3 形式（不解決 UX）。

**目標檔案**：
- `.claude/config/ana-solution-schema.yaml`（範例與描述）
- `.claude/rules/core/agent-definition-standard.md`（H2 白名單）
- 對應 W17-072 hook 實作

---

## 不在本 ANA 範圍

- W11-003.7（ticket create when parent has plain string `who` 觸發 AttributeError）已獨立追蹤
- 其他 hook 提到的 7 個 PC error-patterns（PC-040/PC-064/PC-050/PC-105×2/PC-093/PC-007）皆已有自身 ticket，不重複建立
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending