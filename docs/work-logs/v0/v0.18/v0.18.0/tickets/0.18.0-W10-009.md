---
id: 0.18.0-W10-009
title: 建立 wrap-decision-tripwire-hook 統一實作 tripwire-catalog 與 W9-004 訊號設計
type: IMP
status: in_progress
version: 0.18.0
wave: 10
priority: P2
parent_id: null
children: []
blockedBy:
- 0.18.0-W10-008
relatedTo:
- 0.18.0-W9-004
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第四層:PM 流程改善
  final_decision: Hook 自動偵測層（補 PM 自律缺口）
  rationale: quality-baseline-rule-4 Hook 可見 + 規則 6 框架優先
who:
  current: pending
  history: {}
what: 建立 decision-quality-guard-hook 自動偵測 WRAP 觸發訊號
when: W10-008 規則層建立後執行
where:
  layer: Infrastructure
  files:
  - .claude/hooks/wrap-decision-tripwire-hook.py
  - .claude/hook-state/wrap-tripwire-state.json
  - .claude/settings.json
  - .claude/skills/wrap-decision/references/tripwire-catalog.md
why: W9-004 分析產出方案 S1 + skills/wrap-decision/references/tripwire-catalog.md 已有完整設計規格（source
  W12-004+W12-007）「實際 Hook 程式碼實作為獨立 Ticket（待建立）」——統一採用 tripwire-catalog 命名和絆腳索哲學，加入
  W9-004 新增的 ANA claim 訊號
how:
  task_type: Implementation
  strategy: 1) 採用 tripwire-catalog.md 的 Hook 名稱 wrap-decision-tripwire-hook.py 2)
    觸發事件依 tripwire-catalog：PostToolUse(Agent) + UserPromptSubmit（非 Bash，避免誤觸發）3) 實作偵測訊號：(a)
    連續 2 次代理人失敗 — 依 tripwire-catalog 設計 (b) 「做不到」類關鍵字正則 — 依 tripwire-catalog 設計 (c)
    ANA ticket claim — W9-004 新增訊號 4) 狀態追蹤依 tripwire-catalog：.claude/hook-state/wrap-tripwire-state.json
    含 current_ticket/consecutive_failures/last_failure_time，三個重置條件 5) 提醒內容採絆腳索哲學：「你是有選擇的」而非強制，列三個選項
    6) PEP 723 單檔腳本 7) 註冊到 settings.json 8) 更新 tripwire-catalog.md 移除「待建立」標記並 link
    到此 Ticket 9) **Source-of-truth 約束（W10-052）**：Hook 必須在 runtime 讀取 wrap-decision
    SKILL 觸發條件作為唯一資料來源，禁止硬編碼觸發清單。若 markdown 不利機器解析，提取為 .claude/config/wrap-triggers.yaml
    並建立雙向同步機制（SKILL 為人類來源，YAML 為機器來源，CI 檢查兩者一致）
acceptance:
- '[ ] Hook 檔案建立為 wrap-decision-tripwire-hook.py（符合 tripwire-catalog 命名）'
- '[ ] 觸發事件為 PostToolUse(Agent) + UserPromptSubmit（依 tripwire-catalog 設計）'
- '[ ] 3 個訊號偵測正確運作（連續失敗 + 做不到關鍵字 + ANA claim）'
- '[ ] 狀態追蹤 wrap-tripwire-state.json 含三個重置條件（成功/切換 Ticket/手動執行 /wrap-decision）'
- '[ ] 提醒內容採絆腳索哲學（你是有選擇的 + 三個選項），非強制指令'
- '[ ] stderr 輸出符合規則 4 雙通道'
- '[ ] settings.json 已註冊'
- '[ ] tripwire-catalog.md 的「實際 Hook 程式碼實作為獨立 Ticket（待建立）」標記已移除並 link 到此 Ticket'
- '[ ] **Source-of-truth 約束**：Hook 必須讀取 wrap-decision SKILL 觸發條件章節（或對應 YAML）作為 source，不得硬編碼觸發清單（W10-052）'
- '[ ] **Schema 提取**：若 SKILL 為 markdown 不利機器讀取，提取為 .claude/config/wrap-triggers.yaml
  並雙向同步（W10-052）'
- '[ ] **acceptance-auditor 驗證**：Hook 程式碼確實 import/parse SKILL 或 YAML 而非硬編碼觸發條件（W10-052）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-15T11:25:27'
completed_at: null
created: '2026-04-13'
updated: '2026-04-13'
---

# Execution Log

## Task Summary

建立 decision-quality-guard-hook 自動偵測 WRAP 觸發訊號

---

## Problem Analysis



### Context Bundle for Phase 1 (lavender-interface-designer)

**TDD 階段**: Phase 1（功能規格設計，禁止寫測試或實作）

**已決策（請勿重新評估）**：
1. Hook 名稱: `.claude/hooks/wrap-decision-tripwire-hook.py`（PEP 723 單檔）
2. 觸發事件: `PostToolUse(Agent)` + `UserPromptSubmit`
3. State 檔案: `.claude/hook-state/wrap-tripwire-state.json`
4. Source-of-truth 採 YAML 提取: `.claude/config/wrap-triggers.yaml`（用戶確認）
5. 3 訊號: 連續失敗 / 限制性關鍵字 / ANA claim

**必讀來源（依序）**：
1. 本 Ticket frontmatter（acceptance 11 條 + W10-052 約束章節）
2. `.claude/skills/wrap-decision/references/tripwire-catalog.md`
3. `.claude/skills/wrap-decision/SKILL.md`（觸發條件章節 + description）
4. `.claude/rules/core/observability-rules.md`（規則 4 雙通道 stderr）

**Phase 1 必須產出（寫入本 ticket Solution 章節）**：

1. **觸發條件對照表**（W10-052 防失真核心）
   - 逐條對照 SKILL description / 觸發條件章節 / tripwire-catalog 三處的訊號
   - 不一致處標註「需 Phase 3b 同步修正」

2. **YAML Schema 設計** (`.claude/config/wrap-triggers.yaml`)
   - 訊號類型 (consecutive_failures / restrictive_keywords / ana_claim)
   - 每訊號參數（閾值、關鍵字清單、事件來源）
   - 提醒訊息範本（保持絆腳索哲學）
   - 重置條件清單
   - 提供完整 YAML 範例

3. **Hook API 介面**
   - Input: stdin hook event JSON 結構（兩種事件）
   - Output: stderr 提醒格式 + exit code
   - Side Effects: state.json 讀寫
   - Source 讀取: YAML 載入流程 + parse 失敗 fallback

4. **State JSON Schema** (`.claude/hook-state/wrap-tripwire-state.json`)
   - current_ticket / consecutive_failures / last_failure_time / last_warned_at
   - 必要時加 last_signal_type 等

5. **3 訊號偵測規格**
   - 連續失敗：「失敗」如何認定？跨 ticket 切換行為？
   - 限制性關鍵字：清單來源（指 YAML）+ 比對方式
   - ANA claim：偵測 ticket type=ANA claim 的 hook event 來源

6. **重置條件規格**
   - 成功完成 / 切換 ticket / 手動 /wrap-decision 各自偵測方式

7. **驗收條件結構化**
   - 11 條 acceptance 逐條 mapping 到 Phase 2 測試案例類別（只分類，不寫測試）

**禁止事項**：
- 不寫測試案例（sage Phase 2）
- 不寫 Hook 程式碼（basil Phase 3b）
- 不決定實作細節（函式/變數命名）
- 不建立任何檔案（除 ticket Solution append-log 外）

**產出方式**：
1. 用 `ticket track append-log 0.18.0-W10-009 --section "Solution"` 寫入分析
2. 完成後標註 `## Phase 1 Status: COMPLETE` + 列出待 Phase 2 sage 的測試類別清單

**時間預期**：15-20 tool call。
### WRAP 三問（claim Checkpoint，2026-04-15）

**W（Widen）—— 候選方案**：
1. （採用）建 `wrap-decision-tripwire-hook.py` + `.claude/config/wrap-triggers.yaml` 作為機器可讀來源；SKILL.md 加雙向同步註解
2. （否決）不建 Hook，僅靠 W10-008 規則層引導：W9-004 已分析 PM 自律 60% 失敗率，規則層不足以觸發
3. （否決）整合到既有 askuserquestion-reminder-hook：違反單一職責，且觸發事件不同（PostToolUse Agent vs UserPromptSubmit）

**A（Attain distance）—— 機會成本**：
本 ticket 排擠 W10-049.1~.4 拆分執行（清理 .claude/ 根目錄 md 檔），但 W10-049 為非阻塞 Explore 任務，可後續波次處理。W10-009 為 W9-004 分析的核心治本方案，優先級高於整理工作。

**P（Prepare to be wrong）—— 12h 後最可能失敗原因**：
最可能：YAML schema 設計時與 SKILL.md/tripwire-catalog.md 內容語義對應不齊，導致 Hook parse 後行為與文檔不符。
防護：Phase 1 lavender 必須先列出 SKILL.md `description` + `觸發條件` 章節 + tripwire-catalog 表格的所有觸發訊號，建立完整對照表，才能設計 YAML schema。Schema 必須有對應驗證機制（CI 或 hook）確保與 markdown 來源一致。
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

### Source-of-Truth 約束（W10-052 設計理由）

W10-008 多視角審查（linux 視角）發現「文檔引用」只是表面 DRY 修正——若 Hook 落地時硬編碼觸發條件清單，DRY 違反會從文檔層轉移到實作層（同樣的觸發條件出現在 SKILL.md 與 Hook 程式碼兩處，未來修改 SKILL 時 Hook 不會自動同步）。

**約束內容**：

1. **Single Source of Truth**：觸發條件的權威來源為 `.claude/skills/wrap-decision/references/tripwire-catalog.md`（人類可讀）或對應的 `.claude/config/wrap-triggers.yaml`（機器可讀）
2. **Hook 讀取規則**：`wrap-decision-tripwire-hook.py` 必須在 runtime 載入上述來源，禁止把觸發條件（關鍵字清單、失敗次數閾值、訊號類型）寫死在 Python 程式碼中
3. **格式選擇**：
   - 若 markdown 表格能被 parse 出穩定結構，Hook 可直接讀 markdown
   - 若解析複雜或脆弱，提取為 `.claude/config/wrap-triggers.yaml`，並在 SKILL.md 中註明「機器讀取版本見 .claude/config/wrap-triggers.yaml，兩者必須同步」
   - YAML 提取後需要 CI 或 hook 檢查兩者一致性（避免人類更新 markdown 卻忘記更新 YAML）
4. **acceptance-auditor 驗證**：本 Ticket 完成驗收時，需檢查 Hook 程式碼確實有 `open()` / `yaml.load()` / `parse_markdown()` 等讀取動作，而不是 `TRIGGERS = ["做不到", "沒辦法", ...]` 之類的硬編碼

**為何重要**：

絆腳索（tripwire）的設計重點是「容易被觸發」。如果觸發條件硬編碼在 Hook 內，未來新增訊號（如 W9-004 的 ANA claim 訊號）需要修改 Python 程式碼，而非更新 SKILL 文件。這違反「文檔即配置」原則，也讓 SKILL.md 變成「需要對照程式碼才知道實際生效規則」的失真文件。

**關聯**：

- W10-008（已完成）：建立規則層引導，但被 linux 視角識破為表面 DRY 修正
- W10-051（同時進行）：擴充 acceptance-gate-hook 強制 ANA Solution multi_view_status 標註
- W10-052（本約束來源）：強化 W10-009 frontmatter 加 source-of-truth 約束

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending