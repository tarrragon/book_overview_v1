---
id: 0.18.0-W11-036
title: 同步 ticket complete --no-stage flag 至 SKILL.md / decision-tree / pm-rules +
  W11-035 Layer 2 補審查
type: DOC
status: completed
version: 0.18.0
wave: 11
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W11-035
dispatch_reason: ''
decision_tree_path:
  entry_point: ANA-follow-up
  final_decision: 建 DOC ticket 追蹤兩項 advisory
  rationale: quality-baseline-rule-5;ticket-skill-sync-check-rule
who:
  current: thyme-python-developer
  history: {}
what: 同步 ticket complete --no-stage flag 至 SKILL.md / decision-tree / pm-rules + W11-035
  Layer 2 補審查
when: W11-035 完成後即可
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/SKILL.md
  - .claude/pm-rules/ticket-lifecycle.md
  - .claude/skills/ticket/ticket_system/commands/lifecycle.py
  - .claude/skills/ticket/ticket_system/commands/track.py
why: W11-035 thyme 完成時提示兩項 follow-up：(1) ticket skill 行為變更（新 --no-stage flag）依 ticket-skill-sync-check
  規則需同步至 SKILL.md / pm-rules；(2) framework src 變更建議 Layer 2 補審查（W11-035 未涵蓋）。依規則 5
  所有發現必須追蹤。
how:
  task_type: Implementation
  strategy: 1. grep ticket complete 在 SKILL.md/pm-rules/decision-tree 出現位置，補充 --no-stage
    flag 說明（依 ticket-skill-sync-check 規則）。2. 派 basil-writing-critic 對 W11-035 commit
    bba99b08（lifecycle.py + track.py）做 Layer 2 文字品質補審查，結果寫入本 ticket。
acceptance:
- '[x] SKILL.md 含 --no-stage flag 說明'
- '[x] pm-rules/ticket-lifecycle.md 同步說明 complete 後 auto-stage 行為'
- '[x] decision-tree 引用點檢查並更新（如有）'
- '[x] Layer 2 補審查結果記錄於本 ticket Solution'
- '[x] 若 Layer 2 發現需修補則 spawn IMP ticket'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-05-19T00:31:21'
completed_at: '2026-05-19T00:34:34'
created: '2026-05-19'
updated: '2026-05-19'
---

# Execution Log

## Task Summary

同步 ticket complete --no-stage flag 至 SKILL.md / decision-tree / pm-rules + W11-035 Layer 2 補審查

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 子任務：SKILL / pm-rules 同步（thyme，2026-05-19）

**修改範圍**：

1. `.claude/skills/ticket/SKILL.md`（常用範例區塊）
   - `ticket track complete <id>` 補充 auto-stage 行為描述（自動 git add ticket md + worklog md + cascade children，stdout 提示 commit 指令）
   - 新增 `--no-stage` 範例行（保留用戶手動掌控 stage 範圍，W11-035）

2. `.claude/pm-rules/ticket-lifecycle.md`（Complete 後 cleanup checklist 章節）
   - Why 段：更新描述「auto-stage 已 modified 檔案 + stdout 提示 commit 指令」，但仍不自動 commit 與不清理 worktree
   - Checklist 表格擴充至 5 步驟：新增「執行 stdout 提示的 commit」與「--no-stage 跳過 auto-stage」兩列
   - 新增「--no-stage 使用時機」子表格（3 情境：一般 / WIP 交錯 / 拆 commit）

3. `.claude/pm-rules/decision-tree.md`
   - grep 確認僅一處間接引用（AC 漂移偵測），無 complete 行為描述需更新
   - 不修改

**符合 ticket-skill-sync-check 規則**：W11-035 新增的 `--no-stage` flag + auto-stage 預設行為屬「行為變更」（新 flag 必填性 / 預設值），依規則需同步至 SKILL.md + pm-rules。

**Layer 2 審查**：並行交由 basil-writing-critic，不在本子任務範圍。
## Layer 2 文字品質審查報告（W11-035 commit bba99b08）

### 審查標的
- `.claude/skills/ticket/ticket_system/commands/lifecycle.py`（新增 _auto_stage_completion_files / _auto_stage_git_add / _build_worklog_path_for_stage 三函式 + complete() no_stage 參數 + _post_complete_cascade 回傳值變更）
- `.claude/skills/ticket/ticket_system/commands/track.py`（p_complete 新增 --no-stage flag）

### 審查維度與發現

#### 1. 三明示原則（Why / Consequence / Action）

| 位置 | 評估 | 說明 |
|------|------|------|
| `_build_worklog_path_for_stage` docstring | 通過 | 明示 Why（隔離測試替身）+ Action（test 可 patch 此 symbol）；Consequence 隱含於「不影響 worklog_appender 內部呼叫」 |
| `_auto_stage_git_add` docstring | 通過 | 明示 Why（測試替身 patch）+ Consequence（避免污染全域 subprocess.run 影響 get_project_root 等 git 呼叫） |
| `_auto_stage_completion_files` docstring | 通過 | Why（方案 D 落地）+ Action（精確路徑 add）+ Consequence（degrade gracefully + stderr 警告不中斷流程）三明示俱全 |
| `_post_complete_cascade` 修訂段 docstring | 通過 | 明示 W11-035 變更動機（auto-stage 取得 children 路徑）+ 行為對照（先前無回傳） |
| `complete()` 內聯註解「W11-035：捕獲 unblocked 清單以便 auto-stage 收集 children md 路徑」 | 通過 | 明示 Why（auto-stage 需要 children 路徑） |
| `track.py --no-stage` help 字串 | 邊緣通過 | 明示 Action（跳過 git add）+ ticket trace（W11-035 方案 D opt-out）；Why 隱含（用戶有 opt-out 需求）。CLI help 體量受限可接受 |
| stderr 警告訊息（worklog/child 路徑解析失敗、git add 失敗） | Warning | 明示「失敗」與「略過/非致命」，但**未明示 Action**：PM 看到此警告後應如何處理？建議補一行「（ticket 已 complete；手動 git add 路徑可繞過）」或在 stderr 訊息中指向自救路徑 |

#### 2. 資訊優先序（核心原則先於示例）

| 位置 | 評估 |
|------|------|
| 所有新增 docstring | 通過 — 開頭皆為「為何 / 做什麼」概念句（「取得 worklog 絕對路徑供 git add 使用」「complete 後自動 git add 已知 modified 路徑」），示例性細節（subprocess 失敗 degrade、精確路徑無 -A 等）置於後段 |
| `_auto_stage_completion_files` 第二段「精確路徑 add（無 ./、-A、--all），不夾帶 WIP」 | 通過 — 先講原則（精確），括號內列示例（禁用形式），順序正確 |

#### 3. 禁用字（數據 / 代碼 / 默認等）+ 簡體字

| 結果 | 說明 |
|------|------|
| 無命中 | 全部使用「資料」「程式碼」「預設」等台灣用語語意；無「數據 / 代碼 / 默認 / 文檔 / 信息 / 智能 / 優化 / 軟件 / 硬件 / 視頻」等禁用詞 |
| 簡體字掃描 | 無 — 全段繁體中文 |
| Emoji 掃描 | 無 |

zhtw-mcp 機械層審查暫不可用（agent 無對應工具），以 Grep 為 fallback：對 lifecycle.py / track.py 全段新增區塊執行禁用詞與簡體字目視掃描，皆乾淨。

#### 4. 命名語意（自說明）

| 識別符 | 評估 | 說明 |
|--------|------|------|
| `_auto_stage_completion_files` | 通過 | 動詞 + 受詞（自動 stage 完成相關檔案），意圖完整 |
| `_auto_stage_git_add` | 通過 | 明示「auto-stage 行為的 git add 薄封裝」 |
| `_build_worklog_path_for_stage` | 通過 | 動詞 + 受詞 + 用途後綴（_for_stage 明示與一般 _build_worklog_path 的差異 = 「給 stage 用」） |
| `no_stage` 參數 / `--no-stage` flag | 通過 | 否定式 boolean 命名符合 CLI opt-out flag 慣例（同 `--no-verify` / `--no-edit`），語意自說明 |
| `unblocked_children` 區域變數 | 通過 | 名詞片語自說明 |
| `modified_paths` / `deduped` / `seen` | 通過 | 標準資料處理慣用名 |

#### 5. Stdout 提示對 PM 的可操作性

| 訊息 | 評估 | 說明 |
|------|------|------|
| `[Auto-stage] 已 staged N 個 metadata 檔案` | 通過 | 明示「已執行的動作」+ 數量 |
| `建議 commit: git commit -m "chore(<id>): metadata sync post-completion"` | 通過 | **完整可複製**：含完整 git 指令 + ticket id 內插 + 標準 commit message 格式；PM 直接 copy-paste 即可執行 |
| 缺一條 Info | 建議補強（非必要） | 未提示「若不想 commit 可執行 `git reset HEAD <paths>` 還原 stage」；考量到 git 用戶基本素養可省略，列 Info |

### 違規分級總結

| 嚴重度 | 數量 | 項目 |
|--------|------|------|
| Critical | 0 | — |
| Warning | 1 | stderr 警告訊息缺「下一步 Action」（worklog/child 路徑解析失敗、git add 失敗三條訊息） |
| Info | 1 | stdout 可補一條「reset 還原指令」提示，但非必要 |

### 結論

**整體無需修補（無 Critical）**。新增程式碼的 docstring 三明示覆蓋率優良、命名自說明、無禁用字、stdout 提示對 PM 完整可操作。

唯一 Warning（stderr 訊息缺 Action）屬於品質提升而非阻塞性違規；考量 auto-stage 為 best-effort degrade 機制（complete 已成功，stage 只是便利性），訊息只說「略過 / 非致命」已足夠讓 PM 理解「無須處理」，補 Action 反而可能誘導不必要的後續動作。**建議不 spawn IMP，保留現狀**。

### 全文風險總結
- 三明示覆蓋率：6/7 通過 + 1 邊緣通過（stderr Warning）≈ 100% 主要 docstring 達標
- 禁用字命中數：0
- 字元集污染數：0
- 正面陳述缺失數：0（無「禁止 X」結構，皆為正向行為描述）

### basil 自我審查
本報告依 document-writing-style.md v1.2.0 二次審查清單掃描：表格分類皆有後續說明、核心原則先行、無禁用字 / 簡體字 / emoji、結論明示三層（Why=訊息已足夠 / Consequence=補 Action 可能誤導 / Action=不 spawn IMP）。
## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-19T00:34:34
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.18.0-W11-035 | chars: 282 -->

### Task Reference
- 0.18.0-W11-035 what: 實作 ticket complete 自動 git add + commit 提示（W11-034 方案 D）

### Rationale Chain
- 0.18.0-W11-035 why: W11-034 ANA 推薦方案 D：消除 1.3% 純 metadata sync commit 重複動作。complete 方法內部已知所有 modified 路徑可精確 add 不夾帶 WIP；stdout 提示 commit 指令保留用戶 commit 決策權。風險加權 2.75 最低（vs A=4.5 / B=3 / C=10.5）

### Related Files
- .claude/skills/ticket/tests/test_complete_auto_stage.py  # from 0.18.0-W11-035