---
id: 0.18.0-W10-017.2
title: 新增 ticket track dispatch-check 子命令（取代 PC-050 cat+python3 片段）
type: IMP
status: in_progress
version: 0.18.0
wave: 10
priority: P1
parent_id: 0.18.0-W10-017
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第四層:PM 流程改善
  final_decision: CLI 化 PC-050 腳本片段
  rationale: W10-017 WRAP Solution - 最直接的「決策建議型輸出」範例：PM 只需 call 一次 CLI，不再需要 copy-paste
    腳本
who:
  current: thyme-python-developer
  history: {}
what: 新增 ticket track dispatch-check 子命令（取代 PC-050 cat+python3 片段）
when: 待定義
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/
why: W10-017 WRAP Solution Phase 1b - PC-050 腳本片段每次需要 copy-paste，CLI 化可減少 PM 認知負擔並確保一致性
how:
  task_type: Implementation
  strategy: 在 ticket_system 新增 dispatch-check 子命令：1) 讀取 .claude/dispatch-active.json；2)
    輸出格式：無活躍時「[PASS] 無活躍派發，可繼續」；有活躍時列出 agent_description + ticket_id + 時間；3) exit
    code：無活躍=0，有活躍=1（供 Hook 使用）；4) 與 completion-checkpoint-rules.md 腳本片段行為等價測試
acceptance:
- '[ ] ticket track dispatch-check 子命令可執行'
- '[ ] 無活躍派發時輸出 [PASS] 且 exit 0'
- '[ ] 有活躍派發時列出每個代理人 + exit 1'
- '[ ] 與 PC-050 腳本片段的輸出內容語意等價'
- '[ ] 新增測試覆蓋「空 / 1 個 / 多個」3 情境'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-19T17:19:37'
completed_at: null
created: '2026-04-18'
updated: '2026-04-18'
---

# Execution Log

## Task Summary

新增 ticket track dispatch-check 子命令（取代 PC-050 cat+python3 片段）

---

## Problem Analysis

## Context Bundle（PM 前台驗證整理，2026-04-19）

### 1. PC-050 腳本片段現況

**位置**：`.claude/error-patterns/process-compliance/PC-050-premature-agent-completion-judgment.md` line 88, 93

**內容**：
```bash
cat .claude/dispatch-active.json  # 確認派發數量正確
```

**引用點**：
- 派發後清點（dispatch-gate）
- 收到完成通知時（agent-failure-sop）

兩處邏輯完全相同：讀檔 + 人眼判斷是否為空。

### 2. dispatch-active.json 結構（當前實例）

```json
{
  "dispatches": [
    {
      "agent_description": "W11-004.1.3.1 refactor",
      "tool_use_id": "toolu_01SFf2SGP3B77sBNmrRFrwDe",
      "agent_id": "a393d617c0de2aaf6",
      "ticket_id": "",
      "files": [],
      "branch_name": "",
      "dispatched_at": "2026-04-19T04:27:59.634099+00:00"
    },
    ...（共 3 個）
  ]
}
```

**欄位要求**（dispatch-check 輸出使用）：
- agent_description（主要）
- ticket_id（可能為空）
- dispatched_at（時間戳）

### 3. 實作模板

**最近範例**：`.claude/skills/ticket/ticket_system/commands/track_handoff_ready.py`
- exit codes 設計（0 GO / 2 NO-GO）
- 分離 lib/ (state/view) 與 commands/ (argparse + print)
- 參考其 CLI 註冊方式（ticket_system 主入口）

**另一範例**：`track_checkpoint_status.py`（同 W10-017.1 系列）

### 4. CLI 子命令註冊位置

需修改 ticket_system 主 entry（argparse subparsers）。實作時請先找出 track 子命令的註冊方式（grep "handoff-ready" in ticket_system/）。

### 5. 非範圍明示

- **不處理** dispatch-active.json 死記錄清理（另屬 SubagentStop hook 範疇）
- **不改動** PC-050 文件本身（本 ticket 只新增 CLI，後續 ticket 才改 PC-050 引用）

### 6. 等價性判定

「與 PC-050 腳本片段語意等價」= 讀 dispatch-active.json + 判斷 dispatches 陣列空/非空。
新 CLI 多加：格式化輸出 + exit code（供 Hook/script 程式化判定）。

## Phase 2 測試設計（3 情境）

| 測試 | 情境 | 預期 exit |
|------|------|----------|
| test_dispatch_check_no_file | 檔案不存在 | 0（視同空） |
| test_dispatch_check_empty_list | dispatches=[] | 0 |
| test_dispatch_check_single | 1 個活躍 | 1 |
| test_dispatch_check_multiple | 多個活躍 | 1 |

（原 AC 寫 3 情境，實作時可加 "no_file" 作邊界案例，共 4 個測試）

## Phase 3a 偽碼

```python
def run_dispatch_check(args) -> int:
    dispatch_file = Path(".claude/dispatch-active.json")
    if not dispatch_file.exists():
        print("[PASS] 無活躍派發，可繼續")
        return 0
    try:
        data = json.loads(dispatch_file.read_text(encoding="utf-8"))
    except json.JSONDecodeError:
        print("[FAIL] dispatch-active.json 格式錯誤", file=sys.stderr)
        return 2  # IO error（參考 handoff_ready IO_ERRORS 保守 NO-GO）
    dispatches = data.get("dispatches", [])
    if not dispatches:
        print("[PASS] 無活躍派發，可繼續")
        return 0
    print(f"[WARN] 有 {len(dispatches)} 個活躍派發：")
    for d in dispatches:
        desc = d.get("agent_description", "(unknown)")
        tid = d.get("ticket_id", "") or "(no ticket)"
        ts = d.get("dispatched_at", "")
        print(f"  - {desc} | ticket: {tid} | {ts}")
    return 1
```

（thyme 可依需求調整；偽碼僅供架構參考，非規範）
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending