---
id: 0.18.0-W10-063
title: 重構 test_wrap_decision_tripwire_hook.py 的 DEFAULT_YAML 改結構化 fixture
type: ADJ
status: completed
version: 0.18.0
wave: 10
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.18.0-W10-056.3
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: W10-056.3 scope 邊界外技術債 follow-up
  rationale: 測試檔品質延後-不阻塞
who:
  current: pending
  history: {}
what: 重構 test_wrap_decision_tripwire_hook.py 的 DEFAULT_YAML 改結構化 fixture
when: 待定義
where:
  layer: 待定義
  files:
  - .claude/hooks/tests/test_wrap_decision_tripwire_hook.py
why: W10-056.3 代理人回報 B-3 CE-5 不適用：DEFAULT_YAML 位於 tests/test_wrap_decision_tripwire_hook.py
  作為測試 fixture，受 scope 邊界保護（禁止修改 tests/）。原 Phase 4a 識別的問題是「字串替換隱性依賴」，應改為結構化字典 + yaml.safe_dump。因位於
  tests/ 需獨立 ticket 處理，並評估是否開放 tests/ 修改權限。
how:
  task_type: Implementation
  strategy: 1. 分析 test_wrap_decision_tripwire_hook.py 中 DEFAULT_YAML 的使用情境 2. 將 DEFAULT_YAML
    字串常數改為結構化 dict 3. 改用 yaml.safe_dump 取代字串替換 4. 確保所有既有測試通過 5. 建立測試 fixture 品質指南（避免其他測試重蹈覆轍）
acceptance:
- '[x] DEFAULT_YAML 改為結構化 dict'
- '[x] 改用 yaml.safe_dump 替換字串'
- '[x] 66/66 測試通過率 100%'
- '[x] 測試 fixture 品質指南文件化（可選）'
tdd_phase: phase2
tdd_stage:
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-15T15:58:10'
completed_at: '2026-04-15T16:03:19'
created: '2026-04-15'
updated: '2026-04-15'
---

# Execution Log

## Task Summary

重構 test_wrap_decision_tripwire_hook.py 的 DEFAULT_YAML 改結構化 fixture

---

## Problem Analysis

### Context Bundle (PM 派發指示 2026-04-15)

**來源**: W10-056.3 代理人回報 CE-5「DEFAULT_YAML 位於 tests/，scope 邊界禁修測試檔」，另開 follow-up

**並行環境**:
- basil（W10-060）正在動 .claude/hooks/ 的其他 Hook + hook_utils + test_active_dispatch_tracker_hook.py，**禁止觸碰**
- 本 ticket 只動 .claude/hooks/tests/test_wrap_decision_tripwire_hook.py

### 目標

將 DEFAULT_YAML 字串常數 + `.replace()` pattern 改為結構化 dict + `yaml.safe_dump`：

**現狀**（推測，代理人需 grep 確認）:
```python
DEFAULT_YAML = """
version: "1.0.0"
signals:
  - id: consecutive_failures
    ...
"""
# 測試時用 DEFAULT_YAML.replace("1.0.0", "2.0.0") 變體
```

**目標**:
```python
DEFAULT_CONFIG = {
    "version": "1.0.0",
    "signals": [
        {"id": "consecutive_failures", ...}
    ],
}

def yaml_fixture(**overrides):
    config = {**DEFAULT_CONFIG, **overrides}
    return yaml.safe_dump(config)
```

### Scope 邊界

- **只動** `.claude/hooks/tests/test_wrap_decision_tripwire_hook.py`
- **禁止動** 其他 tests/ 檔（basil 可能在動 test_active_dispatch_tracker_hook.py）
- **禁止動** .claude/hooks/ 任何產品碼（basil 活躍區域）
- **禁止動** PC-050 / PC-070（W10-062 活躍區域）

### 關鍵約束

- 66/66 既有測試通過率 100%
- 保留 DEFAULT_YAML 舊變數名為 alias（backward compat）或明確遷移所有 usage
- 改動應**純重構**，不改測試語義

### 派發時段注意

並行有 2 個活躍工作區（basil W10-060 + W10-062 PC 家族），**嚴守檔案邊界**
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## 執行摘要（acceptance-auditor 驗收後補填）

**commit**: 5b060d71

**實際變更**（僅 `.claude/hooks/tests/test_wrap_decision_tripwire_hook.py`）:
- 新增 `import copy` + `import yaml`
- `DEFAULT_YAML` 字串常數 → `DEFAULT_CONFIG` dict（結構化 source of truth）
- 新增 `yaml_fixture(top_overrides=None, signal_overrides=None)` helper（yaml.safe_dump）
- `DEFAULT_YAML = yaml_fixture()` 保留為 backward-compat alias
- 6 處 `.replace()` pattern 全數替換為 `yaml_fixture(...)` 結構化覆蓋

**Scope 遵守**: 僅動單一測試檔，未觸碰其他並行區域。

## Test Results

`pytest .claude/hooks/tests/test_wrap_decision_tripwire_hook.py -v` — 66/66 passed in 0.33s（100%）
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending