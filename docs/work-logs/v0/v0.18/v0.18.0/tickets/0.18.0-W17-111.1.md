---
id: 0.18.0-W17-111.1
title: 改善 hook 訊息加 nested 結構誤用提示 + 補測試
type: IMP
status: completed
version: 0.18.0
wave: 17
priority: P2
parent_id: 0.18.0-W17-111
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme
  history: {}
what: 改善 hook 訊息加 nested 結構誤用提示 + 補測試
when: W17-111 ANA 完成後
where:
  layer: 待定義
  files:
  - .claude/hooks/acceptance_checkers/multi_view_checker.py
  - .claude/hooks/tests/test_multi_view_checker.py
why: PM 寫 nested YAML 時 hook 訊息無法引導修正方向（PC-117 / W17-095 案例）
how:
  task_type: Implementation
  strategy: _format_invalid_value_warning 加分支：偵測 value 含冒號則附 nested 結構誤用提示與 flat 範例；補對應測試覆蓋
    nested 觸發訊息分支
acceptance:
- '[x] _format_invalid_value_warning 偵測 value 含冒號時附「nested 結構誤用」提示'
- '[x] 新增測試覆蓋 nested YAML 觸發訊息分支'
- '[x] 現有 multi_view_checker 測試全綠不受影響'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-02T22:30:01'
completed_at: '2026-05-02T22:31:46'
created: '2026-05-02'
updated: '2026-05-02'
---

# Execution Log

## Task Summary

改善 hook 訊息加 nested 結構誤用提示 + 補測試

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

W17-111 ANA 落地：hook 訊息「目前值: status: skipped」對用戶不夠清楚，無法引導修正方向（PM 不知道是 nested YAML 結構誤用）。需在 `_format_invalid_value_warning` 加分支偵測 value 含 `:` 字元並附 nested 結構誤用提示。

### 影響範圍

- `.claude/hooks/acceptance_checkers/multi_view_checker.py` `_format_invalid_value_warning`（行 207-218）
- `.claude/hooks/tests/test_multi_view_checker.py` 新增 nested YAML 觸發訊息測試

### 相關 Error Pattern

- PC-117（用戶側防護，本 ticket 為系統側補強）
- W17-111 ANA 父任務

---

## Context Bundle

### 既有實作（行 207-218）

```python
def _format_invalid_value_warning(
    ticket_id: str, field_key: str, value: str, allowed: List[str]
) -> str:
    return (
        f"[WARNING] Acceptance Gate: ANA Ticket {field_key} 值非法\n"
        f"\n"
        f"Ticket: {ticket_id}\n"
        f"目前值: {value}\n"
        f"合法值: {', '.join(allowed)}\n"
        f"\n"
        f"Schema 來源：.claude/config/ana-solution-schema.yaml"
    )
```

### 改造方向（建議）

```python
def _format_invalid_value_warning(
    ticket_id: str, field_key: str, value: str, allowed: List[str]
) -> str:
    base = (
        f"[WARNING] Acceptance Gate: ANA Ticket {field_key} 值非法\n"
        f"\n"
        f"Ticket: {ticket_id}\n"
        f"目前值: {value}\n"
        f"合法值: {', '.join(allowed)}\n"
        f"\n"
        f"Schema 來源：.claude/config/ana-solution-schema.yaml"
    )
    # nested YAML 結構誤用偵測（PC-117 / W17-111）
    if ":" in value:
        base += (
            f"\n\n[偵測到值含冒號]\n"
            f"可能誤用 nested YAML 結構（例：{field_key}: 換行接 status: skipped）。\n"
            f"schema 要求 flat 格式，正確寫法：\n"
            f"  {field_key}: skipped\n"
            f"  reason: <跳過理由>"
        )
    return base
```

### 待新增測試

於 `.claude/hooks/tests/test_multi_view_checker.py` 加：

```python
def test_invalid_value_with_colon_includes_nested_hint(project_dir, logger):
    """value 含冒號時警告訊息應附 nested 結構誤用提示（PC-117 / W17-111）"""
    content = """---
type: ANA
id: TEST-1
---

## Solution

multi_view_status:
  status: skipped
  reason: test
"""
    frontmatter = {"type": "ANA", "id": "TEST-1"}
    should_warn, msg = check_multi_view_status(content, frontmatter, project_dir, logger)
    assert should_warn is True
    assert "偵測到值含冒號" in msg
    assert "nested YAML" in msg
    assert "multi_view_status: skipped" in msg
```

### 修改摘要

於 `_format_invalid_value_warning`（multi_view_checker.py 行 207-230）加 `if ":" in value:` 分支：

- 當 invalid value 含冒號時，附加「[偵測到值含冒號]」段落
- 提示可能誤用 nested YAML 結構（PC-117 / W17-111 案例）
- 給出 flat 格式正確寫法範例

### 設計決策

- **不改判斷邏輯**：僅在訊息層附加提示，主流程不動，避免越界 W17-111.2 schema 範圍
- **觸發條件選 value 含冒號**：`_parse_field` 將同行 nested 形式（如 `multi_view_status: status: skipped`）解析為含冒號 value，正好觸發此分支
- **訊息附加而非取代**：base 訊息不變，新提示作為補充段落，相容既有測試

### 不在本 ticket 範圍

- schema yaml 修改（W17-111.2 並行）
- 多行 nested YAML 偵測（regex 限制需單行有值，多行 nested 會被識別為 missing 而非 invalid）
### 邊界（嚴守）

- 允許：`.claude/hooks/acceptance_checkers/multi_view_checker.py` 改 `_format_invalid_value_warning`；`.claude/hooks/tests/test_multi_view_checker.py` 新增測試
- 禁止：改判斷邏輯（`_parse_field` / `check_multi_view_status` 主流程）；改 schema yaml（W17-111.2 範圍）
- 禁止：git add . / git add -A
- 測試指令：`python3 -m pytest .claude/hooks/tests/test_multi_view_checker.py -v`

---

<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 執行指令

```
python3 -m pytest .claude/hooks/tests/test_multi_view_checker.py -v
```

### 結果

12 passed in 0.06s（含新增 `test_invalid_value_with_colon_includes_nested_hint` + 既有 11 測試全綠）

### 新測試覆蓋

- `test_invalid_value_with_colon_includes_nested_hint`：模擬 `multi_view_status: status: skipped` 同行 nested 誤用，驗證警告含「偵測到值含冒號」「nested YAML」「multi_view_status: skipped」三項關鍵提示
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-02T22:31:46
**Executing Agent**: thyme
**Review Status**: pending