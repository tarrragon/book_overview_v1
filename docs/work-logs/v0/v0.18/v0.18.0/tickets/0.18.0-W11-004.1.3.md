---
id: 0.18.0-W11-004.1.3
title: 實作 agent-dispatch-validation 白名單機制避免合法引用誤觸
type: IMP
status: in_progress
version: 0.18.0
wave: 11
priority: P2
parent_id: 0.18.0-W11-004.1
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-python-developer
  history: {}
what: 識別合法情境（PM 撰寫規則文件引用禁止詞彙、prompt 含引號內的文件節錄、prompt 明示「禁止 X」示範）並排除誤觸；建立白名單規則或 context-aware
  偵測
when: 可與 A/C 並行
where:
  layer: Infrastructure
  files:
  - .claude/hooks/agent-dispatch-validation-hook.py
why: warn 模式下若升級為 block，誤觸將直接阻擋派發造成摩擦；需事先建立白名單避免合法情境被擋
how:
  task_type: Implementation
  strategy: 步驟 1：枚舉合法引用情境 → 步驟 2：設計白名單規則（引號內/特定關鍵字前置/目標路徑為 .claude/docs/）→ 步驟 3：實作
    context-aware 偵測 → 步驟 4：測試涵蓋合法與非法案例
acceptance:
- '[x] 至少識別 3 類合法情境'
- '[ ] 白名單規則可擴充'
- '[ ] 合法案例不觸發警告'
- '[ ] 非法案例仍觸發'
- '[ ] 單元測試涵蓋合法/非法邊界'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-19T11:59:31'
completed_at: null
created: '2026-04-19'
updated: '2026-04-19'
---

# Execution Log

## Task Summary

識別合法情境（PM 撰寫規則文件引用禁止詞彙、prompt 含引號內的文件節錄、prompt 明示「禁止 X」示範）並排除誤觸；建立白名單規則或 context-aware 偵測

---

## Problem Analysis

### 問題根因

`_detect_keyword_conflicts` 以純正規式子字串匹配掃描 prompt，無 context-aware 判斷。在以下情境中，prompt 雖含禁止詞彙但實為合法引用，會觸發 stderr 警告並寫入 events.jsonl，污染誤報率統計（W11-004.1.1 annotate 工作量增加）。

### 影響範圍

- `.claude/hooks/agent-dispatch-validation-hook.py::_detect_keyword_conflicts`（匹配邏輯）
- `.claude/hook-logs/agent-dispatch-validation/events/events.jsonl`（誤報事件寫入）
- 下游 W11-004.1.4（分層判決）：若誤報率高，警告升級為 block 會誤擋合法派發

### 相關 Error Pattern

- W5-001 派發 sage 越界（原始案例：Hook 設計動機）
- W5-045 禁止行為關鍵字衝突掃描（本 Hook 前身實作）
- 本子任務目標：在 W5-045 基礎上加白名單過濾，避免升級 block 後阻擋合法派發

### 合法情境枚舉（至少 3 類）

**情境 A：引號內引用（Quoted Reference）**

PM prompt 用引號包住禁止詞彙以引述 agent definition 或規則文件說明，無實際執行意圖。

- 範例 1：`請遵守 thyme-python-developer 定義中的『禁止實作程式碼』邊界`
- 範例 2：`sage-test-architect 的「禁止 git commit」章節說明…`
- 範例 3：`參照 agent-definition-standard.md 的 "**禁止執行測試**" 格式`
- 範例 4：`說明：本 agent 不可『修改規格』，詳見 .md 第 X 節`

**情境 B：否定語意（Negation Context）**

PM prompt 明確用否定詞框架要 agent **不**做該動作，屬主動宣告邊界而非要求越界。

- 範例 1：`請勿實作程式碼，僅產出設計規格`
- 範例 2：`不要執行 git commit，PM 會自行 commit`
- 範例 3：`禁止修改 src/ 下檔案，僅讀取分析`
- 範例 4：`avoid refactor in this task, focus on analysis only`

**情境 C：路徑/檔名引用（Path / Filename Reference）**

禁止詞彙出現在檔案路徑、ticket ID、error-pattern ID 中，屬文件引用非執行指令。

- 範例 1：`參考 .claude/error-patterns/process-compliance/PC-057-unauthorized-implementation.md`
- 範例 2：`閱讀 docs/spec/implementation-notes.md` 中的 *修改規格* pattern 會誤觸
- 範例 3：`修改 docs/work-logs/v0.18/.../IMP-052-implementation.md` 檔名匹配「實作」
- 範例 4：`ticket track query 0.18.0-W11-004.1.2`（匹配 `ticket CLI` keyword）

**情境 D（延伸，可選）：Meta-task 規則文件修改**

PM 派發任務是「修改 .claude/rules/」「編輯規則文件」，規則內容本身會引用禁止詞彙。

- 範例 1：`修改 .claude/rules/core/ai-communication-rules.md 新增『禁止硬編碼』章節`
- 範例 2：`在 FORBIDDEN_KEYWORD_MAP 加入「執行測試」pattern（本 ticket 的自描述）`
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

### 設計策略

在 `_detect_keyword_conflicts` 產出 conflicts 後、寫入 events.jsonl 前，加入白名單過濾層 `_filter_whitelisted_conflicts(prompt, conflicts) -> conflicts`。白名單規則以可擴充清單組織，每條規則為純函式 `Rule(prompt, match_start, match_end) -> (is_whitelisted: bool, reason: str)`。

### 白名單規則清單（初版）

**規則 A：引號包圍偵測 `_is_quoted_match`**

匹配片段外層被以下任一引號包圍視為引用：

- 中文單/雙引號：`『』`、`「」`
- 英文引號：`"..."`、`'...'`、反引號 `` `...` ``
- Markdown 粗體：`**...**`（僅當內容為禁止詞彙標籤時）

偵測：從 match_start 向前找最近的開引號字元，從 match_end 向後找對應閉引號字元，中間不能跨行。

**規則 B：否定前綴偵測 `_has_negation_prefix`**

匹配位置前 10 字元內（同句內）含否定詞視為主動宣告邊界：

- 中文否定詞：`不要` `請勿` `禁止` `避免` `不得` `不可` `切勿` `勿`
- 英文否定詞（case-insensitive）：`don't` `do not` `avoid` `forbid` `must not` `never`

偵測：`prompt[max(0, start-10):start]` 正規式匹配否定詞且不跨越句號/分號。

**規則 C：路徑/檔名上下文偵測 `_is_in_path_context`**

匹配位置落在檔案路徑內視為文件引用：

- 向前 30 字元內含 `.claude/` / `docs/` / `src/` / `tests/` 等路徑前綴
- 向後 15 字元內含 `.md` / `.py` / `.json` / `.yaml` 等副檔名
- 或匹配整體落在 ticket ID 模式內（`\d+\.\d+\.\d+-W\d+-\d+`）

**規則 D（延伸）：Meta-task 全域放寬 `_is_meta_task_prompt`**

若 prompt 前 100 字元含 `修改 .claude/rules/` / `編輯規則文件` / `更新 FORBIDDEN_KEYWORD_MAP` 等 meta-operation 宣告 → 整個 prompt 降級為 debug log，不寫 events.jsonl。

### 實作步驟（交棒 thyme）

1. **RED 測試**（Phase 2）：在 `test_agent_dispatch_validation_hook.py` 新增 `TestWhitelistFilter` 類別
   - 每規則 3 個正例（應白名單化）+ 3 個負例（不應白名單化）= 24 測試
   - 邊界案例：引號一半、否定詞超過 10 字距、路徑不完整、meta-task 偽裝

2. **GREEN 實作**（Phase 3b）：
   - 新增 `WHITELIST_RULES: List[Callable]` 清單常數
   - 實作 4 個規則純函式（A/B/C/D）
   - 新增 `_filter_whitelisted_conflicts(prompt, conflicts)` 包裝在 `_emit_keyword_conflict_warning_if_any` 內
   - 被白名單化的 conflicts 改寫入 logger.debug（不進 events.jsonl）

3. **整合測試**：回溯 `events.jsonl` 現有 2 筆事件，驗證皆非合法情境（sage 真越界 + thyme git commit 真越界）

### 驗收對應

- `[x] 至少識別 3 類合法情境` → 已枚舉 A/B/C（D 為延伸）
- `[ ] 白名單規則可擴充` → `WHITELIST_RULES` List 設計
- `[ ] 合法案例不觸發警告` → 規則 A/B/C 各 3 正例測試
- `[ ] 非法案例仍觸發` → 規則 A/B/C 各 3 負例測試
- `[ ] 單元測試涵蓋合法/非法邊界` → 24 測試 + 邊界測試

### 風險與局限

- 規則 A 引號偵測若遇未閉合引號（`『漏掉閉合`）需防回圈，限定掃描視窗 50 字元
- 規則 B 否定詞距離 10 字可能漏掉「在本 prompt 中，我們絕對不會 XXX 實作」（否定詞距離過遠）→ 接受漏報，由 W11-004.1.1 annotate 回饋調整
- 規則 C 路徑模式過寬可能漏掉非標準路徑 → 以現有真實案例為準，不過度預設
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending