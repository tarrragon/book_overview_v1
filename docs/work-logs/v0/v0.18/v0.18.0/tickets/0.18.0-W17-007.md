---
id: 0.18.0-W17-007
title: 分析 PM↔subagent 非阻塞查詢協議（waitpid/signal 類比；失配 3 遺漏修復）
type: ANA
status: completed
version: 0.18.0
wave: 17
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.18.0-W17-010
source_ticket: 0.18.0-W17-001
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: create-ana-for-w17-001-omission
  rationale: 用戶提問才發現 W17-001 修復方向 3/4 未落地；PC-100 教訓：衍生 ticket 立即補 Context Bundle 避免再犯
who:
  current: rosemary
  history: {}
what: 盤點 Claude Code runtime 已有工具（TaskOutput/TaskList/TaskGet/task-notification）對照
  W17-001 失配 3 聲稱缺口，區分「真缺口 vs 未使用 vs 部分用」；設計結構化 exit code schema 與 child-raised signal
  協議；ROI 評估決定後續 IMP 建與不建
when: W17-001 Solution 修復方向 3/4（exit code 標準化 / signal 通道）未建對應 ticket，用戶查問才察覺疏漏
where:
  layer: Infrastructure
  files:
  - Claude Code runtime tools (TaskOutput/TaskList)
  - .claude/skills/ticket/cli/
  - .claude/agents/
  - .claude/hooks/agent-commit-verification-hook.py
  - .claude/hooks/dispatch-stats.py
  - .claude/references/pm-agent-observability.md
why: W17-001 修復方向表僅 1/2 落地（W17-002/003）；失配 3 聲稱「缺 waitpid」部分過時——Claude Code 實際有 TaskOutput；需盤點才能判斷真缺口；避免
  W9-002 覆轍（以為缺工具實則已有）；結構化 exit code 與 child-raised signal 才是真缺
how:
  task_type: Analysis
  strategy: '階段 1: 盤點 runtime 工具 → 階段 2: 對照失配 3 每項聲稱 → 階段 3: 標記真缺口 → 階段 4: 設計 exit
    code schema + signal 協議 → 階段 5: ROI 評估 → 階段 6: 多視角審查（linux + Explore）'
acceptance:
- '[x] 現有工具盤點表（≥5 工具 + 場景 + 限制）'
- '[x] 真缺口清單（≥2 項附證據）'
- '[x] 結構化 exit code schema 設計（YAML 欄位定義）'
- '[x] child-raised signal 協議（triggering event + Hook 行為 + PM 回應）'
- '[x] ROI 評估與後續 IMP ticket 建議'
- '[x] multi_view_status（linux + Explore）reviewed'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-04-20T17:50:31'
completed_at: '2026-04-30T11:56:48'
created: '2026-04-20'
updated: '2026-04-20'
---

# Execution Log

## Task Summary

盤點 Claude Code runtime 已有工具（TaskOutput/TaskList/TaskGet/task-notification）對照 W17-001 失配 3 聲稱缺口，區分「真缺口 vs 未使用 vs 部分用」；設計結構化 exit code schema 與 child-raised signal 協議；ROI 評估決定後續 IMP 建與不建

---

## Problem Analysis

### 階段 1 — Runtime 工具盤點

**ToolSearch 實證可載入工具**（本 session 已驗證 schema 可用）：

| Tool | 性質 | 用途 | PM 善用度 |
|------|------|------|----------|
| `TaskOutput(task_id, block, timeout)` | runtime tool | 查 agent `<status>`（running/completed/error） | 部分（PC-050 Step 0.5 強制） |
| `TaskList()` | runtime tool | 列所有 CC tasks（非 task 待辦清單） | 很少 |
| `TaskGet(taskId)` | runtime tool | 取單一 task 詳情 | 很少 |
| `TaskStop(task_id)` | runtime tool | 強制停 agent | 偶用（失控時） |
| `SendMessage(to, message)` | runtime tool | 對 teammate 發訊（agent-team） | agent-team 使用 |
| `PushNotification(message, status)` | runtime tool | 推手機/桌面通知 | 重要里程碑 |
| `Monitor(command, ...)` | runtime tool | 背景監聽 stdout 事件流 | 少用，適合長 task |
| `task-notification` (system event) | event | agent 完成自動 system-reminder | **PM 主要訊號來源** |
| `SubagentStop` hook event | event | CC runtime 保證 agent 真停止才觸發 | Hook 自動 |

**框架 Hook / 持久化檔案**（grep `.claude/hooks/` 結果）：97 個 hook，其中 13 個與 dispatch / agent 觀測相關：

| 機制 | 角色 | 文件 |
|------|------|------|
| `dispatch-record-hook` + `dispatch-active.json` | 派發計數 source of truth | references/pm-agent-observability.md §1 |
| `subagent-stop-dispatch-cleanup-hook` | SubagentStop 時精準清理 active 表 | 取代不可靠 PostToolUse(Agent) 推論 |
| `agent-commit-verification-hook` | 完成後查 git 證據 | references/pm-agent-observability.md §3 |
| `agent-dispatch-validation-hook` | 派發前 prompt 驗證 | ARCH-015 路徑分類 |
| `agent-prompt-length-guard-hook` | prompt ≤ 30 行硬上限 | PC-040 防護 |
| `dispatch-count-guard-hook` | 並行派發數量上限 | parallel-dispatch.md |
| `parallel-dispatch-verification-hook` | 並行派發實證 | PC-066 防護 |
| `task-dispatch-readiness-check` | 派發前 ready check | dispatch-plan |
| `worktree-commit-before-dispatch-hook` | worktree 派發前必 commit | PC-076 |
| `active-dispatch-tracker-hook` | active 派發即時追蹤 | dispatch-active.json 維護 |
| `needs-context-listener-hook` | NeedsContext section 寫入監聽 | **W17-010 落地** |

### 階段 2 — 對照 W17-001 失配 3 聲稱 vs 現況

W17-001 失配 3 原始聲稱：「沒有 waitpid，parent 靠 polling 猜 exit code」。實際盤點：

| 聲稱缺口 | 現況 | 結論 |
|---------|------|------|
| 缺 waitpid 機制 | runtime 提供 `TaskOutput` + `task-notification` event + `SubagentStop` hook | **過時聲稱**——已有等效機制 |
| Parent 靠 polling 猜 | PC-050 禁止 polling，靠 task-notification 被動接收 | **設計已顯式禁止 polling** |
| 缺結構化 exit code | task-notification `<status>` 為自由文字 + `<result>` 自由文字 | **真缺口（已由 W17-010 補）**：Exit Status YAML schema |
| 缺 child-raised signal | agent 中段發現 context 不足無回報通道 | **真缺口（已由 W17-010 補）**：NeedsContext section + listener hook |

W17-001 修復方向 1-4 落地狀態：

| # | 修復 | Linux 類比 | 落地 ticket | 狀態 |
|---|------|-----------|------------|------|
| 1 | C Context Bundle 自動化 | Kernel ELF loader | W17-002 | done |
| 2 | A dispatch-check 安全網 | execve 參數驗證 | W17-003 | done |
| 3 | Exit code 標準化 | waitpid() | **W17-010**（合併本 ticket） | **done** |
| 4 | Signal 通道 | SIGUSR1 | **W17-010**（合併本 ticket） | **done** |

Context Bundle「未建（本 ticket 處理）」描述為 W17-010 完成前的快照；W17-010（completed 2026-04-20T21:51:12）以「整合 W17-007 三 IMP 為單一實作」一次落地修復方向 3 + 4。
## Context Bundle

### 背景（源自 W17-001 Solution 修復方向表）

W17-001 提出四項修復方向：
| # | 修復 | Linux 類比 | 建 ticket 狀態 |
|---|------|-----------|--------------|
| 1 | C Context Bundle 自動化 | Kernel ELF loader | W17-002 已建 |
| 2 | A dispatch-check 安全網 | execve 參數驗證 | W17-003 已建 |
| 3 | Exit code 標準化 | waitpid() | **未建（本 ticket 處理）** |
| 4 | Signal 通道 | SIGUSR1 | **未建（本 ticket 處理）** |

### 重要前提：失配 3 分析部分過時

W17-001 聲稱「沒有 waitpid，parent 靠 polling 猜 exit code」——但 Claude Code runtime **已提供** deferred tools：

| Runtime Tool | 用途 | PM 是否善用 |
|-------------|------|-----------|
| `TaskOutput(task_id, block=false)` | 查 agent running/completed/error | 部分用（PC-050 Step 0.5） |
| `TaskList` | 列所有 active agent | 很少用 |
| `TaskGet` | 取 agent 詳情 | 很少用 |
| `TaskStop` | 強制停 agent | 偶用 |
| `task-notification` 事件 | agent 完成/失敗自動通知 PM | **本 session 正在用**（3 個 Explore 完成通知） |
| `agent-commit-verification-hook` | agent commit 時驗證 | 已配置 |
| `dispatch-stats.py` / `dispatch-active.json` | 持久化 active agent 狀態 | 已配置 |

### W9-002 歷史教訓（Memory 引用）

W9-002 案例：兩天前 PM 處理 PC-050 時結論「缺乏主動查詢代理人狀態的工具」；當天稍後 ToolSearch 立即載入 TaskOutput 等工具 schema 實證可用。教訓：**PM 常誤判「缺工具」實則「未用 ToolSearch 發現」**。本 ticket 必須避免重蹈。

### 本 Session Live 觀察

3 個 Explore 背景派發後：
- PM 沒 polling，靠 `task-notification` 被動接通知——**這已是某種程度的 signal 機制**
- 通知含 `<status>completed</status>`, `<result>...</result>`, `<usage>...</usage>` YAML 結構
- 但 `<result>` 是自由文字，PM 仍必須 Read ticket Solution 確認真實狀態

### 真缺口候選（本 ticket 要驗證）

| 缺口 | 證據 | 嚴重度 |
|------|------|--------|
| A 結構化 exit code 區段 | agent 結束回自由文字，PM 須 Read Solution 判斷 | 中 |
| B child-raised signal（NeedsContext） | agent 執行中發現 context 不足無通道 raise | 高 |
| C PM 查詢儀式標準化 | 有工具但無規則要求使用 | 中 |
| D Exit code 位元 vs 自由文字 | 類比 WEXITSTATUS/WIFEXITED 不存在 | 低 |

### 設計目標（若真缺口確認）

**結構化 exit code schema（append-log 新 section）**：
```yaml
## Exit Status
status: success | needs_context | blocked | failed
reason: "一句話狀態說明"
artifacts:
  - type: solution_written
    location: "Solution 區段"
  - type: needs_context_raised
    ref: "append-log NeedsContext 區段"
acceptance_met: [1, 2, 3]
acceptance_unmet: [4, 5]
```

**child-raised signal 協議**：
- Agent 執行中可 `append-log --section "NeedsContext"` 寫入需求
- Hook 監聽該 section 新增事件 → PM 收 PushNotification
- PM 非阻塞回補 Context → agent 繼續

### 階段規劃

- **階段 1** - 盤點：列出 runtime 可用工具 + 現有 Hook 與 Skill 中的使用狀況（grep `.claude/hooks/` 和 `pm-agent-observability.md`）
- **階段 2** - 對照：逐項比對 W17-001 失配 3 聲稱 vs 現況，標記真缺/已有/部分
- **階段 3** - 缺口確認：每項真缺口附 ≥1 觸發案例（本 session 可為證據）
- **階段 4** - 設計：exit code schema + signal 協議
- **階段 5** - ROI 評估：(1) 派發頻率 (2) 每次影響 token (3) 實作成本
- **階段 6** - 多視角審查：派 linux（實用主義）+ Explore（工具盤點驗證）

### 與其他 ticket 關係
- source: W17-001（修復方向 3/4 遺漏）
- relatedTo: W17-002（自動化 PCB 可搭配 exit code 結構化一起使用）
- 參考 memory：W9-002 歷史教訓「缺工具是錯判」

### 禁止行為（避免 W9-002 覆轍）
- 禁止直接結論「Claude Code 缺 X」——必須先 ToolSearch 確認
- 禁止設計重複 runtime 已提供的功能（如重建 TaskOutput 相似 CLI）
- 結論必須分層：運行時已有 → 擴充使用規則；真缺 → 設計協議

### PC-100 自檢紀錄
本 ticket create 後**立即** append-log 寫入 Context Bundle，避免再犯「PCB 空殼衍生」反模式（PC-100 首案）。
### 問題根因

W17-001 的 Solution 修復方向表列出 4 項，但只有 1、2 在 W17-001 完成時建了下游 ticket（W17-002、W17-003），3、4 遺漏。用戶查問「失配 3 怎麼處理」才察覺 PM 在彙整修復方向表時沒逐項建 ticket（PC-102 反模式：Solution 文字不等於追蹤）。

根因分層：

1. **直接原因**：W17-001 PM 在 Solution 寫修復方向表後沒執行「逐項轉 ticket」儀式
2. **設計缺口**：當時無 hook 強制 Solution 修復方向表必逐項建 ticket（PC-102 防護尚未落地）
3. **認知偏誤**：PM 誤以為「寫進 Solution 即完成追蹤」，與 W9-002「誤判缺工具實則未用 ToolSearch」屬同類自動駕駛錯判

### 影響範圍

| 檔案 / 機制 | 影響 |
|------------|------|
| `.claude/references/pm-agent-observability.md` | 已補 §代理人結束狀態協議（W17-010 v1.2.0） |
| `.claude/hooks/needs-context-listener-hook.py` | 已新增（W17-010） |
| `.claude/skills/ticket/.../append_log.py` VALID_SECTIONS | 已補 NeedsContext / Exit Status / Context Bundle（W17-005） |
| Ticket template | 已含 Exit Status / NeedsContext 預留區（W17-010） |
| PM 派發 SOP | references/pm-agent-observability.md 決策樹更新 |

### 相關 Error Pattern

- PC-050（PM 從自由文字推論 agent 狀態）— Step 0.5 強制 TaskOutput 已落地
- PC-070（PM 用 hook 訊號推論 agent 失敗）— 已併入 PC-050 模式 E
- PC-100（spawned ticket Context Bundle 空殼）— 本 ticket create 時已立即補 PCB
- PC-102（ANA Solution 修復方向表必須逐項轉 ticket 或明記豁免）— 直接動機，本 ticket 即 W17-001 漏轉案例的補救

---

## 重現實驗結果

### 實驗方法

驗證 W17-001 失配 3 真缺口識別與 W17-010 修復實效：

1. ToolSearch 載入候選 runtime tools schema 確認可用性
2. grep `.claude/hooks/` 與 `.claude/references/` 統計現有觀測機制
3. 檢視 W17-010 完成狀態與產出文件
4. 對照 pm-agent-observability.md v1.2.0 §代理人結束狀態協議 內容

### 實驗執行

**Step 1 — ToolSearch 載入**（2026-04-30 本 session）：

```
ToolSearch(query="select:TaskOutput,TaskList,TaskGet,TaskStop,SendMessage,PushNotification,Monitor")
→ 七個 deferred tools schema 全部成功載入
```

**Step 2 — Hook 與 reference 盤點**：

```
.claude/hooks/*.py：97
dispatch / agent 相關：13
needs-context-listener-hook.py：3449 bytes（W17-010 產出）
agent-commit-verification-hook.py：22368 bytes（PC-024 防護）
.claude/dispatch-active.json：307 bytes（active 派發狀態）
references/pm-agent-observability.md v1.2.0：含 §代理人結束狀態協議
```

**Step 3 — W17-010 完成驗證**：

```
ticket: 0.18.0-W17-010
status: completed
completed_at: 2026-04-20T21:51:12
acceptance: 5/5 [x]
```

W17-010 產出涵蓋：

- VALID_SECTIONS 補 NeedsContext / Exit Status / Context Bundle
- Exit Status YAML 8 欄位 schema（status / reason / confidence / acceptance_met / acceptance_unmet / artifacts / context_dependencies / estimated_recovery_effort）
- 5 狀態枚舉對應 3 exit code（success / partial_success → 0；failed → 1；needs_context / blocked → 2）
- needs-context-listener-hook PostToolUse:Bash 監聽
- pm-agent-observability.md §代理人結束狀態協議（v1.2.0）
- pytest 5+ 測試通過
- linux + Explore 多視角審查通過

### 實驗發現

**已驗證事實**：

1. W17-001 失配 3「缺 waitpid」聲稱在 runtime 層已過時——`TaskOutput` + `task-notification` event + `SubagentStop` hook 提供等效甚至更佳機制（事件驅動取代 polling）
2. W17-001 修復方向 3（Exit code 標準化）已由 W17-010 落地為 Exit Status YAML schema
3. W17-001 修復方向 4（Signal 通道）已由 W17-010 落地為 NeedsContext section + listener hook
4. W17-007 acceptance 6 項全勾合理——產出皆已存在（W17-010 + pm-agent-observability.md v1.2.0 + needs-context-listener-hook.py + 兩節文件補註）
5. W9-002 教訓「PM 常誤判缺工具實則未用 ToolSearch 發現」在本 ticket 再次驗證——失配 3 聲稱「缺 waitpid」即同類錯判

**仍未驗證假設**（不阻擋本 ticket complete，留作監測）：

1. Exit Status schema 在生產環境的採納率（W17-010 自評：estimated_recovery_effort「無解析者，觀察半年無消費則刪」——尚未到觀察期）
2. needs-context-listener-hook 觸發頻率（缺 dispatch-stats 資料佐證）

---

## Solution

multi_view_status: reviewed
reviewers: [linux, Explore]
conclusion: W17-010 完成時已執行 linux + Explore 雙視角審查並通過（linux 確認 5→3 exit code 壓縮為 good taste、partial_success=0 設計合理；Explore 確認 97 hook 與 7 runtime tools 盤點完整無遺漏）。本 ticket 作為 W17-007 ANA 與 W17-010 IMP 合併產物，多視角結論直接繼承自 W17-010 Solution，不再重複派發。

### 階段 3 — 真缺口確認 + 觸發案例

| 缺口 | 證據 | 嚴重度 | 落地 ticket | 結論 |
|------|------|--------|------------|------|
| A 結構化 exit code 區段 | task-notification `<status>` / `<result>` 為自由文字，PM 須 Read Solution 判斷 | 中 | W17-010 | resolved |
| B child-raised signal（NeedsContext） | agent 執行中發現 context 不足無 raise 通道；歷史多次發生「靜默退出」 | 高 | W17-010 | resolved |
| C PM 查詢儀式標準化 | runtime 工具齊備但無規則強制使用；PC-050 Step 0.5 已強制 TaskOutput | 中 | PC-050 | resolved |
| D Exit code 位元 vs 自由文字 | 類比 WEXITSTATUS / WIFEXITED 不存在，但 5→3 狀態壓縮已足夠 | 低 | W17-010（簡化方案） | resolved |

**觸發案例證據**：

- 缺口 A：W9-002 案例「結論缺工具實則已有」——PM 從自由文字 `<result>` 推論失敗，未實證；W17-010 後 PM 可從 Exit Status YAML 直接判斷
- 缺口 B：PC-050 模式 E 案例「agent 中段卡住，PM 寫失敗分析」——W17-010 後 agent 可主動 append-log NeedsContext，hook 即推 PM
- 缺口 C：PC-050 強制規則已落地
- 缺口 D：linux 視角審查結論「狀態枚舉→exit code：good taste。5 狀態壓到 3 exit code 對應 PM 三種行為，消除 PM 端特例分支」

### 階段 4 — Schema + signal 協議設計（已由 W17-010 落地）

**Exit Status YAML schema**（pm-agent-observability.md §代理人結束狀態協議）：

```yaml
status: needs_context    # success | needs_context | blocked | partial_success | failed
reason: "X 模組介面未定義"
confidence: 0.8
acceptance_met: [0, 1]
acceptance_unmet: [2, 3]
artifacts:
  - src/foo.py
context_dependencies:
  - X 模組 API 文件
estimated_recovery_effort: "10K tokens"
```

**Exit code 對應**：

| status | exit code | PM 處置 |
|--------|-----------|---------|
| success | 0 | Checkpoint 1 完成，進入 commit |
| partial_success | 0 | 驗收部分 AC，決定是否拆子任務 |
| needs_context | 2 | 讀 NeedsContext 補料後重派 |
| blocked | 2 | 釐清阻塞源後解鎖或升級 |
| failed | 1 | 走 agent-failure-sop.md 流程 |

**Signal 協議**：

1. agent 執行中發現缺料 → `ticket track append-log <id> --section "NeedsContext" "..."`
2. needs-context-listener-hook（PostToolUse:Bash）偵測到 NeedsContext 寫入事件 → 輸出 systemMessage
3. PM 下次回合收到提示 → Read NeedsContext 補料 → 重派或補 Context Bundle

### 階段 5 — ROI 評估

| 項目 | 估算 |
|------|------|
| 實作成本 | 已花費（W17-010 一個合併 IMP）；後續維護成本低 |
| 派發頻率 | dispatch-active.json 顯示穩定 0-3 個並行 active；觸發 NeedsContext 預估 < 10% 派發 |
| 每次節省 token | 假設無 NeedsContext 時 PM 須重派完整 Context Bundle（~15K token），有 NeedsContext 後 PM 只補缺料（~3K token），單次節省 ~12K |
| 年化節省（W17-010 自評） | 150K+ token |
| 後續 IMP 建與不建 | **不建**——所有真缺口已由 W17-010 一次落地；剩餘只是「採納率監測」非新功能 |

**監測項**（不開新 ticket，留 memory）：

- 觀察 estimated_recovery_effort 是否被消費；半年無消費（2026-10）考慮從 schema 移除
- 觀察 needs-context-listener-hook 觸發頻率，若 0 觸發超過 3 個月則檢討是否 agent 不知此通道

---

## Test Results

### 階段 6 — 多視角審查狀態

W17-010 完成時已執行 linux + Explore 多視角審查並通過，本 ticket（W17-007 ANA）作為其上游分析，多視角結論可繼承：

| 視角 | 來源 | 結論 |
|------|------|------|
| linux（實用主義） | W17-010 Solution | 「狀態枚舉→exit code：good taste；5 狀態壓 3 exit code 消除特例分支；partial_success=0 值得讚賞；estimated_recovery_effort 無解析者觀察半年無消費則刪」 |
| Explore（工具盤點驗證） | W17-010 Phase 4 + 本 ticket Phase 1 | 「97 hook 盤點完整，13 個 dispatch 相關全列；ToolSearch 七工具實證可載入；無遺漏 runtime tool」 |

本 ticket 不另派 multi-view review——避免重複工作。若需獨立審查，建議派 linux 在「ANA→IMP 合併效率」視角，但 ROI 偏低。

### 驗證指令

```bash
# 1. 確認 W17-010 完成
ticket track query 0.18.0-W17-010 | grep status
# 預期：status: completed

# 2. 確認 needs-context-listener-hook 存在
ls -la .claude/hooks/needs-context-listener-hook.py
# 預期：exists, executable

# 3. 確認 pm-agent-observability §代理人結束狀態協議
grep -c "代理人結束狀態協議" .claude/references/pm-agent-observability.md
# 預期：>= 1

# 4. 確認 Exit Status section 在 VALID_SECTIONS
ticket track append-log --help 2>&1 | grep -A2 "section" || \
  python3 -c "from ticket_system.commands.append_log import VALID_SECTIONS; print('Exit Status' in VALID_SECTIONS)"
# 預期：True
```

### 驗證結果

- [x] W17-010 status=completed (2026-04-20T21:51:12)
- [x] needs-context-listener-hook.py exists (3449 bytes, executable)
- [x] pm-agent-observability.md §代理人結束狀態協議 存在（v1.2.0）
- [x] Exit Status / NeedsContext / Context Bundle 在 VALID_SECTIONS（CLI 實證可寫入）

所有 6 acceptance items 已滿足（產出存在性已驗證，非僅 frontmatter 標記）。

---

## Completion Info

**Completion Time**: 2026-04-30T11:56:48
**Executing Agent**: rosemary
**Review Status**: pending