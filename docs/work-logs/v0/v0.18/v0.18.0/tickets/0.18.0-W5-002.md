---
id: 0.18.0-W5-002
title: 實作 ticket track claim AC 自動驗證流程
type: IMP
status: in_progress
version: 0.18.0
wave: 5
priority: P1
parent_id: null
children: []
blockedBy:
- 0.18.0-W5-001
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: Phase 1 MVP 核心功能
  rationale: PROP-010 方案 2 — claim 前 AC 驗證
who: lavender-interface-designer
what: claim 命令前端整合 AC 解析器自動驗證 AC 已達成狀態
when: W5-001 完成後
where: CLI 工具層 (ticket_system Python 模組)
why: PROP-010 方案 2：直接解決「派發代理人做已完成工作」的核心問題
how:
  task_type: Implementation
  strategy: 在 claim 前呼叫 ac_parser 解析 AC，匹配 validation_templates 執行驗證，回報「已達成/未達成/無法自動驗證」並提供
    y/n/c 三選一決策（繼續 claim / 取消 / 轉為 complete 記錄外溢達成）
acceptance:
- '[ ] claim 前自動執行可機器驗證的 AC'
- '[ ] 輸出結果含每個 AC 的判定（已達成/未達成/跳過）'
- '[ ] 提供 y/n/c 三選一（繼續 claim / 取消 / 轉 complete 記錄外溢）'
- '[ ] 新增整合測試 模擬 AC 已達成情境'
- '[ ] 不影響既有無 AC 或無可驗證 AC 的 claim 流程'
- '[ ] 輸出控制在 15 行內（符合認知負擔）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-13T21:18:21'
completed_at: null
created: '2026-04-12'
updated: '2026-04-12'
---

# Execution Log

## Task Summary

claim 命令前端整合 AC 解析器自動驗證 AC 已達成狀態

---

## Problem Analysis

### Phase 1 PM 決策（回應 §B 4 個規格缺口）

| # | 缺口 | PM 決定 | 理由 |
|---|------|---------|------|
| 1 | coverage 門檻值 | **從 AC 文字 regex `(\d+)%` 擷取；若無則預設 80%** | 避免 hardcode，優先由 AC 語義推導；80% 是業界常見預設，符合本專案歷史慣例 |
| 2 | c 選項二次確認 | **MVP 加二次確認** prompt: `確定轉為 complete 並記錄 AC 外溢達成? [y/N]`（預設 N） | 轉 complete 是跨語義的 destructive 操作，符合 PM-role「risky actions warrant confirmation」原則 |
| 3 | `--yes` 與 `--skip-verify` 衝突 | **`--skip-verify` 優先**；兩者同指定時：跳過驗證 + 進既有 claim 流程（`--yes` 在無驗證時無意義，靜默忽略） | 語義上「跳過」>「自動 yes」；`--yes` 是驗證階段的 non-interactive flag |
| 4 | 非 tty 行為 | **保持 `return 1`**（非 tty 且無 `--yes` 與 `--skip-verify` → stderr 警告後退出） | Fail-closed 安全預設；CI 環境應顯式傳 `--skip-verify` 或 `--yes` 聲明意圖，避免未達標 claim 誤放行 |

**對 Phase 2 / 3b 的約束**：上述決策為規格一部分，測試案例需覆蓋所有決策點（特別是 #2 二次確認的 y/N 兩條路徑、#3 flag 衝突場景、#4 非 tty 情境）。
### Phase 1 功能規格（lavender）

本規格建立於 PM 已寫入之 Context Bundle 與 WRAP 三問之上，不重述已決策事項，聚焦「claim 如何整合 ac_parser/validation_templates 的端到端行為契約」。

#### 1. 功能範圍與非範圍

**範圍（In-scope）**：
- `ticket track claim <ticket_id>` 在狀態更新**之前**執行 AC 自動驗證
- 新增 CLI flag `--skip-verify`（逃生閥）
- 對每個 AC 呼叫 `match_template`；若 `is_verifiable=True` 則以 `subprocess.run` 執行 `command`，依 `parse_strategy` 判定結果
- 互動式 y/n/c 三選一決策；y 繼續既有 claim 流程、n 取消、c 轉呼叫既有 `complete` 流程（記錄外溢達成）
- 輸出限制 15 行內的摘要
- 中斷處理（Ctrl-C / timeout）確保子程序終止、回傳非零

**非範圍（Out-of-scope）**：
- 新增模板（W5-001 已硬編碼 5 個，變更由 W5-001 owner 處理）
- 修改 `ac_parser` 或 `validation_templates` 的 API
- 驗證結果快取、持久化（每次 claim 重新執行）
- 並行執行多個驗證 command（MVP 循序執行，簡化 Ctrl-C 處理）
- 修改 `complete` 命令本身邏輯（僅從 c 選項呼叫）
- 針對非 npm 環境的替代驗證（由 \`unverifiable_reason\` 標記「環境不符」，不重試）

#### 2. User Story / 使用情境

| # | 情境 | AC 狀態 | 預期行為 |
|---|------|---------|---------|
| S1 | 無 AC Ticket | `acceptance` 為空或 None | 跳過驗證，直接進入既有 claim 流程，無額外輸出 |
| S2 | AC 全部 unverifiable | 所有 AC 的 `match_template` 回 `None` 或 `is_verifiable=False` | 顯示「無可驗證 AC（N 項）」，直接 claim，無 y/n/c 提示 |
| S3 | 部分可驗證、全部未達成 | 至少 1 個 AC 執行後判定未達成 | 顯示結果摘要 → 提示 y/n/c，預設建議 y（正常開工） |
| S4 | 部分可驗證、全部已達成（外溢達成） | 所有可驗證 AC 皆判定已達成 | 顯示結果摘要 + 警告「可能已外溢達成」→ 提示 y/n/c，預設建議 c |
| S5 | `--skip-verify` 指定 | 任何 AC 狀態 | 不執行驗證、不提示，直接走既有 claim 流程；輸出 1 行提示「已跳過 AC 驗證」 |
| S6 | 驗證執行中 Ctrl-C | 驗證執行到一半 | 中止當前 subprocess（含其子程序群）、回傳 130、不更新 Ticket 狀態 |
| S7 | 驗證 timeout | 某 command 超過 `timeout_sec` | 該 AC 標記「timeout」（視為未達成），繼續下一個 AC，不中斷整體流程 |

#### 3. CLI 介面定義

```
ticket track claim <ticket_id> [--skip-verify] [--yes]
```

| Flag | 型別 | 預設 | 行為 |
|------|------|------|------|
| `--skip-verify` | bool flag | False | 完全跳過 AC 驗證與 y/n/c 提示，直接 claim |
| `--yes` / `-y` | bool flag | False | 非互動模式：驗證仍執行，但自動選 `y`（繼續 claim）；適用 CI/script |

**互動式輸入規格**：
- Prompt 文字：`繼續 claim？[y] 繼續 / [n] 取消 / [c] 轉為 complete 並記錄外溢達成 (預設: {default}): `
- `default` 依情境決定（S3→y、S4→c）
- 接受輸入：`y`/`Y`/`yes`、`n`/`N`/`no`、`c`/`C`/`complete`、空字串（採預設）
- 無效輸入：重新 prompt，最多 3 次後預設 `n`（取消）
- 非 tty 環境（`sys.stdin.isatty()=False`）且無 `--yes`：視為 `n`（安全預設），stderr 警告「非互動環境，已取消；請加 --yes 或 --skip-verify」

#### 4. 內部函式分工

所有新函式定義於 `commands/lifecycle.py`（同檔）或拆為 `commands/claim_verification.py`（建議，保持 `lifecycle.py` 單一職責）。

| 函式 | 簽名 | 職責 |
|------|------|------|
| `collect_ac_verifications` | `(ticket_id: str) -> list[tuple[AC, ValidationCommand \| None]]` | 呼叫 `parse_ac` 取得 AC list，逐項 `match_template`，回傳配對清單 |
| `execute_verification` | `(ac: AC, vc: ValidationCommand) -> VerificationResult` | 對單一 `is_verifiable=True` 的 AC 執行 subprocess，套用 parse_strategy |
| `run_all_verifications` | `(pairs: list[tuple[AC, ValidationCommand \| None]]) -> list[VerificationResult]` | 循序呼叫 `execute_verification`，處理 KeyboardInterrupt，回傳完整結果 list |
| `summarize_results` | `(results: list[VerificationResult]) -> VerificationSummary` | 聚合為 `{passed, failed, unverifiable, no_template}` 四類計數與狀態標籤（all_passed / has_failures / none_verifiable） |
| `render_results` | `(summary: VerificationSummary, results: list[VerificationResult]) -> str` | 產出 <= 15 行的文字表格（格式見 §6） |
| `prompt_user_decision` | `(summary: VerificationSummary, auto_yes: bool) -> Literal['y', 'n', 'c']` | 處理 y/n/c 互動，套用預設建議、非 tty 處理、`--yes` 短路 |
| `claim_with_verification` | `(self, ticket_id: str, skip_verify: bool, auto_yes: bool) -> int` | `claim` 主流程整合入口；skip_verify=True 直通既有邏輯 |

**新增資料結構**（定義於 `commands/claim_verification.py` 或 `lib/verification_result.py`）：

```python
@dataclass(frozen=True)
class VerificationResult:
    ac: AC
    status: Literal['passed', 'failed', 'unverifiable', 'no_template', 'timeout', 'env_error']
    template_name: str | None   # None 代表 no_template
    message: str                # 單行摘要（渲染用）
    exit_code: int | None       # subprocess exit code（None 代表未執行）

@dataclass(frozen=True)
class VerificationSummary:
    total: int
    passed: int
    failed: int
    unverifiable: int           # 含 no_template + manual + env_error + timeout
    status: Literal['all_passed', 'has_failures', 'none_verifiable', 'no_ac']
    suggested_default: Literal['y', 'c']
```

#### 5. 資料流

```
claim(ticket_id, skip_verify, auto_yes)
    │
    ├─ skip_verify? ─yes─► 輸出「已跳過」→ 既有 claim 流程 → return 0
    │
    └─ collect_ac_verifications(ticket_id)
          │  uses: parse_ac → list[AC]
          │  uses: match_template → ValidationCommand | None
          ▼
       list[(AC, ValidationCommand | None)]
          │
          ▼
       run_all_verifications(pairs)
          │  for each pair:
          │    - vc is None → VerificationResult(status='no_template')
          │    - vc.is_verifiable=False → VerificationResult(status='unverifiable', message=unverifiable_reason)
          │    - is_verifiable=True → execute_verification(ac, vc)
          │        │
          │        ▼
          │     subprocess.run(vc.command, shell=True, timeout=vc.timeout_sec, capture_output=True)
          │        │
          │        ▼
          │     apply_parse_strategy(vc.parse_strategy, stdout, stderr, exit_code)
          │        │
          │        ├─ tail_lines: 取 stdout 最後 5 行，regex 找 /(\d+) failed/ 或 /(\d+) passing/；exit_code=0 且無 failed → passed
          │        ├─ exit_code: exit_code=0 → passed，否則 failed
          │        ├─ coverage_number: regex 擷取覆蓋率百分比；>= 門檻 → passed（門檻暫訂 80%，見缺口 §D）
          │        └─ manual: 不應在此被呼叫（is_verifiable=False）
          ▼
       list[VerificationResult]
          │
          ├─ summarize_results → VerificationSummary
          │
          ▼
       status == 'no_ac' 或 'none_verifiable' ─► 不 prompt，直接 claim（顯示 1 行摘要）
       其他 ─► render_results → print → prompt_user_decision
                │
                ├─ 'y' ─► 既有 claim 流程（status 更新、輸出 checklist）
                ├─ 'n' ─► return 1（未更新 Ticket）
                └─ 'c' ─► 呼叫 self.complete(ticket_id)；若 complete 成功額外 append-log 註記「claim 轉 complete：AC 外溢達成於 claim 前」
```

#### 6. 輸出格式規格（15 行內硬上限）

**S3/S4 場景（有可驗證 AC）**：

```
[AC 驗證] Ticket 0.18.0-W5-002 (6 項)
  [PASS] #1 npm test 通過 (npm_test_pass, 12.3s)
  [FAIL] #2 覆蓋率 >= 80% (coverage_threshold, 實測 72%)
  [SKIP] #3 lint 通過 (lint_pass, exit=2)
  [N/A]  #4 flaky 測試已修復 (flaky_fixed: 需人工審查)
  [----] #5 文件已更新 (無模板匹配)
  [PASS] #6 輸出 15 行內 (manual 檢查已達成)

結果：2 passed / 1 failed / 3 unverifiable
建議：繼續 claim 開始處理 failed 項 [預設: y]
```

**行數核算（上限 15）**：標題 1 + AC 清單最多 N 行（N <= 10，超出則摘要為「... (N-10 more)」）+ 空行 1 + 結果摘要 1 + 建議 1 + prompt 1 = 最多 15 行。

**S2 場景（全部 unverifiable）**：2 行
```
[AC 驗證] Ticket 0.18.0-W5-002：6 項 AC 皆無法自動驗證（5 no_template + 1 manual）
已略過驗證，直接進入 claim 流程...
```

**S5 場景（--skip-verify）**：1 行
```
[AC 驗證] 已跳過（--skip-verify）
```

**狀態標籤對應**：PASS=passed, FAIL=failed, SKIP=timeout/env_error, N/A=unverifiable(manual), ----=no_template

#### 7. 錯誤處理邊界

| 觸發條件 | 偵測方式 | 對應行為 | Exit Code |
|---------|---------|---------|-----------|
| AC command timeout | `subprocess.TimeoutExpired` | 該 AC 標 `status='timeout'`、`message='超過 {N}s 未完成'`、終止 process group | 繼續下一 AC |
| subprocess 執行失敗（`FileNotFoundError`，如無 npm） | `OSError` on `subprocess.run` | `status='env_error'`、`message='環境缺 npm 或 command 不可用'` | 繼續下一 AC |
| Ctrl-C 中斷 | `KeyboardInterrupt` | 終止當前 subprocess（`os.killpg`）、print `中斷：未更新 Ticket` 到 stderr | 130 |
| `parse_ac` 拋 `FileNotFoundError` | try/except | 既有錯誤處理（`load_and_validate_ticket` 已處理此情境） | 1 |
| `parse_ac` 拋 `ValueError`（YAML 損毀） | try/except | print `[Warning] AC 解析失敗：{err}`、降級為「跳過驗證，直接 claim」 | 走既有 claim |
| 非 tty 且無 `--yes` 與 `--skip-verify` | `sys.stdin.isatty()` | stderr 警告後 `return 1` | 1 |
| `--yes` 且驗證全 failed | summary.status 分支 | 自動選 `y`、顯示結果後繼續 claim（使用者已明示接受） | 走既有 claim |
| `c` 選項但 `complete` 失敗 | `self.complete` 回非零 | 原 Ticket 狀態不變（未 claim），stderr 顯示 complete 失敗原因 | complete 的 exit code |

**子程序終結可靠性要求**：
- 使用 `subprocess.Popen(..., start_new_session=True)` 或 `preexec_fn=os.setsid`（POSIX）建立 process group
- `KeyboardInterrupt` / `TimeoutExpired` 時呼叫 `os.killpg(pgid, SIGTERM)`，1 秒後再 `SIGKILL`
- 驗證執行前 print `執行中: {template_name}... (timeout {N}s，Ctrl-C 中斷)`（stderr，不計入 15 行輸出上限）

#### 8. 驗收標準的可驗證化（為 Phase 2 鋪路）

| AC # | 原文 | 可驗證點 | Phase 2 測試建議 |
|------|------|---------|----------------|
| 1 | claim 前自動執行可機器驗證的 AC | `subprocess.run` 被呼叫次數 == `is_verifiable=True` 的 AC 數 | Mock subprocess，斷言呼叫計數與 command 內容；整合測試建 fixture Ticket 含 `npm test` AC 驗證執行流程 |
| 2 | 輸出結果含每個 AC 的判定 | stdout 包含每個 AC index 與狀態標籤 | 捕捉 stdout，regex 斷言 `[PASS]\|[FAIL]\|[SKIP]\|[N/A]\|[----]` 出現次數 == AC 總數 |
| 3 | 提供 y/n/c 三選一 | stdin 注入 `y`/`n`/`c` 分別觸發三種行為 | 用 `io.StringIO` 或 `unittest.mock.patch('builtins.input')`，斷言：y → status 變 in_progress、n → 不變、c → `self.complete` 被呼叫 |
| 4 | 新增整合測試 模擬 AC 已達成情境 | 建立 fixture Ticket + mock subprocess 回 exit=0 | Phase 2 建整合測試：fixture AC 含 `npm test 通過` + mock `subprocess.run` 回 `CompletedProcess(returncode=0, stdout='... 0 failed')`，斷言 summary.status=='all_passed' 且預設建議為 c |
| 5 | 不影響既有無 AC 或無可驗證 AC 的 claim 流程 | S1/S2 場景下行為與現行 claim 一致 | 回歸測試：既有 claim 測試全部通過；新增 S1/S2 測試斷言無 prompt、輸出 <= 2 行、Ticket 正常進入 in_progress |
| 6 | 輸出控制在 15 行內 | `render_results` 回傳字串 `.count('\n') <= 14` | 單元測試：`render_results(summary, results=[...]*20)` 斷言行數 <= 15；測試多種 AC 數量（0/1/6/20） |

---

#### A. 對 Phase 2 測試設計的建議切入點（非規格本體）

- 先測 pure function：`summarize_results` / `render_results` / `apply_parse_strategy`（無 I/O 依賴）
- 再測 subprocess 整合：`execute_verification` 用 `unittest.mock.patch('subprocess.run')`
- 最後 CLI 端到端：用 `pytest` `capsys` + `monkeypatch.setattr('builtins.input', ...)` 模擬 y/n/c
- 建議新增 fixture：`tests/fixtures/tickets/ac_verification/` 放 S1~S7 的 mock Ticket YAML

#### B. 需 PM 決策的模糊點（規格缺口）

1. **coverage 門檻值**：模板 `coverage_threshold` 的 parse_strategy 為 `coverage_number`，但 W5-001 未定義判定門檻（80%？AC 文字中抽取？）。**建議**：Phase 3b 從 AC 文字 regex `(\d+)%` 擷取；若無則預設 80%。
2. **c 選項是否需二次確認**：「轉 complete」是跨語義的操作，是否需 `[確定要轉為 complete 並記錄 AC 外溢達成? y/N]` 二次確認？**建議**：MVP 加二次確認，降低誤觸風險。
3. **`--yes` flag 是否與 `--skip-verify` 衝突**：兩者同時指定時哪個優先？**建議**：`--skip-verify` 優先（完全不執行），`--yes` 在有驗證時才作用。
4. **非 tty 行為**：目前規格為「非 tty 且無 `--yes` → return 1」。是否改為「非 tty 預設 `--yes`」？**建議**：保持 return 1，避免 CI 誤放行未達標 claim。
### 簡化 WRAP 三問（claim Checkpoint）

**W（Widen）—— 有其他做法嗎？**
1. [預設] claim 前自動執行所有可驗證 AC，結果 + y/n/c 三選一
2. [候選] `ticket track claim --verify` 顯式觸發，預設跳過驗證（低侵入、但易被遺忘）
3. [候選] claim 後僅警告顯示 AC 狀態，不中斷（最弱防護，回到 PC-055 問題）

選 (1) 理由：PC-055 的核心問題是「代理人做已完成工作」，必須在 claim 路徑上強制 prompt，非可選。但加 `--skip-verify` 逃生閥以處理不適用場景（見 P）。

**A（Attain distance）—— 機會成本是什麼？**
- 時間成本：每次 claim 多 0-300 秒（視 AC 類型）
- 擠壓目標：TDD Phase 1 起步時間
- 判斷：W5 整體目的是降低錯誤派發率（每次誤派發 = 代理人回合耗盡 + PM 重新規劃），單次誤派發成本 >> claim 驗證成本。值得。

**P（Prepare to be wrong）—— 最可能失敗的原因是什麼？**
- 最可能：npm test 類 command 在大專案耗時過長，使用者被迫等待 / Ctrl-C 中斷後子程序洩漏
- 12h 後失敗情境：使用者為了加速放棄使用 claim 命令，回到手動 claim → 功能形同不存在
- 防護措施：
  1. 支援 `--skip-verify` flag（逃生閥）
  2. 統一 timeout（使用 `validation_templates` 的 timeout_sec）
  3. 子程序終結必須可靠（`subprocess.run(..., timeout=...)` + 終止訊號傳遞）
  4. 每個驗證執行前顯示「執行中...」提示，給予中斷機會
### Context Bundle（PM 前台分析）

**W5-001 產物（可直接使用的 API）**：
- `ticket_system.lib.ac_parser.parse_ac(ticket_id) -> list[AC]`
  - 回傳 `AC(index, text, checked, raw)` frozen dataclass
  - 已處理 checkbox 前綴剝除（`[x]` / `[ ]` / 無前綴視為未勾選）
- `ticket_system.lib.validation_templates.match_template(ac_text) -> ValidationCommand | None`
  - 5 個模板：npm_test_pass / coverage_threshold / lint_pass / flaky_fixed / skipped_evaluated
  - 大小寫無關子字串匹配，第一註冊者勝出
  - 回傳 `ValidationCommand` 含 `command` / `timeout_sec` / `parse_strategy` / `is_verifiable` / `unverifiable_reason`

**整合位置（TicketLifecycle.claim）**：
- 檔案：`.claude/skills/ticket/ticket_system/commands/lifecycle.py:307-375`
- 插入點：在 `validate_claimable_status` 之後、`_print_phase_prerequisite_warning` 之前（或之後，視 phase 警告順序）
- 實際執行 claim 的三行：line 362-364（status/assigned/started_at 設定）

**必要的新模組/函式**：
1. AC 驗證執行器：對 `is_verifiable=True` 的 AC 執行 command，解析 parse_strategy
2. 結果呈現器：將每個 AC 的 (已達成 / 未達成 / 無法驗證 / 無模板匹配) 列印（限 15 行）
3. y/n/c 互動器：讀 stdin，y=繼續、n=abort return 1、c=轉 complete（呼叫既有 complete 流程）

**AC 解析與驗證的邊界**：
- AC 無 `acceptance` 欄位 → 跳過驗證，直接 claim
- AC 清單全部 `unverifiable`（無模板匹配或 `command=None`）→ 顯示「無可驗證 AC」，直接 claim
- AC 全部 `checked=True` → 自動觸發「可能已外溢達成」提示，預設 c 選項

**風險與已知陷阱**：
- `npm test` 執行耗時（可達 300 秒），使用者可能無法等待 → 必須支援 `--skip-verify` flag 或 timeout 中斷
- Ctrl-C 必須能正常中斷、回傳非零（不 leak 子程序）
- 部分環境無 npm（僅 Python 工具鏈）→ command 執行失敗應標記「環境不符」非「未達成」
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending