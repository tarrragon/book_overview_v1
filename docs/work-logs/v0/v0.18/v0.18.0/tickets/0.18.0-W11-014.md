---
id: 0.18.0-W11-014
title: 釐清 CheckpointState.pending_checks 欄位渲染缺漏 (v2 規格未對應任何輸出)
type: ANA
status: pending
version: 0.18.0
wave: 11
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第四層:PM 流程改善
  final_decision: 延後釐清 pending_checks 應否在 v2 規格渲染
  rationale: 017.1 Phase 1 v2 multi-view Plan 發現 pending_checks 欄位 v1 §5.2 提及 v2 整篇消失
who:
  current: saffron-system-analyst
  history: {}
what: 釐清 CheckpointState.pending_checks 欄位渲染缺漏 (v2 規格未對應任何輸出)
when: 待定義
where:
  layer: 待定義
  files:
  - Infrastructure
why: lib/checkpoint_state.py 的 CheckpointState dataclass 含 pending_checks 欄位（List[PendingCheck]），017.8
  設計時用於補充「需人工確認」訊號。lavender v1 §5.2 提及 v2 整篇消失，未對應到三命令任何渲染輸出。需釐清欄位用途並決定是否補規格
how:
  task_type: Implementation
  strategy: 待定義
acceptance:
- '[x] 確認 pending_checks 欄位在 017.8 設計的原始用途'
- '[x] 決定 v2 規格是否須新增該欄位的渲染輸出'
- '[x] 若需要，提供 v2 規格補丁交給 sage Phase 2'
- '[x] 若不需要，提供 lib 層欄位棄用建議'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-04-19'
updated: '2026-04-19'
---

# Execution Log

## Task Summary

釐清 CheckpointState.pending_checks 欄位渲染缺漏 (v2 規格未對應任何輸出)

---

## Problem Analysis

### 問題根因

CheckpointState.pending_checks 欄位在 lavender Phase 1 v1 §5.2 被列為「介面充足性確認」12 欄位之一（「pending_checks → 補充『需人工確認』訊號」），但 v2 規格整篇未提及該欄位的渲染輸出。問題在於：v2 修正清單 12 條專注於 exit code / 時間欄位 / view function 等 P0-P2 改動，未顯性處理 pending_checks 的渲染缺漏（因 Plan 審查將其歸為 P2 延後）。

### 影響範圍

- `ticket_system/lib/checkpoint_state.py`：PendingCheck dataclass（L63-76）+ CheckpointState.pending_checks 欄位（L104）
- `ticket_system/lib/checkpoint_view.py`：完全無引用 pending_checks（0 處）
- `ticket_system/commands/track_snapshot.py`：SAFE_CALL 產生 PendingCheck 並填入 state
- `ready_for_clear` 推導（L758-761）：依賴 `all(not c.auto_detectable for c in pending)`

### 欄位演進歷史

| 時間點 | 事件 | 來源 |
|--------|------|------|
| W10-017.8 | 設計 CheckpointState dataclass，pending_checks 用於 SAFE_CALL I/O 失敗記錄 | checkpoint_state.py L94-99 docstring |
| W10-017.13 | AC3 決議「保留 + 文件化」，非 dead field（PC-093 二選一原則） | checkpoint_state.py L94-99 |
| Phase 1 v1 5.2 | 列入 12 欄位「介面充足性確認」，標註用途「補充需人工確認訊號」 | 0.18.0-W10-017.1.md L1911 |
| Phase 1 v2 多視角審查 | Plan 視角 P2 發現「v1 5.2 提及，v2 整篇消失」 | 0.18.0-W10-017.1.md L275 |
| 0.18.0-W11-014 建立 | PM 延後釐清，建 ANA ticket | 0.18.0-W10-017.1.md L1275 |

### 相關 Error Pattern

無直接相關 error pattern。欄位本身非 dead（PC-093 已決議保留）。
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution

<!-- To be filled by executing agent -->

---

### 原始用途確認

pending_checks 欄位由 017.8 設計，用途為：
1. SAFE_CALL 在資料源 I/O 失敗時 append `auto_detectable=False` 的 PendingCheck
2. 作為 `ready_for_clear` 推導的輸入條件：`all(not c.auto_detectable for c in pending)` — 若存在 auto_detectable=True 的 check，ready_for_clear 被阻擋
3. 017.13 AC3 決議「保留 + 文件化」，理由：(a) ready_for_clear 依賴此 list，(b) 既有測試驗證 reason 文字為對外契約

### 決議：v2 規格無需新增渲染輸出

**結論**：pending_checks 是正確的「內部推導輸入」，其效果已透過 `ready_for_clear` 間接呈現於 view 層。不需要在 checkpoint_view.py 額外直接渲染 pending_checks 內容。

**理由**：
1. pending_checks 只在 I/O 降級時產生（SAFE_CALL fallback 路徑），此時 ready_for_clear=False 已傳達「不可 clear」語意
2. data_sources dict 已記錄具體 I/O 錯誤訊息（checkpoint-status 命令渲染此 dict），使用者可從中得知降級原因
3. 直接渲染 pending_checks 會與 data_sources 資訊重複，增加認知負擔

**建議**：
- 不修改 v2 規格（不新增 pending_checks 渲染輸出）
- 不棄用 lib 層 pending_checks 欄位（017.13 AC3 已決議保留，ready_for_clear 依賴它）
- v1 §5.2 的「pending_checks → 補充需人工確認訊號」描述為「介面充足性確認」，確認欄位存在且 lib 暴露即可，不等於 view 層必須直接渲染
- 本 ticket 結論：欄位保留，view 層不渲染，v2 規格正確無需補丁
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending