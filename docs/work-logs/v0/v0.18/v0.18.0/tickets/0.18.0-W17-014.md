---
id: 0.18.0-W17-014
title: 重構 track.py handler 雙軌 metadata 二元組統一（W17-008.11 方案 A）
type: IMP
status: completed
version: 0.18.0
wave: 17
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W17-008.11
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: refactor-from-w17-008-11
  rationale: W17-008.11 方案 A；19h 最大 child；P1 基礎重構先於 W17-008.5
who:
  current: thyme-python-developer
  history: {}
what: 重構 track.py 拆分為 version_aware_handlers + version_agnostic_handlers 雙 dict（W17-008.11
  ANA WRAP 結論方案 B），消除 line 224-236 if-elif 雙軌；agnostic 含 snapshot/agent-status/handoff-ready/checkpoint-status/dispatch-check
  5 命令
when: W17-008.11 ANA 推薦方案 A；P1 基礎性重構（先於 W17-008.5 錯誤訊息統一）
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/cli/track.py
  - .claude/skills/ticket/cli/fields.py
  - .claude/skills/ticket/cli/track_acceptance.py
  - .claude/skills/ticket/cli/track_query.py
why: W17-008.11 識別 5 命令（snapshot/agent-status/handoff-ready/checkpoint-status/dispatch-check）走特殊
  handler 路徑；雙軌機制導致新增命令易遺漏；統一為 metadata 二元組可消除雙軌 + 為 W17-008.5 錯誤訊息統一提供基礎
how:
  task_type: Implementation
  strategy: 'Implementation: 在 track.py:160 _create_command_handlers() 旁新增 _create_version_agnostic_handlers()
    回傳 5 命令 dict；execute() 先查 agnostic（無需 version）再查 aware；註冊端在 _register_all_subcommands
    將 5 個 register_* 包入 _register_global_state_commands 群組；保留既有 register 函式'
acceptance:
- '[x] 新增 _create_version_agnostic_handlers() 含 5 命令 dict'
- '[x] execute() 先查 agnostic 再查 aware，移除 5 個 if-elif'
- '[x] _register_global_state_commands 群組化 5 個 register_*'
- '[x] pytest 全綠（1698 passed）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-28T11:29:44'
completed_at: '2026-04-28T11:57:37'
created: '2026-04-20'
updated: '2026-04-20'
---

# Execution Log

## Task Summary

重構 track.py _create_command_handlers() 返回 (handler, metadata) 二元組；metadata 含 requires_version flag；所有 execute_X() 改簽名為 handler(args, version: Optional[str])；消除 line 219-229 特殊處理雙軌

---

## Problem Analysis

### 問題根因

track.py 原本對 5 個 version-agnostic 命令（snapshot / agent-status / handoff-ready / checkpoint-status / dispatch-check）使用 if-elif 雙軌，與主 handler dict 並存，新增類似命令易遺漏；雙軌機制使維護成本上升（W17-008.11 ANA 識別）。

### 影響範圍

- .claude/skills/ticket/ticket_system/commands/track.py（execute / _register_all_subcommands）
- 5 個 agnostic 命令的執行路徑
- 1698 個測試需保持全綠

### 相關 Error Pattern

無新建 PC；本重構為 W17-008.11 ANA 結論落地。
## Context Bundle

### W17-008.11 ANA 結論 (2026-04-28 補充)

W17-008.11 重做分析後的方案比較：

| 方案 | 風險 | LOC | 推薦度 |
|------|------|-----|--------|
| A. metadata 二元組 + 統一 signature（本 ticket 原規劃） | 改動所有 handler signature，高 | ~50 | 中 |
| B. 雙 dict（version_aware + version_agnostic） | 不動既有 handler，最小 | ~25 | 高（ANA 推薦） |

**ANA 推薦方案 B 理由**：避免批量改動 handler signature；兩 dict 名稱即文件；dispatch 邏輯 `agnostic.get(op) or aware.get(op)` 簡潔。

**實作前請與 PM 確認採 A 或 B**。完整分析見 0.18.0-W17-008.11 Solution 章節。
### Source Chain
- W17-004（落差 14）→ W17-008.11 → 本 ticket（方案 A，19h 最大 IMP）

### W17-008.11 產出核心
- 5 命令走特殊 handler 路徑：snapshot / agent-status / handoff-ready / checkpoint-status / dispatch-check
- snapshot / agent-status / dispatch-check：本質複雜度（職責邊界）
- handoff-ready / checkpoint-status：偶然複雜度（可統一）
- 根因：未提供「可選版本參數」統一機制

### 重構範圍（方案 A）
1. `_create_command_handlers()` 返回 `(handler, metadata)` 二元組
2. `metadata` 含 `requires_version`, `needs_blocked_check` 等 flag
3. 所有 `execute_X()` 簽名改為 `handler(args, version: Optional[str])`（35+ 函式）
4. 移除 track.py line 219-229 特殊區

### 關聯
- 前置於 W17-008.5（錯誤訊息雙軌統一）——本重構消除 handler 雙軌後，W17-008.5 才能在乾淨介面統一錯誤格式

### 驗證
- pytest 全綠（35+ 函式改簽名需確保不破既有測試）
- linux 視角多視角審查（19h 大型重構強制）
- 回歸：所有 CLI 子命令行為不變
- metadata schema 公開文件化

### 風險
- 高。大型重構；建議分 Phase 推進（TDD 完整流程）
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

<!-- auto-extracted: v1 | sources: 0.18.0-W17-008.11 | chars: 206 -->

### Task Reference
- 0.18.0-W17-008.11 what: 分析 track.py 中 5 個子命令（snapshot/agent-status/handoff-ready/checkpoint-status/dispatch-check）特殊處理不走 register 字典的設計決策；評估是否應統一到主 handler 或維持雙軌並補文件

### Rationale Chain
- 0.18.0-W17-008.11 why: W17-004 落差 14：5 個子命令走 track.py:219-229 特殊路徑；新增類似命令易遺漏；雙軌機制使維護成本增加

## Solution

<!-- To be filled by executing agent -->

---

### 重構摘要（W17-008.11 方案 B：雙 dict）

採用 ANA 推薦的方案 B（雙 dict），未動既有 handler signature。

#### track.py 變更

1. 新增 `_create_version_agnostic_handlers()` 回傳 5 命令 dict：
   - snapshot / agent-status / handoff-ready / checkpoint-status / dispatch-check
   - handler 簽名為 `handler(args)`，不接受 version
2. `_create_command_handlers()` 補充 docstring，明示為 version-aware dict
3. `execute()` 改為先查 agnostic dict 再查 aware dict：
   - 移除 line 224-236 原 5 個 if-elif 分支
   - 保留 `if operation == "version"` 分支（簽名為 `handler(args, None)`，與兩 dict 都不同）
4. 新增 `_register_global_state_commands()` 群組，將 5 個 register_* 包入
5. `_register_all_subcommands` 簡化：5 行平鋪 register 改為 1 行群組呼叫

### 設計決策

- 未改 handler signature：方案 B 核心優勢，35+ execute_X 函式不動
- version 命令保留 if 分支：其簽名為 (args, None) 與 agnostic 的 (args) 不同
- 群組函式 docstring 明示對應關係
## Test Results

<!-- To be filled by executing agent -->

---

### 測試指令
uv run --directory .claude/skills/ticket pytest -x -q

### 結果
1698 passed in 969.99s (0:16:09)

全綠，無 regression。涵蓋 track_snapshot / track_agent_status / track_handoff_ready / track_checkpoint_status / track_dispatch_check 等 5 個 agnostic 命令的測試檔皆通過。
## Completion Info

**Completion Time**: 2026-04-28T11:57:37
**Executing Agent**: thyme-python-developer
**Review Status**: pending