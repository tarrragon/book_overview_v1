---
id: 0.18.0-W5-033
title: 新增 ANA Ticket 模板「重現實驗結果」必填章節（PC-063 防護 1）
type: DOC
status: completed
version: 0.18.0
wave: 5
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.18.0-W5-031
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: PC-063 ANA 過早收斂防護措施 1/4
  rationale: quality-baseline-rule-5
who: thyme-python-developer
what: 新增 ANA Ticket 模板「重現實驗結果」必填章節（PC-063 防護 1）
when: 待定義
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/templates/
why: PC-063 案例（W5-031）顯示 ANA Ticket 缺少強制重現實驗章節，導致 4 候選方案皆基於未驗證假設。模板新增「重現實驗結果」必填章節強制
  ANA 進行 Reality Test 才能列方案。
how:
  task_type: Implementation
  strategy: 找到 ANA Ticket 模板（ticket_system/templates/ana.md 或類似），於 Problem Analysis
    後新增「重現實驗結果」章節，含「實驗方法」「實驗執行」「實驗發現」三子節，標記為必填
acceptance:
- '[x] ANA Ticket 模板新增「重現實驗結果」章節'
- '[x] 章節含「實驗方法/實驗執行/實驗發現」三子節'
- '[x] 新建 ANA Ticket 自動帶入此章節'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-04-13T20:08:09'
completed_at: '2026-04-13T20:12:40'
created: '2026-04-13'
updated: '2026-04-13'
---

# Execution Log

## Task Summary

新增 ANA Ticket 模板「重現實驗結果」必填章節（PC-063 防護 1）

---

## Problem Analysis

## PC-063 防護措施 1/4

### 背景（來自 W5-031 案例）

W5-031 ANA Ticket 列 4 候選方案（A/B/C/D），皆基於未驗證假設。PM 清 context 後 resume，主動進行重現實驗後發現：
- 真正根因：test_track_batch.py 和 test_track_relations.py 的 mock 錯誤
- 4 候選方案全部不匹配真正根因
- 實驗後衍生出方案 F（根治）和 H（F+D 組合）

結論：**ANA Ticket 沒有強制重現實驗章節，容易過早收斂於假設性方案**。

### 本 Ticket 任務

修改 ticket_builder.py 的 create_ticket_body() 函式，讓 ANA 類型 Ticket 建立時自動帶入「重現實驗結果」必填章節。

### 涉及檔案

**主修改**：
- .claude/skills/ticket/ticket_system/lib/ticket_builder.py (L498-L594: create_ticket_body)

**呼叫端**（3 處，需傳入 ticket_type）：
- .claude/skills/ticket/ticket_system/commands/create.py:717
- .claude/skills/ticket/ticket_system/commands/bulk_create.py:242
- .claude/skills/ticket/ticket_system/lib/ticket_generator.py:247

**測試**：
- .claude/skills/ticket/tests/test_ticket_builder.py:565-595 (2 個既有 test case 需更新)
- 新增 ANA 專屬 test case 驗證「重現實驗結果」章節

### 關鍵現況

模板 body 目前是 hardcoded 在 Python 函式，不是獨立 markdown 檔案。此為設計既有結構，本次遵循。
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## 實作策略

### Step 1: 修改 create_ticket_body() 函式簽名

檔案：.claude/skills/ticket/ticket_system/lib/ticket_builder.py

```python
def create_ticket_body(what: str, who: str, ticket_type: str = '') -> str:
```

- 新增 ticket_type 參數（optional, default ''），向後兼容既有呼叫（非 ANA 無影響）。
- 當 ticket_type == 'ANA' 時，在 Problem Analysis 章節後、Solution 章節前插入「重現實驗結果」章節。

### Step 2: 新增章節 template

章節插入位置：Problem Analysis 章節結束（`---`）之後、Solution 章節開始之前。

章節內容（嚴格依 W5-033 acceptance 第 2 條）：

```markdown
## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---
```

### Step 3: 更新 3 個呼叫端傳入 ticket_type

- commands/create.py:717 → 加 `frontmatter.get('type', '')`
- commands/bulk_create.py:242 → 加 `frontmatter.get('type', '')`
- lib/ticket_generator.py:247 → 加 `frontmatter.get('type', '')`

### Step 4: 更新測試

test_ticket_builder.py L565-595：
- 既有 test_create_ticket_body_structure: 驗證通用結構（不變動）
- 既有 test_create_ticket_body_with_special_characters: 驗證特殊字元（不變動）
- **新增 test_create_ticket_body_ana_includes_reproduction_section**:
  - 呼叫 create_ticket_body(what, who, 'ANA')
  - assert '## 重現實驗結果' in body
  - assert '### 實驗方法' in body
  - assert '### 實驗執行' in body
  - assert '### 實驗發現' in body
- **新增 test_create_ticket_body_non_ana_excludes_reproduction_section**:
  - 呼叫 create_ticket_body(what, who, 'IMP')
  - assert '## 重現實驗結果' not in body

### Step 5: 驗證

執行 ticket test suite 確保：
- 既有測試全部通過
- 新增測試全部通過
- 實際用 `ticket create --version test --type ANA` dry-run 驗證產出含新章節

### Step 6: 重新安裝 CLI（必要）

修改 ticket_system 原始碼後，執行：
```
uv tool install .claude/skills/ticket --reinstall
```

### 驗收條件達成檢查

- [x] AC1 ANA Ticket 模板新增「重現實驗結果」章節 → create_ticket_body() ANA 分支
- [x] AC2 章節含「實驗方法/實驗執行/實驗發現」三子節 → 章節內容明確定義
- [x] AC3 新建 ANA Ticket 自動帶入此章節 → 3 呼叫端皆傳入 ticket_type + 測試驗證
## Test Results

<!-- To be filled by executing agent -->

---

已實作並通過所有測試。修改摘要：
1. ticket_builder.py: 新增 _ana_reproduction_section() 輔助函式 + create_ticket_body() 新增 ticket_type 參數
2. 3 個呼叫端（create.py / bulk_create.py / ticket_generator.py）已傳入 frontmatter.get('type', '')
3. test_ticket_builder.py: 新增 2 個 test case（ANA 含章節 + 非 ANA 不含章節）
4. uv tool install --reinstall 已完成
測試結果：35 passed in 0.05s（含 2 新測試）
## Completion Info

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending