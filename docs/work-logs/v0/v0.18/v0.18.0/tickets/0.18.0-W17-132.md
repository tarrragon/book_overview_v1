---
id: 0.18.0-W17-132
title: 修正 framework-paths SSOT 區分 strict/broad + lifecycle.py S 問改用 broad
type: IMP
status: completed
version: 0.18.0
wave: 17
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W17-131
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: create-imp-from-ana
  rationale: W17-131 ANA 結論推薦 B 方案，落地修正 SSOT 邊界 bug
who:
  current: basil-hook-architect
  history: {}
what: 修正 framework-paths SSOT 區分 strict/broad + lifecycle.py S 問改用 broad
when: W17-131 ANA 完成後（已標推薦 B 方案）
where:
  layer: 待定義
  files:
  - .claude/config/framework-paths.yaml
  - .claude/hooks/lib/framework_paths.py
  - .claude/skills/ticket/ticket_system/commands/lifecycle.py
  - .claude/hooks/framework-rule-edit-skill-trigger-hook.py
why: 'W17-131 ANA 重現實驗確認：framework-paths.yaml framework_paths 不含 .claude/hooks/，導致
  lifecycle.py S 問對 hook 編輯不觸發。但 hook 內含警告訊息屬規範性文字（W17-127.2 自身 acceptance #4 + #8
  即明示）。推薦方案 B：SSOT 內拆 strict（規則文字，PreToolUse hook 用）vs broad（含 hooks，lifecycle.py
  S 問用）兩 list'
how:
  task_type: Implementation
  strategy: (1) framework-paths.yaml 拆兩 list：framework_paths_strict（保留現有 7 路徑）+ framework_paths_broad（strict
    + .claude/hooks/）；(2) lib/framework_paths.py 加 is_framework_path_broad() 函式；(3)
    lifecycle.py _has_framework_path() 改用 is_framework_path_broad()；(4) framework-rule-edit-skill-trigger-hook.py
    維持用 is_framework_path（strict）；(5) 單元測試覆蓋 hooks/ 路徑判定差異（strict=False, broad=True）；(6)
    重現 W17-131 場景：claim hooks/ IMP 應觸發 S 問
acceptance:
- '[x] framework-paths.yaml 拆 strict/broad 兩 list'
- '[x] lib/framework_paths.py 加 is_framework_path_broad() 函式'
- '[x] lifecycle.py _has_framework_path() 改用 broad'
- '[x] PreToolUse skill-trigger-hook 維持 strict（避免 hook refactor 過度誤擋）'
- '[x] 單元測試 .claude/hooks/ 路徑判定 strict=False / broad=True 兩情境通過'
- '[x] 重現 W17-131 場景驗證：claim hooks/ IMP 觸發 S 問'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-04T00:28:02'
completed_at: '2026-05-04T00:33:34'
created: '2026-05-04'
updated: '2026-05-04'
---

# Execution Log

## Task Summary

修正 framework-paths SSOT 區分 strict/broad + lifecycle.py S 問改用 broad

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

### 簡化 WRAP 三問

**W（Widen）候選方案**（W17-131 ANA 已分析三選項）：

1. 採取做法（選定）：方案 B — framework-paths.yaml 拆 strict（規則文字）/ broad（含 hooks）兩 list，lib 加 is_framework_path_broad()，lifecycle.py 改用 broad，PreToolUse hook 維持 strict
2. 候選 A：framework_paths 直接加 .claude/hooks/。被淘汰：PreToolUse 對所有 hook Edit 觸發警告，cache invalidation 失準
3. 候選 C：lifecycle.py inline 判 .claude/hooks/。被淘汰：違反 SSOT 原則

**A（Attain distance）機會成本**：

- 直接成本：basil-hook-architect 預估 5-7 tool calls
- 機會成本：runqueue P1 任務暫緩；W17-127.3 dry-run ANA 仍 blockedBy W17-127.2 已 unblock 但 1 週驗證期非本 session 範圍
- 落地後成本：SSOT 維度 +1（兩 list 共享 yaml），新增類別時需考慮歸 strict 或 broad

**P（Prepare to be wrong）失敗預想**：

- 最可能失敗：is_framework_path_broad() 與 is_framework_path() 函式命名混淆，後續開發者誤用 strict 期望 broad 行為
- 防護：(1) 兩函式 docstring 明示 use case 與消費端對照（PreToolUse hook → strict, lifecycle.py S 問 → broad）；(2) acceptance #5 單元測試明示 strict=False / broad=True 兩情境驗證；(3) acceptance #6 重現 W17-131 場景驗證 lifecycle.py 對 hooks/ IMP 觸發 S 問

### S（SKILL trigger）回應

S 問已觸發（where.files 含 .claude/skills/ticket/ticket_system/commands/lifecycle.py 觸發），但本 ticket 純技術修正（拆 yaml list + lib 加函式 + 改 import），不涉及規範性文字撰寫（hook 警告訊息本案不改）。Read SKILL 可省略；落地時 docstring 文字機會成本 < 100 token，basil 自律遵循 ai-communication-rules 規則 6 即可。

### Context Bundle 補充

- W17-131 已落地 ANA 結論（commit 05fe849f）：root cause 確認 + 推薦 B 方案 + trade-off 分析完整
- W17-127.1 lib/framework_paths.py 已含 is_framework_path() + is_layer1_path()，本 ticket 加 is_framework_path_broad()
- 重現驗證可直接用 claim W17-132 自身觀察結果（W17-132 where.files 含 skills/lifecycle.py 觸發 S 問，符合 W17-131 結論）
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 實作策略（PM 預先分解，basil-hook-architect 落地）

**A. .claude/config/framework-paths.yaml**：
- 既有 `framework_paths`（7 項）改名為 `framework_paths_strict`
- 新增 `framework_paths_broad`：strict + `.claude/hooks/`
- 兩 list yaml 註釋明示 use case（strict = PreToolUse；broad = lifecycle.py S 問）

**B. .claude/hooks/lib/framework_paths.py**：
- 既有 `is_framework_path()` 維持讀 strict（向後相容）
- 新增 `is_framework_path_broad()` 讀 broad
- docstring 對照表明示消費端（PreToolUse → strict / lifecycle.py S 問 → broad）

**C. .claude/skills/ticket/ticket_system/commands/lifecycle.py**：
- `_resolve_framework_path_checker()` 改回傳 `is_framework_path_broad`（line 1270）
- `_FALLBACK_FRAMEWORK_PREFIXES` 加 `.claude/hooks/`

**D. .claude/hooks/framework-rule-edit-skill-trigger-hook.py**：
- 維持 import strict，無變更

**E. 單元測試**：
- `tests/test_framework_paths.py` 加 strict vs broad 對照（`.claude/hooks/foo.py` strict=False / broad=True）

**F. 重現驗證（acceptance #6）**：
- claim 一個假 IMP ticket（或 trace lifecycle.py）where.files=.claude/hooks/test.py 確認 S 問觸發

### 實作摘要（basil-hook-architect 落地，W17-132）

**A. .claude/config/framework-paths.yaml**：
- 新增 framework_paths_strict（YAML anchor，含原 7 路徑）
- framework_paths 改為 alias *framework_paths_strict（向後相容）
- 新增 framework_paths_broad：strict 7 項 + .claude/hooks/
- 兩 list 上方註釋明示 use case 與消費端對照

**B. .claude/hooks/lib/framework_paths.py**：
- _load_config 新增 framework_paths_strict / framework_paths_broad 讀取，雙層 fallback（strict 缺 → 讀 framework_paths；broad 缺 → 讀 strict）
- get_framework_paths() 改讀 strict（語意明確化，行為不變）
- 新增 get_framework_paths_broad() 與 is_framework_path_broad()，docstring 明示與 strict 差異與消費端對照
- 模組級 docstring 補 W17-132 SSOT 邊界拆分說明

**C. .claude/skills/ticket/ticket_system/commands/lifecycle.py**：
- _resolve_framework_path_checker() 改回傳 framework_paths.is_framework_path_broad
- _FALLBACK_FRAMEWORK_PREFIXES 補 .claude/hooks/ 對齊 broad
- _has_framework_path docstring 補 W17-132 銜接

**D. .claude/hooks/framework-rule-edit-skill-trigger-hook.py**：未動，維持 strict（acceptance #4）

**E. tests/test_framework_paths.py**：
- 新增 TestIsFrameworkPathBroad 5 個測試：strict=False/broad=True 邊界 / strict 為 broad 子集 / 非 framework 兩者皆 False / hooks/tests/ 豁免 / get_framework_paths_broad 載入驗證

**F. 重現驗證**：
- is_framework_path('.claude/hooks/...py') = False（strict）
- is_framework_path_broad('.claude/hooks/...py') = True（broad）
- lifecycle._has_framework_path({where:{files:['.claude/hooks/...py']}}) = True（W17-131 場景修正成功）

### 設計決策

- YAML anchor 而非重複列舉 strict 7 項：避免雙寫漂移，未來擴增 strict 時 broad 自動同步
- strict 缺項 fallback 至 framework_paths：保護外部 yaml 若被 downgrade 時不破壞 W17-127.1 既有消費端
- broad 缺項 fallback 至 strict：保守降級，行為等同舊版

### 不在本 ticket 範圍

- W17-127.3 dry-run 模式 ANA（已 unblock，獨立追蹤）
- 其他 hook 是否也需 broad 範圍（依各自 use case 評估）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---


### 測試指令

python3 -m pytest .claude/hooks/tests/test_framework_paths.py -v
python3 -m pytest .claude/hooks/tests/test_framework_rule_edit_skill_trigger.py
python3 -m pytest .claude/skills/ticket/tests/test_track_lifecycle.py

### 通過數

- test_framework_paths.py: 16 passed（新增 TestIsFrameworkPathBroad 5 個 + 既有 11 個全綠）
- test_framework_rule_edit_skill_trigger.py: 9 passed（PreToolUse hook 維持 strict 無回歸）
- test_track_lifecycle.py: 46 passed（lifecycle.py 改 broad 後回歸全綠）

合計 71 passed / 0 failed。

### 重現驗證（acceptance #6）

執行 inline Python 腳本確認 lifecycle._has_framework_path 對 where.files=['.claude/hooks/commit-msg-layer2-marker-check-hook.py'] 回傳 True，符合 W17-131 ANA 預期（修正前回傳 False）。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-04T00:33:34
**Executing Agent**: basil-hook-architect
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.18.0-W17-131 | chars: 562 -->

### Task Reference
- 0.18.0-W17-131 what: 驗證 W17-125 S 問觸發條件邊界檢查（claim W17-126 時未觸發）

### Rationale Chain
- 0.18.0-W17-131 why: 本次 claim W17-126（where.files=.claude/hooks/commit-msg-layer2-marker-check-hook.py，type=IMP）未觸發 W17-125 落地的 claim WRAP S 問。預期應觸發（framework path + IMP 條件成立）。疑為 W17-127.2 lifecycle.py 改用 lib/framework_paths.is_framework_path() 後，路徑判定範圍未覆蓋本 claim 場景。需檢查 lifecycle.py 條件分支與 SSOT framework-paths.yaml 內容一致性。落地不阻塞（觀察期非實質故障），但若邊界 bug 確認則 S 問防護網存在洩漏，需修正

### Source Acceptance (reference)
- [ref] [ ] 重現實驗：claim W17-126（或其他 framework path IMP）並 trace S 問是否觸發  # from 0.18.0-W17-131
- [ref] [ ] 邊界 bug 確認：是 SSOT 缺路徑、is_framework_path 邏輯、或 lifecycle.py 條件分支  # from 0.18.0-W17-131
- [ref] [ ] 結論明示：bug 確認則建 IMP follow-up；無 bug 則記錄重現條件與環境差異  # from 0.18.0-W17-131