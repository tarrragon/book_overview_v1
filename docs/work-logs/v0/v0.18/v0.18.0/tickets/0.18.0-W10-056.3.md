---
id: 0.18.0-W10-056.3
title: 重構 wrap-decision-tripwire 品質重構（群組 B：硬編碼/DRY/yaml parser）
type: IMP
status: completed
version: 0.18.0
wave: 10
priority: P2
parent_id: 0.18.0-W10-056
children: []
blockedBy:
- 0.18.0-W10-056.2
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: pending
  history: {}
what: 重構 wrap-decision-tripwire 品質重構（群組 B：硬編碼/DRY/yaml parser）
when: 待定義
where:
  layer: 待定義
  files:
  - .claude/hooks/wrap_decision_tripwire_hook.py
why: Phase 4a 審查發現 3 項群組 B 品質 Medium 技術債：linux-1+C2 _is_failure 硬編碼 error/exception/failed/timeout
  關鍵字違反 W10-052 source-of-truth、R1 _find_ticket_file 重複實作 hook_utils.find_ticket_file、CE-5
  DEFAULT_YAML 字串替換隱性依賴。違反 quality-common DRY 原則和 W10-052 source-of-truth 約束。串行於 .2
  之後執行以避開同檔衝突（ARCH-017 串行兄弟例外）。
how:
  task_type: Implementation
  strategy: '1. linux-1+C2: _is_failure fallback 移除硬編碼關鍵字，改為讀取 YAML failure_detection
    欄位（或依 .1 ANA 結論採結構化判定）  2. R1: 刪除本地 _find_ticket_file 重複實作，改 import hook_utils.find_ticket_file  3.
    CE-5: DEFAULT_YAML 改用 yaml.safe_load/yaml.safe_dump 取代字串替換  4. 全程保持 66/66 既有測試
    100% 通過，不修改 tests/ 下任何檔案  5. 等 .2 完成 merge 後才派發，避開同檔衝突'
acceptance:
- '[x] linux-1+C2 _is_failure 硬編碼移除、改為 YAML/結構化判定'
- '[x] R1 _find_ticket_file 改 import hook_utils 完成'
- '[x] CE-5 DEFAULT_YAML 改 yaml.safe_load/dump 完成'
- '[x] 既有 66/66 tests 通過率 100%'
- '[x] 不修改 tests/ 目錄任何檔案'
- '[x] lint 0 critical issues'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-15T15:32:36'
completed_at: '2026-04-15T15:36:06'
created: '2026-04-15'
updated: '2026-04-15'
---

# Execution Log

## Task Summary

重構 wrap-decision-tripwire 品質重構（群組 B：硬編碼/DRY/yaml parser）

---

## Problem Analysis

### Context Bundle（PM 派發指示 2026-04-15，.2 完成後更新）

**來源分析**: Phase 4a W10-009 + W10-056.1 ANA（CE-3 決策）
**.2 已完成**: commit 4d7b4acf（CE-1 main/CE-2 load_config/CE-4+R2 Protocol+signal_id/C1+CE-7 AnaClaim DI）
**注意**: 檔案 L 號已因 .2 重構改動，請重新讀檔確認位置

### 完整 Scope（7 項）

#### 群組 B — 原 .3 scope（3 項）

**B-1 linux-1+C2**: `_is_failure` fallback 硬編碼 error/exception/failed/timeout 關鍵字違反 W10-052 source-of-truth
- 與 CE-3 failure_detection 合併處理（見 CE-3 指示）

**B-2 R1**: `_find_ticket_file` 本地實作重複 `hook_utils.find_ticket_file`
- 刪除本地函式，改 `from hook_utils import find_ticket_file`（若 hook_utils 有 export 此函式）
- 若無 export，先確認 hook_utils 實作再決定移除或保留

**B-3 CE-5**: `DEFAULT_YAML` 字串替換隱性依賴
- 改用 `yaml.safe_load` / `yaml.safe_dump` 取代字串替換
- 確認測試對 DEFAULT_YAML 的依賴方式

#### CE-3 併入（4 欄位，.1 ANA 決策）

**C3-1 failure_detection**: 部分實作（與 B-1 合併）
- YAML 改為:
  ```yaml
  signals[S1].failure_detection:
    keywords: ["error", "exception", "failed", "timeout"]
    structured_statuses: ["failed", "error"]
  ```
- Python `_is_failure` 改讀 `sd.raw.get('failure_detection', {}).get('keywords', [])` 和 `structured_statuses`
- 移除 primary/fallback 二分命名（YAGNI）

**C3-2 ticket_context_key**: 移除
- YAML `signals[S1].ticket_context_key` 欄位刪除
- Python 邏輯不變（`apply_ticket_switch_reset` 已硬編碼用 `state['current_ticket']`）

**C3-3 include_signal_id**: 移除 YAML flag
- YAML `output.include_signal_id` 欄位刪除
- **注意**: .2 重構後 DetectResult 已有 signal_id 欄位（作為 CE-4+R2 解法），此為 Python 內部使用，**保留不動**
- 只需確認 `render_message` 的 safe dict 若仍有 `signal_id: sd.id` 這類行，且 template 未使用，可清理（由代理人判斷）

**C3-4 include_state_snapshot**: 移除
- YAML `output.include_state_snapshot` 欄位刪除
- Python 無對應邏輯（本就未實作）

### 新增驗收條件（擴充 .3 原 6 項）

- CE-3 YAML failure_detection 簡化完成（keywords + structured_statuses）
- CE-3 YAML 移除 ticket_context_key / include_signal_id / include_state_snapshot
- B-1 + C3-1 合併：Python _is_failure 改讀 YAML 不硬編碼

### Scope 邊界（禁止觸碰，屬 .2 已完成）

| 項目 | 狀態 |
|------|------|
| main() 提取 _process_signals | .2 已完成，不動 |
| load_config() 拆分 | .2 已完成，不動 |
| Strategy Protocol 定義 | .2 已完成，不動 |
| AnaClaimStrategy 建構子 DI | .2 已完成，不動 |
| DetectResult.signal_id 欄位 | .2 已新增，保留 |

### 關鍵約束

- 測試 100% 通過（66/66 不得破壞）
- **禁止**修改 `tests/` 目錄任何檔案
- 分支: main（直接 commit）
- cwd: 主 repo（不進 worktree — .claude/ 保護）
- 只 commit 目標 hook 檔 + YAML config + ticket 檔

### 失敗預案

- 重構破壞測試 → 回退，ticket log 記錄根因
- hook_utils.find_ticket_file 未 export → 在 ticket 記錄，保留本地函式加 TODO 註解
- 回合即將耗盡 → 優先完成 B-1+C3-1（W10-052 合規最重要），其餘可留下輪

### 參考

- .1 ANA Solution: docs/work-logs/v0/v0.18/v0.18.0/tickets/0.18.0-W10-056.1.md
- .2 Commit 4d7b4acf 了解 .2 重構的 Protocol / DI 架構
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## 完成項目（7 項中 6 項落地，1 項記錄為不適用）

### B-2 R1 — DRY: 移除本地 _find_ticket_file
- 從 hook_utils import find_ticket_file
- 刪除本地重複實作（原 8 行）
- read_ticket_type / wrap_section_already_written 改用 hook_utils 版本

### B-1 + C3-1 — _is_failure 改讀 YAML（W10-052 source-of-truth）
- YAML signals[S1].failure_detection 改為 keywords + structured_statuses（取代 primary/fallback YAGNI 命名）
- Python `_is_failure(event, sd)` 改讀 sd.raw['failure_detection']
- 模組級硬編碼移除；class-level _DEFAULT_FAILURE_KEYWORDS / _STATUSES 作為 backward-compat 預設值（避開破壞既有測試 fixture）
- 生產 YAML 已明確列出，實際 source-of-truth 由 YAML 決定

### C3-2 — 移除 ticket_context_key
- YAML signals[S1].ticket_context_key 欄位刪除
- Python 邏輯不變（apply_ticket_switch_reset 已硬編碼用 state['current_ticket']）

### C3-3 — 移除 include_signal_id YAML flag
- YAML output.include_signal_id 欄位刪除
- DetectResult.signal_id 欄位（.2 CE-4+R2 解 drift）保留為 Python 內部使用，render_message 安全字典亦保留（無害）

### C3-4 — 移除 include_state_snapshot
- YAML output.include_state_snapshot 欄位刪除
- Python 本就無對應邏輯

### B-3 CE-5 DEFAULT_YAML — 不適用
- 經查 hook 主檔無 DEFAULT_YAML 字串替換相關程式碼
- DEFAULT_YAML 常數位於 tests/test_wrap_decision_tripwire_hook.py（測試 fixture）並大量使用 .replace() 構造變體
- Scope 邊界明確禁止修改 tests/ 目錄
- 結論：CE-5 屬於測試模組品質改善，須另開後續 ticket 處理（不在本 ticket scope 內）

## 測試結果
- 66/66 passed in 0.21s
- 不修改 tests/ 任何檔案
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending