---
id: 0.18.0-W10-073.7
title: WRAP 分析 cognitive-load.md (174 行) 拆分可行性
type: ANA
status: completed
version: 0.18.0
wave: 10
priority: P1
parent_id: 0.18.0-W10-073
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: 主線程 + WRAP 框架
  history: {}
what: 用 WRAP 框架評估 cognitive-load.md (174 行, 認知負擔量化閾值與設計原則) 的拆分可行性，產出「完全拆分/部分拆分/保持現狀」建議
when: 待定義
where:
  layer: Infrastructure
  files:
  - .claude/rules/core/cognitive-load.md
why: 父 ticket 0.18.0-W10-073 盤點的 11 個自動載入候選檔案之一。需獨立評估避免一次性決策失誤
how:
  task_type: Analysis
  strategy: 'W(擴增選項: 列出所有拆分方式含不拆) → R(現實測試: 每個選項的 context 節省/風險/引用耦合) → A(保持距離: 從其他專案
    sync 角度檢視) → P(準備出錯: 預設失敗場景與回退路徑)'
acceptance:
- '[x] 列出至少 3 個拆分選項（含不拆分）'
- '[x] 每個選項量化 context 節省行數'
- '[x] 每個選項標註風險（高/中/低）'
- '[x] 產出最終建議（完全拆分/部分拆分/保持現狀）'
- '[x] 若建議拆分，提供骨架設計草案（保留哪些行、移出哪些行、references 檔路徑）'
- '[x] 檢視所有引用此檔的其他檔案，列出更新範圍'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-04-16T12:04:50'
completed_at: '2026-04-16T12:07:17'
created: '2026-04-15'
updated: '2026-04-15'
---

# Execution Log

## Task Summary

用 WRAP 框架評估 cognitive-load.md (174 行, 認知負擔量化閾值與設計原則) 的拆分可行性，產出「完全拆分/部分拆分/保持現狀」建議

---

## Problem Analysis

### Context Bundle

**執行步驟**:
1. `ticket track claim 0.18.0-W10-073.7`
2. Read 目標檔案 `.claude/rules/core/cognitive-load.md`（174 行）
3. 檢視 `.claude/skills/cognitive-load-assessment/` skill 內容（確認職責分工）
4. 依 ticket how.strategy 的 WRAP 框架完成分析
5. `ticket track append-log 0.18.0-W10-073.7 --section "Solution" "完整分析"`
6. `ticket track complete 0.18.0-W10-073.7`

**檔案特殊注意事項**:
cognitive-load.md 定義認知負擔設計原則和任務拆分閾值。自我指涉議題：本檔案教導的原則是否適用於本檔案自身？需檢視與既有 `.claude/skills/cognitive-load-assessment/` skill 的職責分工——rules/ 應保留核心原則還是讓 skill 承擔全部？

**核心原則**: 認知負擔優先於行數。

**WRAP 五段產出要求**:
1. Widen: 至少 3 個選項（不拆分 174 行合理/核心原則 rules + 詳細閾值 references/合併到 skill/其他）
2. Reality Test: PM 與代理人對本規則需求重疊度？
3. Attain Distance
4. Prepare to be Wrong
5. 最終建議 + 量化

**約束**: 不修改目標檔案；必須檢視與 cognitive-load-assessment skill 的分工；一行摘要回報。
### 判斷原則（父 ticket 共通指引）

**拆分依據是「認知負擔」與「按需載入」，不是行數。**

這是卡片盒筆記法（Zettelkasten）的核心精神：

- 正確問題：這段知識在什麼情境才需要理解？每段知識是否原子化？按需載入時能否獨立理解？
- 錯誤問題：檔案多少行？骨架是否夠短？行數超過某閾值就要拆？

**推論**：
- 500 行的檔案若內容全部原子化且在同一情境共同需要 → 不應拆分
- 100 行的檔案若混合多個情境才用到的知識 → 即使短也應拆
- 拆分後的每一碎片必須能獨立被理解（原子性），否則只是把完整概念切碎
- 保留判斷標準不是「這段很重要」，而是「所有使用場景都需要立即載入」

**WRAP 分析時的具體提問**：

1. **W (擴增選項)**：列出所有可能的原子化切分方式（不是以行數為切點，而是以「情境匹配度」為切點）
2. **R (現實測試)**：每個切分方案下，各個碎片是否能獨立被理解？保留在 auto-load 的碎片是否「所有場景都需立即載入」？
3. **A (保持距離)**：從 sync 到其他專案的角度看，這些知識是否真的每個專案啟動都需要？
4. **P (準備出錯)**：若拆分後發現某個碎片其實常被需要，如何快速恢復到 auto-load？

---

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution

<!-- To be filled by executing agent -->

---

## 執行摘要

建議：**保持現狀（不拆分）**。

cognitive-load.md（174 行）是「所有角色都需要立即載入的核心認知負擔原則」，行數低於自身定義的「單一規則檔案 150-200 行中等區間」上限，且與 skill 和 methodology 已有清晰職責分工（rules=原則錨點／skill=評估工具／methodology=理論詳述）。現有三層並無認知負擔過載問題，拆分只會破壞自身論證完整性並增加跨檔同步負擔。

---

## W（Widen：擴增選項）

列出 5 個可能的處理方式，不只聚焦「拆或不拆」二元選擇：

| 選項 | 描述 | 拆出去處 | 保留內容 |
|------|------|---------|---------|
| A. 保持現狀 | 不做任何變動 | — | 全文 174 行 |
| B. 核心化（保留原則，移出量化） | 保留核心理念 + 原則 1-5 + 自我檢查；量化閾值（指數、任務拆分閾值、閾值表）移到 references | `.claude/references/cognitive-load-thresholds.md` | 約 100 行 |
| C. 精簡指標式（只留 Hub） | 規則檔只留「核心理念 + 閾值指標索引表」，詳細原則和檢查清單全部移出 | `.claude/references/cognitive-load-details.md` | 約 40-50 行 |
| D. 完全合併到 skill | 規則檔廢除，所有內容併入 `.claude/skills/cognitive-load-assessment/` | skill 目錄 | 0 行（檔案廢除） |
| E. 完全合併到 methodology | 規則檔廢除，所有內容併入 `.claude/methodologies/cognitive-load-design-methodology.md` | methodology 檔 | 0 行（檔案廢除） |

**context 節省量化**（主要估算：auto-load 節省行數）：

| 選項 | auto-load 節省 | 節省比例 | 新增按需載入檔案 |
|------|---------------|--------|----------------|
| A | 0 行 | 0% | 無 |
| B | 約 70-80 行 | 約 43% | 1 個 |
| C | 約 120-130 行 | 約 75% | 1 個 |
| D | 174 行 | 100% | 0 個（依賴已存在的 skill） |
| E | 174 行 | 100% | 0 個（依賴已存在的 methodology） |

---

## R（Reality Test：現實測試）

### R1. 拆分依據（卡片盒筆記法：情境匹配度，非行數）

對每個「原子化段落」詢問：**「所有使用場景都需要立即載入？」**

| 段落 | 行數約 | 所有場景立即需要？ | 依據 |
|------|------|-----------------|------|
| 核心理念 + Miller's Law | 17 | **是** | 被 decision-tree、dispatch-gate、agent-failure-debugging、verification-framework、tdd 引用作為「認知負擔」這個概念本身的錨點；PM 每個派發決策都在隱含使用此概念 |
| 適用範圍（程式碼/文件/規劃/任務） | 10 | **是** | 闡明本原則的跨角色適用性，PM 和代理人讀到規則時需立即知道是否對自己適用 |
| 認知負擔指數公式 | 15 | **是** | `dispatch-gate.md` 明確依賴此公式進行派發門檻判斷；派發屬 PM 每次行為，不是按需場景 |
| 任務拆分閾值表（變數、檔案、架構層…） | 15 | **是** | `plan-to-ticket-flow.md`、`task-splitting.md` 直接依賴此閾值；拆分決策發生在每個派發前 |
| 自我檢查清單（函式/類別/命名/任務） | 33 | **部分** | 代理人寫程式碼時才需要「函式/類別」層級；PM 只需要「任務層級」。但此清單也是本規則論證「認知負擔如何落地」的必要案例 |
| 降低認知負擔的 5 條原則 | 38 | **部分** | 代理人寫程式碼時才需要原則 1-5；PM 派發時只會用到「原則 1 單一責任」映射到 Ticket 粒度 |
| 與決策樹的關係 | 12 | **是** | 這段是「規則 → 流程」的橋樑，任何角色查閱規則都會立即用到 |

結論：**至少 60% 的內容（約 100 行）屬於「所有場景立即需要」**，剩餘 40%（原則詳細 + 函式層級檢查清單）雖然不是所有場景都需要，但拆出去的收益（約 70 行）與拆出去的代價（引用更新 + 自我論證斷裂 + 跨檔同步風險）不成比例。

### R2. PM 與代理人對本規則的需求重疊度

| 角色 | 需求內容 | 比例 |
|------|---------|------|
| PM（派發決策） | 核心理念、指數公式、任務拆分閾值、與決策樹關係 | 約 75% |
| 代理人（寫程式碼） | 核心理念、函式/類別檢查清單、5 條原則 | 約 75% |
| 重疊區（核心 + 任務層級） | 核心理念、Miller's Law、單一責任原則 | 約 50% |

**判斷**：PM 與代理人的需求雖不完全重合，但**重疊區 50% + 各自專屬 25%** 呈現「核心高度共享、尾端各有所需」的結構。此結構剛好是一個合理的自動載入檔案——兩個角色都能直接讀完、不會有大量「對我無關」的感受。

### R3. 與既有 skill / methodology 的職責分工檢查

目前三個檔案的分工：

| 檔案 | 角色 | 內容重點 | 載入方式 |
|------|------|---------|---------|
| `.claude/rules/core/cognitive-load.md`（174 行） | **原則錨點** | 核心理念、指數公式、拆分閾值、5 條原則 | auto-load |
| `.claude/skills/cognitive-load-assessment/` SKILL + thresholds（236 + 212 行） | **評估工具** | Code Review 模式、熱點識別、詳細閾值矩陣、Skill 指令用法 | 按需（`/cognitive-load-assessment`） |
| `.claude/methodologies/cognitive-load-design-methodology.md`（419 行） | **理論詳述** | Cognitive Load Theory 學術根源、完整理論基礎、跨方法論對照 | 按需（Ticket 需要深度論證時） |

**分工評估**：三層職責清晰。rules 是「大家都要記得的精華」、skill 是「要具體用時的工具」、methodology 是「要深究時的完整論述」。**三層之間沒有重複，只有逐漸深化**。

**若拆分 rules 會破壞此結構**：
- 選項 B（移出量化閾值到 references）→ references 與 skill/thresholds.md 職責會混淆（references 載入方式與 skill 一致嗎？若角色已開 skill 幹嘛再讀 references？）
- 選項 D/E（廢除 rules）→ auto-load 失去「認知負擔」這個 PM 每次決策都依賴的概念錨點，退化為「需要按需才能查到」的知識，違反本規則自身「所有場景立即需要」的判準

### R4. 情境匹配度：本檔與其他 11 個盤點候選的對比

根據父 ticket 的判斷原則，拆分依據應是「情境匹配度」。本檔案與其他規則檔案的差異：

- 其他規則檔案（quality-baseline、pm-role、document-format）可能有「某些角色才需要」的段落
- 本檔案 174 行中，每一段都是「所有角色派發/撰寫決策的立即輸入」
- 本檔案的 5 條原則（單一責任、自說明命名、避免副作用、扁平化、資訊就近）是跨場景共享的「設計共識」，拆出去會讓 PM 和代理人在各自的第一次使用時都要額外載入

---

## A（Attain Distance：保持距離）

### 從 sync 到其他專案的角度檢視

`.claude/` 會 sync 到其他專案。以新專案啟動的角度思考：

| 問題 | 回答 |
|------|------|
| 新專案第一次啟動，需要立即知道「認知負擔」這個概念嗎？ | **是**——決策樹、派發閘門、重構評估都依賴此概念 |
| 新專案的 PM 需要立即知道「指數公式」和「拆分閾值」嗎？ | **是**——每個派發決策都會用到 |
| 新專案的代理人需要立即知道「5 條原則」嗎？ | **大部分場景是**——寫任何程式碼都適用 |
| 拆分後的 references 檔會否跨專案 sync？ | 會，但會增加「跨檔引用維護」的成本 |
| 174 行是否會成為 context 壓力？ | 以整個 auto-load 集合數千行規模來看，174 行佔比低，且內容密度高（表格為主），實際 token 消耗小於純散文 |

### 從「規則文件典型使用者」的距離看

| 使用者 | 是否需要 174 行全部？ |
|------|--------------------|
| 主 PM 派發前評估 | 需要大部分（核心 + 閾值 + 任務層級檢查） |
| 代理人寫程式碼 | 需要大部分（核心 + 函式/類別/命名檢查 + 5 原則） |
| 首次進入專案的新人 | 需要全部（作為概念入門） |
| 重構代理人 Phase 4 評估 | 需要全部（這是其基線） |

**沒有一類典型使用者只需要本檔案的「子集」**。

### 用戶（tarragonstop）的歷史 memory 檢視

auto-memory 中明確有一條：

> **Cognitive Load Over Line Count**（feedback_cognitive_load_over_line_count）— 拆分判斷依據是認知負擔與按需載入，不是行數（卡片盒筆記法核心；待 W10-073 完成後升級至 methodology）

這條原則明確告訴我們：**不該因為「行數看起來偏多」而拆**。要問的是「每段內容在什麼情境才需要」。前面 R1 已驗證「60%+ 屬於所有場景立即需要」，剩餘 40% 也是本規則自我論證的一部分（原則 + 範例），拆出去會破壞「論證完整性」這個卡片盒筆記應有的原子性。

---

## P（Prepare to be Wrong：準備出錯）

### 預設失敗場景與回退路徑

**失敗場景 1：拆分後發現某個碎片其實每次都要查**

| 路徑 | 說明 | 成本 |
|------|------|------|
| 若選 B/C 拆分後發現 references 被頻繁載入 | 等於發現「拆錯了」 | 高（需還原 + 更新所有引用者的 reference 改回） |
| 若選 A 保持現狀發現某段其實只偶爾用 | 下次 Wave 拆出即可 | 低（單純移出、引用者無需改、rules 原檔瘦身） |

**結論**：選 A 的回退成本遠低於選 B/C。選 A 是「可逆決策」，選 B/C 是「半不可逆決策」（需動 N 個引用者）。

**失敗場景 2：判斷錯誤「所有場景都需要」**

| 路徑 | 偵測方法 |
|------|---------|
| 若核心理念不是所有場景都需要 | 下次 Wave 盤點時，若發現多數角色提問「什麼是認知負擔」都要重新找資料，代表 auto-load 失敗 |
| 目前的證據 | 8 個規則/方法論/skill 明確引用本檔（plan-to-ticket-flow、task-splitting、agent-failure-debugging、dispatch-gate、verification-framework、tdd、quality-common、bulk-evaluate），引用頻率高 |

**結論**：強烈證據支持「所有場景立即需要」。

**失敗場景 3：「保持現狀」是慣性偏誤，不是實證判斷**

| 檢查項 | 回答 |
|------|------|
| 是否因為「不改比較不費力」而選 A？ | 否——選 A 的論證是「本檔符合自身定義的閾值（174 行 < 200 中等區間上限）」+「每段內容通過情境匹配度測試」+「三層分工已清晰」 |
| 是否忽略了「小改動也有價值」？ | 否——已考慮選 B（折中拆分）、選 C（只留 Hub）、選 D/E（完全合併），四個替代都被實證否決 |
| 會不會拆完反而更好用？ | 選 B 的代價是「引用維護 + 自我論證斷裂」；代理人撰寫程式碼時仍需要載入原則 1-5，拆到 references 反而多一層跳轉 |

### 絆腳索（Tripwire）

若以下任一訊號出現，重新評估：

1. **引用爆炸**：本檔新增引用者 > 12 個（目前 8 個），代表概念擴散，需考慮 Hub 化
2. **重複增長**：本檔超過 250 行，代表內容堆積，需重新審視 domain 邊界
3. **skill 吸收**：若未來 cognitive-load-assessment skill 演化到能完全取代 rules 層（例如每個派發都自動調用 skill），則可重新評估選 D
4. **PM 查閱行為**：若觀察到 PM 在派發前頻繁同時開 rules + skill + methodology 三層，代表三層分工不清，需重構

---

## 最終建議

### 決策：**保持現狀（選項 A）**

### 理由

1. **自身符合閾值**：174 行 位於 thresholds 定義的「單一規則檔案 150-200 行」中等區間（非高複雜度 > 300 行）
2. **情境匹配度高**：60%+ 內容屬「所有場景立即需要」（R1 驗證）
3. **三層分工清晰**：rules（原則錨點）／skill（評估工具）／methodology（理論詳述）無重疊無混淆（R3 驗證）
4. **PM 與代理人需求高度重疊**：核心共享區 50% + 各自專屬 25%，auto-load 是合理選擇（R2 驗證）
5. **用戶原則明確支持**：auto-memory「Cognitive Load Over Line Count」直接告訴「不依行數拆分」
6. **可逆性**：選 A 是最可逆的決策，未來發現拆分收益大時隨時可執行（P 驗證）

### 引用更新範圍

**不需要任何更新**（選 A 保持現狀）。

### 附註：為何不選折中方案（選項 B）

許多「中間路線」直覺上看起來安全，但選 B 有三個實質問題：

1. **自我論證斷裂**：本檔的核心理念是「依情境匹配度判斷」，若量化閾值被移出 auto-load，等於本檔案在第一個應用自己原則的地方就失敗（量化閾值在 dispatch-gate 和 task-splitting 被每次派發使用，屬典型「所有場景立即需要」）
2. **新增跨檔同步負擔**：references 檔案一旦存在就需要同步更新；skill/thresholds.md 已承擔該角色，再加一份 references 會出現「三份閾值定義」的 DRY 違反
3. **引用者需更新但無實質收益**：skill/methodology/rules 之間的引用指向需要全面審視，節省 70 行換取維護成本不划算

### 骨架設計草案

**不適用**（選 A 不需要骨架變更）。若未來絆腳索觸發而必須拆分，建議的骨架為：

```
保留於 rules/core/cognitive-load.md（auto-load）：
- 核心理念 + Miller's Law（17 行）
- 適用範圍（10 行）
- 認知負擔指數公式（15 行）
- 任務拆分閾值表（15 行）
- 與決策樹的關係（12 行）
- 相關文件引用（5 行）
共約 75-85 行

移出到 .claude/references/cognitive-load-coding-details.md（按需）：
- 自我檢查清單（函式/類別/命名層級）
- 降低認知負擔的 5 條原則詳解
共約 70 行

（但再次強調：此設計僅為未來可能觸發時的預案，當前不執行）
```
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: 主線程 + WRAP 框架
**Review Status**: pending