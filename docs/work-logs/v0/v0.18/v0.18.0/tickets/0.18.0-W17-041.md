---
id: 0.18.0-W17-041
title: 實作 Session-start hook 新增 spawned pending 提醒
type: IMP
status: in_progress
version: 0.18.0
wave: 17
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W17-036
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: create-imp-from-ana-solution
  rationale: W17-036 Solution 軸 D 補強建議；降級理由：IMP-3 handoff 模板已覆蓋主要防護，本項為補強
who:
  current: pending
  history: {}
what: Session-start hook 新增掃描 spawned_tickets pending 項目並提醒 PM；區分原生 pending 與 spawned
  衍生 pending
when: SessionStart hook 觸發時（每 session 啟動）
where:
  layer: Infrastructure
  files:
  - .claude/hooks/session-start-scheduler-hint-hook.py
why: W17-036 軸 D 缺口：session-start 摘要不區分原生 pending 與 spawned 衍生 pending，PM 可能選錯下一步。本
  IMP 降級為 P2（非阻塞性優化，IMP-3 handoff 模板已覆蓋主要防護）
how:
  task_type: Implementation
  strategy: session-start-scheduler-hint-hook.py 擴充：查詢 completed ANA 的 spawned_tickets
    pending 狀態，於 scheduler hint 中額外列出 spawned 推進清單
acceptance:
- '[ ] Hook 查詢 completed ANA 的 spawned pending'
- '[ ] Scheduler hint 額外列出 spawned 推進清單'
- '[ ] 提示文字區分 spawned 與原生 pending'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-21T13:49:07'
completed_at: null
created: '2026-04-21'
updated: '2026-04-21'
---

# Execution Log

## Task Summary

Session-start hook 新增掃描 spawned_tickets pending 項目並提醒 PM；區分原生 pending 與 spawned 衍生 pending

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

W17-036 軸 D 缺口：session-start hook 只顯示 runqueue 的下一個可執行 Ticket，PM 看不到「已完成 ANA 的 spawned IMP 仍 pending」的信號。即便 IMP-3 (W17-039) handoff 模板已覆蓋主要防護，session 間切換仍需 PM 主動查詢。

### 影響範圍

- .claude/hooks/session-start-scheduler-hint-hook.py（目標）
- .claude/hooks/tests/test_session_start_scheduler_hint_hook.py（測試擴充）

### 相關 Error Pattern

PC-075（spawned-children-status-check-asymmetric）：本 Hook 為 PC-075 下游傳播路徑的補強訊號通道。

### 實作方向（選 A：Hook 內部解析 ticket md 檔）

流程：
1. 解析 ticket_id 從環境偵測當前 version，掃描該版本所有 ticket md
2. 找 type=ANA + status=completed + spawned_tickets 非空的 ticket
3. 對每個 spawned，讀取 frontmatter 檢查 status，篩選 pending
4. 彙整為獨立小節（Spawned 推進清單）加入 additionalContext

效能考量：
- 僅掃描當前 version（~500 檔）；find_ticket_file 由 ID 直解版本，避免全量掃描
- 限制最多顯示 5 筆（避免洗版）
- 失敗靜默降級（回傳 None 即只顯示原排程提示）

複用現有工具：
- hook_utils.scan_ticket_files_by_version 取當前版本 tickets
- hook_utils.parse_ticket_frontmatter 解析 frontmatter
- hook_utils.get_project_root 取專案根目錄
- 複用 TERMINAL_STATUSES（terminal=completed/closed）判斷 pending
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.18.0-W17-036 | chars: 641 -->

## Context Bundle（PM 準備給 basil-hook-architect 執行 W17-041）

### 任務脈絡

本 Ticket 是 W17-036 軸 D 的補強 IMP（P2），目標：session-start hook 擴充掃描 completed ANA 的 spawned pending 項目並在 scheduler hint 中區分顯示。

**W17-036 簡要**：ANA complete 後 spawned IMP 無推進引導；四軸分析（A decision-tree、B priority 繼承、C runqueue 加權、D handoff/session-start 提醒），軸 A+B+C 已落地（W17-037/038/042 completed），軸 D 由 W17-039（handoff 模板，PM 同步處理）+ W17-041（本 Ticket，session-start hook）組成。

**降級理由**（ticket frontmatter）：W17-041 降為 P2，IMP-3（W17-039）handoff 模板已覆蓋主要防護；session-start 僅為二重補強。

### 必讀檔案清單

1. `.claude/hooks/session-start-scheduler-hint-hook.py`（目標檔，143 行已存在）
2. `.claude/hooks/hook_utils.py`（統一日誌工具，查看 setup_hook_logging/run_hook_safely）
3. `.claude/rules/core/observability-rules.md`（日誌可觀測性規則 4 — catch 需日誌；規則 2 — 統一日誌工具）
4. `.claude/error-patterns/process-compliance/PC-075-spawned-children-status-check-asymmetric.md`（下游傳播路徑，W17-042 剛擴充）
5. `docs/work-logs/v0/v0.18/v0.18.0/tickets/0.18.0-W17-036.md`（本 ANA 的 Solution 軸 D 建議動作）

可選補充：`.claude/skills/ticket/SKILL.md`（了解 runqueue/deps/list 子命令是否可複用）

### 當前 Hook 結構速覽

```python
fetch_scheduler_context(logger) -> Optional[str]:
    # Step 1: resume（有 handoff 待恢復）
    # Step 2: fallback next（--format=list --top 1）
    # Step 3: 皆無 → None

build_hook_output(scheduler_context) -> Dict[str, Any]:
    # 包裝為 hookSpecificOutput.additionalContext
```

### 實作方向（策略選項，basil 自選最適）

**選項 A：Hook 內部解析 ticket md 檔**
- 掃描 `docs/work-logs/v*/tickets/*.md`，解析 frontmatter
- 找 `type: ANA` + `status: completed` + `spawned_tickets` 含 pending 的清單
- 優點：無需新 CLI 命令；缺點：hook 啟動時多次 file IO

**選項 B：新增 ticket CLI 子命令並呼叫**
- `ticket track spawned-pending` 新子命令（另開 Ticket）
- 本 Hook 呼叫之
- 優點：責任清楚；缺點：需先等 CLI 子命令建立（本 ticket 範圍擴大）

**選項 C：複用既有 CLI（若有足夠篩選能力）**
- 檢查 `ticket track list --status completed --type ANA` 能否搭配 deps 快速聚合
- 評估後可能需組合 2-3 次 CLI 呼叫

**推薦**：選項 A（hook 內部解析），避免本 Ticket 範圍擴展；若發現實作複雜度超出預期，回報 NeedsContext 由 PM 決定是否改走選項 B。

### 驗收條件（3 項，對應 ticket frontmatter acceptance）

1. Hook 查詢 completed ANA 的 spawned pending
2. Scheduler hint additionalContext 額外列出 spawned 推進清單
3. 提示文字區分 spawned 與原生 pending（例如兩個獨立小節 `## 排程提示` + `## Spawned 推進清單`）

### 約束

- **路徑限制**：本 Ticket 只改 `.claude/hooks/session-start-scheduler-hint-hook.py`（若選項 B/C 需建新檔，先 NeedsContext 回報）
- **ARCH-015 v2.1.114 實證**：主 repo cwd 內 `.claude/` 子目錄（非 Bash 寫入）subagent Write/Edit 可行；basil 工具集含 Write/Edit/Bash，寫入 .claude/hooks/ 應通行（Bash 側通道若被擋改用 Write/Edit）
- **可觀測性**：catch 區塊需日誌（observability-rules.md 規則 1）；啟動/錯誤/完成三階段 log 已有模板，維持一致
- **失敗不阻塞**：SessionStart hook 禁止拋出例外阻塞 session；所有錯誤走 logger.error + 靜默（或回傳 suppressOutput=True）
- **超時**：新增掃描邏輯必須在 `CLI_TIMEOUT_SECONDS=8` 秒預算內完成；file IO 建議限制檔案數（> 100 檔時只取最新幾版本）

### 執行流程（強制）

1. `ticket track claim 0.18.0-W17-041 --yes` — 認領 Ticket（非互動環境必 --yes）
2. 讀上述必讀檔案建立認知
3. （可選）`ticket track append-log 0.18.0-W17-041 --section "Problem Analysis"` 記錄實作方向與選項
4. 執行實作（Edit hook + 寫測試若有 test_hooks/ 目錄）
5. 驗證：手動執行 hook（`echo '{}' | uv run .claude/hooks/session-start-scheduler-hint-hook.py`）確認 JSON 輸出
6. `ticket track append-log 0.18.0-W17-041 --section "Solution"` 記錄實作摘要（heredoc 傳長文字）
7. `ticket track append-log 0.18.0-W17-041 --section "Test Results"` 記錄測試輸出
8. `ticket track check-acceptance 0.18.0-W17-041 --all` 勾選全部 AC
9. `ticket track complete 0.18.0-W17-041` 標記完成（hook 會檢 spawned、body schema，不通過會擋）
10. Commit（git add + commit 獨立 Bash 呼叫，不串接）；commit msg 含 Ticket ID

### Exit Status 回報（W17-010 協議）

結束時以 YAML 寫入 ticket `Exit Status` section（ticket track append-log --section "Exit Status"）：

```yaml
status: success|needs_context|blocked|partial_success|failed
reason: ""
confidence: 0.0-1.0
acceptance_met: [0, 1, 2]
acceptance_unmet: []
artifacts:
  - .claude/hooks/session-start-scheduler-hint-hook.py
context_dependencies:
  - docs/work-logs/v*/tickets/*.md (frontmatter parsing)
estimated_recovery_effort: ""  # 若 needs_context/blocked 才填
```

### 注意事項

- 禁止：修改 `.claude/skills/ticket/`、`.claude/rules/`、其他 Ticket body 等非本 Ticket 範圍檔案
- 禁止：串接 git commit 與其他 git 寫入操作（每個 git write 獨立 Bash 呼叫）
- 遵守：bash-tool-usage-rules.md 規則五（長文字走 heredoc 不走 /tmp）
### Task Reference
- 0.18.0-W17-036 what: ANA completed 後 spawned IMP pending 無推進引導機制，分析缺口範圍並設計落地規則

### Rationale Chain
- 0.18.0-W17-036 why: PC-075 揭露 acceptance-gate 對 spawned 只檢存在性不檢狀態；W17-022/W12-002 實證 ANA completed 後衍生 IMP 全 pending 且 priority 割裂（P1 ANA 衍生 P2 IMP）；decision-tree 第八層僅管 ANA 自身 complete 不管 spawned 推進；handoff 可能誤判 Wave 完成

### Related Files
- .claude/pm-rules/decision-tree.md  # from 0.18.0-W17-036
- .claude/pm-rules/ticket-lifecycle.md  # from 0.18.0-W17-036
- .claude/pm-rules/completion-checkpoint-rules.md  # from 0.18.0-W17-036
- .claude/pm-rules/agent-dispatch-enforcement.md  # from 0.18.0-W17-036

### Source Acceptance (reference)
- [ref] [ ] 缺口範圍完整分析涵蓋四軸（decision-tree/priority 繼承/Wave 判定/handoff 觸發）  # from 0.18.0-W17-036
- [ref] [ ] 產出 spawned-ticket-execution-guidance.md 設計雛形（含觸發時機、推進規則、責任歸屬）  # from 0.18.0-W17-036
- [ref] [ ] 每軸衍生至少 1 張 IMP Ticket 並使用 --source-ticket 綁定  # from 0.18.0-W17-036
- [ref] [ ] 評估是否升級為 PC error-pattern（PC-075 擴充 or 新增 PC）  # from 0.18.0-W17-036