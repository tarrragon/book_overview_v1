---
id: 0.18.0-W14-002
title: 修復 test_ac_drift_regression stale 警告天數分級邊界 regression
type: IMP
status: completed
version: 0.18.0
wave: 14
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.18.0-W14-008
- 0.18.0-W14-009
- 0.18.0-W14-010
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: 建立 IMP 追蹤 W12-005 實作期間發現的既有技術債
  rationale: quality-baseline-rule-5：所有發現必須追蹤
who:
  current: rosemary-project-manager
  history: {}
what: 修復 test_ac_drift_regression stale 警告天數分級邊界 regression
when: 待定義
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/tests/test_ac_drift_regression.py
why: W12-005 實作期間發現 test_ac_drift_regression 2 failed，stale 警告天數閾值輸出 [WARNING] 14
  天而非測試預期的 [INFO] 9 天
how:
  task_type: Implementation
  strategy: 待定義
acceptance:
- '[x] stale 警告分級 INFO/WARNING 邊界值輸出正確'
- '[x] 測試全綠'
- '[x] 無回歸'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-17T14:36:44'
completed_at: '2026-04-17T14:49:32'
created: '2026-04-17'
updated: '2026-04-17'
---

# Execution Log

## Task Summary

修復 test_ac_drift_regression stale 警告天數分級邊界 regression

---

## Problem Analysis

### Context Bundle（並行評估審查）

**審查範圍**：本 Ticket 完成的修復（.claude/skills/ticket/ticket_system/commands/lifecycle.py:1340-1345）。

**Diff**：

```diff
 def execute_claim(args: argparse.Namespace, version: str) -> int:
     """...PROP-010 方案 4：claim 前若建立 >= 7 天輸出 stale 提示"""
     try:
-        from ticket_system.lib.ticket_loader import load_ticket
         from ticket_system.lib.staleness import format_stale_warning

         ticket = load_ticket(version, args.ticket_id)
         if (warning := format_stale_warning(ticket)):
             print(warning)
     except Exception as exc:
         sys.stderr.write(f'[staleness] claim 前檢查異常：{exc}\n')
```

**背景**：原 local re-import 建 local binding 遮蔽 module-level（line 24），導致 `unittest.mock.patch('...lifecycle.load_ticket')` 失效。修復：刪 1 行 local import。

**測試結果**：4/4 target passed；1139 regression passed（26 errors 為既存 pytest-mock 缺依賴，已另建 W14-008）。

**審查任務（Quality + Efficiency 視角）**：
1. 修復是否引入 bug/邏輯錯誤/回歸？
2. `format_stale_warning` 仍 local import，不對稱設計是否合理（staleness 允許靜默降級）？
3. try/except 捕獲範圍是否仍正確？
4. 效能是否實質影響？

**審查任務（linux Good Taste 視角）**：
1. verdict: garbage / acceptable / good taste
2. 原 local import 為何會產生？作用域誤解還是防禦性 import？
3. format_stale_warning 留 local import 的不對稱值得嗎？
4. execute_claim 和 claim_with_verification 兩層都碰 ticket loading，是否有架構味道問題？

**Explore 視角已完成**（同家族地雷）：
- track_query.py:208-211 三處 staleness local import
- handoff.py:216 validate_acceptance_criteria local import
- 建議：format_stale_warning 提升 module-level
### 根因定位

**直接原因**：`execute_claim`（.claude/skills/ticket/ticket_system/commands/lifecycle.py:1341）函式體內有本地 re-import：

```python
def execute_claim(args, version):
    try:
        from ticket_system.lib.ticket_loader import load_ticket  # 本地 re-import
        from ticket_system.lib.staleness import format_stale_warning
        ticket = load_ticket(version, args.ticket_id)
```

該 local import 建立新的函式作用域 binding，遮蔽 module-level 的 `load_ticket`（line 24 已匯入）。unittest.mock.patch("ticket_system.commands.lifecycle.load_ticket", ...) 只能攔截 module-level 名稱，無法攔截 local binding，因此 mock 失效。

### 實際執行流程（測試場景）

1. test fixture 建立 ticket dict：created = today - 9 天
2. patch lifecycle.load_ticket → mock 就緒（但被 local import 繞過）
3. execute_claim 執行 local import → 取到真實 load_ticket
4. load_ticket 從磁碟讀取實際的 W3-001.md（docs/work-logs/v0/v0.18/v0.18.0/tickets/0.18.0-W3-001.md）
5. 該檔案 created: 2026-04-03，today 2026-04-17 → age = 14 天
6. format_stale_warning 輸出 `[WARNING] ... 14 天（>= 14 天）` 而非測試預期的 `[INFO] ... 9 天（>= 7 天）`

### 影響範圍

- **目標檔案**：.claude/skills/ticket/ticket_system/commands/lifecycle.py（唯一待修改）
- **測試檔案**：.claude/skills/ticket/tests/test_ac_drift_regression.py（驗證但不修改）
- **失敗測試**：test_regression_stale_info_warning_emitted、test_regression_all_three_signals_co_occur
- **通過測試**：test_regression_ac_achieved_message_emitted、test_regression_claim_rejected_exit_code_one（不檢 stale 級別）

### 相關 Error Pattern

屬 PC-068「Phase 3a 既有 utility 調用時未驗證邊界」變體。W14-001 為同家族前例（load_ticket 隱式 get_project_root 依賴）。

### 修復策略（Phase 3b）

**方案 A（推薦）**：移除 line 1341 的本地 re-import，改用 module-level 已匯入的 load_ticket

- 最小變動：刪除 1 行
- 恢復 mock 相容：測試 patch 可正確攔截
- 不影響生產：module-level load_ticket 本來就是同一個函式
- format_stale_warning 保留 local import（module-level 未匯入，且 try/except 包覆允許 import 失敗不阻擋 claim）

**驗證**：執行 pytest tests/test_ac_drift_regression.py，期待 4/4 通過；執行 pytest tests/ 確認無 regression
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

### Phase 4 重構評估

**評估結果**：無需重構

**評估依據**：
- 認知負擔指數：變更為「刪除 1 行 local import」，複雜度降低
- DRY：消除 module-level 與 local re-import 的符號重複定義
- 命名：無新命名引入
- 架構改進空間：已派生至 W14-009（消除 commands/ 模組同家族 local re-import 地雷）和 W14-010（ANA 評估 stale-check 架構抽象）
- linux 品味審查：approve clean 1-line fix，不對稱設計（format_stale_warning 仍 local）之爭論已交由 W14-009/W14-010 延後處理

**Parallel-evaluation 結論**：
- Reuse: 同家族地雷盤點完成，建 W14-009
- Quality+Efficiency: approve clean 1-line fix
- linux Good Taste: acceptable，deeper 架構氣味建 W14-010
### 修復位置

.claude/skills/ticket/ticket_system/commands/lifecycle.py L1340-1345

### 變更內容

移除 execute_claim 函式體內多餘的 local re-import `from ticket_system.lib.ticket_loader import load_ticket`（舊 L1341），保留 module-level 已匯入的版本（L24）。

### Diff 摘要

```diff
     # Stale 提示（pending 超過 7 天；靜默失敗不影響 claim 主流程）
     try:
-        from ticket_system.lib.ticket_loader import load_ticket
         from ticket_system.lib.staleness import format_stale_warning

         ticket = load_ticket(version, args.ticket_id)
```

### 變更理由

- module-level `load_ticket`（lifecycle.py:24）已提供此符號，函式內重複 import 只會建立新的 local binding
- local binding 導致 `unittest.mock.patch("ticket_system.commands.lifecycle.load_ticket")` 在測試中失效，被 local 名稱遮蔽
- `format_stale_warning` 保留 local import（staleness 非 module-level 匯入，且 try/except 允許降級）

### 不變更項

- staleness.py 閾值（INFO/WARNING/CRITICAL = 7/14/30 天）維持不變
- test_ac_drift_regression.py 測試程式碼未修改（本即為正確測試規格）
- load_ticket 函式本體未動
- claim_with_verification 流程未動
## Test Results

### 目標測試

```
pytest tests/test_ac_drift_regression.py
4 passed in 0.08s
```

- test_regression_stale_info_warning_emitted: PASSED
- test_regression_ac_achieved_message_emitted: PASSED
- test_regression_claim_rejected_exit_code_one: PASSED
- test_regression_all_three_signals_co_occur: PASSED

### 回歸測試

```
pytest --ignore=tests/test_create_duplicate_detection.py
1139 passed in 6.39s
```

### 既有 Known Issue（不屬本次修復範圍）

`test_create_duplicate_detection.py` 26 項 `fixture 'mocker' not found` ERROR（pytest-mock 未裝），屬既存 pre-existing 技術債，已另建 Ticket 追蹤（見 spawned_tickets）
## Completion Info

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending