---
id: 0.18.0-W17-008.10
title: 分析 version 命令 positional vs --version 參數命名歧義
type: ANA
status: pending
version: 0.18.0
wave: 17
priority: P2
parent_id: 0.18.0-W17-008
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.18.0-W17-013
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: create-child-w17-008-batch
  rationale: W17-008 group children；W17-004 落差 12；P2 命名歧義評估
who:
  current: rosemary
  history: {}
what: 分析 track version 命令的 positional version_str 與 --version flag 兩者命名令人混淆；產出使用場景說明或重命名建議
when: 待定義
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/cli/track.py
  - .claude/skills/ticket/references/track-command.md
why: W17-004 落差 12：version_str（positional）與 version_param（--version）易混淆；文件無使用場景說明
how:
  task_type: Analysis
  strategy: 讀 track.py:395-397 實際行為 → 區分兩者角色 → 設計文件範例或重命名方案 → 考慮向後相容
acceptance:
- '[ ] version_str vs version_param 角色對照表'
- '[ ] 使用場景 ≥2 例'
- '[ ] 重命名建議（若需，含相容策略）'
- '[ ] multi_view_status'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-04-20'
updated: '2026-04-20'
---

# Execution Log

## Task Summary

分析 track version 命令的 positional version_str 與 --version flag 兩者命名令人混淆；產出使用場景說明或重命名建議

---

## Problem Analysis

## Context Bundle

### 背景（W17-004 落差 12，P2 ANA）
- 症狀：`track version` 命令 positional `version_str` 與 `--version` flag 兩者命名令人混淆
- 實作：track.py:395-397 定義 `version_str` 和 `version_param`
- 文件 track-command.md 未清楚說明兩者使用場景

### 實作範圍
- `.claude/skills/ticket/cli/track.py` line 395-397 version 命令
- `.claude/skills/ticket/references/track-command.md`
- 本 ticket 為 ANA：分析兩者角色並產重命名或文件補完建議

### 分析步驟
1. 讀 track.py:395-397 確認兩者實際行為（version_str 是目標 ticket 的 version？version_param 是 CLI 全域 --version flag？）
2. 區分兩者角色（顯式 + 隱式）
3. 設計使用場景 ≥2 例
4. 重命名建議（若需，含向後相容策略）或純文件補完
5. ROI 評估：重命名成本 vs 混淆頻率

### 驗證方式
- version_str vs version_param 角色對照表
- 使用場景清單
- 重命名決策（或不重命名理由）
- multi_view_status

### 與其他 ticket 關係
- group: W17-008
- source chain: W17-004（落差 12）→ W17-008 → 本 ticket
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution

<!-- To be filled by executing agent -->

---

## 分析結論

### 1. 角色對照表

| 參數 | 類型 | 實際角色 | 目前使用狀態 |
|------|------|---------|------------|
| `version_str` | positional 必填 | 指定版本號查詢目標（如 `0.31.0`） | 有效使用（track_query.py:540） |
| `--version` → `version_param` | optional flag | 預期備選版本指定方式 | 未使用（dead code） |

### 2. 使用場景分析

**使用場景 1 - 推薦方式（目前使用）**
```bash
/ticket track version 0.31.0
# 等價於：/ticket track version v0.31.0
# 執行流程：args.version_str = "0.31.0" → 正常執行
```

**使用場景 2 - 嘗試使用 --version flag（功能未實現）**
```bash
/ticket track version --version 0.31.0
# 預期行為：指定版本進度摘要
# 實際行為：args.version_param 被設定但從未被讀取
# 結果：TypeError（args.version_str 缺失）
```

### 3. 核心問題

**混淆點**：
- `--version` 在 Unix CLI 慣例中通常指程式版本（如 `git --version`），但此處表示"目標版本"
- `ARG_VERSION_STR` 描述「版本號（如 0.31.0 或 v0.31.0）」vs `ARG_VERSION_PARAM` 描述「版本號（自動偵測）」—— 兩者文件說明互相矛盾
- `version_param` 定義但未使用 = dead code，造成維護負擔

**根因**：設計中可能原意提供多種版本指定方式（positional 或 flag），但實現只完成 positional，導致不一致

### 4. 重命名建議與向後相容策略

#### 方案 A：移除 --version flag（推薦）
**根據**：
- `version_param` 完全未使用
- 單一職責：positional argument 清晰且必須
- 簡化 CLI 表面積

**實施步驟**：
```python
# 移除第 397 行：
# p_version.add_argument("--version", dest="version_param", help=...)

# 改進幫助文本（第 396 行）：
p_version.add_argument("version_str", help="版本號（如 0.31.0 或 v0.31.0；v 前綴可選）")
```

**向後相容**：
- 如未曾有使用者呼叫 `track version --version 0.31.0`，移除無影響
- 若需保留相容性，可在 execute_version() 中檢查 args.version_param 並優先使用

#### 方案 B：保留並實現 --version flag（代價大）
**若選此方案**：
1. 在 track_query.py:540 改為：
   ```python
   target_version_str = args.version_param or args.version_str
   ```
2. 更新幫助文本為更清晰的名稱（見方案 C）
3. 維護成本：每個版本命令變體都需測試

#### 方案 C：重命名以澄清語意（最佳實踐）
若選方案 B，建議改為：
```python
# 更清晰的旗標名稱
p_version.add_argument("--target-version", dest="target_version", 
                      help="目標版本號（可選；預設取 positional 參數）")
```
這樣：
- 避免與全域 `--version` 衝突
- 名稱與角色一致
- 文件與實作相符

### 5. 實驗報告

**實驗方法**：
通過程式碼審查驗證參數使用情況。

**實驗執行**：
- grep 搜尋 `version_param` 全項目：僅發現定義，無使用點
- 追蹤 execute_version() 邏輯：僅讀取 args.version_str
- 確認 version 命令解析流程（track.py:395-397）

**實驗發現**：

1. **確認事實**：
   - `version_str`（positional）實際使用，流程清晰
   - `--version` flag 已定義但未使用，構成 dead code
   - 無現存使用者依賴 `--version` 功能（無測試覆蓋）

2. **未驗證假設**：
   - 原設計是否有意提供兩種方式？（無文件記錄）
   - 是否曾有用戶反饋 positional argument 難用？（無 issue 記錄）

### 6. ROI 估算

| 維度 | 評估 |
|------|------|
| **重命名成本** | 低 — 僅涉及 track.py 第 397 行 + help 文本（10 分鐘）|
| **向後相容成本** | 無 — 該功能從未被使用 |
| **混淆頻率** | 中 — 偶有新用戶嘗試 `--version` 報錯 |
| **文件補完成本** | 低 — 更新 track-command.md（15 分鐘） |
| **淨益** | 高：消除 dead code + 提升可維護性 |

### 7. 建議決策

**建議方案**：方案 A（移除 --version flag）
**理由**：
- version_param 完全未使用 = 明確的 dead code
- positional argument 是必須參數，符合語義
- 簡化 CLI，降低認知負荷
- 0 向後相容成本（功能從未被使用）
- 估計實施時間：15 分鐘（含文件更新）

**替代方案**：若未來希望支援 `--version` 作備用方式，應使用方案 C（重命名為 `--target-version`）並實現邏輯

### 8. 建議後續行動

1. 移除第 397 行 `--version` argument 定義（或重命名為 `--target-version`）
2. 更新 ARG_VERSION_STR 幫助文本以涵蓋 v 前綴處理
3. 在 track-command.md 第 15 行版本命令示例後添加說明：「版本號為必需位置參數，支援 v 前綴（如 v0.31.0）或無前綴（如 0.31.0）」
4. 新增單元測試確認 `track version <version>` 執行正確
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: rosemary
**Review Status**: pending