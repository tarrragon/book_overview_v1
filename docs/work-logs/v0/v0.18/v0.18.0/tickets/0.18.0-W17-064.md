---
id: 0.18.0-W17-064
title: 新增 Hook body schema Layer 1 自檢驗證檢查
type: IMP
status: in_progress
version: 0.18.0
wave: 17
priority: P2
parent_id: null
children:
- 0.18.0-W17-064.1
blockedBy:
- 0.18.0-W17-061
- 0.18.0-W17-064.1
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W17-051
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: create-imp-from-ana
  rationale: W17-051 WRAP 選項 B 階段五：Hook 自檢驗證依賴模板定義先行
who:
  current: pending
  history: {}
what: 在 acceptance-gate-hook 新增 Layer 1 自檢可觀測性 checker（self_check_visibility_checker，warning
  only 不阻擋）
when: W17-061 完成後（已完成 102h ago）+ W17-064.1 ANA 完成後（依 saffron 三維度決策實作）
where:
  layer: .claude/hooks/acceptance-gate-hook.py,.claude/hooks/acceptance_checkers/self_check_visibility_checker.py
  files:
  - .claude/hooks/acceptance-gate-hook.py
  - package/ticket/src/ticket_cli/utils/ticket_validator.py
why: W17-051 WRAP 選項 B 階段五：complete 前 Hook 驗證 Layer 1 自檢模板已套用（body schema 檢查），避免自檢流程遺漏；依賴
  W17-051.2 Layer 1 模板定義
how:
  task_type: Implementation
  strategy: '{"task_type": "Implementation", "strategy": "1. 新建 .claude/hooks/acceptance_checkers/self_check_visibility_checker.py
    實作 self_check_visibility_checker 函式：(a) 接收 ticket body string + ticket type；(b)
    非 IMP/ANA/DOC type 直接 return None（不觸發）；(c) IMP/ANA/DOC 且 Solution 章節不含 ### 自檢結果
    子章節 -> 輸出 warning 訊息；(d) 含則 return None。2. 修改 acceptance-gate-hook.py orchestrator
    註冊新 checker。3. 新增單元測試含三場景：含 ### 自檢結果（靜默）/ 無 ### 自檢結果（warning）/ 非 IMP/ANA/DOC（不觸發）。4.
    設計依據：W17-064.1 ANA 三維度決策（Hook 行為 B warning only / 觸發範圍全 IMP/ANA/DOC / 豁免機制不需要）"}'
acceptance:
- '[ ] 新增 .claude/hooks/acceptance_checkers/self_check_visibility_checker.py 實作 checker
  函式（接收 ticket body + type，回傳 warning message 或 None）'
- '[ ] IMP/ANA/DOC ticket Solution 不含 ### 自檢結果 子章節時，輸出 warning 至 stderr，exit code
  仍為 0（不阻擋 complete）'
- '[ ] IMP/ANA/DOC ticket Solution 含 ### 自檢結果 子章節時，靜默通過（無任何輸出）'
- '[ ] 非 IMP/ANA/DOC type ticket（如 TST/RES/INV/ADJ）不觸發此 checker'
- '[ ] 新增單元測試 3 場景全綠：含 ### 自檢結果（靜默）/ 無 ### 自檢結果（warning）/ 非 IMP/ANA/DOC（不觸發）'
- '[ ] acceptance-gate-hook.py orchestrator 完成新 checker 註冊，集成測試驗證 hook 端對端流程正常'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-08T10:33:32'
completed_at: null
created: '2026-04-24'
updated: '2026-04-24'
---

# Execution Log

## Task Summary

新增 Hook body schema Layer 1 自檢驗證檢查

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---


### PM 前置研究結論（2026-05-08，需上層決策後才能派發 IMP）

**Layer 1 系統現況確認**（W17-061 commit 已落地）：

| 元素 | 位置 / 形式 |
|------|------------|
| 自檢清單模板 | `.claude/references/agent-self-check-template.md` |
| Layer 1 觸發 | PM 派發 prompt 末段插入指令（`agent-dispatch-template.md` line 256-289） |
| 寫入位置 | ticket body Solution 子章節 `### 自檢結果` |
| 觸發條件 | IMP/ANA/DOC 規則類建議插入；純機械可省略；同 session 剛跑可省略 |

**W17-064 spec 重寫前的決策缺口（W17-051 ANA 未明示）**：

| 決策維度 | 候選選項 | 影響 |
|---------|---------|------|
| Hook 行為 | A 硬擋 complete / B warning + 可豁免 / C warning only | 影響 acceptance 強度與用戶體驗 |
| 檢查觸發範圍 | 所有 IMP/ANA/DOC / 排除純機械（判定方式？）/ 排除同 session 已執行（記錄方式？） | 影響檢查精度與 false positive |
| 豁免機制 | CLI `--skip-self-check` flag / commit msg 標記 / ticket frontmatter 欄位 | 影響可繞性與審計性 |

**真因路徑追溯**：

W17-051 IMP-D 描述「在 ticket complete hook 加 body schema 檢查，確保 Layer 1 自檢清單已填寫」——「確保」一詞暗示硬擋，但與 W17-061 落地的「觸發條件是建議非強制」存在語意衝突。

**判斷：W17-064 從 IMP 直接派發風險過高**

- 上游 ANA（W17-051）對豁免邊界與 Hook 行為強度未明示
- 直接以「補完 spec 派發 IMP」推進，等同 PM 替 ANA 決策（PC-104 越權警示）
- 建議升級流程：先派 saffron-system-analyst 做小型 ANA 釐清三決策維度，產出 IMP spec 後再派 thyme

**影響檔路徑修正**：

原 ticket where 含錯誤路徑 `package/ticket/src/ticket_cli/utils/ticket_validator.py`，正確路徑為：
- `.claude/skills/ticket/ticket_system/lib/ticket_validator.py`（641 行 acceptance-gate-hook 的 CLI 對應）

**建議推進選項**：

| 選項 | 說明 |
|------|------|
| 1. 升級為 ANA + IMP 雙 ticket | 建 W17-064.1（ANA：豁免邊界決策）+ 把 W17-064 改為 W17-064.2（IMP：實作）|
| 2. PM 自決三決策維度後重寫 spec 派發 | 風險：PM 替 ANA 決策（PC-104 模式）|
| 3. 暫停推進，handoff 下 session | 在 fresh context 下做完整評估 |

### PM 派發前補強（2026-05-08）

**設計要點（W17-064.1 ANA 三維度決策）**：
- Hook 行為：B warning only（exit 0 + stderr，不阻擋）
- 觸發範圍：全 IMP/ANA/DOC，非適用 type 直接 return None
- 豁免機制：不需要

**檢查邏輯**：解析 ticket body 的 `## Solution` 章節，在該章節下找 `### 自檢結果` 子章節。不含則輸出 warning「[Layer 1] Solution 中未見 ### 自檢結果 子章節...」；含則靜默 return None。

**派發前須觀摩**：
- 既有 checker 介面：`grep .claude/hooks/acceptance_checkers/` 看 checker 命名與介面慣例
- 既有 hook orchestrator：`.claude/hooks/acceptance-gate-hook.py`（641 行）

**Layer 1 dogfooding**：本 ticket 是 Layer 1 機制本身的實作，complete 前應依 `.claude/references/agent-self-check-template.md` 自檢，結果寫入 Solution `### 自檢結果`（自身就是新 hook 檢查的對象，最佳示範）。

**參考引用**：
- W17-064.1 line 160-183：完整 spec rewrite 建議
- W17-061：Layer 1 模板落地產物
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 修復摘要

新增 `acceptance_checkers/self_check_visibility_checker.py`，實作 `check_self_check_visibility(content, ticket_type, logger)`：
- 非 IMP/ANA/DOC type → 直接 return None（豁免）
- 抽取 `## Solution` 章節後，檢查是否含 `### 自檢結果` 子章節
- 缺少時回傳 warning 字串（含 W17-064 標記與引導訊息）
- 含則 return None（靜默通過）

### 設計決策

- **不阻擋**：依 W17-064.1 ANA「Hook 行為 B warning only」決策，warning 透過 `additionalContext` 顯示，不改變 `permissionDecision`。
- **範圍精準**：僅掃描 `## Solution` 章節文字，不掃整個 body（避免 `### 自檢結果` 誤放他處被當合規）。新增測試 `test_solution_with_h3_self_check_only_in_other_section` 守住此邊界。
- **type 大小寫不敏感**：`type_upper` 處理，與 orchestrator 既有 `ticket_type_upper_for_checklist` 用法一致。

### Orchestrator 集成

1. `acceptance_checkers/__init__.py` re-export `check_self_check_visibility`
2. `acceptance-gate-hook.py` 新增 `AcceptanceCheckResult.self_check_warning: Optional[str]` 欄位
3. `check_acceptance_status` 步驟 8 呼叫 checker；checklist 項目 8 顯示狀態（IMP/ANA/DOC 適用、其他標 `[--] 不適用`）
4. `generate_hook_output` 優先級 2.7 將 warning 加入 `context_parts`

### 不在本 ticket 範圍

- W17-061 模板 / dispatch 註入邏輯（已落地）
- 自檢內容品質判定（僅檢查子章節是否存在，不檢查內容）

### 自檢結果

依 `.claude/references/agent-self-check-template.md` Layer 1 自檢清單（2026-05-08）：

- [x] 命名規範：`check_self_check_visibility` 動詞開頭、`_APPLICABLE_TYPES` 常數明示用途
- [x] 函式長度：所有函式 ≤ 30 行
- [x] 巢狀深度：≤ 2 層
- [x] DRY：`_strip_frontmatter` 與 `custom_h2_checker` 結構相同（後續可共用，本 ticket 不重構）
- [x] 註解標準：模組 docstring 含 W17-064 / W17-064.1 三維決策來源；函式 docstring 含 args / returns / 觸發條件
- [x] 二次審查：規範遵循（語言：繁體中文；無 emoji；表格與 Why/Action 結構於 docstring）
- [x] 配合既有介面：與 `check_custom_h2_sections` 介面風格一致
- [x] 邏輯正確性：12 單元測試覆蓋三場景（含 + 缺 + 不適用）+ 邊界（誤放他處、type 大小寫）全綠

備註：本 ticket 為 Layer 1 機制本身的實作，本 Solution 包含自檢結果即實踐 dogfooding。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 執行指令

```
uv run --with pytest pytest .claude/hooks/tests/test_self_check_visibility_checker.py -v
uv run --with pytest pytest .claude/hooks/tests/test_custom_h2_checker.py -q  # 回歸
```

### 結果

- 新增 12 測試全綠（3 場景 × 多 type + 邊界 4 條）
- custom_h2_checker 13 測試全綠（無回歸）
- 模組 import smoke test 通過（acceptance_checkers re-export 正常）

### 場景覆蓋

| 場景 | 測試 |
|------|------|
| 含 ### 自檢結果（靜默） | 3（IMP/ANA/DOC 各 1） |
| 無 ### 自檢結果（warning） | 5（IMP/ANA/DOC + 無 Solution + 誤放他處） |
| 非 IMP/ANA/DOC（不觸發） | 4（TST/RES/空 type/小寫 imp） |
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.18.0-W17-051 | chars: 514 -->

### Task Reference
- 0.18.0-W17-051 what: 分析 代理人成本重新配置策略：Model 降級、估時禁令、多輪驗收

### Rationale Chain
- 0.18.0-W17-051 why: (1) 多數代理人 model 等級過高導致 token 浪費與推算過度反而降低執行速度；(2) 代理人與 PM 常以耗時作決策依據，違反敏捷 SRP 精神（估時本身無意義）；(3) 好計畫是持續推進非追求快速完成；用降 model 省下的 token 投入多輪驗收，取代一次完美執行成果

### Related Files
- .claude/agents/  # from 0.18.0-W17-051
- .claude/rules/core/ai-communication-rules.md  # from 0.18.0-W17-051
- .claude/rules/core/pm-role.md  # from 0.18.0-W17-051
- .claude/methodologies/friction-management-methodology.md  # from 0.18.0-W17-051

### Source Acceptance (reference)
- [ref] [ ] 分析報告已撰寫（含問題敘述、根因分析、改善方案 3 部分）  # from 0.18.0-W17-051
- [ref] [ ] 根因已通過 5W1H 或因果鏈分析確認  # from 0.18.0-W17-051
- [ref] [ ] 改善方案至少包含症狀修復和根因防護兩個方向  # from 0.18.0-W17-051
- [ref] [ ] 分析結論已建立修復 Ticket（症狀修復），Ticket ID 已記錄在 spawned_tickets  # from 0.18.0-W17-051
- [ref] [ ] 根因已建立防護 Ticket（機制防護），Ticket ID 已記錄在 spawned_tickets  # from 0.18.0-W17-051
  ... (truncated, see source ticket)