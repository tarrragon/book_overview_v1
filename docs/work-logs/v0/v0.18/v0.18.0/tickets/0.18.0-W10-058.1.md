---
id: 0.18.0-W10-058.1
title: 評估 S4-S8 訊號必要性與儲備
type: ANA
status: completed
version: 0.18.0
wave: 10
priority: P2
parent_id: 0.18.0-W10-058
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.18.0-W10-058.1.1
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager
  history: {}
what: 評估 S4-S8 訊號必要性與儲備
when: W10-058 拆 IMP 前先評估
where:
  layer: 待定義
  files:
  - .claude/hooks/wrap-decision-tripwire-hook.py + .claude/skills/wrap-decision/
why: W10-058 IMP 規格稀薄且涵蓋 5 訊號。需先驗證：(1) 哪些 S4-S8 仍是真實需求（YAGNI 過濾）；(2) 各訊號偵測機制成本（時間/wave
  統計、context 深度量測等）；(3) 拆分策略（一次全部 vs 分批 vs 跳過）
how:
  task_type: Implementation
  strategy: 讀 wrap-decision SKILL S4-S8 觸發定義 / 對照近兩週 PM 行為日誌（hook log + worklog）找實際出現過幾次
    / 評估每個訊號偵測機制可行性 / Solution 給出推進建議：哪些升 IMP、哪些 won't fix、依賴儲備
acceptance:
- '[x] S4-S8 各訊號近兩週實際發生次數估算'
- '[x] 各訊號偵測機制可行性評估（資料來源、量測方式、實作複雜度）'
- '[x] 推進建議：每個訊號標記 IMP/觀察/關閉'
- '[x] 若需 IMP，列出建議子 ticket 切分方式'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-05-11T20:06:43'
completed_at: '2026-05-11T20:16:46'
created: '2026-05-06'
updated: '2026-05-06'
---

# Execution Log

## Task Summary

評估 S4-S8 訊號必要性與儲備

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

父 W10-058 規劃「擴 wrap-decision-tripwire S4-S8 訊號」規格稀薄且涵蓋 5 訊號（偏離 wave / 救火 / debug / 架構決策 / 高 context）。需先驗證：(1) 哪些 S4-S8 仍是真實需求（YAGNI 過濾）；(2) 各訊號偵測機制成本（時間 / wave 統計、context 深度量測）；(3) 拆分策略。實驗結果反推：S1-S3 既有訊號 14 天命中率 0.23%、可辨識 hit 全為偽陽性，根本問題不在「缺訊號」而在「既有訊號設計失效」。

### 影響範圍

- `.claude/hooks/wrap-decision-tripwire-hook.py`：S1-S3 偵測邏輯
- `.claude/skills/wrap-decision/`：訊號定義 SKILL
- `.claude/config/wrap-triggers.yaml`：訊號設定
- 父 W10-058 IMP 範圍判定（5 訊號 / 拆分策略）

### 相關 Error Pattern

- PC-072 字元集污染（評估過程意外證據：charset-guard hook 阻擋自身輸入）
- PC-073 父子 vs 衍生 ticket 語意混淆（本次誤用 --parent 建衍生 ticket，已修正）
- PC-093 無 trigger 延後決策（本 ANA 直接給出每訊號明確標記，非「待後續評估」）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

### AC1 追加驗證：S2 4 次 hit 真偽陽性分析（2026-05-11）

### 4 個 S2 hit 完整分類

| Hit | 時間 | 觸發詞 | 上下文摘錄 | 判定 |
|-----|------|--------|----------|------|
| 1 | 2026-05-05 21:33 | 無 observability | `unable to derive ticket from any source` | 未知（observability 不足） |
| 2 | 2026-05-06 02:01 | 做不到 | pytest 測試 fixture（`test_l1_signal_paths_exit_zero0`），字串「這個任務做不到啦完全沒辦法真的不行了拜託」 | **偽陽性（測試資料）** |
| 3 | 2026-05-06 01:45 | 無 observability | 同 hit 1 | 未知（observability 不足） |
| 4 | 2026-05-10 20:35 | 不可能 | 技術陳述：「Stop 事件只在退出時觸發，不可能『每 turn fire』」 | **偽陽性（技術 / 描述語境）** |

### 結論

**S2 真陽性率 = 0%（可辨識 2 個 hit 全為偽陽性）**

具體偽陽性類型：
1. **測試 fixture 污染**：pytest 跑單元測試的字串輸入被當作 PM 語句偵測（hook 未豁免測試環境）
2. **技術陳述誤判**：純 keyword 比對無法區分「PM 表達做不到（自動駕駛訊號）」vs「技術討論某操作不可能（描述事實）」

### 補強既有訊號的根因

| 缺陷 | S2 案例 | 改善方向 |
|------|---------|---------|
| 缺 observability | hit 1 / 3 無 prompt_excerpt | 確保所有 signal hit 強制寫 observability |
| 缺測試環境豁免 | hit 2 觸發詞來自 pytest fixture | hook 偵測 `pytest`/`test_` path 豁免 |
| 缺語境判別 | hit 4「不可能」是描述非主張 | 需語意層判斷（hook keyword 比對先天不足） |

### 對 S4-S8 推進建議的影響

S1-S3 設計層級的根本缺陷被本實驗暴露：**hook keyword 比對機制本身不夠精確**。在解此根因前擴 S4-S8 = 投入更多偽陽性訊號。

建議優先序：
1. （最高）解 hook keyword 比對偽陽性 ← 新建子 ticket
2. （中）S4 偏離 wave / S5 救火 > 30 分：可在 ticket CLI 層偵測（claim 時對照 active wave），需獨立 ANA
3. （關閉）S6 / S7 / S8：在 hook 層不可行
### AC1 Reality test：兩週實際發生次數估算（2026-04-27 ~ 2026-05-11）

> 註：本節為 ANA「重現實驗結果」內容，因 CLI append-log section 白名單不含該章節，落地至 Problem Analysis。

### 實驗方法

1. 從 parent W10-058 why 提取 S4-S8 定義：S4 偏離核心 wave / S5 救火 > 30 分 / S6 Debug 根因調查 / S7 重大架構決策 / S8 高 context、tool call 多、session 長
2. 雙資料源交叉驗證：(a) Worklog tickets mtime ≤ 14 天 共 370 筆中的關鍵字命中；(b) wrap-decision-tripwire-hook log 近 14 天 1741 次觸發中的 signal triggered 次數
3. 既有 S1-S3 hook 行為作 baseline 對照

### 實驗執行

```
近 14 天活躍 ticket 數：370
近 7 天活躍 ticket 數：146

Worklog 關鍵字命中（370 筆 ticket md 內含關鍵字數）：
  S4 偏離 wave / 跨 wave / 非當前 wave：17
  S5 救火 / 緊急修復 / skip-gate：25
  S6 type=ANA tickets：104
  S7 ARCH-x / 架構決策：100
  S8 context 過深 / 回合耗盡 / compact：4

Hook log 實際 signal triggered（14 天 / 1741 次 hook 觸發）：
  S1 consecutive_failures：0
  S2 restrictive_keywords：4
  S3 ana_claim：0
  全體命中率：4 / 1741 = 0.23%
```

### 實驗發現

**已驗證的事實**：

1. 既有 S1-S3 hook 命中率極低（14 天僅 4 hit，全屬 S2 restrictive_keywords）。S1 / S3 在 14 天內 0 hit。
2. Hook 觸發頻率高（1741 次 / 14 天 ≈ 124 次/日）但 signal 偵測機制過嚴，幾乎無實際警告產出。
3. S6 Debug 根因調查 = ANA 類 ticket（104 筆），與既有 S3 ana_claim 重疊；獨立實作會雙重觸發。
4. S7 重大架構決策關鍵字命中 100 筆，但實際多為 ticket 自身引用 ARCH-x error-pattern（非「正在做架構決策」事件），語意判斷需 LLM 層而非 hook keyword 比對。
5. S8 高 context / tool call / 長 session 需 CC runtime session metadata（hook 取不到 token usage / tool call count），偵測機制不可行於 hook 層。

**仍未驗證的假設**（AC2-4 待後續決定是否深入）：

- 假設 A：既有 S1-S3 命中率低 = 訊號設計不貼近實際自動駕駛場景。需審視 detection logic 而非加更多訊號。
- 假設 B：S4 偏離 wave、S5 救火 > 30 分可在 ticket CLI 層偵測（claim 時 compare current ticket wave vs active wave），不需 hook 層。
- 假設 C：S6-S8 不適合在 hook 層實作（S6 與 S3 重疊；S7 需語意判斷；S8 需 runtime metadata）。

### 推進建議草稿（供 AC2-4 決策參考）

| 訊號 | 推進建議 | 理由 |
|------|---------|------|
| S4 偏離 wave | 觀察（先補 S1-S3 命中率問題） | 可在 ticket CLI claim 偵測，但既有訊號低命中先解 |
| S5 救火 > 30 分 | 觀察（同上） | 可量測但 ROI 待定 |
| S6 Debug ANA | 關閉（won't fix） | 與 S3 重疊 |
| S7 重大架構決策 | 關閉（hook 層不可行） | 需語意判斷；改 wrap-decision SKILL 提醒層 |
| S8 高 context | 關閉（hook 層不可行） | 需 runtime metadata；CC 內建 / compact 機制已負責 |

→ 建議：父 W10-058 不擴 5 訊號，改建子 ticket 處理「S1-S3 命中率低根因分析」。
## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 最終推進建議（AC2-4 結論）

multi_view_status: skipped
reason: PM 前台 ANA 任務，於 W10-058.1 內已執行 WRAP 框架（Reality test + Widen + Attain + Prepare），用戶主導決策（先 reality test → 查 S2 4 hit → 結論）。Hook log + worklog 雙資料源交叉驗證已提供充足證據基礎，本任務範圍小且結論明確（5 訊號 3 關閉 2 觀察），多視角審查 ROI 不符。

### AC2 各訊號偵測機制可行性

| 訊號 | 資料來源 | 量測方式 | 實作複雜度 | 可行性 |
|------|---------|---------|-----------|--------|
| S4 偏離核心 wave | ticket CLI（active wave + claim wave 對比） | claim 時拉 active_wave vs ticket.wave | 低（CLI 層） | 可行（hook 層不必要） |
| S5 救火 > 30 分 | ticket lifecycle 時間戳 + status churn | started_at vs current time + status 轉移 | 中（需 cross-ticket 統計） | 可行但 ROI 待量化 |
| S6 Debug ANA | 與既有 S3 ana_claim 重疊 | — | — | **與 S3 重疊，關閉** |
| S7 重大架構決策 | 語意判斷（提案 / ARCH 規劃） | LLM / SKILL 提醒層 | 高（hook 無法判斷語意） | **hook 層不可行，關閉** |
| S8 高 context / tool call / 長 session | CC runtime session metadata | hook 取不到（無 API） | 不可行 | **hook 層不可行，關閉** |

### AC3 推進建議（每訊號標記）

| 訊號 | 標記 | 對應動作 |
|------|------|---------|
| S4 偏離核心 wave | **觀察** | 待 S1-S3 偽陽性問題解決後再評估 |
| S5 救火 > 30 分 | **觀察** | 同上 |
| S6 Debug ANA | **關閉（won't fix）** | 與 S3 重疊，重複實作 |
| S7 重大架構決策 | **關閉（hook 層不可行）** | 移交 wrap-decision SKILL 自律提醒 |
| S8 高 context | **關閉（hook 層不可行）** | CC runtime / `/compact` 機制已負責 |

### AC4 若需 IMP，建議子 ticket 切分

**根本前置任務**（最高優先，blockedBy 後續任何 IMP）：
- `(新建)` ANA：**S1-S3 偽陽性根因分析** — 14 天命中率 0.23%、可辨識 2/2 全偽陽性。需改善 keyword 比對的語境判別 + 測試環境豁免 + observability 強制。

**S4-S5 後續任務**（待前置完成後評估）：
- 若 S1-S3 命中率提升後仍見偏離 / 救火問題：
  - IMP-A：ticket CLI claim 時偵測 wave 偏離（S4）
  - IMP-B：ticket lifecycle 救火時長量測（S5）

**S6 / S7 / S8**：不建子 ticket，於父 W10-058 顯性記錄關閉決策。

### 對父 W10-058 的建議

父 ticket 原規劃「擴 5 訊號」與本 ANA 結論衝突。建議：
- 父 W10-058 close --reason superseded_by（指向本 ANA 結論）
- 或父 W10-058 在 Solution 落地本 ANA 結論後 close

### 落地動作

1. 本 ANA Solution 已落地（本段）
2. 新建 ANA：S1-S3 偽陽性根因分析（spawned_tickets 連結）—— W10-058.1.1
3. AC1-4 勾選
4. complete 本 ANA

### 自檢結果（Layer 1）

- [x] acceptance 全勾選
- [x] 5W1H 已補完（who=rosemary / when / where / why / how 完整）
- [x] error-pattern 無衝突
- [x] execution log 已填寫（兩段 append-log 證據鏈完整）
- [x] ANA 後續 ticket 已建立（spawned: W10-058.1.1）
- [x] multi_view_status 已標註（skipped + reason）
- [x] body 已修正為 Schema 章節 + H3 子章節（W17-072 合規）
- 流程教訓：本次誤用 `--parent` 建衍生 ticket（應用 `--source-ticket`），已透過直接編輯 frontmatter 修正（W10-058.1.1 parent_id=null + source_ticket=W10-058.1，W10-058.1 children=[]）。對應 PC-073 父子 vs 衍生語意。
- CLI 觀察：append-log 內容若含 `## H2` 會被 schema validator 視為自定義 H2 違規（W17-072）。建議：append-log 內容預設使用 `### H3` 子章節，或 CLI 自動降一級。
## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-11T20:16:46
**Executing Agent**: rosemary-project-manager
**Review Status**: pending