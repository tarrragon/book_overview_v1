---
id: 0.18.0-W17-011.1
title: 實作 ticket track runqueue CLI（合併 next/schedule/resume-hint 三命令）
type: IMP
status: pending
version: 0.18.0
wave: 17
priority: P0
parent_id: 0.18.0-W17-011
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: create-scheduler-imp-child
  rationale: W17-011 scheduler group children .1；W17-009 IMP-1
who:
  current: thyme-python-developer
  history: {}
what: 新增 track_runqueue.py 實作統一 scheduler CLI；支援 --format={list|dag|critical-path}、--top
  N、--context=resume、--wave N；合併原 W17-011.1（next）/.2（schedule）/.3（resume-hint）三命令為單一
  runqueue（三視角審查一致結論）；復用 critical_path.py CriticalPathAnalyzer 做 DAG 拓撲與關鍵路徑；priority
  P0-P3 排序；group children 處理；ASCII/Unicode 輸出
when: 待定義
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/commands/track_runqueue.py(new)
  - .claude/skills/ticket/ticket_system/commands/track.py
  - .claude/skills/ticket/tests/test_track_runqueue.py(new)
why: W17-009 scheduler 缺口 A；對應 Linux schedule() 挑 runqueue 下一個；W17 18 pending live
  驗證用
how:
  task_type: Implementation
  strategy: step 1 Phase 2 撰寫 test_track_runqueue.py（≥15 測試含 3 format + top + context
    + resume 交集） → step 2 新建 track_runqueue.py 實作 execute_runqueue（復用 critical_path.py
    CriticalPathAnalyzer + cycle_detector.py） → step 3 註冊於 track.py register 字典 →
    step 4 list/dag/critical-path 三 format 輸出 → step 5 --context=resume 交集 handoff/pending
    → step 6 pytest 全綠
acceptance:
- '[ ] ticket track runqueue --wave 17 --format=list 返回可執行清單（blockedBy=[] pending，priority
  排序）'
- '[ ] --format=dag 正確呈現 blockedBy 鏈與關鍵路徑（拓撲層級分組）'
- '[ ] --format=critical-path 返回關鍵路徑節點（slack=0）'
- '[ ] --context=resume 與 .claude/handoff/pending/ 交集邏輯正確'
- '[ ] --top N 在 list / critical-path 生效，dag 忽略'
- '[ ] 復用 critical_path.py CriticalPathAnalyzer 與 cycle_detector.py，無重複實作拓撲/CPM 算法'
- '[ ] 註冊走 track.py register 字典（不走 snapshot/dispatch-check 特殊雙軌）'
- '[ ] 禁止新增 --nice 參數（linux 審查結論）'
- '[ ] pytest 15+ 單元測試 100% 通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-04-20'
updated: '2026-04-20'
---

# Execution Log

## Task Summary

新增 track_runqueue.py 實作統一 scheduler CLI（合併原 next/schedule/resume-hint 三命令）；支援 --format={list|dag|critical-path} / --top N / --context=resume / --wave N；復用 critical_path.py CPM 與 cycle_detector.py

---

## Problem Analysis

## Context Bundle：track runqueue CLI 規格（W17-009 多視角收斂版）

### 背景
本 ticket 為 W17-009 ANA 落地實作，經三視角審查（Evidence/Alternatives/linux）一致收斂：原 next/schedule/resume-hint 三命令合併為單一 `track runqueue`。

### CLI Schema（精確規格）

```
ticket track runqueue [--format FORMAT] [--top N] [--context CONTEXT] [--wave N] [--status STATUS]
```

**參數定義**：

| 參數 | 值域 | 預設 | 語意 |
|------|------|------|------|
| `--format` | `list` / `dag` / `critical-path` | `list` | 輸出視圖 |
| `--top` | int ≥ 1 | 無限 | 返回前 N 筆（僅 list / critical-path 有效） |
| `--context` | `resume` / 未指定 | 未指定 | resume 模式：掃描 .claude/handoff/pending/ 交集 runqueue |
| `--wave` | int | 全部 | 過濾 wave 範圍 |
| `--status` | `pending` | `pending` | 目前僅支援 pending（排除已完成/blocked） |

### --format 三視圖語意

1. **list**（預設）：純可執行清單，`blockedBy == []` 且 `status == pending`，按 priority P0→P3 + creation_date 排序
2. **dag**：拓撲排序 + 層級分組，整個 DAG（含 blocked 者），關鍵路徑高亮
3. **critical-path**：僅返回關鍵路徑節點（slack = 0）

### 復用既有 lib（禁止重寫）

| 需求 | 復用對象 | 檔案 |
|------|---------|------|
| DAG 拓撲排序 + CPM | `CriticalPathAnalyzer` | .claude/skills/ticket/lib/critical_path.py |
| 環檢測 | `CycleDetector` | .claude/skills/ticket/lib/cycle_detector.py |
| Ticket 載入 | 既有 loader | .claude/skills/ticket/lib/（沿用 track_query 模式）|

**禁止重新實作**：Kahn 算法、拓撲排序、關鍵路徑、環檢測。若 critical_path.py API 不敷使用，先派 ANA 評估改 API，不直接複製邏輯。

### CLI 註冊

- 新檔：`.claude/skills/ticket/ticket_system/commands/track_runqueue.py`（execute_runqueue 函式）
- 註冊於：`.claude/skills/ticket/ticket_system/commands/track.py` 的 `_create_command_handlers()` 字典（**不走 snapshot/dispatch-check 類特殊處理雙軌**，對齊 W17-008.11 ANA 結論）
- lib 路徑：`.claude/skills/ticket/ticket_system/lib/critical_path.py`、`cycle_detector.py`（實際位於 ticket_system/ 子目錄）

### 輸出格式

**list 格式**（table）：
```
───────────────────────────────────────────────
可執行清單（wave 17 / top 5 / priority 排序）
───────────────────────────────────────────────
  1. [P0] 0.18.0-W17-005  介面基線設計       blockedBy=[]
  2. [P0] 0.18.0-W17-006  ...
```

**dag 格式**（ASCII）：
```
層級 0（可執行）:
  [P0] W17-005 ◀ 關鍵路徑根
  [P0] W17-006
層級 1（依賴 W17-005）:
  [P1] W17-002  blocked
```

**--context=resume**：交集 .claude/handoff/pending/ JSON 的 ticket_id，格式同 list 但附 handoff direction 與建議動作。

### 禁令（三視角結論）

- 禁止加 `--nice` 參數（linux 審查：為類比而類比，ticket 無動態 CPU share）
- 禁止新建 `track next` / `track schedule` / `track resume-hint` 獨立命令
- 禁止將 DAG 視圖從 runqueue 獨立出去（資料同源僅 view 差異）

### AC 驗收對照

- [ ] `ticket track runqueue --wave 17` 返回 W17-005/006/007（priority 排序驗證）
- [ ] `--format=dag` 正確呈現 blockedBy 鏈與關鍵路徑
- [ ] `--format=critical-path` 返回節點數 ≤ dag 全節點
- [ ] `--context=resume` 與 .claude/handoff/pending/ 交集邏輯正確
- [ ] `--top N` 在 list / critical-path 生效，dag 忽略
- [ ] 復用 critical_path.py，無重複實作拓撲算法
- [ ] 單元測試 ≥ 15 項
- [ ] 100% 綠燈

### 依賴關係

- 本 ticket (.1) 無 blockedBy
- .4（session-hint hook）blockedBy = [.1]（呼叫 runqueue CLI）
- .5（handoff --auto）無依賴（可並行）
---

> **舊版 Context Bundle（next 單命令版）已 superseded** — 由上方「track runqueue CLI 規格（W17-009 多視角收斂版）」取代。W17-009 三視角審查（Evidence/Alternatives/linux）一致收斂：next/schedule/resume-hint 合併為 runqueue + --format。

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending