---
id: 0.18.0-W17-008.5.1
title: 修復 complete schema 錯誤提示使用不存在的 append-log --content
type: ADJ
status: pending
version: 0.18.0
wave: 17
priority: P1
parent_id: 0.18.0-W17-008.5
children: []
blockedBy: []
relatedTo:
- 0.18.0-W6-002
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: create-child-under-W17-008.5
  rationale: 此缺陷屬 W17-004 落差 4 與 PC-105 的具體可修案例；已有 W17-008.5 大任務，新增子任務可避免小 bug 被大範圍錯誤通道整合淹沒。
who:
  current: thyme-python-developer
  history: {}
what: 修復 ticket track complete 在 body schema 驗證失敗時輸出的修復建議；目前 lifecycle.py 會提示 ticket
  track append-log <id> --section "..." --content "內容"，但 append-log 實際語法是 ticket track
  append-log <id> "內容" --section "..."，導致使用者照錯誤提示執行會再次失敗。
when: W6-002 complete 時實際遇到：complete hook 提示 --content，但 ticket track append-log --help
  顯示 content 是 positional argument。此缺陷已被 PC-105 記錄為 CLI 語法 autopilot 案例，但需要 CLI 層修正。
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/commands/lifecycle.py
  - .claude/skills/ticket/tests
why: CLI 錯誤訊息本身是修復路徑的一部分；若錯誤訊息產生不存在的指令，會讓 complete schema 防護從引導變成二次踩坑。
how:
  task_type: Implementation
  strategy: 1) 修改 lifecycle.py 的 body schema 驗證失敗訊息，移除不存在的 --content，改成目前 append-log
    help 一致的 positional content 語法 2) 搜尋文件中同類 --content append-log 範例，判斷是否同步修正或另開
    DOC 3) 增加/更新測試覆蓋錯誤提示不得包含 --content 且包含正確 append-log 語法 4) 用 uv run 執行 ticket skill
    相關測試
acceptance:
- '[ ] complete body schema 驗證失敗訊息不再包含 append-log --content'
- '[ ] 錯誤提示中的 append-log 範例符合 ticket track append-log --help：ticket_id content 為位置參數，--section
  為 option'
- '[ ] 新增或更新測試覆蓋此錯誤提示，避免回歸'
- '[ ] 若搜尋到框架文件仍建議 append-log --content，需同步修正或建立獨立 DOC ticket 追蹤'
tdd_phase: phase2
tdd_stage:
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-04-21'
updated: '2026-04-21'
---

# Execution Log

## Task Summary

修復 ticket track complete 在 body schema 驗證失敗時輸出的修復建議；目前 lifecycle.py 會提示 ticket track append-log <id> --section "..." --content "內容"，但 append-log 實際語法是 ticket track append-log <id> "內容" --section "..."，導致使用者照錯誤提示執行會再次失敗。

---

## Problem Analysis

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending
