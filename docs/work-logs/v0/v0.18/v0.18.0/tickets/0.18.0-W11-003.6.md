---
id: 0.18.0-W11-003.6
title: 補回歸測試保護 ticket migrate 反向引用更新邏輯（W11 重組情境）
type: IMP
status: completed
version: 0.18.0
wave: 11
priority: P2
parent_id: 0.18.0-W11-003
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-python-developer
  history: {}
what: 為 migrate 命令的反向引用更新邏輯補上整合測試，鎖定 W11 重組情境防止退化
when: W11-003 群組內處理
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/commands/migrate.py
why: PM 前台 Phase 1 分析確認 migrate.py `_update_cross_references` 已涵蓋 children/blockedBy/relatedTo/parent_id/source_ticket/spawned_tickets
  六種反向引用且支援 string/dict children；W11-002/003 父 ticket children 殘留舊 ID 屬歷史資料殘留（時間差
  / 早期版本邏輯），不是現存 bug。本 ticket 重新定位為「補回歸測試鎖定現有邏輯」，預防未來退化；資料殘留另起 follow-up
how:
  task_type: Implementation
  strategy: 撰寫整合測試覆蓋 W11 重組情境（多筆 child 從 W5/W10 跨 wave 遷移到 W11-XXX.Y 父子結構），驗證父 ticket
    children/外部 ticket 的 blockedBy/relatedTo/parent_id/source_ticket/spawned_tickets
    全部更新為新 ID；含批量模式跨遷移引用（A→B、C 引用 A 自動改為 B）
acceptance:
- '[x] 整合測試：單筆 migrate 後父 ticket children（含 string + dict 形式）自動更新為新 ID'
- '[x] 整合測試：單筆 migrate 後外部 ticket 的 blockedBy/relatedTo/parent_id/source_ticket/spawned_tickets
  引用同步更新'
- '[x] 整合測試：批量遷移正確處理跨遷移引用（A→B、C 引用 A 自動改為引用 B）'
- '[x] 整合測試：W11 重組情境（多 child 跨 wave 遷入新父子結構）父子反向引用完整一致'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-29T02:04:12'
completed_at: '2026-04-29T03:13:49'
created: '2026-04-16'
updated: '2026-04-16'
current_phase: Phase 4
assignee: cinnamon-refactor-owl
---

# Execution Log

## Task Summary

修復 migrate 命令未同步更新父 ticket 的 children 陣列引用

---

## Problem Analysis

### 深入根因分析（2026-04-29 PM 前台 Phase 1）

讀完 migrate.py 全 529 行，定位實際邏輯狀態：

**_update_ticket_id_references（lines 58-79，自身內部引用）**：
- blockedBy: 處理 list ✓
- children: 只處理 dict 形式 `{id: ...}`，未處理 string list ✗（但 self-loop 異常情境，影響極小）
- source_ticket: 處理 ✓
- 缺 relatedTo / parent_id / spawned_tickets 自我引用（self-loop 異常情境，實務不會發生）

**_update_cross_references（lines 82-182，其他 ticket 對該 ID 的引用）**：
- blockedBy ✓
- relatedTo ✓
- children: 同時處理 string list 和 dict 形式 ✓
- source_ticket ✓
- parent_id ✓
- spawned_tickets ✓
- **六種引用類型完整**

### 根因假設（非 migrate.py bug，而是歷史/測試缺口）

殘留 W11-002/003 children 舊 ID 的可能性：
- (A) **時間差**：父 ticket 在 child migrate 完成後才手動建立並把舊 ID 列入 children，未觸發 cross_references 掃描
- (B) **早期版本**：cross_references 邏輯是後續補完整的，當年 W5→W11 重組時邏輯不完整
- (C) **batch 遷移路徑**：批量遷移每筆都呼叫 _migrate_single_ticket → _update_cross_references，理論上正常

### 工作範圍重新定位

原 AC 字面解讀「修復 migrate 不更新 children」與實際代碼狀態不符。重新定位：

| AC | 實際工作意義 |
|----|-------------|
| AC1 | 防護現有邏輯不退化（regression test） |
| AC2 | 同上 |
| AC3 | 補 batch 模式跨遷移引用測試（A→B、C 引用 A） |
| AC4 | **整合測試 W11 重組情境**——本 ticket 主要產出 |

延伸工作（不在本 ticket，需另起 follow-up）：
- 一次性清理現存 W11-002/003 父 ticket children 舊 ID（純資料修復）
### 問題根因

migrate.py 已具備反向引用更新邏輯（內含 `ticket["children"][i] = new_id`、`blockedBy`、`relatedTo`、`spawned_tickets` 四種引用），但實際歷史遷移發生後，部分父 ticket 的 children 仍指向舊 ID，顯示邏輯路徑或觸發時機有缺口（非完全缺失）。需進一步定位是 batch 模式漏跑、還是 string vs dict children 條件判斷不全。

### 影響範圍

- 受影響檔案：.claude/skills/ticket/ticket_system/commands/migrate.py（529 行）
- 受影響資料：父 ticket 的 children 反向引用
- 已驗證殘留（PC-007 獨立查證 2026-04-29）：
  - 0.18.0-W11-002.children 仍含舊 ID：W5-023, W5-024, W5-025, W5-026, W5-028（應為 W11-002.X）
  - 0.18.0-W11-003.children 仍含舊 ID：W5-018, W10-022, W10-065, W10-038（應為 W11-003.X）
  - 0.18.0-W11-001.children 已是新 ID（W11-001.X）
  - 0.18.0-W11-004.children 已是新 ID（W11-004.X）

### AC 漂移檢查（PC-055）

Ticket 建立 13 天，PM 前台逐項對照 acceptance：
- AC1（migrate 後父 ticket children 自動更新）：仍適用，殘留可重現
- AC2（外部 ticket blockedBy/relatedTo 引用更新）：仍適用
- AC3（批量遷移跨引用處理）：仍適用，疑似批量模式為觸發路徑缺口主因
- AC4（整合測試驗證 W11 重組情境）：仍適用，本次驗證即為測試樣本

### 相關 Error Pattern

- PC-007（接手者必須獨立驗證範圍）— 已執行，殘留 ID 已列舉
- PC-055（pending >7 天 AC 漂移偵測）— 已對 4 條 AC 完成適用性審查
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Context Bundle

<!-- 派發前 PM 寫入，agent 讀取後執行任務 -->

---

<!-- PC-093-exempt: ticket-tracked:0.18.0-W11-003.6 phase3b 已執行 TD#2/TD#3，非延後項目（16 GREEN 已驗證落地） -->
<!-- PC-093-exempt: ticket-tracked:0.18.0-W11-003.6 Phase 4 派發指引 — 已執行完成的歷史記錄，非延後項目；多視角評估已完成 -->
### Phase 4 派發指引 — 歷史記錄（cinnamon 派發指示，已執行）

(已執行完成，cinnamon 派發已被 multi-view 8 視角取代，發現項已建 W11-003.10/11、W11-019/020/021/022 follow-up 追蹤)

<!-- PC-093-exempt: ticket-tracked:0.18.0-W11-003.6 Phase 3b TD 決斷已落地 commit 3109ae19/71372ebc，非延後項目 -->
### Phase 3b 工作擴增 — 歷史記錄（已執行）

PM 用 AskUserQuestion 逐項決斷三項 TD，TD #2 與 TD #3 改為 phase3b 立即執行：

**追加工作 TD #2：補 _update_ticket_id_references self-loop 測試**
- 在 test_migrate_reverse_refs.py 新增 1 測試（共 15 → 16）
- 測試行為：建構 ticket 自身欄位含 old_id（如 source_ticket: old_id），呼叫 _update_ticket_id_references(old_id, new_id)，斷言 source_ticket 變更為 new_id

**追加工作 TD #3：AC4 殘留檢查改集合成員比對**
- AC4 主驗收測試（test_w11_reorganization_full_consistency）的「零殘留」斷言
- 原設計：substring scan 全 ticket 內容
- 改為：load_ticket → 比對 frontmatter 中 children/blockedBy 等欄位，使用 `assert old_id not in ticket["children"]`（list membership）等精確斷言
- 避免 prefix 誤判（例：old_id="W5-1" 不應誤判 "W5-10" 為殘留）

**測試總數**：15 + 1 (TD#2) = 16 測試（AC1×4 / AC2×6 / AC3×3 / AC4×2 / TD#2 self-loop×1）

**commit policy**：仍是一個 commit 完成（測試填補 + TD#2 新測試 + TD#3 斷言修正 + ticket md 更新）
### Phase 3b 派發指引（thyme-python-developer）

phase3a 已由 pepper 完成（commit 5130e8df），策略寫入 ticket Solution 區段，含：
- 共通 GWT 骨架
- 4 AC × 15 測試填補對照表
- AC4 完整虛擬碼
- 6 項技術決策（fixture 沿用、不 parameterize、backup=False 等）
- 3 項技術債紀錄
- Phase 3b 交接清單

phase3b 目標：依 pepper 策略填補 .claude/skills/ticket/ticket_system/tests/test_migrate_reverse_refs.py 的 15 個 RED 測試體，使其全部 GREEN。

### 工作步驟

1. 讀 ticket Solution 區段（pepper 策略 + 虛擬碼）
2. 依虛擬碼填寫每個測試方法的 body（替換 pytest.fail RED 標記）
3. 沿用既有 fixture（_write_ticket / _read_frontmatter / project_with_tickets），不抽新 helper
4. 執行測試確認 15/15 GREEN：
   `uv -d .claude/skills/ticket run pytest tests/test_migrate_reverse_refs.py -v 2>&1 | tail -30`
5. 確認既有測試持續 GREEN（regression 防護）：
   `uv -d .claude/skills/ticket run pytest 2>&1 | tail -10`

### 完成後產出

- ticket Test Results 區段：寫入 GREEN 結果摘要與既有測試回歸結果
- ticket Solution 區段（追加 Phase 3b 補充）：技術決策（如有偏離 pepper 策略需說明）
- 推進 phase 至 phase4：`ticket track phase 0.18.0-W11-003.6 phase4 cinnamon-refactor-owl`
- commit：`feat(0.18.0-W11-003.6): phase3b 完成 15 整合測試 GREEN（migrate 反向引用 W11 重組情境）`

### 限制

- 禁止修改 migrate.py（PM 已驗證邏輯完整；若實際填寫時發現邏輯問題，停手回報 PM 不要修）
- 禁止觸碰其他 ticket 範圍
- 禁止改測試斷言以遷就 migrate.py（測試應驗證預期行為，不是反過來符合現況）

Commit Policy：一個 commit 只放本 ticket 變更（測試填補 + ticket md 更新）。
### Phase 3a 派發指引（pepper-test-implementer）

phase2 已完成 15 RED 測試骨架（commit 0e825a22），位於 .claude/skills/ticket/ticket_system/tests/test_migrate_reverse_refs.py。骨架以 `pytest.fail("RED: phase3 ...")` 標記，每個測試含 GWT docstring 描述行為鏈。

phase3a 目標：規劃語言無關的實作策略指引，給 phase3b（thyme-python-developer）填補測試體並轉 GREEN。

任務範圍：
1. 閱讀 sage 在 ticket Solution 區段的測試案例設計（15 測試 / 4 AC）
2. 規劃每組測試的實作策略（fixture 設置 / mock / 斷言模式 / 共用 helper 抽取）
3. 識別技術債與架構決策（fixture 重用 vs 重複、tmp_path 隔離模式、parameterize vs 獨立 method）
4. 不修改 migrate.py（已驗證邏輯完整）
5. 不寫實作（pepper 的職責是策略，不是程式碼）

完成後產出：
- ticket Solution 區段：Phase 3a 實作策略（每組測試的虛擬碼、fixture 規劃、技術決策）
- 推進 phase 至 phase3b：`ticket track phase 0.18.0-W11-003.6 phase3b thyme-python-developer`
- commit：`chore(0.18.0-W11-003.6): phase3a pepper 實作策略規劃`

Commit Policy：一個 commit 只放本 ticket 變更（ticket md + 任何策略設計文件），禁止觸碰其他 ticket。
### 任務範圍（sage 設計 phase2 測試）

為 .claude/skills/ticket/ticket_system/commands/migrate.py 的反向引用更新邏輯設計整合測試，鎖定現有六欄位反向引用行為防止退化，並覆蓋 W11 重組情境。

### 關鍵實作位置

- migrate.py:58-79 `_update_ticket_id_references`（自身內部引用，self-loop 情境，影響極小）
- migrate.py:82-182 `_update_cross_references`（其他 ticket 對該 ID 的引用，本次測試重點）
- migrate.py:238-342 `_migrate_single_ticket`（單筆遷移流程入口）
- migrate.py:387-450 `_batch_migrate`（批量遷移流程入口，跨遷移引用情境）

### 反向引用六欄位（cross_references 已實作，需測試覆蓋）

| 欄位 | 形式 | 已實作位置 |
|------|------|-----------|
| blockedBy | string list | migrate.py:126-131 |
| relatedTo | string list | migrate.py:134-139 |
| children | string list + dict `{id:...}` | migrate.py:142-150 |
| source_ticket | string | migrate.py:153-155 |
| parent_id | string | migrate.py:158-160 |
| spawned_tickets | string list | migrate.py:163-168 |

### 測試環境

- 測試目錄：.claude/skills/ticket/ticket_system/tests/
- 既有測試：tests/test_migrations.py（同模組既有測試，可參考 fixture 模式）
- 框架：pytest（uv run，設定見 pyproject.toml）
- 測試類型：整合測試（使用 tmp_path fixture 隔離 docs/work-logs/v*/tickets/ 結構）

### AC 對應測試案例（4 條 AC）

1. **AC1 單筆 migrate**：建立父 ticket A 含 children: [old_id]（含 string + dict 兩種形式），migrate old_id → new_id，驗證 A.children 變更為 new_id
2. **AC2 單筆 migrate 外部引用**：建立外部 ticket E 含 blockedBy/relatedTo/parent_id/source_ticket/spawned_tickets 多欄位指向 old_id，migrate old_id → new_id，驗證 E 全部欄位同步更新
3. **AC3 批量跨遷移**：建立 migration list [{from: A, to: B}]，並建 ticket C 含 blockedBy: [A]，執行 batch migrate，驗證 C.blockedBy = [B]
4. **AC4 W11 重組情境**：建父 P，多個 child（c1, c2, c3）跨 wave 遷入（W5/W10 → W11-XXX.Y），父 P.children 應同步更新；採 batch 模式

### 限制與注意

- **禁止寫實作**：sage 只設計測試案例 + 撰寫 RED 測試骨架，不修改 migrate.py
- 使用 tmp_path 建臨時 work-logs 目錄，避免污染真實 docs/
- 注意 migrate.py 寫入時呼叫 save_ticket（會寫入 disk），需在 tmp 目錄完成
- 測試應顯式覆蓋 string list children 與 dict children 兩種形式

### Commit Policy

- 派發完成後 sage commit 測試骨架（RED），訊息格式：
  `chore(0.18.0-W11-003.6): phase2 sage 測試設計 RED 骨架`
- 一個 commit 只放本 ticket 變更，禁止混入其他 ticket 內容

### 不在本 ticket 範圍

- W11-002/003 父 ticket children 舊 ID 資料清理 → 已建 0.18.0-W11-003.8 follow-up
- migrate.py 程式碼變更（除非 phase2 設計時發現邏輯實際缺陷，需 issue follow-up）

---

## Solution

<!-- To be filled by executing agent -->

---


---


<!-- PC-093-exempt: ticket-tracked:0.18.0-W11-003.6 Phase 4 評估已完成（多視角 8 視角共識 + 6 follow-up 建立），非延後項目 -->
## Phase 4 多視角評估結論（PM 彙整 2026-04-29）

### 4 視角評分

| 視角 | 代理人 | 評分 | 主要發現 |
|------|--------|------|---------|
| Universal (taste) | linux | Acceptable B+ | F1 yaml serializer / F2 AC2 parametrize / F3 import 重複；F4 AC4 集合掃描為 good taste 範本 |
| Python 慣例 + Coupling | thyme-python | A- | 散彈式 patch / _update_ticket_id_references 六欄位完整化（行為不一致） |
| Complexity | ginger | B+ | _write_ticket 指數 13 + W11 重組 73行指數 22 兩處熱點 + AC2 parametrize 共識 |
| Redundancy / DRY | bay | A- | AC2 parametrize 共識；TD#3 substring→集合精確比對徹底修復；無跨檔重疊 |

### 共識項（建立 follow-up）

| Follow-up | 優先 | 內容 |
|-----------|------|------|
| 0.18.0-W11-003.10 | P2 | _write_ticket yaml.dump + AC2 parametrize + W11 重組測試拆分（4 視角共識） |
| 0.18.0-W11-003.11 | P3 | _update_ticket_id_references 六欄位 + 散彈式 patch 集中 + import 重複 |

### Phase 4 決策

**是否需重構**：否（不阻擋 complete）
**理由**：4 視角共識「Acceptable / A- / B+」皆未到 Garbage 等級，16/16 GREEN + 全套件 1751 passed 無回歸；發現項已建 follow-up ticket 追蹤
**規則 6 失敗案例學習**：TD#3 substring → 集合精確比對被 4 視角共識讚為 good-taste 範例，預設套用 schema-aware 斷言模式可避免 prefix 誤判（已在 Solution 留存）

### 結論

complete W11-003.6 並進入後續 follow-up 流程。
## Redundancy / DRY 視角審計報告

**審計標的**: `test_migrate_reverse_refs.py`（16 tests, 587 行）
**審計日期**: 2026-04-29
**測試結果**: 16/16 PASSED (0.07s)

---

### 1. 重複邏輯分析

#### 1.1 import 重複（低優先）

每個測試方法內部重複 `from ticket_system.commands.migrate import _update_cross_references`（出現 8 次）。pytest 慣例做法（避免模組級 import 與 monkeypatch 衝突），不構成問題。

#### 1.2 setup 模式重複（中優先，建議但不阻擋）

AC2 的 5 個單欄位測試共享相同 setup 模式，可用 `@pytest.mark.parametrize` 壓縮為 1 個參數化測試 + 2 個 scalar 欄位測試，從 ~60 行降至 ~25 行。當前寫法每個欄位獨立可見，偵錯定位明確。建議作為 P3 技術債，不阻擋合併。

#### 1.3 helper 函式唯一性

`_write_ticket` / `_read_frontmatter` / `project_with_tickets` / `_write_migrations_yaml` 全域搜尋僅存在於本檔，無跨檔重複。若未來新增 migrate 測試應提取至 conftest.py。

### 2. 跨檔重疊覆蓋

| 檔案 | 模組 | 重疊 |
|------|------|------|
| test_migrate_reverse_refs.py | ticket_system.commands.migrate | — |
| test_migrations.py | ticket_system.lib.migrations（Protocol Version） | 零重疊 |
| test_migrate_cross_references.py | 不存在 | — |

### 3. TD#3 斷言精準度

AC4 零殘留檢查（L504-526）使用 `old_ids = {c1, c2, c3}` 集合精確成員比對，覆蓋全部 6 欄位 + 2 種 children 形式。TD#3 已徹底修復，無 substring 殘留。

### 4. 總結

| 維度 | 評分 | 說明 |
|------|------|------|
| DRY | B+ | AC2 可 parametrize，不阻擋 |
| 既有 utility | A | 正確使用 parse_frontmatter |
| 跨檔重疊 | A | 零重疊 |
| TD#3 精準度 | A | 集合比對徹底 |
| 整體 | A- | 唯一改善點為 AC2 parametrize（P3） |

### 5. Follow-up 建議

- P3：AC2 五個 list 欄位單測 parametrize 壓縮
- P3：未來新增 migrate 測試時 helper 提取至 conftest.py
## Phase 4 多視角審查 — Python 慣例 + Coupling 視角（thyme-python-developer）

### 審查標的
`.claude/skills/ticket/ticket_system/tests/test_migrate_reverse_refs.py`（587 行，14 個測試方法，5 個測試 class）

### 評估結論
**整體品質：A-**（測試體骨架健康，pepper 策略下未抽 helper 仍合理；發現 3 個中低優先改善點，皆建議建 follow-up 不阻擋本 ticket complete）

---

### 1. Python 慣例（命名 / type hints / docstring / pytest 慣例）

| 維度 | 評估 | 說明 |
|------|------|------|
| 命名 | 通過 | snake_case 函式名、PascalCase class 名（`TestAC1_ParentChildrenUpdated`）符合 pytest 慣例；底線前綴標記內部 helper 合理 |
| Type hints | 通過 | 三個 helper 函式簽名完整（`_write_ticket` / `_read_frontmatter` / `_write_migrations_yaml`） |
| Docstring | 通過 | 模組級含來源 ticket、欄位對照表、AC 對應；測試方法多採 Given/When/Then 結構 |
| pytest 慣例 | 通過 | fixture / tmp_path / monkeypatch 使用正確；class 命名符合 collection 規則 |
| Local import | 觀察點 | 每個測試方法內部 import SUT 函式（13 次重複），屬刻意設計（避免 module-level import 在 monkeypatch 前觸發路徑解析），不視為缺陷 |

---

### 2. 模組耦合（fixture / monkeypatch 範圍）

| 維度 | 評估 | 說明 |
|------|------|------|
| fixture 依賴 | 通過 | `project_with_tickets` 單一 fixture 涵蓋 tmp_path + monkeypatch + 目錄建構 + 模組 patch，職責內聚 |
| monkeypatch 範圍 | 可改善（低） | 散彈式 patch（migrate_mod / loader_mod / paths_mod 三處），SUT 演進時可能遺漏新引入的 `get_project_root` 引用點 |
| SUT 內部符號依賴 | 觀察點 | 直接 import 4 個 underscore-prefixed private 函式，屬白盒契約測試，與「鎖定既有實作」目標一致；migrate.py 私有函式重構時測試會大量失敗（已知技術債） |
| 跨 fixture 隔離 | 通過 | tmp_path 自動隔離，無共享狀態 |

---

### 3. DRY 違反（重複 setup 邏輯）

| 重複模式 | 出現次數 | 評估 |
|---------|---------|------|
| local import 重複 | 13 | 必要（monkeypatch 時序） |
| `_, tickets_dir = project_with_tickets` 解包 | 11 | pytest 慣用法 |
| AC2 五個測試的「寫入→更新→讀取→assert」四步驟 | 5 | 符合 parametrize 場景，但 pepper 策略禁抽 |

**結論**：在 pepper 策略下，AC2 重複是策略性接受（測試清晰度 > DRY）。

---

### 4. 共用 Helper 抽出評估

| 候選 helper | 是否應抽 |
|------------|---------|
| `_write_ticket` / `_read_frontmatter` / `_write_migrations_yaml` | 已抽（模組級） |
| `assert_field_updated(field, old, new)` 通用 assertion | 不抽（pepper 禁） |
| `pytest.mark.parametrize` 改寫 AC2 | 不抽（pepper 禁；屬 helper 等價形式） |
| `setup_parent_with_children` factory | 不抽（pepper 禁） |

**結論**：pepper 策略是有意識的取捨，所有候選皆合理保留為內聯。

---

### 5. 認知負擔評估

| 指標 | 數值 | 閾值 | 評估 |
|------|------|------|------|
| 最長函式 | `test_w11_reorganization_full_consistency` 約 75 行 | 30 行 | 超標但測試碼放寬可接受 |
| 最大巢狀深度 | 3 層 | 3 層 | 邊界 |
| 單一 class 方法數 | TestAC2 6 個 | 10 | 通過 |
| 模組總行數 | 587 行 | 600（測試碼放寬） | 通過 |

**結論**：最長測試含 4 phase assert（rename / parent / external / 零殘留），拆分會破壞整合場景一致性，建議保留。

---

### 6. 改善建議（follow-up 候選）

| # | 優先級 | 描述 | 建議處理 |
|---|-------|------|---------|
| 1 | 低 | AC2 五個測試符合 parametrize 場景 | 不建 ticket（pepper 策略接受） |
| 2 | 低 | monkeypatch 散彈式 patch 三處，SUT 演進可能遺漏 | 建 follow-up：評估 migrate 模組是否應將 `get_project_root` 集中到單一 lib 入口（治本） |
| 3 | 中 | TD#2 鎖定 `_update_ticket_id_references` 僅覆蓋 3/6 欄位（已記錄技術債） | 建 follow-up：補完該函式六欄位覆蓋，消除與 `_update_cross_references` 兩條更新路徑的行為不一致 |

---

### 7. Phase 4 最終結論

- **重構需要**：否（pepper 策略下測試品質符合預期）
- **技術債務**：發現 2 項（建議 #2 / #3，中低優先）
- **程式碼品質**：A-
- **建議**：W11-003.6 可 complete；後續 PM 建立 2 個 follow-up ticket（建議 #2 / #3）追蹤
### Phase 4 多視角審查 — Complexity / 認知負擔視角（ginger-performance-tuner）

**標的**：`.claude/skills/ticket/ticket_system/tests/test_migrate_reverse_refs.py`（586 行 / 16 個測試方法 / 4 個 TestClass + 1 self-loop class + 2 helper）

#### 1. 認知負擔評估

| 元件 | 變數 | 分支 | 巢狀 | 依賴 | 指數 | 等級 |
|------|------|------|------|------|------|------|
| `_write_ticket` helper | 5 | 3 | 4 | 1 | 13 | 需重構 |
| `_read_frontmatter` helper | 2 | 0 | 1 | 1 | 4 | 優良 |
| `project_with_tickets` fixture | 3 | 1 | 2 | 4 | 10 | 可接受 |
| `_write_migrations_yaml` helper | 2 | 0 | 1 | 1 | 4 | 優良 |
| AC1-AC2 各 test method | 3-5 | 0 | 1 | 1-2 | 5-8 | 優良 |
| `TestAC4.test_w11_reorganization_full_consistency` | 12+ | 4 | 4 | 2 | 22 | 必須重構 |
| `TestAC4.test_w11_reorganization_idempotency` | 6 | 0 | 1 | 1 | 8 | 可接受 |

**熱點**：`_write_ticket` helper 與 `test_w11_reorganization_full_consistency`。

#### 2. 函式長度與單一職責

| 函式 | 行數 | 職責數 | SRP 評估 |
|------|------|--------|---------|
| `_write_ticket` | 38 | 3（檔名、frontmatter、list-of-dict 序列化） | 違反 SRP：自製 YAML 序列化應改用 `yaml.dump` |
| `project_with_tickets` | 25 | 2 | 可接受 |
| `test_w11_reorganization_full_consistency` | 73 | 4（建資料、執行、4 段斷言） | 違反 SRP：零殘留斷言區塊應抽 helper |
| 其他 test methods | 8-15 | 1 | 符合 |

#### 3. 控制流簡化機會

| 位置 | 現況 | 建議 |
|------|------|------|
| `_write_ticket` L60-71 | 三層巢狀 for/if/for/if | 改用 `yaml.dump(...)` 一行解決 |
| `test_w11_reorganization_full_consistency` L506-526 | 4 個 for + isinstance + nested assert | 抽 `_assert_no_residual_old_ids(tickets_dir, old_ids)` helper |
| `project_with_tickets` L108-113 | try/except ImportError + hasattr 雙重防禦 | 確認 paths 模組契約後簡化為單一 monkeypatch |

#### 4. 16 個測試 method 可讀性

| 類別 | 數 | 平均行數 | 可讀性 | 備註 |
|------|----|---------|--------|------|
| TestAC1 | 4 | 18 | 優良 | Given/When/Then docstring 清晰 |
| TestAC2 | 6 | 12 | 優良 | 結構重複，可 parametrize 壓縮 ~80→~25 行 |
| TestAC3 | 3 | 28 | 良好 | 命名清楚 |
| TestAC4 | 2 | 36 | 中等 | full_consistency 過長 |
| TestTD2 | 1 | 9 | 優良 | self-loop 鎖定 |

#### 5. 結論

**保留現狀**：命名清晰、AC 對應明確、子類別分群符合「測試即文件」、全綠通過退化鎖定目標。

**Follow-up（不阻擋本 ticket complete）**：

| 編號 | 問題 | 嚴重度 | 建議 |
|------|------|--------|------|
| F1 | `_write_ticket` 指數 13 + 三層巢狀 + 自製 YAML | 中 | 改 `yaml.dump`，38→~10 行 |
| F2 | `test_w11_reorganization_full_consistency` 73 行 / 指數 22 | 中 | 抽 helper + 拆為正向 + 零殘留兩個 test |
| F3 | TestAC2 六 method 結構重複 | 低 | `pytest.mark.parametrize` 整合 |
| F4 | `project_with_tickets` paths 模組防禦過度 | 低 | 簡化 monkeypatch |

**等級**：B+（紮實但有兩處認知負擔熱點待後續優化；不影響本 ticket complete）
## Phase 4 多視角審查報告（universal_lens / linux）

### 整體評分：Acceptable（接近 Good Taste，3 個風格問題）

測試結構清晰、AC 對應明確、fixture 隔離乾淨。沒有致命問題。但有幾處違反 Linus「good taste — 消除特例與重複」原則。

---

### 關鍵發現

#### F1：[P2] `_write_ticket` 內聯 YAML 序列化是設計補丁（line 60-73）
- 位置：tests/test_migrate_reverse_refs.py:60-73
- 現狀：手寫 13 行邏輯處理 list-of-string / list-of-dict / scalar 三種特例分支，用縮排字串拼 YAML
- 問題：測試已用 pyyaml（line 343），卻在 fixture helper 裡自行手寫 serializer。「special case as patch」——有 yaml.dump 不用，自己重發明半殘版本
- Good taste 改寫：`yaml.dump({**base, **extra_fields}, sort_keys=False, allow_unicode=True)` 一行取代 13 行分支
- 影響：dict 縮排格式僅靠湊巧能被 parser 讀回，parser 嚴格化會靜默斷裂

#### F2：[P2] AC2 五個測試方法是同一份程式碼複製五份（line 240-300）
- 位置：TestAC2_ExternalReferencesUpdated 五個 test_*_updated
- 現狀：五個 method 結構完全相同，差別只在欄位名與 list-vs-scalar
- 問題：經典「if/else 退化成 N 份複製」反模式
- Good taste 改寫：`@pytest.mark.parametrize` 5 cases → 1 test、5 行
- 緩解理由：若刻意保留 5 份為失敗時定位，可接受；但目前無註解說明此意圖

#### F3：[P3] 每個 test 重複 from-import 是噪音（25+ 次）
- 現狀：`from ticket_system.commands.migrate import ...` 在 26 個地方重複
- 問題：fixture 已用 monkeypatch.setattr 改 module 屬性，module-level import 即可
- Good taste 改寫：移到檔案頂端 import 一次
- 影響：純風格

#### F4：[Good taste 點] AC4 零殘留檢查（line 504-526）
- 用集合 + glob 全掃，比 grep prefix 強壯。重新看問題、消除誤判特例的好品味。肯定。

---

### Linus 三層判斷

| 層次 | 判斷 |
|------|------|
| 資料結構 | 良好。frontmatter dict、children 雙形式忠實反映生產資料 |
| 特例情況 | F1 補丁式特例；F2 沒看出共性的複製 |
| 複雜度 | 587 行可接受。F1+F2 修掉後降至約 480 行 |

---

### 建議追蹤（規則 5：所有發現必須 Ticket）

| 草案 ID | 類型 | 優先級 | 主題 |
|---------|------|--------|------|
| W11-後續-test-yaml-helper | IMP | P2 | F1: `_write_ticket` 改用 yaml.dump |
| W11-後續-test-ac2-parametrize | IMP | P2 | F2: AC2 五 test 改 parametrize |
| W11-後續-test-import-cleanup | IMP | P3 | F3: 統一 module-level import |

三項皆測試重構（不影響覆蓋率與 GREEN），可排入 W11 後續或 v0.19 技術債波次。正式 ID 由 PM 建立。

---

### 結論

| 項目 | 結論 |
|------|------|
| 重構必要性 | 否（不影響功能）；建議排入後續波次以 P2/P3 處理 |
| 技術債務 | 三項風格債（F1-F3） |
| 程式碼品質 | Acceptable（B+）。AC4 優秀，AC1-AC3 有改善空間 |
| 可合併 | 是。1751 passed 無回歸；不阻擋當前 ticket complete |

**Linus 風格總評**：coverage is solid, AC4 idempotency check is good taste. But the test helpers are quietly duplicating what pyyaml already gives you for free, and AC2 is five copies of the same function. Not garbage, but not yet good taste. Fix it next wave.
### Phase 3a 實作策略規劃（pepper-test-implementer, 2026-04-29）

#### 策略總覽

phase2 已產出 15 個 RED 骨架（4 類別）+ `_write_ticket` / `_read_frontmatter` / `project_with_tickets` 三個 fixture。phase3a 任務：為 phase3b 提供「填補測試體」的虛擬碼級指引，把 `pytest.fail("RED: ...")` 換成可執行的 GWT 三段式。

**核心原則**（語言無關）：
1. **不改 migrate.py**——PM 前台已驗證邏輯完整，本 ticket 只補 regression 防護
2. **不抽新 fixture**——既有三個工具函式對 15 測試足夠，引入額外 helper 反增認知負擔
3. **每測試自包含**——Given 區段直接呼叫 `_write_ticket(...)`，避免共用 setUp/setUpClass
4. **斷言聚焦該測試的 AC**——避免一個測試驗證所有欄位（會掩蓋 root cause）

#### 共通虛擬碼骨架（GWT 三段式）

每個測試體採用統一結構，phase3b 依此貼樣即可:

```pseudocode
def test_xxx(self, project_with_tickets):
    tmp_path, tickets_dir = project_with_tickets

    # Given: 寫入初始 ticket disk 狀態
    _write_ticket(tickets_dir, "<id_1>", {"<field>": <value>})
    _write_ticket(tickets_dir, "<id_2>", {"<field>": <value>})

    # When: 觸發目標函式
    from ticket_system.commands.migrate import _update_cross_references
    result = _update_cross_references(old_id, new_id)

    # Then: 讀回 disk 驗證
    fm = _read_frontmatter(tickets_dir / "<id>.md")
    assert fm["<field>"] == <expected>
```

**關鍵呼叫慣例**:

| 函式 | 簽名 | 備註 |
|------|------|------|
| `_update_cross_references(old_id, new_id)` | 直接呼叫 | 回傳 int（updated_count），AC2 多欄位測試會用到 |
| `_migrate_single_ticket(version, source_id, target_id, dry_run=False, backup=False)` | 端到端 | AC1 第 4 測試 / AC4 / AC3 內部 |
| `_batch_migrate(version, config_file, dry_run=False, backup=False)` | 批量入口 | AC3 / AC4，需先寫 migrations.yaml |

#### 各 AC 測試填補策略

##### AC1 — TestAC1_ParentChildrenUpdated（4 測試）

共同 Given：父 P 寫入 children 含 old_id 的不同形式 + 子 ticket old_id 寫入 disk。

| 測試 | Given 重點 | When | Then 斷言 |
|------|-----------|------|----------|
| children_as_string_list | P.children = [W5-018, W5-019] | _update_cross_references(W5-018, W11-003.1) | fm["children"] == [W11-003.1, W5-019] |
| children_as_dict_list | P.children = [{id: W5-018, type: IMP}, {id: W5-019, type: ANA}] | 同上 | 第一筆 id 替換、type 保留；第二筆不變 |
| children_mixed | P.children = [W5-018, {id: W5-018, type: IMP}, W5-099] | 同上 | 前兩筆都替換；第三筆不變 |
| full_single_migrate_flow | P.children=[W5-018] + W5-018 自身存在 | _migrate_single_ticket("0.18.0", W5-018, W11-003.1, backup=False) | (a) 新檔存在；(b) 舊檔不存在；(c) P.children=[W11-003.1] |

注意：`_update_cross_references` 內部以 `extract_core_ticket_id(file.stem) == new_id` 跳過剛遷移的 ticket。父 P 的 core_id 與 new_id 不同，不會被誤跳。

##### AC2 — TestAC2_ExternalReferencesUpdated（6 測試）

模式統一：寫一外部 E + 一個 old_id 佔位 ticket → _update_cross_references → 讀 E 驗欄位。

| 測試 | E 欄位 | Then |
|------|--------|------|
| blockedby | blockedBy: [old, other] | [new, other] |
| relatedto | relatedTo: [old, other] | [new, other] |
| parent_id | parent_id: old | new |
| source_ticket | source_ticket: old | new |
| spawned_tickets | spawned_tickets: [old, other] | [new, other] |
| multiple_fields | E 同時含五欄位都引用 old | 五欄位都更新 + 回傳 1（同檔案只計一次） |

##### AC3 — TestAC3_BatchCrossMigrationReferences（3 測試）

共通：寫 migrations.yaml 於 tmp_path → _batch_migrate(version, str(yaml_path), dry_run=False, backup=False)

migrations.yaml 格式（依 _load_migration_config 規格）:

```yaml
migrations:
  - from: 0.18.0-W5-A
    to: 0.18.0-W11-A
```

| 測試 | 設置 | Then |
|------|------|------|
| updates_subsequent | A 存在 + C.blockedBy=[A] + yaml 單筆 A→B | A 不存、B 存、C.blockedBy=[B] |
| two_step_chain | A、C 存在；C.blockedBy=[A]；yaml 兩筆 A→A_new, C→C_new | A、C 舊檔不存；A_new、C_new 存；C_new.blockedBy=[A_new] |
| dict_children | P.children=[{id:A,type:IMP},{id:D,type:IMP}]；yaml 兩筆 | P.children=[{id:A_new,type:IMP},{id:D_new,type:IMP}]，type 保留 |

##### AC4 — TestAC4_W11ReorganizationScenario（2 測試）

主驗收（full_consistency）虛擬碼（zero residue 整合）:

```pseudocode
P_id = "0.18.0-W11-003"
c1, c1_new = "0.18.0-W5-018",  "0.18.0-W11-003.1"
c2, c2_new = "0.18.0-W10-022", "0.18.0-W11-003.2"
c3, c3_new = "0.18.0-W10-038", "0.18.0-W11-003.3"

_write_ticket(tickets_dir, P_id, {"children": [c1, c2, {"id": c3, "type": "IMP"}]})
for cid in [c1, c2, c3]: _write_ticket(tickets_dir, cid, {})
_write_ticket(tickets_dir, "E1", {"blockedBy": [c1]})
_write_ticket(tickets_dir, "E2", {"relatedTo": [c2]})
_write_ticket(tickets_dir, "E3", {"parent_id": c3})

# 寫 migrations.yaml 三筆 → _batch_migrate

# Then:
# 1. 三 child 已 rename（舊不存、新存）
# 2. fm_p["children"] == [c1_new, c2_new, {"id": c3_new, "type": "IMP"}]
# 3. E1.blockedBy=[c1_new] / E2.relatedTo=[c2_new] / E3.parent_id=c3_new
# 4. 零殘留：所有 *.md 文字內無 c1/c2/c3
```

冪等測試（idempotency）：在主驗收最終 disk 狀態後再執行同 yaml → 來源不存在 → 全 skip → return 0；P.children 不變。

#### 技術決策記錄

| 決策 | 選擇 | 理由 |
|------|------|------|
| fixture 重用 vs 抽新 helper | 沿用 sage 三 helper，不抽新 | 15 測試都用相同模式，新 helper 反增閱讀跳轉 |
| parameterize vs 獨立 method | 保持獨立 method | 失敗訊息精準，regression 防護價值 |
| _update_cross_references 直呼 vs 端到端 | AC1/AC2 多直呼；AC1#4 + AC3/AC4 端到端 | 隔離單元 + 整合確認互補 |
| migrations.yaml 位置 | tmp_path 根目錄 | _load_migration_config 接受任意路徑 |
| version 參數值 | 統一 "0.18.0" | _migrate_single_ticket 內部從 source_id 重新提取（line 264-265） |
| backup 必須 False | 不關閉產生附帶 .claude/migration-backups 檔 | 聚焦反向引用測試 |

<!-- PC-093-exempt: ticket-tracked:0.18.0-W11-003.6 phase3a 三項 TD 已決斷（TD#1 移除/TD#2 執行/TD#3 執行）並於 phase3b 落地 -->
#### Phase 3a 技術債決策（PC-093 強制決斷，2026-04-29 PM）

原 pepper 報告三項延後話術，PM 用 AskUserQuestion 逐項決斷如下：

**TD #1 AC2 parameterize（WRAP 分析）**

- W (Widen)：(a) 維持獨立 method (b) 改 parameterize (c) 混合（shared body + 顯式呼叫）
- R (Reality)：migrate.py 反向引用六欄位狀態穩定（PM Phase 1 驗證），新增欄位需要 schema 變更，屬高摩擦事件；獨立 method 失敗訊息精準價值在 regression 防護中不可替代
- A (Distance)：10 個月後若新增第 7 欄位，當下重構 method 名稱化 parameterize 成本可控
- P (Prepare wrong)：假設「5 欄位穩定」失準時，補救路徑是新 ticket 重構（非延後到 Phase 4）

決策：**保持獨立 method 設計**，移除「未來可能膨脹」的速記；若實際發生才觸發新 ticket。

**TD #2 _update_ticket_id_references self-loop 未測**

決策：**Phase 3b 立即補測試**（user 指示）。新增 1 測試覆蓋 self-loop 行為（即使實務罕見，覆蓋率 100% 鎖定現有實作）。

**TD #3 AC4 殘留用 substring 檢查**

決策：**Phase 3b 立即修正**（user 指示）。改用集合成員與精確 ID 比對（list/set membership）取代 substring scan，避免 prefix 誤判。

(以上三項已從技術債轉為 Phase 3b 工作項，無延後)

#### Phase 3b 交接清單（thyme-python-developer）

1. 依此文件填補測試體，每個 pytest.fail 換成 GWT 模式
2. 保留 sage 的 GWT docstring，只替換函式 body
3. 不改 fixture / 不改 _write_ticket / _read_frontmatter
4. 執行 `uv run pytest .../test_migrate_reverse_refs.py -v` 確認 15/15 GREEN
5. 若 GREEN 失敗來自 migrate.py 邏輯缺陷 → 停止實作、回報 PM
6. commit: `chore(0.18.0-W11-003.6): phase3b thyme 補測試體 GREEN`

#### 不在 Phase 3a 範圍

- 實際撰寫測試體（phase3b 職責）
- 修改 migrate.py（PM 已驗證）
- W11-002/003 父 children 舊 ID 殘留清理（0.18.0-W11-003.8 follow-up）
### Phase 2 測試設計（sage-test-architect, 2026-04-29）

#### 測試案例設計（4 條 AC × 15 個測試）

對應檔案：`.claude/skills/ticket/ticket_system/tests/test_migrate_reverse_refs.py`

| AC | 測試類別 | 測試方法 | 行為鏈推演 |
|----|---------|---------|----------|
| AC1 | TestAC1_ParentChildrenUpdated | test_children_as_string_list_updated | 建父 ticket（string list children 含 old_id）→ 呼叫 _update_cross_references → 驗證 string list 內 old_id 已替換 |
| AC1 | TestAC1_ParentChildrenUpdated | test_children_as_dict_list_updated | 建父 ticket（dict children {id: old_id, type: IMP}）→ 更新 → 驗證 dict.id 替換且其他欄位保留 |
| AC1 | TestAC1_ParentChildrenUpdated | test_children_mixed_string_and_dict_forms | 建 children 同時混用 string + dict 形式 → 同時驗證兩種形式被替換 |
| AC1 | TestAC1_ParentChildrenUpdated | test_full_single_migrate_flow_updates_parent_children | 端到端走 _migrate_single_ticket（含 file rename + parent children 同步） |
| AC2 | TestAC2_ExternalReferencesUpdated | test_blockedby/relatedto/parent_id/source_ticket/spawned_tickets_updated | 五欄位單獨各一個測試（單欄位回歸鎖定） |
| AC2 | TestAC2_ExternalReferencesUpdated | test_multiple_fields_in_single_ticket_all_updated | 同一 ticket 五欄位同時引用 old_id → 一次更新 + updated_count 計數正確 |
| AC3 | TestAC3_BatchCrossMigrationReferences | test_batch_migrate_updates_subsequent_references | 寫 migrations.yaml + 兩筆 ticket → 呼叫 _batch_migrate → 驗證後續引用同步 |
| AC3 | TestAC3_BatchCrossMigrationReferences | test_batch_migrate_two_step_chain | 兩筆遷移序列（A→A_new、C→C_new）+ C.blockedBy 引用 A → 跨遷移引用解析正確 |
| AC3 | TestAC3_BatchCrossMigrationReferences | test_batch_migrate_with_dict_children_in_parent | 批量模式下 dict children 兩筆連續更新（type 等其他欄位完整保留） |
| AC4 | TestAC4_W11ReorganizationScenario | test_w11_reorganization_full_consistency | **AC4 主驗收**：模擬實際 W11-003 殘留情境三筆批量 + 父 children 混合形式 + 三類外部引用 + 零殘留檢查 |
| AC4 | TestAC4_W11ReorganizationScenario | test_w11_reorganization_idempotency | 重複執行 batch migrate 的冪等性與 skip 行為 |

#### 反向引用六欄位覆蓋率規劃

| 欄位 | 形式 | migrate.py 實作位置 | 覆蓋測試 |
|------|------|--------------------|---------|
| blockedBy | string list | 126-131 | AC2 / AC3 / AC4 |
| relatedTo | string list | 134-139 | AC2 / AC4 |
| children | string list | 142-147 | AC1 / AC4 |
| children | dict {id:...} | 148-150 | AC1 / AC3 / AC4 |
| source_ticket | string | 153-155 | AC2 |
| parent_id | string | 158-160 | AC2 / AC4 |
| spawned_tickets | string list | 163-168 | AC2 |

#### 測試環境技術決策

| 項目 | 決策 | 理由 |
|------|------|------|
| 測試類型 | 整合測試 | 需驗證 frontmatter parse + disk write + cross_references 全流程 |
| 隔離手段 | tmp_path + monkeypatch get_project_root | 沿用 tests/test_migrate_cross_references.py（W10-037）穩定模式 |
| 兩處 patch | migrate_mod / paths_mod 都需 monkeypatch | migrate.py 直接 import get_project_root，paths_mod 內部 get_tickets_dir 也呼叫 |
| RED 標記方式 | pytest.fail("RED: phase3 ...") | sage 僅設計測試骨架（含完整 GWT docstring），由 phase3 實作者補測試體 |

#### Sociable Unit Tests 邊界

- 真實使用：_update_cross_references / _migrate_single_ticket / _batch_migrate / parse_frontmatter / save_ticket / disk I/O / YAML 解析
- 隔離（monkeypatch）：get_project_root（外部世界）
- 不 mock：Domain 層（ticket dict / frontmatter format / migration config schema）

#### 拆分友善性（Phase 3b 派發考量）

| 測試類別 | 功能職責 | 跨類別依賴 |
|---------|---------|-----------|
| TestAC1_ParentChildrenUpdated | 父 children 字段（兩形式） | 無，可獨立派發 |
| TestAC2_ExternalReferencesUpdated | 外部 ticket 五欄位引用 | 無，可獨立派發 |
| TestAC3_BatchCrossMigrationReferences | batch_migrate 跨遷移引用 | 依賴 AC1/AC2 邏輯但測試獨立 |
| TestAC4_W11ReorganizationScenario | W11 重組整合情境 | 依賴前三組行為，整合層測試 |

#### Handoff to phase3a (pepper-test-implementer)

- 4 條 AC × 15 個測試對應完整
- string list + dict children 兩形式顯式覆蓋（AC1 三個測試）
- 反向引用六欄位全部對應測試類別
- W11 重組情境（AC4）含正向 + 冪等兩個測試
- tmp_path + monkeypatch fixture 沿用 W10-037 既有模式
- 各 AC 群組標註功能職責歸屬與獨立性（拆分友善）
- RED 狀態確認（15/15 fail with explicit pytest.fail markers）
## Test Results

<!-- To be filled by executing agent -->

---


### Phase 3b GREEN（thyme-python-developer, 2026-04-29）

執行指令：`(cd .claude/skills/ticket && uv run pytest ticket_system/tests/test_migrate_reverse_refs.py -v)`

新測試結果：**16 passed in 0.07s**（含 TD#2 self-loop 新測試）

| 測試類別 | 測試數 | 狀態 |
|---------|-------|------|
| TestAC1_ParentChildrenUpdated | 4 | 4/4 GREEN |
| TestAC2_ExternalReferencesUpdated | 6 | 6/6 GREEN |
| TestAC3_BatchCrossMigrationReferences | 3 | 3/3 GREEN |
| TestAC4_W11ReorganizationScenario | 2 | 2/2 GREEN（含 TD#3 集合精確比對） |
| TestTD2_UpdateTicketIdReferencesSelfLoop | 1 | 1/1 GREEN（TD#2 新增） |
| **總計** | **16** | **16/16 GREEN** |

#### 完整 ticket 測試套件 regression 驗證

執行指令：`(cd .claude/skills/ticket && uv run pytest --tb=no -q)`

結果：**1751 passed in 818.65s (0:13:38)**，無回歸。

#### 實作要點

1. 沿用 pepper 策略：每個測試 GWT 三段式，未抽額外 helper
2. TD#2 self-loop：覆蓋 source_ticket 自我引用更新行為（鎖定 migrate.py:58-79 既有實作）
3. TD#3 集合精確比對：AC4 主驗收 zero-residue 從 substring scan 改為 frontmatter 欄位逐一比對 list/dict membership，避免 prefix 誤判
4. monkeypatch 微調：將 paths_mod patch 改為 try/except 容錯（避免 ImportError），並補 patch ticket_loader 模組的 get_project_root（_read_frontmatter 流程需透過 loader 解析）

#### 偏離 pepper 策略說明

無實質偏離；僅補 monkeypatch 容錯（pepper 策略未指定，phase3a/3b 通用工程處理）。
### Phase 2 RED 確認（2026-04-29）

執行指令：
\`uv run pytest ticket_system/tests/test_migrate_reverse_refs.py -v\`

結果摘要：
\`\`\`
============================== 15 failed in 0.07s ==============================
\`\`\`

15/15 全部 RED，所有測試以 \`pytest.fail("RED: phase3 ...")\` 顯式標記，符合 sage 設計骨架階段預期。

| 測試類別 | 測試數 | RED 確認 |
|---------|-------|---------|
| TestAC1_ParentChildrenUpdated | 4 | 4/4 fail |
| TestAC2_ExternalReferencesUpdated | 6 | 6/6 fail |
| TestAC3_BatchCrossMigrationReferences | 3 | 3/3 fail |
| TestAC4_W11ReorganizationScenario | 2 | 2/2 fail |
| **總計** | **15** | **15/15 fail** |

所有失敗都來自顯式 pytest.fail 標記（非 ImportError 或 AttributeError），代表測試結構正確、phase3 實作者可直接依 GWT docstring 補測試體。
## Completion Info

**Completion Time**: 2026-04-29T03:13:49
**Executing Agent**: thyme-python-developer
**Review Status**: pending