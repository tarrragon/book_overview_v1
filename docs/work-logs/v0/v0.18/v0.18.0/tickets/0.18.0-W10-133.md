---
id: 0.18.0-W10-133
title: 修 validator _is_placeholder 對非表格 N/A 字面誤判（PC-138/PC-144 家族延伸）
type: IMP
status: in_progress
version: 0.18.0
wave: 10
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W10-131
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層:Bug 修復
  final_decision: validator 跳過描述性 Layer/Layer 2/Layer X N/A 字面標記
  rationale: W10-131 ANA 自證有實際場景 + PC-138/PC-144 家族治本
who:
  current: thyme-python-developer
  history: {}
what: 修 validator _is_placeholder 對非表格 N/A 字面誤判（PC-138/PC-144 家族延伸）
when: v0.18.0 wave 10 中
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/lib/ticket_validator.py
why: W10-131 ANA 自證案例：撰寫描述「N/A keyword 誤判」的 ANA 過程中遭遇 _is_placeholder 阻擋 complete，必須
  --skip-body-check 強制完成。證實非表格情境（描述性 Layer N/A 標記）有實際 use case。PC-138 W10-125 已修主場景（表格
  cell），需擴展非表格情境
how:
  task_type: Implementation
  strategy: Step 1 確認當前 _is_placeholder 邏輯（非表格情境的 N/A 判定）/ Step 2 新增 Layer N/A / Layer
    X N/A 字面豁免 / Step 3 新增 pytest 案例覆蓋非表格 N/A 合法用途
acceptance:
- '[ ] _is_placeholder 對 Layer 2: N/A 等描述性標記不誤判'
- '[ ] 新增 pytest ≥3 案例覆蓋非表格 N/A'
- '[ ] 既有測試無 regression'
- '[ ] PC-138 同家族案例補登錄'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-15T17:11:39'
completed_at: null
created: '2026-05-13'
updated: '2026-05-13'
---

# Execution Log

## Task Summary

修 validator _is_placeholder 對非表格 N/A 字面誤判（PC-138/PC-144 家族延伸）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

### 問題根因

W10-125（PC-138）已修 `_is_placeholder` 對表格 cell 內 N/A 的誤判（透過 markdown 表格 row 偵測排除）。但 W10-131 ANA 撰寫時自證另一個未覆蓋場景：描述性文字「Layer 2: N/A」「Phase X N/A」這類**非表格情境**的合法 N/A 標記，仍會觸發 placeholder 判定，被 `--skip-body-check` 強制繞過。

PC-144 家族訊號：validator 短英文標記（TODO/TBD/N/A）regex 對 substring 過度敏感，需要更精準的 context 豁免。

### 影響範圍

- `.claude/skills/ticket/ticket_system/lib/ticket_validator.py` — `_is_placeholder()` 函式
- 既有 pytest 測試（`.claude/skills/ticket/tests/`）
- 影響所有含描述性 N/A 標記的 ANA / IMP ticket body

### 相關 Error Pattern

- PC-138（表格 N/A 誤判，W10-125 已修主場景）
- PC-144（validator TODO/TBD/N/A regex word boundary，W17-094 已修 word boundary）
- 本 ticket 為 PC-138 家族延伸：非表格描述性 N/A

### WRAP 三問（claim 後 Checkpoint）

**W（Widen）—— 候選方案**

1. 方案 1（採用）：擴充 `_is_placeholder` 豁免規則，新增「Layer N」「Layer X」「Phase X」等前綴搭配 N/A 的字面豁免
2. 方案 2：改用 context-aware 判斷（是否在列表項 / 描述句中）
3. 方案 3：白名單機制定義合法 N/A 上下文清單

選擇方案 1 理由：W10-125 已採類似 incremental pattern；範圍可控；測試易補。

**A（Attain distance）—— 機會成本**

修這個 P3 bug 會擠壓 P2 W11-025/W11-028。但本 ticket ANA 已完成（W10-131），Strategy 明確，acceptance 清楚，實作成本低（< 1 ticket-day），且為 PC-138 家族治本，ROI 合理。

**P（Prepare to be wrong）—— 失敗風險預想**

最可能失敗：豁免 regex 範圍過寬，誤放行真正的 placeholder（如「Layer 2: TODO N/A」內含 TODO 應仍判 placeholder）。

防護措施：pytest case 必須含混合情境（合法描述性 N/A + 真正 placeholder 共存），確保豁免不擴散。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 修復摘要

於 `.claude/skills/ticket/ticket_system/lib/ticket_validator.py` `_is_placeholder()` 函式新增 W10-133 豁免段：剝除非表格描述性 `Layer N: N/A` / `Layer X N/A` / `Phase X N/A` 行（行內 keyword 為 N/A）後再做 keyword 檢查。

### 修改位置

- `ticket_validator.py:390-411`：新增 `descriptive_na_line` regex `^[\s\-\*\+>]*(?:Layer\s+\w+|Phase\s+\w+)\s*[:：]?\s*N/A\s*\.?\s*$`，剝除描述性 N/A 行；剝除後仍含 keyword → 維持 placeholder 判定；剝除後為空且原本含描述性 N/A 行 → 視為非 placeholder 直接返回 False
- 中文 placeholder 檢查（行 413-419）改用 `target_after_descriptive` 保持一致性

### 設計決策（對應 Problem Analysis WRAP）

- 採方案 1（incremental pattern，延續 W10-125 設計）
- 保守豁免邊界：僅匹配 `Layer\s+\w+` / `Phase\s+\w+` 前綴 + 行內 N/A 為 keyword；混合 TODO/TBD 的行不豁免（如 `Layer 1: TODO` 仍判 placeholder，對應 P 防護）
- 與 acceptance_auditor 共用：`acceptance_auditor._is_placeholder` 已 delegate 到本實作（ARCH-020 治本），單點修復自動同步

### 自檢結果 Layer 1（文件 / 二次審查）

- 表格分類有後續說明：是（行 390-394 註解說明豁免設計）
- 核心原則先行：是（W10-133 註解明示 PC-138 家族延伸）
- 負向對比有正向錨點：是（測試案例含 regression 守護 + 防擴散混合情境）
- 無錯字 / 禁用詞 / 簡體字：已掃描

### 不在本 ticket 範圍

- 其他 short keyword（TBD/TODO）的描述性豁免（未來如 ANA 出現 `Layer 2: TBD` 合法情境再延伸）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 測試指令

```
(cd .claude/skills/ticket && uv run pytest tests/test_ticket_validator.py -x)
npm run test:hooks
```

### 結果

- `test_ticket_validator.py`: **63 passed**（含本 ticket 新增 8 個 `TestDescriptiveNAExemption` 案例）
- `npm run test:hooks`: **2272 passed, 48 skipped**（無 regression）

### 新增測試案例（覆蓋 acceptance #2 ≥3 條）

| 案例 | 預期 | 對應 acceptance |
|------|------|----------------|
| test_table_cell_na_not_placeholder | False | regression 守護（W10-125） |
| test_bare_na_still_placeholder | True | 豁免不擴散 |
| test_bare_todo_still_placeholder | True | 豁免不擴散 |
| test_descriptive_layer_na_not_placeholder | False | W10-133 主場景 #1 |
| test_descriptive_phase_na_not_placeholder | False | W10-133 主場景 #2 |
| test_only_descriptive_na_lines_not_placeholder | False | W10-133 邊界（純描述性 N/A） |
| test_descriptive_layer_with_todo_still_placeholder | True | 混合情境防擴散 |
| test_descriptive_na_mixed_with_bare_na_still_placeholder | True | 混合情境防擴散 |

### CLI 重裝

`uv tool install .claude/skills/ticket --reinstall` 已執行（PC-145 必要步驟）。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.18.0-W10-131 | chars: 420 -->

### Task Reference
- 0.18.0-W10-131 what: 分析 ticket skill 既有測試失敗 + Validator N/A keyword 誤判整合修復方向（W10-107 執行中發現）

### Rationale Chain
- 0.18.0-W10-131 why: W10-107 thyme 完成後報告 2 個範圍外發現：(1) commands/resume.py:195 行號漂移導致 test_no_bare_status_completed_filter.py 2 failures（既有失敗）；(2) Validator _is_placeholder 對 Layer 2 N/A 標記字面誤判（PC-138/PC-144 同家族延伸，hook PostToolUse 建議標記語法與 validator 衝突）

### Related Files
- .claude/skills/ticket/ticket_system/commands/resume.py  # from 0.18.0-W10-131
- .claude/skills/ticket/tests/test_no_bare_status_completed_filter.py  # from 0.18.0-W10-131

### PM 執行指引（dispatch context）

**派發位置安全性**：當前主 repo main branch 非 worktree，ARCH-015 允許 subagent Edit `.claude/` 路徑。

**執行步驟（對應 frontmatter Strategy Step 1-3 + 防護補強）**：

1. 讀取 `.claude/skills/ticket/ticket_system/lib/ticket_validator.py` 的 `_is_placeholder` 當前邏輯，定位 W10-125（PC-138 表格 cell 豁免）與 W17-094（PC-144 word boundary）的修復點
2. 新增豁免：非表格情境的描述性「Layer N: N/A」/「Layer X N/A」/「Phase X N/A」字面（對應 Problem Analysis 方案 1）
3. 在 `.claude/skills/ticket/tests/` 新增或擴充 pytest ≥3 案例：
   - 純表格 N/A cell — 既有應通過（regression 守護）
   - 描述性合法 — 「Layer 2: N/A」/「Phase X N/A」不判 placeholder
   - 混合情境 — 「Layer 2: TODO N/A」內含 TODO 應仍判 placeholder（防豁免擴散，對應 Problem Analysis P 防護）
4. 測試指令：`npm run test:hooks` + `(cd .claude/skills/ticket && uv run pytest tests/ -x)`
5. 修改完源碼後執行 `uv tool install .claude/skills/ticket --reinstall`（PC-145 CLI 重裝必要）

**Body 填寫責任（IMP schema, complete 前必填）**：

- Solution（agent 填）：實作摘要、修改位置（含 line numbers）、設計決策
- Test Results（agent 填）：執行指令、通過數、commit SHA

**收尾政策**：

- commit message：`fix(0.18.0-W10-133): validator _is_placeholder 對非表格描述性 N/A 字面豁免（PC-138/PC-144 家族延伸）`
- commit 後執行 `ticket track check-acceptance --all 0.18.0-W10-133`
- acceptance 全綠 → `ticket track complete 0.18.0-W10-133`
- acceptance 有未通過 → NeedsContext 記錄缺口並停在 in_progress

**Acceptance 4 條（已在 frontmatter）**：

1. `_is_placeholder` 對 `Layer 2: N/A` 等描述性標記不誤判
2. 新增 pytest ≥3 案例覆蓋非表格 N/A
3. 既有測試無 regression
4. PC-138 補登錄家族延伸案例（追加 W10-133 條目到 `.claude/error-patterns/process-compliance/PC-138-*.md` Case History）