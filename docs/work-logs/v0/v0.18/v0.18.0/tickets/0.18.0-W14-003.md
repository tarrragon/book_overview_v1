---
id: 0.18.0-W14-003
title: 修復 test_create_duplicate_detection fixture setup 失敗 (26 errors)
type: IMP
status: completed
version: 0.18.0
wave: 14
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: 建立 IMP 追蹤 W12-005 實作期間發現的既有技術債
  rationale: quality-baseline-rule-5：所有發現必須追蹤
who:
  current: rosemary-project-manager
  history: {}
what: 修復 test_create_duplicate_detection fixture setup 失敗 (26 errors)
when: W12-005 實作期間發現，W14 階段處理
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/tests/test_create_duplicate_detection.py
  - .claude/skills/ticket/tests/conftest.py
why: W12-005 實作期間發現 test_create_duplicate_detection 全類 fixture setup 失敗，26 errors
  未進入 assertion 即掛掉
how: 'task_type: Implementation; strategy: 新增 pytest-mock>=3.10 至 pyproject.toml test
  extras，uv sync 安裝後驗證 46 passed + 1182 無回歸'
acceptance:
- '[x] fixture setup 不再失敗'
- '[x] 26 errors 清除'
- '[x] 測試全綠'
- '[x] 無回歸'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-17T15:08:09'
completed_at: '2026-04-17T15:10:21'
created: '2026-04-17'
updated: '2026-04-17'
---

# Execution Log

## Task Summary

修復 test_create_duplicate_detection fixture setup 失敗 (26 errors)

---

## Problem Analysis

### Context Bundle

**根因**：
- `.claude/skills/ticket/pyproject.toml` 的 `[project.optional-dependencies].test` 只宣告 `pytest>=7.0` 和 `pytest-cov>=4.0`
- `tests/test_create_duplicate_detection.py` 54 處使用 `mocker` fixture（pytest-mock 提供）
- 結果：全類 26 個 test method 都因 fixture 'mocker' not found 觸發 setup error

**影響範圍**：
- 僅 `test_create_duplicate_detection.py` 使用 mocker（grep 確認其他測試檔案為 0）
- 26 errors 未進入 assertion 即掛掉（不是 assertion 失敗）

**修復方案**：方案 A — 新增 pytest-mock 依賴
- 改動點：`.claude/skills/ticket/pyproject.toml` 的 `test` 群組加入 `pytest-mock>=3.10`
- 重新安裝：`uv tool install .claude/skills/ticket --reinstall`（IMP-023 規則）
- 驗證：重跑 `pytest tests/test_create_duplicate_detection.py` 確認 26 errors 歸零

**方案 B 排除理由**：改寫 54 處 mocker 為 unittest.mock.patch 工作量過大且 regression 風險高；pytest-mock 是業界標準 dev-dependency，不增加運行時依賴。

**派發限制**：
- 目標檔案在 `.claude/` 下，CC runtime 禁止 subagent Edit（ARCH-015 / PC-077）
- 唯一路徑：PM 前台直接修改

**WRAP 三問**：
- W（Widen）：方案 A 新增依賴 vs 方案 B 改寫測試。選 A。
- A（Attain distance）：此修復擠壓 v0.18 其他 failing test 修復時段，但成本極低（1 行改動 + 重裝），可忽略。
- P（Prepare to be wrong）：最可能失敗 — uv tool install --reinstall 未正確更新依賴。防護：跑測試後若仍 fixture not found 則 `uv pip install pytest-mock` 直接安裝到 venv。
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

### 修復內容

**單點改動**：`.claude/skills/ticket/pyproject.toml` 的 `[project.optional-dependencies].test` 新增 `pytest-mock>=3.10`

```diff
 test = [
     "pytest>=7.0",
     "pytest-cov>=4.0",
+    "pytest-mock>=3.10",
 ]
```

**環境同步**：`uv sync --extra test` 安裝 pytest-mock 3.15.1 到 .claude/skills/ticket/.venv

**派發限制落地**：PM 前台直接 Edit（subagent 對 `.claude/` 被 CC runtime 擋），符合 ARCH-015 / PC-077。
## Test Results

<!-- To be filled by executing agent -->

---

### 驗證結果

**目標測試**：`tests/test_create_duplicate_detection.py`
- 修復前：20 passed, 26 errors（全類 fixture setup 失敗）
- 修復後：46 passed, 0 errors

**全套測試回歸檢查**：`uv run pytest`
- 結果：1182 passed, 0 failed, 0 errors（3.64s）
- 結論：無回歸

**驗收條件逐項確認**：
- [x] fixture setup 不再失敗
- [x] 26 errors 清除
- [x] 測試全綠（46 passed）
- [x] 無回歸（1182 passed）
## Completion Info

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending