---
id: 0.18.0-W10-086
title: 修復 ticket CLI set-who/set-where/set-how 命令壓扁 dict 欄位為 string bug
type: IMP
status: pending
version: 0.18.0
wave: 10
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層:IMP 修復
  final_decision: 修復 CLI set-* 命令的 dict 欄位處理邏輯（僅更新子欄位，不覆寫整 dict）
  rationale: quality-baseline-rule-5 所有發現必須追蹤 + PM 執行 W10-085 時實測發現
who:
  current: thyme-python-developer
  history: {}
what: 修復 ticket CLI set-who/set-where/set-how 命令壓扁 dict 欄位為 string bug
when: W10-085 PM 執行時實測發現（set-where/set-how 後 frontmatter 被壓扁）
where:
  layer: CLI Command Layer (.claude/skills/ticket/ticket_system/commands/)
  files:
  - .claude/skills/ticket/ticket_system/commands/track.py
  - .claude/skills/ticket/ticket_system/commands/fields.py
why: PM 執行 W10-085 時用 set-where/set-how 設定欄位值，發現三個 set-* 命令將 dict 型欄位（who/where/how）整個覆寫為
  string，導致後續 ticket create --parent 失敗（dict.get AttributeError）；影響 ticket 結構完整性與後續
  CLI 操作可用性；違反 SRP（set-where 應改 layer 子欄位而非整個 where）
how:
  task_type: Implementation
  strategy: 參考 fields.py 第 303-325 行 execute_set_decision_tree 已存在的 dict 子欄位更新模式；修改
    execute_set_field 第 150 行硬編碼 ticket[field] = new_value，改為依欄位型別分流：dict 型欄位（who/where/how）僅更新子欄位（current/layer/strategy），string
    欄位保留原邏輯；建議建立 DICT_FIELD_SUBKEY 映射表（DRY）；測試覆蓋 3 fixture 場景：完整 dict / 已壓扁為 string
    / 初始化「待定義」string
acceptance:
- '[ ] set-who <id> <value> 僅更新 who.current 子欄位，保留 who.history dict 結構'
- '[ ] set-where <id> <value> 僅更新 where.layer 子欄位，保留 where.files list'
- '[ ] set-how <id> <value> 僅更新 how.strategy 子欄位，保留 how.task_type'
- '[ ] 單元測試覆蓋三命令的 dict 結構保護（before/after frontmatter 對照）'
- '[ ] 測試檔案含 regression fixture（3 場景：完整 dict、已壓扁為 string、初始化「待定義」string），assertion
  驗證修復後 dict 結構完整性與子欄位更新正確性'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: true
started_at: null
completed_at: null
created: '2026-04-19'
updated: '2026-04-19'
---

# Execution Log

## Task Summary

修復 ticket CLI set-who/set-where/set-how 命令壓扁 dict 欄位為 string bug

---

## Problem Analysis

## Creation 審核結果（2026-04-19）

### acceptance-auditor 報告
- 審核結論：需修正 → 已修正後通過
- 中缺陷 1: where.layer「待定義」（已改為「CLI Command Layer (.claude/skills/ticket/ticket_system/commands/)」）
- 中缺陷 2: AC 5「回歸重現驗證」不可驗證（已改為「測試檔案含 regression fixture 3 場景 + assertion 驗證」）
- 低缺陷 1: where.files 涵蓋性（thyme claim 前 grep 確認三命令位置；fields.py + track.py 已足涵蓋，依 saffron 研究確認）

### saffron-system-analyst 報告
- 審核結論：通過，建議 creation_accepted: true
- 根因定位：`fields.py` 第 150 行 `ticket[actual_field_name] = new_value`，對所有欄位一律整個覆寫為 string
- 既有正確模式：`execute_set_decision_tree`（第 303-325 行）已實作 dict 子欄位更新（讀取既有 dict → 更新指定 key → 回寫），修復參考此模式無需另造輪子
- DRY 建議：修復不應在每個 set-* 包裝器硬編碼子欄位名，而是在 `execute_set_field` 通用函式加 dict 型欄位的子欄位映射表（如 `DICT_FIELD_SUBKEY = {"who": "current", "where": "layer", "how": "strategy"}`）
- 測試策略：fixture 含 3 場景：(1) 欄位原本完整 dict、(2) 欄位已被壓扁為 string（降級場景，應恢復為 dict）、(3) 欄位初始化為「待定義」string（應升級為 dict）
- 系統衝突：無跨模組依賴衝突；API 簽名不變；無跨版本相容性影響

## Context Bundle（派發實作代理人時參考）

### 必讀文件
- 本 ticket：docs/work-logs/v0/v0.18/v0.18.0/tickets/0.18.0-W10-086.md
- 來源 ticket：docs/work-logs/v0/v0.18/v0.18.0/tickets/0.18.0-W10-085.md（bug 發現情境 + 首次重現）

### 目標修改檔
- .claude/skills/ticket/ticket_system/commands/fields.py（主要：execute_set_field 第 150 行）
- .claude/skills/ticket/ticket_system/commands/track.py（若需調整 set-* CLI 包裝器）

### 根因位置
`fields.py:150` `ticket[actual_field_name] = new_value` — 無型別區分，一律覆寫

### 參考既有正確模式
`fields.py:303-325` `execute_set_decision_tree` — dict 子欄位更新的正確實作

### 修復方向
1. 在 fields.py 加 DICT_FIELD_SUBKEY 映射表：`{"who": "current", "where": "layer", "how": "strategy"}`
2. execute_set_field 偵測欄位是否在映射表中：
   - 是 → dict 子欄位更新（保留既有 dict 其他 key；若欄位為 string 先轉換為 dict）
   - 否 → 原邏輯（string 覆寫）
3. 保留既有 API 簽名（無破壞性變更）

### 測試場景（fixture）
1. `who: {current: X, history: {}}` + set-who Y → `who: {current: Y, history: {}}`
2. `where: "壓扁後 string"` + set-where Y → `where: {layer: Y, files: []}`（降級恢復）
3. `how: "待定義"` + set-how Y → `how: {task_type: Implementation, strategy: Y}`（初始化）

### 精確 git add 範圍（PC-092 防護）
- .claude/skills/ticket/ticket_system/commands/fields.py
- .claude/skills/ticket/ticket_system/commands/track.py（若改動）
- .claude/skills/ticket/tests/ 下新增測試
- docs/work-logs/v0/v0.18/v0.18.0/tickets/0.18.0-W10-086.md
- docs/work-logs/v0/v0.18/v0.18.0/v0.18.0-main.md（若自動更新）

### 禁止行為
- 禁 add 其他 ticket md 或 session 外變更
- 禁修改 API 簽名（相容性）
- 禁無測試情況下 commit
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending