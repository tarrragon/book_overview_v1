---
id: 0.18.0-W16-004
title: 修復 sync-claude-pull.py 保留 hook executable bit
type: IMP
status: in_progress
version: 0.18.0
wave: 16
priority: P2
parent_id: null
children:
- 0.18.0-W16-004.1
- 0.18.0-W16-004.2
- 0.18.0-W16-004.3
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: create-improvement-ticket
  rationale: quality-baseline-rule-5-all-findings-must-track
who:
  current: rosemary (PM 前台，.claude/ subagent 被 CC runtime 擋)
  history: {}
what: 修復 sync-claude-pull.py 保留 hook executable bit
when: 執行 /sync-pull 後觀察 Stop hook Permission denied 時（執行中額外發現）
where:
  layer: Infrastructure
  files:
  - .claude/scripts/sync-claude-pull.py
  - .claude/scripts/sync-claude-push.py
why: '每次執行 /sync-pull 後，.claude/hooks/ 下所有 Python hook 檔案會遺失 executable bit（從 100755
  變 100644），導致 Stop/SessionStart hook 執行時回傳 Permission denied 錯誤。本次 sync-pull 實測需手動對
  40 個檔案（頂層 30 + acceptance_checkers/ 子目錄 10）執行 chmod +x 才能恢復。


  歷史先例：commit ac2dcbaa (chore: add executable bit to acceptance checker hook (auto-fix))
  曾修過同類問題，說明此缺陷反覆出現，屬系統性而非偶發。


  根本原因：sync-claude-pull.py 腳本同步檔案時未保留來源 repo 的 file mode（推測使用 shutil.copy 而非 shutil.copy2，或
  git checkout 時 core.filemode 設定未生效）。


  影響：

  (1) Stop hook 無法執行 → 結束時缺少 session-experience-persistence、worktree-auto-commit 等提醒

  (2) 每次 sync-pull 都需要 PM 手動 chmod，增加認知負擔

  (3) 違反規則 4「Hook 失敗必須可見」的精神（失敗雖可見，但根因未治本）'
how:
  task_type: Implementation
  strategy: '1. 定位 sync-claude-pull.py 中檔案複製/更新的邏輯

    2. 改用 shutil.copy2（保留 metadata）或顯式 os.chmod 還原 755

    3. 新增驗收步驟：sync-pull 結束後 assert 所有 .claude/hooks/**/*.py 為可執行

    4. 考慮對稱修復 sync-claude-push.py（避免推上去的 mode 本身就已損壞）

    5. 驗證方式：本地執行 sync-pull，確認 git diff --summary 無 mode change 100755 => 100644'
acceptance:
- '[x] sync-claude-pull.py 執行後，所有 .claude/hooks/**/*.py 自動擁有 executable bit (100755)'
- '[x] git diff --summary 無任何 mode change 訊息'
- '[x] 若 sync-claude-push.py 也有同樣缺陷，一併修復'
- '[ ] 本 Ticket 完成後執行一次 /sync-pull 驗證，不需手動 chmod'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-20T09:41:47'
completed_at: null
created: '2026-04-20'
updated: '2026-04-20'
---

# Execution Log

## Task Summary

修復 sync-claude-pull.py 保留 hook executable bit

---

## Problem Analysis

## Context Bundle

### 現象確認
本次 /sync-pull 後觀察到 Stop hook 回傳 Permission denied：
- session-experience-persistence-reminder-hook.py
- worktree-auto-commit-hook.py

`ls -l` 顯示檔案為 `-rw-r--r--`（未帶 +x），git diff --summary 顯示 40 個檔案 mode 從 100755 降為 100644。

### 根因定位（實證）
執行 `git clone --depth 1 https://github.com/tarrragon/claude.git` 到 temp 後對遠端 repo 檢查 `git ls-files --stage`：
- 遠端總計 1226 個 100644 + 147 個 100755
- hooks/acceptance_checkers/*.py 遠端記錄為 100644（本地 .git HEAD 是 100755）

**結論**：上游 tarrragon/claude.git 的 mode 本身已損壞。shutil.copy2 雖保留 source mode，但 source（clone 下來的 temp_dir）本身 mode 就錯，任憑如何保留都是錯的。

### 惡性循環根源
1. 某次 push 沒保留 mode → 遠端 repo 變 644
2. 後續 pull 從 644 拿 mode → 本地也變 644
3. 再次 push 把 644 推回去 → 惡化循環

### 解法邊界
- Pull 端 safety net：pull 後對 `.claude/hooks/**/*.py` 強制 chmod +x（convention-based）
- Push 端 safety net：push 前對 temp_dir/hooks/**/*.py 強制 chmod +x，確保 git add 記錄 100755
- 範圍限制：僅 hooks/ 目錄下 .py；scripts/ 有 644/755 混合（sync 腳本本身 644），不盲目處理
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## 實作摘要

### 新增常數
`EXECUTABLE_PY_SUBDIRS = ("hooks",)` — 同時加入 sync-claude-pull.py 和 sync-claude-push.py。未來若需擴充（如 skills/*/scripts/），只需修改此常數。

### 新增函式：restore_executable_bits()
兩檔案各一份對稱實作（scripts/ 下無共用 lib，暫受複製）：
- **pull 端** (`sync-claude-pull.py`): 參數 `claude_dir`，對 `claude_dir/hooks/**/*.py` chmod u+x g+x o+x
- **push 端** (`sync-claude-push.py`): 參數 `root`（=temp_dir），對 `root/hooks/**/*.py` 同樣處理

### 呼叫時機
- Pull: `_sync_with_backup` 內，cleanup_stale_files 後、detect_changed_packages 前
- Push: main 內，copy_filtered 後（clean_mode 後）、git add -A 前

### 驗證結果
單元測試（本 session）：
1. 隨機取一個 hook chmod -x 模擬損壞
2. 動態 import restore_executable_bits 執行
3. 輸出 `restored 1 files`，mode 還原為 -rwxr-xr-x ✓

語法驗證：pull/push 兩腳本都能正常 import 且暴露 restore_executable_bits ✓

完整 /sync-pull 驗證：需先 sync-push 把本修改推到遠端才能驗證（cyclic），屬 complete 後動作。

### 技術債記錄
- TD：restore_executable_bits 雙份複製（DRY 違反）— 可抽出至 `.claude/scripts/lib/` 共用 module
- TD：scripts/ 下的 mode 處理未涵蓋（sync 腳本本身 644 OK，其他如 check_i18n.py 應 755 但無 safety net）
- TD：遠端 repo mode 仍損壞（1226 個 100644）— 需一次性遠端修復（清除歷史 + 推送正確 mode，或逐一 chmod + push）
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending