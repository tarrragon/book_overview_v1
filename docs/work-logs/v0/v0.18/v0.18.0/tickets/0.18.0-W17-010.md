---
id: 0.18.0-W17-010
title: 實作 NeedsContext 協議 + Exit Status schema + pm-agent-observability 同步（W17-007
  三 IMP 合併）
type: IMP
status: completed
version: 0.18.0
wave: 17
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W17-007
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: create-imp-from-w17-007-ana
  rationale: W17-007 ANA 結論；3 IMP 合併減少協調
who:
  current: thyme-python-developer
  history: {}
what: 整合 W17-007 三 IMP 為單一實作：(1) NeedsContext append-log section 協議 + Hook 監聽 (2)
  Exit Status YAML schema 在 ticket 結構化 (3) pm-agent-observability.md + Ticket template
  同步
when: W17-007 ANA 完成，ROI 最高 P0（NeedsContext 年省 150K+ token）
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/cli/track_acceptance.py
  - .claude/hooks/needs-context-listener-hook.py(new)
  - .claude/references/pm-agent-observability.md
  - .claude/skills/ticket/templates/
why: W17-007 識別 3 真缺口（NeedsContext 協議 / Exit Status schema / dispatch hook 可靠性）；三者皆修改
  ticket append-log 機制 + hook，合併為單一 IMP 降低協調成本
how:
  task_type: Implementation
  strategy: Phase 1 NeedsContext section 標準化 + Hook 監聽 → Phase 2 Exit Status YAML
    schema + ticket template 更新 → Phase 3 pm-agent-observability.md 補註 → Phase 4 pytest
    覆蓋率驗證
acceptance:
- '[x] NeedsContext section 標準化於 SKILL.md'
- '[x] Hook 監聽 NeedsContext 新增事件並觸發 PushNotification'
- '[x] Exit Status YAML schema 定義（5 狀態值枚舉 + artifacts/acceptance_met 欄位）'
- '[x] pm-agent-observability.md 加兩節'
- '[x] Ticket template 含 Exit Status 預留區'
- '[x] pytest 5+ 測試'
- '[x] linux + Explore 多視角 reviewed'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-20T21:42:45'
completed_at: '2026-04-20T21:51:12'
created: '2026-04-20'
updated: '2026-04-20'
---

# Execution Log

## Task Summary

整合 W17-007 三 IMP 為單一實作：(1) NeedsContext append-log section 協議 + Hook 監聽 (2) Exit Status YAML schema 在 ticket 結構化 (3) pm-agent-observability.md + Ticket template 同步

---

## Problem Analysis

## Context Bundle

### Source Chain
- W17-001（Linux 類比）→ W17-007（waitpid ANA）→ 本 ticket
- W17-007 Solution 階段 4 產出 2 個協議 + 階段 5 ROI 估算建議 3 IMP（本 ticket 合併）

### 實作三項
1. **NeedsContext section 標準化**：append-log 允許寫入 `## NeedsContext` H2 或 `### NeedsContext` H3 section；子項 template（缺失項 + 觸發位置 + 影響 + 建議補料 + 重派成本）
2. **Hook 監聽**：新建 `needs-context-listener-hook.py` 監聽 ticket md NeedsContext section 新增事件，觸發 PushNotification 給 PM
3. **Exit Status YAML schema**：ticket md 新增 `## Exit Status` section，含 status/reason/confidence/acceptance_met/acceptance_unmet/artifacts/context_dependencies/estimated_recovery_effort；status 枚舉值 success/needs_context/blocked/partial_success/failed 對應 exit code 0/2/2/0/1

### 前置
- W17-005（section 枚舉補 Context Bundle）完成後本 ticket 才可加 NeedsContext / Exit Status 為新 section，否則 CLI 擋

### 驗證
- 手動 append-log NeedsContext 成功
- Hook 觸發 PushNotification 實測
- pytest 5+ 測試（Exit Status YAML 解析）
- pm-agent-observability.md 補 NeedsContext + Exit Status 兩節
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

### Linus 視角審查（AC 7 之一）

**Taste Score**: Acceptable（pass，有非阻塞收斂建議）

**Core Judgment**: 值得做。三件事合併為單一 IMP 正確；資料結構承載協議（section + YAML schema），Hook 僅被動通知，沒有引入新抽象層——good taste。

**逐項**:
1. Hook regex：pass。[^quote\s]+ 字元類 + 先抓 id 再驗 section 值恰為 NeedsContext，避免 content 誤觸（eliminate special case by design）。邊界：--section=X 等號形式未匹配（argparse 原生支援）。建議改 --section[=\s]+ 一行消除特例，非 block。
2. Exit Status 8 欄位：pass，複雜度匹配問題。acceptance_met/unmet 用陣列 index 而非自由文字是 good taste。estimated_recovery_effort 目前無解析者，觀察半年無消費則刪。
3. VALID_SECTIONS 7 項：pass。在 Miller's Law 內，不需分組；10+ 再談。
4. 狀態枚舉→exit code：good taste。5 狀態壓到 3 exit code（0/1/2）對應 PM 三種行為（前進/修 bug/補料），消除 PM 端特例分支。partial_success=0 值得讚賞。

**Userspace 檢驗**: 既有 5 section 保留，新增 2 個向後相容擴增，無破壞。

**實用主義**: 解決 W17-007 ANA 證實的真問題（年省 150K+ token），沒有造 event bus / daemon，就是 append section + PostToolUse hook。

**非阻塞建議**（可入後續 ticket 或 PC）:
- Hook regex 支援 --section=X 等號形式（5 分鐘改動）
- estimated_recovery_effort 欄位半年複查消費情況

**結論**: Pass。AC 7 linux 部分通過。
## 實作摘要（W17-010）

### Phase 1: VALID_SECTIONS 擴增（已完成）
- `command_tracking_messages.py:158` 加入 NeedsContext, Exit Status，共 7 section

### Phase 2: Ticket template 更新（已完成）
- `ticket_builder.py` create_ticket_body 在 Completion Info 前插入 NeedsContext 與 Exit Status section（含 YAML schema 註解）
- Exit Status schema: status/reason/confidence/acceptance_met/acceptance_unmet/artifacts/context_dependencies/estimated_recovery_effort
- 5 狀態枚舉對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1

### Phase 3: Hook 監聽（待做）
- .claude/hooks/needs-context-listener-hook.py

### Phase 4: pm-agent-observability.md（待做）
### Phase 5: pytest（待做）
## Test Results

<!-- To be filled by executing agent -->

---

## pytest 結果

`tests/test_w17_010_exit_status_schema.py`：**16 passed in 0.02s**

覆蓋項：
- TestValidSectionsExpanded（4）：NeedsContext/Exit Status 加入 VALID_SECTIONS、既有 5 section 保留、總數 7
- TestTicketTemplateSections（4）：template 含兩 section、Exit Status 在 Completion Info 之前、YAML schema 欄位齊備
- TestExitStatusYamlParsing（7）：5 狀態枚舉對應 exit code（parametrize）、YAML 範例解析正確、無效 status 可識別
- TestTemplateStructure（1）：NeedsContext 子項 template 含 5 關鍵欄位

## Hook 實測

手動 stdin 注入驗證 needs-context-listener-hook.py：
1. 匹配命令（ticket track append-log ... --section NeedsContext）→ 輸出 hookSpecificOutput systemMessage，exit 0
2. 不匹配命令（ls -la）→ 靜默通過，exit 0
3. 實機 PostToolUse:Bash 觸發成功（system-reminder 確認 additionalContext 送達 PM）

## Skill 重裝驗證

`uv tool install .claude/skills/ticket --reinstall` 成功，ticket-system 1.0.0 已更新。

## Hook 註冊驗證

settings.json PostToolUse:Bash matcher 已加入 needs-context-listener-hook.py（chmod +x 已設定）。

---

## NeedsContext

- **缺失項**: acceptance 第 7 項「linux + Explore 多視角 reviewed」需 PM 派發 parallel-evaluation 審查
- **觸發位置**: `ticket track complete` 阻擋於該 AC
- **影響**: ticket 無法 complete；實作 1-6 已完成並通過 16 pytest
- **建議補料**: PM 派發 parallel-evaluation 對以下三項做 linux + Explore 雙視角審查：
  1. NeedsContext section 協議設計（子項 template 是否充分、section 命名是否穩定）
  2. Exit Status YAML schema 欄位（5 狀態枚舉與 exit code 對應是否合理）
  3. needs-context-listener-hook 正則設計（`--section` 緊鄰參數的精確匹配避免 content 誤判）
- **重派成本**: 多視角審查約 15-25K tokens；若需調整設計，回來修改成本約 5K tokens

---

## Exit Status

```yaml
status: partial_success
reason: "實作 1-6 完成並 16 pytest 全綠；AC 第 7（多視角審查）需 PM 派發 parallel-evaluation"
confidence: 0.9
acceptance_met: [1, 2, 3, 4, 5, 6]
acceptance_unmet: [7]
artifacts:
  - .claude/skills/ticket/ticket_system/lib/command_tracking_messages.py
  - .claude/skills/ticket/ticket_system/lib/ticket_builder.py
  - .claude/hooks/needs-context-listener-hook.py
  - .claude/settings.json
  - .claude/references/pm-agent-observability.md
  - .claude/skills/ticket/tests/test_w17_010_exit_status_schema.py
context_dependencies:
  - parallel-evaluation 代理人（需 PM 派發）
estimated_recovery_effort: "15-25K tokens 多視角審查 + 5K 調整成本"
```

---

## Completion Info

**Completion Time**: 2026-04-20T21:51:12
**Executing Agent**: thyme-python-developer
**Review Status**: pending