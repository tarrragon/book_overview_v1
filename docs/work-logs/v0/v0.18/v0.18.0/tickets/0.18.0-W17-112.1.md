---
id: 0.18.0-W17-112.1
title: 補測試 test_multi_view_checker 加多行 nested YAML 觸發 invalid 分支驗證
type: IMP
status: pending
version: 0.18.0
wave: 17
priority: P2
parent_id: 0.18.0-W17-112
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme
  history: {}
what: 補測試 test_multi_view_checker 加多行 nested YAML 觸發 invalid 分支驗證
when: W17-112 ANA 完成後
where:
  layer: 待定義
  files:
  - .claude/hooks/tests/test_multi_view_checker.py
why: W17-112 重現否證 thyme caveat：多行 nested YAML 確實觸發 invalid（非 missing）。W17-111.1 testing
  gap 須補上多行 nested case，固化「多行/同行皆觸發 invalid 分支」的測試保護
how:
  task_type: Implementation
  strategy: '在 test_multi_view_checker.py 加 test_invalid_value_multiline_nested_includes_nested_hint：fixture
    用多行 nested YAML（multi_view_status: 換行接 status: skipped），assert should_warn=True
    + 訊息含「nested YAML」字樣；保留 W17-111.1 既有同行測試不動'
acceptance:
- '[ ] 新增 test_invalid_value_multiline_nested_includes_nested_hint 測試多行 nested 觸發
  invalid 分支'
- '[ ] 測試全綠（含 W17-111.1 既有同行測試 + 新多行 nested 測試）'
- '[ ] 驗證 nested 提示訊息對多行/同行皆出現'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-05-02'
updated: '2026-05-02'
---

# Execution Log

## Task Summary

補測試 test_multi_view_checker 加多行 nested YAML 觸發 invalid 分支驗證

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

W17-112 父 ANA 透過 python 重現否證 thyme caveat：多行 nested YAML 確實觸發 invalid 分支（regex `^\s*multi_view_status\s*:\s*(.+?)\s*$` 中 `\s*` 跨行吞 newline + 縮排）。W17-111.1 既有測試 `test_invalid_value_with_colon_includes_nested_hint` 只用同行形式，缺多行 nested case 固化。

### 影響範圍

- `.claude/hooks/tests/test_multi_view_checker.py`：新增多行 nested 測試 case

### 相關 Error Pattern

- PC-117（描述驗證為正確，本 ticket 補強 testing 完整性）
- W17-112 父 ANA（thyme caveat 否證 follow-up）

---

## Context Bundle

### 父 ANA 重現結果（W17-112 已驗證）

python 重現三形式皆 match → value = 'status: skipped'：

| 形式 | regex match | value |
|------|------------|-------|
| 多行 nested + yaml code fence | match | `'status: skipped'` |
| 純多行 nested（無 code fence） | match | `'status: skipped'` |
| 同行（W17-111.1 既有測試）| match | `'status: skipped'` |

### W17-111.1 既有測試（不需動）

`.claude/hooks/tests/test_multi_view_checker.py` 已有 `test_invalid_value_with_colon_includes_nested_hint`，使用同行形式 `multi_view_status: status: skipped` 觸發 invalid 分支。本 ticket 補多行 nested case 固化「多行/同行皆觸發」。

### 待新增測試

```python
def test_invalid_value_multiline_nested_includes_nested_hint(project_dir, logger):
    """多行 nested YAML 結構也應觸發 invalid 分支與 nested 提示（W17-112 / PC-117）

    W17-095 收尾踩坑案例的多行 nested 形式必須與同行形式行為一致。
    """
    content = """---
type: ANA
id: TEST-MULTILINE-1
---

## Solution

```yaml
multi_view_status:
  status: skipped
  reason: "test"
```
"""
    frontmatter = {"type": "ANA", "id": "TEST-MULTILINE-1"}
    should_warn, msg = check_multi_view_status(content, frontmatter, project_dir, logger)
    assert should_warn is True, f"多行 nested 應觸發 invalid，實際 should_warn={should_warn}"
    assert "偵測到值含冒號" in msg, "訊息應含 nested 提示"
    assert "nested YAML" in msg
    assert "multi_view_status: skipped" in msg, "訊息應含 flat 範例"
```

### 邊界（嚴守）

- 允許：`.claude/hooks/tests/test_multi_view_checker.py` 新增測試 case
- 禁止：改 hook py 既有邏輯（W17-111.1 已落地正確）；改既有 test case；改 schema yaml
- 禁止：git add . / git add -A
- 測試指令：`python3 -m pytest .claude/hooks/tests/test_multi_view_checker.py -v`
- 預期：13 passed（既有 12 + 新 1）

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme
**Review Status**: pending
