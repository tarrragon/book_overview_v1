---
id: 0.18.0-W17-031.4
title: 銜接 Agent 自主 complete 與 scheduler 狀態感知（W17-031 盲區 D）
type: IMP
status: completed
version: 0.18.0
wave: 17
priority: P2
parent_id: 0.18.0-W17-031
children: []
blockedBy:
- 0.18.0-W17-033
relatedTo:
- 0.18.0-W17-029
- 0.18.0-W17-022
- 0.18.0-W17-033
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層半:執行中額外發現
  final_decision: 建立 W17-031 child 落地盲區 D
  rationale: W17-028 的 D 風險需在 Agent 自主 complete 規則完成後回填 scheduler 狀態感知；避免提早實作錯誤邊界。
who:
  current: thyme-python-developer
  history: {}
what: 在 W17-033 Agent 自主 complete 規則完成後，補上 scheduler 對 completion 狀態的感知，避免 runqueue
  顯示與實際完成狀態脫節。
when: W17-033 完成後；若 W17-033 已完整解決 scheduler 需求，則記錄 no-op 理由並收尾。
where:
  layer: ticket scheduler / agent completion state
  files:
  - .claude/skills/ticket/ticket_system/commands/track_runqueue.py
  - .claude/skills/ticket/tests/
  - .claude/agents/AGENT_PRELOAD.md
  - .claude/pm-rules/agent-definition-standard.md
why: W17-028 盲區 D 原本連到 W17-022；W17-022 已完成後，剩餘風險集中在 W17-033 的 Agent 自主 complete 是否讓
  scheduler 能辨識任務真實狀態。
how:
  task_type: Implementation
  strategy: 先讀 W17-033 結論；若 scheduler 仍缺狀態訊號，為 runqueue 補 stale in_progress 或 commit-without-complete
    標註；若 W17-033 已完全覆蓋，將本任務以 no-op 記錄完成；補對應測試或驗證記錄。
acceptance:
- '[x] W17-033 結論已讀並記錄 scheduler 是否需補強'
- '[x] 若需補強，runqueue 對 stale in_progress 或 commit-without-complete 狀態有標註'
- '[x] 若 W17-033 已完全解決，記錄 no-op 理由並 close/complete'
- '[x] Test Results 記錄至少 1 個測試命令或 no-op 驗證命令，並附通過結果'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-03T15:32:05'
completed_at: '2026-05-03T15:53:19'
created: '2026-04-22'
updated: '2026-04-22'
---

# Execution Log

## Task Summary

在 W17-033 Agent 自主 complete 規則完成後，評估並補上 scheduler 對 stale in_progress、commit-without-complete 或等效狀態的感知，避免 runqueue 顯示與實際完成狀態脫節。

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---


### PM 評估結論（2026-05-03，W17-033 完成後）

**結論**：.4 **不能 no-op**，需補強 runqueue 對 stale in_progress / commit-without-complete 的標註。

#### W17-033 實際覆蓋範圍

W17-033 已完成（commit 期間 2026-04-30），落地兩檔：
- `.claude/agents/AGENT_PRELOAD.md` v1.7.0 §2.4「收尾責任」段落
- `.claude/rules/core/agent-definition-standard.md` v1.3.0「執行責任：Ticket 完成（收尾）」章節

效果：agent 自律於 commit 後執行 `check-acceptance --all + complete`；acceptance-gate-hook 為安全網。

#### 殘餘風險矩陣

| 情境 | W17-033 是否覆蓋 | scheduler 補強需求 |
|------|-----------------|------------------|
| Agent 完成後 self-complete（正常路徑） | 是（自律 + hook 安全網） | 不需要 |
| Agent crash / timeout / 未 commit | 否 | 是 — stale in_progress |
| Agent commit 但未 complete（自律規則執行失敗） | 部分（規則已寫，但 hook 不能保證 commit 後一定觸發 complete） | 是 — commit-without-complete |
| Agent 寫 NeedsContext 但未 release | 否 | 是 — needs_context 標註（W17-031.5 處理 hint 層；runqueue 仍應感知） |

#### 既有 staleness 機制盲區

`.claude/skills/ticket/ticket_system/lib/staleness.py` 用 `created` 日期算 stale level（INFO=7d / WARNING=14d / CRITICAL=30d），但：
- 以 `created` 計時，與 in_progress 狀態無關
- 不識別「started_at 很久之前但 completed_at 為 null」的 stale in_progress
- `track_runqueue.py` 完全未引用 staleness（`grep stale` 0 命中）

#### 補強範圍（.4 應做）

1. **runqueue 標註 stale in_progress**：對 `status=in_progress` 且 `started_at` > 閾值（建議 4-8h）的 ticket 加 `[stale]` 標籤
2. **runqueue 標註 commit-without-complete**（弱風險）：偵測 git log 中 ticket ID 已 commit 但 ticket 仍 in_progress 的情況；實作成本較高，建議列為延伸（不在 .4 必做範圍）
3. **新增測試**：`.claude/skills/ticket/tests/test_track_runqueue.py` 補 stale in_progress 顯示測試

#### 與 .1 / .3 的關聯

- .1（讀 Exit Status）：runqueue 讀 handoff JSON 的 needs_context / blocked / failed 標記
- .3（readiness tag）：runqueue 顯示 ready / no-CB 等可派發訊號
- .4（completion 感知）：runqueue 標 stale in_progress / commit-without-complete

三者都動 track_runqueue.py，且都是「runqueue 顯示 ticket 狀態警示」邏輯，存在合併重構機會（.4 完成後可由 thyme 評估是否抽 unified `tag_ticket_status()` helper）。

#### 派發順序確認

維持原計畫：.5 → .2 → .1 → .3 → .4
- .5（thyme 跑中）
- .2 等 .5 完成
- .1 等 .2 完成
- .3 等 .1 完成
- .4 在 .3 之後（共動 track_runqueue.py 序列）

#### 更新後實作策略

從原 strategy 的兩分支（補強 vs no-op）收斂為**補強路徑**：
1. 在 `track_runqueue.py` 加 `_compute_status_tags(ticket)` helper：依 `status` + `started_at` + `completed_at` + 當前時間判斷標籤
2. stale in_progress 閾值常數（建議 `STALE_IN_PROGRESS_HOURS = 6`）抽到 `staleness.py` 統一管理
3. 顯示格式：在 ticket id 後追加 `[stale-in-progress]` 標籤（與既有 `[INFO]` `[WARNING]` 對齊）
4. commit-without-complete 偵測延後（.4 範圍）— 列為 follow-up（如需另建 ticket）
### PM 前台 W17-033 結論評估（2026-05-03）

#### W17-033 已落地內容

- `.claude/agents/AGENT_PRELOAD.md` v1.7.0 §2.4「收尾責任：自律 complete」三明示段落
- `.claude/rules/core/agent-definition-standard.md` v1.3.0「執行責任：Ticket 完成（收尾）」章節
- agent commit + body 填寫後自律 `check-acceptance --all` + `complete`
- `acceptance-gate-hook` 為安全網（無論 PM 或 agent 觸發 complete 皆驗證）

#### Scheduler 殘餘缺口

| 案例 | W17-033 是否覆蓋 | 結論 |
|------|----------------|------|
| Agent commit 後主動 complete | 是（自律） | OK |
| Agent commit 但漏 complete | 是（hook 兜底 + AGENT_PRELOAD 規範） | OK |
| Agent session 中斷（timeout / runtime error）→ ticket 留 in_progress | 否 | 缺口 |
| 違規 agent 不執行 complete（PC-108 模式） | 部分（規範但無執行強制力） | 缺口 |

#### 結論：採 Option A 補 stale in_progress 標註

- runqueue list 對 `status=in_progress` 且 `started_at` 超過閾值（建議 24h）的 ticket 加 [STALE] 標註
- 與既有 readiness tag 並列顯示（不互斥；STALE 可疊加在其他 readiness tag 上）
- PM 在 runqueue 看到 [STALE] → 人工介入（評估是 agent 真停滯還是長任務）
- 24h 閾值暫硬編碼（YAGNI，日後若需可加環境變數）

#### 不採 Option B（no-op）的理由

- stale 案例罕見但無法被 W17-033 機制覆蓋（中斷時 agent 已不在）
- runqueue 加標註成本低（< 20 行 code + 1-2 測試）
- scheduler 完整性 > 程式碼簡潔（本 group 主旨即補完整性）

#### 實作邊界

- 動 .claude/skills/ticket/ticket_system/commands/track_runqueue.py
- 補 helper `_compute_stale(ticket)`：return True if `status=in_progress` and `now - started_at > 24h`
- list 輸出在 readiness tag 之外加 [STALE] tag（位置建議 readiness tag 之後）
- 補測試覆蓋：(1) 24h+ in_progress 標 STALE / (2) 24h- in_progress 不標 / (3) completed 不標
- 既有測試 100% 通過

#### Commit policy（PC-092）

git add 精確路徑（禁 add ./ 或 -A）：
- .claude/skills/ticket/ticket_system/commands/track_runqueue.py
- .claude/skills/ticket/tests/<相關測試檔>
- docs/work-logs/v0/v0.18/v0.18.0/tickets/0.18.0-W17-031.4.md

Commit msg: `feat(0.18.0-W17-031.4): runqueue stale in_progress 標註`

#### 執行環境

- 主 repo cwd：/Users/tarragon/Projects/book_overview_v1（不切 worktree；ARCH-015）
- ticket CLI 修改後須 `uv tool install .claude/skills/ticket --reinstall`

#### Acceptance 落地對應

- AC 1：本段已記錄 W17-033 結論評估與 scheduler 補強需求 → READY 待 thyme 確認
- AC 2：Option A 實作後滿足
- AC 3：本案採補強路徑非 no-op
- AC 4：Test Results 補 stale 三類測試指令

#### 收尾責任（W17-033）

完成 commit + append-log 填 Solution/Test Results 後：
1. `ticket track check-acceptance --all 0.18.0-W17-031.4`
2. `ticket track complete 0.18.0-W17-031.4`
3. acceptance 任一未過 → 寫 NeedsContext 不要 complete
### 問題根因

W17-028 盲區 D：scheduler 對 ticket 真實完成狀態的感知。W17-033 Agent 自律 complete 規則覆蓋正常路徑（commit + body 填寫後 self-complete + acceptance-gate-hook 安全網），但 agent 中斷（timeout / runtime crash / session 異常結束）時 agent 已不在，無法觸發 complete，ticket 留在 in_progress 狀態，runqueue list 看不到（既有 list 僅顯示 unblocked pending）也無警示。

### 影響範圍

- `.claude/skills/ticket/ticket_system/commands/track_runqueue.py`：list 視圖過濾與顯示邏輯
- `.claude/skills/ticket/ticket_system/lib/staleness.py`：staleness 模組擴充 in_progress 維度（既有僅按 `created` 計算 7/14/30 天等級）
- `.claude/skills/ticket/tests/test_track_runqueue.py`：新增 stale 標註測試類

### 相關 Error Pattern

- 與 PC-105（subagent commit 後未 complete）行為相關但根因不同：PC-105 是 agent 違規不執行 complete，本 ticket 是 agent 已不在無法執行。
- 與 PROP-010（stale Ticket 警告）為同一基礎機制延伸。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 採 Option A 補強路徑

不採 no-op：W17-033 自律 + acceptance-gate-hook 無法覆蓋 agent 中斷案例（agent 已不在，無從觸發 complete）。runqueue list 加 [STALE] 標註讓 PM 在巡視時發現遺留 ticket，人工介入評估。

### 改動清單

| 檔案 | 改動內容 |
|------|---------|
| `.claude/skills/ticket/ticket_system/lib/staleness.py` | 新增常數 `STALE_IN_PROGRESS_HOURS = 24`、`_parse_started_at()` helper、`is_stale_in_progress(ticket, now=None)` 函式 |
| `.claude/skills/ticket/ticket_system/commands/track_runqueue.py` | import `is_stale_in_progress`；新增 `STALE_TAG = "STALE"` 常數；新增 `_is_listable()` helper（unblocked pending OR stale in_progress）；`_render_list` 改用 `_is_listable` 並在 readiness tag 後加 `[STALE]` 標註 |
| `.claude/skills/ticket/tests/test_track_runqueue.py` | 新增 `TestRunqueueStaleInProgress` 5 個測試 |

### 設計決策

1. **24h 閾值** — 直接採 ticket md PM 評估結論建議值，YAGNI 暫硬編碼。日後若需可加環境變數。
2. **Stale tag 與 readiness tag 並列疊加** — 不互斥；in_progress + stale + NO-CB 的 ticket 同時顯示 `[NO-CB] [STALE]`。
3. **list 視圖納入 stale in_progress** — 既有 list 過濾為 unblocked pending；擴充為 `_is_listable` = pending unblocked OR stale in_progress，這樣 PM 在 runqueue list 看見遺留 ticket 才有機會介入。in_progress 但 < 24h 仍不顯示（避免噪音）。
4. **Fail-open** — 缺 `started_at` 或解析失敗 → False，不誤標。
5. **commit-without-complete 偵測延後** — 依 ticket md PM 評估「實作成本較高」結論，不在 .4 範圍。

### 不在本 ticket 範圍

- commit-without-complete 偵測（git log 反查）
- needs_context 標註（W17-031.5 已處理 hint 層）
- 抽 unified `tag_ticket_status()` helper（.1/.3/.4 共動 track_runqueue.py 的合併重構機會，待 .4 完成後另評估）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 執行指令

```
(cd .claude/skills/ticket && uv run pytest)
```

### 結果

- runqueue 模組單檔：37 passed（既有 32 + 新增 5），0.26s
- 全 ticket skill 套件：**1875 passed in 1012.51s**（0 failed / 0 errors）

### 新增測試（TestRunqueueStaleInProgress）

| # | 測試名稱 | 驗證 |
|---|---------|------|
| 1 | test_in_progress_over_24h_marked_stale | started_at 30h 前 → 顯示 [STALE] |
| 2 | test_in_progress_under_24h_not_marked | started_at 2h 前 → 不在 list 也無 [STALE] |
| 3 | test_completed_ticket_not_in_list_and_no_stale_tag | completed → 不在 list 無 [STALE] |
| 4 | test_pending_ticket_not_marked_stale | pending（非 in_progress）→ 不標 [STALE] |
| 5 | test_stale_does_not_break_readiness_tag | stale + NO-CB 並列疊加（兩 tag 同時出現） |

### CLI 安裝

`uv tool install .claude/skills/ticket --reinstall` 完成（pyyaml + ticket-system 1.0.0）。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-03T15:53:19
**Executing Agent**: thyme-python-developer
**Review Status**: pending