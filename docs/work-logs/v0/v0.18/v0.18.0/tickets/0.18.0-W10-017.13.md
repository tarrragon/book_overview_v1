---
id: 0.18.0-W10-017.13
title: 重構 Phase 4b P2 追蹤集合清理 (handoff dict 路由 + unchecked 計數 + pending_checks 決策 +
  TD-E testpaths + TD 錨點)
type: IMP
status: completed
version: 0.18.0
wave: 10
priority: P3
parent_id: 0.18.0-W10-017
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-python-developer
  history: {}
what: 重構 Phase 4b P2 追蹤集合清理 (handoff dict 路由 + unchecked 計數 + pending_checks 決策 +
  TD-E testpaths + TD 錨點)
when: W10-017.8 Phase 4b 重構 P2（依 017.12 後續）
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/lib/checkpoint_view.py
  - .claude/skills/ticket/ticket_system/lib/checkpoint_state.py
  - .claude/skills/ticket/pyproject.toml
why: W10-017 WRAP Solution Phase 1a - 將 snapshot 從「原始資料」升級為「決策建議型」輸出，取代 PM 自行推導 Checkpoint
  + 讀 completion-checkpoint-rules.md 的流程
how: 'AC 1-6 逐項：(1) handoff_status_for 改 dict dispatch 取代 if-elif 字串比對；(2) unchecked
  計數改結構化 CheckItem list，替代 [ ] 字串反推；(3) pending_checks 欄位決策（納入渲染或移除 dead field）並文件化；(4)
  pyproject.toml [tool.pytest.ini_options] testpaths 加入 ticket_system/tests；(5) TD-A~E/AD-1~4
  錨點回填至對應程式碼註解；(6) 107+ 測試全通過 + 新增 P2 對應測試。TDD: baseline → RED→GREEN→REFACTOR。'
acceptance:
- '[x] handoff_status_for 改 dict dispatch 取代 if-elif 字串比對'
- '[x] unchecked 計數改用結構化 CheckItem list 替代 [ ] 字串匹配反推'
- '[x] pending_checks 欄位決策（納入渲染或移除 dead field）並文件化'
- '[x] TD-E pyproject.toml [tool.pytest.ini_options] testpaths 加入 ticket_system/tests'
- '[x] TD-A~E/AD-1~4 錨點回填到對應程式碼註解供後續追溯'
- '[x] 107+ 測試全部通過 + 新增 P2 項目對應測試'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: true
started_at: '2026-04-19T15:03:11'
completed_at: '2026-04-19T15:43:54'
created: '2026-04-19'
updated: '2026-04-19'
chain:
  root: 0.18.0-W10-017
  parent: 0.18.0-W10-017
  depth: 1
  sequence:
  - 17
  - 13
---

# Execution Log

## Task Summary

重構 Phase 4b P2 追蹤集合清理 (handoff dict 路由 + unchecked 計數 + pending_checks 決策 + TD-E testpaths + TD 錨點)

---

## Problem Analysis

### Context Bundle（PC-040）

**來源**: W10-017.8 Phase 4b 多視角 P2（TD 追蹤清理）；017.11 P0 + 017.12 P1 已完成（commits 85e2c567 + b20a26e3）

**目標檔案**:
- .claude/skills/ticket/ticket_system/lib/checkpoint_view.py
- .claude/skills/ticket/ticket_system/lib/checkpoint_state.py
- .claude/skills/ticket/pyproject.toml

**關鍵重構點**:
1. handoff_status_for if-elif 字串比對 → dict dispatch（可讀性 + 可擴展）
2. unchecked [ ] 字串反推 → 結構化 CheckItem list（消除 magic string）
3. pending_checks 欄位：確認是否 dead code，決策納入或移除（符合 PC-093 二選一原則）
4. pyproject.toml testpaths 擴充：讓 ticket_system/tests 被 pytest 自動發現
5. TD 錨點回填：TD-A~E/AD-1~4 寫入對應程式碼註解供後續追溯

**TDD 節奏**:
- baseline `(cd .claude/skills/ticket && uv run pytest -x)` 確認 1296 綠
- AC 1-3 重構：每項獨立 test + 實作
- AC 4 pytest config：新增 testpaths 後確認既有路徑覆蓋不遺漏
- AC 5 TD 錨點：純註解補充
- AC 6 新 regression test 對應 P2 變更

**與 017.12 的檔案重疊**:
- 017.12 已完成，本 ticket 動同兩檔，但為完全不同函式範圍（017.12: compute_blockers/PRIORITIES/render_ready_check；本 ticket: handoff_status_for/unchecked 計數/pending_checks）
- 安全序列派發

**邊界**:
- 僅動 3 檔 + 對應 tests
- 不改 CLI 對外介面
- pending_checks 若決策移除需寫入 commit message 理由

**WRAP**:
- W: (a) 全做；(b) 只做 AC 1-3（refactor 核心）；(c) 拆子 ticket
- A: 機會成本 — 擠壓 W10-017.2/017.4/017.7 + 其他 pending
- P: 失敗預測 — dict dispatch 遷移漏 caller；防護：新 regression test 覆蓋所有原 if-elif 分支
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

Phase 4b P2 6 AC 全數落地：

**AC1**：handoff_status_for 改 dict dispatch。新增 _HANDOFF_STATUS_ROUTES 表（3 caller → 3 builder 函式），取代 if-elif 字串比對。新 caller 需同步擴充 CheckpointCaller Literal 與本表（新 regression test test_AC1_handoff_status_routes_table_covers_all_valid_callers 守邊）。

**AC2**：render_ready_check 改用結構化 CheckItem（@dataclass frozen）。抽出 _build_ready_check_items(state, caller) -> List[CheckItem]。未通過計數改為 sum(1 for ci in items if ci.status == "fail")，消除 "[ ]" in line magic string。CheckItem 納入 __all__。

**AC3**：pending_checks 決策「保留 + 文件化」。非 dead field，ready_for_clear 推導依賴 all(not c.auto_detectable for c in pending)，且既有測試驗證 reason 文字為對外契約。PC-093 走保留選項。

**AC4**：pyproject.toml testpaths 擴充為 ["tests", "ticket_system/tests"]，讓 149 支命令層測試被 pytest 預設發現。TD-E 完成。

**AC5**：TD-A~E / AD-1~4 錨點回填。checkpoint_state.py 模組 docstring 新增統一錨點區塊；checkpoint_view.py handoff_status_for/render_ready_check 附近加入就近錨點。

**AC6**：新增 tests/test_checkpoint_view_p2_refactor.py（11 支 regression test）全綠。
## Test Results

<!-- To be filled by executing agent -->

---

**測試範圍**: 完整 pytest suite（tests/ + ticket_system/tests/，含 AC4 新擴 testpaths）

**結果**: 1461 passed + 3 pre-existing failures in 601s

**新增測試全綠**:
- tests/test_checkpoint_view_p2_refactor.py: 11/11 PASSED
  - test_AC1_handoff_status_routes_table_covers_all_valid_callers
  - test_AC1_dispatch_delegates_to_{snapshot,handoff_ready,checkpoint_status}_builder (3)
  - test_AC2_check_item_exported_in_public_api
  - test_AC2_check_item_render_{pass_produces_x_mark,fail_with_hint,unknown_marks_question} (3)
  - test_AC2_snapshot_footer_counts_failures_structurally
  - test_AC2_snapshot_footer_ignores_unknown_status_in_count
  - test_AC2_non_snapshot_caller_has_no_footer_count

**所有 checkpoint-related 測試全綠**: 244 PASSED / 0 FAILED（包含 tests/test_checkpoint_view.py 既有 V1-V7 + F4、tests/test_checkpoint_state/* 全系列、tests/test_checkpoint_layering_contract.py、ticket_system/tests/test_track_{snapshot,handoff_ready,checkpoint_status}.py）

**3 pre-existing 失敗（out of scope）**:
- tests/test_track_fields.py::TestWhoField::test_set_who_success
- tests/test_track_fields.py::TestWhereField::test_set_where_success
- tests/test_track_fields.py::TestHowField::test_set_how_success

驗證過程: git stash 本 ticket 所有變更後單獨跑這 3 支測試仍 FAILED（3 failed, 15 deselected）。根源為 W10-086 commit 69793419 "修復 set-* dict 欄位壓扁 bug + regression fixture 8/8" 留下的 RED regression fixtures（期望 string 但實際存 {'current': ..., 'history': {}} dict 結構），與本 ticket 完全無關。建議獨立 ticket 追蹤（目前 ticket_ops 的 set-* 對 who/where/how 三個 dict 結構欄位仍未壓扁）。
## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending