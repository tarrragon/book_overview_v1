---
id: 0.18.0-W17-158
title: 修復 stop-worklog-handoff-sync-check-hook Stop event schema 違反（additionalContext
  欄位不被允許）
type: IMP
status: pending
version: 0.18.0
wave: 17
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.18.0-W17-155
- 0.18.0-W17-156
spawned_tickets: []
source_ticket: 0.18.0-W17-083.2
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:框架改善
  final_decision: 立即修復 Stop event JSON 輸出 schema
  rationale: quality-baseline-rule-4-Hook 失敗必須可見+規則3-設計問題立即修正
who:
  current: thyme-python-developer
  history: {}
what: 修復 stop-worklog-handoff-sync-check-hook Stop event schema 違反（additionalContext
  欄位不被允許）
when: 立即（P1，每個 session 結束都會觸發）
where:
  layer: .claude/hooks/stop-worklog-handoff-sync-check-hook.py,.claude/hooks/tests/test_stop_worklog_handoff_sync_check_hook.py
  files:
  - .claude/hooks/stop-worklog-handoff-sync-check-hook.py
  - .claude/hooks/tests/test_stop_worklog_handoff_sync_check_hook.py
why: 'stop-worklog-handoff-sync-check-hook 輸出 hookSpecificOutput.additionalContext，但
  Claude Code Stop event 的 schema 僅允許此欄位於 PreToolUse/UserPromptSubmit/PostToolUse/PostToolBatch。實測
  5/5 stop hooks 全部 fail（error: ''(root): Invalid input''），導致該 hook 想傳達的「worklog handoff
  寫了但 CLI handoff 缺」同步檢查訊息無法到達用戶；其他 4 個 stop hooks 也跟著 fail（因 stop-hook 機制是合併輸出後驗證）'
how:
  task_type: Implementation
  strategy: 1. 用 WebFetch 查 Claude Code Stop event 文件確認合法 hookSpecificOutput 欄位（候選：systemMessage
    / stopReason / 直接 stderr）；2. 修改 stop-worklog-handoff-sync-check-hook.py line 309-314
    改用合法欄位（schema 顯示 Stop event 沒有 additionalContext）；3. 同步更新 line 21 docstring 描述；4.
    寫單元測試驗證輸出 JSON 結構符合 Claude Code Stop event schema；5. grep stop-*.py 與其他 hook 掃描有無同類
    schema 違反——發現則納入本 ticket 或 spawn 子 ticket。
acceptance:
- '[ ] 找出 Claude Code Stop event 正確輸出欄位（candidate: systemMessage 顯示給用戶 / stopReason
  顯示停止原因）並選定方案'
- '[ ] 修改 stop-worklog-handoff-sync-check-hook.py 使用正確 schema 欄位輸出同步檢查訊息'
- '[ ] 單元測試驗證輸出 JSON 結構符合 Claude Code 文件 Stop event schema'
- '[ ] Grep 掃描所有 stop-*.py 確認無同類 schema 違反；發現則納入本 ticket 範圍或 spawn 子 ticket'
- '[ ] 驗證 session stop 時 hook 不再產生 ''Hook JSON output validation failed'' 錯誤訊息'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-05-07'
updated: '2026-05-07'
---

# Execution Log

## Task Summary

修復 stop-worklog-handoff-sync-check-hook Stop event schema 違反（additionalContext 欄位不被允許）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

### 實測錯誤訊息（2026-05-07 session 結束時）

```
Stop hook error: Hook JSON output validation failed — (root): Invalid input
```

### Schema 對照

Claude Code 對 hookSpecificOutput.additionalContext 欄位的 schema 規定：

| Event | additionalContext 允許？ |
|-------|------------------------|
| PreToolUse | 否（用 permissionDecisionReason / updatedInput） |
| UserPromptSubmit | 是（required） |
| PostToolUse | 是（optional） |
| PostToolBatch | 是（optional） |
| **Stop** | **否（schema 完全沒列）** |

### 違規位置

`.claude/hooks/stop-worklog-handoff-sync-check-hook.py` line 309-314：

```python
output = {
    "hookSpecificOutput": {
        "hookEventName": "Stop",
        "additionalContext": warning,  # <- 違反 Stop event schema
    }
}
print(json.dumps(output, ensure_ascii=False))
```

### 影響範圍

5/5 stop hooks 全部 fail（hook 機制應為「任一輸出違反 schema 則整批 fail」），導致：
1. 此 hook 想傳達的「worklog 寫了 handoff 但 CLI 缺 handoff」同步檢查訊息丟失
2. 其他 4 個 stop hooks 也跟著 fail（連鎖效應）
3. session 結束時用戶看到一大段 schema 錯誤訊息（PC-127 風險）

### 修復方向候選

| 方案 | 欄位 | 評估 |
|------|------|------|
| A | `systemMessage` (string) | 顯示給用戶，類似 Stop hook 訊息——首選 |
| B | `stopReason` (string) | 顯示停止原因，語意較窄 |
| C | 直接 stderr 輸出（不用 JSON） | hook docstring line 21 提到的合併機制可能因此失效 |

建議首選方案 A（systemMessage），保留多 hook 合併能力。需 WebFetch 確認 Claude Code 文件。

### 衍生洞察（資料層）

修復後 hook 將正確輸出，session 結束時會看到「48 個 ticket 在 worklog 提到但無 CLI handoff」的清單——這是另一個資料層問題（W17-156 範圍應評估是否覆蓋；hook 偵測精度本身屬 W17-155 範圍）。本 ticket 限定 schema 違反修復，資料清理另開 ticket（如需）。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.18.0-W17-083.2 | chars: 307 -->

### Task Reference
- 0.18.0-W17-083.2 what: 實作 S2 CLI --from-worklog + S3 Stop hook + 19 RED 測試

### Rationale Chain
- 0.18.0-W17-083.2 why: S2 CLI --from-worklog 與 S3 Stop hook 兩者皆 import worklog_parser API；同代理人連續實作可即時偵測 API 不一致；總 4 檔修改 ≤ 5 閾值，符合 cognitive-load §3b 派發前閾值

### Related Files
- .claude/skills/ticket/ticket_system/commands/handoff.py  # from 0.18.0-W17-083.2
- .claude/skills/ticket/ticket_system/tests/test_handoff_from_worklog.py  # from 0.18.0-W17-083.2