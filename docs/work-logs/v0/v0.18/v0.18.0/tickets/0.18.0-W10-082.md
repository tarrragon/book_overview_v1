---
id: 0.18.0-W10-082
title: 實作 Phase 4 強制決斷 hook（PC-093 YAGNI 累積防護）
type: IMP
status: in_progress
version: 0.18.0
wave: 10
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層:框架改善
  final_decision: PC-093 落地
  rationale: Phase 4 強制決斷 hook 防止 YAGNI 累積
who:
  current: basil-hook-architect
  history: {}
what: 實作 Phase 4 強制決斷 hook（PC-093 YAGNI 累積防護）
when: Phase 4 階段執行時（cinnamon / parallel-evaluation 掃描 Ticket md）
where: Hook 層（.claude/hooks/ + settings.json）
why: PC-093 從 W10-017.8 Phase 4 cinnamon 觀察 use_cache/CheckpointStateError 從 Phase
  1 拖到 Phase 4 仍說「再決定」反模式，需建構 hook 強制 Phase 4 做出執行/移除二選一決策
how: 建立 phase4-decision-enforcement-hook.py，於 Phase 4 相關事件觸發時掃描 ticket md 中的延後話術（『Phase
  X 再決定』『之後再說』『保留以防萬一』『以後可能』），命中即 block 並要求 PM 做出『執行/移除』二選一決策；註冊到 settings.json PostToolUse/UserPromptSubmit；撰寫測試覆蓋
  detection/exemption/blocking 三情境
acceptance:
- '[ ] 新建 phase4-decision-enforcement-hook.py'
- '[ ] 偵測 ticket md 中「Phase X 再決定」「之後再說」等 anti-pattern phrase'
- '[ ] Phase 4 階段觸發提示 PM 必須做出明確決定'
- '[ ] hook 註冊到 settings.json'
- '[ ] 測試覆蓋偵測情境'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: true
started_at: '2026-04-19T13:50:57'
completed_at: null
created: '2026-04-19'
updated: '2026-04-19'
current_phase: Phase 3a
assignee: pepper-test-implementer
---

# Execution Log

## Task Summary

實作 Phase 4 強制決斷 hook（PC-093 YAGNI 累積防護）

---

## Problem Analysis


### Phase 1 設計規格要求（PM 下達，basil 執行）

**Phase 1 必須產出（寫入 Solution 區段）**:

1. **偵測 phrase 完整清單**
   - 正規表達式（每條附 rationale + 至少 2 個實際案例）
   - 分級：MUST-block（強延後）/ WARN（模糊）/ INFO（可能延後）

2. **觸發時機決策**
   - PostToolUse（ticket track append-log/phase 變更）vs UserPromptSubmit vs 兩者
   - 明確選擇依據與排除理由

3. **豁免機制**
   - 合理延後案例（如「Phase 2 再補充測試」屬正當 TDD）
   - 豁免語法（ticket md 內標記 `<!-- PC-093-exempt: reason -->`）
   - 豁免審計（Phase 4 進入時必列 outstanding 豁免）

4. **Hook block 訊息樣板**
   - 指引 PM 做「執行/移除」二選一決策
   - 引用 PC-093
   - AUQ-ready 選項骨架

5. **與既有 Hook 互動分析**
   - phase-completion-gate / acceptance-gate / agent-dispatch-validation 是否重疊
   - 決策：整合到既有 hook vs 獨立 hook（附 rationale）

6. **驗收條件細化**
   - 當前 5 項 AC 可能不足，基於以上設計重新產出 8-12 項細化 AC

**Phase 1 邊界**:
- 僅設計，禁止實作 .py / 測試
- 禁止動 .claude/hooks/ 下 .py
- Phase 2 測試設計由 sage 另派
- 完成後：append Solution + `ticket track phase 0.18.0-W10-082 phase2`
- 不 git commit（PM 驗收後處理）

**完成回報**:
phrase 分類數量 / 觸發點決策 / 獨立 vs 整合決策 / 細化後 AC 數量
### Context Bundle（PC-040）

**來源**: PC-093 (.claude/error-patterns/process-compliance/PC-093-yagni-deferred-decision-accumulation.md)

**根因**: Phase 1 預留接口 + Phase 4 「再決定」 → 永久死碼（W10-017.8 use_cache + CheckpointStateError 案例）

**偵測目標 phrase**（非窮盡，Phase 1 可擴充）:
- 「Phase [0-9]+ 再決定」「Phase [0-9]+ 視 .* 決定」
- 「之後再說」「以後再說」「以後可能」
- 「保留以防萬一」「保留擴展彈性」
- 「未來需求再考慮」

**觸發時機候選**:
- TDD phase transition（pending → phase1 → phase2 → phase3a → phase3b → phase4）
- Phase 4 進入時必檢查（強制決斷閘門）
- commit 含 ticket 引用時背景掃描

**Phase 1 設計需回答**:
1. 偵測範圍：全 ticket md 還是僅 Phase 4 相關區段？
2. 豁免機制：真的有合理延後案例嗎？如何標記豁免？
3. 執行/移除二選一 UI：Hook block 訊息如何引導 PM 決策？
4. 與既有 hooks 互動：phase-completion-gate / acceptance-gate 是否有重疊？

**WRAP 三問（claim 認領）**:
- W: 候選方案 — (a) 文字 phrase 掃描 hook；(b) tdd phase transition 強制問答；(c) ticket complete 時 diff 掃描
- A: 機會成本 — 本 ticket P2，擠壓 W11-003.5/004.1/004.2（更高優先認領提醒）
- P: 失敗預測 — 過度偵測造成誤報淹沒；需設計豁免白名單與白名單語法

**相關 Pattern**: PC-090（close 時機誤判）、規則 3（設計問題立即修正）、規則 6（失敗案例學習原則）

**Phase 1 交付給 sage-test-architect**:
- 完整偵測 phrase 清單（含 regex）
- 觸發時機決策
- 豁免機制規格
- Hook block 訊息樣板
- 驗收條件細化（當前 5 項 AC 可能不足）
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Phase 1 功能規格設計

**設計者**: basil-hook-architect | **Hook**: phase4-decision-enforcement-hook.py
**目的**: 防止 PC-093 YAGNI 累積（Phase 1 預留 → Phase 4 「再決定」 → 永久死碼）

---

### 1. 偵測 Phrase 清單（8 條 regex，分 3 級）

#### 1.1 MUST-block（阻塞）

| ID | Regex（re.IGNORECASE + re.MULTILINE） | Rationale | 案例 |
|----|--------------------------------------|-----------|------|
| M1 | `Phase\s*[0-9]+\s*(?:再\|在)?(?:決定\|決斷\|判斷\|評估)` | 遞迴推給未來 phase，PC-093 核心反模式 | (a) W10-017.8 TD1「Phase 4 視 E1 結果決定 in-memory LRU」；(b) TD5「Phase 4 決定無 .git 時 raise」 |
| M2 | `(?:之後\|以後\|日後\|未來)\s*(?:再\|才)?(?:決定\|決斷\|說\|考慮\|處理)` | 無時間錨點＝永久延後 | (a)「use_cache 之後再決定」；(b)「CheckpointStateError 以後再處理」 |
| M3 | `保留\s*(?:以防萬一\|以備不時之需\|彈性\|擴展性\|擴充性)` | 將「不用」偽裝為「保留」（根因 D） | (a)「保留 use_cache 以防萬一」；(b)「保留 extension error 擴展彈性」 |

#### 1.2 WARN（警告）

| ID | Regex | Rationale | 案例 |
|----|-------|-----------|------|
| W1 | `視\s*.{1,30}?\s*(?:結果\|情況\|狀況\|需求)\s*(?:再\|而)?\s*(?:決定\|判斷\|評估)` | 帶條件的延後，條件明確可豁免 | (a)「視 baseline 結果再決定」；(b)「視實測情況決定」 |
| W2 | `(?:未來\|以後)\s*(?:可能\|或許\|也許)\s*(?:需要\|會用\|要用)` | 根因 B「預留樂觀」話術 | (a)「未來可能需要 cache」；(b)「以後或許會用到」 |
| W3 | `先(?:保留\|留著\|不動)\s*(?:再說\|吧)?` | 根因 C「決策疲勞」口語 | (a)「先保留再說」；(b)「先不動吧」 |

#### 1.3 INFO（提醒）

| ID | Regex | Rationale | 案例 |
|----|-------|-----------|------|
| I1 | `(?:TBD\|TODO\|FIXME)\s*[:：]?\s*(?:Phase\s*[0-9]+\|之後\|未來)` | 標記式延後，若有後續 ticket 可豁免 | (a)「TODO: Phase 4 決定」；(b)「FIXME: 之後處理」 |
| I2 | `(?:擴展\|擴充)\s*(?:彈性\|空間\|介面)` | PC-093 偽裝詞前綴 | (a)「保留擴展彈性」；(b)「提供擴充介面」 |

**合計**：8 條（M×3 / W×3 / I×2）

---

### 2. 觸發時機決策

#### 候選比較

| 方案 | Event | 優點 | 缺點 |
|------|-------|------|------|
| A | PostToolUse(Bash: ticket track phase X phase4) | 精準鎖定進入 Phase 4 | 只查當下，事後修改漏失 |
| B | PostToolUse(Bash: ticket track append-log) | 每次寫入即時掃 | 高頻觸發，Phase 1/2/3 過度偵測 |
| C | UserPromptSubmit | 覆蓋所有互動 | stdin 不含 ticket 上下文，須額外 read |
| D | PreToolUse(Bash: ticket track complete) | 結案閘門 | 時機晚，重修成本高 |

#### 決策：A 主 + D 輔（雙閘門，獨立 Hook）

- **主觸發 A**：PostToolUse on `ticket track phase <id> phase4`；命中 MUST-block → Exit 2；WARN/INFO → stdout
- **輔助觸發 D**：PreToolUse on `ticket track complete <id>`；二次掃描防 Phase 4 後又加入延後話術
- **排除 B**：append-log 在 Phase 1-3 寫「Phase 4 再決定」是合法過渡態
- **排除 C**：須主動 read ticket md 成本高且不知當下對應哪個 ticket

---

### 3. 豁免機制

#### 3.1 合法延後案例（必須豁免）

| 類型 | 案例 | 合法性 |
|------|------|--------|
| TDD 階段過渡 | Phase 1 寫「Phase 2 補 RED 測試」 | TDD 流程本身 |
| 明確條件門檻 | 「baseline > 80ms 時加 cache」 | 可驗證門檻（PC-093 §防護 2） |
| 跨 ticket 追蹤 | 「延後至 W11-xxx，已建 follow-up」 | 有後續 ticket |
| 用戶明確拒絕 | PM 判斷特殊情境 | 規則 5 用戶 autonomy |

#### 3.2 豁免語法

在命中 phrase **同行或前 1 行內** 加入標記：

```
<!-- PC-093-exempt: <category>:<reason> -->
Phase 4 再決定是否保留 use_cache。

或同行後綴：
Phase 4 再決定。<!-- PC-093-exempt: baseline-gated:baseline>80ms 才啟用 -->
```

**欄位規範**：
- `category`: `tdd-transition` | `baseline-gated` | `ticket-tracked` | `user-override`
- `reason`: ≥ 10 字元；`baseline-gated` 必含數值；`ticket-tracked` 必含 ticket id regex

#### 3.3 豁免審計（Phase 4 進入時必輸出）

```
[PC-093 豁免清單] 當前豁免：
  line 42: baseline-gated — baseline>80ms 才啟用
  line 87: ticket-tracked — 延後至 W11-005

Phase 4 結束前必須清償（改執行或移除），剩餘豁免於 complete 時 WARN。
```

---

### 4. Hook Block 訊息樣板

#### 4.1 MUST-block（Exit 2, stderr）

```
[PC-093 Phase 4 強制決斷] 偵測到延後話術，禁止遞迴延後

Ticket: {ticket_id}
命中:
  line 45 [M1]: 「Phase 5 再決定是否保留 use_cache」
  line 78 [M3]: 「保留 CheckpointStateError 以防萬一」

根因: PC-093 YAGNI 累積反模式
  詳見: .claude/error-patterns/process-compliance/PC-093-yagni-deferred-decision-accumulation.md

要求對每項做出二選一:
  1. 執行 — 立即實作，附 use case + AC
  2. 移除 — 刪除預留 + dead code，記錄理由
  3. 豁免 — 符合 §3.1，加 <!-- PC-093-exempt: ... --> 標記

修復後重新 ticket track phase {ticket_id} phase4。

AUQ 選項骨架:
  - label: 執行 use_cache    description: Phase 4 實作 in-memory LRU + 加 AC
  - label: 移除 use_cache    description: 刪除參數 + dead code（baseline 已達標）
  - label: 豁免（附條件）    description: 加 PC-093-exempt，附 baseline 門檻
```

#### 4.2 WARN（Exit 0, stdout）

```
[PC-093 警告] 模糊延後話術（不阻塞，Phase 4 前建議釐清）
  line 62 [W1]: 「視 baseline 結果再決定」— 建議補數值門檻
  line 91 [W2]: 「未來可能需要 cache」— 建議改「Phase X baseline 觸發則加」
```

---

### 5. 與既有 Hook 互動

#### 職責比對

| 既有 Hook | 職責 | 重疊? |
|-----------|------|-------|
| phase-completion-gate-hook | Phase 完成 worklog 上報檢查 | 無（上報 vs 話術） |
| acceptance-gate-hook | complete 前 8 項 checker | 部分（輔助觸發 D 同時作用） |
| phase-contract-validator-hook | Phase 契約驗證 | 毗鄰不重疊（契約 vs 話術） |
| ticket-quality-gate-hook | ticket 品質通用閘門 | 部分重疊 |

#### 決策：獨立 Hook（不整合）

Rationale:
1. **單一職責**：PC-093 延後話術偵測與上報/驗收職責正交
2. **Event 時機不同**：主觸發 Bash 命令 vs phase-completion-gate 監控 Write/Edit
3. **可觀察性**：獨立 `phase4-decision-enforcement` named logger 便於除錯
4. **失敗隔離**：regex 誤報時可單獨 disable 不傷 acceptance-gate
5. **協作點**：核心掃描邏輯抽函式 `scan_ticket_for_pc093(path) -> dict`，供 acceptance_checkers/ 新增子模組（如 `pc093_checker.py`）複用；Phase 3b 實作時統一

---

### 6. 細化驗收條件（5 → 10 項）

| ID | 細化 AC | 覆蓋原 AC | 驗證 |
|----|---------|----------|------|
| AC1 | 新建 `.claude/hooks/phase4-decision-enforcement-hook.py`，shebang + hook_utils + Python 3.9 相容 | 原 AC1 | 語法 + import 測試 |
| AC2 | 實作 §1 全部 8 條 regex，每條附 rationale 註解 + ≥2 測試案例 | 原 AC2 | 每 regex ≥3 正例 + ≥2 負例 |
| AC3 | 主觸發 PostToolUse 匹配 `ticket track phase <id> phase4` 全文掃描，MUST-block Exit 2 | 原 AC3 細化 | 整合測試 + fixture |
| AC4 | 輔助觸發 PreToolUse 匹配 `ticket track complete <id>` 二次掃描，MUST-block 殘留 Exit 2 | 原 AC3 細化 | 整合測試 |
| AC5 | WARN/INFO：Exit 0 + stdout（不觸發 hook error，符合 IMP-048） | 新增 | stderr/stdout 分流測試 |
| AC6 | 豁免語法：`<!-- PC-093-exempt: cat:reason -->` 在同行或前 1 行 → 跳過命中 | 新增 | 豁免距離單元測試 |
| AC7 | 豁免欄位驗證：category 4 類白名單；reason ≥10 字元；baseline-gated 含數字；ticket-tracked 含 ticket id regex | 新增 | 4 category × 正/負例 |
| AC8 | 豁免審計：Phase 4 進入時 stdout 列 outstanding 清單（行號+category+reason） | 新增 | fixture ≥2 豁免標記 |
| AC9 | 註冊 settings.json PostToolUse + PreToolUse 兩 matcher；timeout 5s | 原 AC4 細化 | settings.json diff + 觸發測試 |
| AC10 | 測試 `.claude/hooks/tests/test_phase4_decision_enforcement_hook.py` 覆蓋 detection/exemption/blocking/audit，覆蓋率 ≥90% | 原 AC5 細化 | pytest + coverage |

**原 AC 對應**：AC1→1、AC2→2、AC3→3+4+5、AC4→9、AC5→10；新增 6/7/8 覆蓋豁免機制

---

### 7. 交付 Phase 2 sage-test-architect 的輸入

§1 regex 清單 / §2 A+D 雙事件 / §3 豁免語法 / §4 訊息樣板 / §6 10 項細化 AC（1:1 對應設計測試）

---

### 8. 完成摘要

- phrase 分類數：**8 條 regex**（M×3 / W×3 / I×2）
- 觸發時機：**PostToolUse(phase ... phase4) 主 + PreToolUse(complete) 輔**
- 獨立 vs 整合：**獨立 Hook**（掃描函式供 acceptance_checkers 複用）
- 細化 AC 數：**10 項**（原 5 → 10）
## Test Results

<!-- To be filled by executing agent -->

---

### Phase 2 Test Plan（sage-test-architect 設計）

**設計者**: sage-test-architect | **輸入**: Phase 1 §1-§6 規格 | **目標覆蓋率**: ≥90%
**測試檔案**: `.claude/hooks/tests/test_phase4_decision_enforcement_hook.py`
**載入方式**: importlib.util.spec_from_file_location（參考 test_agent_dispatch_validation_hook.py）

---

## 1. 測試分層與策略

| 層級 | 對象 | 策略 | 比例 |
|------|------|------|------|
| L1 單元 | regex 偵測（M1-M3/W1-W3/I1-I2） | Sociable，真實字串 input | 40% |
| L2 單元 | 豁免解析（_parse_exempt_marker） | 正/負 category + reason 格式 | 20% |
| L3 單元 | 豁免距離匹配（同行 / 前 1 行 / 前 2 行不生效） | fixture 行位 | 10% |
| L4 整合 | main() PostToolUse/PreToolUse | Mock stdin JSON + capsys | 20% |
| L5 整合 | settings.json 觸發契約 | 讀取 settings.json + matcher 驗證 | 10% |

---

## 2. 測試案例總表

### 2.1 L1 — Regex 偵測（共 40 案例：8 regex × (3 正 + 2 負)）

#### M1 Phase X 再決定（MUST-block）

| # | 類別 | 輸入（片段） | 預期 |
|---|------|-------------|------|
| M1-P1 | 正例 | Phase 4 再決定是否保留 use_cache | 命中 M1 |
| M1-P2 | 正例 | Phase 5 視 baseline 決定 | 命中 M1 |
| M1-P3 | 正例 | phase 4 再評估 (小寫) | 命中 M1（IGNORECASE） |
| M1-N1 | 負例 | Phase 4 完成實作 | 不命中 |
| M1-N2 | 負例 | Phase 1 → Phase 2 過渡 | 不命中 |

#### M2 之後/以後 再決定（MUST-block）

| # | 類別 | 輸入 | 預期 |
|---|------|------|------|
| M2-P1 | 正例 | use_cache 之後再決定 | 命中 M2 |
| M2-P2 | 正例 | 以後再處理 CheckpointStateError | 命中 M2 |
| M2-P3 | 正例 | 日後再考慮 extension error | 命中 M2 |
| M2-N1 | 負例 | 之後會補充測試於 Phase 2 | 不命中（無「再決定/說/考慮」） |
| M2-N2 | 負例 | 完成後立即處理 | 不命中 |

#### M3 保留以防萬一（MUST-block）

| # | 類別 | 輸入 | 預期 |
|---|------|------|------|
| M3-P1 | 正例 | 保留 use_cache 以防萬一 | 命中 M3 |
| M3-P2 | 正例 | 保留擴展彈性 | 命中 M3 |
| M3-P3 | 正例 | 保留以備不時之需 | 命中 M3 |
| M3-N1 | 負例 | 保留原有實作 | 不命中 |
| M3-N2 | 負例 | 保留此區段註解 | 不命中 |

#### W1 視 X 結果再決定（WARN）

| # | 類別 | 輸入 | 預期 |
|---|------|------|------|
| W1-P1 | 正例 | 視 baseline 結果再決定 | 命中 W1，level=WARN |
| W1-P2 | 正例 | 視實測情況決定 | 命中 W1 |
| W1-P3 | 正例 | 視需求結果而評估 | 命中 W1 |
| W1-N1 | 負例 | 視需要調整 | 不命中（無決定/判斷/評估） |
| W1-N2 | 負例 | 結果已評估完成 | 不命中 |

#### W2 未來/以後 可能需要（WARN）

| # | 類別 | 輸入 | 預期 |
|---|------|------|------|
| W2-P1 | 正例 | 未來可能需要 cache | 命中 W2 |
| W2-P2 | 正例 | 以後或許會用到 | 命中 W2 |
| W2-P3 | 正例 | 未來也許要用 | 命中 W2 |
| W2-N1 | 負例 | 未來版本實作 | 不命中 |
| W2-N2 | 負例 | 可能發生競爭條件 | 不命中 |

#### W3 先保留再說（WARN）

| # | 類別 | 輸入 | 預期 |
|---|------|------|------|
| W3-P1 | 正例 | 先保留再說 | 命中 W3 |
| W3-P2 | 正例 | 先不動吧 | 命中 W3 |
| W3-P3 | 正例 | 先留著 | 命中 W3 |
| W3-N1 | 負例 | 先實作再測試 | 不命中 |
| W3-N2 | 負例 | 保留以供審查 | 不命中 |

#### I1 TBD/TODO/FIXME（INFO）

| # | 類別 | 輸入 | 預期 |
|---|------|------|------|
| I1-P1 | 正例 | TODO: Phase 4 決定 | 命中 I1，level=INFO |
| I1-P2 | 正例 | FIXME: 之後處理 | 命中 I1 |
| I1-P3 | 正例 | TBD: 未來補充 | 命中 I1 |
| I1-N1 | 負例 | TODO: 實作 foo() | 不命中 |
| I1-N2 | 負例 | 已完成 TODO | 不命中 |

#### I2 擴展彈性/擴充介面（INFO）

| # | 類別 | 輸入 | 預期 |
|---|------|------|------|
| I2-P1 | 正例 | 保留擴展彈性 | 命中 I2（與 M3 共命中，取高級） |
| I2-P2 | 正例 | 提供擴充介面 | 命中 I2 |
| I2-P3 | 正例 | 預留擴展空間 | 命中 I2 |
| I2-N1 | 負例 | 介面已實作 | 不命中 |
| I2-N2 | 負例 | 擴展功能完成 | 不命中 |

### 2.2 L2 — 豁免標記解析（共 12 案例）

| # | 輸入 | category | reason | 預期 |
|---|------|----------|--------|------|
| EX-P1 | PC-093-exempt: tdd-transition:Phase 2 補 RED 測試 | tdd-transition | 正當 | valid |
| EX-P2 | PC-093-exempt: baseline-gated:baseline>80ms 才啟用 | baseline-gated | 含數字 | valid |
| EX-P3 | PC-093-exempt: ticket-tracked:延後至 W11-005 | ticket-tracked | 含 ticket id | valid |
| EX-P4 | PC-093-exempt: user-override:PM 判斷特殊情境 | user-override | ≥10 字 | valid |
| EX-N1 | PC-093-exempt: unknown-cat:理由充足十字 | unknown-cat | - | invalid（白名單外） |
| EX-N2 | PC-093-exempt: tdd-transition:短 | tdd-transition | <10 字 | invalid |
| EX-N3 | PC-093-exempt: baseline-gated:沒有數字理由夠長 | baseline-gated | 無數字 | invalid |
| EX-N4 | PC-093-exempt: ticket-tracked:沒ticket引用夠長 | ticket-tracked | 無 ticket id | invalid |
| EX-N5 | PC-093-exempt: missing-reason | - | - | invalid（格式錯誤） |
| EX-N6 | PC-093-exempt:tdd-transition:無空格寬鬆模式 | - | - | 決策：寬鬆 valid |
| EX-N7 | pc-093-exempt: tdd-transition:小寫 | - | - | invalid（大小寫敏感） |
| EX-N8 | 純文字 PC-093-exempt（非 HTML comment） | - | - | invalid |

### 2.3 L3 — 豁免距離匹配（共 5 案例）

| # | fixture 描述 | 預期 |
|---|-------------|------|
| DIST-1 | exempt marker 同行後綴命中 phrase | 豁免生效 |
| DIST-2 | exempt marker 在 phrase 前 1 行 | 豁免生效 |
| DIST-3 | exempt marker 在 phrase 前 2 行 | 豁免**不**生效 |
| DIST-4 | exempt marker 在 phrase 後 1 行 | 豁免**不**生效 |
| DIST-5 | 多個豁免 marker 各自對應不同命中 | 個別生效 |

### 2.4 L4 — main() 整合測試（共 10 案例）

| # | event | tool input | ticket 內容 | 預期 exit | 預期 stream |
|---|-------|-----------|-------------|----------|------------|
| INT-1 | PostToolUse | ticket track phase X phase4 | 乾淨 | 0 | stdout: 豁免清單（空） |
| INT-2 | PostToolUse | phase X phase4 | 含 M1 命中 | **2** | stderr: block + PC-093 + AUQ 骨架 |
| INT-3 | PostToolUse | phase X phase4 | 含 M1 但合法豁免 | 0 | stdout: 豁免審計清單 |
| INT-4 | PostToolUse | phase X phase4 | 含 W1/W2 僅 WARN | 0 | stdout: 警告（不 stderr，IMP-048） |
| INT-5 | PostToolUse | phase X phase4 | 含 I1 僅 INFO | 0 | stdout: 提醒 |
| INT-6 | PostToolUse | phase X phase3b | 含 M1 | 0 | 不觸發 |
| INT-7 | PreToolUse | ticket track complete X | 含 M3 殘留 | **2** | stderr: 殘留延後，不得 complete |
| INT-8 | PreToolUse | ticket track complete X | 乾淨 | 0 | stdout: 無 |
| INT-9 | PostToolUse | phase X phase4 | 同行多命中（M1+M3） | 2 | stderr: 列出全部命中 |
| INT-10 | PostToolUse | 非 ticket 命令 | N/A | 0 | 不觸發 |

### 2.5 L5 — settings.json 契約（共 3 案例）

| # | 驗證項 | 預期 |
|---|--------|------|
| CFG-1 | PostToolUse matcher 針對 ticket track phase.*phase4 | 存在 |
| CFG-2 | PreToolUse matcher 針對 ticket track complete | 存在 |
| CFG-3 | timeout=5s，hook 路徑正確 | 符合 |

---

## 3. Fixture 設計

目錄: `.claude/hooks/tests/fixtures/pc093/`

| Fixture | 用途 |
|---------|------|
| clean_ticket.md | 無任何延後話術（INT-1/INT-8） |
| ticket_with_must_block.md | 含 M1/M2/M3 各 1 處（INT-2/INT-9） |
| ticket_with_exempt.md | 含 M1 + 合法 exempt marker（INT-3） |
| ticket_with_warn_only.md | 僅 W1/W2/W3（INT-4） |
| ticket_with_info_only.md | 僅 I1/I2（INT-5） |
| ticket_with_multi_exempt.md | 4 類 category 各一（DIST-5） |
| ticket_exempt_distance.md | marker 距離變化（DIST-1~4） |

---

## 4. 邊界案例清單（共 8 項）

| # | 邊界 | 測試設計 |
|---|------|---------|
| B1 | 空 ticket md | main() 不 crash，exit 0 |
| B2 | ticket md 不存在 | 記錄 warning log，exit 0 |
| B3 | Unicode 全形標點 | regex 正常命中 |
| B4 | 極長行（>10KB 單行） | regex 不 timeout |
| B5 | 巢狀 HTML comment | 解析不誤判 |
| B6 | phrase 在程式碼區塊內 | 決策：仍命中（保守偵測） |
| B7 | 同行多 phrase 命中 | 全部列出 |
| B8 | stdin JSON 缺 tool_input.command | exit 0（防禦性） |

---

## 5. 覆蓋率目標與 AC 對應

| AC | 對應測試 |
|----|---------|
| AC1 hook 檔存在+語法 | import 測試 |
| AC2 8 regex + rationale | L1 共 40 案例 |
| AC3 PostToolUse MUST-block | INT-2, INT-9 |
| AC4 PreToolUse 二次掃描 | INT-7, INT-8 |
| AC5 WARN/INFO stdout 分流 | INT-4, INT-5 |
| AC6 豁免距離 | DIST-1~4 |
| AC7 豁免欄位驗證 | EX-P1~P4, EX-N1~N8 |
| AC8 豁免審計清單 | INT-3, DIST-5 |
| AC9 settings.json 註冊 | CFG-1~3 |
| AC10 覆蓋率 ≥90% | pytest --cov |

---

## 6. 拆分友善性標註（W3-020）

| GWT Group | 功能職責 | 跨群組依賴 |
|-----------|---------|----------|
| L1 regex 偵測 | Phrase 偵測核心 | 無 |
| L2 豁免解析 | 標記解析器 | 無 |
| L3 豁免距離 | 行位匹配邏輯 | 依賴 L2 |
| L4 main() 整合 | 事件路由 | 依賴 L1+L2+L3 |
| L5 settings.json | 外部契約 | 無 |

Phase 3b 可拆分派發：L1/L2/L5 獨立；L3 在 L2 後；L4 最後整合。

---

## 7. 統計總結

- **測試案例總數**: 78（L1=40 + L2=12 + L3=5 + L4=10 + L5=3 + 邊界 8 納入整合執行）
- **覆蓋率目標**: ≥90%（line + branch）
- **邊界案例數**: 8
- **Fixture 檔案**: 7
- **GWT Groups**: 5（高可拆分度）

---

## 8. Handoff to pepper（Phase 3a）

輸入齊備：§1-§5 分層 / §2 78 案例 / §3 fixture / §4 邊界 / §6 拆分標註。pepper 任務：轉語言無關策略 + pseudo code。
## Completion Info

**Completion Time**: (pending)
**Executing Agent**: basil-hook-architect
**Review Status**: pending