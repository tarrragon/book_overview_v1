---
id: 0.18.0-W16-004.3
title: 修改 sync-claude-push.py 使用 git update-index 顯式設定 hook mode
type: IMP
status: pending
version: 0.18.0
wave: 16
priority: P2
parent_id: 0.18.0-W16-004
children: []
blockedBy:
- 0.18.0-W16-004.2
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary (PM 前台)
  history: {}
what: 修改 sync-claude-push.py 使用 git update-index 顯式設定 hook mode
when: W16-004 根因確認 Windows git 無法從 NTFS 推斷 exec bit 後
where:
  layer: Infrastructure
  files:
  - .claude/scripts/sync-claude-push.py
why: '目前 W16-004 主 ticket 已加 safety net（push 前對 temp_dir/hooks/**/*.py chmod +x），但此
  chmod 僅改 filesystem mode。Windows NTFS 無 executable bit 概念，chmod +x 對 git index 無作用，Windows
  使用者 push 出的新檔案仍會被記為 100644。


  根本治本方案：改用 git update-index --add --chmod=+x 顯式設定 git index mode。此命令直接寫入 git index，不依賴
  filesystem 語意，跨平台一致。


  若不治本，W16-004 的 push 端 safety net 在 macOS 有效但 Windows 無效，Windows 仍是破口。'
how:
  task_type: Implementation
  strategy: "1. 重構 sync-claude-push.py 的 restore_executable_bits 函式：\n   - 保留 filesystem\
    \ chmod +x（macOS/Linux 有效）\n   - 追加 git update-index --add --chmod=+x 呼叫（跨平台一致）\n\
    \   - 呼叫時機：git add -A 之後，git commit 之前（或與 git add 整合）\n2. 程式碼骨架：\n   for py_file\
    \ in (temp_dir / subdir).rglob(\"*.py\"):\n       rel = py_file.relative_to(temp_dir)\n\
    \       run_git([\"update-index\", \"--add\", \"--chmod=+x\", str(rel)],\n   \
    \            cwd=str(temp_dir), check=False)\n3. 需注意：git update-index --chmod\
    \ 要求檔案已 tracked，所以先 git add -A 建立 tracking，再 chmod\n4. 考慮抽出到 sync 共用邏輯（與 pull.py\
    \ 的 restore_executable_bits 對稱）\n5. 本地驗證：在 macOS 執行 sync-push，檢查 temp_dir 的 git\
    \ ls-files --stage 顯示 hooks/*.py 為 100755"
acceptance:
- '[ ] sync-claude-push.py 的 restore_executable_bits 除 filesystem chmod 外，追加 git update-index
  --add --chmod=+x 呼叫'
- '[ ] git add 和 git update-index 順序正確（add 先 tracked，再 chmod）'
- '[ ] macOS 本地 dry-run 確認 temp_dir git ls-files --stage 顯示 hooks/**/*.py 為 100755'
- '[ ] 完成後與 W16-004.1 + W16-004.2 三者一起做 round-trip sync-push 驗證'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-04-20'
updated: '2026-04-20'
---

# Execution Log

## Task Summary

修改 sync-claude-push.py 使用 git update-index 顯式設定 hook mode

---

## Problem Analysis

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: rosemary (PM 前台)
**Review Status**: pending
