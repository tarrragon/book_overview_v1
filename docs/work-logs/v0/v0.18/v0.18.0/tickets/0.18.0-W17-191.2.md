---
id: 0.18.0-W17-191.2
title: 新增 package.json test:hooks script
type: IMP
status: completed
version: 0.18.0
wave: 17
priority: P2
parent_id: 0.18.0-W17-191
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-python-developer
  history: {}
what: 新增 package.json test:hooks script
when: 需建 feature branch / worktree；無 blockedBy（可與 W17-191.1 並行）
where:
  layer: 待定義
  files:
  - package.json
why: W17-191 拆分子任務 .2：package.json 在 main 保護分支外，需建獨立分支處理；隔離為單獨 sub-ticket 便於 worktree
  操作
how:
  task_type: Implementation
  strategy: (1) 建立 feature branch（git checkout -b feat/W17-191.2 或 /worktree create）；(2)
    加 test:hooks script 為 uv run --project .claude/hooks pytest .claude/hooks/tests/；(3)
    驗證 npm run test:hooks 跑 2144+ 測試；(4) commit 並合併回 main
acceptance:
- '[x] package.json scripts 新增 test:hooks 指向 uv run --project .claude/hooks pytest
  .claude/hooks/tests/'
- '[x] npm run test:hooks 成功跑 2144+ 測試（不含 test_analytics 由 W17-192 處理）'
- '[x] 分支已合併回 main'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-11T14:18:41'
completed_at: '2026-05-11T14:20:54'
created: '2026-05-11'
updated: '2026-05-11'
---

# Execution Log

## Task Summary

新增 package.json test:hooks script

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

W17-191.1 已 commit（991b1b20）建立 `.claude/hooks/pyproject.toml` 與 CLAUDE.md §5 `npm run test:hooks` 指令說明，但 `package.json` 尚未實際加入 `test:hooks` script，導致 CLAUDE.md 記載的指令當下會回報 `npm ERR! Missing script: "test:hooks"`，文件與行為不一致。

### 影響範圍

- `package.json` scripts 區塊（單檔變更）
- 不影響既有 `test` / `test:core` / `test:e2e:*` 等指令

### 相關 Error Pattern

PC-124 transitive deps gap（W17-190 父 ANA）：W17-191.1 的 `pyproject.toml` 解決 deps 宣告，本 ticket 補 npm 入口讓 `npm run test:hooks` 一鍵可用，避免後人手動拼 `uv run --project .claude/hooks pytest ...`。

### 既有設計對齊

- `.claude/hooks/pyproject.toml` 已宣告 dev deps（pytest、pyyaml）
- CLAUDE.md §5 寫法為 `uv run --project .claude/hooks pytest tests/`（相對 .claude/hooks/）
- ticket strategy 指令為 `uv run --project .claude/hooks pytest .claude/hooks/tests/`（從專案根）
- 兩者實際指向同一測試目錄，本 ticket 採 ticket strategy 寫法以避免依賴 cwd

---

## Context Bundle

### Dispatch Plan

| 欄位 | 值 |
|------|---|
| ticket | 0.18.0-W17-191.2 |
| agent | thyme-python-developer |
| files | package.json |
| deps | W17-191.1 (completed @ 991b1b20) |
| context source | 父 W17-190 ANA + 本 ticket Problem Analysis |
| commit policy | feature branch (feat/W17-191.2) → commit → 派發後由 PM 合併回 main |
| run mode | 主 repo cwd（package.json 非 .claude/，feature branch 隔離 main） |

### 既有檔案參考

- `/Users/mac-eric/project/book_overview_v1/package.json`（scripts 區塊 line 6-43，test:* 群組 line 7-21）
- `/Users/mac-eric/project/book_overview_v1/.claude/hooks/pyproject.toml`（W17-191.1 建立）
- `/Users/mac-eric/project/book_overview_v1/CLAUDE.md` §5（test:hooks 指令說明）

### 實作關鍵點

1. 新增 script `"test:hooks": "uv run --project .claude/hooks pytest .claude/hooks/tests/"`
2. 插入位置建議：scripts 區塊內 test:* 群組末尾（line 21 `test:comprehensive` 之前），與其他 test 群組視覺對齊
3. 驗證指令：`npm run test:hooks` 應跑 .claude/hooks/tests/ 下測試（2144+ 測試，不含 test_analytics）

### 不在本 ticket 範圍

- 修改 .claude/hooks/pyproject.toml（W17-191.1 已處理）
- 修改 CLAUDE.md §5 文件（W17-191.1 已處理）
- 處理 test_analytics.py（W17-192 已處理）

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 實作摘要

在 `package.json` scripts 區塊（line 21 `test:comprehensive` 之前）新增單行 script：

```json
"test:hooks": "uv run --project .claude/hooks pytest .claude/hooks/tests/",
```

### 流程

1. 建立 feature branch `feat/W17-191.2`
2. Edit `package.json` 插入 test:hooks（與 test:e2e:* 群組視覺對齊，4-space 縮排）
3. Commit 於 feature branch：`a996432b`
4. 切回 main 並 `git merge --no-ff feat/W17-191.2`：merge commit `bd490f04`

### Commit SHA

- Feature commit: `a996432b`
- Merge commit: `bd490f04`

### 設計決策

- 採 ticket strategy 寫法 `uv run --project .claude/hooks pytest .claude/hooks/tests/`（從專案根，不依賴 cwd），與 CLAUDE.md §5 寫法（從 .claude/hooks/ cwd）等效但更穩健
- 插入位置在 test:e2e:* 之後、test:comprehensive 之前，保持 test:* 群組連續性

### 不在本 ticket 範圍

- 修復 .claude/hooks/tests/ 下 44 個既有失敗測試（非 W17-191 範圍；W17-192 已處理 test_analytics）
- 修改 .claude/hooks/pyproject.toml（W17-191.1 已處理）
- 修改 CLAUDE.md §5（W17-191.1 已處理）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

### 驗證指令

```bash
npm run test:hooks
```

實際展開為 `uv run --project .claude/hooks pytest .claude/hooks/tests/`。

### 執行結果

- **執行時間**: 12.37s
- **總測試數**: 2167（2090 passed + 44 failed + 33 skipped）
- **Pass count**: 2090
- **Skip count**: 33（含 test_analytics.py 整檔 skip，由 W17-192 處理）
- **Fail count**: 44（既有 pre-existing 失敗，散落於 test_stop_hook / test_stop_worklog_handoff_sync_check_hook / test_ticket_tracker / test_wrap_decision_tripwire_hook，非本 ticket 範圍）

### Acceptance 對照

- [x] script 成功掛載並執行 pytest（npm 入口可用）
- [x] 跑超過 2144 個測試（實際 2167 collected）
- [x] 不含 test_analytics（已 skip）

### 既有失敗備註

44 個失敗為 W17-191.2 範圍外的 pre-existing issues，需後續獨立 ticket 處理。本 ticket 只負責 script 掛載，不負責修復測試內容。
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-11T14:20:54
**Executing Agent**: thyme-python-developer
**Review Status**: pending