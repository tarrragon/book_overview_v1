---
id: 0.18.0-W17-183
title: 落地 PC-135 防護機制（hook script pep723 dep 達及性檢查 + lib import noisy fallback）
type: DOC
status: pending
version: 0.18.0
wave: 17
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W17-181.1
dispatch_reason: ''
decision_tree_path:
  entry_point: 規則層 / quality-baseline.md 規則 5（所有發現必須追蹤）
  final_decision: PC-135 防護機制獨立 DOC 追蹤
  rationale: PC-135 已寫但防護建議（noisy fallback / 派發 prompt 加註）需獨立落地
who:
  current: thyme-python-developer
  history: {}
what: 落地 PC-135 防護機制（hook script pep723 dep 達及性檢查 + lib import noisy fallback）
when: W17-181.1 / W17-181.2 完成後
where:
  layer: 待定義
  files:
  - .claude/hooks/handoff-auto-resume-stop-hook.py
  - .claude/hooks/handoff-prompt-reminder-hook.py
  - .claude/hooks/handoff-reminder-hook.py
  - .claude/references/agent-dispatch-template.md
  - .claude/skills/ticket/ticket_system/lib/handoff_utils.py
why: PC-135 文件描述了三層防護（規則層 / 派發提示 / hook noisy fallback / lib 正本清源）但未實際落地。本 ticket
  追蹤實際修改：(1) 三 handoff hook silent fallback 改 noisy（stderr warning）(2) 派發 prompt 模板加註「實機
  caller 環境驗證」(3) handoff_utils 改用 ticket_system.constants 取代 ticket_system.lib.constants
  斷 yaml 鏈（W17-181.4 候選）
how:
  task_type: Documentation
  strategy: (1) 修三 hook 的 try/except 加 stderr warning (2) 更新 .claude/references/agent-dispatch-template.md
    派發模板加 PC-135 caller env 驗證提醒 (3) 評估 handoff_utils import refactor (W17-181.4 範圍可能合併)
acceptance:
- '[x] 三 handoff hook silent fallback 改 noisy'
- '[x] 派發模板加 PC-135 caller env 驗證提醒'
- '[x] handoff_utils 切 top-level constants（或標 W17-181.4 待後續）'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-05-10'
updated: '2026-05-10'
---

# Execution Log

## Task Summary

落地 PC-135 防護機制（hook script pep723 dep 達及性檢查 + lib import noisy fallback）

---

## Problem Analysis
<!-- Schema[DOC/Problem Analysis]: 選填（若 DOC 起因於缺陷或盤點結論可填）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---


## 派發前 context

### 任務內容

落地 PC-135（subagent-pytest-pass-but-hook-subprocess-fail）防護機制。

### 檔案所有權

本 ticket **不修改** `.claude/references/agent-dispatch-template.md`（由 W17-182.1 處理）。

| 檔案 | 修改內容 |
|------|---------|
| `.claude/hooks/handoff-auto-resume-stop-hook.py` L91-100 | silent fallback `try: from handoff_utils import is_handoff_stale; except` 區塊改 noisy（stderr warning） |
| `.claude/hooks/handoff-prompt-reminder-hook.py` L46-52 附近 import | 同上，import 失敗加 stderr warning |
| `.claude/hooks/handoff-reminder-hook.py` | 若有同模式 silent fallback 同步處理 |
| `.claude/skills/ticket/ticket_system/lib/handoff_utils.py` | 評估改用 `from ticket_system.constants import` 取代 `from ticket_system.lib.constants` 斷 yaml 依賴鏈（W17-181.4 範圍可整合） |

### 引用依據

PC-135: `.claude/error-patterns/process-compliance/PC-135-subagent-pytest-pass-but-hook-subprocess-fail.md`

### 自檢

完成後實機觸發 stop hook，確認新版本仍能正確 GC（迴歸保護）。若 lib 改 import 路徑後 hook 即使移除 pyyaml dep 也能運作為理想狀態（subagent 執行時可評估）。
## Solution
<!-- Schema[DOC/Solution]: 免填（DOC 類型以變更摘要取代）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## 變更摘要

### Acceptance 1: 三 handoff hook silent fallback 改 noisy（已落地）

| 檔案 | 變更 |
|------|------|
| .claude/hooks/handoff-auto-resume-stop-hook.py | (1) is_handoff_stale import fallback 加 stderr warning（含異常類型與訊息）(2) 新增 is_ticket_terminal import 的 try/except 包裹（原為硬 import），失敗時退化為「永遠非 terminal」並寫 stderr |
| .claude/hooks/handoff-prompt-reminder-hook.py | is_ticket_completed 硬 import 改 try/except，失敗退化為「永遠未完成」並寫 stderr（PC-135 訊息） |
| .claude/hooks/handoff-reminder-hook.py | is_handoff_stale import fallback 加 stderr warning |

所有 PC-135 警告訊息格式統一：[hook-name][PC-135] module/symbol import failed, using degraded fallback-name fallback. Cause: ExceptionType: message

### Acceptance 2: 派發模板加 PC-135 caller env 驗證提醒

不在本 ticket 範圍：W17-183 不修改 .claude/references/agent-dispatch-template.md（W17-182.1 獨佔，已於派發 prompt 明示）。

### Acceptance 3: handoff_utils 切 top-level constants（標 W17-181.4 待後續）

評估結論：本 ticket 不執行，標待 W17-181.4 處理。理由：

1. handoff_utils.py 不只 import ticket_system.lib.constants，還 import ticket_system.lib.paths.get_project_root。
2. paths.py 僅存在於 ticket_system/lib/（無 top-level 對應），因此 from ticket_system.lib.paths import 仍會觸發 ticket_system/lib/__init__.py 的 eager-import → ticket_loader → yaml。
3. 只切換 constants import 無法切斷 yaml 依賴鏈，需同時將 paths.py 上移至 top-level（或改 lib/__init__.py 為 lazy import）。此為較大重構，符合 ticket acceptance 預留豁免條件「或標 W17-181.4 待後續」。

實機驗證：以 /usr/bin/python3（無 pyyaml）執行三 hook，PC-135 noisy fallback 訊息正確輸出至 stderr，hook exit code 仍為 0（功能性退化但可見）。

## 迴歸驗證結果

正常環境（uv with deps）：lib import 成功，無 PC-135 warning，exit 0。

退化環境（/usr/bin/python3 無 pyyaml）：

- handoff-auto-resume-stop-hook.py → stderr 輸出 [handoff-auto-resume-stop-hook][PC-135] handoff_utils import failed, using degraded is_handoff_stale fallback (always returns False). Cause: ModuleNotFoundError: No module named 'yaml'，exit 0
- handoff-prompt-reminder-hook.py → stderr 輸出 [handoff-prompt-reminder-hook][PC-135] handoff_utils import failed, using degraded is_ticket_completed fallback (always returns False). Cause: ModuleNotFoundError: No module named 'yaml'，exit 0

兩 hook 在退化環境下均：(1) 正確輸出 PC-135 標識的 stderr 警告（含 ExceptionType 與訊息）(2) exit 0 維持流程不阻塞 (3) 退化邏輯已於警告訊息明示。
## Test Results
<!-- Schema[DOC/Test Results]: 免填（DOC 類型無需測試）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[DOC/Completion Info]: 必填（需附變更摘要：哪些文件/章節更新）（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.18.0-W17-181.1 | chars: 317 -->

### Task Reference
- 0.18.0-W17-181.1 what: 改 handoff_utils.is_ticket_completed (L49) 和 is_ticket_in_progress_or_completed (L102) 簽章接受 project_root: Path，內部改用 find_ticket_file。is_handoff_stale 同步改簽章。所有呼叫端適配。

### Rationale Chain
- 0.18.0-W17-181.1 why: 子進程環境下無參數 get_project_root() fallback 錯誤 cwd，is_ticket_completed 回 False（W17-181 實驗驗證）。ARCH-020。

### Related Files
- .claude/skills/ticket/ticket_system/commands/handoff_gc.py  # from 0.18.0-W17-181.1