---
id: 0.18.0-W10-017.8.2
title: 重構 phase_label/next_action 與 view 層耦合提取
type: IMP
status: completed
version: 0.18.0
wave: 10
priority: P2
parent_id: 0.18.0-W10-017.8
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: Phase 4 延後追蹤 (linux L10)
  rationale: i18n 觸發後再提取為 view function 避免 view 層耦合
who:
  current: thyme-python-developer
  history: {}
what: 重構 phase_label/next_action 與 view 層耦合提取
when: 待定義
where:
  layer: Infrastructure
  files:
  - .claude/skills/ticket/ticket_system/lib/checkpoint_state.py
why: W10-017 審查後修正：linux 指出 017.1/3/5 共用同一 state 推導邏輯，應先抽象核心函式避免三次實作；017.6 觀測 log
  是橫切關注點應併入
how:
  task_type: Implementation
  strategy: 待定義
acceptance:
- '[x] phase_label/next_action 抽出為 view function'
- '[x] CheckpointState 不持有人類可讀字串'
- '[x] 所有測試 GREEN'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-19T02:36:19'
completed_at: '2026-04-19T02:39:53'
created: '2026-04-19'
updated: '2026-04-19'
---

# Execution Log

## Task Summary

重構 phase_label/next_action 與 view 層耦合提取

---

## Problem Analysis

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

### 變更內容

`.claude/skills/ticket/ticket_system/lib/checkpoint_state.py`：
- `CheckpointState` dataclass 移除 `phase_label` / `next_action` 兩欄位（純 state，不含 view 字串）
- 新增 `format_phase_label(state)` / `format_next_action(state)` 兩個 view function，內部委派 `_derive_checkpoint(state)` 取得對應字串
- `checkpoint_state()` 主函式不再 mutate `state.phase_label` / `state.next_action`（Step 3 只保留 `current_phase` 設定）
- `__all__` 新增 `format_phase_label` / `format_next_action`
- 公開欄位由 12 降為 10（dataclass 瘦身）

測試更新（僅欄位配合，不改語意）：
- `test_dataclass.py` `_make_state` helper 移除兩欄位；A1 改用 `format_phase_label(state)` 驗證；A4 門檻由 `>=12` 調整為 `>=10`；A5 簡化建構簽名
- `test_derivation.py` `_state` helper 移除兩欄位
- `test_observability_log.py` `_make_state` helper 移除兩欄位

### 變更行數

- `checkpoint_state.py`: -9 / +22（移除 2 dataclass 欄位 + 主函式 3 行 mutate；新增 2 個 view function + `__all__` 2 項）
- `test_dataclass.py`: -8 / +10
- `test_derivation.py`: -2 / +0
- `test_observability_log.py`: -2 / +0

### 設計考量

- view function 內部複用現成 `_derive_checkpoint`，避免複製 PRIORITIES 表
- 公開 API 向後兼容：新增函式名稱加入 `__all__`，舊欄位已無消費者（全專案 grep 確認僅 checkpoint_state.py 自身 + 測試 helper 引用）
- 便於未來 i18n 擴展：view function 簽名可擴充 `locale` 參數切換語言

---

## Test Results

55/55 GREEN (`uv run pytest tests/test_checkpoint_state/`)

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending