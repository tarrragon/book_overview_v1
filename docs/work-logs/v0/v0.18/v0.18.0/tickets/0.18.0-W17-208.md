---
id: 0.18.0-W17-208
title: 新增 CLI append-log H2 content warning
type: IMP
status: completed
version: 0.18.0
wave: 17
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.18.0-W17-206
dispatch_reason: ''
decision_tree_path:
  entry_point: 第六層:流程改善
  final_decision: IMP CLI append-log H2 warning
  rationale: W17-206 ANA 根因 a spawn
who:
  current: thyme-python-developer
  history: {}
what: 'CLI append-log 偵測寫入內容含 ## H2 時輸出 stderr warning（不阻擋），引導使用者改用 H3'
when: v0.18.0 wave 17，W17-207 DOC 落地後
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/commands/track_acceptance.py
why: append-log 對非 Execution Log section 零校驗，PM 誤用 H2 無任何系統層提示（W17-206 ANA 根因 a）
how:
  task_type: Implementation
  strategy: 在 execute_append_log 函式中，寫入前偵測 content 是否含 ^## 行，若有則 stderr warning 提示改用
    H3
acceptance:
- '[x] ["[ ] append-log 偵測內容含 ## H2 時輸出 stderr warning","[ ] warning 不阻擋寫入（僅提示）","[
  ] 既有測試通過"]'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-17T20:13:30'
completed_at: '2026-05-17T20:16:32'
created: '2026-05-17'
updated: '2026-05-17'
---

# Execution Log

## Task Summary

CLI append-log 偵測寫入內容含 ## H2 時輸出 stderr warning（不阻擋），引導使用者改用 H3

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

### 修改摘要

於 `execute_append_log`（`.claude/skills/ticket/ticket_system/commands/track_acceptance.py` 取得 content 之後、讀 body 之前）加入 H2 偵測：

- 條件：section ∈ {Solution, Test Results, Problem Analysis, Context Bundle, NeedsContext, Exit Status, Completion Info} 且 content 含 `^## ` 行
- 行為：stderr 輸出 WARNING，引導改用 H3；不阻擋寫入（保留向後相容）
- Execution Log section 不受影響（不在偵測集合內）

### 設計決策

- 使用既有 `re` 模組（檔案上方已 import），不引入新依賴
- Schema 章節集合 inline 定義在函式內，避免新增模組級常數；未來若有複用需求再抽 lib
- warning 字串包含「W17-072」溯源錨點

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

### 單元測試

`uv run --project .claude/skills/ticket pytest .claude/skills/ticket/tests/test_track_acceptance.py -x -q`
→ 24 passed（含新增 `TestAppendLogH2Warning` 3 個案例：H2 觸發 / H3 不觸發 / Execution Log 跳過）

### 手動驗證

`ticket track append-log 0.18.0-W17-208 --section "Solution" "## 測試 H2 觸發"`
→ stderr 輸出 `[append-log] WARNING: 偵測到內容含 H2 標題...`，stdout 仍顯示 `[OK]` 寫入完成（不阻擋）

### 安裝確認

`uv tool install .claude/skills/ticket --reinstall` 已執行（IMP-023）

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-17T20:16:32
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.18.0-W17-206 | chars: 619 -->

### Task Reference
- 0.18.0-W17-206 what: 分析 PM append-log Solution H2 自訂章節與內容重複的流程根因

### Rationale Chain
- 0.18.0-W17-206 why: W17-205 派發時 PM 用 ticket track append-log --section Solution 寫入預寫策略，產生 ## 實作策略 H2（W17-072 違規）且內容與後續 basil 實作的 Solution 高度重疊。表層是 W17-072 警告，但真正問題：(1) 引導層——append-log 是否清楚指引內容應為 H3 子節而非 H2？(2) 流程層——PM 預寫 Solution 與 agent 實作 Solution 的職責邊界是否定義不清，導致雙寫重複？需 ANA 分析引導 vs 執行根因，提出規則/hook/SKILL 強化方案

### Related Files
- .claude/rules/  # from 0.18.0-W17-206
- .claude/pm-rules/  # from 0.18.0-W17-206
- .claude/skills/ticket/  # from 0.18.0-W17-206
- .claude/methodologies/  # from 0.18.0-W17-206
- .claude/error-patterns/  # from 0.18.0-W17-206

### Source Acceptance (reference)
- [ref] [ ] 重現實驗：列舉 append-log Solution 寫入時的 CLI 提示與既有規則引導完整性  # from 0.18.0-W17-206
- [ref] [ ] 分析根因：分離(a)引導不足(b)職責邊界不清(c)PC-040 副作用  # from 0.18.0-W17-206
- [ref] [ ] 至少 3 個防護方案比較（CLI auto-prefix / Schema 強化 / 規則明示 / hook 偵測）  # from 0.18.0-W17-206
- [ref] [ ] spawn 落地 IMP/DOC ticket 或顯性標註豁免  # from 0.18.0-W17-206

### 實作策略（PM 預寫）

依 W17-206 ANA 推薦方案 B，在 `execute_append_log` 函式（`.claude/skills/ticket/ticket_system/commands/track_acceptance.py` line 576 區域）寫入前偵測。

**偵測邏輯**：
- content 中存在 `^## ` 開頭行（H2 標題）
- section 為 Schema 章節（Solution / Test Results / Problem Analysis / Context Bundle / NeedsContext / Exit Status / Completion Info）
- 條件成立 → stderr warning：「偵測到內容含 H2 標題；append-log 寫入應為既有章節的 H3 子節，避免切斷 Schema 章節範圍（W17-072）。建議改用 ### 子標題。」
- warning **不阻擋寫入**（僅引導）

**測試要求**：
- 新增單元測試：含 H2 內容 → warning 觸發 + 寫入成功
- 新增單元測試：純 H3 內容 → 無 warning + 寫入成功
- 既有測試保持通過

**禁止行為**：
- 不修改寫入邏輯（只加 warning）
- 不阻擋 H2 寫入（向後相容既有 ticket）
- 不動 Execution Log section（該 section 結構不同）

**驗證指令**：
- `npm run test:hooks` 或 `cd .claude/skills/ticket && uv run pytest tests/`
- 手動測試：`ticket track append-log <任一 pending ticket> --section "Solution" "## 測試 H2"` → 應見 warning