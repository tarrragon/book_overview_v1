---
id: 0.18.0-W11-004.2
title: 實作 process-skip-guard-hook.py 加入 SA 前置審查 type/phase guard + 關鍵字精確化（IMP-A）
type: IMP
status: in_progress
version: 0.18.0
wave: 11
priority: P2
parent_id: 0.18.0-W11-004
children: []
blockedBy: []
relatedTo:
- 0.18.0-W5-013
- 0.18.0-W5-014
spawned_tickets:
- 0.18.0-W11-017
- 0.18.0-W11-018
- 0.18.0-W11-019
- 0.18.0-W11-020
- 0.18.0-W11-021
- 0.18.0-W11-022
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:Hook 修正
  final_decision: 加入 type/phase guard 過濾 SA 誤報
  rationale: W5-014 分析確認此方向為最低風險修正
who:
  current: thyme-python-developer
  history: {}
what: 為 process-skip-guard-hook.py 加入 Ticket type/phase 上下文感知：IMP type + Phase 4 時排除
  SA 前置審查提醒；DOC/ANA type 全面排除；同時精確化誤觸關鍵字 pair
when: W5-013 分析完成後立即可啟動（分析已提供修正方案）
where:
  layer: 待定義
  files:
  - .claude/hooks/process-skip-guard-hook.py
  - .claude/hooks/lib/ask_user_question_reminders.py
why: W5-014 saffron 分析：SA 前置審查誤報根因為缺 Ticket type/phase 上下文感知。Phase 4 重構非新功能，應排除 SA
  前置審查提醒。關鍵字 pair 過泛（「直接/完成」「不做/審查」）產生大量假陽性
how:
  task_type: Implementation
  strategy: '1. detect_skip_intent 觸發 SKIP_SA_REVIEW 後讀取當前 in_progress ticket（用 frontmatter_parser.read_ticket，失敗回退維持原行為）

    2. 排除規則：type∈{DOC,ANA} 全面靜音；type==IMP AND current_phase=="Phase 4" 靜音 SKIP_SA_REVIEW

    3. 關鍵字精確化：移除 SKIP_ACCEPTANCE.("直接","完成") 與 SKIP_SA_REVIEW.("不做","審查") 兩個高假陽性 pair；保留顯式
    SA 詞 ("跳過","sa")、("不需要","sa")

    4. 性能：active ticket 只在偵測到 skip intent 後才讀取（cold path）

    5. 測試覆蓋：Phase 4 IMP / DOC / ANA 三場景排除，加上既有測試持續 GREEN

    '
acceptance:
- '[x] 派發前查詢當前 Ticket 的 type 和 current_phase'
- '[x] IMP type + Phase 4 / 任何 phase 時排除 SA 提醒'
- '[x] DOC/ANA type 全面排除'
- '[x] 移除或改寫高假陽性關鍵字 pair'
- '[x] 新增測試覆蓋 Phase 4/DOC/ANA 三種排除場景'
- '[x] 既有測試持續 GREEN'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-29T02:24:16'
completed_at: null
created: '2026-04-12'
updated: '2026-04-12'
chain:
  root: 0.18.0-W11-004
  parent: 0.18.0-W11-004
  depth: 1
  sequence:
  - 4
  - 2
current_phase: Phase 4
assignee: cinnamon-refactor-owl
---

# Execution Log

## Task Summary

為 process-skip-guard-hook.py 加入 Ticket type/phase 上下文感知：IMP type + Phase 4 時排除 SA 前置審查提醒；DOC/ANA type 全面排除；同時精確化誤觸關鍵字 pair

---

## Problem Analysis

### PM 前台 Phase 1 分析（2026-04-29）

#### 問題根因

- `process-skip-guard-hook.py:66-76` 的 SKIP_SA_REVIEW 模式採純 keyword pair 偵測，無上下文感知；任何 IMP/DOC/ANA 任務只要文字命中 ("跳過","sa") / ("不做","審查") 就觸發提醒
- SKIP_ACCEPTANCE 含 ("不做","審查") pair，與 SKIP_SA_REVIEW 重疊但無區分；同一句話可能命中多模式
- "直接/完成"（SKIP_ACCEPTANCE）、"跳過/sa"（SKIP_SA_REVIEW）這類組合在重構/修復/維護任務中是合法措辭（例：「直接修復後完成」），高假陽性
- detect_skip_intent (lines 128-150) 僅讀 user_input_lower，未讀取 ticket frontmatter

#### 影響範圍

- .claude/hooks/process-skip-guard-hook.py（233 行，主檔）
- .claude/hooks/lib/ask_user_question_reminders.py（532 行，提醒文案邏輯）
- 相依工具：.claude/hooks/lib/frontmatter_parser.py:159 `read_ticket` 已具備 ticket 解析能力
- Active ticket 來源候選：(a) `ticket track list --status in_progress` (需 subprocess) (b) docs/work-logs/v*/tickets 直接掃描 in_progress

#### AC 漂移檢查（PC-055）

Ticket 建立 17 天，逐項對照 acceptance：
- AC1（派發前查詢 ticket type 和 current_phase）：仍適用，是 type/phase guard 核心需求
- AC2（IMP + Phase 4 排除 SA 提醒）：仍適用
- AC3（DOC/ANA type 全面排除）：仍適用
- AC4（移除/改寫高假陽性關鍵字 pair）：仍適用，今日驗證 SKIP_ACCEPTANCE.("直接","完成") 與 SKIP_SA_REVIEW.("不做","審查") 仍是高 FP 組合
- AC5（測試覆蓋三種排除場景）：仍適用
- AC6（既有測試持續 GREEN）：仍適用

#### 建議實作策略

1. 在 detect_skip_intent 觸發 SKIP_SA_REVIEW 後，讀取當前 in_progress ticket（用 frontmatter_parser.read_ticket）
2. 排除規則：
   - `type ∈ {DOC, ANA}` → 全面靜音
   - `type == IMP AND current_phase == "Phase 4"` → 靜音 SKIP_SA_REVIEW
3. 關鍵字精確化：
   - 移除 SKIP_ACCEPTANCE.("直接","完成")（與「直接修復完成」重疊度過高）
   - 移除 SKIP_SA_REVIEW.("不做","審查")（與一般審查討論重疊）
   - 保留 ("跳過","sa")、("不需要","sa") 等顯式 SA 詞
4. 性能：active ticket 只在偵測到 skip intent 後才讀取（cold path）
5. 失敗回退：若無 in_progress ticket 或讀取失敗，維持原行為（不靜音）

#### 相關 Error Pattern

- PC-007（接手獨立驗證範圍）— 已執行，hook 邏輯與 AC 對應確認
- PC-055（pending >7 天 AC 漂移）— 已逐項審查
- PC-053（規則修改必須 Ticket 追蹤）— 不適用（hook 修改非 rules/）
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Context Bundle

<!-- Phase 2 派發指引（sage-test-architect） -->

---

### Phase 4 派發指引（cinnamon-refactor-owl）

phase3b 完成（commit 4526c32c）：35 tests pass（15 new + 20 既有），thyme 自評認知負擔 ~5、函式 ≤25 行、巢狀 ≤3，結論「無需重構」。cinnamon 進行最終 phase4 多視角評估。

任務：
1. 讀完整 ticket（含 thyme 在 Solution 的偏離 pepper 點：移除 SKIP_SA_REVIEW.('不做','架構審查') pair）
2. 評估 .claude/hooks/process-skip-guard-hook.py + frontmatter_parser.py 程式碼品質
3. 依 PM 決議派發 parallel-evaluation 進行多視角審查
4. 整合結論：是否需重構 / 技術債 ticket / 直接 complete
5. 注意 thyme 報告遺留事項：line 190 INVALID exempt（pepper guidance 字面 cat:reason 範例觸發）→ 評估是否與 W11-017 同合併

完成後產出：
- ticket Solution 區段：Phase 4 評估結論
- complete ticket：`ticket track complete 0.18.0-W11-004.2`
- commit：`chore(0.18.0-W11-004.2): phase4 評估 - <重構/無需重構>`

PC-093 警示：禁延後話術。
Commit Policy：一個 commit 只放本 ticket 變更。
### Phase 3a 派發指引（pepper-test-implementer）

phase2 已由 sage 完成（commit 65c9153e），測試骨架在 .claude/hooks/tests/test_process_skip_guard_type_phase_guard.py（15 測試 RED + 介面契約）。Solution 區段含設計記錄。

phase3a 目標：規劃語言無關的實作策略指引，給 phase3b（thyme-python-developer）填補 hook 邏輯轉 GREEN。

任務範圍：
1. 閱讀 sage 在 Solution 區段的 Phase 3b 介面契約：
   - 新增 process_skip_guard_hook.get_active_in_progress_ticket() -> Optional[dict]
   - SKIP_PATTERNS 修改（移除 3 個高 FP pair）
   - main/detect_skip_intent 加 type/phase guard
2. 規劃實作策略：
   - get_active_in_progress_ticket 的實作路徑（subprocess 呼叫 ticket CLI vs 直接掃描 docs/work-logs/v*/tickets）
   - guard 整合位置（detect_skip_intent 內部 vs main 外層）
   - 失敗回退路徑（無 active ticket / read 例外）
   - cold path 性能保證機制
3. 識別技術債與架構決策（不延後話術，PC-093 即時決斷）
4. 不寫實作（pepper 的職責是策略 + 虛擬碼）
5. 不修改 hook 邏輯

完成後產出：
- ticket Solution 區段：Phase 3a 實作策略（虛擬碼、技術決策）
- 推進 phase 至 phase3b：`ticket track phase 0.18.0-W11-004.2 phase3b thyme-python-developer`
- commit：`chore(0.18.0-W11-004.2): phase3a pepper 實作策略規劃`

Commit Policy：一個 commit 只放本 ticket 變更，禁止觸碰其他 ticket。

<!-- PC-093-exempt: ticket-tracked:W11-004.2 本行為 pepper Phase 3a 元警示文，引號內為禁用話術範例非延後項 -->
PC-093 警示：規劃時若發現需要延後處理的項目，使用 `<!-- PC-093-exempt: cat:reason -->` 標記豁免，不要用「Phase 4 評估」「follow-up」「後續強化」等延後話術。
### Phase 2 任務範圍（sage-test-architect）

依 Problem Analysis 中已定義的 how.strategy（5 步驟）設計測試案例與 RED 骨架。

### 實作目標檔案（phase3b 將動）

- .claude/hooks/process-skip-guard-hook.py（233 行，主 hook）
- .claude/hooks/lib/ask_user_question_reminders.py（532 行，提醒文案）

### 測試目錄

- .claude/hooks/tests/ 或 .claude/tests/hooks/（依既有 hook 測試慣例擇一）
- 既有測試參考：grep -l "process-skip" .claude/hooks/tests/ 確認 fixture 模式
- 框架：pytest

### 測試策略五步對應

phase3b 將實作的策略（已寫入 ticket how.strategy）：

1. detect_skip_intent 觸發 SKIP_SA_REVIEW 後讀取 in_progress ticket（用 frontmatter_parser.read_ticket）
2. 排除規則：type∈{DOC,ANA} 全面靜音；type==IMP AND current_phase=="Phase 4" 靜音 SKIP_SA_REVIEW
3. 關鍵字精確化：移除 SKIP_ACCEPTANCE.("直接","完成") 與 SKIP_SA_REVIEW.("不做","審查")
4. 性能：cold path（只在偵測到 skip intent 後讀 ticket）
5. 失敗回退：無 in_progress ticket 或讀取失敗 → 維持原行為

### AC 對應測試案例（6 條 AC，sage 規劃對應測試）

1. **AC1 派發前查詢 type/phase**：mock active ticket，斷言 hook 有讀取 frontmatter
2. **AC2 IMP+Phase4 排除**：active ticket type=IMP, current_phase="Phase 4" + 含 SA skip 詞 → 不觸發提醒
3. **AC3 DOC/ANA 全面排除**：兩個獨立 case（DOC / ANA）+ SA skip 詞 → 不觸發提醒
4. **AC4 關鍵字精確化**：原 FP 句子（含「直接修復後完成」「不做架構審查改良」）→ 不再誤觸
5. **AC5 三場景排除測試**：對應 AC2/AC3
6. **AC6 既有測試 GREEN**：既有 SKIP_SA_REVIEW / SKIP_ACCEPTANCE 等模式仍能正確命中其他情境

### 失敗回退測試

- 無 in_progress ticket → SA skip 詞仍觸發提醒（向後相容）
- read_ticket 拋例外 → 不阻擋，原行為觸發提醒

### 限制與注意

- **禁止寫實作**：sage 只設計測試案例 + 撰寫 RED 測試骨架，不修改 process-skip-guard-hook.py
- 使用 pytest fixture + monkeypatch 模擬 active ticket（mock frontmatter_parser.read_ticket 或 mock list_tickets subprocess）
- 若 active ticket 來源實作未定（subprocess vs 直接掃描），sage 在 Solution 提出建議方案

### Commit Policy

- 派發完成後 sage commit 測試骨架（RED），訊息格式：
  `chore(0.18.0-W11-004.2): phase2 sage 測試設計 RED 骨架`
- 一個 commit 只放本 ticket 變更

### 不在本 ticket 範圍

- W11-017（phase-completion-gate-hook 假陽性）已分流為獨立 follow-up
- 其他 SKIP_PATTERNS 類型（SKIP_AGENT_DISPATCH 等）的調整 — 本 ticket 只動 SA_REVIEW 相關

---

## Solution

<!-- To be filled by executing agent -->

---




---

## Phase 4 多視角評估結論（PM 彙整 2026-04-29）

### 4 視角評分

| 視角 | 代理人 | 評分 | 主要發現 |
|------|--------|------|---------|
| Universal (taste) | linux | Acceptable B+ | _should_silence_sa_review 兩分支不對稱（資料結構小瑕疵） |
| Python 慣例 + Coupling | thyme-python | A- | glob 範圍應限定 / pepper 拒絕 read_ticket 改自實作合理 |
| Complexity / 性能 | ginger | 中-高風險（F3 P1） | 999 ticket 全掃 1052ms 浪費率 99.4% 超過 100ms 心理閾值 |
| Redundancy / DRY | bay | 中 | 3 套 active ticket 掃描 + 3 套 frontmatter 解析需統一 |

### 共識項（建立 follow-up）

| Follow-up | 優先 | 內容 |
|-----------|------|------|
| 0.18.0-W11-019 | P1 | 性能與巢狀優化（999→100ms 短路退出 / mtime sort，併解雙層 try-except）— ginger 已建 |
| 0.18.0-W11-020 | P2 | main 控制流扁平化（單一 emit point + 5 處 print 收斂）— ginger 已建 |
| 0.18.0-W11-021 | P2 | 跨 hook 統一 active ticket 解析機制 + frontmatter 解析路徑（bay + linux + thyme 共識）|
| 0.18.0-W11-022 | P3 | SKIP_PARALLEL_EVAL pair 重複 + TypedDict + ISO datetime parse |

### Phase 4 決策

**是否需重構**：否（不阻擋 complete）
**理由**：35 tests pass + 既有 20 tests 持續 GREEN；發現項雖含 P1 性能但 cold path 假設目前成立（一般輸入 < 1ms）；正確路徑是建 W11-019 follow-up 而非阻擋本 ticket complete
**Cold path 假設**：成立但脆弱，已由 W11-019 接手追蹤可預見退化點

### 結論

complete W11-004.2 並進入後續 follow-up 流程。
## Phase 4 多視角審查 — Complexity / 認知負擔 + 性能（2026-04-29）

### 審查標的
`.claude/hooks/process-skip-guard-hook.py`（311 行；新增 `get_active_in_progress_ticket` + `_should_silence_sa_review` + `_find_repo_root` + main 中 SA guard 區塊）

### 1. 認知負擔評估

| 函式 | 變數 | 分支 | 巢狀 | 行數 | 指數 | 評等 |
|------|------|------|------|------|------|------|
| `detect_skip_intent` | 3 | 3 | 3 | 12 | 9 | 優良 |
| `_find_repo_root` | 1 | 2 | 2 | 5 | 4 | 優良 |
| `get_active_in_progress_ticket` | 4 | 6 | 4 | 23 | 14 | 需重構 |
| `_should_silence_sa_review` | 3 | 4 | 1 | 10 | 8 | 可接受 |
| `main` | 5 | 8 | 3 | 60 | 16 | 必須重構（控制流串接） |

發現 1（中優）：`get_active_in_progress_ticket` 巢狀深度 4（try→for→try→if）逼近 Miller's Law 上界，雙層 try-except 模糊錯誤來源。

發現 2（中優）：`main` 60 行 + 8 分支屬 Hook 協定 boilerplate + 業務邏輯混合。每個分支末尾重複 print(json.dumps(generate_hook_output(...))) + return EXIT_SUCCESS（5 處），違反 DRY。

### 2. 性能評估（最壞情況）

實測 cold path 成本（999 ticket，macOS local FS）：
- glob 本身：4.1 ms
- glob + read_text + parse_frontmatter 全掃：1052 ms

發現 3（高優先）：函式預期回傳「最新一個 in_progress ticket」，實作上讀取並解析全部 999 個 ticket 才篩選 status。當前 in_progress 僅 6 個，浪費率 99.4%。

優化方向（不在本 ticket 範圍，建 follow-up）：
- 短路退出：sort by mtime desc，遇第一個 in_progress 即 return（< 50 ms）
- 或：靠 ticket track list --status in_progress CLI 而非自寫 glob
- 或：快取 in_progress 清單

### 3. Cold Path 假設驗證

結論：成立但脆弱。

當前流程：
1. detect_skip_intent（純記憶體比對，~0.1 ms）
2. 僅當 skip_type == "SKIP_SA_REVIEW" 才呼叫 get_active_in_progress_ticket

驗證：line 273-276 條件分支正確攔截，一般用戶輸入 hook 增加成本 < 1 ms。

脆弱點：
- 若未來新增類似 type/phase guard 規則（如 SKIP_PHASE4 也需查 ticket），cold path 邏輯複製貼上易誤觸發 hot path
- 1 秒成本對 UserPromptSubmit 是用戶可感知延遲（> 100 ms 心理閾值）

### 4. 控制流複雜度（main 加 guard 後）

main 流程：read_json → subagent_check → empty_check → detect_skip → SA_guard(cold) → emit。每個檢查點各自 print + return，造成多出口 + 重複 emit 反模式。建議統一為單一 emit point。

發現 4（低優）：5 處重複的 print + return EXIT_SUCCESS 應抽出為 helper 或扁平化為單一出口。

### 5. 結論與 Follow-up 建議

| 發現 | 優先級 | 處置 |
|------|--------|------|
| F1: get_active_in_progress_ticket 雙層 try 巢狀 | 中 | 併入 F3 重構 |
| F2: main 60 行 + 5 處 emit 重複 | 中 | 建 follow-up（控制流扁平化） |
| F3: 全掃 999 ticket → 1 秒成本 | 高 | 建 follow-up（短路 / CLI / 快取） |
| F4: 5 處 print+return 重複 | 低 | 併入 F2 |

Cold path 假設目前成立，本 ticket 不需返工；F3 為可預見的退化點（ticket 數量持續成長），應排入 v0.18.0 內或下一 wave。
### Phase 4 多視角審查 — Python 慣例 + Coupling（thyme-python-developer，2026-04-29）

#### 審查標的
- `.claude/hooks/process-skip-guard-hook.py`
- `.claude/hooks/lib/frontmatter_parser.py`（作為 process-skip-guard 新依賴的耦合對象，本次未直接修改）

#### 審查結果總覽

| 維度 | 評分 | 主要觀察 |
|------|------|---------|
| Python 慣例（type hints / docstring） | A- | 完整 type hints、繁中 docstring；少數 Optional 可改 PEP 604 union 但非必要 |
| Hook 通用模式遵循度 | A | 遵循 hook_utils 五件套 + stderr 雙通道（規則 4） |
| 與 hook_utils / frontmatter_parser 耦合 | B+ | 耦合方向正確（hook → lib），但 cold-path glob 掃描所有 ticket 屬重操作 |
| Glob pattern 健壯性 | B | 當前結構正確；對非標準目錄（archive/backup）會誤掃，未限定深度 |
| pepper 自實作 vs read_ticket 設計合理性 | A | 合理。詳見下方分析 |

#### Python 慣例審查

正面：
- 完整 return type annotations（含 `Tuple[Optional[str], Optional[dict]]`）
- module-level docstring 含 Hook 類型/觸發時機/模式定義/防護用途
- 函式 docstring 三段式（描述 / Args / Returns）
- 常數命名 SCREAMING_SNAKE（SA_GUARD_SILENCE_TYPES / SA_GUARD_IMP_SILENCE_PHASE / EXIT_SUCCESS）
- private helper 用 `_` 前綴（_find_repo_root / _should_silence_sa_review）

可改進（非阻擋）：
1. `Tuple[Optional[str], Optional[dict]]` → Python 3.11+ 可改 `tuple[str | None, dict | None]`（hook 已宣告 `requires-python = ">=3.11"`）
2. `pattern_info: dict` 缺乏 TypedDict 約束（pairs / description / full_process 三鍵契約）

#### Hook 通用模式耦合

正確使用：
- `read_json_from_stdin` / `is_subagent_environment` / `generate_hook_output` / `run_hook_safely` 全走 hook_utils 統一介面
- subagent 短路（line 260）、空輸入短路（line 268）順序正確
- stderr 雙通道（line 301）符合 quality-baseline 規則 4

無 reinvent the wheel。

#### Glob Pattern 健壯性

`root.glob("docs/work-logs/**/tickets/*.md")`：

| 維度 | 評估 |
|------|------|
| 當前結構（v0/v0.18/v0.18.0/tickets/）匹配 | 通過 |
| 非標準子目錄誤掃 | 中風險。未來 archive/ 或 backup/ 子樹會被掃進 |
| 深度限制 | 無限制（`**` 遞迴），目前 ≤ 4 層可接受 |
| 性能 | cold path（僅 SKIP_SA_REVIEW 觸發）可接受；ticket 數成長至千級需重評 |

#### pepper 自實作 parse_frontmatter 而非 read_ticket 的合理性

read_ticket 行為（frontmatter_parser.py:189-198）：強制 11 個必需欄位齊全否則 ValueError；額外讀取 body 構建完整 TicketData。

guard 場景僅需 4 欄位（status / type / current_phase / started_at），且必須對單檔解析失敗 graceful。

判定：**合理**。若強用 read_ticket：
1. 任何 frontmatter 不完整的 ticket 都會抛 ValueError，掃描雜訊大
2. 額外讀 body（line 238 extract_body）浪費 I/O，guard 不需要

封裝邊界劃得正確（read_ticket 為「完整 Ticket 契約」，parse_frontmatter 為「YAML 提取」原子介面）。

#### 耦合健康度

| 耦合對象 | 方向 | 健康度 |
|---------|------|--------|
| hook_utils | hook → utils（單向） | 正常 |
| lib.hook_messages | hook → lib（單向） | 正常 |
| lib.frontmatter_parser | hook → lib（單向，新增） | 正常，僅依賴純函式 parse_frontmatter |

無循環依賴、無跨 hook 直接 import。

#### 結論

**Phase 4 評估：A-（無需重構，無阻擋級問題）**

可選 follow-up（建議 PM 決定是否建 IMP ticket）：

1. **F4-001（中）**: glob 限定範圍 — `docs/work-logs/v*/**/tickets/*.md` 或排除 archive/backup
2. **F4-002（低）**: pattern_info 抽 TypedDict 提升型別安全
3. **F4-003（低）**: cold-path glob 掃描成本評估（ticket 數千級時的快取/CLI 呼叫策略）

依規則 5（所有發現必須追蹤），上述 3 項應建 follow-up ticket 由 PM 排序處理。
### Phase 3b GREEN 實作（thyme-python-developer，2026-04-29）

#### 修復摘要

依 pepper Phase 3a 策略 + sage Phase 2 RED 契約落地：

1. SKIP_PATTERNS 關鍵字精確化（高 FP pair 移除）：
   - SKIP_ACCEPTANCE.pairs：移除 ('直接','完成')、('不做','審查')
   - SKIP_SA_REVIEW.pairs：移除 ('不做','架構審查')（與 sage RED 測試 test_legitimate_phrase_不做架構審查改良_not_misfire 對齊；偏離 pepper 保留建議，理由：該 pair 與「不做架構審查改良」高重疊；顯式 SA 詞 ('跳過','sa')/('不需要','sa')/('不需要','審查')/('跳過','系統分析') 已涵蓋意圖）

2. 新增 type/phase guard helper（process-skip-guard-hook.py 模組級）：
   - _find_repo_root()：向上尋找含 docs/work-logs 的祖先（hook 隨 worktree/主 repo 走皆成立）
   - get_active_in_progress_ticket()：使用 pathlib.glob('docs/work-logs/**/tickets/*.md') + lib.frontmatter_parser.parse_frontmatter，回傳最新 started_at 的 in_progress ticket 之 {type, current_phase}；單檔解析失敗 skip 續找；全失敗回 None
   - _should_silence_sa_review(active_ticket)：靜音規則（DOC/ANA 全靜音；IMP+Phase 4 靜音）

3. main() guard 整合（cold path）：
   - 僅 skip_type == 'SKIP_SA_REVIEW' 時才呼叫 get_active_in_progress_ticket()
   - 例外回退至原行為（觸發提醒）
   - 靜音時輸出基本 JSON + 結構化 logger.info

#### 設計決策對齊

| pepper 決策 | 落地狀態 |
|------------|---------|
| D1 直接 glob + parse_frontmatter（非 subprocess） | 採用 |
| D2 guard 放 main() 而非 detect_skip_intent | 採用 |
| D3 cold path（僅 SA_REVIEW 觸發查詢） | 採用，TestColdPathPerformance GREEN |
| D4 失敗回退語意 | 採用 |
| D5 guard 規則邏輯 | 採用 |

<!-- PC-093-exempt: baseline-gated:quality-baseline 規則 2 強制 Phase 4 評估產出，本節為已完成評估記錄非延後項 -->
#### Phase 4 評估

| 指標 | 值 |
|------|---|
| 認知負擔指數（最大函式） | ~5（get_active_in_progress_ticket：3 變數 + 2 分支 + 2 巢狀 + glob/parse 依賴） |
| 函式長度 | 全部 <= 25 行 |
| 巢狀深度 | <= 3 |
| DRY | _should_silence_sa_review helper 抽取 guard 規則 |
| 命名 | 動詞前綴（_find/_should/get），語意自說明 |

結論：無需重構，程式碼品質 A。

#### 不在本 ticket 範圍

- W11-017（phase-completion-gate-hook 假陽性）已分流為獨立 follow-up
- 其他 SKIP_PATTERNS 類型調整
## Phase 3a 實作策略（pepper-test-implementer，2026-04-29）

### 介面契約確認（沿用 sage Phase 2 設計）

| 元素 | 簽名 | 行為 |
|------|------|------|
| `get_active_in_progress_ticket()` | `() -> Optional[dict]` | 回傳 `{"type": str, "current_phase": str}` 或 `None` |
| `detect_skip_intent` 整合點 | 不變 | 仍回傳 `(skip_type, pattern_info)` |
| guard 整合位置 | `main()` 內（見決策 D2） | 偵測到 skip_type 後、輸出提醒前 |

### 技術決策

#### D1：active ticket 來源 — 直接掃描 ticket 目錄（非 subprocess）

選擇：使用 `pathlib.Path.glob` 掃 `docs/work-logs/**/tickets/*.md`，以 `lib.frontmatter_parser.parse_frontmatter` 解析。

| 方案 | 評估 | 採用 |
|------|------|------|
| subprocess `ticket track list --status in_progress` | 啟動成本數百 ms，hook 級不可接受；subagent 環境可能無 PATH | 否 |
| 直接 glob + parse_frontmatter | 無外部依賴、ms 級、與 hook_utils 同層 | 是 |
| `lib.frontmatter_parser.read_ticket` | 強制 ticket_id/version/action/target/agent 欄位，新 schema ticket 會拋 ValueError | 否 |

掃描根目錄推導：以 `Path(__file__).resolve().parents` 向上尋找含 `docs/work-logs` 的祖先（hook 隨 worktree/主 repo 走皆成立）。

實際 ticket 目錄結構：`docs/work-logs/v0/v0.18/v0.18.0/tickets/*.md`（多層 v 子目錄）。glob pattern 使用 `docs/work-logs/**/tickets/*.md`（recursive）涵蓋多層情境。

#### D2：guard 整合位置 — `main()` 而非 `detect_skip_intent`

`detect_skip_intent` 為純函式（input → output），混入 I/O 違反 SRP；`main()` 已是 I/O 層。guard 在 `main()` 中於偵測到 skip_type 後、生成提醒前插入。

#### D3：cold path 性能保證

`get_active_in_progress_ticket()` 唯一呼叫點在 `if skip_type and pattern_info:` 分支內，且僅當 `skip_type == "SKIP_SA_REVIEW"`。無 skip intent 的 prompt 完全不觸發查詢（測試 `TestColdPathPerformance` 守護此契約）。

#### D4：失敗回退語意

| 情境 | 行為 |
|------|------|
| 回傳 None（無 in_progress 或全部讀取失敗） | 維持原行為，觸發提醒 |
| 拋例外 | main() try/except，例外視同 None，觸發提醒 |
| 單一 ticket frontmatter 解析失敗 | 內部 skip 該檔，續找下一個 |
| 多個 in_progress（理論不應發生） | 取 started_at 最新者 |

#### D5：guard 規則邏輯

```
若 skip_type == "SKIP_SA_REVIEW":
    active = get_active_in_progress_ticket()  # cold path
    若 active 存在:
        type = active.get("type"); phase = active.get("current_phase")
        若 type 屬於 {"DOC", "ANA"}: 靜音
        若 type == "IMP" 且 phase == "Phase 4": 靜音
    否則: 照舊觸發
否則: 照舊觸發（其他 skip 模式不受 guard 影響）
```

### 虛擬碼（Phase 3b 落地參考）

```python
def _find_repo_root() -> Optional[Path]:
    for parent in Path(__file__).resolve().parents:
        if (parent / "docs" / "work-logs").exists():
            return parent
    return None

def get_active_in_progress_ticket() -> Optional[dict]:
    try:
        root = _find_repo_root()
        if root is None:
            return None
        in_progress = []
        for path in root.glob("docs/work-logs/**/tickets/*.md"):
            try:
                fm = parse_frontmatter(path.read_text(encoding="utf-8"))
                if fm.get("status") == "in_progress":
                    in_progress.append((fm.get("started_at", ""), fm))
            except Exception:
                continue
        if not in_progress:
            return None
        in_progress.sort(key=lambda x: x[0], reverse=True)
        fm = in_progress[0][1]
        return {"type": fm.get("type"), "current_phase": fm.get("current_phase")}
    except Exception:
        return None

# main() 內，於 detect_skip_intent 後：
if skip_type == "SKIP_SA_REVIEW":
    try:
        active = get_active_in_progress_ticket()
    except Exception:
        active = None
    if active:
        t, p = active.get("type"), active.get("current_phase")
        if t in ("DOC", "ANA") or (t == "IMP" and p == "Phase 4"):
            logger.info(f"SKIP_SA_REVIEW 因 type={t}/phase={p} 靜音")
            print(json.dumps(generate_hook_output("UserPromptSubmit"), ensure_ascii=False, indent=2))
            return EXIT_SUCCESS
```

### SKIP_PATTERNS 修改清單

| 模式 | 操作 | pair |
|------|------|------|
| SKIP_ACCEPTANCE | 移除 | `("直接", "完成")` |
| SKIP_ACCEPTANCE | 移除（目前不存在但須確保不加入） | `("不做", "審查")` |
| SKIP_SA_REVIEW | 移除（目前不存在但須確保不加入） | `("不做", "審查")` |

註：sage 的 RED 斷言是「不存在」即通過；需確保結構檢查成立。SKIP_SA_REVIEW 現有 pairs 不含 `("不做","審查")`，但保留 `("不做","架構審查")` 作為顯式 SA 詞。

### 檔案影響

- `.claude/hooks/process-skip-guard-hook.py`：
  - 新增 import `parse_frontmatter`（from `lib.frontmatter_parser`）
  - 新增 `_find_repo_root()` 內部 helper
  - 新增模組級 `get_active_in_progress_ticket()`
  - SKIP_PATTERNS["SKIP_ACCEPTANCE"]["pairs"] 移除 `("直接","完成")`
  - `main()` 內加入 SKIP_SA_REVIEW guard 邏輯
- `.claude/hooks/lib/ask_user_question_reminders.py`：不需修改

### 給 thyme 的實作清單

1. import `from lib.frontmatter_parser import parse_frontmatter`
2. 加 `_find_repo_root()` helper
3. 加 `get_active_in_progress_ticket()`（依 D1/D4/D5）
4. SKIP_ACCEPTANCE.pairs 刪除 `("直接","完成")`（其餘已不存在的 pair 確認結構正確）
5. main() 在偵測到 skip_type 後、輸出提醒前插入 SKIP_SA_REVIEW guard（虛擬碼第二段）
6. 執行 `pytest .claude/hooks/tests/test_process_skip_guard_type_phase_guard.py .claude/hooks/tests/test_process_skip_guard_hook.py -v` 確認 22+ 測試 GREEN（15 新 + 既有 SKIP_ACCEPTANCE 直接/完成 相關測試需檢視是否需同步調整）
7. complete 前填 Test Results

### 不在本 ticket 範圍

- W11-017（phase-completion-gate-hook 假陽性）
- 其他 SKIP_PATTERNS 調整
- read_ticket 強欄位要求的重構
## Phase 2 RED 測試設計（sage-test-architect）

### 產出
- `.claude/hooks/tests/test_process_skip_guard_type_phase_guard.py`（新檔，RED 骨架）

### 測試覆蓋
| 類別 | AC | 測試案例 |
|------|----|---------|
| TestKeywordPrecisionRemoval | AC4 | 6 個（移除 pair 斷言 3 + 保留斷言 1 + 合法句不誤觸 2） |
| TestTypePhaseGuardExclusion | AC1/AC2/AC3/AC5 | 6 個（helper 存在、IMP+P4、DOC、ANA、IMP+P1 反例、其他模式不受影響） |
| TestFallbackBehavior | 失敗回退 | 2 個（無 active ticket、read 例外） |
| TestColdPathPerformance | 性能假設 | 1 個（無 skip intent 不查 ticket） |

合計 15 個新測試。執行結果：8 fail（預期 RED）+ 7 pass（已被既有實作滿足的反向/保留斷言）。

### Phase 3b 實作介面契約
新增 `process_skip_guard_hook.get_active_in_progress_ticket() -> Optional[dict]`：
- 掃描 `docs/work-logs/v*/tickets/*.md`，找 status=in_progress ticket
- 回傳 `{"type": ..., "current_phase": ...}`
- 找不到或讀取失敗回傳 None / 拋例外（hook 內 try/except 處理）

`detect_skip_intent` 或 `main` 加入 guard：
- 偵測到 SKIP_SA_REVIEW 後呼叫 `get_active_in_progress_ticket`
- type ∈ {DOC, ANA} → 靜音
- type == IMP AND current_phase == 'Phase 4' → 靜音
- 其他情況或回退 → 維持原行為

### 關鍵字精確化
SKIP_PATTERNS 修改：
- SKIP_ACCEPTANCE.pairs 移除 ('直接','完成')、('不做','審查')
- SKIP_SA_REVIEW.pairs 移除 ('不做','審查')

### AC6 既有測試
test_process_skip_guard_hook.py 全 20 個測試持續 GREEN（已驗證）。

### 限制與假設
- 用 create=True 在 RED 階段允許 get_active_in_progress_ticket 屬性尚不存在（monkeypatch 動態建立）
- Phase 3b 實作後 create=True 不影響行為，monkeypatch 會覆蓋真實實作
- active ticket 來源策略由 Phase 3b 選擇（subprocess ticket track list vs 直接掃 tickets dir），測試以 dict 介面解耦
## Test Results

<!-- To be filled by executing agent -->

---

### Phase 3b 驗證結果（2026-04-29）

**指令**：`python3 -m pytest .claude/hooks/tests/test_process_skip_guard_type_phase_guard.py .claude/hooks/tests/test_process_skip_guard_hook.py -v`

**結果**：35 passed in 0.07s

| 測試檔 | 通過 | 說明 |
|--------|------|------|
| test_process_skip_guard_type_phase_guard.py | 15/15 | sage Phase 2 新測試全 GREEN |
| test_process_skip_guard_hook.py | 20/20 | 既有測試持續 GREEN（AC6） |

**AC 對應**：

| AC | 測試 | 狀態 |
|----|------|------|
| AC1 派發前查詢 type/phase | test_ac1_hook_queries_active_ticket_helper | GREEN |
| AC2 IMP+Phase 4 排除 | test_ac2_imp_phase4_excludes_sa_reminder + test_ac2_imp_phase1_does_not_exclude_sa_reminder（負向） | GREEN |
| AC3 DOC/ANA 全面排除 | test_ac3_doc_type_excludes_sa_reminder + test_ac3_ana_type_excludes_sa_reminder | GREEN |
| AC4 關鍵字精確化 | TestKeywordPrecisionRemoval（6 個） | GREEN |
| AC5 三場景排除 | AC2/AC3 涵蓋 | GREEN |
| AC6 既有測試 GREEN | test_process_skip_guard_hook.py 20 個 | GREEN |

**回歸驗證**：
- TestColdPathPerformance.test_no_skip_intent_does_not_query_active_ticket：cold path 契約守住，無 skip intent 不觸發查詢
- TestFallbackBehavior（無 active / 例外）：失敗回退維持原行為
- test_imp_phase4_does_not_affect_non_sa_skip_patterns：guard 僅影響 SKIP_SA_REVIEW，其他模式照常觸發
## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending