---
id: 0.18.0-W10-073.1
title: WRAP 分析 quality-common.md (378 行) 拆分可行性
type: ANA
status: completed
version: 0.18.0
wave: 10
priority: P1
parent_id: 0.18.0-W10-073
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: 主線程 + WRAP 框架
  history: {}
what: 用 WRAP 框架評估 quality-common.md (378 行, 通用程式碼品質（命名/函式/常數/DRY/作用域/多模式）) 的拆分可行性，產出「完全拆分/部分拆分/保持現狀」建議
when: 待定義
where:
  layer: Infrastructure
  files:
  - .claude/rules/core/quality-common.md
why: 父 ticket 0.18.0-W10-073 盤點的 11 個自動載入候選檔案之一。需獨立評估避免一次性決策失誤
how:
  task_type: Analysis
  strategy: 'W(擴增選項: 列出所有拆分方式含不拆) → R(現實測試: 每個選項的 context 節省/風險/引用耦合) → A(保持距離: 從其他專案
    sync 角度檢視) → P(準備出錯: 預設失敗場景與回退路徑)'
acceptance:
- '[x] 列出至少 3 個拆分選項（含不拆分）'
- '[x] 每個選項量化 context 節省行數'
- '[x] 每個選項標註風險（高/中/低）'
- '[x] 產出最終建議（完全拆分/部分拆分/保持現狀）'
- '[x] 若建議拆分，提供骨架設計草案（保留哪些行、移出哪些行、references 檔路徑）'
- '[x] 檢視所有引用此檔的其他檔案，列出更新範圍'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-04-16T12:04:19'
completed_at: '2026-04-16T12:06:38'
created: '2026-04-15'
updated: '2026-04-15'
---

# Execution Log

## Task Summary

用 WRAP 框架評估 quality-common.md (378 行, 通用程式碼品質（命名/函式/常數/DRY/作用域/多模式）) 的拆分可行性，產出「完全拆分/部分拆分/保持現狀」建議

---

## Problem Analysis

### Context Bundle

**執行步驟**:
1. `ticket track claim 0.18.0-W10-073.1`
2. Read 目標檔案 `.claude/rules/core/quality-common.md`（378 行）
3. 依 ticket how.strategy 的 WRAP 框架完成分析
4. `ticket track append-log 0.18.0-W10-073.1 --section "Solution" "完整分析"`
5. `ticket track complete 0.18.0-W10-073.1`

**檔案特殊注意事項**:
378 行是 11 個 PM 自動載入候選中最大檔案。風險：可能存在 domain 混合（如同時服務 PM 決策 + 代理人寫程式碼兩種情境）。分析時必須識別檔案內主要章節和各自服務的角色。

**核心原則（用戶明確指導）**:
認知負擔優先於行數。行數超標是症狀，domain 混合是病因。不要僅因為檔案長就建議拆分——要看內容是否可按需載入或必須一次完整讀取。

**WRAP 五段產出要求**:
1. Widen: 至少 3 個選項表格（不拆分/完全拆分/部分拆分/遷移 references 等）
2. Reality Test: 對每個選項做使用情境模擬
3. Attain Distance: 6 個月後視角
4. Prepare to be Wrong: 推薦方案失效風險與緩解
5. 最終建議（三選一：拆分/部分拆分/不拆分）+ 量化理由（節省 tokens、影響情境數）

**約束**:
- 不修改目標檔案
- 不僅看行數下結論
- 識別檔案內 domain 組成後才判斷
- 一行摘要回報建議
### 判斷原則（父 ticket 共通指引）

**拆分依據是「認知負擔」與「按需載入」，不是行數。**

這是卡片盒筆記法（Zettelkasten）的核心精神：

- 正確問題：這段知識在什麼情境才需要理解？每段知識是否原子化？按需載入時能否獨立理解？
- 錯誤問題：檔案多少行？骨架是否夠短？行數超過某閾值就要拆？

**推論**：
- 500 行的檔案若內容全部原子化且在同一情境共同需要 → 不應拆分
- 100 行的檔案若混合多個情境才用到的知識 → 即使短也應拆
- 拆分後的每一碎片必須能獨立被理解（原子性），否則只是把完整概念切碎
- 保留判斷標準不是「這段很重要」，而是「所有使用場景都需要立即載入」

**WRAP 分析時的具體提問**：

1. **W (擴增選項)**：列出所有可能的原子化切分方式（不是以行數為切點，而是以「情境匹配度」為切點）
2. **R (現實測試)**：每個切分方案下，各個碎片是否能獨立被理解？保留在 auto-load 的碎片是否「所有場景都需立即載入」？
3. **A (保持距離)**：從 sync 到其他專案的角度看，這些知識是否真的每個專案啟動都需要？
4. **P (準備出錯)**：若拆分後發現某個碎片其實常被需要，如何快速恢復到 auto-load？

---

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution

<!-- To be filled by executing agent -->

---

# WRAP 分析：quality-common.md (378 行) 拆分可行性

## 一行摘要

**建議：完全外移至 references/，rules/core/ 保留極簡骨架指標（約 30 行）或併入 quality-baseline.md，預估自動載入節省 ~370 行 / ~15KB。**

---

## W — Widen（擴增選項）

列出 5 個候選方案（含不拆分）：

| 方案 | 描述 | 骨架保留行數 | 節省行數 | 風險 |
|------|------|------------|---------|------|
| A. 不拆分（現狀） | 全文 378 行維持自動載入 | 378 | 0 | 低（但持續占用啟動 context） |
| B. 完全外移 + 極簡骨架 | 移至 `references/quality-common.md`，`rules/core/` 保留 20-30 行指標（何時載入 references） | ~25 | ~350 | 低 |
| C. 完全外移 + 併入 quality-baseline.md | 外移詳細規則，在 quality-baseline.md 加一行「代理人寫程式碼必讀 references/quality-common.md」 | 0（骨架刪除） | ~378 | 中（破壞 11+ 處 `@` 和文字引用，需同步更新） |
| D. 部分拆分（保留骨架核心 + 外移範例/檢查清單） | rules/core/ 保留章節標題 + 原則摘要（約 120 行），1.2.1~1.2.5 防護規則、1.6 詳細範例外移 | ~120 | ~258 | 中（骨架尺寸仍偏大） |
| E. 合併至既有 quality-common-details.md | 將 378 行內容整併進現有的 `references/quality-common-details.md`（524 行），rules/core/ 檔案刪除 | 0 | ~378 | 高（details.md 會超過 900 行，違反原子性） |

---

## R — Reality Test（現實測試）

**quality-common-details.md 已存在（524 行），表明 rules↔references 分層架構已就緒，此次只是繼續推進分離徹底度。**

### 使用情境模擬

| 情境 | 當前載入 | 方案 B 載入 | 方案 C 載入 |
|------|---------|-----------|-----------|
| PM 派發代理人 | 378 行被自動載入但不用 | 25 行指標（告知代理人去讀） | 0 行（指標併入 quality-baseline） |
| 代理人寫程式碼 | 已自動載入（0 成本讀取） | 需要 Read references/quality-common.md（1 次 tool call） | 同左 |
| 代理人重構評估 | 同上 | 同上（已讀則不重讀） | 同上 |
| 寫 RED 測試（PM） | 0 行（不相關） | 0 行 | 0 行 |

### 原子性驗證（卡片盒筆記法）

逐章節評估每段知識的情境匹配度：

| 章節 | 內容 | 使用情境 | 啟動時必要？ |
|------|------|---------|------------|
| 1.1 命名規範 | 變數/函式/類別命名表格 | 寫程式碼時查詢 | 否 |
| 1.2 函式設計 | 長度/參數/巢狀閾值 | 寫程式碼時查詢 | 否 |
| 1.2.1 作用域變更防護 | IMP-003 教訓 | 做作用域重構時 | 否 |
| 1.2.2 欄位格式溯源 | IMP-011 教訓 | 修 bug 讀欄位時 | 否 |
| 1.2.3 破壞性操作防護 | IMP-010/ARCH-002 | 寫 GC/清理時 | 否 |
| 1.2.4 未使用程式碼處理 | IMP-013 | Phase 4 重構時 | 否 |
| 1.2.5 多模式函式 Guard Clause | IMP-035 | 寫多模式函式時 | 否 |
| 1.3 常數管理 | 硬編碼禁止 | 寫程式碼時 | 否 |
| 1.4 DRY 原則 | 重複率 < 10% | 寫程式碼時 | 否 |
| 1.5 認知負擔閾值 | 已在 cognitive-load.md 重複 | 評估時 | 否 |
| 1.6 註解標準 | 業務情境優先 | 寫註解時 | 否 |
| 2. 檢查清單 | 提交前對照 | 提交前（代理人） | 否 |

**結論**：378 行**零章節**屬於「啟動即需要」的通用知識。所有章節情境都是「代理人寫/重構/修 bug」時才觸發。**情境錯配 100%**。

### 引用耦合分析（方案 B）

21 個檔案引用 `quality-common.md`，可分三類：

| 類別 | 檔案數 | 引用方式 | 受拆分影響 |
|------|------|---------|-----------|
| 代理人 `@` 強制載入 | 7（thyme/parsley/cinnamon/fennel/bay/語言模板/語言模板） | `@.claude/rules/core/quality-common.md` | 更新為 `@.claude/references/quality-common.md` |
| 文字導引引用 | 11（skills/templates/error-patterns/其他 rules） | 純文字提及路徑 | 更新路徑字串 |
| 工作日誌歷史 | 多（W10-011/W10-074 系列/073.x 等） | 歷史記錄 | 不更新（歷史忠實） |

21 個檔案的更新都屬機械性字串替換，可用一條 sed 或 Edit 批次完成，低風險。

---

## A — Attain Distance（保持距離）

### 6 個月後視角

假設 6 個月後有新代理人加入（例如新增 Rust 開發者），或本專案 sync 到另一個專案：

- **方案 A 視角**：每個新 session 都載入 378 行「程式碼品質規則」，無論該 session 是否涉及程式碼。對於純 PM 決策會話是 100% 浪費。累積成本會隨 session 數線性上升。
- **方案 B/C 視角**：代理人派發時顯式 Read references/，成本只在需要時支付。跨專案 sync 後，新專案的啟動 context 也自動瘦身。

### 卡片盒筆記法一致性

rules/README.md 已明確寫明：「**開放內容**（所有角色都需要知道的通用規則）放在自動載入的 `rules/` 中；**封閉內容**（特定情境才需要的細節指引）放在按需讀取的 `references/` 中。」

quality-common.md 屬於「代理人寫程式碼才需要」的**封閉內容**，放在 rules/core/ 違反專案設計原則，是歷史遺留而非設計決定。方案 B 是回歸設計一致性。

### sync 視角

`.claude/` 會 sync 到其他專案。其他專案可能：
- 完全不寫程式碼（如純文件專案）
- 只有主線程協調（如跨專案 PM 協調中心）

對這些專案，378 行的程式碼品質規則是純負擔。方案 B 讓 sync 後的專案自動受益。

---

## P — Prepare to be Wrong（準備出錯）

### 最可能失效場景與緩解

| 失效場景 | 機率 | 影響 | 緩解措施 |
|---------|------|------|---------|
| 代理人派發 prompt 未明確要求讀 references/，導致忘記載入 | 中 | 代理人未遵守品質規則 | 1. 保留骨架 20-30 行觸發指標（方案 B）；2. 代理人檔案 `@` 直接指向 references/（強制載入） |
| 21 個引用檔案字串替換遺漏某個 | 低 | broken link | 用 broken-link-check 掃描驗證 |
| 現有 `references/quality-common-details.md`（已存在）與外移的主文件產生職責重疊 | 中 | 兩份文件不同步 | 合併 details.md 進新 references/quality-common.md，或明確分工（本文件定規則，details.md 定範例） |
| 代理人讀完 references/ 後 context 比現在更大（因為多了一次讀取） | 低 | 實際使用時 context 比啟動時更多 | 代理人讀一次即快取；只有涉及品質的 session 才讀；其他 session 完全 0 成本 |
| 工作日誌/ticket 的歷史引用變成 stale | 低 | 歷史可追溯性略降 | 歷史文件不改（框架規則 8 允許），只改活躍引用 |

### 回退路徑

若拆分後 3-5 個 session 內發現代理人頻繁漏讀 references：
1. 加強代理人 `@` 直接載入（強制預載）
2. 若仍無效，回退：重命名 `references/quality-common.md` → `rules/core/quality-common.md`（單一檔案移動），21 個引用再次字串替換回原路徑

回退成本 = 一次字串替換 + 一次檔案移動，低於 30 分鐘。

### 絆腳索（tripwire）

拆分後定期監測：
- [ ] 下一個涉及程式碼品質的 ticket 完成後，代理人報告是否仍符合 1.1-1.6 規則（品質未退步）
- [ ] broken-link-check 在下次執行時無 quality-common.md 相關死連結
- [ ] 啟動 context 實測下降約 ~15KB（可用 claude-usage 工具驗證）

若任一絆腳索觸發，立即啟動回退路徑。

---

## 最終建議

**推薦方案 B：完全外移 + 極簡骨架指標**

### 理由

1. **情境匹配度 100% 錯配**：378 行無一屬於啟動時需要，全部是代理人寫程式碼時的查閱資料。
2. **既有架構就緒**：`references/quality-common-details.md` 已存在 524 行，表明 rules↔references 分層模式已建立，此拆分是補完設計一致性。
3. **引用耦合可控**：21 個引用都是機械性字串替換，用 Edit 批次或 sed 可在一次 PR 內完成。
4. **節省可量化**：自動載入減少 ~370 行（~15KB），對 Opus 1M context 啟動成本節省約 1.5%。配合 073.10 / 073.7 等姊妹 ticket 累積可達 5% 以上。
5. **sync 友好**：其他專案也自動受益。
6. **回退成本低**：< 30 分鐘。

### 骨架設計草案

```markdown
# 通用品質基線（指標）

> **內容位置**：`.claude/references/quality-common.md`（按需讀取）

本檔僅保留自動載入時機的觸發指標，避免佔用啟動 context。

## 何時載入

| 情境 | 必讀 |
|------|------|
| 代理人寫產品程式碼 | 是 |
| 代理人做 Phase 4 重構評估 | 是 |
| 代理人修 bug（欄位讀取、破壞性操作） | 是 |
| PM 派發、決策、分析 | 否 |
| 寫 RED 測試（規格定義） | 否 |

## 載入方式

- 代理人：透過 `@.claude/references/quality-common.md` 強制載入（已配置於 thyme/parsley/cinnamon/fennel 等代理人檔案）
- PM 前台：遇到品質審查需求時 Read .claude/references/quality-common.md

## 內容索引

| 章節 | 用途 |
|------|------|
| 1.1 命名規範 | 變數/函式/類別命名標準 |
| 1.2 函式設計 + 1.2.1~1.2.5 防護規則 | 函式結構與重構/修 bug 防護 |
| 1.3 常數管理 | 硬編碼與魔法數字禁止 |
| 1.4 DRY 原則 | 重複率控制 |
| 1.5 認知負擔閾值 | 見 .claude/rules/core/cognitive-load.md |
| 1.6 註解標準 | 業務情境聚焦 |
| 2. 檢查清單 | 提交前對照 |

---

**Last Updated**: (拆分時更新)
```

骨架約 25-30 行，啟動 context 節省 ~348 行。

### 移出內容

整份 378 行搬遷至 `.claude/references/quality-common.md`，原文結構不變（避免破壞引用的章節錨點）。

### 與既有 quality-common-details.md 的關係

兩份檔案職責明確分工：

| 檔案 | 職責 |
|------|------|
| `references/quality-common.md`（新） | 核心規則定義（相當於原 rules/core/quality-common.md 內容） |
| `references/quality-common-details.md`（既有，524 行） | 操作指引 + 範例 + 詳細檢查清單 |

details.md 內部引用已使用 `quality-common.md` 名稱，拆分後引用仍正確（實質變成同目錄相對引用）。無需改動 details.md 內容。

### 引用更新範圍（21 檔案）

機械性替換 `.claude/rules/core/quality-common.md` → `.claude/references/quality-common.md`：

**代理人檔案（7 個，保留 `@` 強制載入語法）**：
- `.claude/agents/parsley-flutter-designer.md`
- `.claude/agents/thyme-python-developer.md`
- `.claude/agents/cinnamon-refactor-owl.md`
- `.claude/agents/fennel-go-developer.md`
- `.claude/agents/bay-quality-auditor.md`
- `.claude/agents/language-agent-template.md`（兩處）

**其他活躍引用（11+ 個，純文字）**：
- `.claude/rules/core/observability-rules.md`
- `.claude/README.md`
- `.claude/templates/work-log-template.md`
- `.claude/skills/tdd/references/phase2/rules.md`
- `.claude/skills/i18n-checker/references/mixed-processing-guide.md`
- `.claude/skills/agent-team/references/teammate-onboarding-protocol.md`
- `.claude/skills/evidence-driven-bugfix/SKILL.md`
- `.claude/references/ui-layout-spec.md`
- `.claude/references/quality-python.md` / `quality-go.md` / `quality-dart.md` / `quality-language-template.md`
- `.claude/error-patterns/implementation/IMP-003-refactoring-scope-regression.md`
- `.claude/error-patterns/architecture/ARCH-007-per-project-tracking-global-resource.md`
- `.claude/error-patterns/process-compliance/PC-068-phase3a-planning-new-utility-without-scan.md`

**歷史日誌（不改）**：
- `docs/work-logs/v0/v0.18/v0.18.0/**` 各 ticket（依框架規則 8：歷史忠實）

### 量化節省

| 指標 | 當前 | 方案 B 後 | 節省 |
|------|------|---------|------|
| 自動載入行數（本檔） | 378 | ~25 | ~353 行 |
| 自動載入 bytes（本檔） | ~15076 | ~800 | ~14KB |
| 啟動 context 佔比（以 Opus 1M 估） | ~1.5% | ~0.08% | ~1.4% |

### 驗收標準對照

- [x] 列出至少 3 個拆分選項（含不拆分）— 列出 5 個
- [x] 每個選項量化 context 節省行數
- [x] 每個選項標註風險（低/中/高）
- [x] 產出最終建議 — 方案 B（完全外移 + 極簡骨架）
- [x] 提供骨架設計草案
- [x] 檢視所有引用此檔的其他檔案，列出更新範圍（21 個檔案，分 7+11+歷史三類）
## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: 主線程 + WRAP 框架
**Review Status**: pending