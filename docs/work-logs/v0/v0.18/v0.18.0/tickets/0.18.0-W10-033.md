---
id: 0.18.0-W10-033
title: 放寬 主線程允許編輯 .gitignore（Hook 允許清單和保護分支豁免）
type: IMP
status: completed
version: 0.18.0
wave: 10
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第四層:PM 流程改善
  final_decision: 放行 .gitignore 為 repo 層級配置例外
  rationale: 本 session 用戶明確反饋 + 規則 6 框架改善優先
who: 主線程
what: 放寬 主線程允許編輯 .gitignore（Hook 允許清單和保護分支豁免）
when: 2026-04-13 用戶即時回饋後主線程執行
where:
  layer: 待定義
  files:
  - .claude/hooks/main-thread-edit-restriction-hook.py
  - .claude/hooks/branch-verify-hook.py
why: 本 session 實戰發現：主線程嘗試補 .claude/scheduled_tasks.lock 到 .gitignore（由 W10-032 追蹤），被兩個
  Hook 攔截——main-thread-edit-restriction-hook 允許清單只含 .claude/、docs/、CLAUDE.md；branch-verify-hook
  保護分支豁免清單同。但 .gitignore 是 repo-wide 配置，修改頻率低但每次都需要即時處理（如 runtime lock 檔、新工具 artifact），強制走
  feature 分支或代理人路徑過重。放行 .gitignore 可提升主線程自洽性而風險極低（.gitignore 錯誤修改不會污染程式碼）。用戶明確提出此規則應放寬。
how:
  task_type: Implementation
  strategy: 1) main-thread-edit-restriction-hook.py 允許清單新增 .gitignore 2) branch-verify-hook.py
    豁免路徑清單新增 .gitignore 3) 更新 hook docstring 和錯誤訊息中列出的允許路徑 4) 加測試確認 .gitignore 放行、其他
    root 檔案仍阻擋 5) 若有 references/ 文件列了清單需同步 6) commit 到 feature 分支 + merge
acceptance:
- '[x] main-thread-edit-restriction-hook 放行 .gitignore'
- '[x] branch-verify-hook 保護分支放行 .gitignore'
- '[x] 兩 Hook 錯誤訊息同步更新允許清單'
- '[x] 測試覆蓋 .gitignore 放行 + 其他 root 檔（如 package.json）仍阻擋'
- '[x] references/ 中列允許清單的文件同步更新'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-13T17:55:55'
completed_at: '2026-04-13T17:59:06'
created: '2026-04-13'
updated: '2026-04-13'
---

# Execution Log

## Task Summary

放寬 主線程允許編輯 .gitignore（Hook 允許清單和保護分支豁免）

---

## Problem Analysis

## Dogfood 簡化 WRAP 三問（第五次）

### W（其他做法？）
1. 僅 ALLOWED_PATTERNS +1 字串（最輕）
2. 允許清單 + 測試 + 錯誤訊息 + references（目前方案，完整）
3. 抽出 root 允許清單為獨立常數檔（過重）
選 (2) 平衡。

### A（機會成本？）
30-45 min。擠壓 W10-029/030。

### P（最可能失敗原因？）
改動破壞既有 hook（誤放行 package.json 等 root 檔）30%。防護：RED 測試驗證邊界（放 .gitignore + 擋 package.json/secrets.env/tsconfig.json）。

## 實作記錄（TDD）

### RED 階段
- 新增 .claude/hooks/tests/test_gitignore_main_thread_edit.py（6 個測試）
- 初跑：3 FAIL（要修的）+ 3 PASS（邊界已滿足）

### GREEN 階段
- .claude/hooks/lib/path_permission.py：ALLOWED_PATTERNS 新增 `^\.gitignore$`
- .claude/hooks/branch-verify-hook.py：exempt_exact 新增 ".gitignore"
- .claude/hooks/lib/hook_messages.py：EDIT_BLOCKED_DEFAULT_DENY 訊息補 .gitignore 條目

### 驗證
- 6 個測試全通過
- 實戰驗證：在 main 分支直接編輯 .gitignore 成功（補 .claude/scheduled_tasks.lock 到忽略清單，同時解決 W10-032）

### 意外收穫
W10-032 透過 W10-033 修完後立刻實戰解決（gitignore 補 scheduled_tasks.lock）。W10-032 可標記為 superseded_by W10-033 或直接 close。
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending