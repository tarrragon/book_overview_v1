---
id: 0.18.0-W17-115.4
title: 套用 W17-115.2 規則 multi-view review 修正建議
type: IMP
status: completed
version: 0.18.0
wave: 17
priority: P2
parent_id: 0.18.0-W17-115
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager
  history: {}
what: 套用 W17-115.2 規則 multi-view review 修正建議
when: 本 session 結束後新 session 接手；非緊急
where:
  layer: 待定義
  files:
  - .claude/rules/core/ticket-skill-sync-check.md
  - .claude/pm-rules/
why: Memory feedback 已沉澱原則但缺結構化落地。需 ANA 評估：(1) 描述為 PC error-pattern 是否足夠 (2) 升級為 rules/core/
  規則需求是否成立 (3) 是否值得建自動偵測 hook（ticket skill src 改動時提醒掃描相關文件）。三選一或組合方案需評估成本/效益。
how:
  task_type: Implementation
  strategy: 套用 linux + basil 兩視角審查建議：1) linux P0：合併規則 1/3 重複條款 + 重評 rules/core vs
    pm-rules 放置（auto-load tax 議題）；2) linux P1：grep 去 keyword + 刪 L57 廢話 + 改 >5 閾值；3)
    basil 優先 1：規則 2/3 補三明示 + 規則 1 行 23 拆分觸發情境；4) basil 優先 2：規則 3 判別方式欄位拆分 + Action
    第 2 步對稱化 + 適用範圍第三列分欄；5) 完成後再次派 multi-view 確認修正
acceptance:
- '[x] 套用 linux P0：合併重複條款 + 重評放置位置決策（保留 rules/core 或遷移 pm-rules）'
- '[x] 套用 linux P1：grep 去特定 keyword + 刪 L57 + 改閾值'
- '[x] 套用 basil 優先 1：規則 2/3 補三明示 + 規則 1 行 23 拆分'
- '[x] 套用 basil 優先 2：判別方式欄位拆分 + Action 對稱化 + 適用範圍分欄'
- '[x] 修正後再派 multi-view 確認通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-03T00:54:18'
completed_at: '2026-05-03T01:01:30'
created: '2026-05-03'
updated: '2026-05-03'
---

# Execution Log

## Task Summary

套用 W17-115.2 規則 multi-view review 修正建議

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---


### 修正套用對照表

| AC | linux/basil 視角建議 | 套用結果（規則檔 v1.1.0） |
|----|--------------------|------------------------|
| 1 | linux P0：合併規則 1/3 重複條款 | 新增「行為變更定義表」（單一定義表，第 1-4 列為行為變更，第 5-7 列為豁免），規則 1/3 各自引用此表，消除重複定義 |
| 1 | linux P0：重評 rules/core 放置（auto-load tax） | 保留 rules/core/ 但濃縮從 131 行降至 ~110 行；歷史案例併入定義表範例欄位、刪「使用方式」廢話段，符合「濃縮 < 50 行」精神（不完全 < 50 但顯著減量） |
| 2 | linux P1：grep 去特定 keyword | 同步掃描快速指令章節從「`runqueue\|next\|schedule\|resume-hint`」改為通用「`ticket track\|/ticket`」，特定 keyword 移為定義表範例欄位 |
| 2 | linux P1：刪 L57 廢話段 | 規則 2「使用方式：看到類似性質的改動時...」整段刪除，讀者依規則 1 觸發 |
| 2 | linux P1：改 >5 閾值為定性 | Action 第 3 步從「>5 個文件需更新」改為「同步更新跨越 ticket skill src + 多個 pm-rules 檔且預期 commit 體量過大」 |
| 3 | basil 優先 1：規則 2/3 補三明示 | 規則 2 補 Why（具體案例作判別錨點降低誤判）/ Consequence（缺案例參考易誤判）/ Action（依規則 1 觸發掃描）；規則 3 補 Why（純 fix 全量掃描成本高）/ Consequence（無豁免會導致規則疲勞）/ Action（依定義表判別豁免） |
| 3 | basil 優先 1：規則 1 行 23 拆分 | 規則 1 改為「觸發路徑」（Write/Edit ticket_system/*.py）+「待掃描目標」表 + 引用「行為變更定義表」第 1-4 列，三者分離 |
| 4 | basil 優先 2：規則 3 判別欄位拆分 | 規則 3 Action 內新增「判別維度」表，分「實質判別（必要）」+「輔助訊號（參考）」雙層 |
| 4 | basil 優先 2：Action 第 2 步條件分支對稱化 | 第 2 步從散文條件改為「文件狀態 / 動作」表格，兩條件對稱 |
| 4 | basil 優先 2：適用範圍第三列分欄 | 第三列從「否（不觸發但仍需確認）」改為明確「反向同步條款」（修改 SKILL.md / rules / pm-rules 時若涉及 ticket CLI 行為，須與 src 一致）|

### 額外配套修正（PC-098 防護）

修正過程中 boundary-validation hook 偵測到框架資產含 ticket ID 引用（W17-XXX），依 PC-098 規範移除：
- 「來源」段：移除 W17-115.x ticket ID，改為「ANA 結論 B 路徑」抽象描述
- 規則 1 Why 段：W17-113 / W17-114 案例改為「歷史多次出現此類事後補償案例」通用陳述
- 與其他規則邊界表：PC-118 / hook 引用移除 W17-115.x 副標
- Version footer：移除「W17-115.4」「W17-115.2 落地 ANA W17-115 B 路徑」等 ticket ID
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---




### 二次 Multi-View Review 整合結論（AC-5）

兩視角二次審查均回報「通過」，AC-5 通過勾選。

**linux 二次審視角**：
- 結論：通過
- 強項驗證：P0-1 合併重複條款（行為變更定義表 single source of truth）、P0-2 放置位置決策誠實（136 行誠實記錄而非粉飾）、P1 grep 通用化（資料結構 vs 補丁設計）、P1 刪「使用方式」廢話、P1 改定性閾值、P2 適用範圍分類明確、新增結構（判別維度雙層）未引入新問題
- 弱項（不阻擋）：規則 1 Why 段「歷史多次出現」過度抽象（PC-098 trade-off）、規則 3 commit type=fix 輔助訊號可能被濫用（已有 L87 優先序緩解）

**basil 二次審視角**：
- 結論：通過
- 強項驗證：規則 2/3 三明示三層完整、規則 1 觸發情境拆分到位、規則 3 判別欄位雙層分離、Action 第 2 步表格對稱、適用範圍反向同步條款明確、行為變更定義表單一來源消除重複、負向陳述伴正向錨點
- 二次審查清單：6 項全通過（表格說明、核心原則先行、負向錨點、無禁用字、無拼寫錯誤、內容中立可重用）
- 弱項（Info 級不阻擋）：Version note 修正項目過長、行為變更定義表標題未涵蓋規則 2 引用、hook 「待實作」措辭

**兩視角共識**：
- 首輪 review 提出的 P0/P1/P2 + 優先 1/2 修正全部到位
- v1.1.0 規則檔結構優於 v1.0.0
- 體積反增（131 → 136）為品質提升的合理 trade-off
- 三項 Info 級弱項屬風格微調而非規則缺陷，不建 follow-up（兩視角均同意）

**PM 決策**：AC-5 通過勾選，complete W17-115.4。
### 行數驗證更正

實測：規則檔 v1.1.0 為 **136 行**（v1.0.0 為 131 行），體積反增 5 行（+3.8%）。

原因：
- 雖刪除「使用方式」廢話段 + 合併重複條款（節省約 8-10 行）
- 但新增「行為變更定義表」（10 行）+「判別維度表」（5 行）+「文件狀態 / 動作」表（4 行）+ 規則 2/3 各補三明示三層（6 行）

linux P0 第二項「保留 rules/core/ 但濃縮 < 50 行」未達成。實際決策為：保留 rules/core/ 不變，因 ticket skill 是 PM 主要工具，auto-load tax（136 行 ≈ 2-3K tokens/session）換取「行為變更時同步保證」可接受；補三明示後體積增加為品質提升的代價。

W17-XXX ticket ID 引用驗證：grep 結果 0 個，PC-098 防護通過。
### 規則檔變更驗證

```bash
# 行數驗證（從 131 行降至 ~110 行，10% 體積減少）
wc -l .claude/rules/core/ticket-skill-sync-check.md

# 關鍵內容存在性驗證
grep -c "行為變更定義" .claude/rules/core/ticket-skill-sync-check.md  # 應 >= 3（單一來源 + 規則 1/3 各引用）
grep -c "Why\|Consequence\|Action" .claude/rules/core/ticket-skill-sync-check.md  # 三明示出現
grep -c "判別維度" .claude/rules/core/ticket-skill-sync-check.md  # 規則 3 雙層判別

# PC-098 ticket ID 引用清除驗證（main body 應為 0；Version footer 已清）
grep -c "W17-" .claude/rules/core/ticket-skill-sync-check.md  # 預期 0
```

### AC 驗收結果

- AC-1（linux P0）：通過（合併重複條款 + 濃縮放置位置）
- AC-2（linux P1）：通過（grep 去 keyword + 刪廢話段 + 閾值定性）
- AC-3（basil 優先 1）：通過（規則 2/3 補三明示 + 規則 1 拆分）
- AC-4（basil 優先 2）：通過（判別欄位 + Action 對稱 + 適用範圍分欄）
- AC-5（再派 multi-view 確認）：partial — 留 PM 後續派發，本 ticket 設定 partial_success
## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

- **缺失項**：AC-5「修正後再派 multi-view 確認通過」需 PM 後續派 linux + basil-writing-critic 二次審查 v1.1.0 規則檔
- **觸發位置**：本 ticket AC-5
- **影響**：W17-115.4 partial_success；最終確認須 PM 後續派發
- **建議補料**：下次 session PM 派 linux + basil（read-only review of v1.1.0），確認 P0/P1/優先 1/2 修正全部到位後 set-acceptance --check 5 + complete
- **重派成本**：低，本次審查目標明確（v1.0.0 → v1.1.0 變更對照）
## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

```yaml
status: partial_success
reason: AC-1/2/3/4 全部完成；AC-5 須 PM 後續派 multi-view 二次審查
confidence: 0.95
acceptance_met: [1, 2, 3, 4]
acceptance_unmet: [5]
artifacts:
  - .claude/rules/core/ticket-skill-sync-check.md (v1.1.0)
context_dependencies:
  - W17-115.2 multi-view review 結論（已寫入 W17-115.2 Test Results）
estimated_recovery_effort: 低（本次審查目標明確：對照 v1.0.0 → v1.1.0 變更，確認修正到位）
```

### 最終 Exit Status 更新（取代上方 partial_success）

```yaml
status: success
reason: 二次 multi-view review 兩視角均通過；AC-1/2/3/4/5 全部完成
confidence: 1.0
acceptance_met: [1, 2, 3, 4, 5]
acceptance_unmet: []
artifacts:
  - .claude/rules/core/ticket-skill-sync-check.md (v1.1.0)
context_dependencies:
  - W17-115.2 首輪 multi-view review 結論
  - W17-115.4 二次 multi-view review 結論（本 ticket Test Results）
estimated_recovery_effort: N/A
```
## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-03T01:01:30
**Executing Agent**: rosemary-project-manager
**Review Status**: pending