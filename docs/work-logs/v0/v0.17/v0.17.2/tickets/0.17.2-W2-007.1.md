---
id: 0.17.2-W2-007.1
title: 測試 6 狀態篩選 UI（readingStatus badge + 篩選 bar 切換）
type: IMP
status: completed
version: 0.17.2
wave: 2
priority: P2
parent_id: 0.17.2-W2-007
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: pending
  history: {}
what: 測試 6 狀態篩選 UI（readingStatus badge + 篩選 bar 切換）
when: 待定義
where:
  layer: 待定義
  files:
  - tests/unit/overview/overview-page-controller.test.js
  - src/overview/overview-page-controller.js
why: readingStatus 取代舊 status 是 v2 model 的核心變更，需驗證 UI 行為
how:
  task_type: Implementation
  strategy: 待定義
acceptance:
- '[x] 6 狀態篩選 bar 渲染正確（unread/reading/finished/queued/abandoned/reference）'
- '[x] 篩選切換觸發 applyCurrentFilter 正確'
- '[x] 回到全部時清除狀態篩選'
- '[x] readingStatus badge 顯示正確'
- '[x] 4-5 個 test cases 全部 RED 後 GREEN'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-04-07T17:04:09'
completed_at: '2026-04-07T17:16:28'
created: '2026-04-06'
updated: '2026-04-06'
---

# Execution Log

## Task Summary

測試 6 狀態篩選 UI（readingStatus badge + 篩選 bar 切換）

---

## Problem Analysis

### PM 前台分析結果

**現有 applyCurrentFilter (L379-410)**: 只有文字搜尋 + 排序，無狀態篩選。需在搜尋後、排序前加入 readingStatus filter 步驟。

**READING_STATUS 列舉** (BookSchemaV2.js L14-21):
unread, reading, finished, queued, abandoned, reference
（注意：Ticket 原始驗收條件用了錯誤的狀態名，已修正）

**_formatBookRowData (L912-924)**: 目前用 book.status（舊欄位），需改用 book.readingStatus

**initializeElements (L133-160)**: 用 elementMap + document.getElementById 批量取元素。新增 statusFilterBar 需加入 elementMap。

**setupEventListeners (L169-245)**: 搜尋、排序、匯出按鈕事件在此設置。statusFilter bar 事件也應在此設置。

**DOM 結構**: 測試的 beforeEach 建立 JSDOM (L33-118)，需在搜尋區域後新增 statusFilterBar 容器。

**現有測試模式**: 每個 test 內部 require 控制器、new 實例、操作、assert。不共用 controller 實例。
### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

<!-- To be filled by executing agent -->

---

實作與測試已完成。setStatusFilter(L382) 設定篩選狀態並觸發 applyCurrentFilter(L395)。applyCurrentFilter 已含 readingStatus 篩選邏輯：statusFilter 非 null 時過濾 readingStatus。_formatBookRowData(L933) 使用 book.readingStatus 輸出 reading-status-badge span（含 data-status 屬性）。constructor(L106) 初始化 statusFilter=null。5 個測試全部 GREEN。
### 實作摘要

1. constructor 新增 this.statusFilter = null
2. 新增 setStatusFilter(status) 方法：設定/清除 readingStatus 篩選
3. applyCurrentFilter() 管線擴展：狀態篩選 -> 文字搜尋 -> 排序
4. _formatBookRowData() status 欄位改為 readingStatus badge（span.reading-status-badge[data-status]），向後相容舊 book.status

### 修改檔案
- src/overview/overview-page-controller.js（+32 行）
- tests/unit/overview/overview-page-controller.test.js（+80 行）
## Test Results

<!-- To be filled by executing agent -->

---

overview-page-controller.test.js: 26 passed, 0 failed (含 5 個 readingStatus 測試)。全域測試: 4900 passed, 21 failed (5 suites), 41 skipped — 21 個失敗為既存問題（search-engine, operation-result 等），與本 Ticket 無關。
overview-page-controller.test.js: 26/26 通過（21 既有 + 5 新增）

新增測試:
1. 應該在初始化時設定 statusFilter 為 null（全部）
2. 應該能按 readingStatus 篩選書籍（單一狀態）
3. 應該能清除狀態篩選回到全部
4. 應該能同時套用文字搜尋和狀態篩選
5. renderBooksTable 應該為每本書顯示 readingStatus badge

全域測試無新增失敗（既有 flaky 測試不受影響）。
## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending