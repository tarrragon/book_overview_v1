---
id: 0.19.0-W1-097
title: 修復 workflows 系列 3 檔 timing assertion 違規 (~20 行 Rule 1 + 1 行 Rule 4)
type: IMP
status: completed
version: 0.19.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W1-091
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:接收任務
  final_decision: spawn IMP 修復 W1-091 盤點 Batch 3 (workflows Rule 1+4 混合 3 檔)
  rationale: test-assertion-design-rules-規則1/4 + quality-baseline-規則5
who:
  current: thyme-extension-engineer
  history: {}
what: 修復 workflows 系列 3 檔 timing assertion 違規 (~20 行 Rule 1 + 1 行 Rule 4)
when: W1-091 盤點完成後 (Batch 3 workflows 系列)
where:
  layer: 待定義
  files:
  - tests/integration/workflows/daily-usage-workflow.test.js
  - tests/integration/workflows/cross-device-sync-workflow.test.js
  - tests/integration/workflows/new-user-onboarding.test.js
why: W1-091 盤點發現 workflows 整合測試 3 檔含 Rule 1 + Rule 4 違規 (daily-usage 9+1 + cross-device-sync
  5 + new-user-onboarding 7)。daily-usage 含 Rule 4 快取計時比較需重新設計斷言改用命中率/參考比較。
how:
  task_type: Implementation
  strategy: 1) Rule 1 違規依規則 1 處理 (移除/遷 perf) 2) Rule 4 違規依規則 4 重新設計 (改用 getCacheHitRate
    / cache.size / toBe 參考比較) 3) 逐檔 npm test 通過 4) 全套件回歸 5) commit 並 complete
acceptance:
- '[x] 3 檔內 ~21 行 Rule 1/4 違規依規則處理完成'
- '[x] 3 檔個別 npm test 通過'
- '[x] npm test 全套件回歸通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-27T15:12:32'
completed_at: '2026-05-27T15:24:49'
created: '2026-05-27'
updated: '2026-05-27'
---

# Execution Log

## Task Summary

修復 workflows 系列 3 檔 timing assertion 違規 (~20 行 Rule 1 + 1 行 Rule 4)

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### WRAP 三問（認領 Checkpoint）

**W (Widen) — 候選方案**：

1. **混合策略**（推薦）：Rule 1 違規依規則 1 處理（沿用 W1-095/096 模式：移除計時門檻保留功能驗證）；Rule 4 違規依規則 4 重新設計（getCacheHitRate / cache.size / toBe 參考比較取代計時比較）
2. 整 describe 遷 perf：若某檔整個 describe 為效能測試，整段移至 tests/perf/（規則 2）
3. mock 豁免註解：斷言對象為 mock 固定回傳值時，加註解標明

預設方案 1，逐檔審視。Rule 4 必須重新設計而非機械刪除。

**A (Attain distance) — 機會成本**：

W1-091 系列 spawn batch 3（P2）。W1-095/096 已積累 Rule 1 修復經驗，可直接套用；Rule 4 重新設計為新增認知負擔，但 1 行案例不影響整體進度。

**P (Prepare to be wrong) — 最可能失敗原因**：

daily-usage-workflow.test.js 的 Rule 4 違規（快取計時比較）若重新設計時誤判 cache 模組 API（如 getCacheHitRate / cache.size 不存在），可能需在 cache 層加 API 或用替代方案。
防護：改寫前先 grep 確認 cache 模組現有 API；無對應 API 即在 NeedsContext 回報，禁機械改寫。

### PC-163 適用注意

依 PC-163 Approach A：PM 已在 worktree cwd 完成 claim + 本次 append-log，agent 接手後 worktree 的 ticket md 與 main repo 一致；避免 W1-096 雙邊偏離模式重現。


### 違規分類（W1-091 預估 vs 實際逐行分類）

W1-091 預估「9 行 Rule 1 + 1 行 Rule 4」（daily-usage）+「5 行 Rule 1」（cross-device-sync）+「7 行 Rule 1」（new-user-onboarding）共 22 行。實際逐行分類後依規則 1 例外（mock 固定值、業務不變式、資源預算非計時、資料體積、比率/計數）區分如下：

| 檔案 | 行號 | 斷言 | 來源 | 分類 | 處理 |
|------|------|------|------|------|------|
| daily-usage-workflow.test.js | 134 | `book.progress <= 100` | 業務不變式 (進度百分比上限) | 非計時 | 保留 |
| daily-usage-workflow.test.js | 231 | `loadingTime < 500` | `Date.now()` 差值 | **真實計時** | **移除** |
| daily-usage-workflow.test.js | 445 | `popupOpenTime < 500` | `measureOperation` 真實計時 | **真實計時** | **移除** |
| daily-usage-workflow.test.js | 446 | `syncTime < 8000` | 同上 | **真實計時** | **移除** |
| daily-usage-workflow.test.js | 447 | `overviewTime < 1000` | 同上 | **真實計時** | **移除** |
| daily-usage-workflow.test.js | 452 | `memoryUsage < 30MB` | 記憶體差值 | 資源預算非計時 | 保留 |
| daily-usage-workflow.test.js | 455 | `cpu.usage < 0.8` | CPU 百分比 | 資源預算非計時 | 保留 |
| daily-usage-workflow.test.js | 507 | `avgExecutionTime < 10000` | `Date.now()` 差值平均 | **真實計時** | **移除** |
| daily-usage-workflow.test.js | 514 | `lastRoundTime < firstRoundTime * 1.5` | `Date.now()` 相對計時比較 | **Rule 1 (非 Rule 4)** | **移除** |
| daily-usage-workflow.test.js | 521 | `maxMemoryUsage < 50MB` | 記憶體上限 | 資源預算非計時 | 保留 |
| new-user-onboarding.test.js | 181 | `avgInterval < 2000` | `timestamp` 差值平均 | **真實計時** | **移除** |
| new-user-onboarding.test.js | 384 | `executionTime < 15000` | `Date.now()` 差值 | **真實計時** | **移除** |
| new-user-onboarding.test.js | 385 | `memoryUsage < 50MB` | 記憶體差值 | 資源預算非計時 | 保留 |
| new-user-onboarding.test.js | 390 | `responseTime < 100` | `measureButtonResponseTime` 真實計時 | **真實計時** | **移除** |
| new-user-onboarding.test.js | 417 | `popupLoadTime < 500` | UXMonitor 真實計時 | **真實計時** | **移除** |
| new-user-onboarding.test.js | 418 | `buttonResponseTime < 100` | 同上 | **真實計時** | **移除** |
| new-user-onboarding.test.js | 419 | `frameDrops < 5` | 幀數計數 | 非計時 (UX 品質指標) | 保留 |
| cross-device-sync-workflow.test.js | 242 | `exportTime < 15000` | `Date.now()` 差值 | **真實計時** | **移除** |
| cross-device-sync-workflow.test.js | 247 | `fileSize < 20MB` | 檔案體積 | 資料體積非計時 | 保留 |
| cross-device-sync-workflow.test.js | 270 | `importTime < 20000` | `Date.now()` 差值 | **真實計時** | **移除** |
| cross-device-sync-workflow.test.js | 274 | `totalTime < 35000` | 同上 | **真實計時** | **移除** |
| cross-device-sync-workflow.test.js | 278 | `memoryUsage < 100MB` | 記憶體差值 | 資源預算非計時 | 保留 |
| cross-device-sync-workflow.test.js | 421 | `totalProgress toBeCloseTo(v, 1)` | `numDigits=1` | Rule 3 合規 (<= 2) | 保留 |
| cross-device-sync-workflow.test.js | 888 | `avgInterval < 2000` | `timestamp` 差值平均 | **真實計時** | **移除** |

實際真實 Rule 1 違規共 **15 行**（與 W1-091 預估 22 行有出入，原因：盤點未逐行區分非計時斷言；資源預算 6 行、業務不變式 1 行、UX 品質 1 行、Rule 3 合規 1 行均屬保留）。

### Rule 4 重判定（重要設計決策）

W1-091 將 daily-usage:514 `lastRoundTime < firstRoundTime * 1.5` 標為「Rule 4 違規」並要求「重新設計斷言改用命中率/參考比較」。實際讀取上下文：

- 斷言所在測試：「應該支援多次連續使用且保持穩定」（多輪 daily workflow 執行的穩定性驗證）
- 斷言對象：`performanceHistory[i].executionTime = roundEnd - roundStart` 即 `Date.now()` 差值
- 業務語意：驗證效能不退化（最後一輪不比第一輪慢太多）

依 `.claude/rules/core/test-assertion-design-rules.md` Rule 4 適用範圍表：
- 適用：「所有驗證快取效能的 test case」
- **不適用：「非快取相關的計時測試」**

本斷言**非快取驗證**（沒有 cache 模組參與），屬「非快取相關的計時測試」，依適用範圍表回落 Rule 1 處理（移除）。`cache.getHitRate / cache.size / toBe 參考比較` 對此情境不適用——cache 模組未介入；強行套用會引入虛假抽象。

故本案不需 NeedsContext 回報 cache 模組 API 缺失（PM 預設方案 P 的防護條款不適用），改採 Rule 1 移除 + 設計決策註解保留審計軌跡。

### 相關 Error Pattern

無精確匹配；屬 W1-017 / W1-018 / W1-090 / W1-094 / W1-095 / W1-096 同家族（計時硬門檻系統性問題），規則層已固化於 `.claude/rules/core/test-assertion-design-rules.md`。

### 來源驗證

所有 15 行真實計時違規均以 `Date.now()` / `timestamp` / `measureOperation` / `measureButtonResponseTime` / UXMonitor 真實計時量測值為斷言對象，符合規則 1「真實計時」識別條件，不適用 mock 例外條款。接手者可獨立 `rg "toBeLessThan|toBeCloseTo"` 驗證修復後僅剩非計時斷言。
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 派發執行指引（PM Context）

**工作目錄**: `/Users/mac-eric/project/book_overview_v1-0.19.0-W1-097`（worktree，分支 `feat/0.19.0-W1-097`）

**必讀**:

1. 本 ticket（Problem Analysis 含 WRAP 三問 + PC-163 注意）
2. `docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W1-095.md` / `0.19.0-W1-096.md`（同質化前例，Rule 1 模式可直接套用）
3. `.claude/rules/core/test-assertion-design-rules.md` 規則 1（禁主套件絕對計時門檻）+ 規則 2（遷 perf）+ **規則 4（快取加速用命中率非計時比較）**

**目標檔案**:

- `tests/integration/workflows/daily-usage-workflow.test.js`（9 行 Rule 1 + 1 行 Rule 4）
- `tests/integration/workflows/cross-device-sync-workflow.test.js`（5 行 Rule 1）
- `tests/integration/workflows/new-user-onboarding.test.js`（7 行 Rule 1）

**執行步驟**:

1. 逐檔 `rg "toBeLessThan|toBeCloseTo" <file>` 列出 hits
2. **Rule 1 處理**（沿用 W1-095/096 模式）：每行讀上下文 5-10 行，判斷斷言來源（performance.now/Date.now/hrtime 差值 → 移除保留功能驗證；mock 固定值 → 豁免註解；業務不變式 → 加註解保留）
3. **Rule 4 處理**（daily-usage-workflow.test.js 1 行）：改寫前先 grep cache 模組 API：
   - 優先：`cache.getHitRate() > 0`
   - 次選：`cache.size > 0`
   - 強保證：`expect(secondResult).toBe(firstResult)`（toBe 參考比較）
   - 若 cache 模組無對應 API → NeedsContext 回報，禁機械改寫
4. 改完每檔執行 `npm test -- <file>` 確認通過
5. 全套件回歸 `npm test`
6. commit 到 `feat/0.19.0-W1-097`（branch-verify hook 擋 main）

**完成要求**:

1. 填寫本 ticket 的 Problem Analysis（違規分類表）、Solution（修復摘要 + Rule 4 設計決策）、Test Results（npm test 結果）
2. `ticket track check-acceptance --all 0.19.0-W1-097`
3. `ticket track complete 0.19.0-W1-097`

若無法通過 acceptance 或業務不變式無法判別 → NeedsContext 回報，partial_success / needs_context 退出，**不 complete**。

**Commit Policy**:

- target branch: `feat/0.19.0-W1-097`
- message: `test(0.19.0-W1-097): 修復 workflows 系列 3 檔 timing assertion 違規 (Rule 1 + Rule 4)`
- 可拆 3 commit（每檔一個）或合 1 commit，依認知負擔判斷


### 修復策略

採 W1-090 / W1-094 / W1-095 / W1-096 同一修復模式：移除真實計時 pass-fail 斷言，保留功能正確性驗證；不做 perf 遷移（workflows 整合測試已有 mock-based 功能驗證設計，非真實效能基準目標）。

| 處理方式 | 行數 | 依據 |
|---------|------|------|
| 移除計時斷言（保留功能驗證） | 15 行 (Date.now() / timestamp / measureOperation / measureButtonResponseTime / UXMonitor 真實計時) | 規則 1 真實計時差值 |
| 保留（業務不變式） | 1 行 (daily-usage:134 progress <= 100) | 進度百分比上限 |
| 保留（資源預算非計時） | 6 行 (memoryUsage x 4 + cpu.usage + maxMemoryUsage) | 記憶體/CPU 資源邊界檢查 |
| 保留（資料體積非計時） | 1 行 (cross-device-sync:247 fileSize < 20MB) | 檔案大小驗證 |
| 保留（UX 品質指標非計時） | 1 行 (new-user-onboarding:419 frameDrops < 5) | 幀數計數 |
| 保留（Rule 3 合規） | 1 行 (cross-device-sync:421 toBeCloseTo numDigits=1) | numDigits=1 ≤ 2 |

### 修改摘要

對 3 個 workflows 整合測試檔執行 8 處 Edit：

#### daily-usage-workflow.test.js（4 處 Edit）

1. **行 227-232**（步驟 1 載入回饋）：移除 `loadingStart = Date.now()` / `loadingTime` / `expect(loadingTime).toBeLessThan(500)` 計時邏輯，保留 `popupState.extractButtonEnabled === true` 按鈕就緒功能驗證
2. **行 444-455**（日常使用效能驗證）：移除 `popupOpenTime / syncTime / overviewTime` 三個 `toBeLessThan` 計時門檻，保留 `memoryUsage / cpu.usage` 資源預算斷言並加註「非計時」說明
3. **行 501-521**（多輪操作穩定性）：移除 `avgExecutionTime < 10000` 平均計時門檻 + 移除 `lastRoundTime < firstRoundTime * 1.5` 相對計時比較（**Rule 4 重判定為 Rule 1**，詳 Problem Analysis），刪除 `firstRoundTime / lastRoundTime` 局部變數，保留 `maxMemoryUsage < 50MB` 與後續資料一致性檢查

#### new-user-onboarding.test.js（3 處 Edit）

1. **行 173-182**（進度更新頻率）：移除 `timeIntervals` 陣列計算與 `avgInterval < 2000` 計時門檻，刪除整段計時邏輯（功能驗證已由上方 `finalProgress.completed === true` 覆蓋）
2. **行 384-391**（首次提取效能基準）：移除 `executionTime < 15000` + `responseTime < 100` 計時門檻與 `measureButtonResponseTime` 量測，保留 `memoryUsage < 50MB` 資源預算
3. **行 416-421**（流暢 UX 體驗）：移除 `popupLoadTime < 500` + `buttonResponseTime < 100` UXMonitor 計時量測，保留 `frameDrops < 5` 幀數計數與 `smoothnessScore > 0.9` 流暢度比率

#### cross-device-sync-workflow.test.js（3 處 Edit）

1. **行 241-248**（大量資料匯出）：移除 `exportTime < 15000` 計時門檻，保留 `exportResult.success` + `fileSize` 範圍驗證
2. **行 267-278**（大量資料匯入）：移除 `importTime < 20000` + `totalTime < 35000` 兩個計時門檻，保留 `importResult.success` + `importedCount === 1500` 功能驗證 + `memoryUsage < 100MB` 資源預算
3. **行 885-893**（增量同步進度更新間隔）：移除 `avgInterval < 2000` 計時門檻，保留 `importIntervals` 計算邏輯（重新命名變數為 `_avgInterval` 標示 unused）以維持上下文完整性

### 設計決策

1. **Rule 4 重判定**：daily-usage:514 的 `lastRoundTime < firstRoundTime * 1.5` 經實際讀碼後確認**非 cache 驗證**（無 cache 模組介入），屬「非快取相關的計時測試」，依 Rule 4 適用範圍表回落 Rule 1 處理。不需 NeedsContext 回報 cache API 缺失。修改處保留 4 行設計決策註解供審計
2. **資源預算非計時保留**：本批 6 行 `memoryUsage / cpu.usage` 斷言屬資源邊界檢查（記憶體/CPU 百分比），非計時量測；保留並加註「資源預算，非計時」說明，符合 W1-095 / W1-096 同類處理慣例
3. **註解保留**：所有移除點加 `W1-097 Rule 1: 移除 X` 註解標示審計軌跡（與 W1-095 / W1-096 慣例一致）

### 不在本 ticket 範圍

- `tests/integration/workflows/` 以外其他檔案的 timing assertion（屬 W1-091 其他 batch IMP 範圍）
- Rule 2 perf 遷移（本批所有違規均為「整合測試混入計時驗證」，不適合整段遷 perf）
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 個檔執行結果

| 檔案 | 指令 | 結果 |
|------|------|------|
| daily-usage-workflow.test.js | `npx jest tests/integration/workflows/daily-usage-workflow.test.js` | PASS — 12 tests passed (70.36s) |
| new-user-onboarding.test.js | `npx jest tests/integration/workflows/new-user-onboarding.test.js` | PASS — 12 tests passed (20.76s) |
| cross-device-sync-workflow.test.js | `npx jest tests/integration/workflows/cross-device-sync-workflow.test.js` | PASS — 13 tests passed (23.80s) |

### 全套件回歸結果

```
npm test
Test Suites: 2 skipped, 219 passed, 219 of 221 total
Tests:       41 skipped, 5226 passed, 5267 total
Snapshots:   0 total
Time:        135.96s
```

無新增失敗，無新增 skipped；本次修復不影響任何其他測試。

### 修復後 timing assertion 殘留驗證

```
$ rg -n "toBeLessThan|toBeCloseTo" tests/integration/workflows/daily-usage-workflow.test.js
134:        expect(book.progress).toBeLessThanOrEqual(100)
448:      expect(memoryUsage).toBeLessThan(30 * 1024 * 1024) // 記憶體增長<30MB（資源預算，非計時）
451:      expect(performanceEnd.cpu.usage).toBeLessThan(0.8) // CPU使用<80%（資源預算，非計時）
510:      expect(maxMemoryUsage).toBeLessThan(50 * 1024 * 1024) // 單輪記憶體<50MB（資源預算，非計時）

$ rg -n "toBeLessThan|toBeCloseTo" tests/integration/workflows/new-user-onboarding.test.js
379:      expect(memoryUsage).toBeLessThan(50 * 1024 * 1024) // 記憶體增長<50MB（資源預算，非計時）
411:      expect(progressSmoothness.frameDrops).toBeLessThan(5) // 掉幀<5次（幀數計數，非計時）

$ rg -n "toBeLessThan|toBeCloseTo" tests/integration/workflows/cross-device-sync-workflow.test.js
248:      expect(fileSize).toBeLessThan(20 * 1024 * 1024) // 檔案<20MB（資料體積，非計時）
276:      expect(memoryUsage).toBeLessThan(100 * 1024 * 1024) // 記憶體增長<100MB（資源預算，非計時）
419:      expect(syncedDigest.totalProgress).toBeCloseTo(originalDigest.totalProgress, 1)
```

殘留 9 行斷言均屬規則 1/3 例外（業務不變式、資源預算、資料體積、UX 計數、Rule 3 合規 numDigits=1）；零真實 Date.now() / timestamp 計時門檻殘留。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-27T15:24:49
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W1-091 | chars: 565 -->

### Task Reference
- 0.19.0-W1-091 what: 盤點 W1-018 跨檔 timing assertion 違規漏網檔 (PM 預掃 10+ 候選檔)

### Rationale Chain
- 0.19.0-W1-091 why: W1-018 原盤點 5 檔已修復，W1-090 接手預掃發現 popup-background-integration.test.js 單檔即有 12+ 違規未列入 (漏網)；rg toBeLessThan 排除 .backup 後另有 10+ 候選檔未經審視，可能含更多未發現違規。需完整盤點建立 baseline 並逐檔判斷處理方式，避免 W1-018 二次漏網。

### Related Files
- tests/integration/performance/performance-system-integration.test.simplified.js  # from 0.19.0-W1-091
- tests/integration/cross-module-error-propagation.test.js  # from 0.19.0-W1-091
- tests/integration/cross-module/background-content-integration.test.js  # from 0.19.0-W1-091
- tests/integration/event-system-v2-performance-stability.test.js  # from 0.19.0-W1-091
- tests/integration/chrome-apis/content-script-injection.test.js  # from 0.19.0-W1-091
  ... (truncated, see source ticket)