---
id: 0.19.0-W3-034.1
title: 實驗 bgIsolation none + subagent .claude/ Edit 行為受控實驗
type: IMP
status: completed
version: 0.19.0
wave: 3
priority: P2
parent_id: 0.19.0-W3-034
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.19.0-W3-034.4
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager（前台實驗）或 thyme-python-developer
  history: {}
what: 實驗 bgIsolation none + subagent .claude/ Edit 行為受控實驗
when: W3-034 中期策略落地時
where:
  layer: Infrastructure
  files:
  - .claude/error-patterns/process-compliance/PC-137-*
  - .claude/pm-rules/parallel-dispatch.md
why: W3-034 識別 ARCH-015 假設未驗證：bgIsolation none 下 subagent 在主 repo cwd 操作，.claude/
  Edit deny 是否消失取決於 deny 根因（綁 worktree cwd 還是 subagent 身份）
how:
  task_type: Implementation
  strategy: 設定 bgIsolation none → 派發 subagent 嘗試 Edit .claude/ → 觀察 deny 是否觸發 → 對照
    PC-137 假設 → 更新 ARCH-015 結論
acceptance:
- '[x] 完成 bgIsolation none × subagent × .claude/ Edit 對照實驗'
- '[x] 記錄 PC-137 deny 觸發條件'
- '[x] 更新 ARCH-015 / PC-137 結論（confirm or refute）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-26T21:23:01'
completed_at: '2026-05-26T22:04:46'
created: '2026-05-21'
updated: '2026-05-21'
---

# Execution Log

## Task Summary

實驗 bgIsolation none + subagent .claude/ Edit 行為受控實驗

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

parent W3-034 已完成 WRAP 評估，識別 ARCH-015 / PC-137 防護的關鍵未知：subagent 對 `.claude/` Edit 觸發 deny 的根因綁定何種維度。兩個假設：

| 假設 | 內容 | 若成立的影響 |
|------|------|------------|
| A | deny 綁定 subagent cwd（worktree cwd ≠ 主 repo cwd 時觸發） | bgIsolation: none 下 subagent 在主 repo cwd 操作，deny 可能解除 |
| B | deny 綁定 subagent 身份（任何 subagent 對 .claude/ Edit 均受限） | bgIsolation: none 不改變 deny 行為 |

W17-108/109 既有資料：冷快取 + 單一 subagent 在主 repo cwd 下對 `.claude/` Edit 可 success（PC-115 feedback memory），支持假設 A 但範圍受限（單一 subagent，未驗證並行情境）。

### 影響範圍

| 受影響資產 | 影響類型 |
|----------|---------|
| `.claude/error-patterns/process-compliance/PC-137-*` | 防護策略可能簡化（若假設 A 成立） |
| `.claude/pm-rules/parallel-dispatch.md` | 並行 .claude/ ≤ 2 規則可能解除 |
| `.claude/pm-rules/worktree-operations.md` | ARCH-015 條款可能改寫為條件式 |
| settings.json `worktree.bgIsolation` | 實驗中需切換設定 |

### 相關 Error Pattern

- PC-137：並行 3+ subagent 對 `.claude/` Edit 觸發 deny（7/7 命中 vs 0-2 並行 0/18）
- ARCH-015：subagent `.claude/` hardcoded 寫入保護（根因未完全確認，PC-115 調查中）
- PC-115：W17-108/109 三階段對照已否證 Hypothesis K（冷快取 + 主 repo cwd 下 subagent .claude/ Edit success）

### WRAP 三問（PM 認領 checkpoint）

**W（Widen）— 實驗設計候選方案**：

| 方案 | 內容 | 取捨 |
|------|------|------|
| A. 全條件矩陣 | 4 cell：bgIsolation(none/worktree) × subagent 數(1/3+) | 完整對照但需 4 次派發、context 成本高 |
| B. 焦點實驗（採用） | 1 cell：bgIsolation: none × 單一 subagent .claude/ Edit | 直接驗證假設 A 的關鍵反例（若 deny 仍觸發 → 否證假設 A） |
| C. 並行焦點 | bgIsolation: none × 3 subagents 並行 .claude/ Edit | 驗證 PC-137 在 none 模式下的行為，但需先確認單一情境 |

**選擇 B**：W17-108/109 已驗證 worktree 模式 × 單一 subagent 可 success；本實驗只需驗證「切到 bgIsolation: none 後行為是否一致或更寬鬆」。若假設 A 成立，none 模式下單一 subagent 應同樣 success（且可能進一步排除 PC-137）。若假設 B 成立，none 模式下單一 subagent 仍會 deny（直接否證假設 A）。

**A（Attain distance）— 機會成本**：

| 不做本實驗的代價 | 做本實驗的代價 |
|---------------|--------------|
| ARCH-015 / PC-137 結論卡在「假設未驗證」狀態無限期 | settings.json 變更可能影響當前 session 的其他派發行為（中斷其他並行 ticket） |
| W3-034.2 / W3-034.3 spawn 內容無法定稿（blockedBy 本 ticket） | 變更後若 deny 仍觸發 → 確認假設 B，spawn-2/3 簡化為「不建議使用 bgIsolation: none」 |

**P（Prepare to be wrong）— 失敗預想**：

| 失敗情境 | 對應防護 |
|---------|---------|
| settings.json 變更未生效（需重啟 session） | 實驗前先確認 Claude Code 是否支援 hot reload；若否，標註「需獨立 session 執行」並暫停本 ticket |
| 變更生效但行為與假設都不符（第三種 deny 觸發模式） | 在 Test Results 詳細記錄 deny 訊息，建 follow-up ANA |
| 實驗導致 git index 競爭 corruption | 實驗前 commit 所有變更；實驗只派發單一 subagent（無並行），降低 corruption 風險至最低 |
| 觀察結果不可重現（intermittent） | 重複實驗 3 次，記錄每次結果；若有差異則建 follow-up |

### Context Bundle（Subagent 派發指令）

**前提**：PM 已在 settings.json 設定 `worktree.bgIsolation: "none"`（diff 確認 3 行純增量）。

**Step 1**：Edit `.claude/error-patterns/process-compliance/PC-137-parallel-subagent-claude-dir-edit-deny.md`
- old_string: `**Version**: 1.0.0 — 從 W17-177 saffron ANA 收斂落地（W17-177.1 IMP 防護的反模式描述層）`
- new_string: `**Version**: 1.0.0 — 從 W17-177 saffron ANA 收斂落地（W17-177.1 IMP 防護的反模式描述層）\n\n<!-- W3-034.1 experiment marker (will be removed in Step 2) -->`

**Step 2**：立即還原（對等 reverse Edit，移除 marker）。

**強制要求**：
1. 禁 git add / commit / worktree 操作
2. 禁猜測跳過——必須實際呼叫 Edit 工具
3. Step 1 被 deny 仍嘗試 Step 2（觀察一致性）
4. 不需 claim / 更新 ticket body（PM 主線程記錄）

**Exit Status 必填（YAML）**：
- status: success|partial_success|failed
- step1_result: success|deny|error
- step1_message: 完整工具回傳訊息全文
- step2_result: success|deny|error
- step2_message: 完整工具回傳訊息全文
- working_dir: pwd 輸出
- isolation_observed: 是否在 worktree 路徑或主 repo cwd
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 實驗 Protocol 規格（Phase 1）

#### 前置條件（Pre-flight）

1. 工作區乾淨：`git status --porcelain` 輸出為空（已驗證 2026-05-26 21:23）
2. 當前在 main 或可切換的 feature branch（PM 待決：在 main 直接實驗 vs 建 feature branch）
3. 確認 settings.json 目前無 `worktree.bgIsolation` 設定（使用預設 worktree）
4. 記錄當前 Claude Code 版本（CC 版本影響 bgIsolation 支援狀態）

#### 對照組設計（單因子實驗）

| 維度 | 對照組（Baseline） | 實驗組（Treatment） |
|------|------------------|-------------------|
| settings.json `worktree.bgIsolation` | `"worktree"`（預設） | `"none"` |
| subagent 數量 | 1（單一） | 1（單一） |
| 目標檔案 | `.claude/` 下任意安全測試檔 | 同左 |
| Edit 內容 | 添加實驗標記註解（可逆） | 同左 |
| 派發模式 | `isolation: worktree`（預設） | 隱含主 repo cwd（none 模式不建 worktree） |

**為何選單因子實驗**：parent W3-034 已透過 W17-108/109 部分驗證對照組（worktree × 單一 subagent success）。本 ticket 只需驗證 treatment cell；若 treatment success → 假設 A 成立（deny 綁 cwd），若 treatment deny → 假設 B 成立（deny 綁身份）。

#### 執行步驟

**Step 1：環境準備**
1. PM 前台確認當前 Claude Code 版本（從 environment env 取）
2. 查 settings.json 確認無 `worktree.bgIsolation` 設定
3. 選擇實驗目標檔案：建議 `.claude/error-patterns/process-compliance/PC-137-*` 下任一檔案（本 ticket where.files 已標註，符合範圍）

**Step 2：對照組驗證（可選 — 若 W17-108/109 資料足夠則跳過）**
1. 不改 settings.json（保留預設 worktree 模式）
2. 派發 thyme-python-developer subagent，prompt 含「Read PC-137 → Edit 添加一行實驗標記註解 → 報告 Edit 結果」
3. 記錄結果：success / deny + 任何錯誤訊息

**Step 3：實驗組執行**
1. PM 前台修改 settings.json：新增 `"worktree": { "bgIsolation": "none" }`
2. **關鍵不確定**：確認設定是否需要重啟 Claude Code session 才能生效
   - 若需重啟：建 follow-up ticket「需獨立 session 執行 W3-034.1 Step 3 起」，本 session 在 Step 2 後暫停
   - 若不需重啟：繼續 Step 4
3. （若不需重啟）派發 thyme-python-developer subagent，相同 prompt 結構
4. 記錄結果

**Step 4：結果判定**

| Treatment 結果 | 假設判定 | 後續動作 |
|--------------|---------|---------|
| success | 假設 A 成立（deny 綁 cwd） | 規劃 W3-034.1.1 並行焦點實驗（驗證 PC-137 在 none 下行為） |
| deny | 假設 B 成立（deny 綁身份） | 否證假設 A，更新 PC-137 / ARCH-015 結論 → 觸發 W3-034.2/3 spawn 簡化路徑 |
| 其他錯誤 | 第三種未知模式 | 詳記錯誤訊息，建 follow-up ANA 深入調查 |

**Step 5：環境還原（強制）**

無論 Step 3 結果如何，實驗結束後立即還原 settings.json（移除 `worktree.bgIsolation` 設定），避免污染後續 session。

**Step 6：結論寫入**

1. 將實驗結果寫入本 ticket Test Results 章節（含 commit SHA、subagent prompt、結果原文截圖或文字）
2. 在 ARCH-015 / PC-137 文件加 W3-034.1 觀察結果章節
3. 解鎖 W3-034.2 / W3-034.3（依結論決定 spawn 內容方向）

#### 觀察指標

| 指標 | 量測方式 | 用途 |
|------|---------|------|
| Edit 工具回傳狀態 | success / deny / error | 主要判定依據 |
| Deny 錯誤訊息全文 | tool result 完整字串 | 區分 deny 類型（hardcoded / hook / permission） |
| subagent 結束狀態 | Exit Status YAML | 確認 subagent 自我認知 |
| settings.json 變更前後 diff | git diff | 驗證實驗 treatment 純度 |

#### 內退方案（Rollback）

| 觸發條件 | 動作 |
|---------|------|
| settings.json 變更後 PM 主線程行為異常（無法派發 / hook 報錯） | `git checkout -- .claude/settings.json` 立即還原，重啟 session |
| 實驗導致 git index 競爭 | 本實驗只派發單一 subagent，理論不應發生；若發生則記錄為新發現 |
| Step 3 後 settings.json 設定未還原 | 緊急 Edit settings.json 移除設定，commit 還原 |

#### 結論判定規則（明確化）

實驗成功定義 = 完成以下三件事：
1. Treatment cell 至少執行一次（不論結果為 success/deny/error）
2. 結果與其原因被完整記錄在 Test Results 章節
3. settings.json 已還原至實驗前狀態

實驗不成功 = 上述任一項未達成 → 不可 complete，須繼續或建 follow-up。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 實驗結果（2026-05-26 21:50-21:51）

#### 環境

| 項目 | 值 |
|------|---|
| Claude Code 版本 | 2.1.150 |
| 實驗時間 | 2026-05-26 21:50-21:51 |
| 派發代理人 | thyme-python-developer |
| Working dir | /Users/tarragon/Projects/book_overview_v1（主 repo cwd） |
| Git branch | main |
| settings.json treatment | `"worktree": { "bgIsolation": "none" }`（3 行純增量） |

#### Subagent 觀察結果（Exit Status YAML）

| 指標 | 值 |
|------|---|
| status | success |
| step1_result | success |
| step1_message | "The file ... has been updated successfully." |
| step2_result | success |
| step2_message | "The file ... has been updated successfully."（還原 Edit） |
| isolation_observed | 主 repo cwd（非 worktree 路徑） |
| confidence | 1.0 |
| tool_uses | 9 / duration_ms 59046 |

#### 重要附帶觀察

1. **首次 Edit 觸發 "File has not been read yet"** — CC runtime 標準前置檢查（要求先 Read），非 `.claude/` hardcoded deny。subagent Read 該檔案後重試 Edit success。
2. 兩次 Edit 無 ARCH-015 / PC-137 deny 訊號。
3. subagent 確實在主 repo cwd 操作（bgIsolation: none 生效），非 worktree。
4. PC-137 檔案實驗後 `git diff` 為空，確認還原乾淨。

#### 結論判定

| 假設 | 預期（依 Problem Analysis） | 實際 | 判定 |
|------|---------------------------|------|------|
| A（deny 綁 subagent cwd） | bgIsolation: none → success | success | **成立** |
| B（deny 綁 subagent 身份） | bgIsolation: none → deny | success | **否證** |

**核心結論**：bgIsolation: none + 單一 subagent + .claude/ Edit → success；ARCH-015 hardcoded deny 不綁定 subagent 身份。

#### 結果範圍限制（必讀）

本實驗 **未驗證** 以下情境：
- **並行 3+ subagent + .claude/ Edit + bgIsolation: none**（PC-137 真正關注的場景）
- **bgIsolation: worktree + 單一 subagent + .claude/ Edit**（既有 worktree 模式對照組未本次重做；W17-108/109 已驗證 success，假設環境未漂移）
- W17-097.1-.4 DENY 真因（PC-115 標記「待重啟調查」，本實驗未觸發 DENY 故無法定位）

#### 對 ARCH-015 / PC-137 的影響

| 文件 | 影響 |
|------|------|
| PC-137 防護策略（並行 ≤ 2） | **不應僅基於本實驗放寬** — 單一 subagent 無法外推到並行情境，需 W3-034.1.1 並行受控實驗 |
| ARCH-015「.claude/ 不在 worktree 進行」條款 | 本實驗證實 single subagent + bgIsolation: none 不觸發 deny；建議改寫為「並行情境條件式」而非無條件禁止 |
| W17-108/109 既有結論 | 一致（subagent .claude/ Edit success）；本實驗額外證明在 bgIsolation: none 下亦成立 |

#### 環境還原確認

| 操作 | 結果 |
|------|------|
| settings.json 移除 `worktree.bgIsolation` | `git diff .claude/settings.json` 輸出為空 |
| PC-137 還原 | `git diff` 輸出為空 |
| 工作區殘留 | 僅本 ticket md 變更 + untracked log（前 session 遺留，與本實驗無關） |

#### Commit SHA

本實驗未 commit settings.json 變更（treatment 為臨時 + 還原模式）。Ticket md 變更將於 complete 階段 auto-stage。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager（前台實驗）或 thyme-python-developer
**Review Status**: pending