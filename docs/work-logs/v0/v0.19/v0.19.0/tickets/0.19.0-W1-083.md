---
id: 0.19.0-W1-083
title: 反轉 build.js sanity check SSOT：package.json 為版本源，worklog 標記改為 derived
type: IMP
status: pending
version: 0.19.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W1-077
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: ANA spawn IMP 落地反轉方向
  rationale: W1-077-recommended-direction-A
who:
  current: thyme-extension-engineer
  history: {}
what: 反轉 build.js sanity check SSOT
when: 本 wave 內
where:
  layer: 待定義
  files:
  - docs/work-logs/v0/
why: W1-077 ANA 結論推薦方向 A：package.json 為 SSOT，worklog 標記由 script 自動 sync
how:
  task_type: Implementation
  strategy: 修改 validateVersionAlignment 斷言方向 + 新增 syncWorklogStatus + 更新測試
acceptance:
- '[ ] validateVersionAlignment 斷言方向反轉為 package.json 為源（含註解說明 SSOT 設計）'
- '[ ] 新增 syncWorklogStatus 自動更新 worklog 標記'
- '[ ] 單元測試覆蓋三情境（一致 / 不一致 auto-sync / worklog 不存在）'
- '[ ] npm test 全綠 + npm run build:prod 驗證 sanity check 行為符合反轉後設計'
- '[ ] W1-080 esbuild 子系統 0 行變動（隔離度自檢）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-05-25'
updated: '2026-05-25'
---

# Execution Log

## Task Summary

反轉 build.js sanity check SSOT

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 規格補充（PM 預填 - 派發前重要 context）

**來源**：W1-077 ANA 推薦方向 A（commit aec2695e），完整 Solution 章節在 source ticket，agent 必讀。

**修改範圍**（取代 ticket frontmatter where.files 的不完整列表）：

| 檔案 | 變動性質 |
|------|---------|
| scripts/build.js | 修改 validateVersionAlignment 斷言方向 + 新增 syncWorklogStatus |
| tests/unit/scripts/build-version-check.test.js | 更新既有測試（斷言方向變了）+ 新增 syncWorklogStatus 測試 |
| .claude/skills/version-release/ | 評估與 start 子命令整合策略（可選；若不整合需在 Solution 說明理由） |

**禁修改範圍**（W1-080 已落地，必須保留）：

- `getEsbuild()` + `ensureEsbuildAvailable()` 子系統（W1-080 commit b9f1b020）
- `tests/unit/scripts/script-test-helpers.js` createMinimalFsAdapter / createFullFsAdapter 介面（可使用但不重構）

**實作策略**（從 W1-077 Solution 擷取）：

1. `validateVersionAlignment` 反轉斷言方向：
   - 舊邏輯：讀 worklog 標記 → 對比 package.json 版號 → 不一致 fail-fast
   - 新邏輯：讀 package.json 版號 → 確認對應 worklog 目錄存在（如 docs/work-logs/v0/v0.19/v0.19.0/）→ 不存在 fail-fast
2. 新增 `syncWorklogStatus(packageVersion)` 函式：
   - 偵測對應 worklog 的「狀態：」標記
   - 若與 package.json 版號不一致，自動更新為「開發中」（package.json bump 後 build:prod 即同步）
3. 更新單元測試覆蓋三情境：版號一致、版號不一致（auto-sync）、worklog 不存在（fail-fast）
4. 評估與 version-release skill start 子命令整合（W1-073 已用 skill bump 0.19.0，可在 Solution 說明本 ticket 是否擴展 skill）

**Acceptance 補強**（取代 frontmatter 3 acceptance 過簡）：

| # | Acceptance |
|---|-----------|
| 1 | validateVersionAlignment 斷言方向反轉為 package.json 為源（含註解說明 SSOT 設計） |
| 2 | 新增 syncWorklogStatus 自動更新 worklog 標記 |
| 3 | 單元測試覆蓋三情境（一致 / 不一致 auto-sync / worklog 不存在） |
| 4 | npm test 全綠 + npm run build:prod 驗證 sanity check 行為符合反轉後設計 |
| 5 | W1-080 esbuild 子系統 0 行變動（隔離度自檢） |

**參考檔案**：

- `docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W1-077.md` Solution 章節
- `.claude/skills/version-release/` 既有實作（version-release start 子命令）
- W1-074 commit 2109a1bc（原始 sanity check 設計）
- W1-080 commit b9f1b020（esbuild 子系統，禁改）

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W1-077 | chars: 629 -->

### Task Reference
- 0.19.0-W1-077 what: 評估 W1-074 sanity check SSOT 方向反轉（package.json 為 SSOT，worklog 標記自動 sync）

### Rationale Chain
- 0.19.0-W1-077 why: linux parallel-evaluation 審查 W1-074（commits 2109a1bc + dc7fcd7e）標為 Acceptable 但發現 Fatal issue (1)：worklog markdown 是 build-blocking SSOT 在語意上倒置。當前設計是『worklog 標記 = source of truth，package.json 對齊』，但更 Linus 的方案應為『package.json = SSOT，worklog 標記由 script 自動 sync』。Linus 原話：『Don't add a check for the thing humans forget to do. Make it impossible to forget.』本 ANA 評估反轉方向的可行性、影響範圍、與既有 version-release skill 整合策略

### Related Files
- scripts/build.js  # from 0.19.0-W1-077
- scripts/validate-manifest.js  # from 0.19.0-W1-077

### Source Acceptance (reference)
- [ref] [ ] 三方向 trade-off 表格完整（自動化程度/維護成本/風險）  # from 0.19.0-W1-077
- [ref] [ ] 推薦方向含 dissenting view（規則 5 §5.6 反諂媚必填）  # from 0.19.0-W1-077
- [ref] [ ] 若推薦反轉方向，spawn 對應 IMP ticket  # from 0.19.0-W1-077