---
id: 0.19.0-W1-048.10.1.2
title: 建立 ContentParser helper class
type: IMP
status: in_progress
version: 0.19.0
wave: 1
priority: P2
parent_id: 0.19.0-W1-048.10.1
children: []
blockedBy:
- 0.19.0-W1-048.10.1.1
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 建立 ContentParser helper class
when: W1-048.10.1.1 完成後
where:
  layer: overview / 匯入流程整體
  files:
  - src/overview/import/content-parser.js
  - tests/unit/overview/import/content-parser.test.js
why: W1-048.10.1 Phase 1 SOLID 拆分結論第 2 個 sub-IMP：建立 ContentParser class 封裝 _handleFileContent
  邏輯，純函式 content 解析（無 IO 無 state）。詳細 API 簽章見父 ticket Solution 章節 A.3
how:
  task_type: Implementation
  strategy: 依 W1-048.10.1 Solution A.3 規格實作：建立 src/overview/import/content-parser.js，class
    ContentParser 含 parse(content, fileFormat) → ImportResult；涵蓋 JSON/CSV/v1/v2/三區段/INV-1
    等既有邊界；撰寫 content-parser.test.js 涵蓋 _handleFileContent 全部既有測試案例；importer 本階段暫不接（由
    10.1.4 整合）
acceptance:
- '[ ] src/overview/import/content-parser.js 建立完成'
- '[ ] ContentParser.parse(content, fileFormat) API 簽章對齊父 ticket Solution A.3 規格'
- '[ ] content-parser.test.js 涵蓋 JSON/CSV/v1/v2 既有測試案例'
- '[ ] npm test 全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-24T15:24:04'
completed_at: null
created: '2026-05-24'
updated: '2026-05-24'
---

# Execution Log

## Task Summary

建立 ContentParser helper class

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 2 測試設計（2026-05-24，sage-test-architect）

#### 測試策略決策

| 項目 | 決策 | 理由 |
|------|------|------|
| 測試類型 | 單元測試（Sociable Unit） | ContentParser 為純函式 class，無 IO 無 state |
| 測試檔案 | `tests/unit/overview/import/content-parser.test.js`（新建） | 隔離於 importer 既有測試外 |
| Mock 策略 | detectFormatVersion / convertV1ToV2Data 預設用真實模組（內層 Domain 邏輯，依 Sociable 原則不 Mock）；測試僅在版本邊界 case 注入 stub | 對齊 sociable unit 原則：只 Mock 外部依賴 |
| 測試框架 | Jest | 既有 |

#### 既有測試案例遷移清單

來源：
- `tests/unit/overview/book-file-importer.test.js`
- `tests/unit/overview/book-file-importer-import-result.test.js`
- `tests/unit/overview/book-file-importer-version-detection.test.js`
- `tests/unit/overview/overview-import-private-methods.test.js`（_handleFileContent 區段）

| 測試類別 | 遷移後 GWT 簡述 |
|---------|---------------|
| JSON parse 成功 | Given 合法 JSON content + fileFormat='json'，When parse，Then 回傳 ImportResult {books, tagCategories, tags} 三欄位皆陣列 |
| CSV parse 成功 | Given 含 header 的 CSV + fileFormat='csv'，When parse，Then books 含對應 entries |
| v1 → v2 轉換 | Given v1 schema JSON，When parse，Then convertV1ToV2Data 被執行（透過結果結構驗證） |
| v2 直接解析 | Given v2 schema JSON，When parse，Then 不經 v1 converter，直接結構化 |
| 三區段格式（books/tagCategories/tags） | Given metadata 包裝 JSON，When parse，Then 三欄位分別填充 |
| INV-1 不變式 | Given 任意合法 input，When parse，Then books/tagCategories/tags 皆為陣列（即使空） |
| BOM 移除 | Given content with `﻿` 前綴，When parse，Then 解析成功（_removeBOM 等價） |
| 大型資料集警告 | Given books 數量 > 1000，When parse，Then 警告 metadata 存在或 log（依既有契約） |

#### 新增測試案例（API 簽章 + 邊界）

| Scenario | Given | When | Then |
|---------|-------|------|------|
| API 簽章存在 | ContentParser import | `new ContentParser()` | instance.parse 為 function |
| fileFormat 缺漏 | content 合法 | `parse(content, undefined)` | throw TypeError（fileFormat 必填） |
| fileFormat 無效 | content 合法 | `parse(content, 'xml')` | throw Error code=PARSE_ERROR |
| 空 content | content='' | `parse('', 'json')` | throw Error code=VALIDATION_ERROR |
| null content | content=null | `parse(null, 'json')` | throw Error code=VALIDATION_ERROR |
| 損毀 JSON | `'{ broken'` | `parse(_, 'json')` | throw Error code=PARSE_ERROR，details.cause 含原 SyntaxError |
| CSV 缺必要欄位 | header 無 'title' | parse(_, 'csv') | throw Error code=PARSE_ERROR |
| 無法辨識的 JSON 結構 | `'{}'` | parse(_, 'json') | throw Error code=VALIDATION_ERROR |
| INV-1：空 books 但結構合法 | `'{"books":[]}'` | parse | books=[], tagCategories=[], tags=[]（陣列非 undefined） |
| 自訂 largeDatasetThreshold | deps.largeDatasetThreshold=10 + 11 books | parse | 觸發警告路徑 |
| detectFormatVersion 注入 stub | deps.detectFormatVersion=jest.fn().mockReturnValue('v2') | parse(any json) | stub 被呼叫 1 次 |
| convertV1ToV2Data 注入 stub | deps.convertV1ToV2Data=jest.fn(d=>d) | parse(v1 json) | stub 被呼叫 1 次 |

#### 反向斷言

- instance 不暴露舊底線方法（_handleFileContent / _parseJSONContent / _parseCSVContent / _processBookData / _extractBooksFromData 等核心 helper 皆 undefined）

#### 行為鏈推演

parse(content, fileFormat) 前置鏈：
1. fileFormat 必填（TypeError）
2. fileFormat 合法（PARSE_ERROR）
3. content 非空（VALIDATION_ERROR）
4. BOM 移除
5. format-specific parse（JSON SyntaxError → PARSE_ERROR；CSV 欄位 → PARSE_ERROR）
6. 結構辨識（v1/v2/三區段 / 未知 → VALIDATION_ERROR）
7. INV-1 後處理（三欄位陣列化）

每個前置失敗都用獨立 case 驗證短路。

#### Mock 與測試資料

- 真實 detectFormatVersion / convertV1ToV2Data 載入（內層）；僅版本邊界 case 注入 stub
- JSON / CSV fixture 直接 inline 在測試檔（既有 fixture 可從 importer.test.js 複製對應字串常數）
- 無外部 IO

#### 預期測試案例數：~35-40 case（既有 ~25 + 新增 ~12）

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 3a 實作策略（2026-05-24，pepper-test-implementer）

#### 1. 檔案結構

```
src/overview/import/
  content-parser.js        ← 新建（本 sub-IMP 範圍）

tests/unit/overview/import/
  content-parser.test.js   ← 新建（依 Phase 2 測試矩陣 ~35-40 case）
```

#### 2. 擷取邏輯範圍（from src/overview/book-file-importer.js）

**核心方法 + 子 helper 全搬遷清單**：

| 來源行號 | 來源方法 | 搬遷後位置 |
|---------|---------|-----------|
| L322-328 | `_handleFileContent` | `ContentParser.parse`（主入口邏輯） |
| L531-539 | `_validateAndCleanContent` | private helper |
| L547-549 | `_removeBOM` | private helper |
| L561-573 | `_parseJSONContent` | private helper（含 cause 保留） |
| L343-366 | `_parseCSVContent` | private helper |
| L372-374 | `_isBlankRow` | private helper |
| L391-401 | `_parseCSVRows` | private helper（state machine） |
| L412-424 | `_consumeQuotedChar` | private helper |
| L437-458 | `_consumeUnquotedChar` | private helper |
| L464-469 | `_commitRow` | private helper |
| L475-480 | `_flushPendingRow` | private helper |
| L490-499 | `_csvRowToBook` | private helper |
| L509-523 | `_deserializeCSVField` | private helper |
| L587-596 | `_processBookData` | private helper（含 INV-1 組裝） |
| L604-606 | `_filterValidBooks` | private helper |
| L613-622 | `_checkLargeDataset` | private helper（console.warn 保留） |
| L652-709 | `_extractBooksFromData` | private helper（6 線性分支） |
| L725-740 | `_extractTagSection` | private helper |
| L752-754 | `_normalizeToArray` | private helper |
| L768-772 | `_isMetadataWrapFormat` | private helper |
| L781-785 | `_isValidBook` | private helper |
| L791-793 | `_validateBookStructure` | private helper |
| L803-806 | `_validateRequiredFields` | private helper |
| L818-822 | `_validateFieldTypes` | private helper |
| L46-57 | `CSV_HEADER_TO_FIELD` 常數 | 模組頂層常數 |
| L34-44 | `FILE_CONSTANTS.LARGE_DATASET_THRESHOLD` | constructor 預設 deps.largeDatasetThreshold |

**外部模組依賴**（既有 require，繼承）：

| 模組 | 路徑 | 預設 vs 注入 |
|------|------|------------|
| `ErrorCodes` | `src/core/errors/ErrorCodes` | 模組頂層 require，恆定 |
| `detectFormatVersion` | `src/export/format-version-detector` | 預設用模組 require，**deps.detectFormatVersion 可覆寫**（Phase 2 stub 注入 case） |
| `convertV1ToV2Data` | `src/export/v1-to-v2-converter` | 預設用模組 require，**deps.convertV1ToV2Data 可覆寫**（Phase 2 stub 注入 case） |

#### 3. 虛擬碼骨架

```
const DEFAULT_DEPS = {
  detectFormatVersion: require('src/export/format-version-detector').detectFormatVersion,
  convertV1ToV2Data:   require('src/export/v1-to-v2-converter').convertV1ToV2Data
}

class ContentParser:
  constructor(deps = {}):
    this.detectFormatVersion = deps.detectFormatVersion ?? DEFAULT_DEPS.detectFormatVersion
    this.convertV1ToV2Data   = deps.convertV1ToV2Data   ?? DEFAULT_DEPS.convertV1ToV2Data
    this.largeDatasetThreshold = deps.largeDatasetThreshold ?? 1000

  # ===== Public API =====
  parse(content, fileFormat):
    # 前置鏈
    if fileFormat is undefined or null:
      throw TypeError('parse: fileFormat is required')
    if fileFormat not in ['json', 'csv']:
      throw makeError('parse: fileFormat must be "json" or "csv"', PARSE_ERROR, 'parsing')

    cleanContent = this.validateAndClean(content)    # VALIDATION_ERROR if empty
    data = (fileFormat == 'csv')
             ? this.parseCSV(cleanContent)
             : this.parseJSON(cleanContent)
    return this.processBookData(data, fileFormat)    # 含 INV-1 組裝

  # ===== Content cleaning =====
  validateAndClean(content):
    if not content or content.trim() == '':
      throw makeError('檔案內容為空', VALIDATION_ERROR, 'validation')
    return removeBOM(content)

  # ===== JSON path =====
  parseJSON(content):
    try: return JSON.parse(content)
    catch SyntaxError as e:
      throw makeError('JSON 檔案格式不正確', PARSE_ERROR, 'parsing', cause=e)

  # ===== CSV path（state machine 不變動，等價搬遷）=====
  parseCSV(content):
    rows = parseCSVRows(content)         # state machine 兩 helper
    if rows.length == 0:
      throw makeError(INVALID_CSV+'：內容為空', PARSE_ERROR, 'parsing')
    fieldNames = mapHeader(rows[0])
    if 'id' not in fieldNames or 'title' not in fieldNames:
      throw makeError(INVALID_CSV+'：缺少必要欄位', PARSE_ERROR, 'parsing')
    books = rows[1:].filter(not isBlankRow).map(csvRowToBook)
    return books

  # ===== Book data processing（六線性分支提取 + 過濾 + INV-1）=====
  processBookData(data, fileFormat):
    extracted = this.extractBooksFromData(data, fileFormat)
    validBooks = extracted.books.filter(isValidBook)
    this.checkLargeDataset(validBooks)
    return {
      books:         validBooks,
      tagCategories: extracted.tagCategories,
      tags:          extracted.tags
    }

  extractBooksFromData(data, fileFormat):
    # 等價搬遷 L652-709，6 線性分支：CSV / v1 / v2 / MetadataWrap / EmptyObject / throw
    # 內部呼叫 this.detectFormatVersion / this.convertV1ToV2Data（注入點）
    ...
```

#### 4. 子 helper 搬遷策略

**全部以實例方法搬遷**（非靜態），保留 `this.detectFormatVersion / this.convertV1ToV2Data` 注入點被 `extractBooksFromData` 正確讀取。

**反向斷言預期未暴露之 helper**（Phase 2 反向斷言要求）：

```
_handleFileContent / _parseJSONContent / _parseCSVContent /
_processBookData / _extractBooksFromData
```

實作時改為不帶底線的私有風格（JS 無真私有，靠命名約定）：

```
parseJSON / parseCSV / processBookData / extractBooksFromData / ...
```

或使用 `#privateField`（ES2022）需確認 Babel + esbuild 支援。**建議採命名約定**（無底線），與 Phase 2 反向斷言 `instance._handleFileContent === undefined` 相容。

#### 5. INV-1 不變式驗證點

所有 `extractBooksFromData` 回傳 path：

| Path | books | tagCategories | tags |
|------|-------|--------------|------|
| CSV | `Array.isArray(data) ? data : []` | `[]` | `[]` |
| v1 | `converted.books` | `normalizeToArray(converted.tagCategories)` | `normalizeToArray(converted.tags)` |
| v2 | `data.books` | `extractTagSection(data, 'tagCategories')` | `extractTagSection(data, 'tags')` |
| MetadataWrap | `data.data` | `extractTagSection(data, 'tagCategories')` | `extractTagSection(data, 'tags')` |
| EmptyObject | `[]` | `[]` | `[]` |
| Unrecognized | throw（不回傳） | — | — |

**等價搬遷**，無新邏輯。

#### 6. 關鍵實作指引

**順序**：
1. 建 `content-parser.test.js`（先寫 RED 測試 ~35-40 case）
2. 建 `content-parser.js` 骨架 + module export
3. 逐子模組搬遷實作（建議子順序：validateAndClean → parseJSON → parseCSV → extractBooksFromData → processBookData → parse 主入口）
4. 全綠後 commit

**Fixture 來源**：從 `tests/unit/overview/book-file-importer.test.js` / `book-file-importer-import-result.test.js` / `book-file-importer-version-detection.test.js` 直接複製 JSON / CSV 字串常數（避免重新設計）。

#### 7. 權宜方案與技術債務

- **deps.detectFormatVersion / convertV1ToV2Data 注入點**：超越既有 importer（直接 require）的設計，為 Phase 2 stub 注入 case 所需。生產情境不傳 deps 則用預設 require，零變更
- **狀態機 CSV parser 等價搬遷**：W1-048.8 F13 重構成果（state 物件 + quoted/unquoted helper），不再次重構，維持低風險
- **console.warn 保留**：MetadataWrap 警告 + 大型資料集警告 + tagSection 型別降級警告共 3 處，等價搬遷
- **Phase 4 候選**：六線性分支提取邏輯（57 行）可考慮拆 strategy pattern；本 sub-IMP 不處理

#### 8. Phase 3b 交接

- 派發 thyme-extension-engineer 執行 RED + GREEN
- prompt 應強調「等價搬遷，無新邏輯」+ 引 Phase 2 測試矩陣 + 本搬遷清單
- commit 策略：單一 commit「feat: 建立 ContentParser helper class（W1-048.10.1.2）」含 src + test
- 注意：本 sub-IMP 完成後 importer.js 仍保有所有底線方法，10.1.4 才會移除

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending