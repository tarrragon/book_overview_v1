---
id: 0.19.0-W4-005
title: 改善 E2E 測試碼 timeout 常數化與二次提取 helper
type: IMP
status: in_progress
version: 0.19.0
wave: 4
priority: P3
parent_id: null
children:
- 0.19.0-W4-005.1
blockedBy:
- 0.19.0-W4-005.1
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W1-008
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:Phase 4a 完成
  final_decision: W1-008 Phase 4 多視角評估發現低幅度可讀性改善，建 P3 follow-up 追蹤
  rationale: quality-baseline-rule-5
who:
  current: thyme-extension-engineer
  history: {}
what: 改善 E2E 測試碼 timeout 常數化與二次提取 helper
when: 可獨立排程，建議併入 E2E 測試維護批次
where:
  layer: Infrastructure
  files:
  - tests/e2e/browser/helpers/extraction-flow.js
  - tests/e2e/browser/helpers/storage-reader.js
  - tests/e2e/browser/extraction-pipeline.e2e.test.js
why: W1-008 Phase 4 Redundancy + Coupling 視角發現兩項低幅度可讀性問題：(1) timeout magic number
  30000 出現 7 次、90000 出現 2 次未抽具名常數；(2) G4 二次提取在測試碼手寫展開 navigateToFixture 子集（goto +
  waitForContentScriptReady）。linux 視角判定屬可選改善、非 Phase 4 必要重構，故獨立追蹤。
how:
  task_type: Implementation
  strategy: timeout 數值抽為具名常數（如 DEFAULT_NAV_TIMEOUT / EXTRACTION_TIMEOUT）；二次提取邏輯抽為可重用
    reExtract helper
acceptance:
- '[ ] E2E 測試 timeout 字面值收斂為具名常數'
- '[ ] 二次提取邏輯抽為可重用 helper'
- '[ ] tests/e2e/browser E2E 測試維持 9/9 GREEN（pristine baseline 為 9/9，重構後 regression
  修復追蹤於 0.19.0-W4-005.1）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: true
started_at: '2026-05-31T13:02:17'
completed_at: null
created: '2026-05-21'
updated: '2026-05-21'
---

# Execution Log

## Task Summary

改善 E2E 測試碼 timeout 常數化與二次提取 helper

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

W1-008 Phase 4 Redundancy + Coupling 多視角審查發現兩項低幅度可讀性問題（非 Phase 4 必要重構，獨立追蹤）：

1. **Timeout magic number 重複**：30000 出現 7 次（extraction-flow.js / storage-reader.js / extraction-pipeline.e2e.test.js），90000 出現 2 次（extraction-pipeline.e2e.test.js 整檔 jest test timeout）。未抽具名常數，閱讀者需推測語意（導覽 vs 提取 vs jest 整檔 timeout）。
2. **二次提取邏輯重複**：extraction-pipeline.e2e.test.js 第二次提取流程（goto + waitForContentScriptReady + 觸發提取）在測試碼手寫展開 navigateToFixture 子集，與 helper 重複。

### 影響範圍

- `tests/e2e/browser/helpers/extraction-flow.js`（2 處 30000：line 120 / line 153）
- `tests/e2e/browser/helpers/storage-reader.js`（1 處 30000：line 90）
- `tests/e2e/browser/extraction-pipeline.e2e.test.js`（4 處 30000 + 2 處 90000：line 73 / 181 / 194 / 205 / 208）

### 相關 Error Pattern

無直接對應 PC，屬一般可讀性重構。

### WRAP 三問（簡化版）

**W（Widen）— 候選方案**

| 方案 | 描述 | 評估 |
|------|------|------|
| A（採用） | 抽具名常數 + 抽 reExtract helper | ticket 原方向，可讀性收益最大 |
| B | 只抽常數，不抽 helper | 更保守，降破壞 GREEN 風險 |
| C | 維持現狀，移除 ticket | AC 已驗證 magic number 仍在，無理由放棄 |

**選擇 A**：可讀性收益足夠，且 ticket 已明示 helper 抽取。風險用「先跑 baseline GREEN」防護。

**A（Attain distance）— 機會成本**

擠壓 W3-094（P2 trigger 觀察）+ W4-006/16/17（P3 並行可做）。但本 ticket 範圍小（3 檔，無跨架構層），P3 + IMP 屬輕量任務，不擠壓 P2 重要目標。

**P（Prepare to be wrong）— 最可能失敗原因 + 防護**

- 失敗原因：抽 reExtract helper 後語意微差導致 9/9 E2E 從 GREEN 變紅（重構引入 regression）。
- 防護：派發 prompt 要求 agent (1) 先跑 npm run test:e2e:browser 確認 baseline 9/9 GREEN；(2) 每個重構 step 後重跑 9/9；(3) AC#3 必驗 9/9 GREEN 才能 complete。

### 實作策略（agent 執行指引）

1. **抽常數**：建議在 helpers 目錄新增 timeouts.js（或於各 helper 檔頂部宣告），常數命名語意：
   - `DEFAULT_NAV_TIMEOUT = 30000`（page.goto / waitForSelector 用）
   - `JEST_TEST_TIMEOUT = 90000`（jest test/beforeAll 整檔 timeout 用）
2. **抽 reExtract helper**：在 extraction-flow.js 新增 `reExtract(page, options)`，封裝 goto + waitForContentScriptReady + 觸發提取流程；extraction-pipeline.e2e.test.js 改呼叫 helper。
3. **驗證 AC**：
   - AC#1：grep -rn "30000\|90000" tests/e2e/browser/ 應只剩常數宣告處
   - AC#2：grep "reExtract\|二次提取" 確認 helper 抽出
   - AC#3：npm run test:e2e:browser 必 9/9 GREEN

### Commit Policy

單一 commit 即可（小型 refactor）：
- branch：feat/0.19.0-W4-005-e2e-timeout-helper（已建）
- commit msg 格式：`refactor(0.19.0-W4-005): E2E timeout 常數化與 reExtract helper 抽取`

### PM 決策（baseline 8/9 後）

**情境**：thyme baseline 測試發現 G4-1 dedup 失敗為 pre-existing（與重構無關，根因疑 SW 取得時機/storage 狀態），AC#3 原文「維持 9/9 GREEN」無法達成。

**決策（用戶選 B）**：

| 動作 | 落地 |
|------|------|
| 放寬 W4-005 AC#3 | 改為「重構後與 baseline 等價（8/9 GREEN 且 G4-1 失敗訊息不變）」 |
| G4-1 根因追蹤 | 建 0.19.0-W4-021 (ANA, P2, saffron) 找根因並 spawn IMP |
| W4-005 推進 | resume thyme，採新 AC#3 完成本 ticket |

**未選 A（先修 G4 再 resume）**：G4 根因不明須先 ANA，估時長且串行阻塞 W4-005；本 ticket 範圍純可讀性重構與 G4 解耦。

**未選 C（W4-005 blocked）**：本 ticket 與 G4 無實質依賴（重構不會引入新失敗，與 G4 pre-existing 狀態獨立），blocked 過度保守。

**Resume 指示給 thyme**：以 8/9 為新 baseline，重構後再跑必須仍為 8/9 GREEN 且失敗測試名稱與訊息與 baseline 完全相同（用 jest output 比對）；G4 不在本 ticket 修復範圍。
### PM 決策（baseline 校正後再決策）

**baseline 校正**：本次 Resume thyme 跑 4 次測試證實 pristine baseline = **9/9 GREEN**（stash 還原後二次驗證一致）。前次 agent 觀察的 8/9（G4-1 dedup fail）為**單次環境性 transient**，不是 pre-existing bug。

**影響反推**：

| 項目 | 校正 |
|------|------|
| W4-005 AC#3 | 撤回 8/9 等價誤改，還原為「9/9 GREEN」 |
| 0.19.0-W4-021 ANA | 立論「G4-1 為 pre-existing」錯誤，標誤建後 close |
| W4-005 重構引入 regression | 兩次重現 1/9（beforeAll Extension ID timeout 10000ms）穩定 |

**用戶決策（A 方案）**：建子 IMP W4-005.1 診斷重構引入 regression 根因 + 修復。修復後 W4-005 以**重構完全保留**狀態（保留 timeouts.js + reExtract helper + 4 檔字面值替換）達成 9/9 GREEN 後 complete。

**未選 B（部分撤回二分定位）**：用戶優先保留 AC#1 + AC#2 收益。
**未選 C（完整 revert）**：放棄重構收益不接受。

**W4-005 工作區處理**：當前 4 檔修改 + timeouts.js 新增 commit 為 WIP commit 作 W4-005.1 的 base；W4-005.1 從此 base 重現 regression 後找根因。

**W4-005.1 Acceptance（thyme 執行指引）**：
- 二分定位（stash reExtract / stash timeouts.js 分別跑測試）
- 修復不可撤回 W4-005 5 常數與 reExtract helper（重構完全保留）
- 修復後 9/9 GREEN（兩次重現）
## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 重構執行摘要

依 PM Resume 指示執行純可讀性重構（baseline 8/9 改為 9/9，本次執行 baseline 為 9/9 GREEN）：

**新增**：
- `tests/e2e/browser/helpers/timeouts.js`：集中宣告 5 個具名 timeout 常數
  - `DEFAULT_NAV_TIMEOUT = 30000` (page.goto / waitForSelector)
  - `CONTENT_SCRIPT_READY_TIMEOUT = 30000` (waitForContentScriptReady polling)
  - `STORAGE_STABLE_TIMEOUT = 30000` (waitForBooksStable polling)
  - `OVERVIEW_RENDER_TIMEOUT = 60000` (Overview 顯示 test jest timeout)
  - `JEST_TEST_TIMEOUT = 90000` (含完整 pipeline 的 beforeAll/test timeout)

**修改**：
- `extraction-flow.js`：引入常數、line 120 改用 `DEFAULT_NAV_TIMEOUT`、line 153 改用 `CONTENT_SCRIPT_READY_TIMEOUT`；新增 `reExtract(setup, options)` helper 封裝二次提取流程（goto + waitForContentScriptReady + triggerExtraction）
- `storage-reader.js`：引入常數、line 90 改用 `STORAGE_STABLE_TIMEOUT`
- `extraction-pipeline.e2e.test.js`：引入常數、beforeAll/G4-1 jest timeout 改用 `JEST_TEST_TIMEOUT`、G5-1 jest timeout 改用 `OVERVIEW_RENDER_TIMEOUT`、G5-1 內 page.goto/waitForSelector timeout 改用 `DEFAULT_NAV_TIMEOUT`、G4-1 二次提取手寫展開改為 `reExtract(dedupSetup)`、G4-1 第一次提取從 `navigateToFixture + triggerExtraction` 改為 `runExtraction`

### AC#1 / AC#2 驗證

```
$ grep -rn "30000\|90000\|60000" tests/e2e/browser/
# 僅 timeouts.js 內常數宣告與註解（line 29/36/43/50/58/9/15）
```

reExtract helper 已被 G4-1 採用，呼叫處從 4 行（goto + waitForContentScriptReady + triggerExtraction）收斂為 1 行。

### AC#3 驗證失敗（regression 確認）

| 測試執行 | 結果 |
|---------|------|
| Pristine baseline（重構前，stash 還原） | **9/9 GREEN** (10.369s) |
| 重構後 run 1 | **1/9 GREEN, 8 failed** — `[SETUP] beforeAll 前置失敗: 取得 Extension ID 失敗: Timed out after waiting 10000ms` |
| 重構後 run 2（確認非 transient） | **1/9 GREEN, 8 failed**（相同錯誤） |

**重構引入穩定 regression**：beforeAll 階段「取得 Extension ID」逾時 10000ms。

**注意：失敗訊息與 baseline 不同**：
- 本 ticket 原 NeedsContext 記錄的 baseline 8/9 失敗為 G4-1 dedup `chrome.storage.local undefined`
- 本次重構引入的失敗為 G1/G2/G3/G5 beforeAll setup 階段 `取得 Extension ID 失敗 10000ms`
- 兩種失敗模式完全不同，AC#3「失敗訊息不變」條件無法滿足

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### baseline 與重構後測試對照

執行三次 npm run test:e2e:browser：

| 執行 | 工作區狀態 | 結果 | 時間 |
|------|----------|------|------|
| 1 | Pristine baseline（stash 重構前還原） | 9/9 GREEN | 10.369s |
| 2 | 重構後（完整 timeouts.js + reExtract + 4 檔修改） | 1 GREEN / 8 failed | 17.264s |
| 3 | 重構後（再執行，確認非 transient） | 1 GREEN / 8 failed | 17.187s |

### 失敗訊息

G1/G2/G3/G5 全部失敗於相同位置（同訊息）：
```
[SETUP] beforeAll 前置失敗，無法執行斷言: 取得 Extension ID 失敗: Timed out after waiting 10000ms
```

G4-1 通過（用獨立 dedupSetup，不受 shared beforeAll 影響）。

### 驗證證據檔

- /tmp/w4-005-baseline.log：執行 1 完整輸出
- /tmp/w4-005-after.log：執行 2 完整輸出
- /tmp/w4-005-after-2.log：執行 3 完整輸出
- /tmp/w4-005-pristine-baseline.log：執行 1（pristine）詳細輸出

### 結論

- AC#1（timeout 字面值收斂為具名常數）：**已達成**
- AC#2（二次提取邏輯抽為可重用 helper）：**已達成**
- AC#3（重構後與 baseline 等價）：**未達成 — 重構引入穩定 regression**

## NeedsContext
### Baseline E2E 失敗：G4-1 dedup 測試 1/9 RED（pre-existing）

**缺失項**: W4-005 P 防護要求「先跑 baseline 9/9 GREEN」，但實測 baseline 為 8/9（G4-1 失敗）。本 ticket AC#3「tests/e2e/browser E2E 測試維持 9/9 GREEN」在起點即無法成立——不可能透過 W4-005 範圍的純可讀性重構（抽常數 + 抽 helper）讓既有失敗從 RED 變 GREEN。

**觸發位置**:
- 測試：`tests/e2e/browser/extraction-pipeline.e2e.test.js:173` (G4 beforeEach 內呼叫 `clearBooksStorage(dedupSetup.browser)`)
- helper：`tests/e2e/browser/helpers/storage-reader.js:118-125` (clearBooksStorage 在 SW context 內 evaluate `chrome.storage.local.remove`)
- 錯誤訊息：`TypeError: Cannot read properties of undefined (reading 'local')`
  - 意義：SW worker context 內 `chrome` 為 undefined，與 G2 共用 setup（同樣呼叫 clearBooksStorage 卻成功）形成對比
  - 推測：G4-1 用獨立 `dedupSetup = new ExtensionTestSetup()`，其 SW 取得時機/狀態與 G1-G3/G5 共用的 setup 不同；可能與 SW idle 回收或新建 browser 後 SW 尚未完整就緒有關
- 完整失敗位置：`evaluate at clearBooksStorage (storage-reader.js:118:16), <anonymous>:2:21`

**影響**:
- AC#3「9/9 GREEN」無法達成（起點為 8/9）
- 即使完成 AC#1（常數抽離）+ AC#2（reExtract helper）也無法 complete
- 若強行 complete 會違反 P 防護與 quality-baseline 規則 1（測試通過率）

**建議補料**:
PM 應評估以下選項並決策：

1. **選項 A：先處理 G4 baseline 失敗**
   - 開新 IMP ticket（如 W4-005.1 或獨立 P3 ticket）診斷 `clearBooksStorage` 在獨立 browser instance 下 `chrome.storage.local` 為 undefined 的根因
   - 修復 G4 baseline 後，本 W4-005 才有 9/9 GREEN 起點可重構
   - 此選項符合 evidence-driven-bugfix（先有 baseline 才能驗證重構不引入 regression）

2. **選項 B：放寬 W4-005 AC#3 到當前 baseline 等價（8/9 GREEN 且 G4 失敗訊息不變）**
   - 修改本 ticket AC#3 為「重構後測試結果與 baseline 等價（同樣 8 過 1 失，且失敗訊息來自相同位置）」
   - 把 G4 失敗作為已知 pre-existing 狀態接受
   - 風險：可能遮蔽重構引入的新 G4 變化（但失敗訊息比對可緩解）

3. **選項 C：暫停 W4-005 至 G4 修復後重啟**
   - 將 ticket 設為 blocked，blockedBy 新建的 G4 修復 ticket
   - 待 G4 修復 ticket complete 後再 claim W4-005

**重派成本**:
- 選項 A：新 ticket 診斷 + 修復，估 1-2 個 agent dispatch（含 SW context debug）
- 選項 B：PM Edit ticket md 修改 AC#3，0 token；本 agent 繼續執行可直接 resume
- 選項 C：PM 操作 ticket 狀態 + 建新 ticket，0.5 個 dispatch

**baseline 測試輸出摘要**（供 PM 與後續 ticket 參考）:
```
Tests:       1 failed, 8 passed, 9 total
Time:        8.041 s

PASS:
- [SETUP] G1-1 Extension 成功載入且 Service Worker 註冊 (2 ms)
- [SETUP] G1-2 fixture 導航成功且含正好 5 本書 (5 ms)
- [SETUP] G2-1 提取後 storage 寫入 5 本書且無 JS error
- [MODEL] G3-1 每本書具備 v1.1 model 必填欄位 (2 ms)
- [MODEL] G3-2 readingStatus 為 6 狀態 enum 合法成員
- [MODEL] G3-3 readingStatus 由 progress 自動 derive 正確 (1 ms)
- [MODEL] G3-4 提取書名與 fixture 一致
- [DISPLAY] G5-1 Overview 頁面正確顯示 5 本書書名 (123 ms)

FAIL:
- [DEDUP] G4-1 連續 2 次提取無重複記錄 (1776 ms)
  TypeError: Cannot read properties of undefined (reading 'local')
  at clearBooksStorage (storage-reader.js:118:16) → evaluate <anonymous>:2:21
```

**本 agent 已完成的動作**: 僅執行 baseline 確認，未動任何產品 / 測試 / helper 程式碼。Branch `feat/0.19.0-W4-005-e2e-timeout-helper` 維持乾淨（無 staged/未 staged 變更，除既有 ticket md 變更外）。

### [本次 Resume 新狀況] 重構引入穩定 regression：beforeAll Extension ID timeout

> 上方原 NeedsContext 條目記錄前次 baseline 8/9（G4-1 fail）。本次 Resume 後實況變化見以下說明。

**缺失項**: 依 PM Resume 指示完成 AC#1 + AC#2 重構後執行測試，重構引入 **8 個測試** 從 baseline 9/9 GREEN 變為失敗。失敗模式與 baseline 8/9 的 G4-1 完全不同，違反 AC#3「失敗訊息不變」條件。

**本次 Resume baseline 變化**:

| 階段 | baseline 結果 | 來源 |
|------|--------------|------|
| 前次 baseline（上方 NeedsContext 條目） | 8/9（G4-1 fail） | 前次 thyme 執行 |
| **本次 Resume 初次執行（pristine）** | **9/9 GREEN** | 本次 thyme 重構前執行 |
| 本次 Resume 重構後執行 1 | 1/9 GREEN, 8 failed | 本次 thyme |
| 本次 Resume 重構後執行 2（確認非 transient） | 1/9 GREEN, 8 failed | 本次 thyme |

**Note**: 本次 Resume 初次 baseline 升級為 9/9（G4-1 pre-existing failure 在本次環境下消失），但這不改變 regression 結論：重構從 9/9 變為 1/9 是穩定（兩次重現）的 regression。

**重構引入的失敗訊息**（與 baseline 8/9 的 G4-1 失敗訊息完全不同）:

```
[SETUP] beforeAll 前置失敗，無法執行斷言: 取得 Extension ID 失敗: Timed out after waiting 10000ms
```

**失敗範圍**：G1-1 / G1-2 / G2-1 / G3-1 / G3-2 / G3-3 / G3-4 / G5-1 共 8 個（皆因 shared beforeAll setup 失敗連動失敗）。G4-1 反而通過（用獨立 dedupSetup 不受影響）。

**對照前次 NeedsContext 的 G4-1 失敗訊息**:

```
TypeError: Cannot read properties of undefined (reading 'local')
at clearBooksStorage (storage-reader.js:118:16) → evaluate <anonymous>:2:21
```

兩者**完全不同**：
- 前次 baseline 8/9 G4-1：失敗在 SW worker context 取 `chrome.storage.local`（SW 已建立但 chrome global 不可見）
- 本次重構後 1/9：G1-G5 shared beforeAll 失敗在更早期的 `取得 Extension ID`（SW target 本身就 10s 內取不到）

**重構動作對照（為何可能引入此 regression）**:

| 變動類別 | 原狀 | 重構後 | 可能影響 |
|---------|------|--------|---------|
| 新增模組 | 無 | tests/e2e/browser/helpers/timeouts.js（被 storage-reader 和 extraction-flow 共同 require） | Node module 載入順序變化 |
| extraction-pipeline import | 直接 require navigateToFixture / waitForContentScriptReady / FIXTURE_URL | 改為 require runExtraction / reExtract（不再直接引入 FIXTURE_URL / navigateToFixture） | top-level evaluation 順序變化 |
| G4-1 第一次提取 | navigateToFixture + triggerExtraction 兩呼叫 | 改 runExtraction 一呼叫 | 行為等價但呼叫鏈深度變化 |

**timeout 數值無改變**：所有 30000 / 60000 / 90000 字面值都被替換為相同數值的具名常數，沒有任何 timeout 數值變動。失敗原因不會是「timeout 變短」。

**推測候選根因**（未驗證）:

1. **Node module 載入時機**：新增的 timeouts.js 被 extraction-flow.js 和 storage-reader.js 雙向 require，可能改變 jest 模組解析順序或 cache 行為，間接影響 ExtensionTestSetup 的 puppeteer launch 時機（但 timeouts.js 內無任何副作用，僅 const 宣告與 module.exports，理論上不該影響）。
2. **環境性 SW 不穩**：本次測試環境的 SW launch 較不穩定（前次 baseline 8/9 G4-1 失敗 → 本次 baseline 9/9 GREEN → 本次重構 1/9）；可能 SW launch timing 對 puppeteer 載入時機極度敏感，重構的 require 順序微改動觸發臨界。
3. **build cache 影響**：未重 npm run build:dev 即跑 E2E，extension build 與 timeouts.js 變更可能存在不一致狀態。

**範圍邊界判定**:
- 本 ticket how.strategy = 「timeout 數值抽為具名常數 + 二次提取邏輯抽為可重用 helper」，**不含 SW launch 穩定性診斷**
- 本 ticket where.files 僅含 3 個既有檔案 + 新建 timeouts.js，**不含 ExtensionTestSetup / service-worker-target**
- 根因若為「SW launch 對 require 順序敏感」，則屬於 setup 層級的測試基礎設施問題，**超出 W4-005 純可讀性重構範圍**

**建議補料（PM 決策選項）**:

1. **選項 A：建子 IMP 診斷 + 修復**
   - 開新 IMP（如 0.19.0-W4-005.1）「重構引入 beforeAll Extension ID timeout 根因診斷」
   - 派 thyme 詳查 SW launch 對 require 順序的敏感性、是否需先 build / SW pre-warm 等
   - 修復後本 W4-005 可以等價狀態 complete

2. **選項 B：撤回部分重構（先保留常數，撤回 helper）**
   - 撤回 reExtract 抽取 + extraction-pipeline.e2e.test.js 的 import 結構變更
   - 保留 timeouts.js + 三檔的 timeout 字面值替換
   - 風險：仍可能因 timeouts.js require 改變載入順序而失敗，但範圍縮小可二分定位根因
   - AC#2 (reExtract helper) 改 partial-complete 或建 follow-up

3. **選項 C：完整 revert 本 ticket 重構**
   - W4-005 改 close（不採行）+ 建追蹤 ANA 評估「為何 timeout 重構引發 SW launch 不穩」
   - 機會成本最低（pristine baseline 9/9 不被破壞）
   - 缺點：AC#1 / AC#2 重構效益放棄

4. **選項 D：放寬 AC#3 接受重構引入的新 failure 模式**
   - 修改 AC#3 為「重構後 G4-1 GREEN 視為等價」（因兩次 baseline G4-1 狀態不穩，本來就是 pre-existing 環境敏感）
   - 但需 PM 接受 8 個原 GREEN 變紅是「環境敏感而非重構引入」的假設
   - **不建議**：違反 evidence-driven-bugfix（穩定重現 8 倒退無證據是環境）

**重派成本**:
- 選項 A：新 IMP 診斷 + 修復，估 1-2 dispatch（含 SW launch 行為 debug）
- 選項 B：本 agent resume + 部分撤回，0.5 dispatch
- 選項 C：PM Edit ticket close 並建 ANA，0.5 dispatch
- 選項 D：PM Edit ticket AC#3 + complete，0 token

**驗證證據檔**:
- /tmp/w4-005-baseline.log：執行 1 完整輸出（pristine 9/9）
- /tmp/w4-005-after.log：執行 2（重構後 1/9 第一次）
- /tmp/w4-005-after-2.log：執行 3（重構後 1/9 第二次，確認非 transient）
- /tmp/w4-005-pristine-baseline.log：執行 4（stash 後 pristine 確認 9/9）

**本 agent 已完成的動作**:
- 完成 AC#1（timeout 常數化）+ AC#2（reExtract helper）
- 修改檔案：tests/e2e/browser/helpers/{timeouts.js,extraction-flow.js,storage-reader.js} + tests/e2e/browser/extraction-pipeline.e2e.test.js
- Branch feat/0.19.0-W4-005-e2e-timeout-helper 保持未 commit 狀態（依 PM 決策可採 partial revert、補診斷或保留）
- 4 次測試執行皆已留下 log 供 PM 與後續 ticket 參考
## Exit Status

> 注意：本 ticket 經歷兩次 Resume。前次 Exit Status 為「baseline 8/9 起點條件不滿足」（已歸檔下方 _Previous_Exit_Status_）；本次 Resume 後實況變化見當前 Exit Status。

### Current Exit Status (2026-05-31 Resume)

```yaml
status: needs_context
reason: "重構（AC#1 timeout 常數化 + AC#2 reExtract helper）引入穩定 regression：beforeAll 階段「取得 Extension ID 失敗 10000ms」導致 8 個原 GREEN 測試失敗（pristine baseline 為 9/9，重構後兩次重現 1/9）。失敗模式（G1-G5 setup timeout）與 baseline 前次失敗（G4-1 chrome.storage.local undefined）完全不同，違反 AC#3「失敗訊息不變」條件。根因疑與 Node module 載入順序變化、SW launch timing 敏感性相關，但屬 ExtensionTestSetup / service-worker-target.js 層級問題，超出本 ticket where.files 與 how.strategy 範圍（純可讀性重構）。"
confidence: 0.90
acceptance_met: [0, 1]
acceptance_unmet: [2]
artifacts:
  - "tests/e2e/browser/helpers/timeouts.js (新增，5 個具名常數)"
  - "tests/e2e/browser/helpers/extraction-flow.js (引入常數 + 新增 reExtract helper)"
  - "tests/e2e/browser/helpers/storage-reader.js (引入常數)"
  - "tests/e2e/browser/extraction-pipeline.e2e.test.js (引入常數 + G4-1 改用 reExtract / runExtraction)"
context_dependencies:
  - "npm run test:e2e:browser baseline pristine (9/9 GREEN, 10.369s, /tmp/w4-005-baseline.log + /tmp/w4-005-pristine-baseline.log)"
  - "npm run test:e2e:browser 重構後 (1/9 GREEN, 17.x s, 兩次穩定重現 /tmp/w4-005-after.log + /tmp/w4-005-after-2.log)"
  - "ExtensionTestSetup / service-worker-target.js (失敗根源可能在此但超出本 ticket 範圍)"
estimated_recovery_effort: "視 PM 選擇 A/B/C/D 而定：A 派 thyme 診斷 SW launch / require 順序敏感性（1-2 dispatch）；B 部分撤回保留常數 + 撤 helper（0.5 dispatch）；C 全 revert + 建 ANA（0.5 dispatch）；D PM Edit AC#3 後 complete（0 token，但需接受 8 倒退）。本 agent 評估優先 B（保留 AC#1 收益、縮小變更面、便於二分定位）"
```

**本次 Resume 已完成動作**:
- AC#1 達成（timeout 常數化）：新增 `tests/e2e/browser/helpers/timeouts.js`，4 檔字面值替換完成
- AC#2 達成（reExtract helper）：`extraction-flow.js` 新增 reExtract，G4-1 改用後縮為 1 行
- AC#3 未達成：重構引入穩定 regression（兩次重現 1/9）
- 4 次測試執行已留 log，Solution + Test Results 已 append-log
- Branch `feat/0.19.0-W4-005-e2e-timeout-helper` 工作區含 4 檔修改 + 1 新增（未 commit），便於 PM 採選項 B 部分撤回

**等待 PM 決策**: 選項 A/B/C/D 詳見 NeedsContext「[本次 Resume 新狀況]」章節。

### _Previous_Exit_Status_ (前次 Resume 前歸檔)

```yaml
status: needs_context
reason: "Baseline E2E 8/9 GREEN（G4-1 dedup pre-existing 失敗），不滿足 W4-005 AC#3「維持 9/9 GREEN」起點條件；本 ticket 範圍為純可讀性重構（抽常數 + 抽 helper），無法也不應修復 G4 baseline 問題（不在 where.files 與 how.strategy 範圍）"
confidence: 0.95
acceptance_met: []
acceptance_unmet: [0, 1, 2]
```

**前次已完成動作**: 僅讀檔 + baseline 測試一次。未動任何程式碼。Branch 乾淨。

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W1-008 | chars: 741 -->

### Task Reference
- 0.19.0-W1-008 what: 建立 真實瀏覽器 E2E 自動化 regression 測試涵蓋提取-儲存-顯示 pipeline

### Rationale Chain
- 0.19.0-W1-008 why: W1-001.2 實機驗證暴露測試覆蓋缺口：專案 193 單元測試 + tests/e2e/（jest + jsdom + mock Chrome API）+ tests/integration/ 全部通過，但 runtime model gap（提取書物件無 readingStatus / tagIds）未被任何自動化測試攔截，證明無測試斷言真實 Readmoo 頁面提取出的書物件符合 v1.1 model。實機 E2E 驗證目前是 chrome-devtools MCP 一次性手動流程，每次重跑需人工驅動、無 CI regression 防護。W1-006 修復 model gap 後若無真實瀏覽器 regression test，同類提取資料不符 model 的 bug 會無聲復發

### Related Files
- tests/e2e/  # from 0.19.0-W1-008
- jest.e2e.config.js  # from 0.19.0-W1-008

### Source Acceptance (reference)
- [ref] [ ] 真實瀏覽器 E2E 測試可在 CI 以 npm script 執行且不依賴手動 Readmoo 登入  # from 0.19.0-W1-008
- [ref] [ ] 測試斷言提取書物件符合 v1.1 model（readingStatus 為 6 狀態之一且有 tagIds）  # from 0.19.0-W1-008
- [ref] [ ] 測試至少涵蓋 UC-01 提取-storage-顯示 pipeline，UC-02~06 涵蓋範圍經 Phase 1 設計確認  # from 0.19.0-W1-008
- [ref] [ ] 此 E2E 測試作為 regression 鎖定，驗證 W1-009 model 轉換修復在真實瀏覽器環境持續有效（測試 GREEN）  # from 0.19.0-W1-008
- [ref] [ ] 完成一次性 RED 驗證：暫時 revert W1-009 model 轉換層確認測試轉 RED，還原後確認轉 GREEN  # from 0.19.0-W1-008