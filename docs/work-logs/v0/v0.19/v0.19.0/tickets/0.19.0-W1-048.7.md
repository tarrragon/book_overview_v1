---
id: 0.19.0-W1-048.7
title: 收斂 _extractBooksFromData v2 四形狀（DirectArray / WrappedBooks / MetadataWrap /
  空物件）
type: IMP
status: in_progress
version: 0.19.0
wave: 1
priority: P2
parent_id: 0.19.0-W1-048
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 收斂 _extractBooksFromData v2 四形狀（DirectArray / WrappedBooks / MetadataWrap /
  空物件）
when: W1-048.1 + W1-048.5 ImportFlowController 設計完成後
where:
  layer: overview / 匯入流程整體
  files:
  - src/overview/book-file-importer.js
  - tests/unit/overview/book-file-importer-version-detection.test.js
why: W1-048 Phase 4 評估 F10：_extractBooksFromData 四路徑（v1 JSON / v2 四形狀 / 空物件 / 錯誤）認知指數
  11，是合法多型分派但 v2 不應有四種合法形狀——屬歷史包袱（IMP-A~B 演進中為容錯加入的形狀辨識）。v2 應只有一種合法形狀以降低分支複雜度。風險中：改動會破壞既有
  JSON 容錯
how:
  task_type: Implementation
  strategy: (1) 對照 SPEC-EXPORT-V2 確認 v2 唯一合法 export 形狀；(2) 評估其他三形狀（DirectArray / WrappedBooks
    / MetadataWrap）是否屬於合法 user input 或可拒絕；(3) 加入 regression test 鎖定保留行為；(4) 移除非必要分支，保留
    v1 路徑與唯一 v2 路徑；(5) 確認既有測試與實機行為不變
acceptance:
- '[ ] v2 形狀收斂為唯一合法路徑'
- '[ ] 加入 regression test 鎖定保留 / 拒絕行為'
- '[ ] 認知指數降至 ≤ 8'
- '[ ] 既有測試全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-24T02:45:17'
completed_at: null
created: '2026-05-23'
updated: '2026-05-23'
---

# Execution Log

## Task Summary

收斂 _extractBooksFromData v2 四形狀（DirectArray / WrappedBooks / MetadataWrap / 空物件）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### WRAP 答案（claim 前置）

**W（Widen）— 候選方案**：
1. 「四形狀辨識」改為「版本明確分流」（v1 / v2 / CSV / 空 / throw），認知指數從 11 降至 ~6（用戶決策，由 lavender Phase 1 定義細則）
2. 保留四形狀但合併「DirectArray + WrappedBooks」為單一陣列辨識（小幅改善，未根除歷史包袱）
3. 不重構，僅補測試鎖定（不符合 W1-048 F10 收斂目標）

**A（Attain distance）— 機會成本**：
本 ticket 是 W1-048 spawn IMP，估約 30-50K token。同 wave 競爭目標 W1-049（覆蓋模式空檔案二次確認，P2）、W1-058（補測試，P2）。但 F10 屬「v2 應只有一種合法形狀」設計缺陷，越拖越難在 v2.0 多書城支援時收斂；現在做最便宜。

**P（Prepare to be wrong）— 最可能失敗**：
MetadataWrap 形狀（`{data: [...]}`）spec 未定義，移除可能破壞既有 user export 檔案匯入。**防護**：先做歷史檔案存在性確認（grep tests/fixtures 是否使用 metadataWrap），若無則改 console.warn + 拒絕；若有則 spec 補定義並保留。

### 問題根因

`_extractBooksFromData` 行 605-639 走四路徑分流，但實際語意與 SPEC-EXPORT-V2 §2.1 / §3.1 不一致：

| 現況路徑 | 觸發條件 | 對應 spec 語意 |
|---------|---------|--------------|
| 1. v1 JSON converter | `fileFormat='json' && detectFormatVersion==='v1'` | 合法 v1（spec §2.1 Rule 3/4） |
| 2.a DirectArray | Array.isArray(data) | 已被 detectFormatVersion Rule 3 攔截走 v1，**dead branch for JSON** |
| 2.b WrappedBooks | data.books is array | v2 合法形狀 / v1 退化（spec §3.1 / §2.1 Rule 4） |
| 2.c MetadataWrap | data.data is array | **spec 未定義**（歷史包袱，IMP-A~B 容錯加入） |
| 3. 空物件 {} | Object.keys(data).length === 0 | v1 退化合理（合法空匯入） |
| 4. throw | 其他 | 合理錯誤契約 |

實際命中分布：
- JSON 路徑 v1 → 路徑 1（converter）
- JSON 路徑 v2（合法）→ 路徑 2.b WrappedBooks
- JSON 路徑 metadataWrap → 路徑 2.c（**僅此一個情境為 path 2 unique 貢獻**）
- CSV 路徑 → 路徑 2.a DirectArray（fileFormat='csv' bypass detectFormatVersion）
- 空 JSON {} → 路徑 3
- 其他 → 路徑 4

F10「v2 應只有一種合法形狀」實意：v2 path 應由 detectFormatVersion='v2' 顯式分流（如同 v1 path 1），而非依靠形狀辨識 fallthrough。MetadataWrap 屬無 spec 依據的歷史容錯，應決策保留或拒絕。

### 影響範圍

- `src/overview/book-file-importer.js`（行 605-713：_extractBooksFromData + 三個形狀辨識 helper）
- `tests/unit/overview/book-file-importer-version-detection.test.js`（既有版本偵測測試需確認不破）
- `tests/unit/overview/book-file-importer-import-result.test.js`（既有 ImportResult 契約測試需確認不破）
- 規格對齊：`docs/spec/export-interchange-format-v2.md` §2.1（偵測規則）+ §3.1（v2 根結構）
- CSV 路徑：行 605 `fileFormat === 'json'` 條件保證 CSV bypass，重構需維持此 invariant

### 相關 Error Pattern

- W1-048 Phase 4 評估 F10（_extractBooksFromData 四路徑認知指數 11）
- W1-048 Phase 4 評估 F11（CSV 不走版本偵測，已由 W1-048.3 補測試）
- feedback_spec_reference_stability（規格文件穩定性，本 ticket 應對齊 SPEC-EXPORT-V2）

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 規格決策報告

#### 決策矩陣（3 決策 × 候選 × Why / Consequence）

##### 決策 A：MetadataWrap (`{data: [...]}`) 形狀處置

| 選項 | Why（採納理由） | Consequence（後果） |
|------|---------------|-------------------|
| A1 保留（現況） | TC-05 已明確標記「關鍵回歸防護」（W1-047.1 commit 8d891cd2），用戶 export 歷史可能含此形狀；移除即破壞回歸 | v2 path 永遠至少 2 種合法形狀（WrappedBooks + MetadataWrap），認知指數降幅受限；spec 未定義屬「容錯黑箱」 |
| A2 移除 | 對齊 SPEC-EXPORT-V2 §2.1（無 Rule 對應 `data.data`）；減少分支；v2 唯一合法形狀目標達成 | TC-05 必失敗，違反「關鍵回歸防護」字面承諾；user historical export 若存在此形狀直接 throw |
| **A3 保留 + console.warn（採納）** | (1) TC-05 字面要求 `_extractBooksFromData` 對 `{data: [...]}` 不拋錯且 `result.books === data.data`，無法移除；(2) spec 確實未定義，warn 標記「歷史相容路徑」可觀測；(3) fixtures 無此形狀（grep 確認），代表非 PROP-007 後產出，純歷史 user export | 認知指數降幅較 A2 小（保留 helper），但符合 quality-baseline 規則 3 不延後設計問題 + observability-rules 規則 1（降級事件可見） |

**採納 A3**：MetadataWrap 屬「spec 未定義的歷史相容路徑」，保留 helper 並在命中時 `console.warn` 標記「相容路徑（非 v2 spec 形狀）」。TC-05 斷言 `result.books === data.data` 與 not.toThrow() 不變，僅新增 warn 可觀測訊號。未來 v2.1 spec 可決議移除。

##### 決策 B：v2 path 顯式化

| 選項 | Why | Consequence |
|------|-----|------------|
| B1 維持 fallthrough（現況） | 改動最小 | v2 與 metadataWrap / DirectArray-for-CSV 共用形狀辨識，語意混雜，F10「v2 應只有一種合法形狀」未解 |
| **B2 顯式 v2 path（採納）** | 對齊 v1 path 1 已 `detectFormatVersion === 'v1'` 顯式分流的設計；使「v2 合法形狀 = WrappedBooks」可在程式碼結構上看見；認知負擔由「形狀辨識→反推版本」改為「版本→形狀」單向 | 新增一個 if-branch，但消除 2.a DirectArray dead branch（detectFormatVersion Rule 3 已將純陣列攔截為 v1）；fallthrough 容錯區塊縮小為「MetadataWrap + 空物件 + throw」三種非 spec 路徑 |

**採納 B2**：v2 path 由 `detectFormatVersion === 'v2'` 顯式進入，取 `data.books`（spec §3.1 唯一合法位置）。v2 path 內 `data.books` 必為陣列（spec §2.3 驗證清單；若非陣列則 detectFormatVersion 不會回 'v2'）。

##### 決策 C：CSV path bypass 重構（先 fileFormat 分流 再 detectFormatVersion）

| 選項 | Why | Consequence |
|------|-----|------------|
| C1 維持 `fileFormat === 'json'` 條件單點 gate | 改動最小 | CSV 仍走 v2 path fallthrough（觸發 _isDirectArrayFormat），CSV 語意散落兩處 |
| **C2 頂層先 fileFormat 分流（採納）** | 程式碼結構即文件：讀者第一眼看到 `if (fileFormat === 'csv')` → CSV path 獨立，再讀 JSON 子流程；消除 CSV 走 v2 形狀辨識的隱性耦合；F11 CSV 不走版本偵測在程式碼結構上顯式 | 多一層巢狀但每層責任單一；CSV path 變為「直接取陣列 + 空 tag 區段」單一表達式 |

**採納 C2**：頂層 `if (fileFormat === 'csv')` 直接回傳 `{ books: Array.isArray(data) ? data : [], tagCategories: [], tags: [] }`。JSON 子流程再分 v1 / v2 / 空物件 / MetadataWrap 相容 / throw。

---

#### 新 path 結構偽碼

```javascript
_extractBooksFromData(data, fileFormat = 'json') {
  // 頂層分流 1：CSV — 不走版本偵測（F11 顯式化）
  if (fileFormat === 'csv') {
    return {
      books: Array.isArray(data) ? data : [],
      tagCategories: [],
      tags: []
    }
  }

  // 頂層分流 2：JSON — 走版本偵測
  const version = detectFormatVersion(data)

  // JSON path A：v1 — converter 完整三區段
  if (version === 'v1') {
    const converted = convertV1ToV2Data(data)
    return {
      books: converted.books,
      tagCategories: this._normalizeToArray(converted.tagCategories),
      tags: this._normalizeToArray(converted.tags)
    }
  }

  // JSON path B：v2 — spec §3.1 唯一合法形狀（data.books）
  if (version === 'v2') {
    return {
      books: data.books,
      tagCategories: this._extractTagSection(data, 'tagCategories'),
      tags: this._extractTagSection(data, 'tags')
    }
  }

  // JSON path C：MetadataWrap 歷史相容（spec 未定義，保留為相容路徑）
  if (this._isMetadataWrapFormat(data)) {
    console.warn('[book-file-importer] 偵測到 metadata-wrap 形狀（非 SPEC-EXPORT-V2 定義），走歷史相容路徑')
    return {
      books: data.data,
      tagCategories: this._extractTagSection(data, 'tagCategories'),
      tags: this._extractTagSection(data, 'tags')
    }
  }

  // JSON path D：空 JSON 物件（v1 退化合理）
  if (data && typeof data === 'object' && Object.keys(data).length === 0) {
    return { books: [], tagCategories: [], tags: [] }
  }

  // JSON path E：throw — 既有錯誤契約（零回歸）
  const error = new Error('JSON 檔案應該包含一個陣列或包含books屬性的物件')
  error.code = ErrorCodes.VALIDATION_ERROR
  error.details = { category: 'validation' }
  throw error
}
```

**helper 處置**：
- `_isDirectArrayFormat`：**移除**（detectFormatVersion Rule 3 已將純陣列判為 v1，dead branch）
- `_isWrappedBooksFormat`：**移除**（v2 path 改為依 `detectFormatVersion === 'v2'`，不再形狀辨識；TC-04 透過 detectFormatVersion Rule 2 命中 v2）
- `_isMetadataWrapFormat`：**保留**（path C 唯一使用者，標註為「歷史相容判定」）

---

#### ImportResult 三區段提取規則對應表

| Path | 觸發條件 | books 來源 | tagCategories 來源 | tags 來源 | warn? |
|------|---------|-----------|-------------------|-----------|-------|
| CSV | `fileFormat === 'csv'` | `Array.isArray(data) ? data : []` | `[]` | `[]` | 否 |
| JSON-v1 | `fileFormat==='json' && detectFormatVersion==='v1'` | `convertV1ToV2Data(data).books` | `_normalizeToArray(converted.tagCategories)` | `_normalizeToArray(converted.tags)` | 否 |
| JSON-v2 | `detectFormatVersion==='v2'` | `data.books`（spec §3.1 保證為陣列） | `_extractTagSection(data, 'tagCategories')` | `_extractTagSection(data, 'tags')` | type 錯誤時 _extractTagSection 內 warn |
| JSON-MetadataWrap | `_isMetadataWrapFormat(data) === true` | `data.data` | `_extractTagSection(data, 'tagCategories')` | `_extractTagSection(data, 'tags')` | **是**（標記歷史相容路徑） |
| JSON-EmptyObject | `Object.keys(data).length === 0` | `[]` | `[]` | `[]` | 否 |
| JSON-Unrecognized | 其他 | throw VALIDATION_ERROR | — | — | — |

**INV-1 維持**：所有 return path 三欄位恆為陣列，throw path 不回傳。

---

#### 認知指數預估

| 維度 | 現況 | 新設計 |
|------|------|--------|
| 函式分支數（return / throw 路徑） | 5（v1 / v2-4shape / empty / throw + 嵌入式 4-shape if-chain） | 6（CSV / v1 / v2 / metadataWrap / empty / throw），但每分支單一條件平鋪 |
| 巢狀深度 | 3（外 if / 內 4-shape if-else chain / 內部三元） | 2（頂層 if 平鋪 / 內部單 ternary） |
| 形狀辨識 helper 用量 | 3（DirectArray / WrappedBooks / MetadataWrap） | 1（MetadataWrap） |
| 隱性 invariant（讀者需記住） | 「CSV 透過 fileFormat 條件繞過 v1，再 fallthrough 至 DirectArray」「DirectArray 對 JSON 是 dead branch」 | 「CSV 頂層分流」「v2 唯讀 data.books」「MetadataWrap 是 spec 外相容路徑」皆顯式 |
| 認知指數估算（變數 + 分支 + 巢狀 + 依賴） | ~11（acceptance 起點） | ~6-7（< 8 滿足 acceptance） |

**符合 cognitive-load 規則**：分支數雖增 1，但巢狀降低、helper 減少 2、隱性 invariant 全部顯式化，整體閱讀路徑為線性「先 format 再 version」單向決策樹。

---

#### Phase 2 / Phase 3b handoff 提示

- **Phase 2 (sage)**：新增 TC 鎖定 (i) MetadataWrap 命中時 console.warn 被呼叫一次；(ii) v2 path 走 `detectFormatVersion === 'v2'` 而非形狀辨識（可透過 mock detectFormatVersion 驗證）；(iii) CSV 頂層分流不呼叫 detectFormatVersion；既有 TC-01~TC-05 全部維持綠燈
- **Phase 3b (thyme)**：依新 path 結構偽碼實作；移除 `_isDirectArrayFormat` / `_isWrappedBooksFormat` 兩個 helper；保留 `_isMetadataWrapFormat` 並更新 JSDoc 標註「歷史相容判定，非 SPEC-EXPORT-V2 形狀」
- **Phase 4**：評估是否將 MetadataWrap warn 升級為 AppLogger（若專案已導入），以及是否在下版本 spec 補定義或正式 deprecate

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

```yaml
status: success
reason: "Phase 1 規格決策完成；3 個必答問題（A MetadataWrap 處置 / B v2 path 顯式化 / C CSV bypass 重構）皆已產出採納選項與 Why/Consequence；Solution 含決策矩陣 + 新 path 結構偽碼 + ImportResult 對應表 + 認知指數預估；handoff 提示已標註 Phase 2/3b 範圍邊界。"
confidence: 0.9
acceptance_met: []  # Phase 1 不勾選 acceptance；acceptance 由 Phase 3b 實作後驗證
acceptance_unmet:
  - "v2 形狀收斂為唯一合法路徑（待 Phase 3b 實作）"
  - "加入 regression test 鎖定保留 / 拒絕行為（待 Phase 2 測試設計）"
  - "認知指數降至 ≤ 8（預估新設計 ~6-7，待 Phase 3b 驗證）"
  - "既有測試全綠（待 Phase 3b 驗證）"
artifacts:
  - "docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W1-048.7.md (Solution 章節)"
context_dependencies:
  - "docs/spec/export-interchange-format-v2.md §2.1 §2.3 §3.1"
  - "src/overview/book-file-importer.js:605-713 (_extractBooksFromData + 3 helpers)"
  - "tests/unit/overview/book-file-importer-version-detection.test.js:113-125 (TC-05 關鍵回歸防護)"
  - "ticket Problem Analysis (PM 已補完研究材料)"
estimated_recovery_effort: ""
```

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending