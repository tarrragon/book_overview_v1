---
id: 0.19.0-W1-050.6
title: 修復 W1-050 ANA 遺漏的 process.cwd/platform/stderr 5 處違規（spawn-F）
type: IMP
status: pending
version: 0.19.0
wave: 1
priority: P1
parent_id: 0.19.0-W1-050
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W1-050.3
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: Phase 3b 實作（補 W1-050 ANA 遺漏）
  rationale: quality-baseline-rule-5
who:
  current: thyme-extension-engineer
  history: {}
what: 修復 W1-050 ANA 遺漏的 process.cwd/platform/stderr 5 處違規（spawn-F）
when: W1-050.3 PM 前置 grep 發現後，可獨立於 W1-050.3 進行
where:
  layer: 待定義
  files:
  - src/popup/diagnostic-module.js
  - src/content/detectors/page-detector.js
  - src/core/migration/MigrationProgressTracker.js
  - src/core/migration/StandardErrorMigrationAnalyzer.js
  - src/core/migration/MigrationValidator.js
why: W1-050.3 PM 前置 grep -rn 'process\.' src/ 發現 5 處 W1-050 ANA 範圍外違規：popup/content
  runtime 直接觸發崩潰（必修）；migration 系列 3 處屬 dev tool 路徑（需評估 runtime 觸發機率）
how:
  task_type: Implementation
  strategy: 依檔案分類處理：(1) popup/diagnostic-module.js L1053 process.platform → navigator.userAgentData
    或 navigator.platform fallback；(2) content/page-detector.js L64-65 process.stderr.write
    → console.warn 或 AppLogger；(3) migration 3 檔 process.cwd() 先評估 runtime 觸發路徑——若僅
    build/CI 使用則改 typeof guard 包裝避免 runtime 崩潰；若會進 CE bundle 則改 location.href base
    或 chrome.runtime.getURL fallback
acceptance:
- '[ ] popup/diagnostic-module.js L1053 process.platform 改 navigator 等效 API（含 typeof
  guard）'
- '[ ] content/page-detector.js L64-65 process.stderr.write 改 console 或 AppLogger'
- '[ ] migration 3 檔 process.cwd() 評估完成，紀錄是否進 CE bundle，必要時加 guard'
- '[ ] npm test 涵蓋 5 檔的相關測試全綠'
- '[ ] grep -rn ''process\.'' src/ 結果僅剩 process.env（spawn-B 處理範圍）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-05-24'
updated: '2026-05-24'
---

# Execution Log

## Task Summary

修復 W1-050 ANA 遺漏的 process.cwd/platform/stderr 5 處違規（spawn-F）

---

## Problem Analysis

### 問題根因

W1-050.3 PM 前置 grep 發現 5 處 W1-050 ANA 範圍外的 process.* 違規。雖然 popup/content 兩處已有 typeof guard（CE runtime 不會崩潰），但仍出現在 build/development bundle，違反 W1-050 「process.* 全清」AC 標準；migration 3 檔的 process.cwd() 無 guard 且 build 包進 bundle，CE runtime 若實例化這些類別會 ReferenceError。

PM 驗證（已完成）：build/development/src/core/migration/ 確認含 MigrationProgressTracker / MigrationValidator / StandardErrorMigrationAnalyzer 三檔，bundle 內有 process.cwd 殘留。

### 影響範圍（5 檔 5 hits）

| 檔案 | 行號 | 當前 | 既有 guard | 處理方式 |
|------|------|------|----------|---------|
| `src/popup/diagnostic-module.js` | L1053 | `: process.platform` | navigator typeof | 移除 process.platform fallback，改 `'unknown'` literal |
| `src/content/detectors/page-detector.js` | L64-65 | `process.stderr.write(...)` | typeof process | 移除整塊（Jest debug 改 console.warn 或刪除） |
| `src/core/migration/MigrationProgressTracker.js` | L43 | `\|\| process.cwd()` | 無 | 加 typeof process guard fallback `'/'` |
| `src/core/migration/MigrationValidator.js` | L41 | `\|\| process.cwd()` | 無 | 同上 |
| `src/core/migration/StandardErrorMigrationAnalyzer.js` | L61 | `\|\| process.cwd()` | 無 | 同上 |

### 轉換規則細節

**A. popup/diagnostic-module.js L1053**
```js
// 改前：platform: typeof navigator !== 'undefined' ? navigator.platform : process.platform,
// 改後：platform: typeof navigator !== 'undefined' ? navigator.platform : 'unknown',
```

**B. content/page-detector L64-65**
```js
// 改前（保留 typeof guard 但 Jest debug 已過時）：
// if (typeof process !== 'undefined' && process.stderr) {
//   process.stderr.write(`PageDetector Debug: ${JSON.stringify(locationInfo)}\n`)
// }
// 改後：直接移除整塊。Jest 環境 debug 可用 console.warn 或保留為註解
```

**C. migration 3 檔 process.cwd()**
```js
// 改前：options.projectRoot || process.cwd()
// 改後：options.projectRoot || (typeof process !== 'undefined' && process.cwd ? process.cwd() : '/')
```

註：migration 類別在 CE runtime 不會主動實例化（屬 dev tool 路徑），typeof guard 是保險帶。Bundle 內仍有 `process.cwd` 字串殘留但被 guard 包裹，不執行。

### 相關 Error Pattern

- W1-050 ANA：CE-Node 環境前提誤判 anti-pattern（本 ticket 為 ANA 遺漏補丁）
- W1-050.3.1 已驗證：error-handling 模組 process.* 替換工作流可參考

---

## Context Bundle

### 派發前提

- W1-050.3 group 完成（C1+C2 已合併）
- W1-050.4 spawn-D 完成（crypto 已替換）
- 本 ticket 為 spawn-F，獨立可 dispatch
- isolation: worktree

### 變更範圍（嚴格遵守）

僅修改 5 檔：
- `src/popup/diagnostic-module.js`
- `src/content/detectors/page-detector.js`
- `src/core/migration/MigrationProgressTracker.js`
- `src/core/migration/MigrationValidator.js`
- `src/core/migration/StandardErrorMigrationAnalyzer.js`

對應測試檔（若存在）需檢視；若測試 mock process.cwd 或 process.platform，可能需更新。

### Commit Policy（PC-092）

git add 明示 5 src + test + ticket md，禁 `.` / `-A`。Commit message 前綴 `feat(0.19.0-W1-050.6):`。

### 驗證指令

```bash
# 驗證 1：grep 確認 src/ 無 process.cwd/platform/stderr（typeof guard 包裹的可保留並註解）
grep -rnE "process\.(cwd|platform|stderr)" src/

# 驗證 2：測試全綠
npm test -- --testPathPattern='(diagnostic-module|page-detector|MigrationProgressTracker|MigrationValidator|StandardErrorMigrationAnalyzer)'

# 驗證 3：build 成功
npm run build:dev

# 驗證 4：bundle 殘留分類（typeof guard 包裹可保留）
grep -rnE "process\.(cwd|platform|stderr)" build/development/ | grep -v "typeof process"
```

### 環境注意

- 磁碟 1.8 GiB，緊但夠
- 禁止 `cd`（PC-156 cwd 漂移），用 `git -C`

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending
