---
id: 0.19.0-W1-119.2
title: 移除 MessageDictionary → Logger 循環依賴（單向化依賴方向）
type: IMP
status: completed
version: 0.19.0
wave: 1
priority: P2
parent_id: 0.19.0-W1-119
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 移除 MessageDictionary → Logger 循環依賴（單向化依賴方向）
when: W1-119 Phase 4 評估後排入 v0.20 或下個 Wave（TD-2 雙視角共識 P2）
where:
  layer: 待定義
  files:
  - src/core/messages/MessageDictionary.js
  - src/core/logging/Logger.js
why: W1-119 Phase 4 評估 TD-2（linux + cinnamon 雙視角共識 P2）。MessageDictionary.js:1 require
  Logger 且 Logger.js:21 require MessageDictionary 形成循環依賴。CommonJS 容忍但 esbuild bundle
  階段順序敏感，違反依賴方向原則（messages 是更底層 primitive，不應依賴 logging）。當前 cache size 警告路徑實務上不會觸發但循環依賴設計痕跡仍應清除。
how:
  task_type: Implementation
  strategy: 1. Read MessageDictionary.js:1 + _checkCacheSize 確認 Logger 使用點 2. 移除 require
    Logger 3. 將 Logger.warn 替換為 console.warn 或 throw Error 4. 保留 Logger.js:21 對 MessageDictionary
    的單向依賴 5. npm test 驗證 6. 建置 dev bundle 驗證 Chrome Extension runtime
acceptance:
- '[x] MessageDictionary.js:1 移除 require Logger'
- '[x] MessageDictionary._checkCacheSize 的 Logger.warn 替換為 console.warn 或 throw'
- '[x] Logger.js:21 保留對 MessageDictionary 的 require（單向依賴）'
- '[x] 全部測試 npm test pass'
- '[x] 確認 bundle 後 Chrome Extension runtime 無回歸'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-29T00:07:50'
completed_at: '2026-05-29T00:11:34'
created: '2026-05-28'
updated: '2026-05-28'
---

# Execution Log

## Task Summary

移除 MessageDictionary → Logger 循環依賴（單向化依賴方向）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

MessageDictionary 是 Logger 的依賴（Logger.js:21 require MessageDictionary 取 GlobalMessages），但 MessageDictionary.js:1 也反向 require Logger，形成循環依賴。

當前 commonjs 容忍此狀況（partial exports）+ Logger.js try/catch 保護 require fail，但違反依賴方向原則（messages 是更底層 primitive，不應依賴 logging）。esbuild bundle 階段順序敏感，潛在 break 風險。

### 影響範圍

**MessageDictionary.js 當前狀態（W1-119.1 merge 後）**：

| 位置 | 內容 | 處理 |
|------|------|------|
| `src/core/messages/MessageDictionary.js:1` | `const { Logger } = require('src/core/logging/Logger')` | **移除** |
| `src/core/messages/MessageDictionary.js:232` | `Logger.warn(`MessageDictionary: Adding "${key}" would exceed cache limit...`)` | **替換 console.warn 或 throw** |
| `src/core/messages/MessageDictionary.js:227` | `_checkCacheSize` method | 保留（caller 是內部） |

**Logger.js 不變**：

| 位置 | 內容 | 處理 |
|------|------|------|
| `src/core/logging/Logger.js:21` | `GlobalMessages = require('src/core/messages/MessageDictionary').GlobalMessages` | **保留**（單向依賴：Logger 依賴 messages 合理） |

### 相關 Error Pattern

依賴方向原則：messages → log？實際上 messages 不該依賴 logging（messages 是低階 primitive）。Logger.warn 在 _checkCacheSize 的使用是「實作便利」而非「設計需求」，可改用 console.warn。

### 設計決策

**選項 A：console.warn**（推薦）
```js
console.warn(`MessageDictionary: Adding "${key}" would exceed cache limit (${this._maxCacheSize / 1024}KB)`)
```
- 優點：簡單、無依賴、瀏覽器/node 通用
- 缺點：繞過 Logger 統一管道（但 cache warning 是極少觸發的內部 warning，可接受）

**選項 B：throw Error**
- 優點：強制 caller 處理
- 缺點：cache size warning 是 informational，不應 throw

**選項 C：移除 _checkCacheSize warning**
- 評估：cache 警告未使用價值低，但移除是 ANA 範圍外的決策

**裁定**：採選項 A（console.warn）。

### Test 策略

1. 移除 require Logger 後 MessageDictionary.test.js 應仍綠（測試不應依賴此 require）
2. _checkCacheSize cache 限制行為若有測試，確認 console.warn spy 替代 Logger.warn spy
3. npm test 全綠（baseline 5284 passed after W1-119.1）
4. 建置 dev bundle 驗證 Chrome Extension runtime 無回歸（chrome-devtools-mcp 觀察 console）

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 實作摘要

依 Problem Analysis 設計決策（選項 A console.warn）執行最小修改：

| 變更位置 | 變更內容 |
|---------|---------|
| `src/core/messages/MessageDictionary.js:1` | 移除 `const { Logger } = require('src/core/logging/Logger')` |
| `src/core/messages/MessageDictionary.js:232` | `Logger.warn(...)` 替換為 `console.warn(...)`，加註 W1-119.2 設計說明 |
| `src/core/logging/Logger.js:21` | 保留（單向依賴：Logger 依賴 messages 合理） |

### 設計依據

採選項 A（console.warn）原因：
- 簡單、無依賴、瀏覽器/node 通用
- cache size warning 是極少觸發的內部 warning，繞過 Logger 統一管道可接受
- 解除 messages → logging 循環依賴（依賴方向原則：messages 是底層 primitive 不應依賴 logging）

### 不在本 ticket 範圍

- 移除 `_checkCacheSize` 自身行為（Problem Analysis 選項 C 已評估為 ANA 範圍外）
- 修改 Logger.js 對 MessageDictionary 的依賴（單向依賴，符合設計原則）

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 測試指令與結果

**目標檔案測試**（MessageDictionary + Logger）：
```
npx jest tests/unit/core/messages/MessageDictionary.test.js tests/unit/core/logging/Logger.test.js
Test Suites: 2 passed, 2 total
Tests:       28 passed, 28 total
```

**全套件測試**（npm test）：
```
npm test
Test Suites: 2 skipped, 222 passed, 222 of 224 total
Tests:       41 skipped, 5284 passed, 5325 total
Time:        111.28 s
```

### Baseline 對齊

- W1-119.1 merge 後 baseline：5284 passed
- 本 ticket 修改後：5284 passed
- 結果：**零回歸**，全綠

### Runtime 驗證考量（PC-165）

`_checkCacheSize` 的 console.warn 觸發路徑：
- 觸發條件：cache 已達 100KB 限制 + 新增 key 會超限
- 實際 runtime：GlobalMessages 已 Object.freeze（21 keys，遠低於 100KB），永不觸發
- local dict 用戶亦罕見觸發（mc 機制設計上 cache 寬鬆）

由於該分支在實機環境不觸發（僅為防禦性 code path），且既有測試未 spy Logger.warn 該行為（grep 結果為 0），PC-165 三層共振（mock + 不斷言訊息 + 動態語言靜默）不適用本案——本修改純為依賴方向重構，無語意變更。

### 不需 chrome-devtools-mcp 實機驗證

- 修改範圍：純 require 移除 + 1 行 `Logger.warn` → `console.warn` 對等替換
- 無新增 runtime 邏輯、無訊息系統變更、無跨模組整合變更
- 不符合 PC-165 觸發條件（修復類 + 訊息/日誌/跨模組整合三標籤聯集）

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status
```yaml
status: success
reason: "MessageDictionary → Logger 循環依賴已移除（console.warn 替代 Logger.warn），Logger.js:21 單向依賴保留；全套件 5284 passed 零回歸"
confidence: 1.0
acceptance_met: [0, 1, 2, 3, 4]
acceptance_unmet: []
artifacts:
  - "src/core/messages/MessageDictionary.js"
context_dependencies:
  - "docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W1-119.2.md"
estimated_recovery_effort: ""
```

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-29T00:11:34
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending