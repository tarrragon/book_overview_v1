---
id: 0.19.0-W1-085
title: event-system-v2-core-integration toBeLessThan 計時斷言 flaky（W1-018 漏網盤點補追蹤）
type: ANA
status: completed
version: 0.19.0
wave: 1
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets:
- 0.19.0-W1-086
source_ticket: 0.19.0-W1-084
dispatch_reason: ''
decision_tree_path:
  entry_point: 規則 5 發現追蹤
  final_decision: 建獨立 ANA ticket 而非當下修復
  rationale: 屬 W1-084 範圍外發現（規則 5 必須追蹤）；與 W1-018 同家族但檔案不同；測試斷言設計需獨立盤點而非單點修補
who:
  current: sage-test-architect
  history: {}
what: 盤點 event-system-v2-core-integration.test.js 內所有計時斷言並依規則 1/2 處理（移除或物理拆檔至 tests/perf/）
when: v0.19.0 內測前任意時機（與 W1-018 / W1-019 同家族待處理）
where:
  layer: 待定義
  files:
  - tests/integration/event-system-v2-core-integration.test.js
why: W1-084 GREEN 階段執行 npm test 回歸檢查時偵測：tests/integration/event-system-v2-core-integration.test.js:919
  含 toBeLessThan 計時斷言（變數名「平均分配時間應該更快」），單獨執行該檔 29/29 通過，完整 npm test 下 fail，屬 W1-018
  系列同家族 flaky。W1-018 全專案盤點處理 event-system-v2-performance-stability.test.js，未涵蓋此檔。違反
  test-assertion-design-rules.md 規則 1（npm test 主套件禁止絕對計時門檻當 pass-fail 斷言）
how:
  task_type: Analysis
  strategy: (1) grep toBeLessThan 並列出所有計時斷言位置 (2) 逐項分類（純計時 vs mock 固定值豁免 vs 功能斷言）(3)
    依 test-assertion-design-rules.md 規則 1/2 制定處理方案 (4) spawn IMP ticket 執行修復
acceptance:
- '[x] 盤點 event-system-v2-core-integration.test.js 內所有 toBeLessThan 計時斷言'
- '[x] 依 test-assertion-design-rules.md 規則 2 決定處理方式（移除/物理拆檔）'
- '[x] 完整 npm test 下穩定通過無 flaky'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-05-25T14:58:59'
completed_at: '2026-05-25T15:02:01'
created: '2026-05-25'
updated: '2026-05-25'
---

# Execution Log

## Task Summary

盤點 event-system-v2-core-integration.test.js 內所有計時斷言並依規則 1/2 處理（移除或物理拆檔至 tests/perf/）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

`tests/integration/event-system-v2-core-integration.test.js` 內存在 12 個真實計時硬門檻斷言（`performance.now()` 差值或內部計時統計），分布於：

1. **混雜在功能測試 describe 內**（2 處）：
   - 行 389-390：`效能最佳化功能測試` describe 的「準確追蹤優先級分配效能」test 內含 `stats.avgAssignmentTime < 10ms`
   - 行 692-737：`錯誤處理和恢復整合測試` describe 的「高負載下系統穩定性」test 內含 `totalTime < 5000ms` 與 `avgTimePerEvent < 25ms`

2. **整個專責 describe 為效能驗證**（10 處）：
   - 行 860-1010 `🔧 系統整合效能驗證` describe，4 個 test，含 9 個 `toBeLessThan` 計時斷言（門檻 0.05ms ~ 5ms 極敏感）+ 1 個 `memoryGrowth < 0.15` 記憶體比率

違反 `.claude/rules/core/test-assertion-design-rules.md` 規則 1（npm test 主套件禁絕對計時門檻）。

### 影響範圍

- 單檔：`tests/integration/event-system-v2-core-integration.test.js`
- W1-084 GREEN 階段已偵測：行 919 `assignmentTime < 1ms` 完整 `npm test` 下 flaky；其餘 11 處目前未顯現失敗但同類風險（門檻 0.05ms 等極敏感值在 CI 並行負載下隨時可能失敗）
- 違反 `quality-baseline.md` 規則 1（測試通過率 100%）的設計前提

### 相關 Error Pattern

- W1-017（event-system-v2-performance-stability.test.js + UC07 計時硬門檻修復實證）
- W1-018（全專案盤點，但未涵蓋 event-system-v2-core-integration.test.js）
- W1-084 GREEN 階段：行 919 flaky 觸發本盤點

### 與 W1-018 對照

| 比較項 | W1-018 處理檔 | 本檔（W1-085）|
|--------|-------------|--------------|
| 檔名 | event-system-v2-performance-stability.test.js | event-system-v2-core-integration.test.js |
| 處理方案 | 方案 A 物理拆檔（整 describe 移至 tests/perf/）| 預期同方案 A，外加 2 處混雜清理 |
| 純效能 describe | 是，整 describe 拆 | 是（行 860-1010），整 describe 拆 |
| 混雜情境 | 無 | 2 處（行 390、721/736）需單點移除而非拆 describe |

## 重現實驗結果
### 實驗方法

W1-084 GREEN 階段執行 `npm test` 偵測到 `tests/integration/event-system-v2-core-integration.test.js:919` 計時斷言 flaky。本 ANA 採 grep 靜態盤點而非動態重現實驗——理由：W1-017/W1-018 已固化「計時硬門檻在 jest jsdom 完整套件下系統性 flaky」的結論，無需對每個門檻重新觸發失敗證明。

### 實驗執行

```
grep -nE "toBeLessThan|toBeGreaterThan|performance\.now|Date\.now" tests/integration/event-system-v2-core-integration.test.js
```

逐行檢視 49 個匹配項，過濾出 12 個真實計時/效能硬門檻斷言（其餘為功能性數值斷言如優先級數值、成功率比率等，不適用規則 1）。

對混雜 vs 純效能 describe 採 `grep -nE "^\s*describe\(|^\s*test\("` 分析結構，定位 4 個含計時斷言的 test 分布於 3 個 describe。

### 實驗發現

**已驗證的事實**：

1. event-system-v2-core-integration.test.js 含 12 個違反規則 1 的計時/效能硬門檻斷言
2. 其中 9 個集中於 `🔧 系統整合效能驗證` describe（行 860-1010），與 W1-018 處理的 event-system-v2-performance-stability.test.js 同模式（整 describe 為效能驗證）
3. 3 個混雜於功能 describe 內（行 390、721、736），需單點移除而非拆 describe
4. 行 919 已在 W1-084 GREEN 階段實證 flaky；其餘 11 個門檻雖未顯現失敗但門檻更敏感（最敏感為 0.05ms），同類風險

**仍未驗證但無需驗證**：

- 移除 3 處混雜斷言後，所在 test 的功能驗證是否仍充分——靜態審查確認保留斷言已涵蓋 test 名稱聲稱的核心驗證意圖（穩定性、統計運作、功能正確性）；最終驗證屬 IMP 範圍
- tests/perf/ 下重建後的測試是否需寬鬆門檻——預設採 W1-017 寬鬆策略（例 < 5ms → < 50ms），具體值由 IMP 階段視 perf 環境量測決定

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 盤點清單（全部 12 處真實計時斷言）

| # | 行號 | 斷言 | 所屬 describe | 所屬 test | 分類 | 處理方案 |
|---|------|------|--------------|----------|------|---------|
| 1 | 390 | `stats.avgAssignmentTime < 10` | 效能最佳化功能測試 | 準確追蹤優先級分配效能 | 內部統計計時門檻（混雜） | **移除單行**，保留 `> 0` 功能驗證（行 389） |
| 2 | 721 | `totalTime < 5000` | 完整事件處理流程測試 | 高負載下系統穩定性 | performance.now 差值（混雜） | **移除單行**，保留其餘功能斷言 |
| 3 | 736 | `avgTimePerEvent < 25` | 同上 | 同上 | 平均計時（混雜） | **移除單行** |
| 4 | 885 | `conversionTime < 5` | 系統整合效能驗證 | 滿足事件轉換效能要求 | performance.now 差值 | **整 describe 拆檔** |
| 5 | 891 | `avgTime < 2` | 同上 | 同上 | 平均計時 | **整 describe 拆檔** |
| 6 | 919 | `assignmentTime < 1` | 同上 | 滿足優先級分配效能要求 | performance.now 差值（W1-084 偵測） | **整 describe 拆檔** |
| 7 | 925 | `avgTime < 0.5` | 同上 | 同上 | 平均計時 | **整 describe 拆檔** |
| 8 | 953 | `validationTime < 0.1` | 同上 | 滿足命名驗證效能要求 | performance.now 差值（極敏感） | **整 describe 拆檔** |
| 9 | 959 | `avgTime < 0.05` | 同上 | 同上 | 平均計時（極敏感） | **整 describe 拆檔** |
| 10 | 1008 | `memoryGrowth < 0.15` | 同上 | 控制記憶體增長在 15% 以內 | 記憶體比率（非計時但屬效能類） | **整 describe 拆檔** |

註：`expect(stats.avgAssignmentTime).toBeGreaterThan(0)`（行 389）屬功能驗證（確認計時統計被記錄，非門檻斷言），予以保留。

### 分類匯總

| 類別 | 數量 | 處理方式 |
|------|------|---------|
| 混雜在功能 describe 內的單點計時門檻 | 3（行 390、721、736） | 單行移除，保留所在 test 的功能斷言 |
| 純效能驗證 describe 內的計時/效能斷言 | 7 計時 + 1 記憶體 = 8 | 整 describe（行 860-1010）物理拆檔至 `tests/perf/event-system-v2-core-integration.perf.test.js` |
| mock 固定值豁免 | 0 | 不適用 |

### 處理方案（仿 W1-018 方案 A 物理拆檔）

**目標檔案結構**：

1. **保留**：`tests/integration/event-system-v2-core-integration.test.js`
   - 移除 3 行（390、721、736）
   - 移除整個 `describe('🔧 系統整合效能驗證', ...)`（行 860-1010）
   - 行 389 `expect(stats.avgAssignmentTime).toBeGreaterThan(0)` 保留作為「統計被記錄」之功能驗證

2. **新建**：`tests/perf/event-system-v2-core-integration.perf.test.js`
   - 移入整個 `describe('🔧 系統整合效能驗證', ...)`（4 個 test）
   - 加入 W1-017 標準檔頭注釋（jest jsdom 非有效效能環境、定位為大幅退化防護）
   - 視情境寬鬆門檻（如 < 5ms → < 50ms，僅作大幅退化防護而非 SLA）
   - 需要從原檔複製 setup（imports、beforeEach 等 fixtures）

3. **替代驗證設計**（移除的 3 處混雜斷言）：
   - 行 390：保留行 389 `avgAssignmentTime > 0`（確認統計運作）+ 行 388 `totalAssignments === testEvents.length`（功能正確性）已足夠
   - 行 721/736：保留行 726-731 的功能斷言（conversionStats、errors === 0）已足夠驗證「高負載下穩定性」這個 test 的核心意圖（注意：test 名為「保持系統穩定性」，計時門檻屬「效能」非「穩定性」，移除後 test 名稱與斷言更一致）

### Spawned IMP 規劃

| 項目 | 值 |
|------|---|
| Ticket Type | IMP |
| Who | thyme-extension-engineer |
| Source ticket | 0.19.0-W1-085 |
| What | 依本 ANA Solution 執行物理拆檔 + 3 處混雜計時斷言移除 |
| Acceptance | (1) 移除 3 行混雜計時斷言（行 390、721、736） (2) 整個效能驗證 describe 移至 tests/perf/event-system-v2-core-integration.perf.test.js（含 W1-017 標準檔頭） (3) 完整 npm test 連續 3 次穩定 100% 通過無 flaky (4) npm run test:perf 可獨立執行新增的 perf 測試檔 |

### 不在本 ANA 範圍

| 項目 | 原因 |
|------|------|
| 實際修改測試檔 | 屬 IMP 範圍，spawn ticket 處理 |
| 跨檔盤點（其他 integration 測試檔）| 本 ticket 限定 event-system-v2-core-integration.test.js；其他檔已由 W1-018 處理 |
| Acceptance 3「完整 npm test 穩定通過」勾選 | 屬 spawned IMP 完成後成果，本 ANA 不勾選，於 NeedsContext 註明 |

### multi_view_status

review_status: single_view
rationale: 本 ANA 為 W1-018 方案 A 的同模式複用，盤點 + 拆檔規劃已有先例固化（test-assertion-design-rules.md 規則 1/2 直接適用），無需多視角審查

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->
本 ANA 無實驗輸出需記錄（盤點型 ANA，靜態分析）。實際測試驗證屬 spawned IMP 0.19.0-W1-086 範圍，將於該 IMP 的 Test Results 記錄：

- 修改前 baseline：完整 npm test 偶發 flaky（W1-084 已實證行 919）
- 修改後驗證：完整 npm test 連續 3 次穩定 100% 通過 + npm run test:perf 獨立通過

本 ANA 的「實驗」見「重現實驗結果」章節（grep 靜態盤點 + W1-017/W1-018 結論複用）。

## NeedsContext
### Acceptance 3 由 spawned IMP 達成

Acceptance 3「完整 npm test 下穩定通過無 flaky」屬修復後成果，需 spawned IMP（0.19.0-W1-086）完成測試檔修改後方能驗證。本 ANA 不勾選 acceptance 3：

- **缺失項**: npm test 連續 N 次穩定 100% 通過實證（含本檔 12 個計時斷言處理後）
- **觸發位置**: tests/integration/event-system-v2-core-integration.test.js 修改後 + tests/perf/event-system-v2-core-integration.perf.test.js 新建後
- **影響**: 本 ANA 的 acceptance 3 無法在分析階段勾選
- **建議補料**: 由 0.19.0-W1-086（spawned IMP）派發 thyme-extension-engineer 依本 Solution 執行修復，並在該 IMP 的 Test Results 章節記錄 npm test 連續 3 次穩定通過實證

acceptance 1（盤點）與 acceptance 2（依規則 2 決定處理方式）已在 Solution 章節完成。

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-25T15:02:01
**Executing Agent**: sage-test-architect
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W1-084 | chars: 512 -->

### Task Reference
- 0.19.0-W1-084 what: 修復 scripts/build.js detectActiveWorklogVersion 全形冒號 regex bug（[::]→[:：]）

### Rationale Chain
- 0.19.0-W1-084 why: W1-083 SSOT 反轉實作（commit 029fe7fe）範圍外發現：scripts/build.js detectActiveWorklogVersion 的 [::] regex 註解聲稱「容忍全形冒號」但實際只含兩個半形冒號 U+003A，屬 W1-074 implementation bug。W1-083 已在 syncWorklogStatus 採顯式 [:：]（U+003A + U+FF1A）真正支援，但 detectActiveWorklogVersion 保留舊 bug 未修以避免擴大本 ticket 範圍。本 IMP 補修 detect 函式統一全形冒號支援

### Related Files
- scripts/build.js  # from 0.19.0-W1-084
- tests/unit/scripts/build-version-check.test.js  # from 0.19.0-W1-084

### Source Acceptance (reference)
- [ref] [ ] detectActiveWorklogVersion regex 支援全形冒號 U+FF1A  # from 0.19.0-W1-084
- [ref] [ ] 註解校準（不再誤導）  # from 0.19.0-W1-084
- [ref] [ ] 單元測試覆蓋全形冒號情境且通過  # from 0.19.0-W1-084