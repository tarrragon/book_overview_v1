---
id: 0.19.0-W1-048
title: Phase 4 重構評估 UC-04 匯入功能整體（W1-047.1~.5）
type: ANA
status: completed
version: 0.19.0
wave: 1
priority: P2
parent_id: null
children:
- 0.19.0-W1-048.1
- 0.19.0-W1-048.2
- 0.19.0-W1-048.3
- 0.19.0-W1-048.4
- 0.19.0-W1-048.5
- 0.19.0-W1-048.6
- 0.19.0-W1-048.7
- 0.19.0-W1-048.8
- 0.19.0-W1-048.9
- 0.19.0-W1-048.10
blockedBy:
- 0.19.0-W1-047.2
- 0.19.0-W1-047.3
- 0.19.0-W1-047.4
- 0.19.0-W1-047.5
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W1-047.1
dispatch_reason: ''
decision_tree_path:
  entry_point: 執行中發現:IMP-A Phase 4 跳過
  final_decision: 建 ANA 統一評估 UC-04 匯入功能 Phase 4
  rationale: quality-baseline 規則 2 + 規則 5
who:
  current: linux
  history: {}
what: Phase 4 重構評估 UC-04 匯入功能整體（W1-047.1~.5）
when: W1-047.2~.5（IMP-B~E）實作完成後
where:
  layer: overview / 匯入流程整體
  files:
  - src/overview/book-file-importer.js
  - src/overview/overview-page-controller.js
why: IMP-A（W1-047.1）TDD Phase 3b 完成後 thyme 直接 complete，跳過 Phase 4 重構評估（quality-baseline
  規則 2）。IMP-A~E 為 UC-04 匯入功能的連貫拆分，宜於 5 IMP 全數完成後對整體匯入流程做一次 Phase 4 多視角重構評估，補回 IMP-A
  跳過的評估並涵蓋 IMP-B~E
how:
  task_type: Implementation
  strategy: 派 parallel-evaluation 對 UC-04 匯入功能整體（BookFileImporter + overview-page-controller
    匯入流程）做多視角 Phase 4 重構評估，產出重構需要/技術債務/品質結論
acceptance:
- '[x] 對 UC-04 匯入功能整體完成 Phase 4 多視角重構評估（parallel-evaluation）'
- '[x] 產出 Phase 4 評估報告（重構需要/技術債務/程式碼品質結論）'
- '[x] 明確涵蓋 IMP-A（W1-047.1）跳過的 Phase 4 評估範圍'
tdd_phase: null
tdd_stage: []
hook_self_reference: phase4-decision-enforcement
assigned: true
creation_accepted: false
started_at: '2026-05-23T06:25:39'
completed_at: '2026-05-24T19:52:59'
created: '2026-05-23'
updated: '2026-05-23'
---

# Execution Log

## Task Summary

Phase 4 重構評估 UC-04 匯入功能整體（W1-047.1~.5）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### WRAP 答案（claim 前置）

**W（Widen）— 候選方案**：
1. linux + cinnamon-refactor-owl + bay-quality-auditor 三視角並行（情境 B 重構評估，含 Redundancy/Coupling/Complexity，用戶選擇）
2. linux + 2x code-reviewer 同質視角（SKILL.md 情境 B 範例形態，未採用 — 同質視角覆蓋面較窄）
3. PM 單視角審查（**禁止** — 違反 `feedback_phase4_multi_perspective` + `feedback_parallel_eval_needs_wrap`）
4. 跳過 Phase 4（**禁止** — 違反 quality-baseline 規則 2）

**A（Attain distance）— 機會成本**：
本評估佔用 token 預算約 80-90K。同 wave 競爭目標：W1-001.2（in_progress, context-refresh 待處理）、W1-002.2/.3（ZIP 安裝驗證 + 安裝指南）、W1-003.1（跨裝置匯出入驗證）。但 W1-047.1~.5 剛完成（記憶最新），Phase 4 評估在「實作完即評估」時機最低成本；推遲後 commit diff 與設計脈絡會在 context window 中流失。

**P（Prepare to be wrong）— 最可能失敗**：
3 並行 agent 同時讀 1541 行 overview-page-controller.js + 709 行 book-file-importer.js + W1-047.1~.5 commit diff 可能超 100K context 上限。**防護**：（a）overview-page-controller 只給匯入相關方法清單與行號範圍（不要求讀整檔），（b）W1-047 diff 用 `git log --oneline` 摘要 + 關鍵 commit 的 `--stat`，避免完整 diff dump。

**R（Reality Test）— 真根因驗證**：
- 假設 1：UC-04 匯入流程跨 BookFileImporter (709 lines) + overview-page-controller (1541 lines 中匯入子集)
- 已驗證事實：兩檔存在；W1-047.1~.5 全 completed（10/10 spawned IMP 完成）
- 已驗證範圍：overview-page-controller 匯入相關方法為 handleFileLoad / promptImportMode / _showImportModeModal / _hideImportModeModal / process / _handleFileContent / _validateFileBasics / _validateFileSize / _handleDuplicateBooks（行號 304, 1043-1538 主集中區）
- 仍未驗證假設：BookFileImporter 內部分層（v1→v2 converter / 結構提取 / 模式選擇 / storage 持久化）邊界由 IMP-A~E 演進而成，需在派發 prompt 中明示

### 問題根因

W1-047.1 (IMP-A) TDD Phase 3b 完成後 thyme 直接 complete，跳過 Phase 4 重構評估。違反 quality-baseline 規則 2（Phase 4 不可跳過，即使品質 A+ 也須產出「無需重構」結論）。後續 W1-047.2~.5 為 IMP-B~E 連貫拆分；於 IMP-A~E 5 IMP 全完後做一次整體 Phase 4 多視角評估，補回 IMP-A 跳過的評估範圍並涵蓋 IMP-B~E。

### 影響範圍

- `src/overview/book-file-importer.js`（709 行，BookFileImporter 主類別）
- `src/overview/overview-page-controller.js`（1541 行，匯入相關行區段：304 event binding、1043 handleReload、1074-1169 promptImportMode、1176-1213 modal show/hide、1215 handleFileLoad、1436-1538 process / _handleFileContent / _validateFileBasics / _validateFileSize / _handleDuplicateBooks）

### 相關 Error Pattern

- quality-baseline 規則 2（Phase 4 不可跳過）
- `feedback_phase4_multi_perspective`（Phase 4 強制多視角）
- `feedback_parallel_eval_needs_wrap`（parallel-evaluation 結論需 WRAP 驗證）
- `feedback_subagent_pytest_vs_hook_env` / `feedback_lib_refactor_caller_sync_checklist`（refactor 後 caller sync 風險，本評估若提重構需檢驗 caller 覆蓋）

## 重現實驗結果
### 實驗方法

Phase 4 重構評估的「重現實驗」對應**評估標的可達性驗證**（不是 bug 根因實驗）。三項驗證指令：

```bash
# 1. 確認標的檔案存在與規模
wc -l src/overview/book-file-importer.js src/overview/overview-page-controller.js

# 2. 確認 W1-047.1~.5 全 completed
for id in 0.19.0-W1-047.{1,2,3,4,5}; do ticket track query "$id" | head -1; done

# 3. 確認 overview-page-controller 中匯入相關方法集中區段
grep -n "^  async \|^  handle\|^  _\|^  show\|^  hide\|^  promptImportMode\|importJSON\|importBooks" src/overview/overview-page-controller.js
```

### 實驗執行

| 項目 | 觀察結果 |
|------|---------|
| book-file-importer.js | 709 行（PRESENT，整檔屬匯入流程） |
| overview-page-controller.js | 1541 行（PRESENT，匯入相關方法為子集，行區段 304 + 1043-1538） |
| W1-047.1 | [已完成] 接線 BookFileImporter 版本偵測與 v1 轉換 |
| W1-047.2 | [已完成] BookFileImporter v2 結構提取與回傳介面變更 |
| W1-047.3 | [已完成] UC-04 匯入覆蓋模式與 storage 持久化 |
| W1-047.4 | [已完成] UC-04 匯入合併模式與 tag id 重映射 |
| W1-047.5 | [已完成] UC-04 匯入模式選擇 UI |
| 匯入相關方法集中區段 | 304（event binding）、1043（handleReload）、1074-1169（promptImportMode）、1176-1213（modal show/hide）、1215（handleFileLoad）、1436-1538（process / _handleFileContent / _validateFileBasics / _validateFileSize / _handleDuplicateBooks） |

### 實驗發現

**已驗證的事實**：
- 兩檔案存在且可讀
- W1-047 系列 5 個 IMP 全完，blockedBy 已解除
- overview-page-controller 匯入相關方法佔約 500-700 行（1541 行的子集），其餘屬其他職責（搜尋、表格渲染、儲存事件處理等），評估範圍可清楚界定

**仍未驗證的假設**（交派視角驗證）：
- BookFileImporter 內部分層的邊界品質（v1→v2 converter / 結構提取 / 模式選擇 / storage 持久化）— 由 IMP-A~E 演進而成，需評估是否有冗餘、耦合過深、複雜度過高
- overview-page-controller 中匯入相關方法是否該抽出為獨立模組（cohesion 評估）
- 5 個 IMP 拼接後的整體一致性（介面是否統一、錯誤處理是否一致）

**評估範圍邊界宣告**：
- IN scope：`src/overview/book-file-importer.js` 整檔 + `overview-page-controller.js` 行 304、1043-1213、1215-1538 區段（匯入相關）
- OUT of scope：overview-page-controller 的搜尋、表格渲染、匯出、storage event 處理等非匯入職責方法

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### multi_view_status

scope: multi_view（情境 B 重構評估）
views_executed: linux (universal_lens) / cinnamon-refactor-owl (Redundancy+Coupling) / bay-quality-auditor (Complexity+TD)
basil_opt_out: 是（情境 B 不預設加入 basil）

### 整體評估結論

| 項目 | 結論 |
|------|------|
| 重構需要 | **是（中度，非緊急）**——三視角共識：5 個 IMP 拼接後出現「測試耦合滲漏 + 私有方法跨檔呼叫」單一設計缺陷，與「檔案體量超標」之分離訊號 |
| 程式碼品質結論 | **B+**（linux）/ **B**（bay 整體）/ **是需重構**（cinnamon）→ 綜合 **B+**（非阻擋 v1.0.0 內測，但需建追蹤 ticket） |
| 技術債務累積 | 共 16 項 finding（其中 4 項跨視角重複，去重後 12 項獨立 finding） |
| W1-047.1 跳過 Phase 4 補回 | **遺漏 2 項應修正議題**：CSV bypass 版本偵測缺 regression test、cover 型別契約矛盾（required 寬鬆 vs typed 嚴格） |

### 三視角整合發現清單（去重後）

| ID | 分類 | 來源視角 | 描述 | 改善幅度 | 風險 |
|----|------|---------|------|---------|------|
| F1 | Coupling+TD | linux L3 / cinnamon C2+R1 / bay TD2 | controller 三個 test-only proxy 方法（_handleFileContent / _validateFileBasics / _validateFileSize）僅為測試 spy 而存在，使生產程式碼暴露不必要表面積 | 高 | 中 |
| F2 | Coupling+TD | cinnamon C1 / bay TD1 | handleFileLoad 直接呼叫 importer 私有方法（_validateFileBasics / _readFileWithReader），打穿封裝邊界 | 高 | 中 |
| F3 | Redundancy | cinnamon R2 | DuplicateBookMerger 已注入但 UC-04 新流程改走 TagStorageAdapter.mergeAllData，形成孤兒模組 | 中 | 中 |
| F4 | Complexity | linux L1 / cinnamon C4 / bay C4 | handleFileLoad 7 步驟線性流程，職責數 ≥ 5（協調+持久化決策+UI 更新+錯誤映射） | 中 | 低 |
| F5 | Complexity | cinnamon C5 / bay C3 | promptImportMode 96 行內聯 modal 邏輯（focus trap / Esc / pending 單例 / settle 收斂），controller 承擔了「modal 元件」職責 | 高 | 低 |
| F6 | 架構/拆分 | cinnamon 抽出建議 / bay 拆分建議 #1 | 抽出 `ImportFlowController` 為獨立模組（涵蓋 F1/F2/F4/F5 重構之共同骨架），controller 降至 ~1100 行 | 高 | 中 |
| F7 | 規則違反 | linux L5/L6 / bay TD8 | console.warn/error 含 emoji（3 處：importer:518、controller:1084、importer:614），違反 language-constraints 規則 3 | 低 | 低 |
| F8 | 規則違反 | linux L7 | `_logLoadSuccess` 空方法被呼叫但無實作，無 placeholder 註解，dead/incomplete code | 低 | 低 |
| F9 | 可觀測性 | bay 可觀測性 | book-file-importer:461 JSON parse SyntaxError catch 用 `throw new Error(...)` 替換原 error，遺失 cause 鏈與 stack trace | 低 | 低 |
| F10 | Complexity+TD | bay C2 / linux L2 | `_extractBooksFromData` 四路徑（v1 / v2 四形狀 / 空物件 / 錯誤），認知指數 11；歷史包袱：v2 應只有一種合法形狀 | 中 | 中 |
| F11 | IMP-A 補回 | linux L-IMP-A1 | 版本偵測閘門僅在 JSON 路徑啟用（fileFormat === 'json'），CSV 路徑刻意 bypass 但無 regression test 鎖定行為 | 中 | 低 |
| F12 | IMP-A 補回 | linux L-IMP-A2 | `_validateRequiredFields` 移除 cover 強制（行 690-693）但 `_validateFieldTypes` 仍要求 `typeof book.cover === 'string'`（行 702），selectively-loose validation 矛盾 | 中 | 低 |
| F13 | Complexity | bay C1 | `_parseCSVRows` 手寫 CSV parser 認知指數 12，6 個分支處理引號/逗號/CR+LF/LF/CR/收尾 | 中 | 低 |
| F14 | TD | bay TD3 | 12 處硬編碼中文用戶訊息（importer 8 + controller 匯入區段 4），FILE_CONSTANTS.MESSAGES 定義但未全用 | 中 | 低 |
| F15 | TD | bay TD5 | `_loadFileReaderFactory` `typeof require !== 'undefined'` 分支在 esbuild bundle 環境永遠為 true，window.FileReaderFactory 分支為死碼 | 低 | 低 |
| F16 | TD | cinnamon R3 | controller `_handleFileContent` 代理呼叫 importer `_handleFileContent` 時未傳 fileFormat，預設 'json'，CSV 路徑訊號遺失 | 低 | 低 |

### Worth-It Filter 分類

| 類別 | Finding | 處理方向 |
|------|---------|---------|
| 立即執行（P1） | F1+F2+F16 | 同根因「測試耦合 + 私有方法跨檔」，合併為單一 IMP ticket |
| 立即執行（P1） | F7+F8+F9 | quality-baseline 規則 3 立即修正類（規則違反 + dead code + 可觀測性） |
| 立即執行（P1） | F11 | IMP-A 補回 — CSV bypass regression test |
| 立即執行（P1） | F12 | IMP-A 補回 — cover 型別契約釐清 |
| 延後執行（P2） | F6（涵蓋 F4+F5） | 抽出 ImportFlowController 設計評估（ANA），規模大需獨立設計 |
| 延後執行（P2） | F3 | DuplicateBookMerger 孤兒模組確認與處置（先 grep 確認 caller） |
| 延後執行（P2） | F10 | `_extractBooksFromData` v2 形狀收斂（風險中，需 regression test） |
| 延後執行（P3） | F13+F15 | `_parseCSVRows` 認知簡化 + `_loadFileReaderFactory` 死碼清理（同檔，可合併） |
| 延後執行（P2） | F14 | i18n 匯入流程硬編碼字串列入專案級 i18n 技術債 backlog |

### W1-047.1（IMP-A）跳過 Phase 4 補回明確結論

| 議題 | 結論 | 落地 ticket |
|------|------|------------|
| 版本偵測 CSV bypass 缺 regression test | **應補測試**：在 W1-048 之 spawn IMP-3 中加入 CSV input 進入 `_extractBooksFromData` 的 regression test，鎖定「CSV 路徑不走版本偵測」行為 | 見 spawn 規劃 IMP-3 |
| cover 型別契約矛盾（required 寬鬆 vs typed 嚴格） | **應釐清**：決策方向為「真寬鬆」（允許 cover undefined）或「真嚴格」（required 加回 cover），不可保持矛盾狀態 | 見 spawn 規劃 IMP-4 |
| 其他 IMP-A 範圍事項 | **已在 IMP-B~E 自然演進中被掩蓋或解決**（v1→v2 converter 接線、ImportResult 介面、tag 區段處理） | 無遺漏 |

### Spawn 規劃（落地 ticket 表格）

| 序號 | Type | Priority | 標題 | 涵蓋 Finding | spawned_ticket_id（complete 前回填） |
|-----|------|----------|------|------------|----------------------------------|
| IMP-1 | IMP | P1 | 消除 BookFileImporter 私有方法跨檔呼叫 + 移除 controller test-only proxy | F1+F2+F16 | 0.19.0-W1-048.1 |
| IMP-2 | IMP | P1 | 修正匯入流程規則違反項（emoji 3 處 + _logLoadSuccess 空方法 + JSON parse cause 鏈） | F7+F8+F9 | 0.19.0-W1-048.2 |
| IMP-3 | IMP | P1 | IMP-A 補回 — 加入 CSV 不走版本偵測閘門的 regression test | F11 | 0.19.0-W1-048.3 |
| IMP-4 | ANA | P1 | IMP-A 補回 — 釐清 cover 型別契約（寬鬆 or 嚴格）並落地 | F12 | 0.19.0-W1-048.4 |
| ANA-5 | ANA | P2 | 抽出 ImportFlowController 設計評估（涵蓋 modal 邏輯 + handleFileLoad 流程 + 持久化分流） | F6（含 F4+F5） | 0.19.0-W1-048.5 |
| ANA-6 | ANA | P2 | DuplicateBookMerger 孤兒模組確認與處置（grep caller → 刪除或保留決策） | F3 | 0.19.0-W1-048.6 |
| IMP-7 | IMP | P2 | _extractBooksFromData v2 四形狀收斂（保留 v1 路徑、確認 v2 唯一合法形狀） | F10 | 0.19.0-W1-048.7 |
| IMP-8 | IMP | P3 | book-file-importer.js 認知簡化（_parseCSVRows 抽 helper + _loadFileReaderFactory 死碼清理） | F13+F15 | 0.19.0-W1-048.8 |
| DOC-9 | DOC | P2 | 列匯入流程 12 處硬編碼中文進 i18n 技術債 backlog（依 i18n-checker skill） | F14 | 0.19.0-W1-048.9 |

### Out-of-Scope 觀察（非匯入流程，不建 spawn ticket）

| 觀察 | 來源 | 處置 |
|------|------|------|
| overview-page-controller.js 1541 行整檔超標，5 個 domain 混雜 | bay 檔案體量風險 | 已有 W1-039（收斂 detectPageType 重複邏輯）等既有 ticket 處理 controller 拆分，本評估不重複建立 |
| `_formatBookRowData.cover` 與 `createBookRow` cover DOM API 雙重渲染（bay TD6） | bay TD6 | 屬表格渲染 domain，非匯入流程，不在本 ticket 範圍 |
| applyCurrentFilter / createBookRow / setupEventListeners 認知超標（bay C5/C6/C7） | bay Complexity | 同上，屬其他 domain |

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-24T19:52:59
**Executing Agent**: linux
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W1-047.1 | chars: 440 -->

### Task Reference
- 0.19.0-W1-047.1 what: 接線實作 BookFileImporter 版本偵測與 v1 轉換接線

### Rationale Chain
- 0.19.0-W1-047.1 why: W1-001.2.1 實機驗證證實 UC-04 匯入功能未依 SPEC-EXPORT-V2 實作（BookFileImporter 無版本偵測/v1 轉換/tag 處理/合併覆蓋模式/storage 持久化；SPEC 模組 format-version-detector 與 v1-to-v2-converter 存在但零非測試消費者）。UC-04 匯入為 v1.0.0 內測「匯出入正常」硬需求，匯出端 UC-03 已驗證 PASS 但匯入端缺失，功能不對稱

### Related Files
- tests/unit/overview/book-file-importer-version-detection.test.js  # from 0.19.0-W1-047.1
- tests/unit/overview/overview-import-private-methods.test.js  # from 0.19.0-W1-047.1
- tests/unit/overview/overview-import-function.test.js  # from 0.19.0-W1-047.1
### 評估範圍

**IN scope**（兩檔的匯入相關部分）：
- `src/overview/book-file-importer.js` 整檔（709 行，BookFileImporter 主類別）
- `src/overview/overview-page-controller.js` 匯入相關區段：
  - 行 304 — `importJSONBtn` 事件綁定
  - 行 1043-1213 — `handleReload` / `promptImportMode` / `_showImportModeModal` / `_hideImportModeModal`
  - 行 1215-1538 — `handleFileLoad` / `process` / `_handleFileContent` / `_validateFileBasics` / `_validateFileSize` / `_handleDuplicateBooks`

**OUT of scope**：overview-page-controller 的搜尋、表格渲染、匯出、storage event 處理等非匯入職責方法。

### W1-047 演進路徑（IMP-A~E）

| IMP | Ticket | Commit | 內容 |
|-----|--------|--------|------|
| A | W1-047.1 | 8d891cd2 | BookFileImporter 接入 v1 版本偵測與轉換接線 |
| B | W1-047.2 | 77f84503 | BookFileImporter v2 結構提取與回傳介面變更 |
| C | W1-047.3 | f8068c2c | UC-04 匯入覆蓋模式與 storage 持久化 |
| D | W1-047.4 | 243f2da5 | UC-04 合併模式與 tag id 重映射 |
| E | W1-047.5 | 244752cb | UC-04 匯入模式選擇 UI |

**累積變更 stat**：
- `book-file-importer.js`: +165 / -34（淨 +131 行）
- `overview-page-controller.js`: +225 / -34（淨 +191 行）

注意：IMP-A（W1-047.1）TDD Phase 3b 完成後 thyme 直接 complete 跳過 Phase 4 評估，違反 quality-baseline 規則 2；本 ticket 補回 IMP-A 並涵蓋 IMP-B~E 整體。

### 評估視角分工（情境 B 重構評估）

| Agent | 視角 | 重點 |
|-------|------|------|
| linux | universal_lens（Good Taste / 架構複雜度元維度） | 評估整體品味、是否有 over-engineering、控制流複雜度、是否違反 quality-baseline 規則 |
| cinnamon-refactor-owl | Redundancy / Coupling | 評估 5 個 IMP 拼接後的重複邏輯、模組間耦合、是否該抽出獨立模組 |
| bay-quality-auditor | Complexity / 技術債務 | 評估認知負擔指數、檔案體量風險（overview-page-controller 1541 行已超 300 行警戒線）、未來維護風險 |

### Phase 3 PM 收尾責任

各 agent 產出個別報告後，PM 執行 Phase 3：
1. 彙整三視角發現
2. Worth-It Filter 分類（高/中/低 改善幅度 × 風險）
3. 對每個「延後執行」項目 `ticket create` 並填入報告 Ticket ID
4. 結構性發現 → `/error-pattern add`
5. 寫入 ticket Solution 章節
6. 補回 IMP-A 跳過範圍的明確結論