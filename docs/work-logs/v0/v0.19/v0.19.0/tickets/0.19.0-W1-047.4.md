---
id: 0.19.0-W1-047.4
title: 實作 UC-04 匯入合併模式與 tag id 重映射
type: IMP
status: in_progress
version: 0.19.0
wave: 1
priority: P2
parent_id: 0.19.0-W1-047
children: []
blockedBy:
- 0.19.0-W1-047.3
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 實作 UC-04 匯入合併模式與 tag id 重映射
when: IMP-C 覆蓋模式與 storage 持久化完成後
where:
  layer: 待定義
  files:
  - src/overview/overview-page-controller.js
why: W1-001.2.1 實機驗證證實 UC-04 匯入功能未依 SPEC-EXPORT-V2 實作（BookFileImporter 無版本偵測/v1 轉換/tag
  處理/合併覆蓋模式/storage 持久化；SPEC 模組 format-version-detector 與 v1-to-v2-converter 存在但零非測試消費者）。UC-04
  匯入為 v1.0.0 內測「匯出入正常」硬需求，匯出端 UC-03 已驗證 PASS 但匯入端缺失，功能不對稱
how:
  task_type: Implementation
  strategy: 實作 SPEC-EXPORT-V2 §8.2 合併邏輯：books 同 id tagIds 取聯集、tag/category 同名比對保留本地版本+不存在則新建、匯入
    id 重映射至本地 id。詳見 W1-047 Solution R6
acceptance:
- '[ ] 合併模式 books 同 id 的 tagIds 取聯集'
- '[ ] tag/category 同名比對保留本地版本，不存在則新建'
- '[ ] 匯入 tag/category id 重映射至本地 id'
- '[ ] 對應單元測試 100% 通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-23T05:14:21'
completed_at: null
created: '2026-05-23'
updated: '2026-05-23'
current_phase: Phase 3a
assignee: pepper-test-implementer
---

# Execution Log

## Task Summary

實作 UC-04 匯入合併模式與 tag id 重映射

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 功能規格摘要（lavender-interface-designer）

完整規格：`docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W1-047.4-feature-spec.md`（Full 模式 6 區段，/spec validate 2 次迭代收斂、0 未回答問題）。

#### 範圍

實作 SPEC-EXPORT-V2 §8.2 合併模式與 §8.3 tag/category id 重映射。與 IMP-C 覆蓋模式對稱：storage 層新增 mergeAllData（合併），與既有 replaceAllData（覆蓋）介面對稱。模式選擇 UI 屬 IMP-E，不在本 ticket——本 ticket 僅提供可被 IMP-E 呼叫的合併 API。

#### 架構決策

- storage 層新增 mergeAllData({ books, tags, tagCategories })：與 replaceAllData 對稱。合併「讀本地三 key → 計算 → 原子寫回」全程在 operationLock 內，複用 withAtomicRollback / checkQuotaLevel。回傳 { success, error?, counts?, remap? }。
- 內部純函式 computeMergeResult(local, incoming)：合併計算與 storage I/O 解耦，使 books 聯集 / 同名比對 / id 重映射可獨立於 chrome.storage mock 單元測試。新建 id 透過注入的 id 產生器提供（純函式不可內呼 Date.now/Math.random）。
- id 重映射兩階段：先 category（建 categoryIdMap）後 tag（以「重映射後本地 categoryId + name.toLowerCase」為鍵）。多個匯入 category 收斂至同一本地 category 時其下同名 tag 亦收斂。
- 合併不修復本地破損：本地既有孤兒 tagId 在聯集後原樣保留；引用完整性修復屬既有 checkReferentialIntegrity 職責。

#### 核心行為（14 GWT 場景）

books 同 id tagIds 聯集去重 / 新 id 加入 / tag·category 同名（大小寫不敏感）保留本地、不存在則新建 / id 重映射貫穿 category→tag→book 三層 / 原子持久化 / 配額攔截 / 寫入失敗回滾 / 空匯入合法 / 兩階段收斂 / 不修復本地孤兒。

#### 驗收條件

AC-1~AC-17（細化 IMP-D 4 條 acceptance）。詳見 feature-spec §6。

#### Phase 2 移交重點

雙層測試（computeMergeResult 純函式 + mergeAllData storage）；id 產生器注入可測；同名大小寫差異獨立 TC；三層 id 一致性核心 TC；原子回滾對齊 IMP-C TC-S8/S9。

### Phase 2 測試案例設計（sage-test-architect）

完整 14 GWT 場景、AC-1~17 的測試案例設計。雙層測試（純函式 `computeMergeResult` + storage API `mergeAllData`）。

#### 測試策略

| 項目 | 決策 |
|------|------|
| 分層 | `computeMergeResult` Layer 5（純資料轉換，複雜邏輯）→ 純單元測試，無 chrome.storage mock；`mergeAllData` Layer 4（storage 公開 API）→ sociable 單元測試，有狀態 store mock |
| Mock 策略 | `computeMergeResult` 完全無 mock，注入可預測 id 產生器；`mergeAllData` 沿用 `tag-storage-replace-all-data.test.js` 的 `createStorageMock`（有狀態 store + setHook + bytesInUse），不 mock 內部純函式 |
| id 產生器 | 注入式可預測產生器（如 `() => 'cat_test_1'` 序列），斷言 remap 映射表內容；禁止測試斷言真實 `Date.now()` 衍生 id |
| 測試檔案 | 新增兩檔，置於 `tests/unit/`：(1) `tests/unit/storage/merge-all-data-compute.test.js`（純函式）(2) `tests/unit/storage/adapters/tag-storage-merge-all-data.test.js`（storage API，與 replace-all-data 同目錄對稱） |
| 斷言規則 | 遵循 test-assertion-design：無 `toBeLessThan` 計時硬門檻；並發以執行順序 / 最終狀態斷言；無 `toBeCloseTo`（本 ticket 無浮點計算） |
| 既有測試影響 | `mergeAllData` 為純新增 API，不改 `replaceAllData` 與 CRUD 簽名 → 預期 `tag-storage-replace-all-data.test.js` 與既有 CRUD 測試零影響；controller 不接線（IMP-E 才接）→ 預期 `overview-page-controller.test.js` 零影響。Phase 3b 須跑全套件 `npm test` 確認 100% 綠 |

#### 行為鏈推演說明

`computeMergeResult` 為核心。行為鏈：`category 同名比對 → 建 categoryIdMap`（前置：incoming categories 已知）→ `tag 同名比對（鍵 = 重映射後 categoryId + name.toLowerCase）→ 建 tagIdMap`（前置：categoryIdMap 已完成）→ `book tagIds 透過 tagIdMap 重映射 + 本地聯集`（前置：tagIdMap 已完成）。每層測試前先斷言上一階段映射表已正確建立，再驗證本層輸出。`mergeAllData` 行為鏈：`配額前置攔截 → 讀本地三 key → 呼叫 computeMergeResult → withAtomicRollback 原子寫回`。

---

#### 測試群組 A：computeMergeResult — books 同 id tagIds 聯集（功能職責：書籍合併。AC-1, AC-2, AC-16）

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-C1 | 同 id tagIds 聯集去重 | 本地 `book-001` tagIds `[t_local_a, t_local_b]`；incoming `book-001` tagIds `[t_imp_x, t_imp_y]`，`t_imp_x` 重映射對應 `t_local_b`、`t_imp_y` 為新 tag → 重映射為 `t_local_new` | computeMergeResult | result.books 中 `book-001` tagIds `toEqual` `[t_local_a, t_local_b, t_local_new]`（聯集且去重，順序：本地在前、新增在後）；`t_local_b` 不重複 |
| TC-C2 | 同 id 其餘欄位以匯入覆蓋 | 本地 `book-001` title `"舊"`；incoming `book-001` title `"新"` progress `50` | computeMergeResult | `book-001` title `toBe "新"`、progress `toBe 50`；tagIds 仍為聯集 |
| TC-C3 | 新 id 直接加入 | 本地有 `book-001`、`book-002`；incoming 有 `book-001`、`book-999`（本地無） | computeMergeResult | result.books 含 `book-001`(合併)、`book-002`(保留)、`book-999`(新增)，長度為 3；`book-002` 原樣保留 |
| TC-C4 | 本地空、incoming 非空 | 本地 books `[]`；incoming 有 `book-001` | computeMergeResult | result.books `toEqual` incoming books（tagIds 經重映射） |
| TC-C5 | 本地既有孤兒 tagId 原樣保留（AC-16） | 本地 `book-001` tagIds 含 `t_orphan`（指向不存在本地 tag）；incoming `book-001` tagIds `[t_imp_a]` | computeMergeResult | `book-001` tagIds `toContain t_orphan` 且含重映射後 `t_imp_a`；`t_orphan` 不被移除（合併不修復本地破損） |

#### 測試群組 B：computeMergeResult — category 同名比對與重映射（功能職責：category 重映射。AC-5, AC-6）

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-C6 | category 同名保留本地 | 本地 category `cat_local_1` name `"書籍類型"`；incoming category id `c_imp` name `"書籍類型"` | computeMergeResult | result.tagCategories 中 `"書籍類型"` id `toBe cat_local_1`（屬性保留本地）；`remap.categoryIdMap` 含 `c_imp → cat_local_1`；無第二個 `"書籍類型"` |
| TC-C7 | category 不存在則新建 | 本地無 `"閱讀清單"`；incoming category id `c_imp` name `"閱讀清單"` | computeMergeResult（注入 id 產生器回 `cat_test_1`） | result.tagCategories 新增 name `"閱讀清單"` id `cat_test_1`（新建本地 id，非 `c_imp`）；`remap.categoryIdMap` 含 `c_imp → cat_test_1` |
| TC-C8 | category 大小寫不敏感同名 | 本地 category name `"SciFi"`；incoming category name `"scifi"` | computeMergeResult | 兩者視為同名（`toLowerCase` 比對），保留本地 `"SciFi"`，不新建 |

#### 測試群組 C：computeMergeResult — tag 同名比對與重映射（功能職責：tag 重映射。AC-3, AC-4, AC-7, AC-15）

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-C9 | tag 同 category 同名保留本地 | 本地 category `cat_local_1` 下 tag `t_local_sci` name `"科幻"`；incoming 同名 category 下 tag id `t_imp_sci` name `"科幻"` | computeMergeResult | result.tags 中 `"科幻"` id `toBe t_local_sci`（createdAt/sortOrder 等屬性保留本地）；`remap.tagIdMap` 含 `t_imp_sci → t_local_sci`；無重複 `"科幻"` tag |
| TC-C10 | tag 大小寫不敏感同名（場景 4 易漏邊界） | 本地 tag name `"SciFi"`；incoming 同 category 下 tag name `"scifi"` | computeMergeResult | 視為同名保留本地 `"SciFi"`，不新建第二個 |
| TC-C11 | tag 不存在則新建並重映射 | 本地無 `"歷史"` tag；incoming tag id `t_imp_his` name `"歷史"`，其 category 重映射對應某本地 category | computeMergeResult（注入 id 產生器回 `tag_test_1`） | result.tags 新增 `"歷史"` id `tag_test_1`（新建，非 `t_imp_his`）；`categoryId` 指向重映射後本地 category id；`remap.tagIdMap` 含 `t_imp_his → tag_test_1` |
| TC-C12 | 同名比對鍵含 categoryId（不同 category 同名 tag 不誤判） | 本地 category X 下 tag `"通用"`；incoming category Y（重映射後為不同本地 category）下 tag `"通用"` | computeMergeResult | incoming `"通用"` 不誤判為同名本地 tag，新建本地 tag（鍵 = categoryId + name，非僅 name） |

#### 測試群組 D：computeMergeResult — 三層 id 重映射貫穿（功能職責：跨層一致性。AC-8。核心 TC） — 場景 7

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-C13 | category→tag→book 三層 id 一致重映射 | incoming category `c_imp`、tag `t_imp`(`categoryId: c_imp`)、book `book-001`(`tagIds: [t_imp]`)；本地皆無同名 | computeMergeResult（注入序列產生 `cat_test_1`、`tag_test_1`） | 新建 category id `cat_test_1`；新建 tag id `tag_test_1` 且 `categoryId` `toBe cat_test_1`（非 `c_imp`）；`book-001` tagIds `toEqual [tag_test_1]`（非 `t_imp`）；`remap.categoryIdMap` 含 `c_imp → cat_test_1`、`remap.tagIdMap` 含 `t_imp → tag_test_1` |
| TC-C14 | 兩階段順序：先 category 後 tag | incoming category 同名映射至本地 + tag 新建 | computeMergeResult | tag 的 `categoryId` 為「重映射後本地 categoryId」（驗證 tag 階段消費已完成的 categoryIdMap） |

#### 測試群組 E：computeMergeResult — 兩階段收斂（功能職責：收斂去重。AC-15） — 場景 13

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-C15 | 兩個 incoming category 收斂至同一本地 category | 本地 category `"自訂標籤"`；incoming 兩個 category id `c_imp_1`、`c_imp_2` 皆 name `"自訂標籤"` | computeMergeResult | `remap.categoryIdMap` 中 `c_imp_1`、`c_imp_2` 皆映射至同一本地 category id |
| TC-C16 | 收斂後其下同名 tag 不重複 | 承 TC-C15，`c_imp_1` 下 tag `"科幻"`、`c_imp_2` 下也 tag `"科幻"`；本地該 category 無 `"科幻"` | computeMergeResult | result.tags 僅出現一個 `"科幻"` tag（鍵 = 重映射後本地 categoryId + name，兩個 incoming `"科幻"` 收斂單次新建）；`tagIdMap` 中兩個 incoming tag id 皆映射至同一本地 tag id |

#### 測試群組 F：computeMergeResult — 邊界與防禦處理（功能職責：輸入容錯。AC-14。§4 錯誤處理）

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-C17 | book 缺 id 跳過 | incoming books 含一筆無 `id` 欄位 | computeMergeResult | 該書不進 result.books；其餘書籍正常合併；（Phase 3b 須以 `jest.spyOn(console, 'warn')` 斷言 warn 被呼叫含元件名 `[tag-storage-adapter]`） |
| TC-C18 | book tagIds 含無法重映射的 id 過濾 | incoming `book-001` tagIds 含一個既非本地 tag、又不在 incoming tags 的孤兒 id | computeMergeResult | 該孤兒 id 不出現在 result `book-001` tagIds（僅過濾匯入側孤兒，不觸碰本地側）；warn 記錄 |
| TC-C19 | tag categoryId 在 incoming 找不到對應 category | incoming tag 的 `categoryId` 在 incoming tagCategories 內無對應 | computeMergeResult | 建立名為 `"未分類"` category 並歸入該 tag；`"未分類"` 同樣參與同名比對（本地已有 `"未分類"` 則映射至本地） |
| TC-C20 | tag/category name 超 50 字元截斷 | incoming category name 長度 60 | computeMergeResult | result 中該 category name 長度 `toBe 50`（截斷至 `TagSchema` 上限） |
| TC-C21 | book tagIds 聯集後超 100 截斷 | 本地 `book-001` 60 個 tagIds + incoming `book-001` 60 個重映射後不同 tagIds | computeMergeResult | `book-001` tagIds 長度 `toBe 100`（截斷至 `MAX_TAGS_PER_BOOK`）；warn 記錄 |
| TC-C22 | 純函式可重現性（AC-9） | 固定 `local`、`incoming`、相同注入 id 產生器 | computeMergeResult 連續呼叫兩次 | 兩次輸出 `toEqual`（無時間戳以外不確定性，純函式契約） |

#### 測試群組 G：computeMergeResult — 空輸入（功能職責：邊界。AC-13） — 場景 11

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-C23 | 空匯入結果等於本地原樣 | 本地有 books/tags/categories；incoming 三者皆 `[]` | computeMergeResult | result.books/tags/tagCategories 各 `toEqual` 對應本地集合（無新增、無刪除）；`remap` 兩 map 皆空 |

#### 測試群組 H：mergeAllData — 原子持久化成功（功能職責：storage 寫入。AC-10） — 場景 8

> 沿用 `tag-storage-replace-all-data.test.js` 的 `createStorageMock` 與 `makeBook/makeTag/makeCategory` 工廠。

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-M1 | 合併結果原子寫回三 key | store 預置本地 books/tags/categories；合法 incoming 資料 | `mergeAllData({ books, tags, tagCategories })` | 三 storage key 內容為合併後集合；回傳 `success: true`，`counts` 為合併後筆數（本地 + 新增），`remap` 為 `{ categories: N, tags: M }` 統計 |
| TC-M2 | books wrapper 結構保持 | `readmoo_books` 為 `{ books: [...], extractionTimestamp }` wrapper 物件 | mergeAllData | 寫回仍為 wrapper 物件形式，`books` 為合併後陣列，`extractionTimestamp` 既有欄位保留 |
| TC-M3 | 同 id 更新、新 id 新增、永不清空 | store 本地有 3 本書；incoming 1 本同 id + 1 本新 | mergeAllData | 終態 4 本書（本地未匹配的 2 本原樣保留）；驗證合併語意非覆蓋 |
| TC-M4 | counts 與 remap 統計正確 | 本地 1 category；incoming 1 同名 category + 1 新 category | mergeAllData | `counts.tagCategories` 為 2；`remap.categories` 為 1（同名映射至既有本地，新建不計入 remap，依 §2.2 定義） |

#### 測試群組 I：mergeAllData — 配額攔截（功能職責：配額防護。AC-11） — 場景 9

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-M5 | 配額 blocked 攔截 | `createStorageMock({ bytesInUse: ceil(MAX*BLOCK) })`；store 預置本地資料 | mergeAllData | 回傳 `{ success: false, error: 'quota_exceeded' }`；三 storage key 維持寫入前狀態（`toEqual` 原值，未讀算未寫） |
| TC-M6 | 配額 normal 不誤擋 | `bytesInUse` 低於 WARNING 門檻 | mergeAllData | `success: true`，合併正常寫入 |

#### 測試群組 J：mergeAllData — 原子回滾（功能職責：寫入失敗一致性。AC-12。對齊 IMP-C TC-S8/S9） — 場景 10

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-M7 | 第二 key（tags）寫入失敗回滾 | `setHook` 使首次寫 `tags` 失敗；store 預置舊三 key | mergeAllData | 回傳 `{ success: false, error: 'storage_error' }`；三 key 全數回滾至合併前快照（`toEqual` 舊值，books 也回滾無部分更新） |
| TC-M8 | 第三 key（tag_categories）寫入失敗回滾 | `setHook` 使首次寫 `tag_categories` 失敗 | mergeAllData | 回傳 `storage_error`；三 key 全數回滾至合併前快照 |
| TC-M9 | 回滾後資料可正常讀取 | 承 TC-M7 失敗情境 | mergeAllData 後 `getAllTags()` / `getAllTagCategories()` | 取回合併前舊資料，storage 處於一致可用狀態 |

#### 測試群組 K：mergeAllData — 空匯入與並發（功能職責：邊界 + 序列化。AC-13。§2.1） — 場景 11

| TC | 場景 | Given | When | Then（預期斷言） |
|----|------|-------|------|-----------------|
| TC-M10 | 空匯入結果等於本地原樣 | store 本地有 books/tags；incoming 三者皆 `[]` | mergeAllData | 三 key 內容 `toEqual` 本地原值；`success: true`，`counts` 為本地原筆數 |
| TC-M11 | operationLock 序列化兩次合併不交錯 | store 預置本地；不 await 同時發起 mergeAllData(A) 與 mergeAllData(B)（B 內容不同） | `Promise.all([pA, pB])` | 兩次回傳皆 `success: true`；最終 store 為兩次合併依序疊加結果（B 在 A resolve 後執行，含 A 與 B 的合併內容），無交錯部分寫入 |

#### 跨群組依賴與拆分友善性（Phase 3b 派發參考）

| 群組 | 功能職責 | 跨群組依賴 |
|------|---------|-----------|
| A | books 合併 | 依賴 id 重映射結果作輸入前提（測試以預先準備好的重映射 tag id 構造，群組獨立可跑） |
| B / C / D / E | category / tag 重映射、三層貫穿、收斂 | B→C→D→E 概念遞進，但各群組測試自帶完整 Given，可獨立執行 |
| F / G | 邊界與空輸入 | 獨立 |
| H~K | mergeAllData storage 層 | 共用 `createStorageMock` helper（建議抽為共用 fixture 或各檔複製，避免共享 mutable 狀態）；各 TC `beforeEach` 呼叫 `testUtils.cleanup()` 獨立 |

Phase 3b 可按「純函式群組（A~G，單檔）」與「storage 群組（H~K，單檔）」拆為兩個獨立實作單元，符合 SRP，各自只需讀對應群組即可實作。

#### Phase 3a 移交重點（pepper-test-implementer）

- id 產生器注入策略未定：feature-spec §5.4 標「Phase 3a 決策」——「函式參數」或「mergeAllData 預算 id 陣列傳入」。Phase 3a 須先定此策略，純函式測試的注入形式隨之確定。
- `computeMergeResult` 是否匯出：建議匯出以利純函式獨立單元測試（feature-spec §2.4），Phase 3a 確認。
- 觀測性斷言：TC-C17/C18/C21 的 `console.warn`、TC-M5/M7/M8 的 `console.error` 須以 `jest.spyOn` 斷言含元件名 `[tag-storage-adapter]`。

#### 既有測試影響評估

| 既有測試檔 | 預期影響 | 驗證方式 |
|-----------|---------|---------|
| `tests/unit/storage/adapters/tag-storage-replace-all-data.test.js` | 零影響（`mergeAllData` 純新增，不改 `replaceAllData`） | Phase 3b 跑該檔確認綠 |
| 既有 CRUD 測試（`chrome-storage-tag-operations.test.js` 等） | 零影響（不改 CRUD 簽名與 `operationLock`/`withAtomicRollback` 行為） | 全套件 `npm test` |
| `tests/unit/overview/overview-page-controller.test.js` | 零影響（controller 不接線，IMP-E 才接 `mergeAllData`） | 全套件 `npm test` |

AC-17 要求全套件 `npm test` 維持 100% 綠，Phase 3b 完成後須執行確認。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending