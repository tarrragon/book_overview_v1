---
id: 0.19.0-W1-047.4
title: 實作 UC-04 匯入合併模式與 tag id 重映射
type: IMP
status: in_progress
version: 0.19.0
wave: 1
priority: P2
parent_id: 0.19.0-W1-047
children: []
blockedBy:
- 0.19.0-W1-047.3
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 實作 UC-04 匯入合併模式與 tag id 重映射
when: IMP-C 覆蓋模式與 storage 持久化完成後
where:
  layer: 待定義
  files:
  - src/overview/overview-page-controller.js
why: W1-001.2.1 實機驗證證實 UC-04 匯入功能未依 SPEC-EXPORT-V2 實作（BookFileImporter 無版本偵測/v1 轉換/tag
  處理/合併覆蓋模式/storage 持久化；SPEC 模組 format-version-detector 與 v1-to-v2-converter 存在但零非測試消費者）。UC-04
  匯入為 v1.0.0 內測「匯出入正常」硬需求，匯出端 UC-03 已驗證 PASS 但匯入端缺失，功能不對稱
how:
  task_type: Implementation
  strategy: 實作 SPEC-EXPORT-V2 §8.2 合併邏輯：books 同 id tagIds 取聯集、tag/category 同名比對保留本地版本+不存在則新建、匯入
    id 重映射至本地 id。詳見 W1-047 Solution R6
acceptance:
- '[ ] 合併模式 books 同 id 的 tagIds 取聯集'
- '[ ] tag/category 同名比對保留本地版本，不存在則新建'
- '[ ] 匯入 tag/category id 重映射至本地 id'
- '[ ] 對應單元測試 100% 通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-23T05:14:21'
completed_at: null
created: '2026-05-23'
updated: '2026-05-23'
current_phase: Phase 2
assignee: sage-test-architect
---

# Execution Log

## Task Summary

實作 UC-04 匯入合併模式與 tag id 重映射

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 功能規格摘要（lavender-interface-designer）

完整規格：`docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W1-047.4-feature-spec.md`（Full 模式 6 區段，/spec validate 2 次迭代收斂、0 未回答問題）。

#### 範圍

實作 SPEC-EXPORT-V2 §8.2 合併模式與 §8.3 tag/category id 重映射。與 IMP-C 覆蓋模式對稱：storage 層新增 mergeAllData（合併），與既有 replaceAllData（覆蓋）介面對稱。模式選擇 UI 屬 IMP-E，不在本 ticket——本 ticket 僅提供可被 IMP-E 呼叫的合併 API。

#### 架構決策

- storage 層新增 mergeAllData({ books, tags, tagCategories })：與 replaceAllData 對稱。合併「讀本地三 key → 計算 → 原子寫回」全程在 operationLock 內，複用 withAtomicRollback / checkQuotaLevel。回傳 { success, error?, counts?, remap? }。
- 內部純函式 computeMergeResult(local, incoming)：合併計算與 storage I/O 解耦，使 books 聯集 / 同名比對 / id 重映射可獨立於 chrome.storage mock 單元測試。新建 id 透過注入的 id 產生器提供（純函式不可內呼 Date.now/Math.random）。
- id 重映射兩階段：先 category（建 categoryIdMap）後 tag（以「重映射後本地 categoryId + name.toLowerCase」為鍵）。多個匯入 category 收斂至同一本地 category 時其下同名 tag 亦收斂。
- 合併不修復本地破損：本地既有孤兒 tagId 在聯集後原樣保留；引用完整性修復屬既有 checkReferentialIntegrity 職責。

#### 核心行為（14 GWT 場景）

books 同 id tagIds 聯集去重 / 新 id 加入 / tag·category 同名（大小寫不敏感）保留本地、不存在則新建 / id 重映射貫穿 category→tag→book 三層 / 原子持久化 / 配額攔截 / 寫入失敗回滾 / 空匯入合法 / 兩階段收斂 / 不修復本地孤兒。

#### 驗收條件

AC-1~AC-17（細化 IMP-D 4 條 acceptance）。詳見 feature-spec §6。

#### Phase 2 移交重點

雙層測試（computeMergeResult 純函式 + mergeAllData storage）；id 產生器注入可測；同名大小寫差異獨立 TC；三層 id 一致性核心 TC；原子回滾對齊 IMP-C TC-S8/S9。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending