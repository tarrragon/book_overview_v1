---
id: 0.19.0-W3-046
title: 實作 claim 移除 AC verification + 確認 complete 階段並行安全性
type: IMP
status: in_progress
version: 0.19.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W3-045
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層:命令處理
  final_decision: 建立 spawn IMP1 為 W3-045 ANA 結論的 L3-b 主解實作
  rationale: 三視角審查共識策略 B（linux + basil + bay）；source-ticket 0.19.0-W3-045 是 ANA 衍生關係（PC-073
    ANA spawned not children）
who:
  current: thyme-python-developer
  history: {}
what: L3-b 策略 B 落地：ticket track claim 命令移除 AC verification（不再跑 npm_test_pass 模板等全套件測試），延遲到
  complete 階段；同時評估 complete-gate 在同 wave 並行場景的安全性，若有風險一併設計 mitigation
when: W3-045 ANA complete 後啟動；P1 為三 spawn 中最高優先
where:
  layer: framework-cli (.claude/skills/ticket/ticket_system + .claude/hooks + .claude/pm-rules)
  files:
  - .claude/skills/ticket/ticket_system/lib/validation_templates.py (npm_test_pass
    模板硬編碼，PC-007 修正自原誤路徑 .claude/config/)
  - .claude/skills/ticket/ticket_system (claim/complete 命令程式碼)
  - .claude/skills/ticket/ticket_system/tests (新增 pytest claim/complete/並行)
  - .claude/skills/ticket/tests (既有 test_validation_templates.py / test_ac_drift_regression.py
    可能影響)
  - .claude/hooks/acceptance-gate-hook.py (complete 階段並行安全評估)
  - .claude/pm-rules/ticket-lifecycle.md (claim/complete 流程文件更新)
why: 三視角審查共識：策略 B 為 good taste 解（linux）+ 與 L1 hook 同構但更乾淨（basil）+ 避免狀態耦合反模式（bay）。根因位置：validation_templates.py:65-76
  npm_test_pass 硬編碼跑全套件；claim 是狀態切換動作不該綁全域測試副作用。bay 補充：complete-gate 也跑 npm_test_pass，範圍要擴展
how:
  task_type: Implementation
  strategy: (1) 修改 ticket track claim 流程：跳過 AC verification（保留可選 --verify flag 給單獨除錯場景），(2)
    評估 complete-gate 並行風險：若 complete 階段同 wave 仍有 in_progress 會撞，設計排序或範圍縮限機制，(3) 撰寫測試驗證
    claim 不再跑全套件、complete 仍跑且安全，(4) 更新 ticket-lifecycle.md 文件，(5) 處理既有依賴 claim AC
    verification 的場景
acceptance:
- '[x] ticket track claim 命令不再執行 npm_test_pass 等全套件 verification'
- '[x] complete 階段並行安全性已評估並有對應結論（若需 mitigation 已實作）'
- '[x] pytest 測試覆蓋 claim 不跑全套件 / complete 仍跑 / 並行場景安全性'
- '[x] ticket-lifecycle.md 文件對應更新'
- '[x] 既有 ticket 若有 acceptance 依賴 claim 時跑全套件，已盤點並處理'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-25T22:45:24'
completed_at: null
created: '2026-05-25'
updated: '2026-05-25'
---

# Execution Log

## Task Summary

L3-b 策略 B 落地：ticket track claim 命令移除 AC verification（不再跑 npm_test_pass 模板等全套件測試），延遲到 complete 階段；同時評估 complete-gate 在同 wave 並行場景的安全性，若有風險一併設計 mitigation

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因（摘自 W3-045 ANA 結論）

PC-078 並行 session 衝突的根本解定位：`ticket track claim` 階段執行 AC verification（npm_test_pass 模板）會跑全套件 npm test，當 PM 同時 claim 多個同 wave ticket 時，多進程競爭 Jest 暫存 / git stash 中間狀態 → false test failure。

**Layer 三方案三視角審查共識**：
- L1（PreToolUse:Bash hook 偵測同 wave in_progress 並阻擋）— 補位
- L2（acceptance 設計約定 + linter 偵測）— 補位
- **L3-b（CLI AC verification 自身只跑相關檔案測試 / claim 完全跳過 AC verification）— 主解**

三視角支撐：linux 評「good taste 解」、basil 評「與 L1 hook 同構但更乾淨」、bay 評「避免狀態耦合反模式」。

### 影響範圍

**程式碼層**：
- `.claude/skills/ticket/ticket_system/lib/validation_templates.py`（npm_test_pass 模板硬編碼定義位置；非原 ticket where.files 中的 `.claude/config/validation_templates.py`，**已 PM 前台修正路徑 PC-007**）
- `.claude/skills/ticket/ticket_system/`（claim 流程 + complete 流程程式碼）
- `.claude/hooks/acceptance-gate-hook.py`（complete 階段並行安全評估範圍）

**測試層**：
- `.claude/skills/ticket/ticket_system/tests/test_claim_with_verification.py`（既有）
- `.claude/skills/ticket/ticket_system/tests/test_claim_verification.py`（既有）
- `.claude/skills/ticket/tests/test_validation_templates.py`（既有）
- `.claude/skills/ticket/tests/test_ac_drift_regression.py`（既有）
- 新增測試覆蓋 claim 不跑全套件 / complete 仍跑 / 並行情境安全

**文件層**：
- `.claude/pm-rules/ticket-lifecycle.md`（claim/complete 流程文件更新）
- `.claude/rules/core/ticket-skill-sync-check.md` 觸發（行為變更 = 屬同步檢查範圍）

### 相關 Error Pattern

| Pattern | 關聯 |
|---------|------|
| PC-007 接手者獨立驗證 | W3-045 ANA 寫的 `.claude/config/validation_templates.py` 路徑錯誤；實際 lib/validation_templates.py，**PM 前台已驗證並修正** |
| PC-078 並行 session 衝突 | 根因同源；本 ticket 是其根本解之一 |
| PC-073 ANA spawned not children | source_ticket=W3-045 ANA 衍生關係（已正確使用） |
| ticket-skill-sync-check（規則） | 改 ticket skill 行為時必同步檢查 decision-tree.md / pm-rules/ ticket CLI 引用 |

### 派發策略

**TDD Phase 對應**：
- Phase 1（功能規格）：W3-045 ANA Solution 已等同 Phase 1 設計（含 multi_view_status 三視角共識）→ **跳過 lavender**
- Phase 2（測試規格）：acceptance 5 條已等同測試 spec → **派發 sage 設計測試 + 寫 RED 測試**，或直接派 thyme 一併處理
- Phase 3b（實作）：thyme-python-developer 實作 GREEN + 文件更新
- Phase 4（重構評估）：派發 parallel-evaluation 或 cinnamon 完成後評估

**派發路徑**：考量本 session context 較大，採輕量 TDD：派發 thyme-python-developer 一併負責 Phase 2 (RED 測試) + Phase 3b (GREEN 實作) + 文件更新，commit 後派 parallel-evaluation 做 Phase 4 評估。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 實作策略：L3-b 策略 B 落地（claim 預設不執行 AC verification）

#### 變更摘要

1. **`.claude/skills/ticket/ticket_system/commands/lifecycle.py::execute_claim`**：
   - 新增 `verify` arg 讀取（opt-in 旗標）
   - 預設路徑改走 `lifecycle.claim()`（純狀態切換，無 subprocess）
   - 僅 `--verify` 明示啟用時走 `claim_with_verification`（保留除錯場景）
   - `--skip-verify` 變成 no-op（向後相容，與其他 flag 同用時 stderr 提示）

2. **`.claude/skills/ticket/ticket_system/commands/track.py`**：
   - argparse 新增 `--verify` flag（help 含 W3-046 / PC-078 來源說明）
   - `--skip-verify` help 文字更新標註「W3-046 後 no-op」

3. **`.claude/skills/ticket/ticket_system/lib/validation_templates.py`**：
   - **不刪除** `npm_test_pass` 模板（W3-045 ANA 明確要求保留 --verify 除錯場景）
   - 模板僅在 `--verify` opt-in 時被觸發

4. **`.claude/pm-rules/ticket-lifecycle.md`**：
   - 「AC 漂移偵測」章節改標 `（claim 階段；W3-046 改為 --verify opt-in）`
   - 新增三明示段（Why / Consequence / Action）說明預設行為變更
   - 機制總覽表標註「opt-in」與「預設啟用」差異
   - 豁免條件表加入新預設路徑與 `--verify` 行為說明
   - 範例 A/B 加註 `--verify` 旗標

5. **`.claude/skills/ticket/tests/test_ac_drift_regression.py`**：
   - 既有 W3-001 回歸測試 fixture 加入 `verify=True`，符合新 opt-in 契約
   - 測試契約變更為「`--verify` 啟用時 S4 仍正確拒絕」（行為不變，僅啟用方式變）

6. **`.claude/skills/ticket/ticket_system/tests/test_claim_no_verify_by_default.py`**（新檔）：
   - 5 個測試覆蓋預設不執行驗證、`--verify` opt-in、`--skip-verify` 向後相容、complete 無 subprocess

#### Layer 1 hook 自檢結果

無新增/修改 hook；本 ticket 僅修改 ticket skill CLI + lifecycle 規則文件 + 新增測試。
無需 hook_self_reference 豁免（非 hook 設計 ticket）。

#### Complete-gate 並行安全分析（bay 視角擔憂回應）

**結論：complete-gate 在 ticket CLI 層級無 subprocess 副作用，並行 complete 安全，不需 mitigation。**

證據：

1. `lifecycle.TicketLifecycle.complete` 源碼靜態檢查（test_complete_does_not_spawn_subprocess_for_ac_verification 涵蓋）：
   - 未 import / 未呼叫 `run_all_verifications` 或 `collect_ac_verifications`
   - 完成驗證僅透過 `validate_acceptance_criteria`（checkbox 純字串檢查）+ `validate_execution_log_by_type`（body schema 純文字檢查）

2. `acceptance-gate-hook` 與 `.claude/hooks/acceptance_checkers/*.py` 全檔 grep：
   - 無 `match_template` / `run_all_verifications` / `npm_test_pass` / `npm test` 引用
   - 僅做 metadata / spawned ticket / children 狀態檢查（無 subprocess）

3. file_lock（W14-044）已包圍 complete 的 load → modify → save 全段，logical race 已消除

**因此 bay 視角的「complete-gate 並行 npm test 撞 jest 暫存」風險在當前實作中不存在**。後續若有人為 complete 加入 subprocess 驗證（如效能 baseline 量測），需重新評估並加入並行安全 mitigation；本 ticket 在 ticket-lifecycle.md 留下說明，禁止未經評估再次引入 subprocess 進入 complete。

#### 既有 ticket 依賴盤點

執行 `grep -rln "npm_test_pass\|npm test 2>&1 | tail" docs/work-logs/`，結果僅 7 個檔案命中：

| Ticket | 性質 | 處理 |
|--------|------|------|
| 0.19.0-W1-048.10.1.4 / .10.1 / .10.1.5.4 | 歷史 commit log 中含 npm test 命令字串 | 無 acceptance 依賴；無需處理 |
| 0.19.0-W3-045 / W3-046 | 本任務鏈本身 | N/A |
| 0.18.0-W11-001.1 / W5-001 | 原 AC verification 功能設計 ticket | 歷史脈絡，已完成；行為調整由 W3-046 接手，不修改既有 ticket |

無任何尚未 complete 的 ticket 在 acceptance 欄位中要求 `claim` 時跑 npm_test_pass，因此**無需修改既有 ticket acceptance**。

#### ticket-skill-sync-check 結果

執行 `grep -rln "ticket track claim\|/ticket" .claude/skills/ticket/SKILL.md .claude/pm-rules/`，所有引用：

- `.claude/skills/ticket/SKILL.md`：僅描述 `ticket track claim <id>` 概念性指令，無 verify/skip-verify 細節，無需更新
- `.claude/pm-rules/ticket-lifecycle.md`：含 AC 驗證章節，已更新
- 其他 pm-rules（command-routing / decision-tree / dispatch-* 等）：grep 命中為一般 `ticket track` 命令引用，無 `--skip-verify` / `--verify` 細節，無需更新

範圍適中，無需獨立 DOC ticket 追蹤。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 執行指令與結果

**新增測試套件**：

```
$ uv run --project .claude/skills/ticket pytest \
    .claude/skills/ticket/ticket_system/tests/test_claim_no_verify_by_default.py \
    -v --tb=short
============================= test session starts ==============================
collected 5 items

test_execute_claim_default_does_not_call_collect_ac_verifications PASSED [ 20%]
test_execute_claim_default_does_not_spawn_subprocess              PASSED [ 40%]
test_execute_claim_with_verify_flag_invokes_verification          PASSED [ 60%]
test_execute_claim_skip_verify_flag_still_works                   PASSED [ 80%]
test_complete_does_not_spawn_subprocess_for_ac_verification       PASSED [100%]
============================== 5 passed in 0.03s ===============================
```

**相關回歸測試（含 W11-001.x 既有套件）**：

```
$ uv run --project .claude/skills/ticket pytest \
    .claude/skills/ticket/ticket_system/tests/test_claim_no_verify_by_default.py \
    .claude/skills/ticket/ticket_system/tests/test_claim_with_verification.py \
    .claude/skills/ticket/ticket_system/tests/test_claim_verification.py \
    .claude/skills/ticket/tests/test_validation_templates.py \
    .claude/skills/ticket/tests/test_ac_drift_regression.py
============================== 87 passed in 0.08s ==============================
```

**全 ticket 測試套件回歸**（pre-existing 2 個失敗已 git stash 驗證為與本 ticket 無關）：

```
$ uv run --project .claude/skills/ticket pytest .claude/skills/ticket/ -q
================== 2 failed, 2239 passed in 383.28s (0:06:23) ==================
```

| 失敗測試 | 與 W3-046 關聯 | 證據 |
|---------|--------------|------|
| test_error_channel_integration::test_scenario_3_section_not_found | 無關 | git stash 後仍失敗（append-log section 不存在驗證；不依賴 verify flag） |
| test_track_hook_health::test_table_format_renders_status_column | 無關 | hook health 表格 status 欄位渲染問題，無 verify/claim 關聯 |

兩個 pre-existing 失敗已透過 git stash 驗證為非本 ticket 引入；新增 5 個測試 + 既有 80 個相關測試全綠（87/87）。

### 並行安全靜態驗證

`test_complete_does_not_spawn_subprocess_for_ac_verification` 使用 `inspect.getsource`：

- 驗證 `validate_acceptance_criteria` 源碼不含 `subprocess` / `npm`
- 驗證 `TicketLifecycle.complete` 源碼不含 `run_all_verifications` / `collect_ac_verifications`

加 grep 確認 `.claude/hooks/` 全目錄無 `match_template` / `run_all_verifications` 引用。

### CLI 行為驗證

```
$ ticket track claim --help
usage: ticket track claim [-h] [--version VERSION] [--verify] [--skip-verify]
                          [--yes] [--quiet] [--verbose] [--json]
                          ticket_id

  --verify           W3-046: 明示啟用 AC 自動驗證（預設關閉，避免 npm test 全套件副作用 / PC-078
                     並行衝突）。僅供除錯場景使用
  --skip-verify      W3-046: 已預設不執行 AC 驗證，此旗標保留為 no-op（向後相容）
  --yes, -y          非互動模式：AC 驗證（若以 --verify 啟用）自動選 y 繼續 claim
```

CLI reinstall：`uv tool install ./.claude/skills/ticket --force --reinstall` 完成。

### Commit

待 commit（PM 派 Phase 4 評估後再 complete）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending — PM 將派 Phase 4 評估後 complete)
**Executing Agent**: thyme-python-developer
**Review Status**: pending Phase 4 評估

### Pre-existing test failures（與本 ticket 無關，需 PM 另開 ticket 追蹤）

| 測試 | 失敗原因 | git stash 驗證 |
|------|---------|--------------|
| `test_error_channel_integration::test_scenario_3_section_not_found` | append-log section not found 斷言失敗（與 verify flag 無關） | 已驗證 stash 後仍失敗 |
| `test_track_hook_health::test_table_format_renders_status_column` | hook health 表格 status 欄位渲染問題 | 已驗證 stash 後仍失敗 |

兩者建議 PM 依 quality-baseline 規則 5 各開獨立 ticket 追蹤，本 ticket 範圍未涵蓋。

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W3-045 | chars: 855 -->

### Task Reference
- 0.19.0-W3-045 what: 評估三層方案的定位與優先序：(L1) PreToolUse:Bash hook 偵測同 wave in_progress 並阻擋、(L2) acceptance 設計約定（禁寫全套件測試字面）+ linter 偵測、(L3) CLI AC verification 自身只跑相關檔案測試（根本解）。產出 Layer 主解定位 + 其餘 Layer 補位的 spawn IMP 規劃表。多視角審查（含 linux + 至少 2 個其他視角）

### Rationale Chain
- 0.19.0-W3-045 why: 用戶 memory「Parallel Claim AC Verification Collision」記錄：當 PM 同時 claim 多個同 wave ticket 且 acceptance 觸發全套件測試（npm test / pytest），會撞 jest 暫存或 git stash 中間狀態，造成測試 false failure。屬 PC-078 並行 session 衝突同源問題。已知 hook 是補丁解、CLI AC verification 行為才是根本解。依 quality-baseline 規則 5 必須追蹤；依 Ticket Guidance Over Hooks memory 應評估非 hook 解法（CLI 行為）

### Related Files
- .claude/hooks  # from 0.19.0-W3-045
- .claude/pm-rules/ticket-lifecycle.md  # from 0.19.0-W3-045

### Source Acceptance (reference)
- [ref] [ ] ANA 結論明確指出 Layer 1/2/3 各自定位與優先序（哪個是主解、哪個是補位）  # from 0.19.0-W3-045
- [ref] [ ] 含至少 3 個歷史案例（從 git log / memory / 既有 W6-001.x 派發紀錄抽取觸發場景）  # from 0.19.0-W3-045
- [ref] [ ] Solution 章節含 spawn IMP 規劃表（含或標明豁免理由）  # from 0.19.0-W3-045
- [ref] [ ] 多視角審查（含 linux + 其他 2 個視角）紀錄與結論寫入 Solution multi_view_status  # from 0.19.0-W3-045
- [ref] [ ] ANA 完成後可立即派出對應 spawn ticket（spawned_tickets 欄位回填）  # from 0.19.0-W3-045

### 派發任務範圍（thyme-python-developer）

執行 Phase 2 + Phase 3b 合併（W3-045 ANA 等同 Phase 1，跳過 lavender）：

1. **Phase 2 RED 測試**：對應 5 條 acceptance 寫 pytest，run 確認 RED
2. **Phase 3b GREEN 實作**：
   - 改 `.claude/skills/ticket/ticket_system/lib/validation_templates.py` + ticket CLI claim/complete 流程
   - complete-gate 並行安全評估（bay 視角補充：若同 wave in_progress 存在則延後 complete 或範圍縮限）
   - ticket CLI source 變更後必須 `uv tool install . --force --reinstall`（W11-037 模式）
   - run pytest 確認 GREEN
3. **文件更新**：`.claude/pm-rules/ticket-lifecycle.md`（claim/complete 章節）
4. **盤點既有 ticket**：grep acceptance 字面含 `npm_test_pass` 的 ticket，列出影響清單
5. **觸發 ticket-skill-sync-check**：grep .claude/skills/ticket/SKILL.md + .claude/pm-rules/ 中 ticket CLI 行為描述需同步

### Agent 自律要求

- commit msg: `feat(0.19.0-W3-046): claim 移除 AC verification + complete 階段並行安全`
- PC-135：若改 hook 共用 lib（lib/hook_utils.py），caller 同步檢查四欄表
- 完成後（依序）：
  - append-log Solution（變更摘要 + Layer 1 自檢結果 + complete-gate 並行安全分析）
  - append-log Test Results（pytest 輸出 + commit SHA）
  - Edit ticket md Completion Info section
  - check-acceptance --all
  - **不要** ticket track complete（PM 將派 Phase 4 評估後再 complete）

### 工作目錄與禁止行為

- main repo /Users/tarragon/Projects/book_overview_v1，無 worktree
- 禁止：刪除既有 AC verification 邏輯（保留 --verify flag 為選用）
- 禁止：未 reinstall ticket CLI 就 commit（W11-037 stale install 模式）