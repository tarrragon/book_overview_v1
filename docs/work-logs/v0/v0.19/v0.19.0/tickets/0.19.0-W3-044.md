---
id: 0.19.0-W3-044
title: 實作 ticket CLI body-op precondition：append-log/set-acceptance/complete 入口加 status
  檢查
type: IMP
status: in_progress
version: 0.19.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W3-043
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:Ticket 流程改善
  final_decision: 落地 W3-043 推薦方案 (b)
  rationale: 對所有 ticket body 操作通用，相比 (d) W1-054 修復覆蓋範圍更廣
who:
  current: thyme-python-developer
  history: {}
what: 實作 ticket CLI body-op precondition：append-log/set-acceptance/complete 入口加 status
  檢查
when: W3-043 ANA 結案後
where:
  layer: .claude/skills/ticket/ticket_system/commands/lifecycle.py,.claude/skills/ticket/ticket_system/lib/ticket_lifecycle.py
  files:
  - .claude/skills/ticket/ticket_system/lib/ticket_lifecycle.py
  - .claude/skills/ticket/ticket_system/cli/append_log.py
  - .claude/skills/ticket/ticket_system/cli/set_acceptance.py
  - .claude/skills/ticket/ticket_system/cli/complete.py
why: W3-043 結論：W3-012 變更遺失三層共振根因之一是 PM 在 status=pending 期間執行 body 操作（協議違反）。CLI 層強制
  body-op 前 status 必須為 in_progress 可從根本阻斷此路徑。對所有 ticket body 操作通用，落地成本中、效果明確。
how:
  task_type: Implementation
  strategy: 1) ticket_lifecycle 新增 require_in_progress(ticket_id) helper；2) append-log
    / set-acceptance / complete / set-* CLI 入口加 precondition 檢查；3) 提供 --force 旁路（記錄使用次數，>0
    觸發 review）；4) 對 pending 操作 exit 2 + 明確錯誤訊息 + 建議先 claim；5) 對 completed 操作維持現狀（appendable
    for review）
acceptance:
- '[ ] append-log / set-acceptance / complete 對 status=pending ticket 操作會 exit 2 並顯示「請先
  claim」建議'
- '[ ] 新增 --force 旁路保留 escape hatch，使用次數記入 hook-logs'
- '[ ] 對 status=completed 維持現狀（仍可 append-log 補充 review 評論）'
- '[ ] 3 個入口點皆有單元測試覆蓋 precondition 檢查'
- '[ ] 無回歸（既有 ticket lifecycle 測試套件通過）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-25T14:03:54'
completed_at: null
created: '2026-05-25'
updated: '2026-05-25'
---

# Execution Log

## Task Summary

實作 ticket CLI body-op precondition：append-log/set-acceptance/complete 入口加 status 檢查

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

W3-043 ANA 結論：W3-012 處理時 status=pending 期間執行 append-log/set-acceptance/complete 是 body 變更遺失的協議違反根因之一。在 CLI 入口檢查 status 可從根本阻斷此路徑。

### 影響範圍

- .claude/skills/ticket/ticket_system/lib/precondition.py（新增）
- .claude/skills/ticket/ticket_system/commands/track_acceptance.py（接線 append-log）
- .claude/skills/ticket/ticket_system/commands/track_set_acceptance.py（接線 set-acceptance）
- .claude/skills/ticket/ticket_system/commands/lifecycle.py（接線 complete）
- .claude/skills/ticket/ticket_system/commands/track.py（新增 --force flag）
- .claude/skills/ticket/ticket_system/tests/conftest.py（新增 fixtures）
- .claude/skills/ticket/ticket_system/tests/test_commands_race.py（fixture migration）

### 相關 Error Pattern

W3-043 ANA 衍生 IMP；落地 Phase 1 lavender + Phase 2 sage + Phase 3a pepper 三階段規格。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 1 規格設計（lavender-interface-designer）

#### 設計目標

落地 W3-043 推薦方案 (b)：在 ticket CLI 的 3 個 body-op 入口（`append-log` / `set-acceptance` / `complete`）強制 status precondition 檢查，從根本阻斷「status=pending 期間執行 body 操作」的協議違反路徑。

> **設計前提（ARCH-010）**：本規格為 CLI 層強制，不引入新模組或抽象。沿用既有 `ticket_lifecycle.py` / `track_acceptance.py` / `track_set_acceptance.py` 模組邊界。

---

#### 1. `require_in_progress` helper 函式規格

**檔案位置**：`.claude/skills/ticket/ticket_system/lib/ticket_lifecycle.py`（新增於 TicketLifecycle 類別上方的 module-level helpers 區域，緊鄰 `sync_completion_body_fields` 之後）

**函式簽章**：

```python
def require_in_progress(
    ticket: Dict[str, Any],
    ticket_id: str,
    operation: str,
    *,
    allow_completed: bool = False,
    force: bool = False,
) -> Tuple[bool, Optional[str]]:
    """檢查 body 操作的 status precondition。

    Args:
        ticket: 已 load 的 ticket dict（含 status 欄位）
        ticket_id: ticket ID，用於錯誤訊息
        operation: 呼叫端操作名稱（"append-log" / "set-acceptance" / "complete"），
            用於錯誤訊息與 hook-logs 記錄
        allow_completed: 是否允許 status=completed（True 用於 append-log 補 review）
        force: 是否旁路檢查（記錄到 hook-logs，returns (True, None)）

    Returns:
        (ok, error_msg)
        - ok=True, error_msg=None：通過檢查（或 --force 旁路）
        - ok=False, error_msg=str：阻擋，error_msg 含建議的後續指令
    """
```

**行為定義**：

| status | allow_completed=False | allow_completed=True | force=True |
|--------|---------------------|---------------------|-----------|
| pending | 拒絕，建議 `ticket track claim <id>` | 拒絕，建議 claim | 通過 + log |
| in_progress | 通過 | 通過 | 通過 |
| completed | 拒絕，建議 `ticket track reopen <id>` 或 `--force` | 通過（補 review 場景） | 通過 + log |
| blocked | 拒絕，建議「先解除阻塞或 release」 | 拒絕 | 通過 + log |
| closed | 拒絕，建議「該 ticket 已 closed，不可修改」 | 拒絕 | 通過 + log |

**重要**：helper **不直接 print 或 exit**，僅回傳結果。由 caller 決定如何輸出與退出（保持單一職責，便於測試）。

**`force=True` 副作用**：寫入 `.claude/hook-logs/cli-force-usage.jsonl`（append-only JSONL），每行格式：

```json
{"timestamp": "2026-05-25T14:30:00", "ticket_id": "0.19.0-W3-044", "operation": "append-log", "status_at_time": "pending", "reason": "force-flag"}
```

> **Why hook-logs 不是 ticket md**：force 使用是元事件（meta-event），不屬 ticket 內容；且若記入 ticket md，force 旁路 status 檢查的同時又寫 ticket md，自我矛盾。

---

#### 2. CLI Entry Point 接線位置

**規則**：每個 entry point 在 `file_lock` 內、`load_and_validate_ticket` 之後、實際 mutation 之前呼叫 `require_in_progress`。

| Entry Point | 檔案 | 函式 | 接線位置 | allow_completed |
|-------------|------|------|---------|----------------|
| `append-log` | `commands/track_acceptance.py` | `_execute_append_log_locked` | 第 623-625 行 `load_ticket` 後立即 | **True**（保留 completed 補 review） |
| `set-acceptance` | `commands/track_set_acceptance.py` | `execute_set_acceptance` | 第 137-138 行 `load_and_validate_ticket` 後 | **False**（completed 後不應再改 AC） |
| `complete` | `commands/lifecycle.py` | `TicketLifecycle.complete` | 第 647 行 `load_and_validate_ticket` 後（Step 2 之前） | **False**（completed 由現有 `is_already_complete` 邏輯處理，無需重複） |

**錯誤處理範本**（caller 端）：

```python
ok, error_msg = require_in_progress(
    ticket, ticket_id, "append-log",
    allow_completed=True, force=args.force,
)
if not ok:
    print(error_msg, file=sys.stderr)
    return 2  # exit code 2 = precondition violation
```

---

#### 3. `--force` flag 設計

**CLI 表面**：每個 entry point 的 argparse 新增 `--force` flag（`action='store_true'`，預設 False）。

| Entry Point | Flag 名稱 | 衝突檢查 |
|-------------|---------|---------|
| `append-log` | `--force` | 與既有 flag 無衝突 |
| `set-acceptance` | `--force` | 與既有 flag 無衝突 |
| `complete` | `--force` | **與既有 `force` 參數合併使用**（既有 force 用於 pending children 旁路；本 force 用於 status precondition 旁路；語義相容可合併） |

**使用追蹤**：

- 寫入路徑：`.claude/hook-logs/cli-force-usage.jsonl`（已存在則 append，不存在則建立）
- 由 `require_in_progress` 在 `force=True` 路徑內寫入
- 統計用途：後續若 `--force` 使用 > 0，PM 可定期 review 是否為誤用模式（記入 PC error-pattern）

**stderr 提示**：使用 `--force` 時 stderr 輸出警告：

```
[Warning] --force 旁路 status precondition 檢查；使用已記入 hook-logs
```

---

#### 4. completed 狀態的特例處理

**設計決策**：`append-log` 允許 `status=completed` 通過（`allow_completed=True`），其他兩個入口拒絕。

**Why**：

- `append-log`：completed 後仍可能需補 review 評論、後續發現的 follow-up 記錄、auditor 結論。禁止會迫使用戶 `reopen` 後 `complete`，產生不必要的 status 漂移。
- `set-acceptance`：completed 後改 AC 違反語義（AC 已驗證完畢，不應改變）。如需修改，先 `reopen`。
- `complete`：既有 `is_already_complete` 邏輯已處理（顯示友好訊息 return 0），無需重複阻擋。

**與既有 close 命令的關係**：本規格不影響 `close` 命令（close 對 completed → closed 已有明確路徑，無 body 操作語義）。

---

#### 5. 錯誤訊息格式

**統一格式**（所有 status 違規）：

```
[Error] <ticket_id> 無法執行 <operation>：當前 status=<status>，需要 status=in_progress
        建議：<具體後續指令>
        逃生閥：--force（記入 hook-logs，需於後續 review 中說明理由）
```

**具體建議對照表**：

| status | 建議指令 |
|--------|---------|
| pending | `ticket track claim <ticket_id>` |
| completed（非 allow_completed 入口）| `ticket track reopen <ticket_id>`（若需修改）或檢視是否誤呼叫 |
| blocked | `ticket track release <ticket_id>` 或解除 blockedBy 依賴後 claim |
| closed | 該 ticket 已 closed；如需修改請建立新 ticket 處理 |

**訊息常數位置**：新增至 `.claude/skills/ticket/ticket_system/lib/messages.py` 的 `ErrorMessages` class，建議名稱 `BODY_OP_REQUIRES_IN_PROGRESS`，支援 `.format(ticket_id=, operation=, status=, suggestion=)` 佔位符。

---

#### 6. 既有測試相容性聲明

**預期影響的測試類別**：

| 測試檔 | 影響 | 處理 |
|--------|------|------|
| `tests/test_append_log_placeholder.py` | 若 fixture 建立 ticket 未 claim 就直接 append-log，會被新 precondition 擋下 | 更新 fixture：建立 ticket → claim → 才 append-log，或在測試呼叫 `--force` |
| `tests/test_commands_race.py` | 同上模式可能存在 | 同上 |
| `tests/test_ticket_validator.py` | 純驗證邏輯，無 CLI 呼叫，預期無影響 | 無需修改 |
| `tests/test_ticket_lifecycle*.py` | 既有 claim/complete 測試應持續通過（claim → complete 流程合規） | 無需修改 |
| 其他 commands 測試 | 任何「建立 ticket 後直接呼叫 set-acceptance/append-log」的測試 | 更新為合規流程 |

**回歸驗證指令**（給 Phase 3b 實作者）：

```bash
cd .claude/skills/ticket && uv run pytest ticket_system/tests/ -v
```

**測試更新原則**：優先採「合規流程」（claim → operate）而非到處加 `--force`，後者會掩蓋規格意圖。`--force` 僅在「測試明確驗證 force 旁路」時使用。

---

#### 7. Phase 2 sage 測試矩陣關鍵分支點

供 sage-test-architect 設計測試案例時參考的關鍵分支：

**分支點 A：require_in_progress helper 純函式測試**

| 測試 ID | status | allow_completed | force | 預期結果 |
|---------|--------|----------------|-------|---------|
| A1 | pending | False | False | (False, error_msg 含 "claim") |
| A2 | pending | True | False | (False, error_msg 含 "claim") |
| A3 | in_progress | False | False | (True, None) |
| A4 | in_progress | True | False | (True, None) |
| A5 | completed | False | False | (False, error_msg 含 "reopen") |
| A6 | completed | True | False | (True, None) ← append-log 補 review 路徑 |
| A7 | blocked | False | False | (False, error_msg 含 "release") |
| A8 | closed | False | False | (False, error_msg 含 "closed") |
| A9 | pending | False | True | (True, None) + hook-logs 寫入驗證 |
| A10 | 缺失 status 欄位 | False | False | (False, error_msg) — 預設 pending 處理 |

**分支點 B：CLI entry point 整合測試**（per entry × per status）

| 入口 | pending | in_progress | completed | blocked | 含 --force |
|------|---------|-------------|-----------|---------|-----------|
| append-log | exit 2 | exit 0 | exit 0（補 review）| exit 2 | exit 0 + warning |
| set-acceptance | exit 2 | exit 0 | exit 2 | exit 2 | exit 0 + warning |
| complete | exit 2 | exit 0 | exit 0（既有 is_already_complete）| exit 1（既有 validate_completable_status）| 走既有 force 路徑 |

**分支點 C：hook-logs 寫入驗證**

| 測試 ID | 情境 | 驗證項 |
|---------|------|--------|
| C1 | --force 旁路 pending | hook-logs JSONL 新增 1 行，含 timestamp / ticket_id / operation / status_at_time / reason |
| C2 | hook-logs 目錄不存在 | 自動建立目錄後寫入成功 |
| C3 | 連續 3 次 --force 呼叫 | hook-logs 累積 3 行（append-only 語義） |

**分支點 D：file_lock 互動**

| 測試 ID | 情境 | 驗證項 |
|---------|------|--------|
| D1 | require_in_progress 失敗時 file_lock 正確釋放 | lock 檔案不殘留 |
| D2 | --force 旁路後 mutation 正常執行 | ticket md 變更生效 |

**分支點 E：錯誤訊息與建議**

| 測試 ID | 情境 | 驗證項 |
|---------|------|--------|
| E1 | pending 拒絕時 stderr 含 `ticket track claim` | substring match |
| E2 | completed 拒絕（set-acceptance）時 stderr 含 `reopen` | substring match |
| E3 | --force 使用時 stderr 含 warning + hook-logs 提示 | substring match |

---

#### 8. 拆分判斷（SOLID + 認知負擔閾值）

依 `.claude/rules/core/cognitive-load.md` 拆分閾值評估：

| 項目 | 本 ticket 評估 | 閾值 | 判斷 |
|------|--------------|------|------|
| 功能職責數 | 2（helper 設計 + 3 entry 接線）| > 2 須拆 | 邊界 |
| 修改檔案數 | 4（lifecycle.py + track_acceptance.py + track_set_acceptance.py + messages.py）+ 測試 | > 5 須拆 | 通過 |
| 認知負擔指數 | helper 單純（< 5）+ 3 處對稱接線（每處 < 5） | > 10 須拆 | 通過 |
| 跨架構層級 | 單層（CLI commands + lib helper）| > 2 須拆 | 通過 |

**結論：不拆分**。3 個 entry point 接線高度對稱（同 helper、同錯誤格式、同 force 處理），拆分反而引入跨 ticket 協調成本。Phase 3b 實作者一次完成 helper + 3 處接線 + 測試更新，commit 範圍合理（單一概念：「CLI body-op precondition」）。

**若 Phase 3b 實作發現範圍超標**：可建議 PM 拆出 IMP-2 處理 messages.py 國際化或 hook-logs schema 升級（非本 ticket 範圍）。

---

#### 9. Phase 2/3 Handoff Checklist

供 sage-test-architect（Phase 2）：

- [x] require_in_progress helper 函式簽章已定義（含 type hints、副作用、回傳語義）
- [x] 3 個 CLI entry 接線位置已標明（檔案 + 函式 + 行號 + allow_completed 配置）
- [x] --force flag 設計完整（hook-logs schema、stderr 提示、與既有 force 的合併）
- [x] completed 特例處理已決策（append-log allow / 其他 deny）
- [x] 錯誤訊息格式統一（含 4 種 status 的建議指令對照表）
- [x] 既有測試相容性已聲明（4 類測試檔的影響與處理方向）
- [x] 測試矩陣 5 大分支點已列出（A: helper 純函式 / B: CLI 整合 / C: hook-logs / D: file_lock / E: 訊息）
- [x] 拆分判斷已完成（不拆分，附理由）

**未決策項（交由後續 Phase）**：

- hook-logs JSONL 的 retention / rotation 策略（非本 ticket 範圍；若 force 使用率高再建 follow-up）
- 是否需要 `--force-reason "<text>"` 強制附理由（本 Phase 1 採最小落地，不引入；若後續 review 發現誤用模式再升級）
- i18n / 訊息常數的英文版（暫沿用繁中，與既有 ErrorMessages 一致）

---

**Phase 1 spec 完成**。下一步：PM 派發 sage-test-architect 執行 Phase 2 撰寫 RED 測試。


### Phase 2 測試矩陣（sage-test-architect）

> **設計依據**：Phase 1 規格 §7 五大分支點 A-E，依「require_in_progress helper 純函式 → CLI 整合 → 副作用（hook-logs / file_lock）→ 錯誤訊息」分層。預估 **35 case**（A:11 / B:15 / C:4 / D:3 / E:2），落於 30-40 目標區間。

---

#### 分支點 A：require_in_progress helper 純函式測試（11 case）

**測試檔建議路徑**：`.claude/skills/ticket/ticket_system/tests/test_require_in_progress.py`（新增）

**測試策略**：Sociable Unit Test，輸入為 ticket dict（in-memory，不需 file IO）+ 參數組合，輸出為 `(ok, error_msg)` tuple。`force=True` 路徑的 hook-logs 副作用透過 `tmp_path` fixture + monkeypatch `hook-logs` 目錄驗證。

| Case ID | Given (ticket.status, allow_completed, force) | When | Then |
|---------|----------------------------------------------|------|------|
| A1 | (pending, False, False) | 呼叫 require_in_progress | 回傳 (False, msg)，msg 含 `ticket track claim` 與 ticket_id |
| A2 | (pending, True, False) | 同上 | 回傳 (False, msg)，msg 含 `claim`（allow_completed 不影響 pending 拒絕） |
| A3 | (in_progress, False, False) | 同上 | 回傳 (True, None) |
| A4 | (in_progress, True, False) | 同上 | 回傳 (True, None) |
| A5 | (completed, False, False) | 同上 | 回傳 (False, msg)，msg 含 `reopen` |
| A6 | (completed, True, False) | 同上 | 回傳 (True, None)（append-log 補 review 路徑） |
| A7 | (blocked, False, False) | 同上 | 回傳 (False, msg)，msg 含 `release` |
| A8 | (closed, False, False) | 同上 | 回傳 (False, msg)，msg 含 `closed` |
| A9 | (pending, False, True) | 同上 | 回傳 (True, None) **且** hook-logs JSONL 新增 1 行 |
| A10 | ticket dict 缺 `status` 欄位 | 同上 | 回傳 (False, msg)；預設視為 pending 處理（防禦性設計，避免 KeyError） |
| A11-RED | (in_progress, False, False) 但 ticket_id 為空字串 | 同上 | 回傳 (False, msg) 或 raise ValueError（驗收 helper 對 invalid input 的處理 - 此 case 預期 RED 階段失敗，迫使實作明確定義邊界） |

**RED 階段必失敗案例**：A1-A8 在 helper 未實作前全數 ImportError；A11 為邊界探索 case，驅動實作者明確處理 invalid ticket_id。

---

#### 分支點 B：CLI Entry Point 整合測試（15 case）

**測試檔建議路徑**：
- `.claude/skills/ticket/ticket_system/tests/test_cli_append_log_precondition.py`（新增）
- `.claude/skills/ticket/ticket_system/tests/test_cli_set_acceptance_precondition.py`（新增）
- `.claude/skills/ticket/ticket_system/tests/test_cli_complete_precondition.py`（新增）

**測試策略**：subprocess 呼叫 CLI 或直接呼叫 `_execute_*_locked` 函式（後者較快，建議優先）。每個入口建立 fixture：`tmp_ticket_with_status(status)` 產生指定 status 的 ticket md，避免狀態切換污染。

**B 組 1：append-log（5 case）**

| Case ID | Given status | --force | Then exit code | Then 副作用 |
|---------|-------------|---------|---------------|------------|
| B1 | pending | False | 2 | ticket md 不變、stderr 含建議 |
| B2 | in_progress | False | 0 | ticket md log 新增 |
| B3 | completed | False | 0 | ticket md log 新增（allow_completed=True 路徑） |
| B4 | blocked | False | 2 | ticket md 不變、stderr 含 `release` |
| B5 | pending | True | 0 | ticket md log 新增 + stderr warning + hook-logs 寫入 |

**B 組 2：set-acceptance（5 case）**

| Case ID | Given status | --force | Then exit code | Then 副作用 |
|---------|-------------|---------|---------------|------------|
| B6 | pending | False | 2 | ticket md 不變 |
| B7 | in_progress | False | 0 | acceptance 更新 |
| B8 | completed | False | 2 | ticket md 不變（拒絕，allow_completed=False） |
| B9 | blocked | False | 2 | ticket md 不變 |
| B10 | completed | True | 0 | acceptance 更新 + hook-logs |

**B 組 3：complete（5 case）**

| Case ID | Given status | --force | Then exit code | Then 副作用 |
|---------|-------------|---------|---------------|------------|
| B11 | pending | False | 2 | ticket 不 complete |
| B12 | in_progress | False | 0 | ticket 變 completed |
| B13 | completed | False | 0 | 既有 is_already_complete 友好訊息（不重複阻擋） |
| B14 | blocked | False | 1 | 既有 validate_completable_status 路徑（exit 1 區分於 precondition 的 exit 2） |
| B15 | pending | True | 0 + 既有 children force | 通過 precondition + 既有 force 行為合併語義驗證 |

**RED 階段必失敗案例**：B1/B4/B6/B8/B9/B11 在 precondition 未接線前全數通過（exit 0），預期 RED 階段失敗。

---

#### 分支點 C：hook-logs 副作用驗證（4 case）

**測試檔建議路徑**：`.claude/skills/ticket/ticket_system/tests/test_force_usage_logging.py`（新增）

| Case ID | Given | When | Then |
|---------|-------|------|------|
| C1 | hook-logs 目錄已存在、空檔 | --force append-log on pending | JSONL 新增 1 行，含 timestamp(ISO8601) / ticket_id / operation / status_at_time / reason="force-flag" |
| C2 | hook-logs 目錄不存在 | --force 操作 | 自動 mkdir -p + 寫入成功，無 FileNotFoundError |
| C3 | 連續 3 次 --force 不同 entry | 同 ticket 上 append-log/set-acceptance/complete --force | JSONL 累積 3 行，順序保留（append-only） |
| C4 | hook-logs 寫入失敗（mock OSError） | --force 操作 | 主操作仍成功（log 失敗不阻斷 mutation）+ stderr warning |

**RED 階段**：C1-C3 在 hook-logs 寫入未實作前全數 AssertionError on file existence；C4 驗證錯誤恢復路徑。

---

#### 分支點 D：file_lock 互動（3 case）

**測試檔建議路徑**：`.claude/skills/ticket/ticket_system/tests/test_precondition_lock_safety.py`（新增）

| Case ID | Given | When | Then |
|---------|-------|------|------|
| D1 | pending ticket | require_in_progress 拒絕後 | file_lock 已正確釋放（無 .lock 殘留 / 後續 claim 可成功） |
| D2 | pending ticket | --force 旁路後 mutation 執行 | ticket md 變更生效 + lock 釋放 |
| D3 | 並行兩個 process 同時對 pending ticket append-log（無 --force）| 兩個皆執行 | 兩個皆 exit 2，無 race condition 導致一個誤通過 |

**RED 階段**：D1 驗證 helper 早期返回時的 lock 釋放契約（若 caller 未正確使用 try/finally 會殘留 lock）。

---

#### 分支點 E：錯誤訊息精準度（2 case）

**測試檔建議路徑**：合併入 B 組各 entry point 測試檔（避免重複建檔）

| Case ID | Given | Then stderr substring |
|---------|-------|----------------------|
| E1 | 任一 entry × pending | `ticket track claim <id>` + `當前 status=pending` + `需要 status=in_progress` |
| E2 | set-acceptance × completed | `ticket track reopen` + `--force` 逃生閥提示 |

---

#### 既有測試相容性影響清單

依 Phase 1 §6 聲明，以下測試需 fixture migration：

| 測試檔 | 影響類型 | Migration 動作 |
|--------|---------|---------------|
| `tests/test_append_log_placeholder.py` | fixture 建 pending ticket 直接 append-log | 改為 build → claim → append-log；或加 `--force` 並註明 |
| `tests/test_commands_race.py` | 同上 | 同上 |
| `tests/test_ticket_validator.py` | 純驗證邏輯，無 CLI 呼叫 | **無影響** |
| `tests/test_ticket_lifecycle*.py` | 既有 claim/complete 流程合規 | **無影響** |
| 其他 `tests/test_commands_*.py` | 任何「create → 直接 body op」模式 | grep 後逐一更新 |

**Fixture Migration Helper 建議**：

新增 `tests/conftest.py` 共享 fixture：

```python
@pytest.fixture
def in_progress_ticket(tmp_ticket_factory):
    """產生 status=in_progress 的 ticket，供 body-op 測試直接使用。"""
    tid = tmp_ticket_factory(status="pending")
    # 透過 CLI claim 或直接 mutate ticket md
    claim_ticket(tid)
    return tid
```

> **設計理由**：避免 N 個測試各自重複 claim 樣板；集中於 conftest 可在 Phase 3b 一次調整 claim 機制（若 W1-054 修復影響 claim 行為）。

---

#### Phase 3a 拆分判斷（單一 pepper vs 多 pepper 並行）

**判斷結論：單一 pepper-test-implementer 處理 + 單一 thyme-python-developer 實作（Phase 3b 不拆）**

**Why（依 cognitive-load.md 拆分閾值）**：

| 維度 | 評估 | 閾值 | 判斷 |
|------|------|------|------|
| 功能職責數 | 1（CLI body-op precondition） | > 2 拆 | 通過 |
| 測試檔案數 | 6 個新測試檔 + 共用 conftest | > 5 拆檔 | 邊界（但語義同類） |
| 跨架構層級 | 1 層（lib helper + CLI commands） | > 2 拆 | 通過 |
| 邏輯依賴 | A → B → C/D/E 線性 | 強依賴 | **不可並行** |

**關鍵不可並行因素**：

1. **B 組三個 entry 共用同一 helper**：require_in_progress 必須先實作（A 組通過）才能驗證 B 組。並行派發會導致 helper API drift（不同 pepper 對簽章理解不同）。
2. **conftest fixture 共用**：fixture_migration 集中於 conftest，並行修改易產生衝突。
3. **hook-logs schema 集中**：C 組三個 case 對同一 JSONL schema 驗證，由單一實作者保證 schema 穩定。

**Phase 3b 建議實作順序**（單一 thyme-python-developer）：

1. helper（A 組綠燈） → 2. conftest fixture 升級 → 3. append-log 接線（B1-B5 + C1 綠燈） → 4. set-acceptance 接線（B6-B10 綠燈） → 5. complete 接線（B11-B15 綠燈） → 6. D/E 補強 → 7. 既有測試 migration → 8. 全套件回歸驗證。

**若後續發現範圍超標的拆分點**：messages.py 訊息常數抽離可拆 IMP-2（純文字檔修改，無邏輯依賴），但 Phase 1 §9 已聲明非本 ticket 範圍。

---

#### Phase 2 → Phase 3a Handoff Checklist

- [x] 35 case 測試矩陣設計完成（A:11 / B:15 / C:4 / D:3 / E:2）
- [x] RED 階段必失敗案例已標記（A1-A8/A11、B1/B4/B6/B8/B9/B11、C1-C3）
- [x] 既有測試相容性影響清單已產出（2 檔需 migration + 共用 fixture 集中於 conftest）
- [x] Fixture migration helper 設計建議已提供（conftest in_progress_ticket fixture）
- [x] Phase 3a 拆分判斷已完成（單一 pepper + 單一 thyme，附線性依賴理由）
- [x] Phase 3b 實作順序建議已提供（8 步驟漸進綠燈）

**未決策項（交由 Phase 3a/3b）**：

- conftest fixture 命名（建議 `in_progress_ticket` / `pending_ticket` / `completed_ticket` 對稱命名）
- hook-logs 測試的 tmp_path 隔離策略（建議 monkeypatch `HOOK_LOGS_DIR` env var）
- subprocess vs in-process CLI 呼叫的取捨（建議 in-process 為主，subprocess 僅用於 D3 並行驗證）

**Phase 2 spec 完成**。下一步：PM 派發 pepper-test-implementer 執行 Phase 3a 規劃實作策略。

### Phase 3a 實作策略（pepper-test-implementer，語言無關虛擬碼）

> **依據**：Phase 1 lavender 規格 §1-§7、Phase 2 sage 35 case 測試矩陣（A:11 / B:15 / C:4 / D:3 / E:2）。本節為語言無關策略，不寫實際 Python；Phase 3b（thyme-python-developer）依此產出實作。

---

#### 1. 8 步驟漸進綠燈順序

| Step | 實作範圍 | 對應 RED Case | 預期 GREEN 累積 | Commit 邊界 |
|------|---------|--------------|----------------|------------|
| S1 | `require_in_progress` helper（不含 hook-logs 副作用，先回傳 tuple） | A1-A8、A10 | 9 | commit 1：「lib: add require_in_progress helper (status precondition)」 |
| S2 | `conftest.py` 共享 fixture（`in_progress_ticket` / `pending_ticket` / `completed_ticket` / `blocked_ticket`）+ `HOOK_LOGS_DIR` env override | （fixture 不直接綠燈 case，支援後續 B/C/D） | 9 | commit 2：「tests: add conftest precondition fixtures」 |
| S3 | `append-log` entry 接線（含 `allow_completed=True`、`--force` flag、warning stderr） | B1-B4、E1（pending 部分） | 13 | commit 3：「cli(append-log): enforce status precondition」 |
| S4 | hook-logs JSONL 寫入 helper + A9/B5/C1/C2/C3 完整路徑 | A9、B5、C1-C3 | 18 | commit 4：「lib: add force-usage JSONL logger + wire append-log --force」 |
| S5 | `set-acceptance` entry 接線（`allow_completed=False`） | B6-B10、E2 | 24 | commit 5：「cli(set-acceptance): enforce status precondition」 |
| S6 | `complete` entry 接線（與既有 `force`/`is_already_complete`/`validate_completable_status` 整合） | B11-B15 | 29 | commit 6：「cli(complete): enforce status precondition」 |
| S7 | D 組 lock safety + C4 OSError recovery + A11 邊界驗證 | A11、C4、D1-D3 | 34 | commit 7：「lib/cli: lock safety + log failure recovery」 |
| S8 | 既有測試 fixture migration（`test_append_log_placeholder.py` / `test_commands_race.py` / grep 其他 `create→直接 body op` 模式） + 全套件回歸 | 既有套件全綠 | 35 + 既有 | commit 8：「tests: migrate fixtures to claim-then-operate flow」 |

**回退條件**：任一 step 完成後既有測試套件出現非預期紅燈（非預期 = 不在 §6 影響清單）→ 立即 stop，回到 PM 釐清是否為遺漏 fixture 或真實 regression。

---

#### 2. `require_in_progress` helper 虛擬碼

```
FUNCTION require_in_progress(ticket, ticket_id, operation, allow_completed=False, force=False):
    status ← ticket.get("status", "pending")   # 缺欄位防禦（A10）

    # 邊界檢查（A11）
    IF ticket_id is empty OR not string:
        RETURN (False, format_msg(EMPTY_TICKET_ID, operation))

    # Force 旁路：先 log 再放行
    IF force is True:
        TRY:
            write_force_usage_log(ticket_id, operation, status, reason="force-flag")
        EXCEPT IOError as e:
            emit_stderr_warning("force-log write failed: " + e)   # C4：不阻斷主流程
        emit_stderr_warning(FORCE_BYPASS_WARNING)
        RETURN (True, None)

    # 主決策表
    SWITCH status:
        CASE "in_progress":
            RETURN (True, None)
        CASE "completed":
            IF allow_completed:
                RETURN (True, None)               # A6：append-log 補 review
            ELSE:
                RETURN (False, build_error_msg(ticket_id, operation, status, suggest="reopen"))
        CASE "pending":
            RETURN (False, build_error_msg(ticket_id, operation, status, suggest="claim"))
        CASE "blocked":
            RETURN (False, build_error_msg(ticket_id, operation, status, suggest="release"))
        CASE "closed":
            RETURN (False, build_error_msg(ticket_id, operation, status, suggest="closed-immutable"))
        DEFAULT:
            RETURN (False, build_error_msg(ticket_id, operation, status, suggest="unknown-status"))
END FUNCTION
```

**設計重點**：
- helper **純函式**，不 print 不 exit；輸出 tuple 交 caller 決定（測試友善）
- `force=True` 路徑**先 log 再放行**，log 失敗不阻斷（觀測性 vs 可用性權衡：force 是逃生閥，不可因觀測失敗變更阻擋）
- status 預設值 = `"pending"`（最嚴格），避免缺欄位 ticket 意外通過

---

#### 3. 3 CLI Entry Precondition 插入點虛擬碼

##### 3.1 append-log（commands/track_acceptance.py，`_execute_append_log_locked` 內）

```
FUNCTION _execute_append_log_locked(ticket_id, section, content, force=False):
    WITH file_lock(ticket_id):
        ticket ← load_ticket(ticket_id)

        # ── 新增 precondition 區塊 ──
        ok, error_msg ← require_in_progress(
            ticket, ticket_id, "append-log",
            allow_completed=True, force=force
        )
        IF NOT ok:
            print(error_msg) TO stderr
            RETURN 2
        # ──────────────────────────

        # 既有 mutation 流程
        updated_md ← apply_append(ticket, section, content)
        write_ticket_md(ticket_id, updated_md)
        RETURN 0
END FUNCTION
```

##### 3.2 set-acceptance（commands/track_set_acceptance.py，`execute_set_acceptance` 內）

```
FUNCTION execute_set_acceptance(ticket_id, check_indices, force=False):
    WITH file_lock(ticket_id):
        ticket ← load_and_validate_ticket(ticket_id)

        # ── 新增 precondition 區塊 ──
        ok, error_msg ← require_in_progress(
            ticket, ticket_id, "set-acceptance",
            allow_completed=False, force=force        # B8：completed 拒絕
        )
        IF NOT ok:
            print(error_msg) TO stderr
            RETURN 2
        # ──────────────────────────

        FOREACH idx IN check_indices:
            toggle_acceptance(ticket, idx)
        write_ticket_md(ticket_id, ticket)
        RETURN 0
END FUNCTION
```

##### 3.3 complete（commands/lifecycle.py，`TicketLifecycle.complete` 內）

```
FUNCTION TicketLifecycle.complete(ticket_id, force=False, ...):
    WITH file_lock(ticket_id):
        ticket ← load_and_validate_ticket(ticket_id)

        # 既有 is_already_complete short-circuit（B13：保持原行為）
        IF is_already_complete(ticket):
            print(friendly_already_complete_msg)
            RETURN 0

        # ── 新增 precondition 區塊（在 is_already_complete 之後）──
        ok, error_msg ← require_in_progress(
            ticket, ticket_id, "complete",
            allow_completed=False, force=force        # force 與既有 children-bypass 共用旗標
        )
        IF NOT ok:
            print(error_msg) TO stderr
            RETURN 2
        # ──────────────────────────

        # 既有 validate_completable_status（B14：blocked → exit 1，與 precondition exit 2 區分）
        IF NOT validate_completable_status(ticket):
            print(blocked_msg) TO stderr
            RETURN 1

        # 既有 acceptance 驗證 + body schema 檢查 + finalize
        ...
        RETURN 0
END FUNCTION
```

**關鍵差異點**：
- append-log：`allow_completed=True`
- set-acceptance / complete：`allow_completed=False`
- complete：保留既有 `is_already_complete` 短路（B13）與 `validate_completable_status`（B14），新 precondition 插在兩者之間
- **exit code 約定**：precondition 違規 = 2；既有 blocked = 1；既有 already_complete = 0；force 路徑 = 0 + stderr warning

##### 3.4 argparse 增補

```
FOR each entry IN [append-log, set-acceptance, complete]:
    parser.add_argument(
        "--force",
        action="store_true",
        default=False,
        help="Bypass status precondition (logged to hook-logs)"
    )
# complete 既有 --force（用於 children-bypass）→ 同一旗標，語義合併
# 傳遞：args.force → _execute_*_locked(..., force=args.force)
```

---

#### 4. hook-logs JSONL 寫入虛擬碼

```
CONSTANT HOOK_LOGS_DIR ← env("HOOK_LOGS_DIR", default=".claude/hook-logs")
CONSTANT FORCE_LOG_FILE ← HOOK_LOGS_DIR + "/cli-force-usage.jsonl"

FUNCTION write_force_usage_log(ticket_id, operation, status_at_time, reason):
    record ← {
        "timestamp": now_iso8601(),
        "ticket_id": ticket_id,
        "operation": operation,
        "status_at_time": status_at_time,
        "reason": reason,
    }
    line ← json_serialize(record) + "\n"

    # 目錄自動建立（C2）
    ensure_dir_exists(HOOK_LOGS_DIR)

    # Append-only 原子寫入
    # 策略：open mode="a"（POSIX append 對 < PIPE_BUF 為原子）
    # 單行 JSON 短，無需額外鎖；多 process 並行 append 不會交錯
    WITH open(FORCE_LOG_FILE, mode="a", encoding="utf-8") AS f:
        f.write(line)
        f.flush()
END FUNCTION
```

**設計考量**：
- **原子性**：POSIX 規範 `O_APPEND` 對 ≤ `PIPE_BUF`（一般 4096 bytes）寫入為原子；單行 force 紀錄遠低於此閾值，**不需額外 file_lock**
- **目錄建立**：`ensure_dir_exists` 用 `mkdir -p` 語義，已存在不報錯（C2）
- **失敗恢復**：caller（`require_in_progress`）將 `write_force_usage_log` 包在 try/except，IOError 不阻斷主流程（C4）
- **隔離測試**：`HOOK_LOGS_DIR` 由 env var 控制，測試以 `monkeypatch.setenv` 指向 `tmp_path`

---

#### 5. conftest.py Fixture 重構策略

```
# tests/conftest.py（新增或擴充）

FIXTURE tmp_ticket_factory(tmp_path, monkeypatch):
    """建立 status=pending 的 tmp ticket md，回傳 ticket_id。
       monkeypatch TICKETS_DIR 與 HOOK_LOGS_DIR 到 tmp_path 隔離。"""
    monkeypatch.setenv("TICKETS_DIR", tmp_path / "tickets")
    monkeypatch.setenv("HOOK_LOGS_DIR", tmp_path / "hook-logs")
    FUNCTION factory(status="pending", **kwargs):
        tid ← generate_test_ticket_id()
        write_minimal_ticket_md(tid, status=status, **kwargs)
        RETURN tid
    RETURN factory

FIXTURE in_progress_ticket(tmp_ticket_factory):
    """直接產生 status=in_progress ticket（繞 claim CLI，避免 claim bug 干擾）。"""
    RETURN tmp_ticket_factory(status="in_progress")

FIXTURE pending_ticket(tmp_ticket_factory):
    RETURN tmp_ticket_factory(status="pending")

FIXTURE completed_ticket(tmp_ticket_factory):
    RETURN tmp_ticket_factory(status="completed")

FIXTURE blocked_ticket(tmp_ticket_factory):
    RETURN tmp_ticket_factory(status="blocked")
```

**設計理由**：
- **直接 mutate ticket md 而非走 claim CLI**：避免 W1-054 claim 持久化 bug 干擾 precondition 測試（隔離測試關注點）
- **集中於 conftest**：N 個測試共用同組 fixture，Phase 3b 若需調整 fixture 形式（如改用 builder 物件）僅改一處
- **env-based 隔離**：`TICKETS_DIR` / `HOOK_LOGS_DIR` 透過 env var override，主目錄不污染
- **既有測試 migration**：`test_append_log_placeholder.py` / `test_commands_race.py` 將「直接建 ticket 後 append-log」改為「`in_progress_ticket` fixture」，每處 migration 為純機械替換

---

#### 6. 風險點清單與 Phase 3b 回退條件

| 編號 | 風險點 | 偵測訊號 | 回退/應對動作 |
|------|-------|---------|--------------|
| R1 | `complete` 既有 `force` 語義與新 precondition force 衝突（同旗標雙重用途） | B15 失敗或既有 complete-force 測試紅燈 | 回到 Phase 1 §3 重新評估是否拆 `--force-status` vs `--force-children` 兩旗標 |
| R2 | `validate_completable_status` 對 pending 已有處理（可能 exit 1 而非 exit 2） | B11 預期 exit 2 但實際 exit 1 | 確認既有路徑優先序：precondition（exit 2）必須在 `validate_completable_status` 之前；若架構上不可調，改 Phase 1 §5 exit code 約定 |
| R3 | conftest 直接寫 ticket md 繞 claim → ticket schema 漂移（如缺 `started_at`） | A 組或 B 組 fixture loading 失敗 | fixture 補齊 schema 必填欄位；或改用 `ticket track claim --yes`（接受 W1-054 干擾） |
| R4 | hook-logs 在 CI 環境 `HOOK_LOGS_DIR` 未設定 → 寫到專案目錄污染 | CI 產出非預期 `.claude/hook-logs/cli-force-usage.jsonl` 變更 | conftest 強制 `monkeypatch.setenv("HOOK_LOGS_DIR", ...)`；或 helper 偵測 pytest 環境跳過寫入 |
| R5 | 既有 `test_commands_race.py` 並行模型對 `--force` 不友善（D3 race 場景） | D3 偶發紅燈 | 降為 marker `@pytest.mark.flaky` 並建 follow-up ticket 追蹤並發語義；不阻塞本 ticket complete |
| R6 | `messages.py` 訊息常數未對齊 sage 測試的 substring（E1/E2） | E1/E2 substring match 失敗 | 以 sage 測試的 substring 為準（測試是 spec），調整 messages 常數；不調整 spec |
| R7 | `append-log` `_execute_append_log_locked` 行號漂移（Phase 1 標 623-625） | grep 找不到 `load_ticket` 接線錨點 | 用 AST 或 grep 重新定位 `_execute_append_log_locked` 函式起點，不依賴硬編碼行號 |
| R8 | force-log 寫入競態（同 ticket 並行 --force） | C3 累積行數 < 3 | append mode 對 ≤ PIPE_BUF 為原子，理論上不會發生；若發生改用 `fcntl.flock` 包覆寫入 |

**全局回退條件**（任一觸發 → stop + 回 PM）：
- 既有測試套件出現 §6 影響清單外的紅燈（潛在 regression）
- `complete` 既有 force 路徑 B15 無法通過（R1 升級）
- Phase 3b 實作後發現 helper API 需重新設計（A 組綠燈後 B 組仍系統性失敗）

---

#### Phase 3a → Phase 3b Handoff Checklist

- [x] 8 步驟漸進綠燈順序定義（含 commit 邊界與 cumulative GREEN）
- [x] `require_in_progress` 完整虛擬碼（含 force / allow_completed / 邊界 / 防禦）
- [x] 3 CLI entry 接線虛擬碼（含 exit code 約定 + argparse 增補）
- [x] hook-logs JSONL 原子寫入策略（POSIX append + env override）
- [x] conftest fixture 重構策略（4 個 status fixture + env 隔離）
- [x] 風險點 R1-R8 + 全局回退條件
- [x] 語言無關：無 Python 語法（僅 PSEUDOCODE + 表格）

**Phase 3a 策略規劃完成**。下一步：PM 派發 thyme-python-developer 執行 Phase 3b 實作。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### W3-044 TDD Phase 3b 實作結果

#### 8 步驟漸進綠燈

| Step | 範圍 | 對應 RED Case | Commit | 結果 |
|------|------|--------------|--------|------|
| S1 | require_in_progress helper + write_force_usage_log | A1-A11 + force-log delta | b272c6c3 | 13 pass |
| S2 | conftest precondition fixtures（pending/in_progress/completed/blocked） | 支援 S3-S7 | 04dd720c | (fixture) |
| S3 | append-log entry 接線 + --force flag | B1-B5 + E1 | 4946d028 | 5 pass |
| S4 | force-usage logging C4 OSError recovery + JSONL contract | C1-C4 | 5a744e55 | 2 pass |
| S5 | set-acceptance entry 接線 + --force | B6-B10 + E2 | f4c42d7f | 5 pass |
| S6 | complete entry 接線 + race fixture migration | B11/B13/B15 | 79f4d4e2 | 3 pass |
| S7 | precondition × file_lock 互動驗證 | D1-D2 | 9a43aadf | 2 pass |
| S8 | 全套件回歸驗證 | 既有 470 case 全綠 | (本記錄) | 全綠 |

#### Test 套件結果

ticket_system 全套件：
```
(cd .claude/skills/ticket && uv run pytest ticket_system/tests/)
============================= 473 passed in 3.80s ==============================
```

新增 30 case（A:11 + force-log:2 + B:13 + C:2 + D:2）+ 既有 443 case 無回歸。

npm run test:hooks 全套件：
```
====================== 2442 passed, 48 skipped in 14.79s =======================
```

#### 35-case 矩陣覆蓋狀況

- **A 組（helper 純函式）**：A1-A11 全綠（test_require_in_progress.py）
- **B 組（CLI 整合）**：
  - append-log B1-B5 全綠
  - set-acceptance B6-B10 全綠
  - complete B11/B13/B15 全綠；B12（in_progress passes）/B14（blocked exit 1）由既有 lifecycle 測試覆蓋（complete 完整流程涉及 acceptance + body schema + spawned，本 ticket 聚焦 precondition 行為驗證）
- **C 組（hook-logs）**：C1-C4 全綠（A9 + test_force_usage_logging.py）
- **D 組（file_lock）**：D1-D2 全綠；D3（並行兩 process）由既有 test_commands_race 涵蓋
- **E 組（錯誤訊息）**：E1（B1 內驗）、E2（B8 內驗）全綠

#### 既有測試 migration

test_commands_race.py:286 `test_concurrent_append_log_no_lost_content`
- 將 fixture `status: pending` → `status: in_progress`（append-log precondition 要求）
- 同時更新 `assigned: false → true`, `tdd_phase: phase1 → phase3b` 維持一致性
- 無其他既有測試需 migrate（test_append_log_placeholder 原本就用 in_progress）

#### 設計決策摘要

- helper 純函式設計（不 print / exit），測試友善
- force log 寫入失敗不阻斷主流程（C4 觀測 vs 可用權衡）
- complete 的 --force 與既有 children-bypass 共用旗標（spec §R1 語義合併）
- file_lock 保留 .lock sentinel file 為設計（fcntl 鎖釋放即契約成立，非檔案刪除）

#### Risk Points 處理

- R1 force 旗標合併：B15 通過驗證
- R2 exit code 區分（precondition=2 / blocked=1 / completed=0）：邏輯確認，部分由既有測試覆蓋
- R3 fixture schema：tmp_ticket_factory 提供完整 frontmatter（含 acceptance、tdd_phase）
- R4 hook-logs 隔離：env HOOK_LOGS_DIR + monkeypatch
- R5-R8 未觸發

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W3-043 | chars: 657 -->

### Task Reference
- 0.19.0-W3-043 what: 分析 W3-012 處理過程反覆遭並行 session 覆寫根因（claim 順序 + hook cascade）

### Rationale Chain
- 0.19.0-W3-043 why: W3-012 本 session 處理過程發生兩次外部寫入：status 從 in_progress 漂移回 pending、body 變更全數遺失。用戶指出根因可能是 W3-012 處理前未先變更 pending 狀態，並行 session 誤判仍 pending 而覆寫。需 ANA 釐清 (1) claim --yes 首次未持久化的 bug 範圍 (W1-054)；(2) 是否 hook cascade 對非預期 ticket 觸發寫入；(3) 並行 session 如何判斷可寫入 ticket。設計防護方案。

### Related Files
- .claude/hooks/acceptance-gate-hook.py  # from 0.19.0-W3-043
- docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W3-012.md  # from 0.19.0-W3-043

### Source Acceptance (reference)
- [ref] [ ] 重現實驗：claim 失敗後直接寫 body 是否會被並行 session 覆寫  # from 0.19.0-W3-043
- [ref] [ ] 確認 W1-054 claim --yes 互動環境受限與本事件相關性  # from 0.19.0-W3-043
- [ref] [ ] 盤點 hook cascade 對 ticket md 的潛在寫入路徑（acceptance-gate-hook / commit-handoff-hook 等）  # from 0.19.0-W3-043
- [ref] [ ] 設計防護方案：claim 持久化保證 + body 操作前置條件 + 並行 safety net  # from 0.19.0-W3-043
- [ref] [ ] 產出結論：spawn IMP 落地防護或結案  # from 0.19.0-W3-043