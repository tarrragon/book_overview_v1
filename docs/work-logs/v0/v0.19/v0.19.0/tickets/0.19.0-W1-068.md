---
id: 0.19.0-W1-068
title: 實作 append-log CLI 自動降級 H2 → H3（W1-038 方案 B 落地）
type: IMP
status: completed
version: 0.19.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W1-038
dispatch_reason: ''
decision_tree_path:
  entry_point: ANA 結論落地:方案 B 工具層改動
  final_decision: append-log CLI 寫入時 re.sub 規範化 H2 為 H3
  rationale: W1-038 ANA 結論-源頭阻斷 > 事後偵測-工具層改動確定性最高
who:
  current: thyme-python-developer
  history: {}
what: 實作 append-log CLI 自動降級 H2 → H3（W1-038 方案 B 落地）
when: 本 ticket 為 framework 改善，可獨立排程不阻擋業務任務；建議與其他 ANA 落地任務一起執行
where:
  layer: 待定義
  files:
  - .claude/skills/ticket/ticket_system/commands/track_acceptance.py
  - .claude/skills/ticket/tests/
  - docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W1-038.md
why: W1-038 ANA 結論方案 B：append-log 寫入時 re.sub H2 → H3 規範化，源頭阻斷 agent 自定義 H2 進入 ticket
  md；解決 W1-037 三 agent 連續違規 + PM 批次降級 H2 連帶降級 H3 子標題的連鎖效應（acceptance-gate-hook false
  negative）
how:
  task_type: Implementation
  strategy: (1) 修改 track_acceptance.py _execute_append_log_locked 函式：在 W17-208 stderr
    warning 後加 content = re.sub(r'(?m)^## ', '### ', content) (2) 新增測試 covering 自動降級行為（含
    multi-line H2、純文字 H2、無 H2 三類）(3) 確認既有測試無回歸 (4) uv tool install . --force --reinstall
    同步部署 (5) ticket-skill-sync-check：grep .claude/skills/ticket/SKILL.md + pm-rules/
    對 append-log H2 處理的描述，若不一致同步更新 (6) commit policy 限 .claude/skills/ticket/ + ticket
    md
acceptance:
- '[x] track_acceptance.py _execute_append_log_locked 在 W17-208 warning 後執行 re.sub
  H2 → H3 規範化'
- '[x] 新增測試 3 類別（multi H2 / 純文字 H2 / 無 H2 不變）皆通過'
- '[x] uv tool install --reinstall 完成且 ticket CLI 行為驗證 H2 自動降級'
- '[x] SKILL.md / pm-rules/ 對 append-log H2 處理的描述已同步（含 W17-208 行為更新）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-25T01:12:20'
completed_at: '2026-05-25T01:35:29'
created: '2026-05-25'
updated: '2026-05-25'
---

# Execution Log

## Task Summary

實作 append-log CLI 自動降級 H2 → H3（W1-038 方案 B 落地）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 實作摘要（smoke test 故意 H2，預期 reinstall 後自動降級為 H3）

PM 主線程接手 thyme-python-developer 因 PC-115 isolation:worktree deny 退回的工作。

### 核心改動

`.claude/skills/ticket/ticket_system/commands/track_acceptance.py` 行 638-656（W17-208 區塊）：
- W17-208 stderr warning 文案附加「已自動降級 H2 → H3（W1-068 方案 B：源頭阻斷）」
- 新增 `content = re.sub(r'(?m)^## ', '### ', content)` 在 warning 之後執行
- 只匹配行首 `## ` 後接空格（不影響 H3/H4+）

### 測試新增

`.claude/skills/ticket/tests/test_track_acceptance.py` `TestAppendLogH2Warning` class 新增 3 測試：
- `test_h2_auto_downgraded_to_h3`：多 `## H2` 全降為 `### H3` 且 warning 含「自動降級」
- `test_pure_text_unchanged`：純文字無 H2，不觸發降級也無 warning
- `test_existing_h3_h4_unaffected`：已含 `###` / `####` 不受影響

### 部署

- `uv tool install /Users/tarragon/Projects/book_overview_v1/.claude/skills/ticket --force --reinstall` 完成
- ticket CLI 已更新

### 文件同步

`.claude/pm-rules/context-bundle-spec.md` 條款 2「Action」新增「雙層防護」表格，註明 W17-208 + W1-068 寫入層 + W17-072 complete 層分工；禁止行為表更新「PM 用 append-log 寫 ## 實作策略」描述為「W17-208 stderr warning + W1-068 自動降級」。

### 設計決策

- **未動 acceptance-gate-hook**：W17-072 complete 層偵測維持現況作最後一道防線（涵蓋手動 Edit ticket md 等非 append-log 路徑），與 W1-068 寫入層雙層防護互補
- **未在 SKILL.md 新增說明**：W1-068 為實作細節，不影響用戶 CLI 介面（仍可寫 H2，會自動轉），保持 SKILL.md high-level 純淨
- **不破壞既有 W17-208 行為**：stderr warning 仍保留作觀測訊號（agent 可從 warning 知道有降級發生）

### CLI 變更（smoke test 故意 H2）

本段第一行的 `## CLI 變更` 為 smoke test 預期會被自動降級為 `### CLI 變更`。讀取本 ticket 後若該標題顯示為 `### CLI 變更`（而非 `## CLI 變更`），即 smoke test PASS。

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 單元測試（test_track_acceptance.py）

```bash
uv --directory .claude/skills/ticket run pytest tests/test_track_acceptance.py -v
```

結果：**27/27 passed**（0.40s）

新增 3 測試（TestAppendLogH2Warning class）全 PASS：

| Test | 驗證 |
|------|------|
| test_h2_auto_downgraded_to_h3 | 多 `## H2` 全降為 `### H3` + warning 含「自動降級」 |
| test_pure_text_unchanged | 純文字無 H2，不觸發降級也無 warning |
| test_existing_h3_h4_unaffected | `### H3` / `#### H4` 不受影響（只匹配行首 H2） |

既有 24 測試無回歸：TestCheckAcceptance (5) + TestAppendLog (6) + TestAppendLogH2Warning 原 3 + TestAppendLogSectionMatching (6) + TestExecuteLog* + TestCheckAcceptanceAuto* 等。

### 全套件測試（tests/ 全量）

```bash
uv --directory .claude/skills/ticket run pytest tests/
```

結果：**exit code 0**（背景 task `bkgbks5yo` 完成，輸出因 chpwd hook 干擾被 ls 覆蓋，但 exit code 0 確認全綠無回歸）

第二次驗證（前景重跑，無 cd 避免 chpwd）：

```bash
uv --directory /Users/tarragon/Projects/book_overview_v1/.claude/skills/ticket run pytest tests/
```

結果：背景 task `bhj8bclmy`（待補完整通過數）

### CLI Smoke Test

```bash
ticket track append-log 0.19.0-W1-068 --section "Solution" "## 實作摘要... ## CLI 變更..."
```

故意寫入兩段 `## H2` 標題到 Solution 章節。

驗證結果（grep ticket md）：

| 預期 | 實際 |
|------|------|
| `## 實作摘要` 自動降級為 `### 實作摘要` | 行 97 確認 `### 實作摘要` |
| `## CLI 變更` 自動降級為 `### CLI 變更` | 行 130 確認 `### CLI 變更` |
| 既有 `## Solution` schema header 保留 | `^## ` 計數 8 個（schema headers 全保留） |

**結論**：自動降級 H2 → H3 在 source-level 生效，與單元測試行為一致。

### 部署狀態

`uv tool install /Users/tarragon/Projects/book_overview_v1/.claude/skills/ticket --force --reinstall`：

```
Uninstalled 2 packages in 20ms
Installed 2 packages in 2ms
 ~ pyyaml==6.0.3
 ~ ticket-system==1.0.0
Installed 1 executable: ticket
```

CLI 已部署最新版本。

### Commit SHA

待 commit 後補。

## NeedsContext
- **缺失項**: subagent isolation:worktree 環境下無法 Edit `.claude/skills/ticket/ticket_system/commands/track_acceptance.py`
- **觸發位置**: 嘗試對 `.claude/skills/ticket/ticket_system/commands/track_acceptance.py` 行 645-651 Edit 時被 CC runtime 阻擋（Permission to use Edit has been denied）
- **影響**: 無法完成所有 4 個 acceptance（核心實作 + 測試新增 + 部署 + 文件同步）
- **根因推測**: ARCH-015（CC runtime 對 worktree subagent 編輯 `.claude/` 有硬性 deny）+ W17-097.1-.4 / PC-115 仍存在的 DENY 真因
- **建議補料**: 改派發於主 repo cwd（非 worktree），或由 PM 在主線程直接執行此 IMP（檔案改動小：1 個 re.sub + 3 個測試 + 文件同步）。本 ticket prompt 已含完整實作細節與行號定位
- **重派成本**: 低（< 5K tokens），核心改動僅 3 行程式碼 + 3 個測試

## Exit Status
```yaml
status: needs_context
reason: "subagent isolation:worktree 環境下 Edit .claude/skills/ticket/ticket_system/commands/track_acceptance.py 被 CC runtime 硬性 deny（ARCH-015 / PC-115）；無法完成核心程式碼修改與後續測試/部署/文件同步"
confidence: 0.95
acceptance_met: []
acceptance_unmet: [0, 1, 2, 3]
artifacts: []
context_dependencies:
  - .claude/skills/ticket/ticket_system/commands/track_acceptance.py (行 645-651, 需 PM 直接 Edit)
estimated_recovery_effort: "低 (< 5K tokens)。核心改動：track_acceptance.py 加 3 行 re.sub + warning 文案微調；test_track_acceptance.py 新增 3 個測試（multi H2 / 純文字 H2 / 已含 H3 不變）；uv tool install --reinstall；grep SKILL.md / pm-rules 同步。本 ticket prompt 已含完整實作細節與精確行號"
```

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-25T01:35:29
**Executing Agent**: thyme-python-developer
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W1-038 | chars: 438 -->

### Task Reference
- 0.19.0-W1-038 what: 分析 append-log 自定義 H2 系統性違規與防護升級

### Rationale Chain
- 0.19.0-W1-038 why: W1-037 收尾觀察：sage / thyme / cinnamon 三個 agent 透過 ticket track append-log 寫入 Solution 時各自帶 ## Phase X 自定義 H2 標題，違反 W17-072（禁止自定義 H2）。連鎖效應：PM 批次修正 H2 降 H3 時，agent 寫的 ### 自檢結果 被連帶降為 #### H4，使 acceptance-gate-hook 的「### 自檢結果」固定 H3 偵測產生 false negative warning。W17-072 既有 warning 機制未能阻止連續三個 agent 違規，顯示自律層 + warning 層不足。須分析根因（agent 寫作習慣 vs append-log 未規範化）並評估防護升級。

### Related Files
- .claude/skills/ticket/ticket_system/  # from 0.19.0-W1-038
- .claude/hooks/  # from 0.19.0-W1-038