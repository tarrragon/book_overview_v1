---
id: 0.19.0-W1-048.10.1.4
title: 重構 BookFileImporter 改寫為 orchestrator + 測試遷移
type: IMP
status: blocked
version: 0.19.0
wave: 1
priority: P2
parent_id: 0.19.0-W1-048.10.1
children: []
blockedBy:
- 0.19.0-W1-048.10.1.1
- 0.19.0-W1-048.10.1.2
- 0.19.0-W1-048.10.1.3
- 0.19.0-W1-048.10.1.5
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 重構 BookFileImporter 改寫為 orchestrator + 測試遷移
when: W1-048.10.1.3 完成後
where:
  layer: overview / 匯入流程整體
  files:
  - src/overview/book-file-importer.js
  - tests/unit/overview/import-mode-modal.test.js
  - tests/unit/overview/overview-page-controller.test.js
  - tests/unit/overview/overview-import-private-methods.test.js
why: W1-048.10.1 Phase 1 SOLID 拆分結論第 4 個 sub-IMP：BookFileImporter constructor 接 DI
  注入 3 個 helper class，移除 4 個底線私有方法，4 個 public method 改 thin wrapper delegate。測試端改寫
  8 處 jest mock 為注入 stub helper。詳細 DI signature 與兼容策略見父 ticket Solution 章節 B 與 C
how:
  task_type: Implementation
  strategy: 依 W1-048.10.1 Solution B + C 規格實作：(1) BookFileImporter constructor 接 {
    document, showError, validator?, reader?, parser? }；(2) 4 個 public method (handleFileLoad
    / validate / read / parseContent) 改 thin wrapper delegate；(3) 移除 _validateFileBasics
    / _validateFileSize / _readFileWithReader / _handleFileContent；(4) 改寫 import-mode-modal.test.js（5
    處）與 overview-page-controller.test.js（3 處）8 處 jest mock 為注入 stub helper；(5) 更新
    overview-import-private-methods.test.js 反向斷言檢查 importer 不再暴露底線方法
acceptance:
- '[ ] BookFileImporter constructor 接 DI 注入 helper instance'
- '[ ] 移除 4 個底線私有方法（_validateFileBasics / _validateFileSize / _readFileWithReader
  / _handleFileContent）'
- '[ ] 4 個 public method 維持 thin wrapper（向後相容）'
- '[ ] 改寫 import-mode-modal.test.js 與 overview-page-controller.test.js 8 處 jest mock
  為注入 stub'
- '[ ] 更新 overview-import-private-methods.test.js 反向斷言'
- '[ ] 完整測試套件（npm test）全綠'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-05-24'
updated: '2026-05-24'
---

# Execution Log

## Task Summary

重構 BookFileImporter 改寫為 orchestrator + 測試遷移

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### Phase 2 測試設計（2026-05-24，sage-test-architect）

#### 測試策略決策

| 項目 | 決策 | 理由 |
|------|------|------|
| 測試類型 | 整合測試（importer + 注入 stub helper） | importer 為 thin orchestrator，測試重點在 DI 與 delegate 行為 |
| 測試檔案異動 | 改寫 3 既有檔（import-mode-modal.test.js / overview-page-controller.test.js / overview-import-private-methods.test.js）+ 既有 importer 測試檔（book-file-importer.test.js 等）需確認仍綠 | 8 處 jest.fn 直接賦值替換為 stub helper 注入 |
| Mock 策略 | 統一使用 createStubHelpers() 工廠注入 validator/reader/parser stub | 取代 `importer._method = jest.fn()` 反模式 |

#### 共用 stub helper 規格（必須跨檔對齊）

新增 `tests/helpers/import-stub-helpers.js`：

```js
function createStubValidator (overrides = {}) {
  return {
    validate: jest.fn(),
    detectFormat: jest.fn(() => 'json'),
    ...overrides
  }
}
function createStubReader (overrides = {}) {
  return {
    read: jest.fn().mockResolvedValue({ books: [], tagCategories: [], tags: [] }),
    ...overrides
  }
}
function createStubParser (overrides = {}) {
  return {
    parse: jest.fn().mockReturnValue({ books: [], tagCategories: [], tags: [] }),
    ...overrides
  }
}
function createStubHelpers (overrides = {}) {
  return {
    validator: createStubValidator(overrides.validator),
    reader: createStubReader(overrides.reader),
    parser: createStubParser(overrides.parser)
  }
}
```

8 處遷移後 pattern：

```js
// Before（反模式）
controller.bookFileImporter._readFileWithReader = jest.fn().mockResolvedValue(...)

// After（注入 stub helper）
const { validator, reader, parser } = createStubHelpers({
  reader: { read: jest.fn().mockResolvedValue({ books: [...], ... }) }
})
const importer = new BookFileImporter({ document, showError, validator, reader, parser })
controller.bookFileImporter = importer  // 或 controller 建構時注入
```

#### DI Constructor 簽章測試

新增獨立 describe `BookFileImporter constructor (DI)`：

| Scenario | Given | When | Then |
|---------|-------|------|------|
| 預設自建 helpers | 僅傳 {document, showError} | new BookFileImporter | importer.validator instanceof FileValidator；importer.reader instanceof FileContentReader；importer.parser instanceof ContentParser |
| 注入 validator | 傳入 stubValidator | new BookFileImporter | importer.validator === stubValidator |
| 注入 reader | 傳入 stubReader | new BookFileImporter | importer.reader === stubReader |
| 注入 parser | 傳入 stubParser | new BookFileImporter | importer.parser === stubParser |
| reader 預設用內部 parser | 僅傳 stubParser | new BookFileImporter | 預設 reader.parser === stubParser（生產情境鏈一致） |

#### 4 Public Method Thin Wrapper 向後相容測試

每個 public method 一組 GWT，驗證 delegate 行為而非實作細節：

| Method | GWT |
|--------|-----|
| `validate(file)` | Given stubValidator + importer，When validate(file)，Then stubValidator.validate 被呼叫 1 次參數 file |
| `read(file)` | Given stubReader + importer，When read(file)，Then stubReader.read 被呼叫 1 次參數 file；回傳值 === stub resolved value |
| `parseContent(content, fileFormat)` | Given stubParser + importer，When parseContent(c, 'json')，Then stubParser.parse 被呼叫 1 次參數 (c, 'json')；回傳值 === stub return |
| `handleFileLoad(file)` | Given stubValidator + stubReader + importer，When handleFileLoad(file)，Then validate 先被呼叫（順序斷言），read 後被呼叫，回傳 reader 結果 |
| handleFileLoad 失敗短路 | stubValidator.validate throw，When handleFileLoad，Then stubReader.read 不被呼叫 |

#### 既有測試遷移（8 處 jest.fn 賦值）

來源行（grep 結果）：

| 檔案 | 行號 | 原 pattern | 新 pattern |
|------|------|----------|-----------|
| import-mode-modal.test.js | L338, L383, L425, L456, L661 | `importer._readFileWithReader = jest.fn()...` | 注入 stubReader |
| import-mode-modal.test.js | L404 | `importer._validateFileBasics = jest.fn(throw)` | 注入 stubValidator with throw |
| overview-page-controller.test.js | L1178, L1258 | `importer._readFileWithReader = jest.fn()` | 注入 stubReader |
| overview-page-controller.test.js | L1404 | `importer._validateFileBasics = jest.fn(throw)` | 注入 stubValidator with throw |

遷移後既有測試行為（外部觀察）必須完全等價，僅注入點改變。

#### 反向斷言更新（overview-import-private-methods.test.js）

新增 describe `BookFileImporter 不再暴露底線方法`：

| Scenario | 斷言 |
|---------|------|
| 底線方法已移除 | `importer._validateFileBasics === undefined`；`_validateFileSize`、`_readFileWithReader`、`_handleFileContent` 同 |
| DI 注入點存在 | `importer.validator`、`importer.reader`、`importer.parser` 皆為物件且有對應方法 |
| controller proxy 不再暴露 | 既有反向斷言保留（L772 等） |

L293/L345/L530 既有「已透過 handleFileLoad 走 public」測試案例保留（行為已驗證）。

#### 跨 sub-IMP 共用 stub helper 規格（全域，重申）

放置位置：`tests/helpers/import-stub-helpers.js`（新建）
匯出 ESM 命名匯出：`createStubValidator / createStubReader / createStubParser / createStubHelpers / createFakeFileReader / createStubReaderFactory`
- 前四個供 10.1.4 用
- 後兩個供 10.1.3 用（file-reader.test.js 引用）

10.1.1 / 10.1.2 不依賴此 helper（純測試各自 class），但若新測試需 stub showError，可從此檔取 createShowErrorStub。

#### 測試覆蓋率目標

- 既有 `book-file-importer.test.js` / `book-file-importer-import-result.test.js` / `book-file-importer-version-detection.test.js` 三檔測試在 10.1.4 完成後**必須全綠**（不可退化）
- 新增 3 helper 測試檔 + 4 個既有檔改寫後總計覆蓋既有底線方法的 100% 場景
- npm test 全綠（最終驗收門檻）

#### 預期測試案例數

10.1.4 新增 / 改寫：
- DI constructor 測試：~5 case
- 4 public method wrapper：~6 case
- 既有 8 處 jest.fn 遷移：行為等價（不增 case 數）
- 反向斷言：~3 case
總計新增 ~14 case + 8 處遷移

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

### Phase 3a 實作策略（2026-05-24，pepper-test-implementer）

本策略為語言無關虛擬碼與遷移計畫，Phase 3b 由 thyme-extension-engineer 依此落地。

#### A. BookFileImporter 改寫策略

**A.1 Constructor DI 注入點**

當前（src/overview/book-file-importer.js L67）：

```
constructor ({ document, showError }) {
  this.document = document
  this.showError = showError
  // ...無 helper field
}
```

改寫為（依父 ticket Solution §B）：

```
constructor ({ document, showError, validator?, reader?, parser? }) {
  this.document = document
  this.showError = showError

  // 順序關鍵：parser 先於 reader（reader 預設依賴 parser）
  this.parser    = parser    ?? new ContentParser()
  this.validator = validator ?? new FileValidator({ showError })
  this.reader    = reader    ?? new FileContentReader({
                                   parser: this.parser,
                                   showError,
                                   detectFormat: (file) => this.validator.detectFormat(file)
                                 })
}
```

**設計要點**：
- 三 helper 全選用：production 路徑零變更（自建預設實例），test 路徑可注入 stub
- reader 預設 detectFormat 走 validator 委派，保持 validator 為 format 偵測 SSOT
- import 順序：檔頭 require './import/file-validator' / './import/file-reader' / './import/content-parser'（待 10.1.1~10.1.3 落地後存在）

**A.2 4 個 public method 改 thin wrapper**

| Method | 當前實作（行號） | 改寫後虛擬碼 |
|--------|----------------|------------|
| `handleFileLoad(file)` | L82, 內呼 `validate` + `read` | `this.validate(file); return await this.read(file)`（行為等價） |
| `validate(file)` | L97, 內呼 `_validateFileBasics` + `_validateFileSize` | `return this.validator.validate(file)` |
| `read(file)` | L111, 內呼 `_readFileWithReader` | `return this.reader.read(file)` |
| `parseContent(content, fileFormat)` | L127, 含 fileFormat 缺漏 / 非 'json'\|'csv' TypeError + `_handleFileContent` | `return this.parser.parse(content, fileFormat)`（TypeError/PARSE_ERROR 檢查移入 parser.parse 內部，10.1.2 已實作） |

**Wrapper 厚度約束**：每個 wrapper ≤ 3 行（無 try/catch / 無條件分支），保持「純 delegate」純度。errorshowError 副作用由各 helper 內部處理（既有設計）。

**A.3 移除 4 個底線方法 + 17 子 helper 搬遷對照**

| 原方法（行號） | 搬遷目的地 |
|--------------|-----------|
| `_validateFileBasics` (L146) | FileValidator（10.1.1）|
| `_isJSONFile` (L169) / `_isCSVFile` (L185) | FileValidator (作為 detectFormat 內部) |
| `_validateFileSize` (L198) | FileValidator |
| `_readFileWithReader` (L214) | FileContentReader（10.1.3）|
| `_createFileReader` (L228) / `_loadFileReaderFactory` (L250) | FileContentReader |
| `_setupReaderHandlers` (L267) / `_handleReaderSuccess` (L280) / `_handleReaderError` (L294) / `_handleFileProcessError` (L309) | FileContentReader |
| `_handleFileContent` (L322) | ContentParser（10.1.2）|
| `_parseCSVContent` / `_parseCSVRows` / `_consumeQuotedChar` / `_consumeUnquotedChar` / `_commitRow` / `_flushPendingRow` / `_csvRowToBook` / `_deserializeCSVField` / `_isBlankRow` (L343-523) | ContentParser |
| `_validateAndCleanContent` / `_removeBOM` (L531/L547) | ContentParser |
| `_parseJSONContent` (L561) | ContentParser |
| `_processBookData` (L587) / `_filterValidBooks` (L604) / `_checkLargeDataset` (L613) | ContentParser |
| `_extractBooksFromData` (L652) + `_normalizeToArray` / `_extractTagSection` / `_isMetadataWrapFormat` | ContentParser |
| `_isValidBook` (L781) + `_validateBookStructure` / `_validateRequiredFields` / `_validateFieldTypes` | ContentParser |

**假設**：10.1.1 / 10.1.2 / 10.1.3 完成後，所有上述方法已存在於對應 helper class 內。本 ticket 僅做「從 importer.js 刪除」+「constructor 改 DI」+「public method 改 wrapper」三件事。

**保留範圍**（importer.js 改寫後保留欄位 / 方法）：
- field: `document` / `showError` / `validator` / `reader` / `parser`
- public: `constructor` / `handleFileLoad` / `validate` / `read` / `parseContent`
- 預期改寫後檔案 < 80 行（vs 當前 ~800 行）

#### B. 測試遷移策略：8 處 jest.fn 對照表

依父 ticket Phase 2 sage 規格，新建 `tests/helpers/import-stub-helpers.js` 提供統一 stub factory（10.1.1~10.1.3 已建或本 ticket 補建）。

**8 處遷移對照表**：

| 檔案 | 行號 | 原 pattern | 注入策略 |
|------|------|----------|---------|
| import-mode-modal.test.js | L338 | `importer._readFileWithReader = jest.fn().mockResolvedValue(R)` | `createStubHelpers({ reader: { read: jest.fn().mockResolvedValue(R) } })` |
| import-mode-modal.test.js | L383 | 同上 | 同上 |
| import-mode-modal.test.js | L404 | `importer._validateFileBasics = jest.fn(() => { throw E })` | `createStubHelpers({ validator: { validate: jest.fn(() => { throw E }) } })` |
| import-mode-modal.test.js | L425 | `importer._readFileWithReader = jest.fn().mockResolvedValue(R)` | 同 L338 |
| import-mode-modal.test.js | L456 | 同上 | 同上 |
| import-mode-modal.test.js | L661 | 同上 | 同上 |
| overview-page-controller.test.js | L1178 | `importer._readFileWithReader = jest.fn().mockResolvedValue(R)` | 同 L338 |
| overview-page-controller.test.js | L1258 | 同上 | 同上 |
| overview-page-controller.test.js | L1404 | `importer._validateFileBasics = jest.fn(throw)` | 同 L404 |

**遷移虛擬碼模板**（reader 注入情境）：

```
// Before
const importer = controller.bookFileImporter // 既有實例
importer._readFileWithReader = jest.fn().mockResolvedValue(IMPORT_RESULT)

// After
const { validator, reader, parser } = createStubHelpers({
  reader: { read: jest.fn().mockResolvedValue(IMPORT_RESULT) }
})
controller.bookFileImporter = new BookFileImporter({
  document, showError, validator, reader, parser
})
```

**Controller 建構時機**：若 controller 在 test setup 內建 importer，需確認注入點——必要時改傳 importer factory 給 controller，或在 controller 建好後立即 reassign（既有 8 處皆採 reassign，保留此 pattern）。

**斷言更新**：
- `expect(importer._readFileWithReader).toHaveBeenCalled()` → `expect(reader.read).toHaveBeenCalled()`（stub 變數引用）
- `expect(importer._validateFileBasics).toHaveBeenCalledWith(file)` → `expect(validator.validate).toHaveBeenCalledWith(file)`

#### C. 反向斷言更新（overview-import-private-methods.test.js）

新增 describe 區塊 `BookFileImporter 不再暴露底線方法`：

```
GIVEN 一個 importer = new BookFileImporter({ document, showError })
THEN
  importer._validateFileBasics === undefined
  importer._validateFileSize    === undefined
  importer._readFileWithReader  === undefined
  importer._handleFileContent   === undefined
AND DI 注入點存在
  importer.validator 為物件且有 validate / detectFormat method
  importer.reader    為物件且有 read method
  importer.parser    為物件且有 parse method
```

既有「已透過 handleFileLoad 走 public」案例（L293/L345/L530/L772 等）**保留原行為**，因 public API 簽章不變。

#### D. import-stub-helpers.js 引用模式

**位置決策**：`tests/helpers/import-stub-helpers.js`（依父 ticket Phase 2 規格）。

**引用方式**（測試檔頂部）：

```
const {
  createStubValidator,
  createStubReader,
  createStubParser,
  createStubHelpers
} = require('../../helpers/import-stub-helpers')
```

**前置假設**：
- 若 10.1.3 已建立此檔（供 file-reader.test.js 用），10.1.4 直接引用既有檔，僅補 createStubValidator / createStubParser / createStubHelpers 三個 export（若缺）
- 若 10.1.3 未建：10.1.4 新建完整檔（Phase 3b 第一步）

**factory 行為**（依 sage Phase 2 規格，原樣搬入）：
- `createStubValidator(overrides)` → `{ validate: jest.fn(), detectFormat: jest.fn(() => 'json'), ...overrides }`
- `createStubReader(overrides)` → `{ read: jest.fn().mockResolvedValue({ books: [], tagCategories: [], tags: [] }), ...overrides }`
- `createStubParser(overrides)` → `{ parse: jest.fn().mockReturnValue({ books: [], tagCategories: [], tags: [] }), ...overrides }`
- `createStubHelpers(overrides)` → 組合上述三者

#### E. Commit 階段化策略

**Stage A：Constructor DI 注入點**（單一 commit）
- 範圍：`src/overview/book-file-importer.js` 僅改 constructor，加 3 helper field
- public method 與底線方法**暫不動**（既有行為完全保留）
- 驗證：既有 `book-file-importer.test.js` / `book-file-importer-import-result.test.js` / `book-file-importer-version-detection.test.js` 三檔全綠（無回退）
- 預期 commit message：`refactor(0.19.0-W1-048.10.1.4): BookFileImporter constructor 加入 helper DI 注入點`

**Stage B：Public method 改 thin wrapper + 移除底線方法**（單一 commit）
- 範圍：`src/overview/book-file-importer.js`
  - 4 個 public method 內部改 delegate（A.2 對照表）
  - 移除 4 底線方法 + ~17 子 helper（A.3 對照表）
  - import 路徑加入 3 helper class
- **不**動測試檔（既有 8 處 jest mock 此時暫時失效——下個 stage 修）
- 驗證：本 stage 期間 8 處測試會紅燈（預期）；既有 importer 三檔測試走 public API，**仍應全綠**（thin wrapper + helper class delegation 行為等價）
- 若 importer 三檔測試紅燈 → STOP，回退 Stage B，root cause 為 helper class 行為與既有底線方法不等價（屬 10.1.1~10.1.3 缺陷）

**Stage C：測試遷移（8 處 mock 改寫 + 反向斷言更新 + DI/wrapper 新測試）**（單一 commit）
- 範圍：
  - 新建或補完 `tests/helpers/import-stub-helpers.js`
  - 改寫 `tests/unit/overview/import-mode-modal.test.js`（6 處）
  - 改寫 `tests/unit/overview/overview-page-controller.test.js`（3 處）
  - 更新 `tests/unit/overview/overview-import-private-methods.test.js` 反向斷言（新增 describe + 保留既有）
  - 依 Phase 2 sage 規格新增 DI constructor 測試（~5 case）+ thin wrapper 測試（~6 case）
- 驗證：`npm test` 全綠（最終驗收門檻，acceptance #6）
- 預期 commit message：`test(0.19.0-W1-048.10.1.4): 遷移 8 處 jest mock 為 stub helper 注入 + 新增 DI/wrapper 測試`

**Stage 順序理由**：A → B → C 為線性依賴。允許單 stage 失敗時局部回退，不影響其他 stage（B 失敗不污染 A）。

#### F. Migration 風險點與回退條件

| 風險點 | 偵測訊號 | 回退條件 |
|-------|---------|---------|
| R1：helper class 行為與原底線方法不等價 | Stage B 後既有 importer 三檔測試紅燈 | 回退 Stage B；建立 10.1.1/.2/.3 補丁 ticket 修正行為差異 |
| R2：reader 預設 parser 注入鏈斷裂 | Stage A 後 production 路徑（E2E / integration test）解析結果異常 | 回退 Stage A；檢查 constructor 順序（parser → validator → reader） |
| R3：showError 副作用呼叫時機改變 | 測試斷言 `showError` 被呼叫次數不符（既有測試） | 確認 helper class 在 throw 前先呼叫 showError，與原 importer 行為等價 |
| R4：FileReader DOM 全域命名衝突 | jsdom 環境下 `global.FileReader` 被覆蓋 | helper class 命名為 `FileContentReader`（父 ticket §A.2 已決策） |
| R5：8 處測試遷移後行為改變 | Stage C 後某 case 紅燈但業務語意應通過 | 對照 sage Phase 2 規格逐 case 比對 stub return value 與原 jest.fn return value，補對齊 |
| R6：parser.parse 內 TypeError 檢查位置改變 | parseContent wrapper 簡化後 fileFormat 缺漏的 case 改 throw 路徑 | 確認 10.1.2 ContentParser.parse 內含 TypeError 檢查（與原 L127-132 等價） |

**全局回退條件**：若 Stage B 後 `npm test` 失敗超過 20 案（評估標準：原 importer 行為改變幅度過大），整體回退本 ticket，重新評估 10.1.1~10.1.3 helper class 行為一致性（升級至 PM 並考慮重派 10.1.2 修補）。

**通過門檻**：Stage C 結束時 `npm test` 全綠且 `tests/unit/overview/overview-import-private-methods.test.js` 反向斷言通過（acceptance #2 + #5）。

#### G. 技術債務識別（Phase 4 預留）

- TD1：`handleFileLoad` 內 validate + read 序列若需 transactional rollback（例：read 失敗後復原 validate side-effect），當前 thin wrapper 無此能力；目前無需求，記錄供未來評估
- TD2：parser 預設情況下 reader 與 parser 共用同實例，若未來 reader 需切換 parser（例多檔批次匯入混合 format），需擴充 reader API；當前單檔場景無需求

#### H. 對 Phase 3b 代理人（thyme-extension-engineer）交接

- 執行順序：嚴格 A → B → C 三 stage commit，禁止合併
- 每 stage commit 前執行 `npm test`（Stage A/B 預期部分紅燈以 R1/R5 對照）
- 若任一 stage 觸發 F 表回退條件，立即 STOP 並回報 PM，禁止自行修補底線方法
- import-stub-helpers.js 若 10.1.3 已建：採增量補 export 模式；若未建：新建完整檔
- 完成後依規則填寫 Test Results / Completion Info，並執行 `ticket track check-acceptance --all` + `complete`

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext
## 範圍盲點（Phase 3b STOP，回報 PM）

**STOP 觸發時間**：2026-05-24，Stage A 開始前 pre-flight 檢查

**回退條件命中**：「helper class 行為不等價或核心測試破壞」（依本 ticket §H、Phase 3a §F1）

### 缺失項

ticket scope（where.files + 禁止跨範圍編輯）未覆蓋 **3 個既有 BookFileImporter 測試檔**，但 Phase 3a §E Stage B 驗證條件要求這 3 檔在移除底線方法後**仍須全綠**。

**3 個受影響的測試檔（NOT in ticket scope）**：

| 檔案 | 直呼次數 | 呼叫的底線方法 |
|------|---------|--------------|
| tests/unit/overview/book-file-importer.test.js | 4 | `_isCSVFile` |
| tests/unit/overview/book-file-importer-import-result.test.js | 17 | `_extractBooksFromData`、`_processBookData` |
| tests/unit/overview/book-file-importer-version-detection.test.js | 30 | `_extractBooksFromData`、`_filterValidBooks`、`_validateRequiredFields` |
| **合計** | **51** | **5 種底線方法** |

### 觸發位置

- 規格層：本 ticket Solution §A.3 「移除 4 個底線方法 + 17 子 helper」+ 「預期改寫後檔案 < 80 行」
- 規格層：Phase 3a §E Stage B 「既有 importer 三檔測試走 public API，仍應全綠」
- 程式碼層：5 種底線方法（`_isCSVFile` / `_extractBooksFromData` / `_processBookData` / `_filterValidBooks` / `_validateRequiredFields`）為 §A.3 表中「搬遷至 helper class」對象，移除後 importer 實例上不存在 → 3 檔 51 處直呼 throw `TypeError: importer._xxx is not a function`

### 規格內部矛盾

Stage B 規格描述（§E）說明：「既有 importer 三檔測試**走 public API**，仍應全綠」——但實際 grep 確認，3 檔中 51 處**直呼底線方法**，並非全部走 public API。Phase 3a 規格與 Phase 2 sage 規格皆假設「3 檔已遷移至 public API」，但實況非如此。

可能根因（推測，待 PM 確認）：
- Phase 2 sage 與 Phase 3a pepper 未 grep 既有 importer 測試檔的底線方法直呼
- W1-048.1 Stage C 雖將 import-mode-modal / overview-page-controller 兩檔遷至 public，但 3 個 importer 自家測試檔保留底線方法直呼（合理：importer 自家測試覆蓋實作細節，並非反模式）

### 影響

無法完成的 acceptance：
- AC#2 「移除 4 個底線私有方法」+ §A.3 「~21 helper 搬遷」→ 若完整執行，3 檔 51 處紅燈
- AC#6 「完整測試套件（npm test）全綠」→ 同上紅燈

無法獨立決策的選項（需 PM 拍板）：

| 方案 | 描述 | 影響 |
|------|------|------|
| 方案 A | 擴大本 ticket scope 至 3 個 importer 測試檔（51 處遷移） | scope 大幅膨脹；違反 PC-040 + cognitive-load 認知負擔閾值；需 PM 批准範圍擴張 |
| 方案 B | importer 保留 5 個底線方法作 thin wrapper（delegate 至 helper class） | 違反 §A.3「移除 ~21 helper」+「< 80 行」目標；但保持向後相容；本 ticket 範圍最小 |
| 方案 C | 移除底線方法、3 檔 51 處紅燈先放著、建 follow-up sub-IMP（10.1.5）專門遷移 importer 自家測試 | 違反 quality-baseline 規則 1（測試通過率 100%）；需 follow-up ticket 連帶降版 acceptance #6 |
| 方案 D | 回退本 ticket，重新評估 10.1.1~10.1.3 helper class 是否該保留 backward-compat 入口 | 違反「禁止修改 helper class」；但若 PM 認可可考慮 |

### 建議補料

PM 決策需要的 context：

1. 是否擴大 ticket scope 至 3 個 importer 測試檔（方案 A）？若是，建議由本 agent 繼續執行（cognitive-load 評估：51 處遷移 + 反向 grep 確認 + Stage C 整合，估算超過單 ticket 容量，可能需要 sub-split）
2. 是否接受 importer 保留 5 個底線 thin wrapper（方案 B）？若是，§A.3 表的「~21 helper 搬遷」需修訂為「保留 5 個 thin wrapper + 16 個移除」
3. 是否將完整移除拆為 follow-up ticket（方案 C）？若是，本 ticket AC#2 / AC#6 需鬆綁
4. 是否回退到 10.1.1~10.1.3 設計階段（方案 D）？

### 重派成本估算

- 方案 A：本 agent 繼續執行，需擴增 prompt 範圍授權；估算 +30% token（Stage C 雙倍工作量）
- 方案 B：本 agent 可立即執行，token 估算與原規劃相近
- 方案 C：本 agent 可立即執行原規劃（不含 3 檔），AC 鬆綁後可獨立 complete；follow-up ticket 由 PM 另派
- 方案 D：本 ticket retract，需 PM 重派 Phase 1 重新設計

### Stage A/B/C 執行狀態

| Stage | 狀態 | 備註 |
|-------|------|------|
| Stage A（constructor DI 注入點） | **未開始** | pre-flight 偵測到範圍盲點即 STOP |
| Stage B（移除底線方法 + thin wrapper） | 未開始 | 待 PM 決策方案 |
| Stage C（測試遷移） | 未開始 | 待 PM 決策方案 |

無任何 commit，working tree clean，可立即回退。

## Exit Status
```yaml
status: needs_context
reason: "ticket scope (where.files) 未覆蓋 3 個既有 BookFileImporter 測試檔，但 Phase 3a Stage B 驗證條件要求這 3 檔全綠；grep 確認 3 檔含 51 處直呼底線方法（_extractBooksFromData / _isCSVFile / _processBookData / _filterValidBooks / _validateRequiredFields），移除後必定 TypeError。需 PM 在方案 A/B/C/D 中拍板。"
confidence: 0.95
acceptance_met: []
acceptance_unmet: [0, 1, 2, 3, 4, 5]
artifacts: []
context_dependencies:
  - "docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W1-048.10.1.4.md (本 ticket Phase 2/3a 規格)"
  - "docs/work-logs/v0/v0.19/v0.19.0/tickets/0.19.0-W1-048.10.1.md (父 ticket Phase 1 §B/§C)"
  - "src/overview/book-file-importer.js (待改寫)"
  - "src/overview/import/file-validator.js / file-reader.js / content-parser.js (helper 已存在)"
  - "tests/unit/overview/book-file-importer.test.js (4 處直呼，NOT in scope)"
  - "tests/unit/overview/book-file-importer-import-result.test.js (17 處直呼，NOT in scope)"
  - "tests/unit/overview/book-file-importer-version-detection.test.js (30 處直呼，NOT in scope)"
estimated_recovery_effort: |
  方案 A (擴 scope): +30% token, 51 處遷移 + 反向 grep
  方案 B (thin wrapper 保留): 原規劃 token，違反 < 80 行目標
  方案 C (follow-up ticket): 原規劃 token，AC 鬆綁
  方案 D (回退設計): retract，重派 Phase 1
```

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending