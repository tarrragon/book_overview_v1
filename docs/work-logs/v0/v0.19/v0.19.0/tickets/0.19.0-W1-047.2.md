---
id: 0.19.0-W1-047.2
title: 接線實作 BookFileImporter v2 結構提取與回傳介面變更
type: IMP
status: in_progress
version: 0.19.0
wave: 1
priority: P2
parent_id: 0.19.0-W1-047
children: []
blockedBy:
- 0.19.0-W1-047.1
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 接線實作 BookFileImporter v2 結構提取與回傳介面變更
when: IMP-A 版本偵測接線完成後
where:
  layer: 待定義
  files:
  - src/overview/book-file-importer.js
  - src/overview/overview-page-controller.js
why: W1-001.2.1 實機驗證證實 UC-04 匯入功能未依 SPEC-EXPORT-V2 實作（BookFileImporter 無版本偵測/v1 轉換/tag
  處理/合併覆蓋模式/storage 持久化；SPEC 模組 format-version-detector 與 v1-to-v2-converter 存在但零非測試消費者）。UC-04
  匯入為 v1.0.0 內測「匯出入正常」硬需求，匯出端 UC-03 已驗證 PASS 但匯入端缺失，功能不對稱
how:
  task_type: Implementation
  strategy: BookFileImporter v2 路徑提取頂層 tagCategories/tags 區段，回傳結構由純 books 陣列改為 {books,
    tagCategories, tags}；overview-page-controller handleFileLoad 適配新回傳結構。詳見 W1-047
    Solution R3
acceptance:
- '[ ] BookFileImporter v2 路徑提取頂層 tagCategories 與 tags 區段'
- '[ ] 回傳結構變更為 {books, tagCategories, tags} 物件'
- '[ ] overview-page-controller handleFileLoad 適配新回傳結構'
- '[ ] 對應單元測試 100% 通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-23T04:28:08'
completed_at: null
created: '2026-05-23'
updated: '2026-05-23'
current_phase: Phase 3a
assignee: pepper-test-implementer
---

# Execution Log

## Task Summary

接線實作 BookFileImporter v2 結構提取與回傳介面變更

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

IMP-A（W1-047.1）已讓 `BookFileImporter` 接入版本偵測與 v1→v2 轉換，但回傳值仍為純 `books` 陣列。`_extractBooksFromData` 在 v1 路徑只取 `convertV1ToV2Data(data).books`、在 v2 路徑完全忽略頂層 `tagCategories`/`tags`（程式碼註解明示「tagCategories/tags 暫時丟棄，回傳介面變更屬 IMP-B」）。tag 資料在匯入第一站即遺失，後續 IMP-C 的 storage 持久化無資料可寫。

### 影響範圍

| 檔案 | 影響 |
|------|------|
| `src/overview/book-file-importer.js` | `_extractBooksFromData` 新增 tag 區段提取；`_processBookData` / `_handleFileContent` / `_readFileWithReader` / `handleFileLoad` 回傳結構由陣列改為 `{ books, tagCategories, tags }` |
| `src/overview/overview-page-controller.js` | `handleFileLoad` 與 `_handleFileContent` 代理方法解構 `books` 後傳給 `_updateUIWithBooks`；`_updateBooksData` 不變 |

### 相關 Error Pattern

無直接對應的 PC error pattern。此為介面契約擴充（IMP-A 已預留銜接點），非流程或實作錯誤。

### Phase 1 功能規格

完整功能規格見 `0.19.0-W1-047.2-feature-spec.md`（Full 模式，6 區段 + 10 個 GWT 場景 + 4 條細化 AC）。spec validate 迭代 2 次通過，無未回答問題。

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

### Phase 1 設計摘要（lavender-interface-designer）

**核心介面變更**：定義 `ImportResult = { books: Book[], tagCategories: TagCategory[], tags: Tag[] }`，三欄位型別恆為陣列。

**回傳鏈變更**：`_extractBooksFromData` → `_processBookData` → `_handleFileContent` → `_readFileWithReader` → `handleFileLoad` 五處回傳由 `Book[]` 改為 `ImportResult`。

**各路徑 tag 來源**：
- v1 JSON：取 `convertV1ToV2Data(data)` 的 `.tagCategories`/`.tags`（converter 回傳已含三區段，IMP-A 僅取 `.books`，本 ticket 補取其餘兩段）。
- v2 JSON：從根物件讀 `data.tagCategories`/`data.tags`，缺失或非陣列降級為 `[]`（SPEC §2.3）。
- CSV / 空物件：`tagCategories: []`、`tags: []`。

**controller 適配**：`handleFileLoad` 與 `_handleFileContent` 解構 `books` 傳給 `_updateUIWithBooks`；`tagCategories`/`tags` 本 ticket 暫不流入 UI 層（IMP-C 接手 storage 持久化）。`_updateBooksData` 簽名與非匯入呼叫端不變。

**範圍邊界**：僅 v2 結構提取 + 回傳介面變更；覆蓋/合併模式、storage 持久化、tag id 重映射、模式選擇 UI 屬 IMP-C~E。

**錯誤處理原則**：tag 區段「寬鬆降級」（缺失/型別錯 → `[]`，不拋錯）；books 提取「維持既有嚴格契約」（無法辨識結構仍拋 `VALIDATION_ERROR`）。本 ticket 不新增拋錯路徑。

詳見 `0.19.0-W1-047.2-feature-spec.md`。Phase 2（sage 測試設計）依該規格 10 個場景與 AC-1~AC-4 設計 RED 測試。

---

### Phase 2 測試案例設計（sage-test-architect）

#### 測試策略

| 項目 | 決策 |
|------|------|
| 測試分層 | Sociable Unit Test。被測單元 = `BookFileImporter` 模組 + `overview-page-controller` 匯入路徑 |
| Mock 邊界 | 不 mock `format-version-detector` / `v1-to-v2-converter`（內層真實依賴，沿用 IMP-A 策略）。controller 測試以真實 `BookFileImporter` 注入，僅 mock DOM / showError / FileReader |
| 入口點 | importer 測試直接呼叫 `_extractBooksFromData` / `_processBookData` / `_handleFileContent`（避免 jsdom FileReader 非同步差異，沿用 IMP-A 入口策略）。controller 測試呼叫 `handleFileLoad`（以 stub `_readFileWithReader` 回傳 `ImportResult`）與 `_handleFileContent` |
| 斷言原則 | 三欄位（books/tagCategories/tags）一律以 `Array.isArray()` 驗型別恆為陣列；內容用 `toEqual`；參考保持驗證用 `toBe`。無計時斷言；無 `toBeCloseTo`（無浮點），符合 test-assertion-design 規則 |
| 測試檔位置 | 新增 `tests/unit/overview/book-file-importer-import-result.test.js`（v2 結構提取 + ImportResult 介面）；controller 適配併入 `tests/unit/overview/overview-page-controller.test.js`（既有檔） |
| fixture | 重用 `tests/e2e/fixtures/uc04-import/sample-v1.json`（含 category）、`sample-v2-base.json`（含 tagCategories/tags）。型別降級與邊界情境用 inline 物件構造 |

#### 行為鏈推演前置驗證原則

每個測試在 When 之前先以斷言驗證前置條件（測試資料結構就緒），再觸發行為，再驗證 ImportResult 三欄位。型別降級類場景（場景 3）前置須先確認輸入欄位「確為非陣列」，避免測資自身錯誤偽裝成 pass。

#### 測試清單

新增測試檔 `book-file-importer-import-result.test.js`，11 個 TC，5 組：

**Group A：v2 路徑 tag 區段提取（AC-1、場景 1-3）**

| TC | 場景 | Given（前置驗證） | When | Then（預期斷言） |
|----|------|------------------|------|-----------------|
| TC-01 | 場景 1：v2 含非空 tag 區段完整提取 | `data` 含 `metadata.formatVersion:'2.0.0'`、非空 `tagCategories`/`tags`/`books`；前置斷言三者皆 `Array.isArray` 且 length>0 | `_extractBooksFromData(data,'json')` | 回傳物件含 `books`/`tagCategories`/`tags` 三鍵；`result.tagCategories` `toEqual` `data.tagCategories`；`result.tags` `toEqual` `data.tags`；`result.books` 為書籍陣列 |
| TC-02 | 場景 2：v2 缺 tag 欄位降級 | `data` 為 v2（`metadata`+`readingStatus` 推斷）但無 `tagCategories`/`tags` 鍵；前置斷言 `data.tagCategories === undefined` | `_extractBooksFromData(data,'json')` | `Array.isArray(result.tagCategories)` 為 true 且 `toEqual([])`；`result.tags` 同；不拋錯；`result.books` 正常提取 |
| TC-03 | 場景 3：v2 tag 區段非陣列降級（回歸防護重點） | `data.tagCategories` 為物件 `{}`、`data.tags` 為字串 `'invalid'`；前置斷言 `Array.isArray(data.tagCategories)===false` 且 `Array.isArray(data.tags)===false` | `_extractBooksFromData(data,'json')` | `result.tagCategories` `toEqual([])`；`result.tags` `toEqual([])`；`expect(()=>...).not.toThrow()`；`result.books` 正常 |

**Group B：v1 路徑 tag 結構回傳（AC-2、場景 4-5）**

| TC | 場景 | Given（前置驗證） | When | Then（預期斷言） |
|----|------|------------------|------|-----------------|
| TC-04 | 場景 4：v1 含 category 回傳轉換 tag 結構 | v1 純陣列，書籍含 `category:'V1匯入分類'`；前置斷言 `detectFormatVersion(data)==='v1'` | `_extractBooksFromData(data,'json')` | `result.books` 為轉換後書籍；`Array.isArray(result.tagCategories)` true 且 length>0；`Array.isArray(result.tags)` true 且 length>0；tag 結構等於 `convertV1ToV2Data(data).tagCategories`/`.tags`（用 `toEqual`） |
| TC-05 | 場景 5：v1 無 category 回傳空 tag 結構 | v1 純陣列，書籍皆無 `category`（或空字串/「未分類」）；前置斷言 `detectFormatVersion(data)==='v1'` | `_extractBooksFromData(data,'json')` | `result.books` 正常轉換；`result.tagCategories` `toEqual([])`；`result.tags` `toEqual([])` |

**Group C：CSV 與空物件路徑（AC-2、場景 6-7）**

| TC | 場景 | Given（前置驗證） | When | Then（預期斷言） |
|----|------|------------------|------|-----------------|
| TC-06 | 場景 6：CSV 匯入 tag 區段為空 | 合法 CSV 文字（含 id/title header） | `_handleFileContent(csvText,'csv')` | 回傳 `ImportResult`；`result.books` 為 CSV 解析結果且 length>0；`result.tagCategories` `toEqual([])`；`result.tags` `toEqual([])` |
| TC-07 | 場景 7：空物件 JSON 三區段皆空 | `data = {}`；前置斷言 `Object.keys(data).length===0` | `_extractBooksFromData(data,'json')` | 回傳 `{books:[],tagCategories:[],tags:[]}`（用 `toEqual`）；`expect(()=>...).not.toThrow()` |

**Group D：錯誤契約零回歸（AC-2、場景 8）**

| TC | 場景 | Given（前置驗證） | When | Then（預期斷言） |
|----|------|------------------|------|-----------------|
| TC-08 | 場景 8：無法辨識結構維持 VALIDATION_ERROR（回歸防護重點） | `data = {foo:'bar',count:3}`；前置斷言非陣列、無 `books`/`data` 鍵、非空物件 | `_extractBooksFromData(data,'json')` | `expect(()=>...).toThrow(objectContaining({message:'JSON 檔案應該包含一個陣列或包含books屬性的物件', code:ErrorCodes.VALIDATION_ERROR, details:{category:'validation'}}))` — 訊息/code/details 與 IMP-A TC-14 完全一致 |
| TC-09 | 場景 8 延伸：JSON 解析失敗維持 PARSE_ERROR | 非法 JSON 字串 | `_handleFileContent(invalidJson,'json')` | `toThrow` 含 `code:ErrorCodes.PARSE_ERROR`（既有契約不變） |

**Group E：ImportResult 型別恆定（AC-2、場景 1-7 橫切）**

| TC | 場景 | Given（前置驗證） | When | Then（預期斷言） |
|----|------|------------------|------|-----------------|
| TC-10 | 全路徑型別降級驗證 | 參數化：v1/v2-含 tag/v2-缺 tag/v2-非陣列/CSV/空物件 六組輸入 | 對應 `_extractBooksFromData` / `_handleFileContent` | 每組回傳值的 `books`/`tagCategories`/`tags` 三鍵皆 `Array.isArray()===true`，皆非 `undefined`/`null`（消費端可無條件解構） |
| TC-11 | `_processBookData` 回傳 ImportResult | v2 含 tag 區段 data | `_processBookData(data,'json')` | 回傳物件含三鍵；`result.books` 為「過濾後」書籍（驗證 `_filterValidBooks` 仍只作用 books）；`tagCategories`/`tags` 透傳未經過濾 |

**Group F：controller 適配（AC-3、場景 9-10），併入 `overview-page-controller.test.js`**

| TC | 場景 | Given（前置驗證） | When | Then（預期斷言） |
|----|------|------------------|------|-----------------|
| TC-12 | 場景 9：`handleFileLoad` 解構 books | stub `bookFileImporter._readFileWithReader` 回傳 `{books:[...3本], tagCategories:[...], tags:[...]}`；spy `_updateUIWithBooks` | `await controller.handleFileLoad(file)` | `_updateUIWithBooks` 被呼叫且引數為**陣列**（非物件）、length===3；`controller.currentBooks` length===3；無 `undefined` 流入 UI |
| TC-13 | 場景 9：`_handleFileContent` 代理解構 books | stub `bookFileImporter._handleFileContent` 回傳 `ImportResult`；spy `_updateUIWithBooks` | `controller._handleFileContent(content)` | `_updateUIWithBooks` 引數為 `ImportResult.books` 陣列 |
| TC-14 | 場景 10：非匯入呼叫端零回歸 | 構造 `STORAGE.LOAD.COMPLETED` / `EXTRACTION.COMPLETED` / `UI.BOOKS.UPDATE` 事件，`eventData.books` 為純陣列 | 觸發對應事件處理路徑 | `_updateBooksData` 收到純陣列；`currentBooks`/`filteredBooks` 正確；行為與變更前一致（`_updateBooksData` 簽名不變） |

#### 既有測試更新清單（AC-4：斷言「回傳陣列」→ 斷言 `ImportResult.books`）

回傳介面由 `Book[]` 改為 `ImportResult` 後，既有直接斷言回傳值為陣列的測試會 RED，必須同步更新：

| 檔案 | 位置 | 現況斷言 | 更新後斷言 |
|------|------|---------|-----------|
| `book-file-importer.test.js` | `parseCSV` helper（L31-33） | `return importer._handleFileContent(csvText,'csv')`（直接回陣列） | helper 改回 `_handleFileContent(...).books`，或各 TC 解構 `.books` |
| `book-file-importer.test.js` | CSV happy path 全部 TC（L70-132） | `books = parseCSV(...)`；`books.toHaveLength` | helper 已解構 `.books` 則 TC 不需改；否則逐處 `parseCSV(...).books` |
| `book-file-importer.test.js` | 失效情境 TC（L196-221） | `parseCSV` 預期 throw 的不受影響；L210 過濾測試 `books.toHaveLength(1)` | 同 helper 處理 |
| `book-file-importer-version-detection.test.js` | Group A TC-01~05（L32-124） | `result = _extractBooksFromData(data)`；`Array.isArray(result)`、`result.toBe(data.books)`、`result.toBe(data.data)` | `_extractBooksFromData` 回傳改為 `{books,tagCategories,tags}`：斷言改 `result.books`；參考比較改 `result.books toBe data.books`、`result.books toBe data.data` |
| `book-file-importer-version-detection.test.js` | Group B TC-06~08（L128-177） | `result.map(b=>...)`、`result[0].progress` | 改 `result.books.map(...)`、`result.books[0].progress` |
| `book-file-importer-version-detection.test.js` | Group C TC-12（L202-212） | `_extractBooksFromData` 結果傳 `_filterValidBooks` | `_filterValidBooks(result.books)` |
| `book-file-importer-version-detection.test.js` | Group D TC-13（L216-222） | `_extractBooksFromData(data).toEqual([])` | `_extractBooksFromData(data).toEqual({books:[],tagCategories:[],tags:[]})`；或 `.books toEqual []` |
| `book-file-importer-version-detection.test.js` | Group E TC-15~17（L240-271） | `result.toEqual([])`、`result.toHaveLength`、`result[0].tagIds` | 改 `result.books.*` |
| `overview-page-controller.test.js` | 任何測試呼叫 `handleFileLoad` / `_handleFileContent` 並驗證書籍數量處 | 若 stub `_readFileWithReader` 回陣列 | stub 改回 `ImportResult` 物件；斷言不變（controller 內部解構後 UI 仍收陣列） |

> 注意：TC-08（version-detection TC-14 對應的錯誤契約）不需改 — 拋錯路徑不回傳值，訊息/code/details 不變。

#### 回歸防護重點對照

| 防護重點 | 對應 TC | 失敗代表 |
|---------|---------|---------|
| ImportResult 型別降級（三欄位恆陣列） | TC-02、TC-03、TC-10 | 消費端解構 `undefined` → IMP-C storage 寫入崩潰 |
| 錯誤契約零回歸 | TC-08、TC-09 | 既有 UC-04 錯誤提示行為改變 |
| 非匯入呼叫端零回歸 | TC-14 | `_updateBooksData` 被誤改影響 STORAGE/EXTRACTION 路徑 |
| books 過濾邊界不變 | TC-11 | `_filterValidBooks` 誤作用於 tag 區段 |

#### Phase 2 Handoff to pepper-test-implementer（Phase 3a）

- [x] 測試案例依 10 個 GWT 場景設計（14 新 TC + 既有測試更新清單）
- [x] 測試策略決策完成（Sociable Unit Test、Mock 邊界、入口點）
- [x] 行為鏈前置驗證已納入（每 TC 的 Given 含前置斷言）
- [x] 四維度分支：正常（TC-01/04/06/12）、異常（TC-08/09）、邊界（TC-02/03/05/07/10）、零回歸（TC-11/13/14）
- [x] Mock 策略符合判斷標準（內層 converter/detector 不 mock，外部 DOM/FileReader mock）
- [x] 既有測試更新清單完整（AC-4 回傳陣列 → ImportResult.books）
- [x] 符合 test-assertion-design 規則（無計時硬門檻、無 toBeCloseTo）
- [x] GWT scenario group 功能職責歸屬：Group A=v2 提取、B=v1 轉換、C=CSV/空物件、D=錯誤契約、E=型別恆定、F=controller 適配；各組可獨立執行，無跨組 mutable 狀態依賴

<!-- Phase 3b 實作由 thyme-extension-engineer 接續 -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending