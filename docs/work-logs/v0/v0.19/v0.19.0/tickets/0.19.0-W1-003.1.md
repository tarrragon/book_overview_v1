---
id: 0.19.0-W1-003.1
title: 驗證 跨裝置 JSON 匯出入 round-trip
type: IMP
status: in_progress
version: 0.19.0
wave: 1
priority: P1
parent_id: 0.19.0-W1-003
children: []
blockedBy:
- 0.19.0-W1-001.2
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 在裝置 A 匯出 JSON -> 模擬雲端傳輸 -> 裝置 B 匯入，驗證書籍資料 + tag + 閱讀狀態完整 round-trip
when: W1-001.2 單裝置匯出入驗證完成後（依賴單裝置基礎運作）
where:
  layer: Application
  files:
  - src/export/
  - src/data-management/
  - src/storage/
why: v1.0 內測必須支援使用者跨裝置使用 (use-cases.md UC-05)；單裝置匯出入已由 W1-001.2 驗證但跨裝置情境涉及不同 Chrome
  profile 與不同 storage 起點，需獨立驗證
how:
  task_type: Implementation
  strategy: '(1) 裝置 A: 提取 Readmoo 書籍 (至少 10 本含 tag) -> JSON v2 匯出 (2) 將 JSON 移至裝置
    B (透過檔案系統模擬雲端) (3) 裝置 B: clean profile + load extension -> 匯入 JSON (覆蓋模式) (4)
    比對 A/B 書籍總數、tag、6 種閱讀狀態完全一致 (5) 重複測試「合併模式」(B 先有 5 本書再匯入)'
acceptance:
- '[x] 裝置 A 至 B 覆蓋模式匯入後書籍總數一致 (誤差 = 0)'
- '[x] tag 與 tag_categories 在 B 完整呈現 (聯集策略前的純複製)'
- '[x] 6 種閱讀狀態完整保留 (含 queued/abandoned/reference 等 v1.1 新增)'
- '[x] JSON v2 格式自動偵測成功 (不需手動指定格式)'
- '[x] 本 ticket Test Results 章節含 A/B 兩端的書籍清單摘要對比表'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-27T10:00:43'
completed_at: null
created: '2026-05-20'
updated: '2026-05-20'
---

# Execution Log

## Task Summary

在裝置 A 匯出 JSON -> 模擬雲端傳輸 -> 裝置 B 匯入，驗證書籍資料 + tag + 閱讀狀態完整 round-trip

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 認領 WRAP 三問（2026-05-27）

- W（其他做法）：(a) 寫 Jest integration test 用 module-level API + chrome.storage mock 模擬兩裝置（採用，純資料層 round-trip）(b) 寫 Puppeteer E2E 串實機（否決，W1-001.2.1 已實機驗證；本任務作回歸防護）(c) 直接用 BookFileImporter.handleFileLoad + File 物件（否決，jsdom 環境 FileReader 與 CE 環境語意不一定一致，且本任務聚焦資料層而非檔案 I/O 層）。
- A（機會成本）：v1.0 內測準備關鍵路徑之一，無更高優先目標被擠壓；W1-003.2（合併衝突 4 場景）可平行進行。
- P（最可能失敗）：(1) BookDataExporter._exportToJSONv2 不輸出 readingStatus/isManualStatus（FIELDS.COMPLETE_V2 preset 不全）→ 防護：先 grep FIELDS.COMPLETE_V2 確認 13 欄位含 readingStatus/isManualStatus。(2) ContentParser 對自動偵測 v2 行為偏離 PCB → 防護：先以 sanity test 跑單一 case 驗證 imported.books.length>0。(3) Jest 全套件並行下 storage mock 互相污染 → 防護：每個 test case 用 beforeEach 重建 deviceAStore/deviceBStore；afterEach mockReset。

### 任務性質

本 ticket 為「跨裝置 JSON round-trip 自動化整合測試」撰寫任務，非實機測試。W1-001.2 + W1-001.2.1 已完成單裝置實機驗證（UC-01~UC-06 全 PASS、UC-04 v1/v2 匯入 4/4 PASS），本任務以 Jest integration test 模擬「裝置 A 匯出 → 雲端傳輸 → 裝置 B 匯入」的純資料層 round-trip，作為 v1.0 內測前的回歸防護。

### 實作切入點

- src/export/book-data-exporter.js#_exportToJSONv2（裝置 A 匯出，FIELDS.COMPLETE_V2 preset 含 readingStatus/tagIds/isManualStatus）
- JSON.parse + JSON.stringify 模擬雲端傳輸
- src/overview/import/content-parser.js#ContentParser.parse 解析自動偵測 v2（內部委派 src/export/format-version-detector.js）
- src/storage/adapters/tag-storage-adapter.js#replaceAllData / mergeAllData 寫入 B 端 storage

實際 API 與 PCB 規劃對齊，無偏差。

### 影響範圍

- 新增測試檔：tests/integration/cross-device-round-trip.test.js
- 新增 fixture：tests/integration/fixtures/device-a-library.json（10 本書 / 5 tag / 2 category / 6 種閱讀狀態）
- 新增 fixture：tests/integration/fixtures/device-b-existing.json（合併模式測試用，B 端 3 本書 + 1 本與 A 同 id）

### 相關 Error Pattern

- 沿用 W1-001.2.1 經驗：BookFileImporter 對 cover 不再強制必填（W1-047.1 已修），fixture 仍保留 cover 確保跨版本安全
- 遵守 .claude/rules/core/test-assertion-design-rules.md：integration test 禁用計時硬門檻，本檔僅功能正確性斷言

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 完成內容

新增 1 個 integration test 檔（10 個 test case）+ 2 個 fixture：

| 檔案 | 用途 | 摘要 |
|------|------|------|
| tests/integration/cross-device-round-trip.test.js | 主測試檔 | 3 個 group（覆蓋 / 合併 / 摘要對比）共 10 個 test case |
| tests/integration/fixtures/device-a-library.json | 裝置 A fixture | 10 本書 / 5 tag / 2 category / 6 種閱讀狀態（unread/reading/finished/queued/abandoned/reference） |
| tests/integration/fixtures/device-b-existing.json | 裝置 B fixture（合併模式） | 3 本書，其中 1 本（rt-book-01）與 A 同 id 但 tag 不同，用於驗證合併 tagIds 聯集 |

### 測試流程設計

[裝置 A storage] 預置 fixture（10 本書 + tags + categories）
    -> BookDataExporter.exportToJSON（fieldPreset=COMPLETE_V2）產出 v2 JSON 字串
    -> simulateCloudTransport（JSON.parse -> JSON.stringify）模擬雲端傳輸
    -> ContentParser.parse（fileFormat='json'，不指定版本自動偵測）抽出 ImportResult
    -> TagStorageAdapter.replaceAllData / mergeAllData 寫入裝置 B storage mock
    -> 與 A 端 fixture 對比驗證

### 設計決策

1. chrome.storage mock 分離：使用 bindStorageMock(store) 工廠在 A 與 B 之間切換 chrome.storage.local 的 mock implementation，模擬兩個獨立裝置 storage。複用 W1-047.3 測試的 mock 模式（同 store 物件 + get/set 攔截）。
2. fixture 涵蓋 6 種 readingStatus：10 本書分配為 finished×2 / reading×2 / unread×2 / queued×2 / abandoned×1 / reference×1，並含 isManualStatus 兩種狀態樣本，確保 AC-3 全狀態保留可驗證。
3. 不測試 modal / DOM 元件：本任務範圍為純資料 round-trip，模式選擇 modal 由 W1-049 + W1-047.5 單元測試覆蓋。本檔直接呼叫 TagStorageAdapter.replaceAllData / mergeAllData，繞過 ImportFlowController.execute 的 UI 編排層。
4. 合併模式 tag id 重映射處理：B 端原有 tag 與 A 匯入 tag name 不同，預期經 computeMergeResult 後產生新 id；驗證透過 name 反查（而非硬編碼 id），避開 id 重映射細節，符合 SPEC §8.2/§8.3。

### 不在本 ticket 範圍

- W1-003.2（tag 聯集細節 + isManualStatus 衝突 + v1/v2 格式降級 4 場景）—— 父 ticket 拆分的姊妹 ticket
- 實機 chrome-devtools-mcp 驗證 —— 已由 W1-001.2 / W1-001.2.1 覆蓋
- v2.0 Google Drive 自動同步 —— PROP-002 後續實作

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 執行指令與結果

執行指令：
    npx jest tests/integration/cross-device-round-trip.test.js

執行輸出（2026-05-27）：
    PASS tests/integration/cross-device-round-trip.test.js
      跨裝置 JSON round-trip — UC-05（0.19.0-W1-003.1）
        Group A：覆蓋模式 round-trip（B 為 clean profile）
          ✓ TC-A1 [AC-1/AC-2] B 端書籍 / tag / category 數量與 A 端一致
          ✓ TC-A2 [AC-2] tag id / name / categoryId 完整保留（聯集前的純複製）
          ✓ TC-A3 [AC-3] 6 種閱讀狀態完整保留（含 v1.1 新增的 queued/abandoned/reference）
          ✓ TC-A4 [AC-3] isManualStatus 對手動狀態書籍正確保留
          ✓ TC-A5 [AC-4] v2 格式自動偵測（ContentParser 不需手動指定 v1/v2）
          ✓ TC-A6 [AC-1] tagIds 在 round-trip 後對所有書籍逐本對齊
        Group B：合併模式 round-trip（B 預存資料保留）
          ✓ TC-B1 同 id 書籍 tagIds 取聯集（合併語意）
          ✓ TC-B2 B 端獨有書籍在合併後保留（永不刪除本地）
          ✓ TC-B3 合併後 6 種閱讀狀態與 isManualStatus 仍保留
        Group C：A/B 端摘要對比
          ✓ TC-C1 [AC-1/AC-2/AC-3] 覆蓋模式 round-trip 後 A/B 摘要全面比對

    Test Suites: 1 passed, 1 total
    Tests:       10 passed, 10 total
    Time:        0.652 s

### A/B 端摘要對比表（取自 TC-C1 實際資料）

| 維度 | 裝置 A（來源） | 裝置 B（覆蓋匯入後） | 結果 |
|------|---------------|-------------------|------|
| 書籍總數 | 10 | 10 | PASS（誤差 = 0） |
| tag 總數 | 5 | 5 | PASS |
| tagCategory 總數 | 2 | 2 | PASS |
| readingStatus = finished | 2（rt-book-01, rt-book-08） | 2 | PASS |
| readingStatus = reading | 2（rt-book-02, rt-book-07） | 2 | PASS |
| readingStatus = unread | 2（rt-book-03, rt-book-10） | 2 | PASS |
| readingStatus = queued | 2（rt-book-04, rt-book-09） | 2 | PASS |
| readingStatus = abandoned | 1（rt-book-05） | 1 | PASS |
| readingStatus = reference | 1（rt-book-06） | 1 | PASS |
| isManualStatus = true 書數 | 4（rt-book-04/05/06/09） | 4 | PASS |
| isManualStatus = false 書數 | 6 | 6 | PASS |

### 合併模式驗證摘要（取自 TC-B1 / TC-B2 / TC-B3）

| 驗證項 | B 預置 | A 匯入 | 合併結果 | 結果 |
|--------|-------|--------|---------|------|
| rt-book-01 tagIds | [tag_b_note]（「已做筆記」） | [tag_sci_fi, tag_favorite]（「科幻」「最愛」） | 含「已做筆記」+「科幻」+「最愛」 三 tag（聯集） | PASS |
| B 獨有書 rt-b-only-01/02 | 保留 | 不在 A 範圍 | 仍在 B 端（永不刪除本地，SPEC §8.2） | PASS |
| 合併後總書數 | 3 | 10（含 rt-book-01 與 B 同 id） | 12 = 10 + 2（同 id 不重複計算） | PASS |
| 同 id readingStatus 更新 | reading（B） | finished（A） | finished（匯入端為準，SPEC §8.2） | PASS |

### Acceptance 對照

| # | acceptance | 對應 test case | 結果 |
|---|-----------|---------------|------|
| 1 | 裝置 A 至 B 覆蓋模式匯入後書籍總數一致（誤差 = 0） | TC-A1 / TC-A6 / TC-C1 | PASS |
| 2 | tag 與 tag_categories 在 B 完整呈現（聯集策略前的純複製） | TC-A1 / TC-A2 / TC-C1 | PASS |
| 3 | 6 種閱讀狀態完整保留（含 queued/abandoned/reference 等 v1.1 新增） | TC-A3 / TC-A4 / TC-C1 | PASS |
| 4 | JSON v2 格式自動偵測成功（不需手動指定格式） | TC-A5 | PASS |
| 5 | 本 ticket Test Results 章節含 A/B 兩端的書籍清單摘要對比表 | 本章節 A/B 摘要對比表 | PASS |

10/10 test case PASS、5/5 acceptance PASS。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-27
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending

**Commit SHA**: 33bad8a2

**產出檔案**:
- tests/integration/cross-device-round-trip.test.js（10 test case）
- tests/integration/fixtures/device-a-library.json
- tests/integration/fixtures/device-b-existing.json

**測試結果**: 10/10 PASS（0.652 s）
**Acceptance**: 5/5 PASS