---
id: 0.19.0-W1-081
title: 動態化 W1-074 build.js WORK_LOGS_ROOT v0 硬編碼改為自動探測（v1.0.0 wave 開版前）
type: IMP
status: in_progress
version: 0.19.0
wave: 1
priority: P3
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W1-074
dispatch_reason: ''
decision_tree_path:
  entry_point: 第五層:TDD
  final_decision: 標 pending 由 v1.0.0 wave 開版時提取
  rationale: parallel-evaluation-finding,future-compatibility,milestone-trigger
who:
  current: thyme-extension-engineer
  history: {}
what: 動態化 W1-074 build.js WORK_LOGS_ROOT v0 硬編碼改為自動探測（v1.0.0 wave 開版前）
when: v1.0.0 wave 開版時觸發（pending 由 PM 從 priority list 提取）
where:
  layer: 待定義
  files:
  - scripts/build.js
why: bay parallel-evaluation 審查 W1-074 發現 F-3 (Low)：scripts/build.js:17 WORK_LOGS_ROOT
  硬編碼 'v0'。當專案進入 v1.x（CLAUDE.md §8 milestone v1.0.0 內測版本）後，worklog 若放 docs/work-logs/v1/
  則 sanity check 永遠回傳『未找到 active worklog』。現階段無影響但 v1.0.0 milestone 接近，需提前準備。實作時機：v1.0.0
  wave 開版前由 PM 從 pending priority list 提取
how:
  task_type: Implementation
  strategy: (1) 改 WORK_LOGS_ROOT 為 path.join(SOURCE_DIR, 'docs', 'work-logs') (2)
    detectActiveWorklogVersion 改為掃描 v* 子目錄並依 lexicographic order 取最新 (3) 加單元測試覆蓋 v0/v1
    並存與 v1 only 情境
acceptance:
- '[x] WORK_LOGS_ROOT 不硬編碼 v*'
- '[x] detectActiveWorklogVersion 支援 v0/v1 並存'
- '[x] 單元測試覆蓋 v0/v1 並存與 v1 only'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-29T01:16:52'
completed_at: null
created: '2026-05-25'
updated: '2026-05-25'
---

# Execution Log

## Task Summary

動態化 W1-074 build.js WORK_LOGS_ROOT v0 硬編碼改為自動探測（v1.0.0 wave 開版前）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

scripts/build.js:64 `WORK_LOGS_ROOT = path.join(SOURCE_DIR, 'docs', 'work-logs', 'v0')` 硬編碼 'v0' major dir。當專案進入 v1.x（CLAUDE.md §8 milestone v1.0.0 內測版本）後，worklog 目錄落於 docs/work-logs/v1/ 時：

- `resolveWorklogPath('1.0.0', WORK_LOGS_ROOT)` 會推斷至 `docs/work-logs/v0/v1.0/v1.0.0/`（錯誤路徑）
- `detectActiveWorklogVersion(WORK_LOGS_ROOT)` 直接掃描 docs/work-logs/v0/ 下的 minor 目錄，看不到 v1/v1.0/...
- build:prod 階段 validateVersionAlignment 永遠回 ok=false，sanity check 形同失效

### 影響範圍

- `scripts/build.js`：WORK_LOGS_ROOT 常數、resolveWorklogPath、detectActiveWorklogVersion
- `tests/unit/scripts/build-version-check.test.js`：W1-081 Phase 2 已調整為新結構（commit 5c8618c3，RED 狀態）

### 相關 Error Pattern

無直接對應 PC；W1-074 → W1-083 → W1-084 修復鏈的最後一片，將 SSOT 反轉設計擴展到 v1+ 世代。

### 用戶確認的設計邊界（2026-05-29）

> 「這裡的前提有問題，不應該有多個 active 的版本」

multi-active fail-fast 行為**保留不變**，僅擴展 major dir 掃描範圍。不取消多 active 失敗判定，不採 lexicographic 取最新。

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 實作策略（Phase 3a 規劃）

#### 修改範圍：scripts/build.js（3 處 + 1 註解更新）

**(1) WORK_LOGS_ROOT 常數（line 64）**

```javascript
// 舊
const WORK_LOGS_ROOT = path.join(SOURCE_DIR, 'docs', 'work-logs', 'v0');

// 新
// W1-081：去除硬編碼 v0 major dir，改由 resolveWorklogPath 自 packageVersion
// 取 major 動態推斷。支援 v0/v1+ 並存與 v1 only 場景。
const WORK_LOGS_ROOT = path.join(SOURCE_DIR, 'docs', 'work-logs');
```

**(2) resolveWorklogPath（line 111-125）**

從 packageVersion 取 major（第一段），組裝 `v{major}/v{major}.{minor}/v{patch}` 三層路徑：

```javascript
function resolveWorklogPath (packageVersion, workLogsRoot) {
  const match = /^(\d+)\.(\d+)\.(\d+)$/.exec(packageVersion);
  if (!match) {
    return {
      ok: false,
      error: `[VERSION CHECK] package.json 版號格式不符 (預期 M.m.p)：${packageVersion}`
    };
  }
  const [, major, minor] = match;
  const majorDir = `v${major}`;                       // W1-081：新增 major 層
  const minorDir = `v${major}.${minor}`;
  const patchDir = `v${packageVersion}`;
  const patchPath = path.join(workLogsRoot, majorDir, minorDir, patchDir);
  const mainFile = path.join(patchPath, `${patchDir}-main.md`);
  return { ok: true, patchDir: patchPath, mainFile };
}
```

**(3) detectActiveWorklogVersion（line 149-197）**

新增最外層 major dir 掃描（regex `^v\d+$`），再依現有邏輯掃描 minor / patch：

```javascript
function detectActiveWorklogVersion (workLogsRoot, fsAdapter = defaultFsAdapter) {
  if (!fsAdapter.fileExists(workLogsRoot)) {
    return { ok: false, reason: `worklog 根目錄不存在: ${workLogsRoot}` };
  }

  const activeVersions = [];

  // W1-081：新增 major dir 掃描層（v0 / v1 / v2...）
  const majorDirs = fsAdapter.readdir(workLogsRoot)
    .filter((name) => /^v\d+$/.test(name));

  for (const majorDir of majorDirs) {
    const majorPath = path.join(workLogsRoot, majorDir);
    if (!fsAdapter.stat(majorPath).isDirectory()) continue;

    const minorDirs = fsAdapter.readdir(majorPath)
      .filter((name) => /^v\d+\.\d+$/.test(name));

    for (const minorDir of minorDirs) {
      const minorPath = path.join(majorPath, minorDir);
      if (!fsAdapter.stat(minorPath).isDirectory()) continue;

      const patchDirs = fsAdapter.readdir(minorPath)
        .filter((name) => /^v\d+\.\d+\.\d+$/.test(name));

      for (const patchDir of patchDirs) {
        const patchPath = path.join(minorPath, patchDir);
        if (!fsAdapter.stat(patchPath).isDirectory()) continue;

        const mainFile = path.join(patchPath, `${patchDir}-main.md`);
        if (!fsAdapter.fileExists(mainFile)) continue;

        const content = fsAdapter.readFile(mainFile);
        if (/\*\*狀態\*\*\s*[:：]\s*開發中/.test(content)) {
          activeVersions.push(patchDir.replace(/^v/, ''));
        }
      }
    }
  }

  if (activeVersions.length === 0) {
    return { ok: false, reason: '未找到任何 active worklog（標記為「**狀態**: 開發中」）' };
  }

  if (activeVersions.length > 1) {
    return {
      ok: false,
      reason: `找到多個 active worklog（應僅 1 個）: ${activeVersions.join(', ')}`,
      candidates: activeVersions
    };
  }

  return { ok: true, version: activeVersions[0] };
}
```

**(4) JSDoc 註解同步**

- `resolveWorklogPath` 註解的「workLogsRoot - docs/work-logs/v0/ 絕對路徑」需改為 `docs/work-logs/ 絕對路徑`
- `detectActiveWorklogVersion` 註解的「列舉 docs/work-logs/v0/ 下所有 v* 主版本目錄」需改為「列舉 docs/work-logs/ 下所有 v{major} 目錄並進入掃描」
- `syncWorklogStatus` 與 `validateVersionAlignment` 註解中含 `docs/work-logs/v0/` 字面也需同步調整

### 不在本 ticket 範圍

- syncWorklogStatus 邏輯本身不需修改（resolveWorklogPath 已處理 v{major} 推斷）
- `buildWorklogTree` 測試 helper 已在 Phase 2 RED commit 中調整（5c8618c3）
- v0/v1 之間的版號相對排序語意（用戶澄清：不應有多 active，fail-fast 保持）

### 驗收指令

```bash
npx jest tests/unit/scripts/build-version-check.test.js
```

預期：28/28 全綠（從 RED 20 failed / 8 passed 轉 GREEN）。

### Commit Policy

單一 commit，message 範本：

```
fix(0.19.0-W1-081): 動態化 build.js WORK_LOGS_ROOT 移除 v0 硬編碼

- WORK_LOGS_ROOT 上移至 docs/work-logs/，去除 'v0' 段
- resolveWorklogPath 從 packageVersion 取 major 組 v{major} 層
- detectActiveWorklogVersion 新增最外層 major dir 掃描，保留
  multi-active fail-fast 邏輯
- JSDoc 註解同步更新引用路徑

支援 v0/v1 並存（v0 系列已完成 + v1 active）與 v1 only（v0 archive
後）情境。multi-active 仍 fail-fast（用戶確認：架構上不應有多 active）。

Test Results: 28/28 passed（含 7 個 W1-081 新增案例）

ticket: 0.19.0-W1-081
```


---

### Phase 4 重構評估報告（多視角共識）

**派發**：linux（good taste + 架構複雜度）+ ginger-performance-tuner（效能與架構優化）並行掃描 commit 04547551 範圍。

#### 評估結果

| 維度 | 結論 |
|------|------|
| 重構需要 | **否**（雙視角共識） |
| 技術債務 | 無新增 |
| 程式碼品質 | Acceptable |

#### linux 視角發現

| # | 發現 | 幅度 | 是否建 Ticket |
|---|------|------|--------------|
| 1 | 三層巢狀 for-loop 深度 3，逼近 cognitive-load 上限但與目錄結構同構 | Acceptable | 否 |
| 2 | 三個 regex 重複 `v\d+` 模式 | Acceptable | 否（提取常數反增認知跳轉）|
| 3 | resolveWorklogPath 與 detectActiveWorklogVersion 各自重組路徑層級命名 | Good taste | 否（兩函式語意不同，強行 DRY 破壞 SRP）|
| 4 | activeVersions.push 字串剝離 v 前綴散落最內層 | Acceptable | 否（目前僅一處）|
| 5 | 建議訊息 `v{M}/v{M.m}/v{M.m.p}` 佔位符語法與實際格式風格不一致 | Acceptable | 否（UX 小議題）|

linux 結論：「資料結構驅動」而非「特例補丁」，符合 good taste 原則，無「應被刪除的特例」。

#### ginger 視角發現

| # | 發現 | 幅度 | 是否建 Ticket |
|---|------|------|--------------|
| 1 | 三層 readdir + stat I/O 線性放大 | 低 | 否（build 階段單次、亞秒級）|
| 2 | 三層 regex filter 結構重複 | 低 | 否（對稱性提升可讀性）|
| 3 | 無法 short-circuit | — | 否（fail-fast 設計正確性需求）|
| 4 | fsAdapter.stat().isDirectory() 每層呼叫一次 | 低 | 否（syscall 級無感）|
| 5 | 認知負擔指數巢狀深度 4，接近閾值 5 | 中 | 否（未越過必須重構閾值；若未來擴至 pre-release / build metadata 再評估提取 walker helper）|

ginger 結論：純函式 + fsAdapter 依賴注入 + 完整 JSDoc 三明示，符合 quality-common §1.2 / cognitive-load §3b 標準。

#### Phase 4 結論

**無需重構**。雙視角共識「Acceptable / 無新增技術債」。所有發現均屬可接受範圍，未達建 ticket 閾值（依 Worth-It Filter：低風險 × 低改善幅度，發現本身已於評估報告留存追蹤）。

未來觸發條件（規則 1.5 邊界，本身屬已決策結論非延後）：若 detectActiveWorklogVersion 需擴展支援 pre-release / build metadata，將觸發 walker helper 提取的重新評估（屬未來功能需求觸發點，非無 trigger 延後）。
## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 驗證指令

```bash
npx jest tests/unit/scripts/build-version-check.test.js
```

### 結果

- 28/28 全綠（從 Phase 2 RED 20 failed / 8 passed 轉 GREEN）
- 含 7 個 W1-081 新增案例覆蓋 v0/v1 並存與 v1 only 情境
- 跨 major dir 多 active fail-fast 邏輯保留

### 廣域回歸

- `npx jest tests/unit/scripts/` 53/53 全綠（含 validate-manifest.test.js 25 個案例未受影響）

### 實作摘要

- scripts/build.js:64 WORK_LOGS_ROOT 從 `docs/work-logs/v0` 上移至 `docs/work-logs/`
- resolveWorklogPath 新增 majorDir 層（`v${major}`），與 minorDir、patchDir 組三層路徑
- detectActiveWorklogVersion 新增最外層 major dir regex `^v\d+$` 掃描，內層 minor/patch 遞迴
- JSDoc 註解三處同步更新（resolveWorklogPath / detectActiveWorklogVersion / validateVersionAlignment / syncWorklogStatus 的 workLogsRoot 參數說明）
- build flow 修復建議訊息更新為 `v{M}/v{M.m}/v{M.m.p}` 三層格式

### 測試 fixture 微調

- tests/unit/scripts/build-version-check.test.js:230-237「全形冒號」測試補上 `path.join(WORK_LOGS_ROOT, 'v0')` 目錄條目
  - 原因：W1-081 後 detectActiveWorklogVersion 經 `stat().isDirectory()` 檢查 major dir，原測試 literal file map 漏掉 `v0` 條目，使 stat 回 isDirectory=false 阻斷遍歷
  - buildWorklogTree 樹工廠已涵蓋此情境（line 64 `files[majorVersionPath] = null`），但本 test 使用 literal map 而非工廠
  - 屬 fixture 完整性修補，保持原測試「regex 容忍全形冒號」之意圖不變

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-29
**Executing Agent**: thyme-extension-engineer（Phase 3b GREEN）+ PM 前台（Phase 2 RED + Phase 4 評估彙整）
**Review Status**: Phase 4 多視角共識「無需重構」

#### 變更摘要

- scripts/build.js：WORK_LOGS_ROOT 移除 v0 硬編碼，resolveWorklogPath 新增 majorDir 層，detectActiveWorklogVersion 新增最外層 major dir 掃描，JSDoc 註解三處同步
- tests/unit/scripts/build-version-check.test.js：buildWorklogTree helper 加 majorDir 自動推斷，新增 7 個 W1-081 案例（v0/v1 並存、v1 only、跨 major multi-active、v1.0.0 推斷、v2.3.4 通用化、validate/sync 對 v1.0.0 行為驗證）+ 全形冒號 fixture 微調

#### Commits

- 5c8618c3 test(0.19.0-W1-081): Phase 2 RED（28 中 20 failed 為預期）
- 04547551 fix(0.19.0-W1-081): GREEN 實作（28/28 全綠 + 廣域 53/53 回歸通過）
- bd37d79c chore(0.19.0-W1-081): acceptance state sync

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W1-074 | chars: 436 -->

### Task Reference
- 0.19.0-W1-074 what: 增強 scripts/build.js 加入版號 sanity check（assert package.json 版號與 active worklog 版號一致）

### Rationale Chain
- 0.19.0-W1-074 why: 0.19.0-W1-072 ANA spawn IMP-B：build:prod 缺版號 sanity check 是 W1-072 觀察到的根因之一（v0.18.0 已發布但 wave 進入 v0.19.0 未 bump 卻無 fail-fast）。借鑑 IMP-068 sync-push 缺 sanity check 案例：版號錯亂可靜默 push。本 IMP 在 build.js 加入 assert：若 package.json 版號與 active worklog 版號（從 docs/work-logs/v0/*/v*-main.md 路徑推斷）不一致則 fail-fast，避免內測 ZIP 帶錯版號

### Related Files
- tests/unit/scripts/build-version-check.test.js  # from 0.19.0-W1-074