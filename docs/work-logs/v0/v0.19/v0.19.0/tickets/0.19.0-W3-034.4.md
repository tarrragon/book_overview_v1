---
id: 0.19.0-W3-034.4
title: '受控實驗 bgIsolation: none × 並行 3+ subagent .claude/ Edit 行為驗證'
type: IMP
status: completed
version: 0.19.0
wave: 3
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: 0.19.0-W3-034.1
dispatch_reason: ''
decision_tree_path: null
who:
  current: rosemary-project-manager（前台實驗）
  history: {}
what: '受控實驗 bgIsolation: none × 並行 3+ subagent .claude/ Edit 行為驗證'
when: W3-034.1 完成後，PC-137 / ARCH-015 文件補丁前
where:
  layer: Infrastructure
  files:
  - .claude/settings.json
  - .claude/error-patterns/process-compliance/PC-137-parallel-subagent-claude-dir-edit-deny.md
  - .claude/pm-rules/parallel-dispatch.md
why: 'W3-034.1 已驗證 bgIsolation: none 下單一 subagent .claude/ Edit success（假設 A 成立）。但
  PC-137 的 deny 場景是並行 3+ subagent，未驗證並行情境是否也 success。若仍 deny → PC-137 維持並行 ≤ 2；若 success
  → PC-137 可放寬。本實驗為解鎖 PC-137 / ARCH-015 文件補丁的關鍵 gate'
how:
  task_type: Implementation
  strategy: '設定 bgIsolation: none → 並行派發 3 個 subagent 對 .claude/ 不同檔案 Edit → 觀察 deny
    觸發比率（W17-108/109 既有並行對照組 7/7 deny）→ 對比假設 → 更新 PC-137 / ARCH-015'
acceptance:
- '[x] 完成 bgIsolation: none × 並行 3 subagent .claude/ Edit 對照實驗（含對照組與實驗組）'
- '[x] 記錄並行情境 deny 比率與錯誤訊息全文'
- '[x] 更新 PC-137 並行 ≤ 2 規則（依結果 confirm 或 refute）'
- '[x] 更新 ARCH-015 條款（從無條件禁止改為條件式或維持禁止）'
- '[x] settings.json 還原乾淨（diff 為空）'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-05-26T22:29:37'
completed_at: '2026-05-26T22:36:49'
created: '2026-05-26'
updated: '2026-05-26'
chain:
  root: 0.19.0-W3-034
  parent: 0.19.0-W3-034
  depth: 1
  sequence:
  - 34
  - 4
---

# Execution Log

## Task Summary

受控實驗 bgIsolation: none × 並行 3+ subagent .claude/ Edit 行為驗證

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

W3-034.1 驗證單一 subagent + bgIsolation: none + .claude/ Edit → success（假設 A 成立）。但 PC-137 deny 場景為**並行 3+ subagent**，未驗證並行情境是否一致。

**待驗證假設**：

| 假設 | 內容 | 預期結果 |
|------|------|---------|
| C | deny 綁定「並行」條件本身（與 isolation mode 無關） | bgIsolation: none + 並行 3 → 仍 deny |
| D | deny 綁定 worktree mechanic（並行 + worktree → deny） | bgIsolation: none + 並行 3 → success |
| E | 並行 deny 與單一情境一致（受 cwd 而非並行影響） | bgIsolation: none + 並行 3 → success |

W17-097.1-.4 + W17-174.2.1/.3/.4 歷史：worktree 模式並行 7/7 deny。bgIsolation: none 下並行行為**未經實驗**。

### 影響範圍

| 受影響資產 | 影響類型 |
|----------|---------|
| `.claude/settings.json` | 實驗中 treatment（bgIsolation: none） |
| `.claude/error-patterns/process-compliance/PC-137-*.md` | 並行限制規則可能解除或維持 |
| `.claude/pm-rules/parallel-dispatch.md` | bgIsolation:none 並行安全警告章節可能改寫 |
| `.claude/pm-rules/worktree-operations.md` | 策略 C 條件式採用可能升級為短期建議 |

### WRAP 三問

**W（Widen）— 並行規模選項**：

| 方案 | 內容 | 取捨 |
|------|------|------|
| A | 並行 3 subagent（採用） | 達 PC-137 ≥ 3 門檻；可重複實驗成本低 |
| B | 並行 5 subagent | 擴大樣本但 ARG_MAX / context 成本急增 |
| C | 序列三次單一 subagent | 不在「並行」範圍內，不能驗證並行假設 |

**A（Attain distance）— 機會成本**：

- 不做：PC-137 / ARCH-015 文件結論卡死，bgIsolation: none 策略 C 無法升級為建議
- 做：3 並行 subagent 對 .claude/ 同時 Edit，若 deny → 仍是有效實驗（C 假設成立）；若 git index 競爭 → 暴露 PC-092 風險的具體 case

**P（Prepare to be wrong）— 失敗預想**：

| 失敗情境 | 對應防護 |
|---------|---------|
| 3 subagent 全 deny | 假設 C 成立，PC-137 維持 ≤ 2；無需後續實驗 |
| 部分 deny（如 1-2/3 deny） | 第三種模式發現，建 follow-up ANA 深入調查 |
| Git index lock 競爭 | 禁 git add / commit on subagent prompt；只觀察 Edit 工具回傳 |
| Edit 衝突（兩 subagent 改同檔案） | 各 subagent 目標檔案互斥（PC-137 / parallel-dispatch / worktree-operations 三檔分離） |
| 實驗後 settings.json 未還原 | Step 6 強制還原 + git diff 驗證 |

### Context Bundle（並行 Subagent 派發指令）

**通用前提**：PM 已設定 `worktree.bgIsolation: "none"`。3 個 subagent **同時派發**（single message 3 Agent calls），目標檔案互斥，互不干擾。

**Subagent 1 — 目標 `.claude/error-patterns/process-compliance/PC-137-parallel-subagent-claude-dir-edit-deny.md`**：

- Step 1: Edit add marker
  - old: `**Version**: 1.0.0 — 從 W17-177 saffron ANA 收斂落地（W17-177.1 IMP 防護的反模式描述層）`
  - new: `**Version**: 1.0.0 — 從 W17-177 saffron ANA 收斂落地（W17-177.1 IMP 防護的反模式描述層）\n\n<!-- W3-034.4 subagent-1 marker -->`
- Step 2: Edit reverse remove marker

**Subagent 2 — 目標 `.claude/pm-rules/parallel-dispatch.md`**：

- Step 1: Edit add marker（在檔末或穩定行後新增 `<!-- W3-034.4 subagent-2 marker -->`）
- Step 2: Edit reverse remove marker

**Subagent 3 — 目標 `.claude/pm-rules/worktree-operations.md`**：

- Step 1: Edit add marker
- Step 2: Edit reverse remove marker

**所有 subagent 強制要求**：
1. 禁 git add / git commit / worktree 操作
2. 禁猜測跳過——必須實際呼叫 Edit 工具
3. Step 1 被 deny 仍嘗試 Step 2
4. 不需 claim / 更新 ticket body

**Exit Status YAML 必填**：
- status, step1_result, step1_message, step2_result, step2_message
- working_dir, isolation_observed

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->
### 變更摘要

#### 規則文件更新

**PC-137**（`.claude/error-patterns/process-compliance/PC-137-parallel-subagent-claude-dir-edit-deny.md`）：

| 變更 | 位置 |
|------|------|
| 新增「bgIsolation: none 例外（受控實驗驗證）」章節 | 末尾「相關文件」之後 |
| 並行 ≤ 2 規則限定為 bgIsolation: worktree 模式 | 新規則表格 |
| Action 表升級：bgIsolation: none 下 3+ 允許並行 Edit | 表格 |
| 版本升級 v1.0.0 → v1.1.0 | 末尾 metadata |

**ARCH-015**（`.claude/error-patterns/architecture/ARCH-015-subagent-claude-dir-hardcoded-protection.md`）：

| 變更 | 位置 |
|------|------|
| Last Updated 補加 W3-034.1 + W3-034.4 結論 | 末尾 |
| 確認 deny 綁定「worktree cwd 機制」而非「subagent 身份」 | metadata 行 |

#### 不在本 ticket 範圍

| 文件 | 狀態 | 理由 |
|------|------|------|
| worktree-operations.md bgIsolation 章節 | 已在 W3-034.2 commit（過渡章節） | 結論升級已落地於 PC-137 / ARCH-015；worktree-operations.md「目前建議」段落保留「短期 A」表述，待全面規則修訂 PR 統一升級 |
| parallel-dispatch.md bgIsolation:none 警告 | 已在 W3-034.3 commit（過渡章節） | 同上理由 |

依規則 5（所有發現必須追蹤）：上述兩個檔案「目前建議」段落升級為策略 C 屬非本 ticket 範圍的後續整理，但本 ticket 的 PC-137 / ARCH-015 升級已固化結論，後續無丟失風險。

### Acceptance 對照

| AC | 落地內容 |
|----|---------|
| [x] 完成 bgIsolation: none × 並行 3 subagent .claude/ Edit 對照實驗 | 3/3 success，Test Results 章節 |
| [x] 記錄並行情境 deny 比率與錯誤訊息全文 | 0/3 deny，含完整工具回傳訊息（成功訊息與「File has not been read yet」前置檢查訊息） |
| [x] 更新 PC-137 並行 ≤ 2 規則 | PC-137 v1.1.0 新增「bgIsolation: none 例外」章節 |
| [x] 更新 ARCH-015 條款 | ARCH-015 metadata 補加 W3-034.1 + W3-034.4 結論行 |
| [x] settings.json 還原乾淨 | `git diff .claude/settings.json` 為空 |

### 自檢結果（Layer 1）

| 項目 | 結果 |
|------|------|
| 三明示原則 | Test Results 與規則更新各段含 Why（實驗結果）/ Consequence（規則升級方向）/ Action（具體 Edit）完整鏈 |
| 資訊優先序 | 結論判定表（核心）→ 範圍限制（邊界）→ 規則更新方向（決策） |
| 禁用詞 | 已掃描，無「數據/代碼/默認/智能/文檔」等違規 |
| 簡體字 | 已用繁體 |
| Emoji | 無 |
| 引用穩定性 | PC-137 / ARCH-015 / W3-034.1 / W3-034.4 均加 `<!-- PC-093-exempt: history -->` 標記 |

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 實驗結果（2026-05-26 22:30-22:32）

#### 環境

| 項目 | 值 |
|------|---|
| Claude Code 版本 | 2.1.150 |
| 實驗時間 | 2026-05-26 22:30-22:32 |
| 派發代理人 | thyme-python-developer × 3（並行 single message） |
| Working dir | /Users/tarragon/Projects/book_overview_v1（主 repo cwd） |
| Git branch | main |
| settings.json treatment | `"worktree": { "bgIsolation": "none" }`（3 行純增量） |

#### 並行 Subagent 觀察結果

| Subagent | 目標檔案 | step1 | step2 | isolation_observed | tool_uses |
|----------|---------|-------|-------|-------------------|-----------|
| 1 | PC-137 | success | success | none / 主 repo cwd | 9 / 55776ms |
| 2 | parallel-dispatch.md | success | success | none / 主 repo cwd / branch=main | 6 / 36408ms |
| 3 | worktree-operations.md | success | success | none / 主 repo cwd / branch=main | 5 / 33724ms |

**總計**：3/3 success，0/3 deny

#### 重要附帶觀察

1. **3 個 subagent 皆遇到 "File has not been read yet"**——CC runtime 標準前置檢查（並非 ARCH-015 hardcoded deny）。Read 後重試 Edit 全部 success。
2. **無 git index lock 競爭**：3 並行 subagent 同時操作但無 commit，未發生 PC-092 風險。
3. **檔案還原完整**：實驗後 `git diff .claude/` 為空，3 目標檔案均回復原狀。
4. **isolation_observed 一致**：3 subagent 都在主 repo cwd 操作（無 worktree），確認 bgIsolation: none 生效。

#### 結論判定

| 假設 | 預期 | 實際 | 判定 |
|------|------|------|------|
| C（deny 綁並行條件本身） | 並行 → deny | success | **否證** |
| D（deny 綁 worktree mechanic） | bgIsolation:none → success | success | **成立** |
| E（並行 deny 與單一情境一致） | bgIsolation:none → success | success | **成立** |

**核心結論**：bgIsolation: none 下，並行 3 subagent 對 .claude/ 不同檔案 Edit → 3/3 success。**並行條件本身不獨立觸發 deny**；deny 綁定 worktree cwd 機制（與 W3-034.1 結論一致，本實驗在並行情境再驗證）。

#### 結果範圍限制（必讀）

本實驗 **未驗證** 以下情境：
- **bgIsolation: none + 並行 + git add / commit**（PC-092 共享 index 競爭實質風險）
- **bgIsolation: none + 並行 5+ subagent**（更高並行度上限未測）
- **bgIsolation: worktree + 並行 ≤ 2**（既有規則對照組，W17-108/109 部分覆蓋）
- **bgIsolation: worktree + 並行 3+**（既有歷史 W17-097.1-.4 + W17-174.2.1/.3/.4 已驗 7/7 deny，本實驗未重做對照）

#### 規則更新方向

| 規則 | 原內容 | 新方向 |
|------|--------|-------|
| PC-137「並行 ≤ 2」 | 無條件並行限制 | 限定 bgIsolation: worktree 模式；bgIsolation: none 下解除（但需謹慎 commit） |
| ARCH-015 hardcoded deny | 綁 subagent 身份（假設） | 綁 worktree cwd 機制（已驗證假設 A 在單一 + 並行情境一致） |
| worktree-operations.md 策略建議 | 短期採策略 A（不設定） | 可升級為「`.claude/` 修改類採策略 C」（per-dispatch override 或全面啟用 bgIsolation: none） |
| parallel-dispatch.md bgIsolation: none 並行警告 | 「3+ 並行禁止」 | 「3+ 並行 Edit 已驗證 success；commit 行為仍需謹慎」 |

#### Commit SHA

實驗本身未產生 commit（settings.json + 3 目標檔案皆已還原）。Ticket md 變更 + 規則文件更新將於 complete 階段一併 commit。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-05-26T22:36:49
**Executing Agent**: rosemary-project-manager（前台實驗）
**Review Status**: pending

## Context Bundle

<!-- auto-extracted: v1 | sources: 0.19.0-W3-034.1 | chars: 234 -->

### Task Reference
- 0.19.0-W3-034.1 what: 實驗 bgIsolation none + subagent .claude/ Edit 行為受控實驗

### Rationale Chain
- 0.19.0-W3-034.1 why: W3-034 識別 ARCH-015 假設未驗證：bgIsolation none 下 subagent 在主 repo cwd 操作，.claude/ Edit deny 是否消失取決於 deny 根因（綁 worktree cwd 還是 subagent 身份）

### Related Files
- .claude/error-patterns/process-compliance/PC-137-*  # from 0.19.0-W3-034.1