---
id: 0.19.1-W2-001
title: 盤點並設計 design-system Phase 2 (CSS 變數橋接) 落地策略 design-system.css 產生機制 + 頁面硬編碼收斂
  + spec 對比度缺陷修正
type: ANA
status: pending
version: 0.19.1
wave: 2
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.19.1-W1-003
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: Layer 2 任務分類
  final_decision: Analysis 類：先盤點硬編碼 + 決定 CSS 橋接產生機制 + 診斷 spec 3.3 對比度缺陷 + 吸收 W1-003
    漸層盤點，再 spawn IMP/DOC
  rationale: 跨多檔案 + 含設計決策(雙來源漂移與 spec 缺陷修正方向) + 涉設計系統收斂原則
who:
  current: saffron-system-analyst
  history: {}
what: 盤點並設計 design-system Phase 2 (CSS 變數橋接) 落地策略 design-system.css 產生機制 + 頁面硬編碼收斂
  + spec 對比度缺陷修正
when: 使用者用 impeccable critique 發現 design-system 清單只被 JS 引用、CSS/HTML 全硬編碼，要求從根因修起
where:
  layer: 待定義
  files:
  - src/core/design-system/colors.js
  - src/core/design-system/index.js
  - src/overview/overview.css
  - src/popup/popup.html
  - docs/spec/design-system-spec.md
why: 'SPEC-DESIGN-SYSTEM Phase 1 (JS 常數) 已出貨但 Phase 2 (CSS 橋接 design-system.css +
  頁面接線) 從未實作，導致 design-system 清單只被 JS 邏輯引用、CSS/HTML 全硬編碼。實證：popup 主色 #4f46e5、overview
  modal #667eea、清單 primary #2196F3 三套並存。此為使用者回報 預設版型太糟/清單沒正確使用 的根因'
how:
  task_type: Implementation
  strategy: grep 盤點全專案硬編碼 color/spacing/radius/gradient → 量測 badge 對比度證明 spec 缺陷 →
    多方案評估 design-system.css 產生機制(自動 vs 手維護) → 設計徽章對比度修正 → spawn IMP/DOC 並繼承 Context
    Bundle
acceptance:
- '[ ] Problem Analysis 含全專案硬編碼樣式盤點(color/spacing/radius/gradient，檔案+行號+分類)，吸收 W1-003
  漸層盤點'
- '[ ] 重現實驗結果：量測 reading-status-badge unread 實際對比度證明 spec 3.3 深色字規則與 primaryLightest
  淺色字自相矛盾'
- '[ ] Solution 含 design-system.css 產生機制決策 + 徽章對比度修正方向 + spawn 規劃表'
- '[ ] spawned IMP/DOC ticket 已建立並繼承 Context Bundle(PC-100)'
- '[ ] W1-003 relatedTo 連結建立並於 Solution 註記吸收範圍'
tdd_phase: null
tdd_stage: []
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-06-04'
updated: '2026-06-04'
---

# Execution Log

## Task Summary

盤點並設計 design-system Phase 2 (CSS 變數橋接) 落地策略 design-system.css 產生機制 + 頁面硬編碼收斂 + spec 對比度缺陷修正

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### PM 前置決策鎖定（WRAP 完整模式，2026-06-04）

--force 理由：PM 派發前寫入前置 context（PC-040），ticket 仍 pending 屬正常時序。

4 項已由 PM + 用戶 WRAP 定案，ANA 執行時直接採用，不重新爭論：

| # | 決策 | 結論 | 依據 |
|---|------|------|------|
| D1 | design-system.css 產生機制 | build 自動產生：colors.js/spacing.js/typography.js 為單一來源，design-system.css 為 generated artifact + CI 一致性檢查 | 用戶要求結構性杜絕漂移 > 成本；service worker 無 DOM 淘汰 CSS-為-SSOT；對齊 spec 1.5 SSOT |
| D2 | 漸層去留 | 全面移除改扁平色，廢棄 spec 2.6 brand gradient token | 遵 W1 章程 全專案 UI 無漸層色 |
| D3 | 徽章對比度 | 改深字淺底 WCAG AA，修 spec 3.3 + colors.js STATUS_COLORS | spec 3.3 自相矛盾（規定深色字卻給 unread 淺色字，實測約 1.1:1） |
| D4 | APP 端對齊 | 不納入本次，標註 book_overview_app follow-up | 跨 repo 範圍蔓延 |

ANA 仍需執行：硬編碼全量盤點（含 W1-003 漸層）、build 產生器具體設計、徽章新配色具體值、spawn IMP/DOC。

build 產生器設計要點：讀 3 個 .js 輸出 design-system.css（:root 對齊 spec 10）→ 接 build.js（dev/prod 皆產）→ 產物檔頭標 AUTO-GENERATED 禁手改 → snapshot test + CI 檢查 .css 與重新產生一致（不一致則 fail）。

## 重現實驗結果

### 實驗方法

（必填：如何重現問題？用什麼指令/測試？本章節為 ANA Ticket 強制章節——PC-063 防護措施）

### 實驗執行

（必填：實驗執行過程記錄，包含輸入、步驟、觀察到的實際行為）

### 實驗發現

（必填：實驗結論——已驗證的事實 vs 仍未驗證的假設。僅在完成實驗後才允許列候選方案。）

---

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 整體 Roadmap 與 Spawn 規劃表（PM 預先定案，接手照表執行，2026-06-04）

--force 理由：PM 派發前寫入執行藍圖（PC-040），ticket 仍 pending 屬正常時序。

本 ANA 執行時照下表 spawn 子任務，不需重新評估順序。優先序由 priority + blockedBy 表達，runqueue scheduler 會據此算出可執行順序。

#### Wave A 拓樸（本 ANA spawn）

| 預定 ID | type | priority | blockedBy | agent | 範圍 | 檔案 |
|---------|------|----------|-----------|-------|------|------|
| W2-002 | IMP | P1 | W2-001 | thyme-extension-engineer | build 產生器（3 個 .js → design-system.css，:root 對齊 spec 10）+ 接 build.js + 產物檔頭 AUTO-GENERATED + snapshot test + CI 一致性檢查 | scripts(產生器)、build.js、src/core/design-system/design-system.css(產物) |
| W2-003 | IMP | P1 | W2-002 | thyme-extension-engineer | overview.css 全面改 var() + 移除所有漸層改扁平色 + 徽章改深字淺底（D2/D3） | src/overview/overview.css |
| W2-004 | IMP | P1 | W2-002 | thyme-extension-engineer | popup.html inline style 改 var() + 移除所有漸層 + 移除 [START]/[ALERT] 佔位符 + 主色 #4f46e5 歸一 primary | src/popup/popup.html |
| W2-005 | DOC | P2 | W2-003, W2-004 | thyme-documentation-integrator | 修 spec：§2.6 廢漸層 token / §3.3 徽章對比度新值 / 標記 Phase 2 完成 / §11.1 APP 對齊改標 book_overview_app follow-up | docs/spec/design-system-spec.md |

執行順序（runqueue 自動浮出）：W2-002 →（W2-003 ∥ W2-004 可並行，檔案不重疊）→ W2-005。

#### 跨 ticket acceptance 共通要求（src 字串/樣式變更）

- W2-003 / W2-004 修改 src 樣式與字面（移除佔位符屬字串變更），acceptance 必含 npm test exit 0（W1-040 規則，非僅 build:dev）+ npm run build:dev 成功。
- W2-005 為 spec DOC，acceptance 含 grep 驗證 spec 與 colors.js 值一致。

#### Wave B 銜接

W3-001（Wave B 視覺重塑 ANA，P2，blockedBy W2-001）在 Wave A 全部 complete 後執行。D1=build 產生器使 Wave B 換配色為廉價操作（改 colors.js 重新產生），故 Wave B 配色策略（D5）留 W3-001 決定，無需提前。

待 W2-002~005 實際建立後，PM 補 W3-001 blockedBy 指向 W2-005（trigger=W2-001 spawn 完成）。

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: saffron-system-analyst
**Review Status**: pending