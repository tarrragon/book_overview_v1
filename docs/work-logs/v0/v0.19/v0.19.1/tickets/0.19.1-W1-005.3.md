---
id: 0.19.1-W1-005.3
title: 清理 tests/ 裝飾性 emoji 與 npm test 全綠驗證
type: IMP
status: in_progress
version: 0.19.1
wave: 1
priority: P2
parent_id: 0.19.1-W1-005
children: []
blockedBy:
- 0.19.1-W1-005.2
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 清理 tests/ 裝飾性 emoji 與 npm test 全綠驗證
when: child-2（W1-005.2）src 移除後
where:
  layer: 待定義
  files:
  - tests/
why: tests/ 裝飾性 emoji（describe 名稱/console.log）一併清理保持一致；並驗證 src 全移除不破壞 npm test。注意
  test-data-generator 模擬書名 emoji 屬功能測試資料（書名可含 emoji），須保留不移除
how:
  task_type: Implementation
  strategy: (1) 清理 tests/ describe 名稱 + console.log 裝飾 emoji；(2) 保留 test-data/fixtures
    模擬書名 emoji（功能測試資料）；(3) npm test 全綠驗證；(4) 特別複驗 'Popup Script 載入完成'/'Popup Interface
    初始化完成' 等被引用字串未因 emoji 移除破壞（PM 前置取證 1 檔命中 vs ANA 0 依賴 之矛盾觀察項）
acceptance:
- '[ ] active tests/ emoji 移除/替換（複用 scripts/remove-emoji.js，排除 test-data/fixtures
  與備份檔）'
- '[ ] 斷言 emoji（toHaveBeenCalledWith/toContain 等，約 12 檔 48+ 處）修正對齊 src [KEYWORD]
  格式，npm test 不因斷言期待值落差失敗'
- '[ ] test-data/fixtures 模擬書名 emoji 保留（不誤清）'
- '[ ] 122 垃圾備份檔（.backup-*/.bak/.tmp）刪除 + .gitignore 規則防再生'
- '[ ] npm test 全綠（單元+整合），無 regression'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-03T02:24:15'
completed_at: null
created: '2026-06-03'
updated: '2026-06-03'
---

# Execution Log

## Task Summary

清理 tests/ 裝飾性 emoji 與 npm test 全綠驗證

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

PC-162（接手者須獨立重新驗證範圍）：本 ticket grep 範圍須用完整 emoji 定義（見下）。

### PM 接手發現：完整 emoji 偵測範圍（W1-005.2 執行期揭露）

W1-005.2 執行時 thyme 揭露父 ticket AC-2 決策表 grep 範圍不完整：原僅涵蓋 `U+1F000-1FFFF` + `U+2600-27BF`，src/ 實際另含 **U+2139-25B6 區段 9 種**（codepoint：`U+2139 U+2328 U+23ED U+23F0 U+23F1 U+23F3 U+23F8 U+23F9 U+25B6`，約 69 處）+ **U+FE0F variation selector**（336 處）。權威映射見 `scripts/remove-emoji.js`。

**本 ticket 適用指引**：

1. 辨識 tests/ 裝飾 emoji 時須用**完整範圍**（上述 9 種 codepoint + U+FE0F 一併納入），否則 describe / console.log 中的 U+2139-25B6 emoji 漏網 + 孤立 FE0F 殘留。
2. 本 ticket **非「grep emoji = 0」**——須保留 `tests/helpers/*-data-generator.js`、`tests/fixtures/*.json` 模擬書名 emoji（功能測試資料，書名可含 emoji）。驗收為「裝飾 emoji（describe 名稱 + console.log）移除 + npm test 全綠」，非全清。
3. 可複用 `scripts/remove-emoji.js`，但須排除 test-data / fixtures 目錄（避免誤清資料 emoji）。

### W1-005.3 範圍重大修訂（PC-162 二階複驗，2026-06-03 PM 接手）

**ANA AC-4 二度誤判**：AC-4 判定「tests/ emoji 屬裝飾性、直接斷言依賴=0」。PM 接手複驗推翻——active test **130 檔 1115 處** emoji（非 ANA 列的 ~10 檔），其中 **12 檔約 48+ 處 emoji 在斷言中**（`toHaveBeenCalledWith('<U+2705> 成功訊息')` / `toContain('<U+1F3AF> 目標訊息')` 等，尖括號內為原 emoji codepoint），直接比對 src/ 的 emoji log 字串。

**W1-005.2 已引入隱性回歸**：src/ emoji 移除後，這些斷言期待值（舊 emoji）與 src 實際輸出（[KEYWORD]）落差，測試 FAIL。實證：`npx jest install-handler.test.js` = 10 failed（斷言 `Expected "<U+274C> 服務重新初始化失敗:"` 但 `Received "[FAIL] 服務重新初始化失敗:"`，U+274C 為原 emoji）。W1-005.2 acceptance 僅 build:dev 未跑 npm test 故未發現。

**關鍵洞察（簡化策略）**：斷言 emoji 與 src emoji **同源**，複用同一個 `scripts/remove-emoji.js` 映射 apply 到 tests/，斷言期待值即對齊 src 新格式，npm test 轉綠。

### 執行步驟（thyme）

1. **複用 `scripts/remove-emoji.js`** apply 到 `tests/`，但用 rg glob 排除：`-g '!*.backup-*' -g '!*.bak' -g '!*.tmp' -g '!*.deprecated'` + 排除 `tests/helpers/*-data-generator.js`、`tests/fixtures/*.json`（保留資料 emoji）。此步同時修斷言 emoji + 清裝飾 emoji。
2. **刪除 122 垃圾備份檔**：`git rm` 所有 `tests/**/*.backup-[0-9]*`、`*.bak`、`*.tmp`（git ls-files 確認 122 個）。
3. **加 .gitignore 規則** 防再生：`*.backup-*` / `*.bak` / `*.tmp`（置 tests/.gitignore 或根 .gitignore）。
4. **npm test 全綠驗證**（同步 Bash + tail，勿用 Monitor——PC：agent Monitor 等待 process 結束未收尾）。debug 殘留失敗：主要是 U+1F504（retry 類）等 src 中「視上下文判斷」的 emoji，tests/ 斷言須與 src 實際選擇一致（查 src 對應行確認 [RETRY] vs 移除）。
5. 抽樣確認 test-data/fixtures 資料 emoji 未被誤清。

### Commit Policy

- `git add tests/ + .gitignore + 本 ticket md`；備份檔刪除用 `git rm`。**禁** `git add -A`（工作區有無關 .claude/ mode 變更，PC-092）。
- branch 已建 `feat/0.19.1-W1-005.3-tests-emoji`（PM 已切換），勿切換。
- commit 標題：`test(0.19.1-W1-005.3): tests/ emoji 對齊 src + 清理 122 垃圾備份檔 + npm test 全綠`。

### 範圍邊界

- 不動 src/（W1-005.2 已完成）。不動 e2e-contract（W1-005.1 已完成）。
- 122 備份檔屬獨立技術債，經用戶裁定併入本 ticket 一次清理（避免再開 ticket）。

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending