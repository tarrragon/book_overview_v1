---
id: 0.19.1-W3-001.3
title: 實作 popup.html 視覺套用：body 去藍改 canvas + 卡片權重三級 + error-card 按鈕收斂 6→2 + 字級層次 +
  移除殘留佔位符
type: IMP
status: completed
version: 0.19.1
wave: 3
priority: P2
parent_id: 0.19.1-W3-001
children: []
blockedBy:
- 0.19.1-W3-001.1
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 實作 popup.html 視覺套用：body 去藍改 canvas + 卡片權重三級 + error-card 按鈕收斂 6→2 + 字級層次 + 移除殘留佔位符
when: W3-001.1 配色就位後（與 W3-001.2 並行，檔案不重疊）
where:
  layer: 待定義
  files:
  - src/popup/popup.html
why: impeccable critique 判定 popup/overview 為典型 AI 預設版型(漸層貼到所有元素 + Material 預設藍 + 同尺寸卡片氾濫)，Design
  Health 23/40。Wave A 補完 token 橋接後需在穩定地基上重塑視覺，否則美化會再次硬編碼。本 ANA 為 Wave B 入口
how:
  task_type: Implementation
  strategy: popup.html inline style 純 var()：body 改 canvas + 主操作卡升 sm shadow 其餘降 panel
    + error-card 6 按鈕收斂 2 + 字級層次 + grep 移除佔位符
acceptance:
- '[x] body background 由 primary 藍改 var(--color-background) canvas，版本文字改 muted on
  canvas 修復對比失敗'
- '[x] 卡片權重三級：主操作卡升 var(--shadow-color-sm)，其餘降 panel 底次階（W3-001 Solution §c）'
- '[x] error-card 按鈕由 6 收斂為 1 primary（重試）+ 1 secondary，其餘移除或收進更多'
- '[x] 字級層次 + 按鈕主次三級 + radius 去 pill'
- '[x] 移除殘留方括號佔位符（FIX/STATS/SAVE/WARN/RETRY/LOG，以字串內容 grep 為準非硬編行號；console.log 前綴保留）'
- '[x] 純 var() 零硬編碼；npm test exit 0 + npm run build:dev 成功'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-05T02:31:23'
completed_at: '2026-06-05T02:36:38'
created: '2026-06-05'
updated: '2026-06-05'
---

# Execution Log

## Task Summary

實作 popup.html 視覺套用：body 去藍改 canvas + 卡片權重三級 + error-card 按鈕收斂 6→2 + 字級層次 + 移除殘留佔位符

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### Context Bundle（繼承自 W3-001 Solution，PC-100）

（--force：PM 派發前寫入 context，PC-040）

- popup body 去藍改 canvas + 卡片權重三級 + 主操作卡升 shadow + error-card 6→2 按鈕 + 字級層次：見 W3-001 Solution §c
- review 觀察 3：佔位符以字串內容 grep 清理（FIX/STATS/SAVE/WARN/RETRY/LOG），非硬編行號（ticket 行號有漂移）；console.log 前綴（如 ERROR 前綴）為 e2e-contract 合法規範須保留
- review 觀察 2：成功文字走 success-text 非 success 色塊
- 前置：依賴 W3-001.1 新 design-system.css；純 var() 零硬編碼

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->
### 驗證指令與結果

- `npm test`：Test Suites 229 passed / 2 skipped (229 of 231 total)；Tests 5466 passed / 41 skipped (5507 total)；exit 0。無新增失敗。
- `npm run build:dev`：成功，產物輸出至 build/development。
- `rg '\[(FIX|STATS|SAVE|WARN|RETRY|LOG)\]' src/popup/popup.html`：NONE（7 個 UI 文案方括號佔位符全數移除，前綴去除保留正式文案）。
- `rg '#[0-9A-Fa-f]{3,6}' src/popup/popup.html`：NONE（零硬編碼 hex，全部 var()）。

### 實作摘要（對應 acceptance）

1. body background 由 var(--color-primary) 藍改 var(--color-background) canvas；版本文字 font-size 11px → var(--font-size-caption)（12px）on canvas，色 var(--color-on-surface-muted) 修復對比。
2. 卡片權重三級：主操作卡新增 .primary-surface（var(--color-surface) 白底 + box-shadow 0 2px 8px var(--shadow-color-sm) + radius md + padding lg），其餘 .status-card 預設降為次階（var(--color-panel) 底 + 1px border + radius sm + padding md，無陰影）；版本資訊置於 canvas 平階。
3. error-card 按鈕收斂：errorContainer 由 5 顯示按鈕（retry/reload/healthCheck/report/initReport）收斂為 1 primary（重試）+ 1 secondary（重新載入擴展），移除 systemHealthCheckBtn/reportBtn/initReportBtn（popup.js 等以 if (elements.X) null-safe guard，移除不破 runtime）。
4. 字級層次：套用 title-medium(18)/body-large(16)/body-medium(14)/caption(12) token；weight 拉開（標題 semi-bold 600，body medium）。按鈕主次三級（primary var(--color-primary) 底白字 / secondary var(--color-panel) 底 ink + border / 新增 danger）。radius 去 pill：button radius 由 6px 改 var(--radius-sm)（8px）。
5. 移除殘留方括號 UI 佔位符 FIX/STATS/SAVE/WARN/RETRY/LOG（7 處）；popup.html 無 console.log/console.error（JS 邏輯在 popup.js），無 e2e-contract console 前綴需保留。

### 保留的非 token px（合理）

- width:350px / min-height:400px：popup 視窗尺寸（非設計 token 範疇）。
- 1px solid border：邊框寬度（非 spacing token）。

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: 2026-06-05T02:36:38
**Executing Agent**: saffron-system-analyst
**Review Status**: pending