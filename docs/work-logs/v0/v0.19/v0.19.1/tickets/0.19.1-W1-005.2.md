---
id: 0.19.1-W1-005.2
title: 移除 src/ 全專案 emoji 與 ASCII 前綴替換（85 檔 ~1840 處）
type: IMP
status: pending
version: 0.19.1
wave: 1
priority: P2
parent_id: 0.19.1-W1-005
children: []
blockedBy:
- 0.19.1-W1-005.1
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: thyme-extension-engineer
  history: {}
what: 移除 src/ 全專案 emoji 與 ASCII 前綴替換（85 檔 ~1840 處）
when: child-1（W1-005.1）契約修訂後
where:
  layer: 待定義
  files:
  - src/
why: 全專案 85 檔 1840 處 emoji 違反 language-constraints 規則 3（禁所有文字輸出 emoji）。background/
  55 檔為最大宗。依 W1-005 ANA 決策全專案一次移除以避免規則執行不一致
how:
  task_type: Implementation
  strategy: 依 W1-005 ANA AC-2 決策表：14 語意前綴→ASCII（✅→[OK]/❌→[FAIL]/⚠→[WARN]/📍→[DETECT]
    等，格式 [KEYWORD] 訊息）+ 62 純裝飾直接移除。逐檔 rg -P 定位替換。契約前綴對齊 child-1 已修訂的 §3.4
acceptance:
- '[ ] src/ 全目錄 grep emoji range（1F000-1FFFF+2600-27BF）結果為 0'
- '[ ] 語意前綴依 AC-2 決策表替換為 [KEYWORD] 格式，對齊既有 [SW]/[DIAG]/[ERROR] 慣例'
- '[ ] 契約 2 前綴（[DETECT] 頁面檢測: / [FAIL] <名詞>失敗:）與 child-1 §3.4 字面一致'
- '[ ] npm run build:dev 通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-06-03'
updated: '2026-06-03'
---

# Execution Log

## Task Summary

移除 src/ 全專案 emoji 與 ASCII 前綴替換（85 檔 ~1840 處）

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

全專案 85 檔 ~1840 處 emoji 違反 language-constraints 規則 3。範圍與替換規範詳見父 ticket W1-005 ANA（AC-1 範圍裁定、AC-2 替換決策表）。

### PM 派發前須先拆分（重要，handoff 關鍵指引）

**此 IMP 不可整包派一個代理人**。85 檔 / 1840 處遠超單一代理人限制（PC-042 ~20 tool call + cognitive-load.md >5 檔須拆分）。下一 session PM 第一動作為**拆分為子任務**：

| 建議拆分波 | 範圍 | 命中 |
|-----------|------|------|
| W1-005.2.1 | `src/background/`（最大宗，獨立成波）| 55 檔 ~1200 |
| W1-005.2.2 | `src/popup/` + `src/overview/` + `src/ui/` | ~12 檔 ~96 |
| W1-005.2.3 | `src/core/` + `src/content/` + `src/platform/` + 其餘 | ~16 檔 ~150 |

每子波依 W1-005 ANA AC-2 決策表執行（14 語意前綴→ASCII + 62 純移除），契約 2 前綴對齊 W1-005.1 已修訂的 §3.4。各子波獨立 build:dev 驗證。

### 影響範圍

src/（85 檔，分布見 W1-005 ANA「影響範圍」表）。

### 相關 Error Pattern

PC-042（subagent ~20 tool call 限制）、PC-162（接手者須獨立複驗範圍）。

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: thyme-extension-engineer
**Review Status**: pending