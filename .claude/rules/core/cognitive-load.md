# 認知負擔設計原則

本文件定義程式碼和任務設計中的認知負擔管理原則。

> **核心理念**：降低閱讀者的認知負擔是所有設計的終極目標。

---

## 核心概念

### 什麼是認知負擔？

認知負擔（Cognitive Load）是指閱讀者在理解程式碼時需要**同時記住**的資訊量。

> **Miller's Law**：人類工作記憶一次只能處理約 7±2 個項目。

### 設計目標

```
最小化外在認知負擔，讓閱讀者的認知資源用於
理解真正重要的事情（內在負擔）和建立心智模型（相關負擔）
```

---

## 適用範圍

| 領域 | 應用 |
|------|------|
| 程式碼 | 函式、類別、模組設計 |
| 文件 | 規則、方法論、交接文件 |
| 系統規劃 | 架構設計、流程設計 |
| 任務設計 | Ticket 拆分、代理人派發 |

---

## 量化標準

### 認知負擔指數

```
認知負擔指數 = 變數數 + 分支數 + 巢狀深度 + 依賴數
```

### 閾值表

| 指數 | 評估 | 行動 |
|------|------|------|
| 1-5 | 優良 | 維持 |
| 6-10 | 可接受 | 考慮優化 |
| 11-15 | 需重構 | 建立重構 Ticket |
| > 15 | 必須重構 | 立即處理 |

### 任務拆分閾值

| 條件 | 閾值 | 行動 |
|------|------|------|
| 需追蹤概念數 | > 7 個 | 必須拆分 |
| 需理解檔案數 | > 5 個 | 考慮拆分 |
| 跨架構層級數 | > 2 層 | 必須拆分 |
| 依賴模組數 | > 3 個 | 考慮拆分 |
| 修改檔案數 | > 5 個 | 必須拆分 |
| 目標檔案行數 | > 300 行 | 分析 domain 邊界，考慮拆分檔案 |
| 單一檔案承載 domain 數 | > 2 個 | 必須拆分（DDD 邊界違規） |
| 事件經過處理層數 | > 5 層 | 簡化流程或拆分為獨立處理鏈 |

> **回合耗盡 = 認知負擔過載的具體訊號**：代理人回合耗盡時，不只是「重試」或「PM 代做」，而是系統性訊號——某處的檔案體量或 domain 混合度需要降低。行數超標是症狀，domain 混合是病因。

---

## 3b 派發前閾值（PM 派發檢查門）

> **核心定位**：本章節是 PM 在 TDD Phase 3b（GREEN 實作派發）之前的整合性檢查門，將散佈在三份文件的閾值收斂為單頁三項核心指標，避免 PM 派發過大任務壓垮代理人認知負擔（3b 派發前閾值 ANA 收斂結論，具體 ticket 參工作日誌）。

### 三項核心閾值

| 閾值 | 拆分門檻 | 取得方式 |
|------|---------|---------|
| 功能職責數 | > 2 須拆分 | 數一數 ticket 涵蓋的獨立功能職責（一個職責 = 一個動詞 + 單一目標） |
| 修改檔案數 | > 5 須拆分 | `where.files` 欄位計數，或從 Problem Analysis 影響範圍推算 |
| Context Bundle tokens | > 3000 tokens 須拆分（軟上限）；> 5000 tokens 強制拆分 | claim 後 `ticket track show <id>` 查 PCB 區段字數，或 `wc -c` 估算 |

### 三明示

#### 閾值 1：功能職責數 > 2

**Why**：一個 ticket 涵蓋多重職責時，代理人需同時建立多套心智模型，工作記憶受 Miller's Law 7±2 限制，職責越多越易卡住或產出遺漏。

**Consequence**：派發後常見症狀為（1）代理人 commit 時遺漏部分職責；（2）回合耗盡前只完成一半；（3）跨職責的測試互相干擾，難以定位失敗根因。

**Action**：以「動詞 + 單一目標」描述每個職責；> 2 個動詞時依職責邊界拆分為多個 ticket，於 `spawned_tickets` 互相連結。

#### 閾值 2：修改檔案數 > 5

**Why**：跨檔案修改需代理人保持多檔案的內部一致性（命名、介面、行為），檔案越多一致性維護成本越高，且 Edit 工具的 old_string 唯一性檢查易因相似內容而失敗。

**Consequence**：> 5 檔修改的 ticket 常出現（1）部分檔案修改完整、其他檔案遺漏；（2）跨檔不一致導致測試失敗；（3）代理人需反覆讀取既有內容造成 token 浪費。

**Action**：依 domain 邊界拆分；同 domain 內 > 5 檔仍應評估「批量機械操作」（純重命名/格式化）vs「設計變更」分類，前者可用 inline script 一次處理（PC-069），後者必須拆 ticket。

#### 閾值 3：Context Bundle tokens > 3000

**Why**：Context Bundle 是 claim 時自動載入給代理人的前置脈絡，token 數越大代理人讀取成本越高，且 PCB 過大常代表 ticket 範圍過寬（牽涉多個 source ticket / 多個檔案的歷史脈絡）。

**Consequence**：PCB > 3000 tokens 的 ticket 常見（1）代理人讀完 PCB 已耗 30%+ context window；（2）PCB 中混雜不直接相關的歷史，代理人誤判任務焦點；（3）PCB > 5000 tokens 時代理人在實作前已逼近回合耗盡。

**Action**：> 3000 軟上限：審視 PCB 是否含與本 ticket acceptance 無關的歷史段落，可在 Problem Analysis 加註「PCB 部分段落僅供背景，本 ticket 焦點為 X」；> 5000 強制拆分：依 source ticket 數或檔案 domain 切分為多個 ticket，每個 ticket 限定 2-3 個 source。

### PM 派發前檢查清單

派發 3b 實作 ticket 前，PM 在主線程逐項勾選：

- [ ] 功能職責數 ≤ 2（一個 ticket 一個動詞 + 單一目標，最多兩個高度耦合的副職責）
- [ ] 修改檔案數 ≤ 5（`where.files` 或 Problem Analysis 影響範圍）
- [ ] Context Bundle tokens ≤ 3000（claim 後 `ticket track show` 查驗；> 3000 走豁免 / > 5000 必拆）
- [ ] 已對齊既有閾值（本檔 §量化標準/任務拆分閾值，無矛盾）
- [ ] 若任一項超標，已建立拆分 ticket 並更新 `spawned_tickets`

> **與 §量化標準/任務拆分閾值的關係**：上方 §量化標準 是通用閾值矩陣（適用程式碼 / 任務 / 文件設計多場景）；本章節是 **3b 派發前單頁檢查門**，從中精選三項最關鍵指標供 PM 派發決策即時對照。詳細閾值矩陣（含複雜度分級、並行派發判斷、程式碼品質）見 `@.claude/skills/cognitive-load-assessment/thresholds.md`。

### 跨進程同步修復豁免條款

> **理論依據**：跨進程同步修復豁免 ANA 收斂結論與其首例案例（具體 ticket 參工作日誌）。本條款是 §三項核心閾值「功能職責數 > 2 須拆分」的特例豁免，**不取代**通用閾值，僅在符合全部 5 特徵時放寬。

**適用情境**：當 IMP 滿足以下「跨進程同步修復」全部特徵時，可豁免「功能職責數 > 2 須拆分」閾值：

| 特徵 | 判定 |
|------|------|
| 跨進程數 | 2-3 個進程（如 CLI lib + Hook，或 validator + checker + linter） |
| 邏輯同步性 | 各進程實作同一根因修復，同步修法防止漂移（ARCH-020 教訓） |
| 修改檔案數 | ≤ 5（含測試檔，超過走通用閾值） |
| 預估執行時長 | ≤ 30 分鐘（PM 依過往案例估算，超過走通用閾值） |
| 根因數 | ≤ 2（多根因仍應拆 ANA → 多 IMP） |

**Why**：跨進程同步修復的拆分會破壞「同 bug 同 commit」一致性，導致兩 IMP 修法漂移風險（A 修了但 B 漏修，或 A 用方案 X、B 用方案 Y）；且 PM overhead（4 次 claim/complete）與測試重複設定成本高於合併執行。

**Consequence**：誤套用通用拆分閾值會：

1. 增加 ARCH-020 模式的修法漂移風險（首例已見：validator 漏修 hook 導致 3 天後 regression 重爆，同步修復才終結）
2. 拆 4 ticket 後 PM overhead = 4× claim + 4× complete + 4× commit + 4× 驗收
3. 測試難以共用 fixture（每 ticket 重建測試環境）

**Action**：豁免時必須於 IMP Problem Analysis 顯性記錄：

- 跨進程清單（哪些進程同步修復）
- 邏輯同步性說明（同根因 / 同修法）
- ARCH-020 或 PC-110 引用（理論依據）
- 預估執行時長（與基線案例對照）

**首筆基線案例**：28 分 37 秒 / 3 檔 / 12 測試 / 全綠 / 2 進程 / 2 根因（具體 ticket 參工作日誌）

**邊界澄清**：本豁免不適用於以下情境（仍走通用閾值）：

| 情境 | 處理方式 |
|------|---------|
| 跨進程 > 3 個 | 認知負擔超 7±2 上限，仍須拆分 |
| 跨 domain 修復（不同職責領域） | 合併會混淆責任，仍須拆分 |
| 多根因（> 2） | 先拆 ANA → 多 IMP，避免單 IMP 過載 |
| PM 估時 > 30 分鐘 | 超過經驗豁免上限，仍須拆分 |

> **後續觀測**：30 天觀測 ANA（具體 ticket 參工作日誌）將追蹤本豁免條款命中率與漂移率；若觀測發現命中後常超時，啟動豁免條款修訂。

---

## 監測校準框架：設計性偏差 vs 失效性偏差

> **核心理念**：監測類 ANA 設計 acceptance 時，必須預先區分「設計性偏差」與「失效性偏差」兩種不同性質的偏差，避免以混合主指標驅動架構決策。

### 概念定義

**Why**：監測類 ANA 的偏差訊號天然混合「設計意圖」與「真正失效」兩種性質；不區分會讓主指標被「保守設計」dominate，掩蓋真正的漏網訊號。

**Consequence**：混合主指標誤觸發架構複雜化——保守規則被誤判為「不準」，驅動建構更複雜的分級系統，但實際漏網率可能為零，等於用錯誤訊號驅動重建（歷史案例參工作日誌）。

**Action**：監測類 ANA 設計與分析時，必須將觀測偏差依下表三類分流，分別計算各自比率而非單一混合主指標。

| 偏差類別 | 定義 | 與系統目標的相關性 | 是否視為失效訊號 |
|---------|------|------------------|----------------|
| **設計性偏差**（design-by-default） | 系統規則「保守端」主動觸發保護的案例 | 與保護目標正相關（保守是設計意圖） | **否**（規則正在發揮作用） |
| **失效性偏差**（failure-mode） | 系統規則應觸發但未觸發的案例（漏網） | 與保護目標負相關（規則漏失） | **是**（真正失效訊號） |
| 落實偏差（execution-drift） | 規則已觸發但被執行者忽略 | 屬執行落差，與規則設計無關 | 否（屬監測規則執行落實的獨立訊號） |

### 失效偏差率（核心指標）

```
失效偏差率 = under-restricted / total
           = 應觸發保護但未觸發的案例數 / 總樣本數
```

**取代原則**：監測類 ANA 的命中率 acceptance 應以「失效偏差率」取代「混合偏差率」作為核心指標。

**Action**：

| 步驟 | 操作 |
|------|------|
| 1. 採樣後分類 | 將每個樣本判別為 over-restricted / under-restricted / 對齊三類 |
| 2. 套用公式 | 失效偏差率 = under-restricted 樣本數 / 總樣本數 |
| 3. 與閾值對照 | acceptance 預先聲明的閾值（如 30%）判定觸發 / 結案 |
| 4. 設計性偏差獨立記錄 | over-restricted 數量列入觀測但不視為失效訊號 |

| 指標 | 取代前（混合偏差率） | 取代後（失效偏差率） |
|------|------------------|------------------|
| 計算範圍 | over-restricted + under-restricted | 僅 under-restricted |
| 是否反映「不漏網」 | 否（被 over-restricted dominate） | 是（直接量化漏網率） |
| 觸發架構複雜化的風險 | 高（設計性偏差被誤判為失效） | 低（僅真正漏網才觸發） |

### 適用情境

**判別準則**：判斷監測對象是否存在「保守 vs 寬鬆」對立軸——若系統規則的設計權衡含「寧可保守觸發、避免漏網」的取捨，套用本框架；若監測目標無此對立（如效能 / 覆蓋率），混合主指標即合理。

| 情境 | 是否應用本框架 | 辨識訊號 |
|------|-------------|---------|
| 監測類 ANA 設計 acceptance（如三層攔截命中率） | 是 | 規則有「應觸發 vs 不應觸發」二分判別 |
| 後驗校準現有規則的有效性 | 是 | 規則已上線，需驗證實際漏網率 |
| 評估保守規則是否應放寬 | 是 | 設計意圖明顯偏保守，需驗證放寬風險 |
| 純效能監測（無「保守 vs 寬鬆」對立） | 否 | 指標單向（越快越好 / 越省越好） |
| 程式碼覆蓋率統計 | 否 | 無「應 cover vs 不應 cover」二分 |

### 預先聲明要求

**Why**：缺判別方式則失效偏差率回退為混合主指標——監測者沒有規則可依、只能事後憑感覺裁定哪些算「應觸發」，結果與「不區分」等同。三項要求互為前提：缺保護目標則無法判別「應 vs 不應」；缺判別方式則無從計算 under-restricted；缺閾值則無觸發 / 結案邊界。

**Consequence**：若僅有第 3 項閾值而缺前兩項，acceptance 仍會被新一輪「保守設計」dominate；歷史案例即此後果——觸發 acceptance 重啟原設計，但實際漏網率為零（具體 ticket 參工作日誌）。

**Action**：監測類 ANA 設計 acceptance 時，**必須在 acceptance 條款中**：

1. 聲明保護目標（如「防止高認知負擔任務被低估」）
2. 區分設計性偏差與失效性偏差的判別方式（如「ANA 強制 full 為設計性，IMP/DOC 簡化但 idx > 15 為失效性」）
3. 失效偏差率閾值（如「> 30% 觸發重啟原設計，≤ 30% 結案」）

**反例**（混合主指標 acceptance，禁用，歷史案例參工作日誌）：

```
[ ] 偏差率 > 30% 則重啟設計（混合 over-restricted + under-restricted）
```

**正例**（失效偏差率 acceptance）：

```
[ ] 失效偏差率（under-restricted / total）> 30% 則重啟設計
[ ] 設計性偏差（over-restricted）獨立記錄，不視為失效訊號
```

### 案例來源

| 來源 | 內容 |
|------|------|
| 三層攔截命中率後驗校準 ANA（具體 ticket 參工作日誌） | N=30 採樣，主指標 86.7% 被 over-restricted=10/30 dominate，under-restricted=0/30 是真正訊號 |
| 失效偏差率重評 ANA（具體 ticket 參工作日誌） | 以新框架重評，得 0% ≤ 30% 結案，避免實作原 claim hook 自動分級設計 |
| PC-141（memory） | 監測 ANA 信號分解教訓初次固化 |

### 與三層攔截閾值的關係

本框架與本檔「§3b 派發前閾值（PM 派發檢查門）」併陳：

| 層次 | 用途 |
|------|------|
| §3b 派發前閾值 | **執行層**：PM 派發任務前的拆分檢查 |
| §監測校準框架（本章節） | **校準層**：監測 §3b 閾值是否有效的方法論 |

未來監測類 ANA 設計新規則時，必須先依本框架設計 acceptance，避免重蹈混合主指標誤觸發架構複雜化的覆轍（歷史案例參工作日誌）。

---

## 自我檢查清單

### 函式層級

- [ ] 函式長度 <= 15 行（理想 5-10 行）
- [ ] 參數數量 <= 3
- [ ] 巢狀深度 <= 3
- [ ] 區域變數 < 5
- [ ] 函式名稱是動詞片語
- [ ] 函式只做一件事
- [ ] 無隱藏副作用

### 類別層級

- [ ] 類別方法數 <= 10
- [ ] 公開方法數 <= 5
- [ ] 類別只代表一個概念
- [ ] 類別名稱是名詞

### 命名層級

- [ ] 無縮寫或僅使用通用縮寫
- [ ] 變數名稱是名詞
- [ ] 函式名稱是動詞
- [ ] 布林變數是 is/has/can 開頭
- [ ] 不使用 data/info/value 等模糊詞

### 任務層級

- [ ] 能用「動詞 + 單一目標」描述
- [ ] 5 分鐘後還能記住任務目標
- [ ] 不需要同時記住超過 7 個概念

---

## 降低認知負擔的原則

### 原則 1：單一責任

> 一個函式只做一件事，讓讀者只需理解一個概念。

| 函式描述 | 責任數 | 評估 |
|---------|--------|------|
| 「計算總價」 | 1 | 符合 |
| 「計算總價並發送通知」 | 2 | 違反 |
| 「驗證、計算、儲存」 | 3 | 嚴重違反 |

### 原則 2：自說明命名

> 名稱本身就是文件，讓讀者不需要額外資訊。

| 命名 | 自說明程度 | 改善 |
|------|-----------|------|
| `calc()` | 低 | `calculateMonthlyRevenue()` |
| `isValid` | 中 | `isEmailFormatValid` |
| `processUserRegistration()` | 高 | 維持 |

### 原則 3：避免副作用

> 函式的行為應該可預測，讓讀者不需要追蹤隱藏狀態。

### 原則 4：扁平化結構

> 減少巢狀，讓讀者不需要追蹤深層上下文。

| 層級 | 評估 | 處理 |
|------|------|------|
| 1-2 | 優良 | 維持 |
| 3 | 可接受 | 考慮重構 |
| 4+ | 需重構 | 使用 Guard Clause 或提取函式 |

### 原則 5：資訊就近原則

> 相關的資訊應該放在一起，讓讀者不需要跳轉尋找。

---

## 與決策樹的關係

認知負擔是決策樹進行任務拆分的核心依據：

```
任務進入
    |
    v
評估認知負擔指數
    |
    +-- <= 10 --> 直接派發
    +-- > 10 --> 必須拆分後再派發
```

---

## 相關文件

- .claude/pm-rules/decision-tree.md - 主線程決策樹
- .claude/pm-rules/task-splitting.md - 任務拆分指南
- @.claude/skills/cognitive-load-assessment/thresholds.md - 詳細閾值
- @.claude/methodologies/cognitive-load-design-methodology.md - 完整方法論

---

**Last Updated**: 2026-05-13
**Version**: 1.2.1 — basil-writing-critic Layer 2 審查補修：概念定義 / 取代原則 / 適用情境 / 預先聲明要求四段補三明示（Why / Consequence / Action）；補辨識訊號欄位降低判別摩擦（W10-111 follow-up）

**Version**: 1.2.0 — 新增「監測校準框架：設計性偏差 vs 失效性偏差」章節：定義失效偏差率核心指標 + 預先聲明 acceptance 要求 + 三類偏差判別表（W10-111 重評 W10-096 框架缺陷落地，固化 PC-141 教訓）

**Version**: 1.1.0 — 新增「3b 派發前閾值（PM 派發檢查門）」章節：三項核心閾值（功能職責數 > 2 / 修改檔案數 > 5 / Context Bundle tokens > 3000）+ Why/Consequence/Action 三明示 + PM 派發前檢查清單（3b 派發前閾值 ANA 收斂落地）

**Version**: 1.0.0 — 初始建立
